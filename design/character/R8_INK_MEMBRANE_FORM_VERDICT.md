# R8 — 역잉크 분리막의 **조형** 판정 (design-character)

2026-09-03 · `design-art` `PALETTE_SPEC.md` §22~§26이 넘긴 판정.
**프로덕션 `.cs`/`.asset` 0줄 수정. 앱 실행·빌드 안 함.**

검산 도구 `design/character/verify/r8_membrane.py` · 출력 `r8_membrane.out.txt`
```bash
python3 design/character/verify/r8_membrane.py     # A~H, 교정·양성대조 내장
```
교정 통과: `CR(#000,#FFF) = 21.0000` · `CR(#808080,#808080) = 1.0000` ·
프리팹 계수 2방법 일치(파서 11 / `grep` 11) · 양성 대조(없는 클래스 ID) 0.

---

## 0. 세 줄

1. **막을 반대하지 않는다.** 조형으로는 못 푸는 문제라는 것을 독립으로 확인했다 —
   **대비는 기하와 무관하다.** 획을 굵게 하든 실루엣을 바꾸든 `CR`은 1도 안 움직인다(§5).
2. **그러나 「전 배율 × 전 플랫폼 × 1.000 물리픽셀 × 획마다 한 겹」은 승인하지 않는다.**
   ① 정렬층이 **구조적으로 없다**(§2) ② **Windows 100%의 출하 기본 배율에서 관절 규칙을
   0.5% 차이로 어긴다**(§3) ③ 배경이 잉크에 가까운 17%의 경우에 그림이 **다른 그림이 된다**(§4).
3. **미확인을 채웠다** — 본체 선 개수는 **11개**다(프리팹 실측, §1). 그리고
   `[렌더품질]`의 `LineRenderer N개`는 **씬 전체 계수라 이 답의 출처가 될 수 없다**(§1-2).

---

## 1. 【미확인 해소】 몸의 선 개수 = **11**

`design-art` §22-6: *"몸의 선 개수는 프리팹에 굽는 값이라 소스에서 세지 못했다 — 미확인."*
→ **프리팹을 직접 파싱해서 셌다.** `Assets/_Project/Prefabs/Stickman.prefab` (2026-09-02 11:26 굽기)

| sortingOrder | 이름 | loop | 점 수 | 무엇인가 |
|---:|---|:--:|---:|---|
| 0 | `LeftLeg` `LeftLegLower` `RightLeg` `RightLegLower` | 0 | 5 | 다리 4 (마디마다 별개 렌더러) |
| 1 | `Torso` | 0 | 2 | 몸통 1 |
| 2 | `LeftArm` `LeftArmLower` `RightArm` `RightArmLower` | 0 | 5 | 팔 4 |
| 3 | `HeadFill` | **1** | 24 | 머리 채움(폭 1.0909R 과채움) |
| 4 | `HeadOutline` | **1** | 24 | 머리 링(폭 = 링 두께) |

**LineRenderer 11개 / GameObject 13개**(+ 루트 `Stickman`, + `Head` 허브).
두 방법이 일치한다 — 내 YAML 파서 11, `grep -c '^LineRenderer:'` 11.
양성 대조(존재하지 않는 클래스 ID)는 0을 냈다.

부수 확인: **눈 오브젝트가 프리팹에 0개다.** 결함이 아니라 의도다 —
`SceneBootstrapper.cs:1263`의 `if (BakeEyes)`가 `false`로 닫혀 있고, 좌표·상수는
사용자 지시("코드는 남겨")대로 보존돼 있다. **「얼굴 없음」 잠금이 실물에서도 참이다.**

### 1-1. 막을 걸면 선이 몇 개 느는가 — **+11이 아니라 +10이다**

`HeadFill`과 `HeadOutline`은 **같은 잉크색**이고(`SceneBootstrapper.cs:1237~1240`이 같은 `outline`
변수를 둘 다에 넘긴다 — `CHARACTER_FORM_SPEC` 21-1), 머리의 바깥 경계는 `HeadOutline`의 바깥
가장자리 하나뿐이다(`R + W/2`). **머리에 막을 두 겹 걸면 안쪽 한 겹은 정의상 안 보인다.**

> **머리는 막 1개** — `HeadOutline`을 폭 `+2 물리픽셀`로 복제한 링 하나.
> `HeadFill`에는 막을 걸지 마라(내부다).

⇒ 본체 막 = **10개**(몸통 1 + 팔다리 8 + 머리 1). 그리고 이 10개는 §2 때문에 **한 층에 몰아야 한다.**

### 1-2. ★ `[렌더품질]` 줄로는 이 수를 못 센다 — `design-art`의 확인 방법 정정

`design-art` §22-6: *"`[렌더품질]` 줄의 `선 N개`가 그 수를 직접 찍는다. 실기 캡처 때 그 줄로 확정할 것."*

**그 줄은 몸의 선이 아니라 씬 전체를 센다.**
`Platform/StrokeWidthDiagnostics.Measure()`는
`Object.FindObjectsByType<LineRenderer>(FindObjectsInactive.Exclude, …)` 로 **씬 전량**을 훑고,
`Describe()`가 그 총수를 `LineRenderer {r.LineCount}개`로 찍는다.
거기에는 **장비(최대 수십 조각) · 펫 · FX · 그라피티 · 각종 위젯**이 전부 들어간다.
그 줄로 확정하면 **몸의 선 수를 크게 과대평가**한다. 확정 출처는 **프리팹**이다(위 표).

*(이 계측기 자체는 정상이다 — 자기 문서가 "씬 전체 계측"이라고 이미 적어 뒀다. 잘못은 그것을
"몸의 선 개수"로 읽은 쪽에 있다.)*

### 1-3. 막을 걸 후보의 선 수 (본체 밖, 참고)

| 대상 | 동시 최대 선 수 | 출처 |
|---|---:|---|
| 발자국 | **12** | `CharacterFxRenderer.FootprintCapacity = 12` × 1선 |
| 반짝임 / 먼지 | 4 / 4 | `SparkleCapacity=2` `DustCapacity=2` × 2선 |
| 물방울 / 나뭇잎 | 3 / 6 | `BubbleCapacity=3`×1 / `LeafCapacity=3`×2 |
| 착지 먼지(FX 「없음」에서도 뜬다) | **5** | `landingDustPuffCount: 5` (`DefaultStickConfig.asset:354`) |
| 리틀스틱메이트 | **6** | `CharacterPetRenderer` `BuildMini()` |

FX 슬롯은 한 번에 하나이므로 FX 최악은 발자국 12. **본체 10 + 발자국 12 + 착지먼지 5 + 펫 6 = 33**이
막의 상한이다(장비 제외 — 장비는 §22-4 M3로 이번 대상이 아니다).

---

## 2. 【판정 1 · 반려】 `막 정렬층 = 그 획 바로 뒤`는 **본체에서 구현 불가능하다**

`design-art` §22-3의 처방 네 줄 중 마지막 줄이 성립하지 않는다.

**본체 잉크의 sortingOrder는 `0, 1, 2, 3, 4` — 빈틈 없는 연속 정수다.**
그러니 `order − 1`은 **전부 다른 잉크가 이미 쓰고 있다**:

| 막 대상 | 잉크 order | 막이 가야 할 order | 그 자리에 이미 있는 것 |
|---|---:|---:|---|
| 다리 4 | 0 | **−1** | `AccessoryShapeBuilder.SortBack = -1` (망토·날개·배낭·판초) |
| 몸통 | 1 | **0** | **다리 잉크** |
| 팔 4 | 2 | **1** | **몸통 잉크** |
| `HeadFill` | 3 | **2** | **팔 잉크** |
| `HeadOutline` | 4 | **3** | **`HeadFill` 잉크** |

검산 출력: `잉크 정렬층 = [0, 1, 2, 3, 4] → 'order-1'이 비어 있는가: []` (**한 자리도 없다**).
그 아래도 `−1`(망토) · `−2`(`SortFootprint`)로 차 있다.
위쪽도 마찬가지다 — `5`는 **머리카락 채움**(`SortHair − 1`)과 **눈**(현재 `BakeEyes = false`로 안 굽지만
좌표·상수가 살아 있다)이 함께 예약한 자리다(`SceneBootstrapper.cs:1248~1268`).
**정렬 정수는 위아래 양쪽으로 빈틈이 없다.**

### 2-1. 실제로 무엇이 화면에 나오는가

같은 sortingOrder 안의 순서는 Unity가 **거리/렌더 순서로 임의 결정**한다. 즉 팔의 막(역잉크)이
몸통 잉크 **위**로 올라올 수 있고, 그러면 **어깨를 가로지르는 1px 역색 선**이 몸통 한가운데 그어진다.
프레임마다 바뀔 수도 있다(깜빡임). 이건 색 문제가 아니라 **조형 사고**다.

### 2-2. 처방 — **본체 전체에 공용 막 층 1개.** 그리고 그게 조형적으로도 더 낫다

> **막 = 모든 본체 잉크보다 아래인 층 **하나**. 획마다 따로 두지 않는다.**

이유는 성능이 아니라 그림이다.

- **내부 테두리가 안 생긴다.** 팔이 몸통 위를 지날 때, 팔의 막이 몸통 잉크 위로 올라가면
  팔 둘레에 역색 윤곽이 생겨 **팔이 몸에서 오려낸 스티커처럼** 보인다. 참고 이미지(@alanbecker)의
  스틱맨은 마디가 만나는 곳에서 **그냥 합쳐진다**(`CHARACTER_FORM_SPEC` 1-4 "taper 없음"과 같은 계열).
- **★ 목이 생기지 않는다.** 우리 머리는 척추 접선 위에 **정확히 얹혀 있고 목이 없다**(1-3절).
  머리 막이 몸통 잉크 위로 올라가면 머리와 몸통 사이에 **1px 역색 띠 = 없던 목**이 그어진다.
  공용 층이면 그 자리는 몸통 잉크가 덮으므로 아무 일도 안 일어난다.
- **어깨·고관절에서 뭉치지 않는다.** 공용 층에서는 팔 막·몸통 막이 **같은 색이라 그냥 합쳐진다.**
  검산: 어깨에서 팔 막의 반경 `ρ_arm + 1px`가 몸통 막의 반경 `ρ_torso + 1px` 안에 들어간다
  (`ρ_arm < ρ_torso`는 설계상 항상 참 — 팔 0.1045 < 몸통 0.11495). **돌기 0.**
  고관절은 `ρ_leg > ρ_torso`라 막이 몸통 밖으로 나오지만, **그 단차는 잉크에 이미 있는 것**이고
  막이 그대로 따라갈 뿐이다(새 형태 아님).

### 2-3. ★ 그러나 그 층이 **없다** — 이건 "새 상수 0개"가 아니다

`−1`은 망토, `−2`는 발자국. **막을 놓을 빈 정수가 본체 아래에 없다.** 셋 중 하나가 필요하다.

| 안 | 내용 | 비용 |
|---|---|---|
| **L1** | 본체 잉크를 **짝수로 재번호**(0,2,4,6,8) + 막은 그 아래 홀수 | 프리팹 재굽기 + `"캐릭터 획(0~5)"`이라고 적힌 주석·상수 **최소 5개 파일**(`HardwareReactionRenderer:74` `RunawayRenderer:128` `FocusWatchRenderer:95` `GraffitiRenderer:52` 등)이 전부 낡는다 |
| **L2** | 공용 막 층 하나를 **`−3`**에 두고, 발자국 막은 `−4` | 정수 2칸만 새로 쓴다. **가장 싸다.** 대신 막이 망토(−1)·발자국(−2)보다 **아래**로 가서, 망토 위에 선 몸은 막을 못 받는다 — 다만 망토는 **우리가 칠한 아트 대역 면**이라 배경을 아는 자리다(§22-2). **손실 0** |
| **L3** | Unity **Sorting Layer**(제2 축)를 새로 판다 | 프로젝트 설정 변경. 가장 깨끗하지만 전역 상태다 |

> **내 권고는 L2**다. 조형 손실이 0이고(막이 필요한 자리는 "우리가 안 칠한 배경" 위뿐이다),
> 새 정수 2개면 끝나며, 기존 `0~5` 주석을 하나도 안 건드린다.
> **판정과 배정은 리더 몫이다** — 나는 조형 손익만 잰다.

---

## 3. 【판정 2 · 조건부】 관절 — **막은 발(足) 실패와 같은 계열의 위험을 다시 켠다**

이게 이번 판정에서 가장 무거운 항목이다. `design-art`는 이 축을 재지 않았다(잴 수 없는 축이다).

### 3-1. 전제 — 막을 **같은 폴리라인의 굵은 복사본**으로 그린다는 가정

"새 상수 0개 · 획 바로 뒤"라는 처방이 성립하는 유일한 구현은 **같은 점 배열에 폭 `W + 2px`짜리
LineRenderer 하나를 더 깔기**다. 아래 숫자는 **전부 그 가정 위에서만** 유효하다.
다른 구현(셰이더 아웃라인, 별도 패스, 스텐실)을 고르면 **이 절을 처음부터 다시 재야 한다.**

### 3-2. 규칙 — 이 저장소가 이미 쓰는 것 그대로

`CHARACTER_FORM_SPEC` 4-1의 규칙 B/C: 두께 `W`(반폭 `ρ`)인 폴리라인이 곡률반경 `r`인 원호를
`Δφ` 간격으로 표본하면 **안쪽 오프셋이 자기교차하지 않을 조건은 `r ≥ ρ / cos(Δφ/2)`**.
`LimbCurveRenderer`의 필렛 원호는 `ArcSamplesPerHalf = 4`라 반호를 3등분한다 → `Δφ = θ/6`.

**막은 `ρ`를 1 물리픽셀 늘린다. 점은 그대로다.** 그래서 요구량만 오르고 여유는 줄어든다.

### 3-3. 실측 — 겹침 깊이 `δ = ρ_막 − r·cos(Δφ/2)` (양수 = 자기교차, 단위 물리픽셀)

최악 자세 두 개(`FORM_SPEC` 4-6): **다리 = 착지웅크림 앞무릎 126°** / **팔 = 주위살피기 122°**.

| 배율 | macOS Retina · Win200% |  | **Windows 100%** |  | 굽기근사 846pt × Win100% |  |
|---:|---:|---:|---:|---:|---:|---:|
| | 다리 δ | 팔 δ | 다리 δ | 팔 δ | 다리 δ | 팔 δ |
| 0.35 | **+0.288** | **+0.572** | **+0.644** | **+0.786** | **+0.832** | **+0.954** |
| 0.50 | −0.309 | −0.331 | **+0.346** | **+0.334** | **+0.436** | **+0.506** |
| 0.60 | −0.571 | −0.598 | **+0.215** | **+0.201** | **+0.323** | **+0.312** |
| **0.75 (출하 기본)** | −0.963 | −0.997 | **+0.018** | **+0.002** | **+0.154** | **+0.140** |
| 1.00 | −1.618 | −1.663 | −0.309 | −0.331 | −0.128 | −0.147 |

**막 1.000 물리픽셀이 관절 규칙과 양립하는 최소 배율**(이분 탐색):

| 화면 | 다리 | 팔 |
|---|---:|---:|
| macOS Retina / Win 200% | **s ≥ 0.3872** | **s ≥ 0.4324** |
| **Windows 100%** | **s ≥ 0.7640** | **s ≥ 0.7511** |
| 굽기근사 846pt × Win100% | s ≥ 0.8869 | s ≥ 0.8719 |

### 3-4. 읽는 법 — 과장하지 않는다

- **Windows 100%의 출하 기본 배율 0.75에서 팔의 여유는 `+0.002 물리픽셀`이다.**
  발 기록(`States/LimbCurveRenderer.cs` (C2))이 적은 그 문장이 그대로 재현된다 —
  *"`|bias|=1에서 여유가 정확히 0`… 즉 발은 거의 항상 **자기교차 경계**에서 그려졌다."*
  **같은 경계에 이번엔 캐릭터 전체가 선다.**
- 다만 **겹침 깊이는 작다**: 0.002 ~ 0.95 물리픽셀. `δ = 0.002px`가 눈에 보이는 노치를 만드는지는
  **오프라인으로 알 수 없다.** 그리고 이 팀은 정확히 그 지점에서 한 번 속았다 —
  *"그 렌더러는 모든 점에 둥근 캡을 찍어 (C2)의 코너 퇴화를 **가려 버렸다**."*
  ⇒ **나는 δ를 「위반/통과」가 아니라 「깊이」로 낸다. 판정은 캡처가 한다**(§7).
- **`numCornerVertices`가 이 퇴화를 어떻게 처리하는지도 미확인**이다.
  본체 프리팹의 코너/캡 정점 수는 프리팹 YAML에서 읽히지 않았다(미확인, §8).

### 3-5. 관절이 뭉툭해지는 것은 **확실하다** (자기교차와 별개, 계산으로 확정)

배경이 잉크에 가까우면 **실루엣은 막이 진다.** 그 실루엣의 관절 바깥 곡률반경은 `r + ρ + 1px`다.

| 배율 | macOS Retina 다리 / 팔 | **Windows 100% 다리 / 팔** |
|---:|---:|---:|
| 0.35 | +21.0% / +22.4% | **+42.0% / +44.8%** |
| 0.60 | +12.8% / +14.7% | +25.6% / +29.4% |
| **0.75** | +10.2% / +11.8% | **+20.5% / +23.5%** |
| 1.00 | +7.7% / +8.8% | +15.4% / +17.7% |

참고: 이 팀은 **"관절이 의도보다 33% 더 뭉툭"**(`FORM_SPEC` 4-3, 배율 1.0의 단위 불일치)을
**고쳐야 할 결함**으로 판정하고 실제로 고쳤다(M3-1 착지 확인 — `LimbCurveRenderer.cs:382` `LocalStrokeWidth`).
**Windows 100%의 +20~45%는 그 결함과 같은 자릿수다.** 방금 고친 것을 막이 되돌린다.

---

## 4. 【판정 3 · 리더 결정 요청】 **막이 있는 그림은 두 개다**

`design-art`가 답하지 않은 질문 — *"막을 몸에 걸면 그림의 인상이 어떻게 바뀌는가."*

### 4-1. 배경이 잉크에 가까우면 **잉크가 사라지고 막만 남는다**

내 독립 계산(교정: `CR(#000,#FFF) = 21.0000`, `CR(#808080,#808080) = 1.0000`):

| 문턱 | 검 잉크가 미달인 그레이 칸 | 흰 잉크 |
|---|---:|---:|
| `CR < 3.0` (비텍스트 하한) | 90 / 256 (35.2%) | 107 / 256 (41.8%) |
| `CR < 2.0` | 64 (25.0%) | 72 (28.1%) |
| **`CR < 1.5` (사실상 소멸)** | **44 (17.2%)** | **45 (17.6%)** |

앞의 두 줄은 `design-art` §22-0과 **바이트 단위로 일치**한다(다른 구현, 같은 답 — 교차검증 통과).
세 번째 줄이 내가 더한 것이고, **조형 판정은 여기서 난다.**

### 4-2. 그 17%에서 화면에 실제로 남는 그림

잉크가 안 보이고 막만 보이는 상태의 캐릭터는 **테두리가 붙은 스틱맨이 아니다.**

| 무엇이 | 잉크가 보일 때 | **잉크가 사라졌을 때** |
|---|---|---|
| 팔 · 몸통 · 다리 | 굵기가 다른 세 획 (0.910 : 1 : 1.091) | **전부 1.000 물리픽셀 한 굵기** — 위계 **0** |
| 획의 성질 | 꽉 찬 획 | **속 빈 두 줄**(간격 = 잉크 폭) |
| 머리 | **꽉 찬 원반** | **링**(속이 빈 원) |
| 22.3% 법칙 | 성립 | **무의미**(모든 획이 1px) |
| 머리 개수 | 4.411 | 아래 표 |

★ **머리가 링이 되는 것이 가장 무겁다.** 「머리 꽉 참(`HeadFill`)」은 2026-09-01 P1에서
**확정해서 넣은 것**이고, 「검정/흰색 프리셋에서만 머리를 링으로」(`FORM_SPEC` 2-4 **M1-B**)는
**리더 판단 대기 상태로 남아 있는 제안**이다. 막은 그 제안을
**"배경이 잉크에 가까울 때만, 자동으로, 아무도 결정하지 않은 채"** 출하한다.
→ **리더 결정 사항. 나는 이걸 조용히 통과시키지 않는다.**

### 4-3. 머리 개수 — 참고에서 **멀어지는 방향**이다

`머리 개수 = 전신 / 머리 잉크 지름`. 막이 붙으면 전신도 머리도 각각 2px씩 커지는데,
**작은 쪽(머리)이 비율로 더 많이 커진다.** 참고 4.80~5.00 / 우리 잉크 4.411.

| 배율 | 잉크 | macOS Retina(막) | **Windows 100%(막)** | 굽기근사 Win100%(막) |
|---:|---:|---:|---:|---:|
| 0.35 | 4.411 | 4.004 | **3.684** | 3.567 |
| 0.60 | 4.411 | 4.162 | 3.946 | 3.883 |
| **0.75** | 4.411 | 4.209 | **4.029** | 3.975 |
| 1.00 | 4.411 | 4.257 | 4.116 | 4.073 |

**전부 아래로 간다.** 브리프의 확정 사항 *"머리를 키우거나 다리를 줄이는 것은 참고에서 멀어지는
방향"*이 그대로 적용된다 — 막은 **머리를 키우는 것과 같은 일을 한다.**
Windows 100% · 0.75에서 `4.411 → 4.029`, **참고 하한 4.80과의 거리가 0.39 → 0.77로 두 배가 된다.**

### 4-4. 팔<몸통<다리 위계와 막의 크기 비교

`LineWidthScale` 문서가 못 박은 설계 의도는 *"팔은 기본보다 아주 약간 얇게, 다리는 아주 약간 굵게"*다.
그 **설계차 전부**와 막의 크기를 나란히 놓으면:

| 배율 | 팔↔다리 설계차(px) — macOS / **Win100%** | 막 총폭 | **막 ÷ 설계차** |
|---:|---:|---:|---:|
| 0.50 | 0.855 / **0.428** | 2.000 | 2.34× / **4.68×** |
| 0.60 | 1.026 / **0.513** | 2.000 | 1.95× / **3.90×** |
| **0.75** | 1.283 / **0.641** | 2.000 | 1.56× / **3.12×** |
| 1.00 | 1.710 / **0.855** | 2.000 | 1.17× / **2.34×** |

> **막 한 겹이 팔↔다리 굵기 설계차 전부보다 크다 — 전 배율 · 전 플랫폼에서.**
> 위계가 죽는 것은 아니다(잉크에는 그대로 남는다). 그러나 **막은 이 그림에서 위계보다 큰 사건**이다.

그리고 「막 포함 굵기」로 읽으면 위계가 실제로 압축된다:

| 배율 | 잉크 팔/다리 | macOS 막포함 (압축률) | **Win100% 막포함 (압축률)** |
|---:|---:|---:|---:|
| 0.60 | 0.8333 | 0.8742 (75.5% 잔존) | **0.8990 (60.6% 잔존)** |
| 0.75 | 0.8333 | 0.8677 (79.4%) | **0.8903 (65.8%)** |
| 1.00 | 0.8333 | 0.8605 (83.7%) | 0.8801 (72.0%) |
| 0.35 | **1.0000**(이미 하한 평탄) | — | — |

### 4-5. ★ `design-art`의 머리 링 수치를 정정한다 — **틀린 방향이 안전한 쪽이 아니다**

§22-6/§22-7의 **머리 링 행만** 재현되지 않는다(다른 행은 전건 일치).

| 배율 | `design-art` 값 | **내 재계산** | 차이 |
|---:|---:|---:|---:|
| 0.35 면적 증가 | +86.8% | **+92.3%** | +6.4% 더 나쁘다 |
| 0.60 | +50.6% | **+53.8%** | +6.4% |
| 0.75 | +40.5% | **+43.1%** | +6.4% |
| 1.00 | +30.4% | **+32.3%** | +6.4% |
| §22-7 Win100%·0.35 「막/링」 | 0.868 | **0.923** | +6.4% |

원인 추적: 그들의 값은 **실효 링 계수 0.0805**에서 나온다(4행 전부 같은 배수 1.0641).
정본은 **0.0756501**이다 — `SceneBootstrapper.cs:181`이 직접 적고 있고,
`FORM_SPEC` 21-4의 M6 검산표(0.35/0.60/0.75/1.00 = **1.083 / 1.857 / 2.321 / 3.095 pt**)와
내 값이 소수점까지 같다. **결론은 안 바뀌고 오히려 강해진다** —
*"머리 링은 배율 0.35에서 막이 획보다 굵어진다"*는 판정은 **더 이른 배율에서 참이 된다.**

정정 후 실측:
**Windows 100% · 배율 0.35에서 링 획은 1.083px, 막은 1.000px.**
머리는 **꽉 찬 원반**이므로 막은 **바깥 한쪽에만** 붙는다(안쪽 한 겹은 `HeadFill`에 묻힌다, §1-1).
따라서 바깥 테두리 2.083px 중 **48.0%가 역잉크**다 —
그 자리에서 머리 테두리는 획이 아니라 **잉크와 역잉크가 반씩인 2색 띠**다.

> ★ 다만 `design-art` §22-7의 문장 **"막이 획보다 굵어진다(0.868)"**은 숫자와 안 맞는다.
> `0.868 < 1`은 *막이 획보다 **얇다***는 뜻이다(한쪽 기준). 양쪽 합 2.000px 기준이라면 참이지만,
> **머리에서는 막이 한쪽에만 붙으므로 그 읽기가 성립하지 않는다.**
> 정정값으로 다시 재도 **막이 링 획을 넘는 배율은 존재하지 않는다** — 하한(1.00pt) 때문에
> 링 획은 Windows 100%에서 **최소 1.000px**로 바닥을 치고, 막과 **정확히 동률**이 되는 것이 한계다
> (굽기근사 화면 · 배율 0.35에서 1.000 : 1.000). **"굵어진다"가 아니라 "같아진다"가 참이다.**
> 판정 방향은 그대로다(가장 위험한 지점은 머리 링이 맞다).

---

## 5. 【판정 4】 막 없이 조형으로 푸는 길 — **없다. 증명한다**

브리프 질문 4의 답이다. 세 후보를 전부 죽였다.

| 후보 | 판정 | 근거 |
|---|---|---|
| **획을 굵게** | **불가능** | `CR`은 두 **휘도**의 함수다. 면적·두께·길이가 식에 없다. 검은 획을 10배 굵게 해도 검은 배경 위 `CR`은 **1.00 그대로**다. 굵기는 *보이는 것을 더 잘 보이게* 할 뿐 *안 보이는 것을 보이게* 하지 못한다 |
| **실루엣을 바꾼다** | **불가능** | 같은 이유. 형태는 `CR`에 안 들어간다 |
| **중간 밝기 잉크 한 톤** | **불가능** | 어떤 단일 톤 `L₀`도 배경 `L = L₀`에서 `CR = 1.00`이다. `4.5826 : 1`은 **두 톤이 동시에 있을 때의 쌍 하한**이지 한 톤의 성질이 아니다(`design-art` §22-0이 이미 자기반증한 지점) |

> **그러므로 「잉크에 인접한 두 번째 톤」은 선택이 아니라 필요조건이다.**
> 조형은 *막을 쓸지*를 정하지 못한다. 조형이 정하는 것은 **막을 어디에 어떻게 두는가**뿐이다.
> 나는 그 범위 안에서만 조건을 단다.

### 5-1. 검토했다가 버린 조형 변형 두 개 (근거를 남긴다)

| 안 | 왜 버렸나 |
|---|---|
| **안쪽 척추선**(막을 획 **위**에 얇게 얹어 실루엣을 안 키운다) | 비율·머리개수·관절 규칙이 **전혀 안 바뀐다**는 장점이 있다. 그러나 **모든 배경에서 항상 보인다** — 획이 언제나 관 모양이 된다. 그리고 Win100%·0.35에서 획이 2.000px이라 1px 척추를 빼면 **양쪽 잉크가 0.5px씩** 남아 잉크가 먼저 죽는다. **막보다 나쁘다** |
| **비대칭 그림자막**(1px 대각 오프셋 복사본) | 실루엣 대칭이 깨지고 **광원 방향**이라는 새 시각 언어가 생긴다. 참고 이미지에 그런 것이 없다(1-4 "taper 없음"과 같은 계열). 그리고 코너 문제는 그대로 남는다 |

---

## 6. 【판정 5】 무엇에 거는가 — **M1에 동의한다. 단 두 곳을 뺀다**

`design-art` M1(= 잉크로 그리는 것 전부)의 근거 *"부분만 걸면 안 건 쪽은 그대로 사라지고
유저는 「안 켜진다」로 신고한다"* 에 **동의한다.** 조형 쪽 근거를 하나 더 보탠다:

> **우리 FX는 색이 아니라 「같은 펜」으로 캐릭터에 묶여 있다.**
> 발자국·먼지·착지먼지는 전부 `ResolveInk()`로 캐릭터 잉크색을 그대로 쓴다
> (`LandingDustRenderer:150` / `CharacterFxRenderer:955`). 그것이 **유일한 소속 표시**다.
> 몸에만 막을 걸면 몸과 FX가 **다른 펜**이 되고, FX는 캐릭터의 것으로 안 읽힌다.
> ⇒ **막은 「펜의 속성」이어야 한다. 부위별 옵션이 아니다.**

### 6-1. 빼야 하는 것 — **우리가 칠한 배경 위의 잉크**

| 대상 | 막 | 왜 |
|---|:--:|---|
| 정보창 **초상화** 미니 피규어 | **걸지 않는다** | 무대가 `PortraitSurface #E9EAE6` — **우리가 칠한 값**이다(§22-2 1행). 배경을 안다 |
| 보관함/상점 **카드 아이콘**(44px) | **걸지 않는다** | 같은 이유. 그리고 카드는 이미 하한 면제라 그림이 다르다 |
| 설정창 **미리보기** | **걸지 않는다** | 같은 이유 |
| `HeadFill` | **걸지 않는다** | 내부다(§1-1). 머리 막은 `HeadOutline` 1개 |

**빼는 이유가 절약이 아니다.** 이 저장소는 이미 *"같은 좌표, 다른 펜, 3.3배 다른 결과"*
(`FORM_SPEC` §22 — 카드 72.5% vs 착용 22.0%)로 **한 캐릭터가 두 그림이 되는 사고**를 겪었다.
**아는 배경 위에 막을 걸면 그 사고를 자발적으로 한 번 더 만든다.**

### 6-2. 걸어야 하는 것

몸통 · 팔 4 · 다리 4 · 머리 링 1 · 발자국 · 착지먼지 · FX 5종 · 리틀스틱메이트.
**= `design-art`의 M1과 같다. 위 4건만 뺀다.**

### 6-3. ★ 리틀스틱메이트에 붙는 조형 비용 — 리더에게 넘긴다

`CharacterPetRenderer.RenderStroke = max(Height × 0.022, MinStrokeWorld)`라
**출하 전 배율(s ≤ 0.977)에서 펫 획은 언제나 2.00pt에 눌려 있다.**
막은 절대 픽셀이므로 **작은 쪽이 비율로 더 많이 낸다.**

| 배율 | 주인 머리개수(잉크→막) | 펫 머리개수(잉크→막) | **주인·펫 격차 변화** |
|---:|---|---|---:|
| macOS 0.75 | 4.411 → 4.209 | 2.910 → 2.748 | −0.041 |
| **Win100% 0.75** | 4.411 → **4.029** | 2.910 → **2.611** | **−0.084** |
| Win100% 0.35 | 4.411 → 3.684 | 2.401 → 2.056 | −0.381 |

R2 §12-1이 못 박은 **"펫은 주인의 축소판이다"**가 막 아래에서 조금씩 어긋난다.
격차 자체는 줄어들지만(둘 다 뭉툭해지므로) **펫이 주인보다 빠르게 뭉툭해진다.**
숫자는 작다 — **막을 반려할 근거는 아니고, 기록해 둘 사실**이다.
(이 항목은 `design-equipment` 소관과 겹친다 → **리더 경유 교차검토 요망.** 나는 직접 지시하지 않는다.)

---

## 7. 실기 캡처로만 확정되는 것 — **목록. 계산으로 대체 금지**

지금 사용자 화면이 잠겨 앱을 못 띄운다. **아래는 전부 「미확정」이고, 위 §3·§4의 숫자는 계산이다.**
`FORM_SPEC` §21의 캡처 절차에 이 항목들을 더해 달라(리더 경유).

| # | 무엇을 | 조건 | 통과 기준(내가 제안하는 것) |
|---|---|---|---|
| **C1** | **관절 코너 퇴화** | **Windows 100% × 배율 0.75 × 착지웅크림(앞무릎 126°) 정지 프레임** — δ = +0.018px, 가장 아슬아슬한 조합 | 무릎 안쪽에서 막이 **끊기거나 부풀지 않는다.** 잉크 픽셀 계수로 무릎 전후 획 폭이 ±0.5px 안 |
| **C2** | 같은 것 · 팔 | Win100% × 0.75 × 주위살피기(122°) — δ = +0.002px | 위와 같음 |
| **C3** | 같은 것 · 최악 | Win100% × **0.35** × 126° — δ = +0.644px | 여기서 깨지면 **저배율 구간에 막을 안 거는 것**이 답이다 |
| **C4** | **AA 후 실효 대비** | 잉크=검, 배경 `#141414` / `#2D2D30` / `#808080` 3종 | 막 색 **순색 픽셀이 막 길이 1px당 1개 이상** 남는가. §17-2의 야구모자 챙이 **2물리픽셀 폭에서 순색 2개만 남긴** 선례가 있다 — **1px는 그 절반이다** |
| **C5** | **막만 남은 그림** | 잉크=검, 배경 `#000000` (CR 1.00) | 캐릭터가 **1px 와이어프레임 + 링 머리**로 읽히는가. 읽힌다면 §4-2는 실증되고 **리더 결정 항목**이 된다 |
| **C6** | **정렬층 사고** | 어깨/고관절/목을 확대 | 몸통을 가로지르는 1px 역색 선이 **없다**(§2-1). L2(공용 층 −3) 적용 전에는 **있을 것으로 예측** |
| **C7** | 머리 링 2색 띠 | Win100% × 0.35 | 링 1.083px + 막 1.000px가 **두 색 띠로 갈라져 보이는가**(§4-5) |
| **C8** | A/B 대조 | 같은 프레임 · 막 On/Off | **막 없는 캡처를 반드시 같은 세션에서** 뜬다. 이게 없으면 "원래 그랬다"와 구분 불가 |

> ★ **오프라인 렌더러로 C1~C3을 판정하지 마라.** 2026-09-01 발 실패의 직접 원인이
> *"모든 점에 둥근 캡을 찍어 코너 퇴화를 가린"* 오프라인 렌더러였다.

---

## 8. 미확인 — 추측으로 메우지 않는다

1. ~~`numCornerVertices` / `numCapVertices`~~ → **해소.** 프리팹 11개 전부
   `numCornerVertices: 8` · `numCapVertices: 8`이다(`grep -c` 11 / 11, 양성 대조 0).
   소스 `SceneBootstrapper.LineCapVertices = 8`과 일치한다.
   ★ **그런데 이것이 안심의 근거가 아니다** — 코너 정점 8개는 꺾임의 **바깥쪽**을 둥글게 깎는 값이고,
   LineRenderer는 **안쪽 오프셋의 자기교차를 풀어 주지 않는다.** 즉 §3의 δ가 만드는 결함은
   **꺾임 안쪽**(무릎/팔꿈치의 오목한 면)에 나온다 — 발 기록이 적은 *"발목에 핀치/노치"*와 같은 자리다.
   **남은 미확인은 「δ = 0.002~0.95px에서 그 핀치가 눈에 보이는가」이고, 그건 캡처만이 답한다(C1~C3).**
2. **막의 구현 형태가 미정이다.** §3 전체가 *"같은 폴리라인의 굵은 복사본"* 가정 위에 있다.
   셰이더/스텐실/별도 패스면 **다시 재야 한다.**
3. **Windows의 실제 `pt/유닛`을 모른다.** 나는 이 맥의 실측 `40.9167 pt/유닛`을 Windows에도
   같게 놓고 DPI 배수만 1/2로 바꿨다. **Windows 창 크기가 다르면 표가 통째로 이동한다**
   (곡선 모양은 같고 배율축만 움직인다 — `FORM_SPEC` 9-2의 선례). 굽기근사(35.25) 열을
   함께 실은 이유가 그것이다. **Windows 실기 로그 `[렌더품질]` 한 줄이면 확정된다.**
4. **`FilletLengthRatio`를 올려 막을 수용할 수 있는가**는 부분적으로만 쟀다.
   sagitta 여유가 배율 0.75에서 8%뿐이라(`sagitta/W = 0.92`, `MaxSagittaPerStrokeWidth = 1.0`)
   Win100%·0.75의 −0.6% 부족은 덮을 수 있어 보이지만 0.60의 −8.5%는 못 덮는다.
   **`MaxSagittaPerStrokeWidth`를 함께 올리면 관절이 뭉툭해져 §3-5와 충돌한다** — 안 쟀다.
5. **알파 1.0 막이 우리 오버레이 창의 알파 합성에서 그대로 나오는지** 확인하지 않았다
   (`opacity`가 창 알파까지 내리는 사례가 `PALETTE_SPEC` §5-4에 있다). 색 담당 소관.
6. **`design-art`의 나머지 수치는 재현했다** — 몸통/다리/팔/FX 면적 증가, Win100% 몸통 0.177→0.354,
   `CR < 3.0` 미달 칸 90/107. **머리 링 행 하나만 어긋난다**(§4-5).

---

## 9. 리더에게 — 결정이 필요한 것 (내 권한 밖)

| # | 결정 | 내 권고 | 왜 내 권한 밖인가 |
|---|---|---|---|
| **CH-1** | **정렬층을 어떻게 낼 것인가**(L1 재번호 / **L2 −3·−4** / L3 새 Sorting Layer) | **L2** | 프로덕션 상수·프리팹 변경. `coder` 배정 규모 |
| **CH-2** | **머리가 배경에 따라 링이 되는 것을 승인하는가**(§4-2) | **명시적으로 결정해야 한다.** 「머리 꽉 참」은 확정 사항이고 M1-B는 대기 중이다 | 확정 사항 변경 |
| **CH-3** | **저배율 구간(Win100% s < 0.76)에서 막을 어떻게 할 것인가** | 세 갈래: ⓐ 그 구간만 막 제외(→ 그 구간은 다시 사라진다) ⓑ 전 구간 적용 후 C3 캡처로 판정 ⓒ `MinCharacterScale`을 올린다(사용자 기능 축소) | 제품 판단 |
| **CH-4** | **캡처 순번** — C1~C8은 **Windows 실기**가 있어야 절반이 확정된다 | Windows 캡처가 없으면 이 판정은 **macOS 절반짜리**다 | 실기 순번은 리더 소유 |
| **CH-5** | `design-art` §22-6/22-7 **머리 링 행 정정**(§4-5) | 정정 반영 | 남의 문서 |
| **CH-6** | `design-art` §22-6의 *"`[렌더품질]` 줄이 몸의 선 수를 찍는다"* **정정**(§1-2) | 정정 반영 — 출처는 프리팹 | 남의 문서 |

---

## 10. 인계 — 상대 산출물의 어느 지점과 맞물리는가

| 상대 | 지점 | 무엇을 |
|---|---|---|
| `design-art` | **§22-3 4번째 줄** `막 정렬층 = 그 획 바로 뒤` | **본체에서 불가능**하다(§2). 공용 층으로 바꿔 달라 |
| `design-art` | **§22-6 머리 링 행 · §22-7 「0.868」** | 실효 링 계수 오류 +6.4%. 정정값 §4-5 |
| `design-art` | **§22-6 마지막 줄**(선 개수 미확인) | **11개로 해소.** 확인 방법도 정정(§1-2) |
| `design-art` | **§22-8 3번**(*"막이 그림을 어떻게 바꾸는가 — design-character 소관"*) | **이 문서 §4가 그 답이다** |
| `design-equipment` | 리틀스틱메이트 비율(§6-3) · 장비 바깥 윤곽(M3) | 펫이 주인보다 빨리 뭉툭해진다. **리더 경유 교차검토 요망** |
| `design-motion` | 최악 자세 두 개(**착지웅크림 앞무릎 126°** / **주위살피기 122°**)가 §3의 구속 조건이다 | `idleAmbientLookElbowDegrees`를 122° → 110°로 내리면 팔 여유가 크게 는다(`FORM_SPEC` 4-4의 기존 제안). **막이 그 제안의 값을 올린다** — 리더 경유 |
| `coder` | 구현 형태가 §3의 전제다 | *"같은 폴리라인의 굵은 복사본"*이 아니면 **§3을 다시 재야 한다.** 형태가 정해지면 알려 달라(리더 경유) |

---

## 11. 플랫폼 영향

| | 내용 |
|---|---|
| **Windows 영향** | **함께 검토했다 — 그리고 이 라운드의 결론은 Windows 쪽이 지배한다.** ① 관절 규칙: macOS Retina는 `s ≥ 0.387`이면 통과하는데 **Windows 100%는 `s ≥ 0.764`가 필요하다** — 출하 기본 0.75가 **그 밖**이다(§3-3). ② 막의 상대 무게가 **정확히 2배**다(막/몸통 0.142 → 0.283 at 0.75). ③ 머리 개수가 4.209 → **4.029**로 더 내려간다. **즉 이 처방은 플랫폼 중립이 아니다** — 같은 상수가 두 플랫폼에서 다른 그림을 만든다. 이 라운드는 `.cs`/`.asset`을 0줄 고쳤고 `#if UNITY_STANDALONE_*` 분기가 0건이라 `Tools/CrossCompile/xcheck.sh` 대상은 없다 |
| **macOS 영향** | **함께 검토했다.** Retina(81.8333 물리px/유닛)에서는 배율 0.50 이상 전 구간이 관절 규칙을 통과하고(§3-3), 막의 상대 무게도 절반이다. **위험 구간은 배율 0.35~0.387 하나**뿐이고 그 구간은 이 문서가 이미 「실루엣 전용」으로 선언한 곳이다(`FORM_SPEC` 9-2). 단 §4(막만 남는 그림)는 **플랫폼 무관**이다 — 대비는 DPI를 안 본다 |
| **모바일(iPad/iPhone)** | **미확인.** 스크린샷 백드롭 모드에서는 배경 텍스처를 우리가 그리므로 §22-2의 "아는 배경"에 가깝다. 그러면 **막이 필요 없을 수도 있다** — 그러나 그건 처방이 플랫폼마다 달라진다는 뜻이고 `design-art` 스스로 그 이유로 배경 샘플링을 기각했다. **판정 보류, 리더 경유 `dev-platform` 확인 필요** |
| **공통** | 이 라운드가 만든 파일은 `design/character/` 아래 `.md` 1 · `.py` 1 · `.txt` 1뿐이다 |

---
---

# R9 — 대안 A′(`numCapVertices = 0`)의 **조형** 판정 (design-character, 2026-09-03)

`perf-doc`의 실제 Metal GPU 렌더(T3·T5·T7)를 받아 낸 판정. **프로덕션 `.cs`/`.asset` 0줄 수정.
Unity 배치모드·앱 실행 안 함.** 검산 도구:

```bash
python3 design/character/verify/r9_form.py     # T7 PNG 독립 재측정(교정 내장)
python3 design/character/verify/r9_reftip.py   # 참고 이미지 말단 캡 판정(합성 대조군 교정 내장)
python3 design/character/verify/r9_body.py     # 프리팹 실좌표로 조립된 몸 전체
```

## 12. 세 줄

1. **각진 말단을 반려한다.** 참고 실측 6/6이 둥근 캡이고(§13-1), 앱 안의 **캐릭터 잉크 렌더러
   15개 전부**가 둥근 캡이며(§13-2), 각지면 **머리 개수가 4.6063 → 4.5016**으로 참고에서
   멀어지고(§13-3), **보행 중 발끝 도달거리가 0.087ρ~0.839ρ로 흔들리며**(§13-4),
   **고관절에 오늘은 없는 실패 모드가 생긴다**(§13-5, 여유 12°).
2. **마디 병합은 채택 권고한다 — 조형을 바꾸지 않는다.** 브리프의 전제(*"지금은 두 선이 겹쳐
   관절이 살짝 두껍다"*)는 **거짓**이다(§14). 그리고 ★ **병합은 캡과 분리된 결정이다** —
   `T5_singlePolyline_elbow.png`는 **`cap = 8`인데 팔꿈치 잉크 노출이 0.00%다**(§14-3).
   **팔꿈치를 고치는 것은 병합이지 `cap = 0`이 아니다.**
3. **각짐 없이 노출 0을 얻는 길이 있다**(§15) — `A + 마디 병합 + 종단 막 원판 4개`.
   렌더러 **11 → 7 + 4 = 11(증가 0)**. 대가는 **정렬층 정수 1칸**(CH-1이 되살아난다).
   ⇒ **진짜 선택지는 「각짐 vs 정렬층 1칸」이다.** 나는 정렬층 1칸을 산다.

## 12-1. 재검증 — `perf-doc` 숫자는 **전건 재현된다** (다른 구현으로 다시 쟀다)

PNG를 파이썬으로 독립 판독(`r9_form.py`). 분류기는 알려진 색으로 먼저 교정하고 통과.

| 항목 | `perf-doc` 보고 | **내 재측정** | |
|---|---:|---:|---|
| 종단 A(cap8) 잉크 노출 | 25.8% | **25.80%** | ✅ |
| 종단 A′(cap0) | 0.0% | **0.00%** | ✅ |
| 팔꿈치 A′(마디 2개) | 0.0% | **0.00%** | ✅ |
| 팔꿈치 A′+병합 | 0.0% | **0.00%** | ✅ |
| 관절 쐐기 cap8 / 깊이 | 0.3% / 98% | **0.294% / 98.39%** | ✅ |
| 관절 쐐기 cap0 / 깊이 | 15.1% / 0% | **15.080% / 0.00%** | ✅ |
| 관절 쐐기 병합 / 깊이 | 3.6% / 87% | **3.616% / 87.36%** | ✅ |

★ 처음 판독에서 0.191% / 14.975% / 3.475%가 나왔다. 원인은 **y축 뒤집힘 1행**이다
(`ReadPixels`는 아래가 y=0, PNG는 위가 y=0). 뒤집어 맞추니 세 자리까지 일치했다.
**「거의 맞는다」로 넘기지 않고 차이의 원인을 찍었다** — 그 1행이 다른 곳에서도 어긋날 수 있다.

**이론 대조도 통과한다**(계산은 내 것):
- 두 렌더러 cap0 쐐기 = 꺾임각/360 = 54/360 = **15.0%** ↔ 실측 15.08%
- cap8 깊이 = `cos(π/16)` = **98.08%**(8정점 캡의 현 사그타) ↔ 실측 98.39%
- 병합 넓이 ↔ 깊이 자기정합: `0.15 × (1 − 0.87²) = 3.65%` ↔ 실측 3.62% **(두 값이 서로를 검증한다)**

**MSAA도 검증했다** — T7·T5·T3의 mode2/3/4 PNG 21장에서 중간색 픽셀 **0개**.
(mode0uv·mode1v 2장은 연속 그라디언트라 중간색이 있는 것이 정상이다.)

**그러나 라벨 하나가 틀렸다.** 브리프 표의 *"팔꿈치 잉크 노출 12.8%"*는 **팔꿈치 값이 아니다** —
`T5_twoSegmentArm_full.png`(**팔 전체 뷰**) 값이고 12.83%다. **팔꿈치 근접은 3.34%**
(`T5_twoSegmentArm_elbow.png`), 손끝 근접은 28.95%(`T5_twoSegmentArm_hand.png`).

## 12-2. ★ 그리고 그 백분율들은 **전신 기준이 아니다** — 근접 크롭 값이다

노출률은 「**그 화면 안**의 잉크 윤곽 ÷ 전체 윤곽」이다. 손끝을 화면에 꽉 채우면 25.8%가 나오고,
같은 결함을 **전신**에서 재면 다른 수가 나온다. 전신을 래스터로 재면(`r9_body.py` §7,
1103×2402, 1px = 0.000761 월드):

```
전신 실루엣 둘레(잉크, cap8) = 6.4466 월드
그중 막이 못 덮는 종단 4캡   = 0.5585 월드  →  둘레의 8.66%
해석 검산 2π(ρ_arm + ρ_leg) = 0.5417 월드  (+3.1%, 1.02배 팽창 마스크 탓)
```

> **A(cap8)+병합이 남기는 무방비 구간은 전신 둘레의 25.8%가 아니라 8.66%다.**
> 그리고 그 8.66%는 **팔·다리·몸통·머리가 아니라 손끝 2 · 발끝 2**뿐이다.

★ 이건 `perf-doc`을 반박하는 것이 아니다. 그들의 수는 그 화면에서 맞다.
**결정에 쓰는 분모가 다를 뿐이고, 조형 판정의 분모는 전신이다.**

## 13. 【판정 6 · 반려】 **각진 말단은 스틱메이트가 아니다** — 근거 다섯

### 13-1. 참고 실측 — **6/6이 둥근 캡, 각진 캡 0건**

`8875f6ce-image.png`(@alanbecker, 3인). 색 마스크 + 정확 EDT + **말단 폭 프로파일**.
판별식: 말단에서 축방향 `d` 지점의 획 폭 `w(d)`.

```
둥근 캡  w(d) = 2√(2ρd − d²)   →  d = 0.5ρ 에서 전폭의 86.6%
각진 캡  w(d) = 2ρ (상수)      →  d = 0.5ρ 에서 전폭의 100.0%
```

**합성 대조군으로 파이프라인을 먼저 교정했다**: 둥근 78.4% / 각진 100.0%
(내 말단 국소화가 1~2px 보수적이라 이론보다 낮게 나온다 — **분리는 21.6%p로 충분**).

| 말단 | ρ(px) | 획폭(px) | `w(.5ρ)/w(1.5ρ)` | 판정 |
|---|---:|---:|---:|---|
| 노랑 오른손 | 17.03 | 34.1 | **82.4%** | 둥근 |
| 노랑 왼손 | 16.49 | 33.0 | **81.1%** | 둥근 |
| 노랑 왼발 | 18.44 | 36.9 | **68.2%** | 둥근 |
| 노랑 오른발 | 19.80 | 39.6 | **73.2%** | 둥근 |
| 파랑 앞손 | 22.80 | 45.6 | **89.7%** | 둥근 |
| 주황 손 | 26.93 | 53.9 | **87.3%** | 둥근 |
| ~~파랑 뒷손~~ | 31.00 | — | 179.0% | **측정 실패**(몸통을 물었다 — ρ가 팔이 아니라 몸통값) |

**7건 중 6건이 유효하고 6건 전부 둥근 캡이다. 측정 실패 1건은 「각짐」으로 세지 않는다.**
이는 `CHARACTER_FORM_SPEC`이 잠근 *"참고 스틱맨의 팔 끝은 순수한 둥근 캡"*의 **독립 재확인**이다.

### 13-1-2. ★ 부수 발견 — **참고 이미지 자체가 「막」을 쓰고 있고, 그 막은 둥근 캡을 감싼다**

노랑 인물에는 **어두운 아웃라인**이 있다(파랑에는 없다). 단면 실측(x=940/960/980):

```
배경 (0,48,96) → 어두운 띠 (5,18,26) 1px → 노랑 (255,197,0) 34px → (12,15,10) 1px → 배경
```

**아웃라인 ≈ 1px / 획 34.1px = 획의 2.9% / 머리 지름(~153px)의 0.65%.**
그리고 **손끝·발끝 크롭에서 그 아웃라인이 둥근 캡을 그대로 돌아 나간다** — 끊기지 않는다.

> ★ **참고가 이미 이 문제를 풀었고, 답은 「말단을 각지게」가 아니라 「막이 둥근 캡을 감싸게」다.**
> 각짐은 「막」의 성질이 아니라 **`LineRenderer`의 `uv.y` 구현 한계**다. 그 한계를 조형에
> 청구하는 것은 순서가 거꾸로다.

**우리 막의 상대 무게**(참고 0.65% 대비):

| 화면 | 배율 | 몸통획 | 막/획 | 머리지름 | **막/머리** | 참고 대비 |
|---|---:|---:|---:|---:|---:|---:|
| Win100 | 0.35 | 2.00px | 50.0% | 7.38px | **13.54%** | **20.8배** |
| Win100 | 0.75 | 3.53px | 28.3% | 15.82px | **6.32%** | **9.7배** |
| Retina | 0.75 | 7.06px | 14.2% | 31.65px | 3.16% | 4.9배 |
| Retina | 1.00 | 9.41px | 10.6% | 42.20px | **2.37%** | **3.6배**(최선) |

**가장 좋은 조건(Retina · 배율 1.0)에서도 참고의 3.6배다.** 막은 참고에 있는 장치지만
**우리 해상도에서는 참고보다 3.6~20.8배 무겁다.** 이건 A/A′ 공통이고 반려 사유가 아니라
**기록해야 할 사실**이다(§4-4의 「막은 위계보다 큰 사건이다」와 같은 계열).

### 13-2. 앱 안에서 **각진 캡은 「소품」의 표식이다** — 본체가 가져가면 그 구분이 무너진다

`AddComponent<LineRenderer>`를 하는 프로덕션 파일 **15개 전수**(양성 대조: 없는 심볼 0건).
**`numCapVertices`를 안 세팅하는 파일 0개** — 전부 명시한다.

| 값 | 어디 |
|---:|---|
| **8** | **본체 프리팹 11선 전부** |
| 6 | 그라피티 — 소스 주석: **`// 스프레이 자국처럼 끝이 둥글게.`** |
| 4 | 장비 · 펫 · 초상화 · FX · 착지먼지 · 도망 · 하드웨어반응 · 톱니 · 할일 · 스트레스 · 창도난 |
| 3 | 창 충돌 |
| 2 | 활 받침대 · 시위 · 충격 광선 |
| **0** | **활의 활대 · 재어진 화살 · 날아가는 화살** ← **3건뿐이고 전부 「도구/소품」이다** |

> **이 저장소의 펜은 이미 「캐릭터 획 = 둥근 / 도구 = 각진」으로 갈라져 있다.**
> A′는 본체를 **도구 쪽으로** 옮긴다. 그리고 `GraffitiRenderer.cs:221`의 명시적 조형 결정을
> **조용히 뒤집는다**(막을 「펜의 속성」으로 거는 §6의 결론상 FX·펫에도 같이 걸려야 하므로).

### 13-3. 머리 개수 — **4.6063 → 4.5016.** 배율·플랫폼 무관

프리팹 실좌표로 계산(`r9_body.py` §2). 먼저 **자를 교정**한다:

```
몸통획 0.086212 × 40.9167 pt/유닛 = 3.528 pt   ↔ FORM_SPEC 0.75행 3.527pt   ✅
몸통획 ÷ 머리 잉크 지름 = 0.086212 / 0.386738 = 22.29%  ↔ 팀 확정 22.3% ±0.2%  ✅
위계 팔:몸통:다리 = 0.9091 : 1.0000 : 1.0909                                  ✅
```

| 배율 | 잉크 높이 cap8 | cap0 | 차이 | **머리개수 cap8** | **cap0** | Δ |
|---:|---:|---:|---:|---:|---:|---:|
| 0.35 | 0.833822 | 0.812783 | −2.52% | 4.6201 | 4.5035 | −0.1166 |
| 0.50~1.00 | — | — | **−2.27%** | **4.6063** | **4.5016** | **−0.1047** |

**Win100%와 Retina가 소수점까지 같다**(캡은 월드 기하라 DPI를 안 본다).

> 참고 대역 **4.80~5.00** 하한까지의 거리: **0.194 → 0.298 (+54.0%).**
> 브리프의 확정 사항 *"머리를 키우거나 다리를 줄이는 것은 참고에서 멀어지는 방향"*이
> 그대로 적용된다. **각진 캡은 다리를 줄이는 것과 같은 일을 한다.**
> (잉크 면적도 −3.08%.)

★ **이 손실은 밝은 배경에서 100% 발생하고, 그 자리에서 막은 아무것도 안 보인다.**
막이 이기는 배경은 §4-1 실측으로 **17.2%**뿐이다. ⇒ **A′는 100%에서 대가를 내고 17%를 산다.**

### 13-4. ★ 각진 캡은 **회전 불변이 아니다** — 걸을 때 발끝이 숨을 쉰다

둥근 캡의 실루엣 극점은 방향과 무관하게 관절점에서 정확히 `ρ`다. 각진 캡은
`ρ·|sin ψ|`(ψ = 획 축과 재는 방향의 각)라 **마디가 돌면 도달거리가 변한다.**
보행 키표(`LegHipKeys`/`LegKneeKeys`, 프로덕션 상수)로 1주기 16표본:

| | 발끝 도달거리(ρ 배수) |
|---|---|
| 둥근 캡 | **항상 1.000** |
| 각진 캡 | **0.087 ~ 0.839** (진폭 **0.752 ρ**, 걸음당 2회) |

| 화면 | 배율 | ρ_leg | 발끝 도달거리가 흔들리는 폭 |
|---|---:|---:|---:|
| Win100 | 0.75 | 1.92px | **1.45 px** |
| Win100 | 1.00 | 2.57px | 1.93 px |
| Retina | 0.75 | 3.85px | 2.89 px |
| **Retina** | **1.00** | 5.13px | **3.86 px** |

**물리 발목 위치는 그대로인데 그림만 변한다.** 그리고 가장 짧아지는 시점(0.087ρ)이
**정강이가 수직인 디딤 중간** = 접지 순간이다.

> 이건 발 실패 (C1)의 재현이다 — `LimbCurveRenderer` 발 기록:
> *"걸음마다 발이 100%↔1%로 두 번 커졌다 작아진다 … 화면에서는 「발이 생겼다 사라졌다」로 읽힌다."*
> **기전은 다르지만 읽힘은 같다.** 그 문장은 사용자가 *"다리가 이상해졌어"*라고 신고해서 적힌 것이다.

### 13-5. ★ 고관절에 **오늘은 없는 실패 모드**가 생긴다 — 여유 12°

각진 끝은 관절점 주변 반경 ρ 원에서 **자기 방향 쪽 반원판만** 덮는다. 고관절에는
몸통(위) · 좌다리 · 우다리 세 개가 모이므로, **연속한 두 방향의 간격이 180°를 넘으면 쐐기가 열린다.**

```
안식 자세: 허벅지 −102° / −78°, 몸통 +90°
간격 = 24° / 168° / 168°   →  최대 168°  →  여유 12.0°
```

프로덕션 애셋 값으로 자세별 판정(`DefaultStickConfig.asset`):

| 자세 | 좌허벅지 | 우허벅지 | 최대간격 | 쐐기각 | Win100/0.75 | **Retina/1.0** | 근거 |
|---|---:|---:|---:|---:|---:|---:|---|
| IDLE 서기 | −102 | −78 | 168.0 | 0 | — | — | `idleLegSpreadDegrees: 12` |
| WALK 최대 | −115 | −65 | 155.0 | 0 | — | — | `LegHipKeys` peak 25 |
| LANDING CROUCH | −130 | −8 | 140.0 | 0 | — | — | Front 82 / Rear −40 |
| PARKOUR 맨틀 | −130 | −50 | 140.0 | 0 | — | — | `MantleHipDegrees: 40` |
| ARCHERY | −108 | −74 | 164.0 | 0 | — | — | 16 / −18 |
| **FALL 낙하** | −91 | −61 | **179.0** | 0 | — | — | `fallPoseHipDegrees 14` + spread 15 — **여유 1.0°** |
| **DRAG 발버둥** | −68 | −44 | **202.0** | 22.0° | 0.73px | **1.96px** | `dragStruggleHipDegrees: 34` |
| **THROW TUMBLE** | −23 | −5 | **247.0** | **67.0°** | **2.12px** | **5.66px** | `throwTumbleHipDegrees: 76` |

- **`throwTumbleEnabled: 1`은 출하 기본이고**(CLAUDE.md — 사용자가 직접 요청한 «공중 회전 후
  무릎앉아 착지»), 마우스로 던질 때마다 보인다. 거기서 **엉덩이 뒤가 67° 물어뜯긴다.**
- **FALL은 179.0° / 180°다.** `design-motion`이 `fallPoseHipDegrees`를 **1° 올리면 깨진다.**
  오늘은 이 값이 조형과 아무 관계가 없다. A′를 채택하면 **모션 상수가 조형 계약이 된다.**

**어깨는 안전하다 — 그리고 그 이유가 내 담당 수치다.**

```
ρ_arm 0.039188 < ρ_torso 0.043106  (= 위계 0.9091 : 1.0000)
⇒ 어깨점이 몸통 축 위에 있으므로 팔의 각진 끝은 팔 각도와 무관하게 항상 몸통 안. 여유 0.003919 월드
반대로 ρ_leg 0.047025 > ρ_torso  (1.0909)  ⇒ 몸통 혼자로는 다리 끝을 못 덮는다
```

> **팔·몸통·다리 두께 위계(0.910 : 1.000 : 1.091)가 각진 캡의 안전 여부를 관절마다 가른다.**
> 팔은 얇아서 무조건 안전하고, **다리는 굵어서 반대쪽 다리에 의존한다.**
> 그리고 고관절은 **병합으로 구제할 수 없다** — 폴리라인은 갈라지지 않는다.
> ⇒ **A′ + 병합의 잔여 결함은 「종단 4개 각짐」 + 「고관절 자세 계약」 둘이다.**

### 13-6. 열린 선 9개 · 끝 18개 — **화면에서 실제로 보이는 끝은 4개** (브리프 질문 2)

| 판정 | 개수 | 어디 | 여유 |
|---|---:|---|---|
| **파묻힘** | 4 | 몸통 위(목) · 몸통 아래(고관절) · 어깨 2 | 목 **+0.028369**(머리 잉크 안, 둥근 캡이 덮던 0.043106도 원래 머리 속) / 어깨 **+0.003919** |
| **병합하면 소멸** | 8 | 팔꿈치 4 · 무릎 4 | 끝이 아니라 꼭짓점이 된다 |
| **조건부** | 2 | 고관절 2 | §13-5. 안식 여유 12° |
| ★ **노출** | **4** | **손목 2 · 발목 2** | 실루엣 바깥 |

**닫힌 선 2개**(`HeadFill` · `HeadOutline`)는 캡이 없어 무관하다 — `perf-doc` 실측대로다.
목이 안 벌어지는 것은 **운이 아니라 구조다**: 몸통 끝점이 머리 링 중심선(중심에서 0.165) 위에
정확히 얹혀 있고 머리 잉크 바깥반경이 0.193369라, **각진 끝도 링 두께(0.028369)만큼 파묻힌다.**

## 14. 【판정 7 · 채택 권고】 **마디 병합은 조형을 바꾸지 않는다**

### 14-1. 브리프의 전제가 틀렸다 — 지금 관절은 두껍지 않다

*"지금은 두 선이 겹쳐 관절이 살짝 두꺼운데"* — **아니다.**
두 마디의 둥근 캡은 **같은 점을 중심으로 한 같은 반지름 ρ의 원판 두 개**라 합집합이
**정확히 원판 하나**다. 실측이 그대로 말한다: cap8 관절 구멍 0.294%, **깊이 98.39%**.
그리고 그 1.61% 결손은 겹침이 아니라 **8정점 캡 팬의 현 사그타**다 — 이론 `1 − cos(π/16) = 1.92%`.

> **오늘의 관절 바깥 곡률반경은 정확히 ρ다. 더도 덜도 아니다.**

### 14-2. 병합해도 **새 코너가 생기지 않는다** — 관절 꼭짓점은 이미 필렛의 6분의 1이다

`LimbCurveRenderer.FillArcs`는 반호를 `φ = h·k/(ArcSamplesPerHalf−1)`로 **균등 표본**한다
(`ArcSamplesPerHalf = 4` → 반호당 3구간). 두 반호를 이으면 **총 6구간, 꼭짓점 회전각 Δφ = θ/6**.
그리고 관절점에서의 회전각도 **정확히 Δφ**다(위 마디 마지막 현의 방향 = 접선(5h/6),
아래 마디 첫 현 = 접선(7h/6) → 차 = h/3 = θ/6).

**프리팹으로 검증**(안식 팔꿈치 총굽힘 10°): 꼭짓점 회전 0.833 / 1.667 / 1.667 / **[관절] 1.666** /
1.667 / 1.667 / 0.833 — **관절이 다른 다섯과 구별되지 않는다.** `FORM_SPEC` 4-6의
「무릎 126° → Δφ 21°」와 같은 식이다.

| 꺾임 | 병합 관절 깊이 | 결손 | Win100/0.75 | Retina/1.0 |
|---|---:|---:|---:|---:|
| 하드 코너 54°(`perf-doc` 프로브) | 87.4% | 12.6%ρ | 0.24px | 0.65px |
| **우리 최악 Δφ = 21°**(무릎 126°) | **≈98%** | **≈2%ρ** | **0.04px** | **0.10px** |
| 안식 Δφ = 1.67° | ≈100% | ≈0.01%ρ | ~0 | ~0 |
| (대조) 오늘 cap8 | 98.4% | 1.6%ρ | 0.03px | 0.08px |

*(21° 값은 실측 54° 한 점을 `deficit ∝ τ²` 및 `∝ 1−cos(τ/2)`로 외삽한 것 — 두 모형이 같은
98.1%를 준다. **실측이 아니다, 미확인.** 그러나 오늘 cap8의 1.6%와 같은 자릿수라는 결론은 튼튼하다.)*

⇒ ★ **`perf-doc`의 프로브 기하는 프로덕션과 다르다.** 그들은 **필렛 없는 하드 54° 코너**를 썼는데
우리 관절은 **Δφ ≤ 21°의 필렛 호**다. 결론 방향(병합이 쐐기를 없앤다)은 같고 **더 강해진다** —
두 렌더러 cap0의 쐐기도 실제로는 15.0%가 아니라 **Δφ/360 ≤ 5.8%**다(깊이는 여전히 0).

### 14-3. ★ **팔꿈치를 고치는 것은 병합이지 `cap = 0`이 아니다** — 브리프의 표가 두 결정을 붙여 놨다

`T5_singlePolyline_elbow.png`는 `MakeLine(..., 0.10f, false)` = **기본 `capVerts = 8`**이다.
그 그림의 잉크 노출은 **0.00%**다(내 재측정).

| | 팔꿈치/무릎 노출 | 종단 노출 | 획 말단 | 새 결함 |
|---|---:|---:|---|---|
| 현행 A(cap8, 마디 2개) | 3.34%(근접) | 25.8~29.0%(근접) | **둥근** | — |
| ★ **A + 마디 병합**(cap8) | **0.00%** | 25.8~29.0%(근접) / **전신 8.66%** | **둥근** | **없음** |
| A′(cap0, 마디 2개) | 0.00% | 0.0% | 각짐 | 관절 쐐기 깊이 0 |
| A′ + 마디 병합 | 0.00% | 0.0% | **각짐** | 머리개수 −0.105 · 발끝 호흡 · 고관절 |

> **`cap = 0`이 사는 것은 「종단 4개」뿐이고, 그 값으로 위 4열을 전부 지불한다.**

### 14-4. 병합의 부수 효과 — 대부분 이득

- **렌더러 11 → 7.** 아래 마디의 `LineRenderer`만 없어지고 `Transform`/`Rigidbody2D`/`HingeJoint2D`는
  그대로다. 프로덕션 소비자는 전부 **아래 마디의 Transform**을 찾지 `LineRenderer`를 찾지 않는다
  (전수 grep: `StickmanPoseAnimator:534` · `CharacterPetRenderer:1366` · `RagdollRig` 전부 Transform).
  쓰는 곳은 `LimbCurveRenderer:282` 하나다.
- **이미 두 곳에서 출하 중인 형태다.** 정보창 초상화(`CharacterPortraitStage._limbPoints[9]`)와
  리틀스틱메이트(`BuildLimbPolylineBetween`)가 **이미 마디를 한 폴리라인으로 그린다**
  (`PolylinePointCount = 9`). ⇒ **병합은 본체를 펫·초상화 쪽으로 맞추는 것**이지 새 형태가 아니다
  (R2 §12-1 *"펫은 주인의 축소판이다"*가 이 축에서 지금 어긋나 있다).
- **오늘 캡 밑에 숨어 있는 Δφ 방향 불연속이 정식 꼭짓점이 된다** — §14-2대로 오늘은 전체 굽힘의
  1/6이 관절에서 **점프**하고 둥근 캡 두 개가 그것을 덮고 있다. 병합하면 그 점프가 사라진다.
- **발 실패는 전이되지 않는다.** `FORM_SPEC` 4-7의 *"발이 실패한 이유가 정확히 이것 — 발은 같은
  폴리라인에 붙었다"*는 **발의 조건**(필렛 없는 90°·|bias| 경각 코너, 길이 = ρ·|bias|, **여유 정확히 0**)
  에 붙은 말이다. 무릎/팔꿈치는 **필렛 호**이고 `r ≥ ρ`를 `LimbCurveGeometryTests (4)`가
  전 배율에서 이미 검사한다. **같은 규칙, 다른 여유.**
- 폭이 위/아래 공유가 된다 — **오늘도 같은 값**(0.078375 / 0.094050)이라 무변.
- ⚠ **`LineRenderer 11개`를 세는 모든 문서·계측이 낡는다**(내 §1 표 · `design-art` §22-6 · 막 계수 10).
  `[렌더품질]`은 씬 전체를 세므로 그쪽은 무관하다(§1-2).

## 15. 【판정 8】 각짐 없이 노출 0을 얻는 길 — **있다** (브리프 질문 4)

### 15-1. 처방 — `A + 마디 병합 + 종단 막 원판 4개`

노출이 남는 곳은 §13-6대로 **손목 2 · 발목 2**뿐이다. 그 네 곳에
**막색 원판을 잉크보다 아래 층에 깐다.** 잉크의 둥근 캡이 그 위에 얹혀 **1물리픽셀 링만 남는다.**

**원판 사양 — `HeadFill`과 같은 식을 그대로 쓴다**(새 수식 0개):

```
R_out = ρ_ink + m          (m = 막 두께 1 물리픽셀, 잉크와 같은 환산으로)
경로 반경 a = 0.45455 · R_out        폭 w = 1.09091 · R_out        점 24개, loop = 1
검산: 머리 R_out 0.165 → a 0.075 ✓  w 0.180 ✓   (프리팹 HeadFill 실측값과 일치)
안쪽 = a − w/2 = −0.0909·R_out  ⇒ 중심까지 과채움, 24각형 최대 결손 R(1−cos7.5°) = 0.86% 를 10배 덮는다
```

위치 오차 허용 **±0.25 물리픽셀**(이 저장소가 이미 쓰는 MSAA 4× 커버리지 양자화, `FORM_SPEC` 9-1).

### 15-2. 대차대조 — **렌더러 증가 0, 정렬층 1칸**

| | 오늘 | A′+병합 | **A+병합+종단원판** |
|---|---:|---:|---:|
| 렌더러 | 11 | **7** | 7 + 4 = **11** |
| 전신 잉크 노출 | (막 없음) | 0.0% | **0.0%** |
| 획 말단 | 둥근 | **각짐** | **둥근** |
| 머리 개수 | 4.6063 | **4.5016** | **4.6063** |
| 발끝 도달거리 진폭 | 0 | **0.752 ρ** | **0** |
| 고관절 자세 계약 | 없음 | **여유 12°** | **없음** |
| **정렬층 새 정수** | — | **0** | **1 (CH-1 부활)** |

> ★ **진짜 선택지는 「각짐 vs 정렬층 정수 1칸」이다.** 브리프가 *"CH-1이 통째로 사라진다"*를
> A′의 장점으로 들었는데, **그 장점의 가격이 §13의 다섯 항목이다.**
> 나는 **정렬층 1칸을 산다.** R8 §2-3의 **L2(막 층 −3 / 발자국 막 −4)**가 그대로 답이고,
> 그때 내가 낸 근거(*"조형 손실 0 · 새 정수 2개 · 기존 0~5 주석 무수정"*)는 지금도 유효하다.

**미확인 2건 — 추측으로 메우지 않는다:**
1. **같은 `sortingOrder` 안에서 z 오프셋으로 순서를 확정할 수 있는가.** 가능하면 정렬층 0칸으로
   끝난다. R8 §2-1에서 나는 같은 order 안 순서를 *"임의"*라고 적었는데, 결정적 z 차이가 있으면
   임의가 아닐 수 있다. **내가 잴 수 없는 축이다 — `coder`/`perf-doc` 확인 필요(리더 경유).**
2. **더 깨끗한 대안: 셰이더가 `uv.y`가 아니라 폴리라인까지의 실거리로 띠를 만들면** 캡·코너·종단이
   한꺼번에 풀리고 원판도 정렬층도 필요 없다(`perf-doc`의 B계열). **조형 축에서는 이게 최선이다.**
   비용은 내 소관이 아니다.

### 15-3. 그리고 **가장 싼 답이 하나 더 있다 — 아무것도 안 하는 것**

`A + 마디 병합`만 하고 종단 4개를 그대로 두면 **전신 둘레의 8.66%가 무방비**다(§12-2).
잉크색에 가까운 배경(§4-1 실측 **17.2%**)에서 **손끝·발끝의 막이 5~6px 끊겨 팔다리가 열린 관처럼
보인다.** 조형상 나는 이것을 **각짐보다는 낫고 원판보다는 나쁘다**고 판정한다 —
결함이 **4곳 · 각 5~6px**로 국소적이고, **17%의 배경에서만** 나타나며,
**밝은 배경 83%에서는 아무 대가도 없기 때문**이다(A′는 100%에서 대가를 낸다).

> **권고 순위: ① A+병합+원판 ≫ ② A+병합(원판 없이) ≫ ③ A′+병합.**

## 16. 【판정 9 · 반려】 곁가지 `A-carve` — **22.3%를 부순다**

`A-carve`는 실루엣을 안 키우므로 §3-5(뭉툭)·§4-3(머리개수)·§4-4(위계 압축)가 실제로 사라진다.
**그 장점은 참이다.** 그러나 **잉크가 2물리픽셀 얇아진다**는 대가가 이 프로젝트에서
가장 여러 번 재확인된 단 하나의 잠금 수치를 정면으로 친다.

| 화면 | 배율 | 몸통획 | **현행 비율** | A-carve 잉크 | **A-carve 비율** |
|---|---:|---:|---:|---:|---:|
| Win100 | 0.35 | 2.00px | 27.08%(하한) | **0.00px** | **0.00% — 잉크 소멸** |
| Win100 | 0.60 | 2.82px | **22.29%** | 0.82px | **6.49%** |
| Win100 | 0.75 | 3.53px | **22.29%** | 1.53px | **9.65%** |
| Win100 | 1.00 | 4.70px | **22.29%** | 2.70px | 12.81% |
| Retina | 0.75 | 7.06px | **22.29%** | 5.06px | 15.97% |
| Retina | 1.00 | 9.41px | **22.29%** | 7.41px | **17.55%**(최선) |

**두 가지가 동시에 깨진다:**
1. **값이 22.3%에서 6.5~17.6%로 내려간다.** 최선(Retina·1.0)조차 −21% 상대오차다.
   참고의 **링 머리 인물조차 15.8%**였다(`FORM_SPEC` 8-3) — A-carve의 Win100% 값들은 그 아래다.
2. ★ **더 나쁜 것은 값이 배율마다 달라진다는 것이다.** 지금 22.29%는 하한이 안 무는 전 구간에서
   **소수점까지 불변**이다(획도 머리도 `k`에 비례). A-carve는 그 불변성을 없앤다 —
   **배율 다이얼이 캐릭터의 종(種)을 바꾼다.**

> **판정: 조형 축에서 반려.** 「배율 0.47 이상 구간의 별도 선택지」로도 반려한다 —
> 배율에 따라 조형 법칙이 달라지는 것이 이 프로젝트가 M3-1 단위 불일치를 고치면서 없앤 바로 그 병이다.
> ★ 다만 **A-carve의 장점 자체는 살릴 수 있다**: 실루엣을 안 키우는 것이 목표라면
> **막을 바깥으로 0.5px · 안쪽으로 0.5px** 나눠 무는 절충이 있다. **안 쟀다 — 미확인.**

## 17. 【판정 10】 앞 판정 중 **바뀐 것 / 버틴 것**

| R8 판정 | 새 입력 | 결과 |
|---|---|---|
| **CH-1 정렬층** | A′면 불필요 | ★ **되살아난다** — 각짐을 거부하면 종단 원판이 필요하고, 그건 층이 필요하다(§15-2). **다만 필요한 것은 L2 한 갈래뿐**이고 그때 내가 권고한 것 그대로다 |
| **δ 축(§3-3)** | ρ'가 같다 | ★ **재계산 불필요는 맞지만, A′면 축 자체가 바뀐다** — 병합하면 관절이 캡 두 개가 아니라 필렛 꼭짓점이 되므로 δ의 대상이 「캡 겹침」에서 「Δφ 꼭짓점」으로 이동한다. **그 축은 `LimbCurveGeometryTests (4)`가 이미 보고 있다** — 새 표가 필요 없다 |
| **머리 링 +6.4%(CH-5)** | 프리팹 0.056738 확정 | ✅ **버틴다.** 내 파서도 0.056738을 읽었다(§13-3 교정) |
| **§3-5 관절 뭉툭 +20~45%** | — | ✅ 유효. **A·A′ 공통**이고 캡 결정과 무관하다 |
| **§4-2 「막만 남은 그림」** | `perf-doc` 자기정정 12.8%(과대 27.2% 철회) | ⚠ **결론 불변, 강도 하향.** 그 정정은 **A의 무방비 비율**에 대한 것이고 §4-2는 **배경이 잉크색일 때 잉크가 안 보인다**는 별개 사실이다. 대비는 기하와 무관하다(§5). ★ 다만 **전신 기준 8.66%**(§12-2)를 알게 됐으니 「무방비」의 무게는 내가 R8에서 쓴 것보다 **작다** |
| **§6 「막은 펜의 속성」** | — | ✅ **강해진다.** cap 값이 15개 파일에서 캐릭터/도구를 가르고 있다(§13-2) — 펜은 실제로 하나다 |
| **§4-3 머리 개수가 내려간다** | — | ✅ **A′가 같은 방향으로 한 번 더 내린다**(4.6063 → 4.5016). 막(−0.38 at Win100/0.75)과 **합치면 4.6063 → 3.92 수준**. ★ **합산은 안 쟀다 — 미확인** |

## 18. 실기 캡처 요청 — R8 §7의 C1~C8에 **더한다**

| # | 무엇을 | 조건 | 통과 기준 |
|---|---|---|---|
| **C9** | **종단 각짐이 눈에 보이는가** | **macOS Retina × 배율 1.00** × 손을 뻗은 정지 프레임 확대. A/A′ 같은 세션 A-B | ρ_arm 4.28px에서 두 90° 코너가 보이는가. 보이면 §13 확정 |
| **C10** | **발끝 도달거리 호흡** | Retina × 1.00 × **WALK 1주기 연속 프레임** (t = 0.25 디딤중간 / t = 0.625 스윙) | 발끝 잉크 최저점이 두 프레임에서 **3.86px 차이**가 나는가(§13-4 예측) |
| **C11** | **고관절 쐐기** | **THROW TUMBLE 공중 프레임** × Retina × 1.00 | 엉덩이 뒤에 5.66px 쐐기가 보이는가(§13-5 예측). 여기서 안 보이면 §13-5는 과대다 |
| **C12** | **병합 관절** | Win100% × 0.75 × **착지웅크림 앞무릎 126°** | 무릎 바깥 곡률이 병합 전후로 **0.04px 이내**(§14-2 예측). 오프라인 렌더러 금지 |
| **C13** | **종단 원판 이음매** | 손목 확대 × Retina × 1.00 | 원판의 막 링과 마디의 막 띠가 **단차 없이** 이어지는가(±0.25px) |

★ **오프라인 렌더러로 C9~C13을 판정하지 마라.** §13·§14의 숫자는 전부 **기하 계산**이고,
캡슐/사각형을 내가 직접 구현한 래스터(§12-2)조차 **LineRenderer의 실제 테셀레이션이 아니다.**
2026-09-01 발 실패의 직접 원인이 *"모든 점에 둥근 캡을 찍어 코너 퇴화를 가린"* 오프라인 렌더러다.

## 19. 리더에게 — 결정이 필요한 것 (R9 추가분)

| # | 결정 | 내 권고 | 왜 내 권한 밖인가 |
|---|---|---|---|
| **CH-7** | **`numCapVertices = 0`을 본체에 채택하는가** | ★ **반려.** 근거 5개(§13) | 조형은 내 판정이지만 대가(정렬층·렌더러)는 리더 몫 |
| **CH-8** | **마디 병합을 채택하는가** | ★ **채택.** 조형 무변(§14) · 렌더러 11→7 · 팔꿈치 노출 0 · 펫/초상화와 정합 | 프리팹 재굽기 + `LimbCurveRenderer` 수정. `coder` 배정 |
| **CH-9** | **종단 4개를 어떻게 할 것인가** | ①원판(정렬층 1) ≫ ②그냥 둠(전신 8.66% 무방비) ≫ ③각짐 | 정렬층은 되돌리기 어렵다 → `game-architect` 경유 권고 |
| **CH-10** | `A-carve` | ★ **반려**(§16) | — |
| **CH-11** | **`fallPoseHipDegrees` 여유 1.0°**(A′ 채택 시에만) | A′를 반려하면 이 항목 자체가 없어진다 | `design-motion` 소관 |
| **CH-12** | **`design-art` §22 정정 1건** — 「선 11개 / 막 10개」가 병합 후 **7 / 6**이 된다 | 병합 확정 후 반영 | 남의 문서 |

## 20. 인계 — 상대 산출물의 어느 지점과 맞물리는가 (R9)

| 상대 | 지점 | 무엇을 |
|---|---|---|
| `perf-doc` | **T7 요약표 「팔꿈치 12.8%」** | **라벨 오류** — 그건 `T5_twoSegmentArm_full`(팔 전체)이고 팔꿈치는 3.34%다(§12-1) |
| `perf-doc` | **T5/T7 프로브 기하** | **하드 54° 코너는 프로덕션과 다르다** — 우리 관절은 Δφ ≤ 21° 필렛 호다(§14-2). 결론 방향은 같고 강도만 다르다 |
| `perf-doc` | **`T5_singlePolyline_elbow.png`가 `cap = 8`이다** | ★ **병합만으로 팔꿈치 0.00%**다. **두 결정을 분리해 달라**(§14-3) |
| `perf-doc` | 「A′ + 병합 = 렌더러 증가 0」 | 정확히는 **11 → 7(감소)**이다. 그 여유 4칸이 §15-1 원판의 예산이 된다 |
| `design-art` | §22 선 개수 · 막 개수 | 병합하면 11 → 7, 막 10 → 6 |
| `design-art` | 막 두께 1물리픽셀 | 참고 이미지의 실제 아웃라인은 **머리 지름의 0.65%**인데 우리는 **2.37~13.54%**다(§13-1-2). 반려 사유는 아니고 **알고 결정할 사실** |
| `design-motion` | **`fallPoseHipDegrees` 14 · `throwTumbleHipDegrees` 76 · `dragStruggleHipDegrees` 34** | A′ 채택 시 이 세 값이 **조형 계약**이 된다(§13-5). 리더 경유 |
| `design-motion` | **보행 키표**(`LegHipKeys`/`LegKneeKeys`) | A′ 채택 시 발끝 도달거리가 걸음당 2회 0.752ρ 흔들린다(§13-4). 리더 경유 |
| `design-equipment` | **장비·펫도 `cap = 4`(둥근)다** | 막을 「펜의 속성」으로 걸면 cap 값도 같이 간다 — 본체만 0으로 하면 펜이 갈라진다(§13-2). 리더 경유 교차검토 요망 |
| `coder` | `LimbCurveRenderer:282` `lowerName` | 병합의 유일한 수정 지점. 소비자는 전부 아래 마디 **Transform**을 본다(§14-4) |
| `game-architect` | 정렬층 정수 1칸(L2) | 되돌리기 어려운 결정 |

## 21. 플랫폼 영향 (R9)

| | 내용 |
|---|---|
| **Windows 영향** | **함께 검토했다.** ① **각짐의 대가는 Win100%에서 가장 작다** — 배율 0.35에서 ρ = 1.00px이라 각짐이 1픽셀이고 사실상 안 보인다. ② 그러나 **머리 개수 손실 −0.1047은 플랫폼 무관**이고 Win100%에서도 그대로다. ③ **하한 계약 파국(잉크 0px)은 여전히 Windows 100% 전용이고 A′가 고치지 않는다** — 폭 예산이 같기 때문이다. `MinStrokeScreenPoints`를 **잉크 코어 하한**으로 재해석하고 전체 폭을 `하한 + 2px`로 만드는 것이 유일한 답이고, 그건 A·A′ 공통이다(T3의 `win035` R = 2.000/4.000이 이미 그 가정 위에 서 있다). ④ 이 라운드는 `.cs`/`.asset` 0줄 · `#if UNITY_STANDALONE_*` 0건이라 `Tools/CrossCompile/xcheck.sh` 대상이 없다 |
| **macOS 영향** | **함께 검토했다. 그리고 이 라운드는 macOS 쪽이 지배한다.** ① **A의 캡 결함이 Retina에서 가장 나쁘다 — 내 재측정 실증: `mac075` 33.70% > `win075` 27.69% > `win035` 22.10%.** 이유는 DPI가 아니라 **잉크 코어 비율 R**이다(R이 클수록 캡 팬이 전부 잉크로 분류된다). ② **각짐의 대가도 Retina에서 가장 크다** — 배율 1.00에서 ρ_leg = 5.13px, 발끝 도달거리 진폭 3.86px, 고관절 쐐기 5.66px. ★ **즉 문제와 대가가 같은 방향으로 커진다 — 플랫폼별로 갈라지지 않는다.** 「Windows만 A′」 같은 조건부 정책은 아무것도 사지 못한다 |
| **모바일(iPad/iPhone)** | **미확인.** 픽셀 밀도가 Retina 이상(2~3×)이므로 각짐의 대가는 **더 커진다**(ρ가 물리픽셀로 더 크다). 스크린샷 백드롭 모드에서 배경을 우리가 그린다면 막 자체가 불필요할 수 있다는 R8 §11의 보류가 그대로 유효하다. `dev-platform` 확인 필요 |
| **공통** | 이 라운드가 만든 파일: `design/character/` 아래 `.md` 이 절 · `.py` 3개(`r9_form.py` · `r9_reftip.py` · `r9_body.py`). **프로덕션 트리 무수정.** |

## 22. 미확인 (R9) — 추측으로 메우지 않는다

1. **병합 관절 깊이 ≈98%(Δφ = 21°)는 외삽이다.** 실측점은 하드 54° 하나뿐이고, 두 모형이
   같은 답을 주지만 **Unity 조인의 실제 기전을 나는 모른다**(내 모형은 94%를 줘서 실측 87%와 어긋난다).
2. **고관절 각도 부호 규약**은 프리팹 안식 자세(−102/−78)에서 역산했다. `design-motion` 확인 필요.
   **구조(공통 회전 12° 초과 → 쐐기)는 부호와 무관하게 성립하지만, 어느 자세가 걸리는지는 규약에 달렸다.**
3. **같은 `sortingOrder` 안 z 오프셋으로 순서를 확정할 수 있는가** — 못 쟀다(§15-2).
4. **막 + 각진 캡의 머리 개수 합산 효과**를 안 쟀다. 각각 −0.105(캡)와 −0.38(막, Win100/0.75)이지만
   두 효과가 같은 분모를 흔들어 단순 합이 아니다.
5. **참고 이미지 말단 7건 중 1건(파랑 뒷손)은 측정에 실패했다.** 「각짐」으로 세지 않았고
   「둥근」으로도 안 셌다. 6/6은 **유효 측정만의 비율**이다.
6. **`A-carve`의 절충안(바깥 0.5px + 안쪽 0.5px)을 안 쟀다.**
