# StickMate — 프로젝트 컨벤션 (CLAUDE.md)

바탕화면에서 돌아다니는 스틱메이트 앱. Unity 6 LTS 기반. **4개 플랫폼 동시 지원 목표: macOS / Windows / iPad / iPhone.**

- 데스크톱(macOS/Windows): 실시간 타 윈도우 열거 오버레이 (기존 설계)
- 모바일(iPad/iPhone): iOS 샌드박스 정책상 실오버레이 불가 → **"스크린샷 백드롭 모드"**(유저가 캡처한 홈 화면 스크린샷을 정적 배경으로 쓰고 그 위에서 캐릭터가 노는 착시 연출, 사용자 확정 결정). 상세: `docs/ARCHITECTURE.md` 0-1절.

## 필독 문서
- `docs/ARCHITECTURE.md` — 설계 요약, 기술 스택, 무빙 방식 결정 근거
- `docs/UX_FLOW.md` — UX Designer 산출물 (화면/상태 흐름)
- `Tasklist.md` — 팀 공유 진행상황 트래커 (항상 최신 유지)
- `process.md` — 리더가 단계별로 남기는 진행 로그

## 절대 불변 원칙
1. **행동-텍스트 싱크**: 말풍선 대사는 상태 전이가 확정된 뒤 그 상태로부터만 파생. 대사 먼저 정하고 행동을 끼워 맞추지 않는다.
2. **비침해**: 클릭 관통 기본 ON, 전체화면 게임 감지 시 자동 숨김.
3. **유저 자산 불변**: 실제 파일/아이콘/타 윈도우는 절대 이동·삭제·수정하지 않는다. 전부 읽기 전용 열거 + 시각적 복사본 연출.
4. **플러그인 구조**: 신규 모션/이펙트(DLC)는 기본 로직 무수정으로 ScriptableObject 매니페스트를 통해 추가.

## 캐릭터 무빙 방식 (결정됨, 변경 시 팀 합의 필요)
Active Ragdoll(Rigidbody2D + Joint2D) + IK 하이브리드.
- 능동 상태(IDLE/WALK/JUMP/PARKOUR_CLIMB/ATTACK): 모터/IK가 목표 포즈로 힘을 가함
- RAGDOLL 상태(피격/던짐/추락 충격): 전신 물리에 위임 → 감속 후 GETUP으로 자동 복귀
- 근거: docs/ARCHITECTURE.md 0절

## 팀 구성 (.claude/agents/)
- `ux-designer` — UX 플로우, 와이어프레임, 예외 상태 (창/패널/버튼 등 **UI 표면** 담당)
- 게임 디자인 3인 (사용자 확정 결정, 2026-09-01 — "지금 디자인이 중요한데 인원이 부족한 것 같다").
  **서로 범위가 겹치지 않게 나눠져 있다. 겹치면 리더를 거쳐 조정한다.**
  - `design-character` — **캐릭터 본체 형태**: 비율, 획 두께, 관절 곡률, 실루엣 가독성, 머리 구조
  - `design-equipment` — **장비·소품 조형**: 42종 + 머리카락 + 펫. 카드 44px와 착용 크기 양쪽 검증
  - `design-motion` — **모션·연출**: 자세, 박자, 전이, 무게감, 행동-텍스트 싱크
  - 셋 다 **프로덕션 `.cs`를 수정하지 않는다.** 설계만 하고 구현은 리더가 `coder`에게 배정한다.
  - 산출물은 숫자와 검산을 동반해야 한다("더 두껍게"가 아니라 "0.4750R").
  - **최종 판정은 실제 빌드 캡처로만 한다** — 오프라인 렌더러가 모든 점에 둥근 캡을 찍어
    코너 붕괴를 가린 사고가 있었다(2026-09-01 발 실패의 직접 원인).
- 리더(현재 세션) — Architect: 통합/최종 판단, Phase 게이트 승인, process.md 갱신 + 커밋
- `coder` — Teammate1: 실제 구현
- `debugger` — Teammate2: 버그 리포트, 원인불명 버그는 가설 기반 과학적 토론
- `test-engineer` — Teammate3: 테스트 + 최종 리뷰어 겸임 (개선점 있으면 Architect로 반려)
- `perf-doc` — Teammate4: 성능 최적화(요청 시) + 문서화(최종 단계)
- 페르소나 검증단(사용자 확정 결정, 2026-09-01) — `persona-newcomer`(민지, 처음 쓰는 유저) /
  `persona-stress`(재현, 장시간·멀티태스킹 유저) / `persona-immersion`(소은, 몰입·디테일 비평가).
  프로덕션 코드를 직접 수정하지 않고 비판적으로 검증만 한다.

## 협업 프로토콜
- 교차 레이어 영향은 `Tasklist.md`의 "교차 레이어 영향 로그"에 즉시 기록
- 원인 불명 버그는 "과학적 토론 로그"에 가설/검증방법/결과/결론 기록
- **기능 추가/변경이 완료될 때마다** 리더가 페르소나 검증단 3명을 병렬로 호출한다. 각자의 의견은
  `Tasklist.md`의 "페르소나 검증 로그"에 기록되고, 리더가 그중 실제 조치가 필요한 항목만 골라
  해당 담당자(coder/debugger/ux-designer 등)에게 피드백으로 전달한다. 페르소나는 서로 다른
  담당자에게 직접 지시하지 않는다 — 리더를 거친다.
- **테스트에 프로덕션 상수를 숫자로 베끼지 않는다.** 상한값/스키마 버전 등은 반드시 그 상수를
  참조해서 검증한다(2026-09-01: `MaxCharacterScale`와 저장 스키마 버전 각각 2건씩, 총 4건이
  하드코딩 잔존으로 깨졌던 사고 이후 확정). 저장 스키마 `CurrentVersion`을 올리는 라운드는
  `vN-1` 구버전 파일을 읽었을 때 신규 필드가 안전한 기본값으로 채워지는지 검증하는 하위 호환
  테스트 1건을 반드시 동반한다.
- ### ★ 플랫폼 동시 검토 (사용자 상시 지시, 2026-09-01 확정)
  **macOS 쪽을 수정하면 그 라운드 안에서 Windows 쪽도 반드시 함께 검토한다.** "나중에 맞추자"는
  금지 — 그 나중은 오지 않는다.
  - 완료 보고에 **"Windows 영향: 없음 / 함께 수정함 / 별도 배정 필요(사유)"** 중 하나를 반드시
    적는다. 이 줄이 없는 완료 보고는 리더가 반려한다.
  - **Windows 전용 파일은 이 개발 머신에서 한 번도 컴파일되지 않는다** — 갭이 조용히 쌓이는
    근본 원인이다. 그래서 Windows 쪽을 건드렸으면 Unity 번들 Roslyn
    (`MonoBleedingEdge/.../csc.exe`)을 직접 몰아 `-define:UNITY_STANDALONE_WIN`으로 런타임
    어셈블리를 크로스 컴파일해 0 에러를 확인한다(`-define:UNITY_STANDALONE_OSX`도 함께 돌려
    macOS 회귀 확인).
  - 구조적 패리티는 `Tests/EditMode/PlatformParityAuditTests.cs`가 자동 검사한다. 새로 생긴
    플랫폼 분기는 이 검사에 항목을 추가한다. 아직 못 고친 갭은 `Assert.Fail`이 아니라
    `Assert.Ignore`(사유 포함)로 남겨 러너에 "건너뜀"으로 계속 보이게 한다 — 잊히지 않게.
  - 정책 판정 로직은 **플랫폼 중립 위치**(`Platform/`)에 두고, 플랫폼 전용 코드는 "사실 조회"만
    담당한다. 정책이 `Platform/MacOS/` 안에 있으면 Windows가 물리적으로 호출할 수 없다(실제
    사고 사례: `FullscreenSuspendPolicy.cs`).
  - **사용자 신고에 플랫폼 단서가 있으면 그 플랫폼을 먼저 고친다.** 실제 사고: "맥os는
    모르겠는데 엑셀같은 프로그램 전체화면에서…"라는 신고에 macOS만 고치고 정작 신고된 Windows를
    놓쳤고, 릴리즈 직전 패리티 감사에서야 발견됐다.
- **시간 기반 연출을 검증하는 PlayMode 테스트는 반드시 벽시계(초) 기준으로 예산을 잡는다.**
  `for (i < N프레임)` 같은 프레임 수 기반 대기는 금지(2026-09-01 확정: 이 저장소의 배치모드
  PlayMode는 2,000fps 이상으로 돌아서, 180프레임짜리 예산이 실제로는 0.01~0.08초밖에 안 되는
  경우가 있었다 — 프로덕션은 멀쩡한데 테스트만 "불안정"으로 오판되어 네 라운드 동안 아무도
  못 고쳤다).
- Phase 완료마다 리더가 process.md 갱신 + git commit

## 환경 메모
- 개발 환경에 tmux/Homebrew/Unity가 설치되어 있지 않을 수 있음 — 팀원 에이전트는 Unity 프로젝트 파일(.cs, .asset, .meta 등)을 소스 형태로 작성하고, 실제 빌드/에디터 실행은 Unity 설치 후 사용자가 수행.
