# 핸드오프 검토 — 캐릭터 정보창 「브라스 아카이브」 (2026-09-02, ux-designer)

> 대상: `scratchpad/redesign/design_handoff_equipment_window/` (README 378줄 + HTML 프로토타입 2종)
> 시안 **1a 「브라스 아카이브」**가 구현 대상(1b 「네온 콘솔」은 비교용).
> 짝 문서: `docs/UX_SHOP_AND_CURRENCY.md`(내 선행 설계) · `docs/UX_FLOW.md` §47
> **프로덕션 `.cs` 수정 0건. 앱 실행/빌드 안 함.**

---

## 0. 한 줄 판정

> **방향은 채택. 팔레트는 그대로 쓸 수 없다.**
> 실사용 텍스트 조합 **43건 중 14건(32.6%)**이 우리 `MinTextContrast 4.5`에 미달하고,
> 미달은 전부 **잉크 2종**(`#6E665C` / `#5C574E`)에서 나온다 — 이 둘은 **이 팔레트의 어떤 표면
> 위에서도 4.50을 넘을 수 없다**(최댓값 3.46 / 2.73). 그리고 **주 구매 CTA인 `동전 N` 버튼의
> 면 대비가 1.00 : 1**이다 — 오늘 밤 `[✕]`가 걸린 그 결함(1.00 : 1)과 **같은 숫자**다.

## 0-A. 측정 방법 — 교정 먼저 (거짓 통과 방지)

`Tools/ContrastProbe/measure_chip.py`와 **완전히 같은** `lin/L/CR`을 쓴다. 매 실행마다 5건 교정,
하나라도 어긋나면 숫자를 내지 않고 죽는다.

**재현**(이 문서의 모든 수치는 두 줄로 다시 나온다):
```bash
Tools/ContrastProbe/handoff_palette_audit.py   # §1 대비 전수
Tools/ContrastProbe/handoff_layout_audit.py    # §2 창 크기 / §4 타입 스케일 / §5 램프
```

| 교정 | 정답 | 실측 | 판정 |
|---|---|---|---|
| 흰 / 검 | 21.0000 | 21.0000 | PASS |
| 동일색(흰) | 1.0000 | 1.0000 | PASS |
| 동일색(`#0D0C0B` = 핸드오프 창 배경) | 1.0000 | 1.0000 | PASS |
| `#767676` / 흰 (WCAG 예제) | 4.5422 | 4.5422 | PASS |
| `#000000` / `#808080` (WCAG 예제) | 5.3172 | 5.3172 | PASS |

**미확인으로 남기는 것**
- 프로토타입 HTML을 **브라우저로 렌더해서 재지 않았다**(이 환경에 브라우저가 없다). 색은 README의
  **선언값**에서 계산했다. 그라디언트(`linear-gradient(180deg,#161311,#100E0D)`)는 **양 끝값을 각각** 쟀다.
- `opacity` / `box-shadow` 합성은 CSS 합성 규칙으로 **계산**한 것이지 캡처 픽셀이 아니다.
- 글자 폭·줄바꿈은 **재지 않았다** — 폰트가 바뀌므로(§4) 지금 재는 것이 의미가 없다.
- 최종 판정은 **Unity 실기 캡처**다. 여기 숫자는 전부 **예측**이다.

---

# 1. ★ 대비 실측 — 리더가 가장 먼저 요구한 것

## 1-1. 텍스트: 실사용 43건 중 **14건 미달** (하한 4.50)

미달 14건 **전부**가 아래 두 잉크에서 나온다. 그리고 이 둘은 **구제 불가**다 —
팔레트의 7개 표면 중 **가장 밝은 표면 위에서조차** 하한을 못 넘는다.

| 잉크 | 창 `#0D0C0B` | 카드 `#161311` | 활성 `#17130E` | 착용카드 `#1D1813` | **최댓값** | 4.50 |
|---|---|---|---|---|---|---|
| **`#6E665C` (라벨)** | 3.46 | 3.27 | 3.27 | 3.12 | **3.46** | **불가** |
| **`#5C574E` (비활성)** | 2.73 | 2.58 | 2.58 | 2.46 | **2.73** | **불가** |
| `#8A8578` (3차) | 5.31 | 5.03 | 5.02 | 4.79 | 5.31 | 통과 |
| `#9A9184` (본문) | 6.29 | 5.95 | 5.95 | 5.67 | — | 통과 |
| `#A79E90` (보조) | 7.39 | 6.99 | 6.99 | 6.66 | — | 통과 |
| `#EDE7DB` (기본) | 15.87 | 15.02 | 15.01 | 14.30 | — | 통과 |
| `#C8A15A` (황동) | 8.10 | 7.66 | 7.66 | 7.30 | — | 통과 |
| `#8FBF6A` (증가값) | 9.15 | 8.66 | 8.65 | 8.24 | — | 통과 |

**미달하는 실제 자리 14곳**

| 자리 | 색 / 바탕 | 대비 |
|---|---|---|
| 헤더 레벨 `Lv.7` 11px | `#6E665C` / `#0D0C0B` | 3.46 |
| 착용행 라벨 `모자 · 집중력` 9.5px | `#6E665C` / `#17130E` | 3.27 |
| 스탯 `모자 주스탯` 9.5px | `#6E665C` / `#161311` | 3.27 |
| 카테고리 영문 라벨 10px | `#6E665C` / `#0D0C0B` | 3.46 |
| 카드 메타 테마명 10px | `#6E665C` / `#161311` | 3.27 |
| 스테이지 `PREVIEW` 라벨 10px | `#5C574E` / `#0F0D0C` | 2.70 |
| 착용행 `비어 있음` 12.5px | `#5C574E` / `#111010` | 2.65 |
| 단계 칩 `미달` 10px | `#5C574E` / `#1B1713` | **2.48** |
| `중급까지 4` 10px | `#5C574E` / `#161311` | 2.58 |
| 세트 효과 설명 10.5px | `#5C574E` / `#151311` | 2.58 |
| 세트 하단 주석 10.5px | `#5C574E` / `#151311` | 2.58 |
| 카드 `보유` 10px | `#5C574E` / `#161311` | 2.58 |
| 보관함 슬롯명 10px | `#5C574E` / `#161311` | 2.58 |
| 프리셋 `미보유` 버튼 글자 | `#5C574E` / `#17130E` | **2.58** |

> ★ **이건 취향 문제가 아니라 이 저장소가 이미 판례를 남긴 자리다.**
> `UiChrome.RetiredInk`에 **폐기된 잉크 3종**이 값째로 보존돼 있고(`TabInactive` / `Quaternary` /
> `Disabled`), 그중 `Disabled`가 `#4B525C`다. **폐기 사유가 정확히 "글자에 쓸 수 없다"**이고,
> `UiInkHierarchyTests`가 소스를 훑어 위반을 잡는다. 핸드오프의 `#5C574E`는 그 폐기색과
> **같은 밝기대**다(창 대비 2.73 vs 우리 폐기색 2.28).
>
> ★ **`단계 칩 미달`(2.48)과 `프리셋 미보유 버튼`(2.58)이 특히 나쁘다.** 둘 다 *"왜 못 쓰는가"*를
> 말하는 글자다. `UiChrome.Ink(InkRole.Meta, *)`가 **비활성에서도 절대 안 흐려지는** 이유가
> 문서에 이렇게 적혀 있다: *"비활성 행에서 가장 중요한 글자는 '왜 못 쓰는가'를 말하는 그 한 줄이다.
> 그걸 흐리면 유저는 '뭔가 준비 중이구나'만 읽고 '뭐가?'는 영영 못 읽는다."*

### 처방 — 두 잉크를 밝힌다 (계산으로 최소치를 냈다)

| 토큰 | 지금 | **제안** | 배율 | 10개 표면 최악 | 판정 |
|---|---|---|---|---|---|
| 라벨 `#6E665C` | 최악 3.12 | **`#8A8073`** | ×1.25 | **4.54 : 1** | PASS |
| 비활성 `#5C574E` | 최악 2.46 | **`#878073`** | ×1.47 | **4.50 : 1** | PASS |

> 색조(따뜻한 회갈색)는 유지하고 밝기만 올린 값이다. 다만 **두 값이 서로 가까워져**
> (`#8A8073` / `#878073`) 라벨과 비활성이 구별되지 않는다 — 즉 **이 팔레트의 잉크 서열은
> 6단이 아니라 5단까지만 성립한다.** 우리 앱은 이미 그 결론에 도달해 **4단**(기본/보조/3차 + 아이콘)만
> 쓰고 있고, `Ink()` 문서가 *"Meta가 안 내려가는 것은 실수가 아니다 — 하한. 더 내릴 곳이 없다"*라고
> 못 박고 있다.
> **→ 리더 결정 요청: 잉크 서열을 6단 → 5단으로 줄이고 `#5C574E`를 통째로 폐기하는 안을 권고한다.**

## 1-2. ★ "누를 수 있는 것"의 **면** — 오늘 밤 `[✕]`가 걸린 그 검사

우리 규칙: `ControlFaceContrastTarget = 3.60`(면) + `ControlInkContrastTarget = 5.175`(그 위 글자).
**둘을 언제나 한 쌍으로 잰다** — 면만 재면 붕괴를 못 본다.

| 컨트롤 | 면 / 바탕 | 면 대비 | 잉크 대비 | 판정 |
|---|---|---|---|---|
| 버튼 `착용`(황동) | `#C8A15A` / `#161311` | **7.66** | 7.87 | **PASS** |
| 탭 활성(황동) | `#C8A15A` / `#141210` | **7.74** | 7.87 | **PASS** |
| **버튼 `동전 3,200`** | `#17130E` / `#161311` | **1.00** | 7.66 | **FAIL — 면이 없다** |
| **버튼 `미보유`(프리셋)** | `#17130E` / `#161311` | **1.00** | **2.58** | **FAIL — 면도 글자도 없다** |
| 버튼 `해제` | `#2A2119` / `#161311` | **1.17** | 6.54 | FAIL |
| 토글 `꺼짐` | `#231F1B` / `#141210` | **1.14** | — | FAIL |
| 토글 노브(꺼짐) | `#6E665C` / `#231F1B` | **2.90** | — | FAIL(3.00) |
| 착용행(빔) | `#111010` / `#0D0C0B` | **1.03** | **2.65** | FAIL |
| 동전 칩(헤더) | `#17130E` / `#0D0C0B` | **1.06** | 7.66 | 읽기 전용이면 면제 |

**대조**

| | 면 대비 |
|---|---|
| 오늘 밤 고치기 **전**의 우리 `[✕]` | **1.00 : 1** |
| **핸드오프 `동전 N` 버튼 / `미보유` 버튼** | **1.00 : 1** |
| 고친 뒤 우리 `[✕]`(`ChromeButtonSurface`) | 5.27 : 1 |
| (미수정) 우리 카드 `[착용]` 버튼 | 1.35 : 1 |

> ★ **패턴이 보인다: 이 팔레트에서 면을 가진 컨트롤은 "황동으로 칠한 것" 하나뿐이다.**
> 황동이 아닌 모든 버튼은 **1px 테두리만으로** 버튼임을 주장한다. 우리 `UiChrome`이
> **왜 테두리가 아니라 면인가**를 세 가지 실측 근거로 못 박아 둔 그 결정과 정면으로 어긋난다:
> ① 배율 1에서 1px는 물리 1픽셀(커밋 39ab690에서 이미 한 번 잃었다) ② 생 알파 테두리는 그 화소의
> **창 알파를 0.91로 떨어뜨려 바탕화면이 9% 비친다** ③ 테두리만 있는 것은 버튼이 아니라 **입력칸**으로 읽힌다.
>
> **처방**: 황동이 아닌 버튼 3종(`해제` / `동전 N` / `미보유`)의 면을 `ControlFaceOnSurface(카드바탕)`으로
> 띄운다. 계산된 값 = **`#838689` (면 4.51 : 1 / 잉크 `#0B1016` 5.22 : 1)** — 단 이건 **중성 회색**이라
> 브라스 팔레트와 색조가 맞지 않는다. **황동 색조를 유지하면서 3.60을 넘기는 면을 디자이너가
> 다시 골라 주는 편이 낫다**(예: `#17130E` → 밝은 브라운 계열). 내가 색을 고르지 않고
> **하한만 넘긴다**: 카드 `#161311` 위에서 **면 ≥ 3.60**, 그 위 글자 **≥ 5.175**.

## 1-3. 비텍스트(하한 3.00) — **의미를 지는 것만** 골라서 잰다

> 순수 장식(구분선·기본 카드 보더·스크롤 thumb)은 WCAG 1.4.11 대상이 아니므로 **미달로 세지 않는다.**
> ★ 그리고 **공정성 대조**: `카드 면이 창과 1.06:1`은 **우리 앱도 같다**(`CardSurface` 대
> `PanelSurface` = **1.09 : 1**). 이건 핸드오프의 결함이 아니다 — 반려 사유로 쓰지 않는다.

| 의미를 지는 그래픽 | 대비 | 3.00 | 왜 의미를 지는가 |
|---|---|---|---|
| 카드 보더(착용) `#C8A15A`/`#1D1813` | **7.30** | PASS | "지금 입고 있다" |
| 게이지 채움 `#C8A15A`/`#221E1A` | **6.86** | PASS | 스탯 총량 |
| 등급 리본 4종 (보유) | 5.69 / 6.62 / 5.95 / 9.37 | PASS | 등급 |
| 착용 오버레이 4종 | 9.76 / 8.67 / 7.74 / 11.61 | PASS | 프리뷰 장비 |
| **카드 보더(선택) `#4A4036`/`#161311`** | **1.83** | **FAIL** | "지금 고른 카드" — **유일한 선택 신호** |
| **세트 진행 도트(미달) `#2A2622`** | **1.23** | **FAIL** | `3/4`를 **세려면** 빈 칸이 보여야 한다 |
| **세트 효과 마커 `○` `#332E28`** | **1.38** | **FAIL** | 활성/비활성 효과 행 구분 |
| **게이지 트랙 `#221E1A`/`#161311`** | **1.12** | **FAIL** | 채움의 상한(= 40) |
| **캐릭터 머리 `#111111`/무대 `#0F0D0C`** | **1.03** | **FAIL** | §3 참고 — **머리가 사라진다** |
| 망토 뒤판 `#A8332A`/무대 | 2.93 | FAIL(근소) | — |
| 망토 선 `#7E1F17`/뒤판 | 1.51 | FAIL | 이 선은 아무 일도 안 한다 |
| **미보유 등급 리본 `opacity .25`** | 1.45 / 1.50 / 1.46 / **1.69** | **FAIL** | 미보유 아이템의 등급 |

## 1-4. ★ 최악의 단일 수치 — 미보유 아이템 이름

README: *"미보유: 아이콘·이름 dim (`#6E665C`, opacity .34)"*

```
합성색 = #6E665C × 0.34 + #161311 × 0.66 = #342F2A
#342F2A 대 카드 #161311 = 1.40 : 1        ← 텍스트 하한 4.50의 31%
```

**상점에서 팔려는 물건의 이름이 1.40 : 1이다.** 그리고 이건 `opacity`라
**그 화소의 창 알파까지 함께 내려간다**(우리 오버레이 창 기준). 밝은 바탕화면 위에서는 더 나빠진다.

> **처방**: dim을 `opacity`가 아니라 **미리 합성한 α=1 색**으로 하고, 하한은 유지한다.
> 우리 앱의 선례가 정확히 이것이다 — 잠긴 카드의 이름은 `InkTitle(owned:false)` = `TextSecondary`
> **7.9 : 1**이고, 흐려지는 것은 **아이콘(비텍스트 3.0)**이지 이름이 아니다.
> **"모른다"를 흐림으로 표현하되, 글자는 흐리지 않는다.**

## 1-5. 등급 램프 — **휘도 서열이 뒤집혀 있다** (내 설계와의 병합 지점)

등급이 오를수록 눈에 띄어야 한다. 아니면 **그림이 낱말과 다른 말을 한다**.

| 등급 | 핸드오프 | 상대휘도 L | 흑백 근사 | |
|---|---|---|---|---|
| 일반 | `#8A8F98` | 0.2732 | 141 | |
| 희귀 | `#6E9BE8` | 0.3258 ↑ | 153 | |
| **영웅** | `#B07BE0` | **0.2878** | **145** | **★ 희귀보다 어둡다 — 역전** |
| 전설 | `#E0B24A` | 0.4818 ↑ | 183 | |

**흑백으로 누르면 141 / 153 / 145 / 183** — 희귀와 영웅이 자리를 바꾼다.
전색맹에서는 **영웅이 희귀보다 낮아 보인다.**

**착용 오버레이 램프는 더 심하다** (3/4가 역전):

| 등급 | 색 | L | |
|---|---|---|---|
| 일반 | `#D8B27A` | **0.4784** | ★ 넷 중 둘째로 밝다 |
| 희귀 | `#7FB0F2` | 0.4197 | ★ 역전 |
| 영웅 | `#C08FEC` | **0.3691** | ★ 역전 — **넷 중 가장 어둡다** |
| 전설 | `#F0C25C` | 0.5788 | |

> **처방**: 등급 낱말(`일반/희귀/영웅/전설`)이 카드에 **글자로도** 있으므로 치명적이지는 않다
> (핸드오프의 이중 표기가 여기서 값을 한다). 그러나 **리본·오버레이만 보이는 자리**(프리뷰 위 장비,
> 미보유 카드)에서는 서열이 거짓이 된다.
> **영웅을 한 단 밝히면 해결된다** — 내 선행 설계의 `#C39BF5`(L=0.4164)를 쓰면
> 일반 0.2732 < 희귀 0.3258 < **영웅 0.4164** < 전설 0.4818로 **단조가 성립한다**(카드 대비 7.34:1).
> 색상(보라)은 그대로다.

---

# 2. ★ 창 크기 — 1242 × 802가 데스크톱 오버레이에서 타당한가

## 2-1. 실측 (`ClampPanelToScreen`: `min(설계값, max(320, 화면논리 − 32))`)

| 환경 | 논리 해상도 | 가용 | 현행 880×861 | **핸드오프 1242×802** |
|---|---|---|---|---|
| mac 1512×982 (@2x) | 1512 × 982 | 1480 × 950 | OK | **OK** |
| Win 2560×1600 @100% | 2560 × 1600 | 2528 × 1568 | OK | **OK** |
| Win 2560×1600 @125% | 2048 × 1280 | 2016 × 1248 | OK | **OK** |
| Win 2560×1600 @150% | 1707 × 1067 | 1675 × 1035 | OK | **OK** |
| Win 2560×1600 @175% | 1463 × 914 | 1431 × 882 | OK | **OK** |
| **Win 2560×1600 @200%** | 1280 × 800 | 1248 × 768 | 세로 **−93** | 세로 **−34** / 가로 **여유 6pt** |
| **Win 1920×1080 @150%** | 1280 × 720 | 1248 × 688 | 세로 **−173** | 세로 **−114** |

**세로 802는 현행 861보다 낫다** — 모든 환경에서 잘림이 **줄어든다**(−93 → −34, −173 → −114).
**세로는 채택.**

## 2-2. ★ 가로 1242의 진짜 위험은 "잘림"이 아니라 **점유율**이다

이 창은 **바깥 클릭으로 안 닫힌다**(사용자 확정). 탈출구는 `[✕]` 하나다.

| 환경 | 현행 880×861 | **핸드오프 1242×802** |
|---|---|---|
| mac 1512×982 | 51.0% | **67.1%** |
| Win 2560×1600 @150% | 41.6% | 54.7% |
| **Win 2560×1600 @200% (1280×800)** | 74.0% | **97.3%** |
| **Win 1920×1080 @150% (1280×720)** | 82.2% | **108.1%** ← 화면보다 크다 |

2560×1600 16" 패널의 **Windows 권장 배율은 200%**다. 그 환경에서 이 창은 **화면의 97.3%를 덮고**,
사용자가 되돌아갈 수 있는 유일한 길이 오른쪽 위 `[✕]` 하나가 된다.
**원칙 2를 정면으로 시험하는 조합이다.**

## 2-3. ★ 핸드오프에 **리플로우 규칙이 없다** — 이게 진짜 구멍이다

README는 *"창 크기와 헤더는 탭에 따라 변하지 않는다"*라고만 쓰고, 컬럼을 **고정 px**로 못 박았다
(`306px / 292px / 1fr`). 그런데 우리 코드는 화면이 작으면 **패널을 줄인다.** 줄었을 때 무슨 일이
일어나는지가 **정의돼 있지 않다.** 고정 폭 컬럼 2개(306 + 292 = 598)는 안 줄고 `1fr`만 줄어든다.

**내가 계산한 임계점** (카드 폭 188 유지 기준):

| 구성 | 필요 창 폭 |
|---|---|
| 3열 카드 (설계 상태) | **1242** |
| **2열 카드** | **1042** |
| 컬럼2까지 접고 1+3열 | 748 |

**제안 리플로우 규칙 (디자이너 확인 필요)**
```
창 폭 ≥ 1242  →  3컬럼 / 카드 3열   (설계 그대로)
1042 ~ 1241   →  3컬럼 / 카드 2열   (1fr만 줄고 컬럼 1·2는 고정)
< 1042        →  ★ 미정 — 디자이너가 정해야 한다
```
그리고 **세로**: 802 미만이면 컬럼 3만 스크롤한다는 규칙은 있는데, **컬럼 1·2가 잘리면**
(`margin-top:auto`로 바닥에 붙은 **아이템 상세 카드**와 **테마 세트 패널**이 먼저 사라진다)
어떻게 되는지가 없다. Win@200%에서 34pt가 잘리므로 **실제로 일어난다.**

## 2-4. 판정

> **세로 802 채택. 가로 1242는 "설계 폭"으로 채택하되 리플로우 규칙을 받아야 구현 가능하다.**
> 그리고 **점유율 97%가 나오는 환경이 실재한다**는 사실을 사용자에게 전달해 주기 바란다 —
> 이건 내가 단독으로 정할 문제가 아니다(창 크기는 제품 결정이다).
> 내 쪽 대안 1건: **가로만 1242 → 1042로 줄이면**(카드 2열) Win@200%에서 점유율이
> **97.3% → 81.6%** (1,042 × 802 = 835,684 pt² / 1,024,000 pt²)로 내려가고, 카드 열 수 외의 모든 사양이 그대로 산다.

---

# 3. ★ 캐릭터 색 — 창 안에서만 흰 캐릭터가 되는 것이 옳은가

## 3-1. 판정: **아니다. 잉크는 유저 것이고, 무대가 잉크를 따라가야 한다.**

## 3-2. 근거 — 사실부터

| # | 사실 | 출처 |
|---|---|---|
| 1 | 캐릭터 잉크는 **유저 설정**이다(흑/백 2택, 기본 Black). 정보창 좌측 컬럼에 **스와치 2개**가 있고 클릭하면 바뀐다 | `CharacterAppearanceModel.InkColor` / `CharacterInfoWindow.BuildLeftColumn` `InkBlack`·`InkWhite` |
| 2 | **무대 바탕은 이미 잉크를 따라 뒤집힌다** | `CharacterPortraitStage.ResolveBackdropColor`: 흰 잉크 → 목탄 `#25282E` / 검은 잉크 → 종이 `#E9EAE6`. 함수 주석: *"흰 잉크에 흰 종이면 선이 보이지 않는다"* |
| 3 | 우리 머리는 **채운 원이 아니라 링(획)**이다 | `StickmanMetrics.cs:30` — *"머리 반경 : `Head/HeadOutline` 링 LineRenderer의 첫 점 x"* |
| 4 | 초상화는 **거울이 아니라 종이인형**으로 확정됐다 | `UX_FLOW` §45-1 판정 |

## 3-3. 핸드오프의 조합을 재 봤다 — **그 안에서 이미 깨진다**

| 조합 | 대비 |
|---|---|
| 핸드오프: 흰 선 `#FFFFFF` / 무대 `#0F0D0C` | 19.39 : 1 ✔ |
| **핸드오프: 검은 머리 `#111111` / 무대 `#0F0D0C`** | **1.03 : 1 ✘** |
| 핸드오프: 흰 눈 / 검은 머리 | 18.88 : 1 |
| 우리(검은 잉크): 선 `#111111` / 종이 `#E9EAE6` | 15.62 : 1 ✔ |
| 우리(흰 잉크): 선 `#FFFFFF` / 목탄 `#25282E` | 14.77 : 1 ✔ |

**핸드오프의 무대 위에서 머리는 1.03 : 1로 사라진다.** 남는 것은 **흰 눈 두 개와 흰 팔다리**뿐이다.
*"흰 선 + 검은 머리로 고정"*이라는 규칙은 **밝은 배경을 전제로 한 규칙**인데 무대를
`#0F0D0C`로 깔아서 자기모순에 빠졌다. (README가 *"게임 원본 규칙"*이라 부른 것도 우리 사실과
다르다 — 원본 규칙은 **유저가 고르는 잉크**이고 기본이 Black이다.)

## 3-4. 확정 처방

```
무대 바탕 ← 잉크에서 파생  (ResolveBackdropColor — 이미 구현돼 있다. 그대로 둔다)
   검은 잉크(기본) → 밝은 무대.  핸드오프의 브라스 톤을 쓰려면 밝은 브라스 종이색으로 다시 고를 것
   흰 잉크        → 어두운 무대. 핸드오프의 #0F0D0C radial 그대로 쓰면 된다
머리 채움 ← 잉크색과 같게.  눈 ← 무대색과 같게 (지금은 머리=검정 / 눈=흰색으로 고정돼 있다)
```

> ★ **§45 판정이 이 결론을 강제한다.** 초상화가 **종이인형**(= 지금 입고 있는 것을 보여주는 그림)
> 이라면, 그 인형은 **같은 인형**이어야 한다. 바탕화면의 검은 스틱메이트를 보다가 창을 열었더니
> 흰 스틱메이트가 서 있으면 그건 **다른 캐릭터의 종이인형**이다.
> 그리고 잉크 스와치 2개가 **바로 그 창 좌측에** 있다 — 유저가 방금 고른 색을 그 창이 무시한다.

> ★ **조형 충돌 1건은 내 영역이 아니다 — `design-character`로 넘긴다(리더 경유).**
> 핸드오프는 머리를 `circle r=28 fill #111111`(**채운 원**)로 그리고, 우리는 **링(획)**이다.
> 이건 색 문제가 아니라 **머리 구조**이고 CLAUDE.md가 그 담당을 명시하고 있다.

---

# 4. 폰트 — 세리프/산스 페어가 없다

## 4-1. 사실

| | 핸드오프 | 우리 |
|---|---|---|
| 제목·수치 | **Noto Serif KR** 600 | — |
| 본문·UI | **Pretendard** 400~600 | — |
| 실제 | 웹폰트 CDN 2벌 | **`LegacyRuntime.ttf` 1벌**(`UiChrome.Font`가 무조건 이것) |
| 한글 | 위 두 벌에 포함 | **`LegacyRuntime.ttf`에 한글 글리프가 없다**(`DialogueBubbleRenderer.cs:108` 주석). 창 UI는 Unity의 글리프 단위 OS 폴백에 의존 — macOS에서 `AppleSDGothicNeo-Heavy` |
| OS 폴백 탐색 | — | **`DialogueBubbleRenderer`에만 있다**(`Font.GetOSInstalledFontNames` → 후보 순회 → 글리프 실측). **`UiChrome`에는 없다** |

## 4-2. 무엇이 무너지는가 — 숫자로

**(a) 위계 채널 하나가 통째로 사라진다.** 핸드오프는 *"제목·수치 = 세리프 / 본문 = 산스"*로
계열을 나눈다. 두 계열이 **같은 px를 공유하는 크기가 `13px`**이고, 그 자리에서는 **서체만이 유일한
구분**이다. 폰트가 하나면 그 두 요소가 **완전히 같아진다.**

**(b) 타입 스케일 13계단 중 11계단이 배율에서 비정수다.**

| px | ×1.25 / ×1.5 / ×1.75 / ×2.0 | |
|---|---|---|
| 16 | 20 / 24 / 28 / 32 | OK |
| 12 | 15 / 18 / 21 / 24 | OK |
| **12.5** | 15.625 / 18.75 / 21.875 / 25 | FAIL |
| **10.5** | 13.125 / 15.75 / 18.375 / 21 | FAIL |
| **9.5** | 11.875 / 14.25 / 16.625 / 19 | FAIL |
| 21 / 17 / 15 / 14 / 13 / 11.5 / 11 / 10 | 전부 비정수 | FAIL |

> **공정하게 말한다: 우리 계단도 완벽하지 않다.** 20 / 12는 통과하지만 **14 / 10은 실패**한다
> (14 → 17.5, 10 → 12.5). 즉 이건 핸드오프만의 문제가 아니라 **우리가 이미 안고 있는 문제**다.
> 다만 우리는 **4계단 중 2건**이고 핸드오프는 **13계단 중 11건**이다.

**(c) 9.5px 한글.** 우리가 출하한 최소는 `FontCaption 10`이고 그것도 *"텍스트가 선명하지 않다"*는
신고를 받은 적이 있다(`UI_SURFACE_SPEC` §11). 그리고 macOS 폴백이 **Heavy 굵기**라 작은 한글에서
속공간이 먼저 막힌다. **9.5px 한글은 이 환경에서 검증된 적이 없다 — 미확인이 아니라 위험이다.**

## 4-3. 처방

1. **계단을 우리 4단(20 / 14 / 12 / 10)에 매핑한다.** 13계단 → 4~5계단.
   대응표는 디자이너가 확정해 주는 편이 좋다(내가 임의로 매핑하면 위계가 뒤틀린다).
2. **9.5px 자리는 10으로 올린다** — 그 자리들(영문 라벨 `STATUS`/`PREVIEW`, `모자 · 집중력`)은
   전부 대문자 영문 또는 짧은 한글이라 1단 올려도 폭 예산이 남는다.
3. **세리프/산스 위계는 폰트가 아니라 다른 축으로 옮긴다** — 굵기(합성 볼드)와 **크기 단차**.
   핸드오프의 `13px 세리프 vs 13px 산스`처럼 **크기가 같은 짝은 반드시 한 단 벌린다.**
4. (선택) `UiChrome`에도 `DialogueBubbleRenderer`의 OS 폴백 탐색을 이식하면
   **`Pretendard`/`Noto Serif KR`이 설치된 사용자에게는 실제로 쓸 수 있다.** 없으면 지금대로 폴백.
   **이건 코드 구조 영향이라 리더 배정 사항이다.**

---

# 5. 내 선행 설계와 핸드오프의 차이 — 어느 쪽인지는 리더가 판단

> 내 설계 = `docs/UX_SHOP_AND_CURRENCY.md`. **버리라는 뜻이 아니라, 이미 잰 숫자를 붙여 둔다.**

| # | 항목 | 내 설계 | 핸드오프 | 내 의견 |
|---|---|---|---|---|
| 5-1 | 창 | 880×861(탭별 705) | 1242×802 **고정** | **핸드오프.** 세로가 더 낫고, 탭별 높이 변동(P0-1)이 사라져 §7-5의 `max()` 가드 문제도 함께 없어진다 |
| 5-2 | 탭 조형 | 밑줄 | 세그먼트 칩(활성만 황동면) | **핸드오프.** 단 비활성 탭 면 1.00 — 컨테이너 보더(1.18)가 유일한 그룹 경계다 |
| 5-3 | **재화 위치** | 탭 스트립 오른쪽 끝 | **헤더 우측 칩** | **핸드오프 채택 — 내 §2-4를 철회한다.** 내 반려 사유는 *"타이틀바에 `[설정]`·`[✕]` 크롬 칩이 이미 둘이라 세 번째 버튼으로 읽힌다"*였는데, 핸드오프 헤더에는 그 둘이 없고 탭이 중앙에 있다. **전제가 달라졌으므로 결론도 바뀐다** |
| 5-4 | **톱니 근처 노출** | **반려**(4.37/4.18 < 4.50) | *"미확정 — 현재는 창 헤더에만"* | **내 판정 유지 = 반려.** 핸드오프도 이 판단을 UX로 넘겼다(README "미확정" 절). 근거는 `UX_SHOP_AND_CURRENCY` §2-2 4건 |
| 5-5 | 등급 표기 | 썸네일 좌상단 배지(낱말+면) | 상단 **리본 2px** + 우측 **등급 낱말** | **핸드오프.** 이중 표기가 더 낫고 썸네일을 안 가린다. 단 미보유 리본 `opacity .25` → **1.45~1.69 미달** |
| 5-6 | 등급 램프 | `#8B939F`/`#3EB3A9`/`#C39BF5`/`#F0B84A` | `#8A8F98`/`#6E9BE8`/`#B07BE0`/`#E0B24A` | **핸드오프 기본 + 영웅만 내 값.** 핸드오프는 **영웅이 희귀보다 어둡다**(§1-5). 내 `#C39BF5`로 바꾸면 단조가 성립한다. 내가 희귀를 청록으로 옮겼던 이유(우리 `Accent` 파랑과 충돌)는 **핸드오프의 악센트가 황동이라 사라진다** — 희귀는 파랑으로 되돌린다 |
| 5-7 | 임계 표시 | 세그먼트 3칸, **색(영구)×높이(현재) 2채널** | 게이지 + 눈금 3 + 단계 칩. **"해금 취소 표현은 하지 않음"** 명시 | **★ 충돌. 리더 판단 필요.** 내 3상태는 *"임계는 영구인데 값은 내려간다"*를 그림으로 정직하게 말하려던 것이고, 핸드오프는 *"현재 단계만 보여주고 해금 취소를 표현하지 않는다"*로 **의도적으로 단순화**했다. 핸드오프가 더 단순하지만, **값이 내려간 유저는 "내 연출이 사라졌나?"를 확인할 방법이 없다.** 절충안: 게이지·단계 칩은 핸드오프대로 두고, **한 번 넘긴 눈금만 황동으로 남긴다**(눈금 3개는 이미 있다 — 새 요소 0개) |
| 5-8 | 세트 | 도트 4 + 파생 문구 1줄 | 진행 도트 4 + **효과 4행 패널** + 주석 | **핸드오프.** 훨씬 풍부하고, *"세트는 4부위 착용 중에만 / 임계는 영구"* 주석이 5-7의 혼동을 직접 해소한다 |
| 5-9 | **상점 탭 내용** | 장비와 같은 4섹션 카드 그리드 | **좌: 동전 단품 리스트 / 우: DLC 팩 2열** | **핸드오프.** 내 안은 *"창이 880이라 카드 그리드밖에 못 넣는다"*는 제약에서 나온 것이고, 1242에서는 그 제약이 없다. DLC를 **팩 단위**로 파는 것이 아이템 낱개보다 정직하다 |
| 5-10 | 보관함 | 20행 목록 | **4열 그리드 + 판매 환급 + 세트 프리셋** | **핸드오프.** 단 판매 환급(구매가×0.4)과 스탯 재분배는 **경제 신규 항목**이라 `design-systems` 확인 필요 |
| 5-11 | 구매 확인 | 버튼 2단(`정말 살까요?` 3초) | **없음**(1클릭 확정) | **내 설계 유지 권고.** 재화 소모는 되돌릴 수 없고, 이 창은 **바깥 클릭으로 안 닫힌다**. 핸드오프는 확인 단계를 다루지 않았다(누락으로 보인다) |

## 5-12 ★ 핸드오프 ↔ `design-systems` 수치 충돌 6건 (내 영역 밖 — 리더가 조정)

| 항목 | `ECONOMY_SPEC` | 핸드오프 | 차이 |
|---|---|---|---|
| 임계 3단계 | **6 / 12 / 18**, 캡 20 | **10 / 20 / 32**, 게이지 상한 40 | **2배** |
| 등급 주스탯 | 3 / 5 / 7 / **10** | 3 / 6 / 10 / **15** | 전설 1.5배 |
| 등급 부스탯 | (미기재) | 1 / 2 / 4 / 6 | — |
| 가격 | 30 / 70 / 150 / 330 | **600 / 1,400 / 3,200** | **약 20배** |
| 세트 완성 보너스 | **4스탯 +2** | **총합 +8** | 4배 |
| **유예 자동 해금** | **있다** (5 / 15 / 35 / 80시간) | **없다** (동전 아니면 못 산다) | **구조가 다르다** |

> ★ 마지막 줄이 가장 크다. `ECONOMY_SPEC` 4-1은 *"동전은 해금이 아니라 **앞당기기**"*이고
> 그 근거가 **이미 출하된 문구**다: *"`Lv.5에서 열립니다`는 이미 출하된 문구다. 동전이 없으면
> 못 산다는 두 번째 벽을 세우면 그 문구가 유저를 속이게 된다."*
> 핸드오프의 상태 매트릭스(`동전 3,200으로 해금`)는 **그 두 번째 벽 그 자체**다.
> **둘 중 하나를 골라야 하고, 고른 쪽이 카드 메타 칸의 내용을 바꾼다**(유예 모델이면 남은 시간이 들어간다).

---

# 6. 【판정】 `[행동]` 팝오버 560 → **508** — 승인

## 6-1. coder의 산술을 검산했다 — **맞다**

```
콘텐츠 높이 = Height − (Space3 + 22 + Space2) − Space4 = 508 − 42 − 16 = 450
푸터 바닥   = FooterY − QuitButtonHeight        = −422 − 28    = −450     ✔ 일치
(종전) 560 − 42 − 16 = 502  /  −474 − 28 = −502                          ✔ 일치
```
타일 1개 = `RowHeight 52`. 508 + 52 = 560. **정확히 한 행이다.**

## 6-2. 판정: **508 승인.** 근거 3건

**(1) `설정창과 같은 560`은 애초에 설계 제약이 아니라 우연이었다.** 두 값 다 **내용에서 파생**된다:
```
설정창 560 = HeaderHeight 48 + TabBarHeight 40 + ContentHeight 426 + FooterHeight 46
행동창 560 = 42(머리) + 콘텐츠 502 + 16(꼬리)   ← 콘텐츠가 타일 수에서 나온다.
             타일 1개(RowHeight 52)가 빠지면 502 → 450 이 되어 즉시 깨진다
```
**서로 독립적으로 계산된 두 값이 같은 수에 착지한 것**이지, 하나가 다른 하나를 참조한 적이 없다.
참조 관계가 없는 일치는 **유지할 수 없다** — 어느 한쪽 내용이 바뀌는 순간 끝난다. 실제로 끝났다.

**(2) 560을 유지하면 P0-1을 다시 저지르는 것이다.** P0-1이 고친 결함이 정확히
*"없는 콘텐츠의 자리를 예약해 176pt를 비워 둔 것"*이었다. 여기서는 **52pt**다.
같은 창에서 두 달 안에 두 번 같은 실수를 하는 셈이 된다.

**(3) 가족 신호는 세로 치수가 아니라 다른 것이 지고 있고, 면적 서열도 유지된다.**

| 창 | 면적 | 서열 |
|---|---|---|
| 정보창 880×861 | 757,680 pt² | 1 |
| 설정창 720×560 | 403,200 pt² | 2 |
| **행동창 480×508** | **243,840 pt²** | 3 ✔ |

가로 480은 그대로이고, 반경·간격 토큰·타이포는 `UiChrome` **한 벌**이다.
세로 일치는 그 셋 중 **가장 약한 신호**라는 coder의 판단에 동의한다.

## 6-3. ★ 덤 — 508이 비침해에도 **이득**이다 (coder가 안 적은 것)

이 팝오버는 **보이지 않는 클릭 차단막**을 같은 크기로 깐다. 480×560 → 480×508이면
차단막 면적이 **268,800 → 243,840 pt², −24,960 pt² (−9.3%)** 줄어든다.
100% 관통이던 화면에 뚫는 구멍이 줄어드는 것이므로 **원칙 2 쪽으로 이득**이다.

## 6-4. 함께 고쳐야 할 것 (구현 시)

`ActionCommandPopover.cs`에 **560을 문장으로 적은 주석이 최소 3곳** 남아 있다
(`480×560 차단막` / `화면 상단에서 아래로 560pt` 등). 값은 바뀌었는데 문장이 남으면
다음 사람이 그 문장을 근거로 판단한다. **주석의 560도 함께 508로 고친다.**
(§43-1이 `SettingsWindow.cs:1058` 한 줄을 고친 것과 같은 종류의 정리다.)

---

# 7. 리더 결정 요청 (전부 내 권한 밖)

| # | 결정 | 내 권고 | 근거 |
|---|---|---|---|
| **H-1** | 잉크 `#6E665C` / `#5C574E`를 어떻게 할 것인가 | **밝혀서 `#8A8073` 하나로 합치고 서열을 6단 → 5단으로 줄인다** | 두 색 다 어떤 표면에서도 4.50 불가(최댓값 3.46 / 2.73). 살리면 서로 구별이 안 된다 (§1-1) |
| **H-2** | 황동이 아닌 버튼 3종의 면 | **디자이너에게 되돌린다** — 카드 위에서 **면 ≥ 3.60 / 그 위 글자 ≥ 5.175**를 만족하는 브라스 톤을 골라 달라 | `동전 N` 버튼 면 **1.00 : 1** = 오늘 밤 `[✕]`와 같은 숫자 (§1-2) |
| **H-3** | 미보유 dim `opacity .34` | **`opacity` 폐기.** 아이콘만 흐리고 **이름은 안 흐린다**(우리 선례: 잠긴 카드 이름 7.9 : 1) | 합성 후 **1.40 : 1** — 이 문서 최악값 (§1-4) |
| **H-4** | 창 가로 1242 | **리플로우 규칙을 받아야 구현 가능.** 그리고 Win@200%에서 **97.3% 점유**를 사용자에게 알려 달라. 대안: 1042(카드 2열) | §2-2 / §2-3 |
| **H-5** | 캐릭터 색 | **무대가 잉크를 따라간다(현행 유지).** 흰 잉크에서만 핸드오프의 어두운 무대를 쓴다 | 핸드오프 조합에서 **머리가 1.03 : 1로 사라진다** (§3-3) |
| **H-6** | 머리 = 채운 원 vs 링 | **`design-character`로 라우팅** | CLAUDE.md가 "머리 구조"를 그쪽 담당으로 명시 |
| **H-7** | 타입 스케일 13계단 | **우리 4단(20/14/12/10)으로 매핑표를 디자이너에게 요청.** 9.5px 자리는 10으로 | 13계단 중 11건 배율 비정수, 9.5px 한글 미검증 (§4-2) |
| **H-8** | 임계 "해금 취소 표현 안 함" | **절충 권고**: 게이지·단계 칩은 핸드오프대로, **한 번 넘긴 눈금만 황동으로 남긴다**(눈금 3개는 이미 있다 = 신규 요소 0) | §5-7 |
| **H-9** | 핸드오프 ↔ `design-systems` 수치 충돌 **6건** | **리더가 조정.** 특히 **유예 자동 해금의 유무**는 카드 메타 칸 내용을 바꾼다 | §5-12 |
| **H-10** | 구매 확인 단계 | **내 2단 버튼 유지 권고** — 핸드오프에 확인 단계가 없다 | 재화 소모는 비가역 + 이 창은 바깥 클릭으로 안 닫힌다 |
| **H-11** | 톱니 근처 동전 노출 | **반려 유지** | 헤일로 보장 4.37 / 4.18 < 4.50, 히트 사각형 +138% (§5-4) |
| **H-12** | `[행동]` 팝오버 508 | **승인** | §6 |

---

# 8. 플랫폼 영향

| | 내용 |
|---|---|
| **Windows 영향** | **함께 검토함 — 이 문서의 핵심 절반이 Windows 전용 발견이다.** §2-1 / §2-2의 잘림·점유율 문제는 **Windows DPI 200%에서만** 나타난다(mac은 ×1/×2뿐이라 1512×982에서 안전). §4-2의 배율 비정수도 Windows DPI 사다리(1.25/1.5/1.75) 기준이다. **별도 배정 불필요 — 대상 파일에 `#if UNITY_STANDALONE_*` 분기 0건** |
| **macOS 영향** | 대비·폰트·캐릭터 색 판정은 양 플랫폼 공통. §4-1의 한글 폴백은 **macOS는 `AppleSDGothicNeo-Heavy`로 확인**됐고 **Windows 폴백 폰트는 미확인**(맑은 고딕으로 추정되나 재지 않았다) |
| **미확인** | 브라우저 렌더 캡처를 못 했다(환경에 브라우저 없음) — 색은 선언값 계산. Windows 실제 작업영역(작업표시줄 제외 크기)을 재지 않았다 — `Screen.height`가 작업표시줄을 뺀 값인지 아닌지에 따라 §2-1의 여유가 **더 줄 수 있다** |

---
---

# 【제2부】 채움색 판정 — 안 A / B / C (2026-09-03, ux-designer)

> 발단: 리더가 제1부의 구멍을 찾았다 — **제1부는 창 UI만 봤고 `ItemIcon` 16종 · 아이템 아트는
> 히트 0건이었다.** 그 사이 팀 전체가 인계본보다 하루 빠른 문서 위에서 도형을 고쳤다.
> 이번 라운드는 `docs/TEAM.md`의 신설 **디자인 인계 접수 절차**를 내가 먼저 지킨다 —
> **목록부터 만들고, 리뷰는 그 표를 소진해야 끝나고, 「무엇을 안 봤는가」를 반드시 적는다.**
>
> **프로덕션 `.cs` 수정 0건. Unity 배치모드·앱 실행 안 함.**

## 0. 한 줄 결론

> **안 C를 권고한다. 단 「카드색」의 범위는 리본·라벨·테두리까지이고 바탕면은 물들이지 않는다(C1).**
>
> **결정적 근거**: 카드 바탕면을 등급색으로 물들이는 순간(C3) 두 수치가 **동시에** 반대 방향으로 움직인다 —
> 3차 잉크 `#8B939F`를 4.50 위에 유지하는 틴트 상한은 **α = 0.0754**인데,
> **그 α에서 인접 등급끼리의 ΔE는 1.44 ~ 1.71**로 변별 하한 7.8의 **5분의 1**이다.
> 최대 쌍(일반↔전설)조차 **4.81**이다.
> ⇒ **C3는 글자 대비를 내주고 등급 신호를 하나도 사지 못한다.** 낼 것만 있고 받을 것이 없는 거래다.
> 바탕면을 뺀 C1은 등급 채널 3개(리본 칸 수 · 리본 색 · 라벨 낱말)를 그대로 두고
> **아이템 그림과 착용 장비만 재질색으로 돌린다** — 그 둘은 우리 구조에서 **같은 색이어야만 하는 자리**다(§2-3).

## 0-A. ★ 접수 목록 — 인계본 전수표 (이 표를 소진해야 리뷰가 끝난다)

인계본 = `docs/handoff/design_handoff_equipment_window/` (파일 3 / 112,446 bytes).

| # | 항목 | 제1부 | **제2부(이번)** | 비고 |
|---:|---|:---:|:---:|---|
| 1 | `README.md` 프레임·레이아웃 | ✔ | — | |
| 2 | 헤더 12요소 | ✔ | — | |
| 3 | 컬럼1 프리뷰 스테이지 | ✔ | — | |
| 4 | 졸라맨 캐릭터 좌표 7행 | ✔ | — | |
| 5 | **장비 오버레이 위치 6슬롯** | ✔ | — | |
| 6 | **착용 시 장비 색 4종(한 톤 밝게)** | ✕ | **✔ §4** | 전제가 틀렸다 |
| 7 | 망토 착용 전용 도형 5행 | ✕ | **△ §2-2** | 재질색 `#D2402F`만 잼. 도형 좌표는 `design-equipment` 소관 |
| 8 | 착용 현황 4행 | ✔ | — | |
| 9 | 아이템 상세 카드 | ✔ | — | |
| 10 | 컬럼2 스탯 카드 ×4 | ✔ | — | |
| 11 | 테마 세트 패널 | ✔ | — | |
| 12 | **컬럼3 아이템 카드 규격** | △ | **✔ §1 §3 §5** | 제1부는 잉크만 쟀다 |
| 13 | **`ItemIcon.dc.html` 벡터 16종** | ✕ | **✔ §1 §2** | **제1부 히트 0건** |
| 14 | **아이콘 구성 규칙 3조(채움/외곽/하이라이트)** | ✕ | **✔ §1-1** | |
| 15 | **망토류 재질색 강제** | ✕ | **✔ §2-2** | |
| 16 | 외형 탭 전체 | ✕ | ✕ | **안 봤다** |
| 17 | 보관함 그리드 4열 46px | ✕ | **✔ §5-3** | 프리셋 패널은 안 봤다 |
| 18 | 보관함 프리셋 패널 | △ | ✕ | 제1부가 버튼 대비 1건만 |
| 19 | **상점 DLC 팩 카드 4칸** | ✕ | **✔ §3** | |
| 20 | 상점 좌측 단품 리스트 | ✕ | ✕ | **안 봤다** |
| 21 | 데이터 모델(슬롯→스탯/등급/세트/재화) | ✔ | — | §5-12 충돌 6건 |
| 22 | **상태 매트릭스(버튼) 4행** | △ | **✔ §6** | **README ↔ 프로토타입 불일치 발견** |
| 23 | Interactions & Behavior 8항 | ✕ | ✕ | **안 봤다** |
| 24 | State Management | ✕ | ✕ | **안 봤다** |
| 25 | Design Tokens 전체 | ✔ | ✔ | |
| 26 | **시안 1b 「네온 콘솔」** | ✕ | ✕ | **구현 대상 아님 — 의도적으로 안 봤다** |

**★ 이번 라운드가 여전히 안 본 것: #16 외형 탭 · #18 프리셋 패널 · #20 상점 단품 리스트 ·
#23 인터랙션 · #24 상태 관리.** 다음 라운드가 이 다섯을 받아야 표가 소진된다.

## 0-B. 측정 방법 — 교정 먼저

```bash
Tools/ContrastProbe/fill_policy_audit.py    # §1 §2 §3(대역) §4 §5
Tools/ContrastProbe/fill_policy_audit2.py   # §5 틴트 상한 · 화소 예산 · 팩 가족성
```

교정 **13건**을 매 실행마다 통과해야 숫자를 낸다(하나라도 깨지면 `sys.exit`).
대비 5건(흰/검 21.0000 · 동일색 1.0000 ×2 · WCAG 예제 4.5422 / 5.3172),
ΔE 5건(L\*흰 100 · L\*검 0 · 회색 a\*b\* 0 · ΔE흰검 100 · ΔE동일 0),
**합성기 3건**(α=1 → src / α=0 → dst / α=.5 회색 중간 150) — 이번에 새로 붙였다.
**α 합성을 안 재면 이 문서의 §1이 통째로 무의미하다.**

★ **바깥 대조 1건**: 내가 잰 브라스 램프의 카드 바탕 대비가 **5.68 / 7.40 / 9.12 / 10.85**인데,
`UiChrome.cs:396-399` 주석이 독립적으로 **5.68 / 7.41 / 9.13 / 10.86**이라 적고 있다.
8bit 반올림 차이 안에서 일치한다 ⇒ **내 `CardSurface` 값(0.106/0.122/0.149)이 램프를 튜닝할 때
쓴 그 면과 같은 면임이 확인됐다.** (생성기·검사기가 같이 틀리는 형태를 피하려고 붙인 대조다.)

**미확인으로 남기는 것**
- 프로토타입 HTML을 **브라우저로 렌더해서 재지 않았다**(환경에 브라우저 없음). 색은 선언값 + CSS 합성 규칙 계산이다.
- 라디얼 그라디언트는 **중심점 값**으로 쟀다 — 인계본에 가장 유리한 지점이다(가장자리로 갈수록 더 옅어진다).
- 아이콘 잉크 점유율 38%는 **가정값**이다(§5-2에서 그렇게 표기했다). 실측하려면 래스터가 필요하다.
- **최종 판정은 Unity 실기 캡처다. 여기 숫자는 전부 예측이다.** 빌드가 09-02 19:07로 낡았다 → §8 캡처 대기 목록.

---

# 1. ★ 먼저 사실 정정 — 「채움 = 등급색」은 **솔리드가 아니다**

리더의 안 A 서술은 *"채움 = 등급색 일반 `#8A8F98` / …"*이다. **인계본 코드는 그렇게 동작하지 않는다.**

`reference/ItemIcon.dc.html`의 실제 채움:
```js
e('stop', { offset: '0%',   stopColor: A, stopOpacity: MAT[KIND0] ? 1 : 0.34 }),
e('stop', { offset: '100%', stopColor: A, stopOpacity: MAT[KIND0] ? 0.62 : 0.08 })
```
즉 **위 α 0.34 → 아래 α 0.08의 그라디언트 워시**다(망토류만 1.00 → 0.62).
README도 *"위 34% → 아래 8% 알파"*라고 적어 두었지만, **「채움 = 등급색」이라는 요약이 그 α를 지운다.**

## 1-1. 그래서 화면에 실제로 나오는 색

아이콘 슬롯 바탕 = `radial({등급색}1F, transparent 72%), #0F0D0C` → 글로우 중심에서 잰다(인계본 최선치).

| 등급 | 등급색 | 슬롯 실제 면 | **채움 위끝** | **채움 아래끝** | 채움위 vs 슬롯 | 채움아래 vs 슬롯 |
|---|---|---|---|---|---:|---:|
| 일반 | `#8A8F98` | `#1E1D1D` | **`#434447`** | `#272627` | 1.72 | 1.12 |
| 희귀 | `#6E9BE8` | `#1B1E27` | **`#374968`** | `#212836` | 1.83 | 1.13 |
| 영웅 | `#B07BE0` | `#231A26` | **`#533B65`** | `#2E2235` | 1.75 | 1.12 |
| 전설 | `#E0B24A` | `#282114` | **`#675226`** | `#372D18` | 2.14 | 1.17 |

> **유저가 보는 채움색은 `#434447` / `#374968` / `#533B65` / `#675226`다.**
> `#8A8F98` / `#6E9BE8` / `#B07BE0` / `#E0B24A`는 **화면에 한 번도 나타나지 않는다.**
> 그리고 채움 대 슬롯 대비는 **1.12 ~ 2.14** — 비텍스트 하한 3.00을 **전부 미달**한다.

## 1-2. 등급 변별도 절반이 날아간다

| 쌍 | 원본 ΔE | **합성 채움 ΔE** | 감쇠 |
|---|---:|---:|---:|
| 일반↔희귀 | 38.91 | **18.86** | 51.5% |
| 희귀↔영웅 | 33.46 | **16.85** | 49.6% |
| 영웅↔전설 | 107.92 | **52.67** | 51.2% |
| 일반↔전설 | 65.25 | **31.79** | 51.3% |

변별 하한 7.8은 넘는다(**인계본이 옳은 지점 — 아래 §7-1**). 다만 **식별 하한 48.6**을 넘는 쌍은
영웅↔전설 하나뿐이라, *"카드 한 장만 보고 등급을 맞힌다"*는 **A에서도 성립하지 않는다.**
이건 우리 램프에 대해 `design-art`가 이미 낸 결론(`UiChrome.cs:373-377`)과 **같은 결론**이다.

## 1-3. 아이콘 배경 글로우는 **보이지 않는다** — 「세 번 말한다」의 전제가 틀렸다

리더의 전제: *"인계본 카드는 등급을 세 번 말한다 — 리본 / 아이콘 배경 radial glow / 이름 옆 라벨."*

| 등급 | 글로우 실제 면 | vs 슬롯 `#0F0D0C` | 판정 |
|---|---|---:|---|
| 일반 | `#1E1D1D` | **1.151 : 1** | 안 보임 |
| 희귀 | `#1B1E27` | **1.166 : 1** | 안 보임 |
| 영웅 | `#231A26` | **1.153 : 1** | 안 보임 |
| 전설 | `#282114` | **1.218 : 1** | 안 보임 |

**넷 다 비텍스트 하한 3.00은 물론이고 1.22도 못 넘는다.**
⇒ **인계본 카드는 등급을 세 번이 아니라 「두 번 + 채움」으로 말한다.**
리더가 물은 *"채움까지 등급색이면 네 번인가"*의 답은 **아니다 — 실효 채널은 3개(리본·라벨·채움)다.**

★ 이 정정이 판정을 바꾼다. **글로우는 A/B/C 어느 안에서도 도입하지 마라 — 비용만 있고 신호가 0이다.**
(안 C의 범위 후보에서 「아이콘 배경 glow」를 **먼저 지운다.**)

---

# 2. ★ 우리 구조가 인계본과 갈라지는 지점 — **카드 색 = 몸 색**

여기가 이번 판정의 중심이다. 리더가 준 A/B/C 어느 것도 이 제약을 명시하지 않았다.

## 2-1. 인계본은 카드와 몸에 **다른 색**을 쓴다

`equipment-screen.dc.html:874`:
```js
return { kind: g.it.kind, ac: RAR[g.it.rar].c, pc: WEAR[g.it.rar] };
//                        ↑ 채움 = 등급색      ↑ 외곽선 = "한 톤 밝게"
```
카드에서는 채움 `#8A8F98`, 몸에서는 외곽선 `#D8B27A`. **두 벌이다.**

## 2-2. 우리는 **한 벌**이고, 그건 사고 끝에 얻은 것이다

`Core/ItemCatalog.cs:622`: *"덤으로 새 27색은 전부 **WornColor 항등**이다 — 카드 색과 몸 색이
바이트 단위로 같다."* 그 앞 문장이 왜 그렇게 됐는지 적고 있다 —
**옛 27색 중 20색이 `WornColor`를 통과한 뒤에도 자립 대역 밖이었다**(최악 `#FFF0B8` → 몸 `#CCBA76` = **1.60 : 1**, 종이 무대).

우리 아이템 색이 놓이는 배경은 카드만이 아니다. **흰 바탕화면 · 검은 바탕화면 · 종이 무대 `#E9EAE6` ·
목탄 무대 `#25282E` 넷**이고 넷 모두에서 비텍스트 3.00을 넘어야 한다.
그 교집합이 **상대휘도 L ∈ [0.1632, 0.2396]**이다.

## 2-3. 그 대역에 무엇이 들어가는가 — 실측

| 색군 | 색 | L | 대역 | 배경 4종 최악 | 판정 |
|---|---|---:|---|---:|---|
| **인계본 등급** 일반 | `#8A8F98` | 0.2732 | **밖** | 2.69 (종이 무대) | **미달** |
| **인계본 등급** 희귀 | `#6E9BE8` | 0.3258 | **밖** | 2.31 (종이 무대) | **미달** |
| **인계본 등급** 영웅 | `#B07BE0` | 0.2878 | **밖** | 2.57 (종이 무대) | **미달** |
| **인계본 등급** 전설 | `#E0B24A` | 0.4818 | **밖** | **1.63** (종이 무대) | **미달** |
| **인계본 착용** 일반 | `#D8B27A` | 0.4784 | **밖** | 1.65 | **미달** |
| **인계본 착용** 희귀 | `#7FB0F2` | 0.4197 | **밖** | 1.85 | **미달** |
| **인계본 착용** 영웅 | `#C08FEC` | 0.3691 | **밖** | 2.07 | **미달** |
| **인계본 착용** 전설 | `#F0C25C` | 0.5788 | **밖** | **1.38** | **미달** |
| 우리 브라스 램프 일반~전설 | `#9C978C`~`#F9CB70` | 0.31~0.64 | **밖** | 2.41 ~ **1.26** | **미달** |
| 재질 Canvas(밀짚) | `#AB7942` | 0.2273 | 내 | 3.14 | OK |
| 재질 Gold(왕관) | `#9B7922` | 0.2076 | 내 | 3.38 | OK |
| 재질 TintHead(목도리) | `#CC5512` | 0.1938 | 내 | 3.43 | OK |
| 재질 Leather | `#BA5928` | 0.1774 | 내 | 3.20 | OK |
| 재질 Silver | `#587398` | 0.1660 | 내 | 3.04 | OK |
| **인계본 망토 재질 `#D2402F`** | | 0.1757 | **내** | **3.18** | **OK** |

★ **브라스 램프도 대역 밖이다 — 그건 결함이 아니라 설계다.** `UiChrome.cs:388-390`이
*"몸에 칠하지 않는다"*고 못 박고 있고, 램프는 크롬 대역(L 0.31~0.64) 전용이다.
**이 행은 내 대역 검사의 양성 대조이기도 하다** — 재질색은 통과하고 크롬색은 떨어지므로,
검사가 실제로 두 부류를 가르고 있음이 확인된다.

★★ **그리고 인계본의 망토 재질색 `#D2402F`는 우리 대역 안에 있고 배경 4종을 전부 통과한다.**
인계본은 **망토류에만** 재질색을 강제했는데, **그 하나가 우리 규칙과 정확히 일치한다.**
§7-2에서 이 지점을 다시 다룬다.

> ### 판정
> **몸에 붙는 것과 카드 그림은 재질색이어야 한다** — 등급색·브라스 램프 8색 중
> **대역을 통과하는 것이 하나도 없다.** 안 A를 채택하면 **항등을 깨고 두 벌로 되돌아가야 하고**,
> 그건 `ItemCatalog`가 2026-09-02에 27색을 통째로 옮기며 빠져나온 바로 그 상태다.
> ⇒ **안 A는 「색이 예쁘냐」가 아니라 「우리 캐릭터가 바탕화면 위에서 보이느냐」에서 떨어진다.**

---

# 3. ★ DLC 팩 카드 — **인계본 자신이 여기서 등급 채움을 버렸다**

리더 질문: *"한 팩 안에서 4개가 같은 색이면?"*

## 3-1. 인계본은 팩 카드에서 등급색을 **안 쓴다**

`equipment-screen.dc.html:501`:
```html
<dc-import name="ItemIcon" kind="{{ k.kind }}" color="#E8E2D6" accent="#C8A15A" hint-size="36px,36px">
```
**`accent="#C8A15A"` — 브라스 고정이다.** 그리드(`accent="{{ it.rc }}"` = 등급색)와 **다르다.**

그리고 팩 데이터에는 **등급 필드가 아예 없다**(`:998` `kinds: p.kinds.map(k => ({ kind: k }))`).
대신 `cyber` 팩의 주석 줄이 **낱말로** 말한다 — *"글리치 파티클 · **전설 왕관 포함**"*(`:991`).

> **인계본의 답: 팩 카드에서 등급은 색이 아니라 「문장」이 진다.**
> 리더가 걱정한 *"4개가 같은 색"*은 인계본에서 **일어나지 않는다.**
> ⇒ **이 지점은 A/B/C 판정과 무관하다. 세 안 모두 팩 카드에서는 등급 채움을 쓰지 않는다.**

## 3-2. 그럼 C에서 4칸이 **재질색이라 서로 다른 것**은 문제인가 — 실측

현 카탈로그 42종의 색상 가족은 **5개**뿐이다.

| 색상 가족 | 종수 |
|---|---:|
| 호박·가죽·캔버스 (18~43°) | 17 |
| 청 (213~218°) | 13 |
| 녹 (97~103°) | 4 |
| 적 (0°대) | 4 |
| 무채(잉크 표식) | 4 |

인계본 `office` 팩 구성(fedora / roundglasses / stripedtie / backpack)에 대응하는 우리 아이템으로 재면:

| 팩 | 4색 | 쌍별 ΔE (최소/최대/평균) | 색상각 스프레드 | 판정 |
|---|---|---|---:|---|
| 오피스(현 카탈로그 대응) | `#5577AE` `#587398` `#428C24` `#AB7942` | 10.63 / 91.28 / 62.20 | **185.7°** | **가족 아님** |
| 가정 팩 A(같은 가족으로 짰을 때) | `#AB7942` `#9B7922` `#BA5928` `#BA7636` | 10.70 / 32.02 / 19.36 | **23.0°** | 가족 |
| (대조) 안 A식 = 4칸 동일 등급색 | — | 0.00 | 0.0° | 완전 균질 |
| (대조) 인계본 실제 = 4칸 브라스 | — | 0.00 | 0.0° | 완전 균질 |

★ **정직하게: 185.7°는 「팩이 아직 없어서」 나온 숫자다.** 현 42종은 **DLC 팩 단위로 조직돼 있지 않고**,
재질이 팩 테마가 아니라 물건 자체(펠트 중절모 = 파란 펠트, 캔버스 배낭 = 황갈)를 따른다.
**이 수치를 「C가 팩 카드에서 깨진다」의 근거로 쓰면 안 된다.**

> ### 판정 — 팩 카드는 **세 안 공통으로 브라스 균질**로 간다
> 1. 팩 카드는 **사기 전** 화면이라 등급 상태가 구매자에게 의미를 갖지 않는다(인계본도 등급 필드를 안 넣었다).
> 2. C에서 4칸을 재질색으로 두면 **가족성이 팩 저작에 의존**하게 된다 — 위 두 줄의 차이가 23.0° vs 185.7°다.
>    **저작 규율에 화면의 읽힘을 맡기지 않는다.**
> 3. ⇒ **팩 카드 4칸(52px 상자 / 아이콘 36)은 브라스 `#C8A15A` 균질.** 팩의 내용물은 **실루엣**이 말하고,
>    등급은 **주석 줄의 낱말**이 말한다(인계본의 *"전설 왕관 포함"* 방식을 그대로 채택).
> 4. ★ **이건 「팩 저작 규칙」을 함께 낳는다** — `design-art`·`design-equipment`에게:
>    **한 팩의 4부위는 색상각 스프레드 60° 이내**로 짠다(위 두 줄이 그 경계를 실측으로 준다).

---

# 4. ★ 「흰 선 위 가독성을 위해 한 톤 밝게」 — **전제도 틀렸고, 방향도 반대다**

리더 질문 5: *"그 색 체계는 무엇을 근거로 서 있나?"*

## 4-1. 사실 — 우리 출하 기본은 검은 선이다 (코드·애셋 둘 다 확인)

| 출처 | 값 |
|---|---|
| `Core/StickConfig.cs:1489` | `public StickmanInkColor inkColor = StickmanInkColor.Black;` |
| `Core/StickConfig.cs:10-13` | `enum StickmanInkColor { Black = 0, White = 1 }` |
| `Assets/_Project/Data/DefaultStickConfig.asset:237` | `inkColor: 0` |
| 〃 `:238` | `primaryOutlineColor: {r: 0, g: 0, b: 0, a: 1}` |

**코드 기본값과 애셋이 일치한다**(CLAUDE.md가 경고한 *"애셋이 코드 기본값을 덮는다"* 사고 형태 아님).
인계본의 *"캐릭터는 **흰 선** + 검은 머리로 고정합니다(게임 원본 규칙)"*는 **우리 규칙이 아니다.**

## 4-2. 그리고 밝히면 흰 선 대비는 **떨어진다**

| 등급 | 기본 등급색 | vs 흰 선 | 한 톤 밝게 | vs 흰 선 | 개선? | vs 검은 선 | 개선? |
|---|---|---:|---|---:|---|---:|---|
| 일반 | `#8A8F98` | 3.25 | `#D8B27A` | **1.99** | **악화** | 10.57 | 개선 |
| 희귀 | `#6E9BE8` | 2.79 | `#7FB0F2` | **2.24** | **악화** | 9.39 | 개선 |
| 영웅 | `#B07BE0` | 3.11 | `#C08FEC` | **2.51** | **악화** | 8.38 | 개선 |
| 전설 | `#E0B24A` | 1.97 | `#F0C25C` | **1.67** | **악화** | 12.58 | 개선 |

> **4등급 전부 「흰 선 대비」가 나빠진다.** 밝은 색은 흰 것과 붙는다 — 당연한 결과다.
> 그러니 *"흰 선 위 가독성을 위해"*라는 **문장은 인계본 자신의 전제 안에서도 성립하지 않는다.**
> (실제 의도는 *"어두운 무대 `#0F0D0C` 위에서 잘 보이게"*였을 것이다. 그건 맞는 이유다 — 다만
> 그렇게 적혀 있지 않고, **적힌 이유를 그대로 믿고 이식하면 반대 방향으로 튜닝하게 된다.**)

## 4-3. 리더 질문 4(신)에 대한 답: **맞다. C에서는 이 색 체계가 통째로 사라진다.**

C는 착용 장비를 재질색으로 칠하므로 `WEAR[]` 4색이 **필요 없어진다.**
그리고 그와 함께 **틀린 전제 문장도 같이 사라진다.** 없어지는 것이 이득인 자리다.

★ 다만 **한 가지는 살려서 흡수한다**(진 제안을 버리지 않는다 — `docs/TEAM.md` 2단계 규칙):
인계본이 **「착용 색을 무대/잉크와의 관계로 따로 정해야 한다」는 문제 자체는 옳게 봤다.**
우리는 그 답을 **자립 대역 L ∈ [0.1632, 0.2396]**으로 이미 갖고 있고(§2-3), 그것이
인계본의 4색보다 **배경을 4개 더 본다.** ⇒ 문제 인식은 채택, 해답은 우리 것.

---

# 5. ★ 안 C의 범위 확정 — 픽셀·색값으로 못박는다

리더 지시: *"C를 택하면 「카드색 범위」를 픽셀·색값으로 못박아라."*

## 5-1. 후보 범위와 판정

| 범위 | 무엇을 등급색으로 | 판정 | 근거 |
|---|---|---|---|
| **C1** | 리본 + 이름 옆 라벨 | **채택** | §5-2 |
| C2 | C1 + 아이콘 배경 radial glow | **기각** | 글로우가 **1.15~1.22 : 1**로 안 보인다(§1-3). 비용만 있고 신호 0 |
| C3 | C2 + 카드 바탕면 틴트 | **기각** | §5-2 — 글자를 지키는 α에서 **등급이 안 갈린다** |

## 5-2. C3가 왜 죽는가 — **두 수치가 서로를 잡아먹는다**

우리 카드면 `CardSurface` = `#1B1F26`. 그 위 글자 3종은 지금 **14.99 / 7.93 / 5.32 : 1**이다.
바탕면을 등급색 α로 물들이면:

| 틴트 α | 기본 | 보조 | **3차** | 램프 vs 물든 면 | 미달 |
|---:|---:|---:|---:|---:|---|
| 0.06 | 13.16~13.73 | 6.96~7.26 | **4.67~4.87** | 5.20~9.53 | 0건 |
| 0.10 | 11.93~12.88 | 6.31~6.81 | **4.23~4.57** | 4.88~8.64 | 일반 빼고 3건 |
| 0.16 | 10.18~11.63 | 5.39~6.15 | **3.61~4.13** | 4.40~7.37 | 4건 |
| 0.24 | 8.16~10.06 | 4.31~5.32 | **2.89~3.57** | 3.81~5.91 | 5건 |

**가장 먼저 죽는 것은 3차 잉크 `#8B939F`다.** 4.50을 지키는 최대 α를 등급별로 이분탐색하면:

| 등급 | 최대 α | 물든 면 | 3차 잉크 | 램프 대비 | 구속 조건 |
|---|---:|---|---:|---:|---|
| 일반 | 0.1095 | `#292C31` | 4.50 | 4.80 | 3차 잉크 |
| 희귀 | 0.0929 | `#2A2C2F` | 4.50 | 6.26 | 3차 잉크 |
| 영웅 | 0.0826 | `#2B2C2D` | 4.50 | 7.71 | 3차 잉크 |
| **전설** | **0.0754** | `#2C2C2C` | 4.50 | 9.18 | 3차 잉크 |

**4등급 공통 상한 α = 0.0754** (가장 빡빡한 전설이 결정한다).

### ★ 그 α에서 등급이 갈리는가 — **안 갈린다**

| 쌍 | ΔE (α = 0.0754) | 변별 하한 7.8 |
|---|---:|---|
| 일반↔희귀 | **1.44** | **미달** |
| 희귀↔영웅 | **1.71** | **미달** |
| 영웅↔전설 | **1.69** | **미달** |
| 일반↔전설 | **4.81** | **미달** (최대 쌍) |

> **물든 면 네 개가 `#25282E` / `#272A2E` / `#2A2B2D` / `#2C2C2C`다.**
> 정상 시각에서 나란히 놓아도 안 갈린다. **C3는 글자 대비를 팔아서 아무것도 사지 못한다.**
> 참고로 인계본 글로우 α(0.1216)는 이 상한을 **초과**한다 — 다만 글로우 위에는 글자가 없으므로
> 그건 위반이 아니라 **§1-3의 「안 보인다」쪽으로** 떨어진다.

## 5-3. ★ 확정 범위 — 색값·픽셀

```
안 C1 (확정 권고)
┌─────────────────────────────────────────────────────────────┐
│ 등급이 말하는 곳 (3채널)                                       │
│  ① 상단 리본 — 칸 수 (int)rarity+1                            │
│       채움  UiChrome.RarityColor(r)  #9C978C/#BCAC8B/          │
│                                     #DBBD7F/#F9CB70          │
│       빈칸  UiChrome.RarityTrack     #3A4049                  │
│       폭 133px(카드 161 − 좌우 14 인셋) / 높이 2 / 칸 1개 ≈30.2px │
│  ② 이름 옆 등급 라벨 — 낱말 + 같은 램프색                        │
│  ③ 카드 테두리 — 착용/선택/기본 (등급 아님, 상태다. 현행 유지)      │
│                                                              │
│ 등급이 말하지 않는 곳 (= 재질색)                                │
│  ④ 아이콘 그림 44×44 — ItemCatalog 주색/보조색 (대역 내)         │
│  ⑤ 몸에 붙는 장비 — 같은 값 (WornColor 항등, 바이트 단위로 같음)   │
│  ⑥ 카드 바탕면 #1B1F26 — 물들이지 않는다                        │
│  ⑦ 아이콘 배경 글로우 — 도입하지 않는다                          │
└─────────────────────────────────────────────────────────────┘
```

## 5-4. 리더 질문 1(신) — **44px 리본만으로 등급 4개가 구별되나**

**우리 리본은 색이 주 채널이 아니라 「칸 수」다.** 그래서 자가 다르다.

| 채널 | 면적 | 인접 최소 ΔE | 변별 |
|---|---:|---:|---|
| A 아이콘 채움 (44×44 잉크 38% 가정) | 736 px² | 16.85 | OK |
| A 상단 리본 (133×2) | 266 px² | 33.46 | OK |
| **B/C 상단 리본 (133×2, 브라스)** | 266 px² | **15.16** | OK |
| C3 카드 바탕면 (α 0.0754) | 17,388 px² | **1.44** | **미달** |

★ **면적이 큰 채널이 이기는 것이 아니다.** 변별이 하한 아래면 면적을 곱해도 0이다 —
C3가 카드의 **100%**를 쓰고도 지는 이유다. **ΔE가 먼저 관문을 통과하고, 면적은 그 다음이다.**

**칸 수 채널의 실측**: 칸 1개 ≈ **30.2px**(리본 133px / 4칸 + 간격 3×4).
**44px 아이콘보다 칸 하나가 더 크다.** 그리고 이 채널은 **색맹·흑백 출력·저조도에서 무너지지 않는다** —
`design-art` 실측상 우리 램프는 완전색맹에서 영웅↔전설 ΔE **6.14**로 변별이 깨지는데, **칸 수는 안 깨진다.**

### 미보유 dim에서도 읽히나

| | 인계본 리본 dim(opacity .25) | 우리 |
|---|---|---|
| 대 카드 대비 | **1.45 ~ 1.69 : 1** (4등급 전부 비텍스트 하한 미달) | — |
| dim끼리 ΔE | 11.09 ~ 33.37 (변별은 유지) | — |
| **판정** | **색은 살아 있는데 「거기 리본이 있다」가 안 보인다** | **칸 수는 opacity에 안 죽는다 — 빈칸 트랙이 남아 형태가 유지된다** |

> ★ 이건 제1부 H-3(`opacity .34` 폐기 권고)과 **같은 뿌리다.** `opacity`는 전경과 배경을 **함께**
> 끌어내려 대비를 곱으로 깎는다. **미보유 표현은 opacity가 아니라 「색을 바꾸는」 방식이어야 한다.**

## 5-5. 리더 질문 3(신) — **보관함 4열 46px에서도 같은 답인가**

**같다. 그리고 보관함에서 더 강하다.**

1. 인계본 보관함은 아이콘에 **`color="#E8E2D6"` 고정**을 넘긴다(`:405`) — 장비 그리드(`it.ic`)와 달리
   **미보유 dim 분기조차 없다.** 보관함은 정의상 **보유한 것만** 나오는 화면이기 때문이다.
2. 그래서 보관함에서 **등급 채움은 「내가 가진 물건 13종을 서로 구별하는」 일을 전혀 돕지 않는다** —
   4열 × 46px에서 유저가 찾는 것은 *"내 밀짚모자가 어디 있지"*이지 *"희귀가 어디 있지"*가 아니다.
3. **재질색이 여기서 유일한 검색 키다.** 같은 등급의 모자 4개가 A에서는 **완전히 같은 색**이 된다.

★ **이것이 안 C가 안 A를 이기는 두 번째 근거다**(첫 번째는 §2-3 대역).

---

# 6. ★ 인계본 내부 불일치 1건 — README와 프로토타입이 다르다 (신규 발견)

제1부는 **README의 선언값만** 쟀다. 이번에 프로토타입 코드를 읽고 대조했더니 **갈라져 있다.**

**미보유(재화 구매 가능) 카드 버튼**, 바탕 = 카드 위끝 `#161311`:

| 출처 | 면 | 글자 | 보더 | 면 대비 | 잉크 대비 | 판정 |
|---|---|---|---|---:|---:|---|
| README 상태 매트릭스 | `#17130E` | `#C8A15A` | `#3A3026` | **1.00** | 7.66 | 면 < 3.60 |
| **프로토타입 `:820-822`** | `#141210` | `#5C574E` | `#231F1B` | **1.01** | **2.61** | 면 < 3.60 **AND** 잉크 < 5.175 |

**면·글자·보더 셋 다 다르다.** README는 *"아래 수치대로 구현해 주세요"*라고 지시하는데,
**그 수치대로 만들면 프로토타입과 다른 화면이 나온다.**
(README가 기술한 값은 실은 **상세 카드 버튼**(`mainBg`/`mainColor`)의 값이다 — 그쪽은 일치한다.
즉 **상태 매트릭스가 카드 버튼과 상세 버튼을 한 칸에 합치면서 카드 쪽이 틀렸다.**)

> **리더에게**: 제1부 §1-2가 낸 *"`동전 N` 버튼 면 1.00 : 1"*은 **README 값이다.**
> 프로토타입 실제 값은 **1.01 : 1이고 글자까지 2.61**로 **더 나쁘다.**
> 제1부의 결론(H-2 되돌림)은 유지되고 **근거가 강해진다.**

---

# 7. ★ 인계본이 옳은 지점 (리더 지시: *"「우리가 낫다」로 끝내지 마라"*)

## 7-1. 등급을 **큰 면적**에 놓으려 한 것은 옳다

44px 카드에서 리본은 카드의 **1.53%**(266 px²)뿐이고 아이콘은 **4.23%**(736 px²)다.
**아이콘이 리본의 2.8배다.** *"가장 큰 표면에 가장 중요한 분류를 얹는다"*는 판단 자체는 옳다.

**우리가 그걸 안 해도 되는 이유는 채널이 다르기 때문이다** — 칸 1개가 **30.2px**로 아이콘(44px)에
필적하고, 그 채널은 **세는 일**이라 저조도·색맹에서도 안 죽는다.
⇒ **인계본은 「색의 면적」으로 풀었고 우리는 「형태의 개수」로 푼다. 같은 문제를 봤다.**

## 7-2. ★ 망토에 재질색을 강제한 것은 **정확히 옳았다**

인계본은 16종 중 망토 2종에만 `MAT = { shortcape:'#D2402F', longcape:'#D2402F' }`를 걸고
알파도 **1.00 → 0.62**로(다른 것의 0.34 → 0.08 대비 3배) 올렸다.

**그 `#D2402F`는 L = 0.1757로 우리 자립 대역 [0.1632, 0.2396] 안에 있고 배경 4종 최악 3.18 : 1로 통과한다.**
16색 중 **유일하게 우리 규칙을 만족하는 값**이다.

> **인계본은 「등급색으로는 안 되는 물건이 있다」를 스스로 발견했다.** 다만 그 예외를 **망토에서 멈췄다.**
> 우리 대역 검사는 **그 예외가 16종 전부에 적용되어야 함**을 보여준다.
> ⇒ **안 C는 인계본의 부정이 아니라 인계본이 시작한 예외의 완성이다.** 이 프레이밍으로 채택을 권고한다.

## 7-3. 팩 카드에서 브라스로 균질화한 것도 옳다 (§3-1). 그대로 채택한다.

## 7-4. 등급 라벨을 **낱말로** 남긴 것

인계본은 색을 크게 쓰면서도 **이름 옆 등급 라벨(낱말)을 지웠다가 되살리지 않았다** — 계속 갖고 있다.
*"색만으로 등급을 표시하지 마라"*는 우리 `UiChrome.cs:380`의 지시와 **같은 결론**에 독립적으로 도달해 있다.

---

# 8. 캡처 대기 목록 (빌드가 09-02 19:07로 낡았다 — 실기 전 확정 금지)

| # | 캡처 대상 | 무엇을 확인 | 이 문서의 예측 |
|---|---|---|---|
| C-1 | 장비 탭 카드 1장 (44px 아이콘, 4등급 각 1장) | 리본 칸 수가 **30.2px**로 렌더되는가 | §5-4 |
| C-2 | 같은 카드 **미보유** 상태 | dim 처리 후에도 칸 경계가 남는가 | §5-4 |
| C-3 | 보관함 4열 46px 한 화면 | 같은 등급 모자 4개가 **재질색으로 갈라지는가** | §5-5 |
| C-4 | 상점 DLC 팩 카드 (52px 상자 / 36px 아이콘 ×4) | 브라스 균질에서 **실루엣만으로 4부위가 갈라지는가** | §3-2 |
| C-5 | 바탕화면 4종 위 캐릭터 착용 상태 | 재질색이 실제로 3.00을 넘는가(계산 아닌 픽셀) | §2-3 |
| C-6 | 흰 잉크 프리셋으로 전환한 착용 상태 | 잉크를 바꿔도 장비가 사는가 | §4-2 |
| C-7 | Windows DPI 150% / 200% 카드 | 리본 높이 2px가 **배율에서 사라지지 않는가** | ★ 미측정 |

★ **C-7은 이 문서가 재지 못한 축이다.** 리본 높이 2px는 Windows 150%에서 3px, 125%에서 2.5px →
`round`로 **2 또는 3**이 된다. `UiChrome`의 *"전부 짝수 pt"* 규칙(캔버스 배율 1.5에서 글리프가 번진 사고)이
**리본에도 적용되는지 확인되지 않았다.** 실기 캡처 전에는 리본 규격을 확정하지 마라.

---

# 9. 【판정】 폐기된 `TabLabelWidth` 식 — 문서 3곳 수정 완료

`coder-ui`가 폭 모형 7곳을 `Text.preferredWidth` 실측으로 교체했다. 프로덕션 현재 형태:

```
Interaction/CharacterInfoWindow.Tabs.cs:294
  TabLabelWidth(label, name) = SettingsControls.MeasuredWidth(label, name) + TabLabelPadX * 2f
  TabLabelPadX = 2f     // 옛 식의 "+ 4f"를 반으로 나눈 값
```

**옛 식 `글자수 × FontTitle + 4`는 한글에서만 맞고 라틴에서 반쯤 빈 상자를 남긴다**(그 문장이
`UiChrome.Ellipsize` 문서에 이미 있었다). 문서 3곳이 그 식을 **정본처럼** 적고 있었다.

★ **수정 원칙**: 새 숫자를 다시 베끼지 않는다. `preferredWidth`는 폰트·아틀라스·배율에 따라 달라져
**이 환경에서 잴 수 없다.** 그래서 문서에서 **식과 파생 좌표를 지우고 「실측이며 심볼이 정본」이라고만 남긴다** —
숫자를 새로 적으면 그것이 다음 번 폐기 대상이 된다(CLAUDE.md: 프로덕션 상수를 문서에 베끼지 않는다).

| 파일 | 위치 | 조치 |
|---|---|---|
| `docs/UX_FLOW.md` | §「5번째 탭으로 오해되는가」 | 파생 x좌표 4행 → **실측 표기로 교체** |
| `docs/UX_SHOP_AND_CURRENCY.md` | §1-1 식 블록 | **새 식으로 교체 + 옛 식 폐기 표기** |
| 〃 | §1-2 결과 표 | **숫자 폐기, 부등식 판정만 남김** |

---

# 10. 【판정】 상태 표시명 정본 — **어휘는 `StateLabel`, 단 `ThrowTumble`은 「공중 회전」**

`code-inspection` 실측: `StickmanStateId` **27개 중 19개가 다른 낱말**. 뿌리는
`Core/StickMateDisplayNames.cs:6`의 *"유일한 생산자"* 주석이 거짓이 된 것.

## 10-1. 내 축(UI 표면)에서 본 판정 — inspection과 **일치**

`StateLabel`(정보창)은 창이 열린 **내내** 보이고 `BusyText`(행동 명령창)는 **막힌 타일을 마주친 순간**만 보인다.
그리고 **둘 다 "지금"으로 시작해 한 화면에 동시에 뜬다.** 노출 시간이 비교가 안 되므로
**어휘 등록(register)은 상태 서술 쪽(`StateLabel`)이 정본**이다. 여기까지는 inspection이 옳다.

## 10-2. ★ 그런데 `ThrowTumble` 한 건은 **반대로 판정한다** — 사용자의 낱말이기 때문

CLAUDE.md에 보존된 사용자 원문(2026-08-29):
> *"던져도 **공중에서 회전**하면서 무릎앉아 착지할수있게 해줘"*

**「회전」은 사용자가 직접 쓴 낱말이다.** 그리고 이 상태는 그 요청 하나 때문에 존재한다
(`throwTumbleEnabled`가 그 스위치이고 CLAUDE.md가 *"되살리지 마라 — 사용자가 닫은 문"*이라 못 박은 그 건).

- **「날아가는 중」은 원칙 1에 대해서도 약하다** — 유저가 보는 것은 *날아가는 것*이 아니라
  *던져져서 회전하다 무릎앉아 착지하는 것*이다. 「날아가는」은 자력 비행을 함의한다.
- 「공중 회전」은 `StickConfig` 툴팁 10곳 · `StickmanEventBus` · 테스트 2종에 이미 정착해 있다.

> **판정: 어휘 등록은 `StateLabel`(상태 서술), 개별 낱말은 사안별로 고르되 `ThrowTumble` = 「공중 회전」.**
> 나머지 18건은 `StateLabel` 쪽 낱말을 취한다(`Idle` → 「가만히 있는」, `Graffiti` → 「낙서하는」 …).
> **「그라피티」·「창 도둑」·「블랙홀」은 기능 이름이지 상태가 아니다** — 그건 `SpectacleEventKind` 표에서
> 낱말을 재사용한 흔적이고, 스펙터클에는 맞지만 상태에는 맞지 않는다.

## 10-3. ★★ 구조 영향 — 리더에게 명시 보고 (설계가 코드 구조를 규정한다)

**두 표를 손으로 맞추는 것은 답이 아니다.** 지금도 맞춰 놓으면 다음에 상태가 하나 늘 때 다시 갈라진다
(그 함정을 `StickMateDisplayNames`의 주석이 이미 경고했고, **경고한 그대로 일어났다**).

**어간 하나에서 두 문장을 파생시켜라:**

```
정본 = 어간 S  (상태 서술, 예: "공중 회전" / "가만히 있는" / "낙서하는")

정보창 프레즌스   "지금  ·  " + S + " 중"          ← CharacterInfoWindow
행동 명령창 사유  "지금 " + S + " 중이라 못 해요"    ← BusyText 호출부 7곳
```

- 두 표면이 **같은 배열**을 읽고 **접미만 다르다** ⇒ 19건이 구조적으로 재발 불가.
- **문구를 미리 만들어 두는 무할당 설계(하루 종일 켜져 있고 0.25초 폴링)는 유지된다** —
  어간이 유한하므로 **두 벌의 완성 문장을 정적으로** 구워 두면 된다.
- ⇒ **`CharacterInfoWindow.StateLabel`의 문자열 테이블을 지우고 `StickMateDisplayNames`로 흡수.**
  거처는 inspection 판정대로 `Core` 쪽이 맞다.

**이건 내 권한 밖의 코드 변경이다. `coder` 또는 `dev-platform` 배정이 필요하다.**
동반 테스트 1건 권고: **enum 값 전수에 대해 두 표면 문자열이 같은 어간을 공유하는지** 검사
(니들 하드코딩 금지 — 어간 배열을 참조해서 비교할 것. CLAUDE.md의 부재 단언 함정을 피하려면
**「27개 전부 매칭됐다」는 개수 단언**을 같이 넣는다. 안 그러면 배열이 비어도 조용히 초록이 된다).

---

# 11. 리더 결정 요청 (제2부)

| # | 결정 | 내 권고 | 근거 |
|---|---|---|---|
| **H-13** | **채움색 A / B / C** | **C1 채택** — 등급은 리본(칸 수)·라벨, 아이템 그림과 몸은 재질색 | §2-3 대역 8/8 미달 · §5-2 C3 무효 |
| **H-14** | 아이콘 배경 radial glow | **도입 안 함** | 1.15~1.22 : 1 (§1-3) |
| **H-15** | 카드 바탕면 틴트 | **안 함.** 굳이 한다면 **α ≤ 0.0754**이나 그 α에서 ΔE 1.44라 무의미 | §5-2 |
| **H-16** | DLC 팩 카드 4칸 | **브라스 `#C8A15A` 균질** + 등급은 주석 줄 낱말 | §3-1 (인계본 방식 채택) |
| **H-17** | 팩 저작 규칙 | **한 팩 4부위 색상각 스프레드 ≤ 60°** — `design-art`·`design-equipment`에 전달 | §3-2 (23.0° vs 185.7°) |
| **H-18** | 착용 오버레이 `WEAR[]` 4색 | **폐기** — C에서 필요 없다. 「흰 선」 전제도 함께 폐기 | §4-1 §4-2 |
| **H-19** | 인계본 README 상태 매트릭스 오류 | **디자이너에게 되돌림** — 카드 버튼과 상세 버튼이 한 칸에 합쳐져 카드 쪽이 틀렸다 | §6 |
| **H-20** | 상태 표시명 통합 | **어간 1벌 + 접미 2종으로 구조 통합.** `ThrowTumble` = 「공중 회전」 | §10 — **코드 배정 필요** |
| **H-21** | 리본 높이 2px의 Windows 배율 | **실기 캡처 전 확정 금지** | §8 C-7 |

---

# 12. 플랫폼 영향 (제2부)

| | 내용 |
|---|---|
| **Windows 영향** | **없음 (문서 변경만).** 이번 라운드는 `docs/*.md` 3개만 수정했고 프로덕션 `.cs` 0줄이다. 대상 파일에 `#if UNITY_STANDALONE_*` 분기 0건. ★ **다만 §8 C-7(리본 2px의 DPI 125/150/175% 반올림)은 Windows 전용 미측정 축**이고, 이 문서는 그것을 **미확인으로 남긴다** — 리본 규격을 코드로 옮기는 라운드가 반드시 함께 재야 한다 |
| **macOS 영향** | 대비·대역·ΔE 판정은 플랫폼 공통(색 계산에 플랫폼 분기 없음). macOS 배율은 ×1/×2뿐이라 리본 2px가 정수로 떨어져 C-7 위험이 **구조적으로 없다** |
| **미확인** | 브라우저 렌더 캡처 없음(선언값 계산) · 아이콘 잉크 점유율 38%는 가정값 · Unity 실기 캡처 7건 대기(§8) |
