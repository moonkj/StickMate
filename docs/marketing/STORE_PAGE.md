# 스팀 스토어 페이지 문안 — **v2 / 본편 무료(F2P) 판** (★ 게시 금지, 리더 승인 대기)

작성: `marketing` · **R5 재구성 2026-09-03** (v1은 유료 $7.99 전제였다 — **폐기**)
★ **v2.1 갱신 2026-09-05 — 지원 OS 선언 반전.** 아래 0-4절을 **먼저** 읽어라.
★ **v2.2 갱신 2026-09-05 (R7)** — ① **Playtest 가입 칸**이 이 페이지에 생길 수 있다(0-4절 끝) ·
② **P22 배치 판정**(3-2) · **P21 규칙**(3-3) · ③ **금지 3건 추가**(7절: 가격 숫자 · 뽀모도로류 ·
「할일/하드웨어가 알아서 뜬다」).

전제(`product-strategy` 확정): **본편 $0(F2P) / DLC 팩 $4.99 / 6팩 번들 $19.99 /
채널 스팀 단독 / ~~Win+Mac 동시~~ → ★ **Windows 단독**(2026-09-05 반전) / 팩 단위만 판매 /
현금↔재화 다리 없음**
근거 창고: `TRUTH_INVENTORY.md`(쓸 수 있는 사실) · `CLAIM_AUDIT_R5.md`(최신 검증) ·
`personas/_시장축_공통실측.md`(숫자) · `docs/strategy/CHANNEL_PRICING_DECISIONS.md`(가격)

> ★★ **이 문서는 초안이다. 외부에 아무것도 게시하지 않는다.**
> 스토어 등록은 **리더 승인 후 사용자가** 한다.
> ★ **영어 문안은 앱 내부 영어화가 착지한 뒤에만 올린다** — 페이지만 영어이고 앱이 한국어인 상태가
> 가장 나쁘다(M3 6-1). ★ **F2P가 이 게이트를 더 급하게 만들었다** — 0-3절.

---

## 0-4. ★★★ v2.1 (2026-09-05) — **지원 OS 선언이 반전됐다. Windows만 선언한다**

사용자 지시로 팀 목표가 **「Windows 스팀 출시」**로 좁혀졌다.
`docs/strategy/ROADMAP.md` §17-2의 *"최초 작성 시점에는 **macOS만** 선언한다"*는 **뒤집혔다** —
**이제 Windows만 선언하고 macOS를 나중에 추가한다**(같은 문서 §26, `game-architect` §0-1).
★ **비대칭 논증 자체는 그대로 유효하다** — *늦게 넣는 비용 ≪ 넣었다가 빼는 비용.* **방향만 바뀌었다.**

### 이 페이지에서 실제로 바뀌는 것 — 넷

| # | 무엇 | 어디 |
|---|---|---|
| **1** | **지원 OS 체크박스는 Windows만.** macOS·SteamOS/Linux는 끈다. 시스템 요구사항 칸도 Windows만 채운다 | 스토어 관리 화면(문안 아님) |
| **2** | ★★ **「곧 macOS도 지원」류 문장을 어디에도 쓰지 않는다** | 7절 금지 목록에 등재 |
| **3** | ★★ **무서명 한 줄이 「Windows에만 필요한 각주」에서 「이 페이지의 유일한 신뢰 문장」으로 승격된다** | 4-3절(반전 반영 완료) |
| **4** | ★★ **스크린샷이 전부 Windows 화면이어야 한다** | 8절 · `CAPTURE_REQUESTS_R6.md` |

> ### ★ 왜 2번이 체크박스보다 중요한가
> 체크박스는 껐는데 **설명문에 약속이 남는** 형태가 흔하다. 그러면 *"선언했다가 뺀다"*의 비용을
> **체크박스 없이 그대로 치른다** — 위시리스트 유저는 상점 **문장**을 읽고 기다리기 때문이다.
> **되돌리기 비용은 체크박스가 아니라 약속에 붙는다.**
>
> ★ **그래도 거짓말은 하지 않는다.** macOS를 **버린 것이 아니다**(4플랫폼 목표는 살아 있다).
> **약속하지 않는 것**과 **없다고 말하는 것**은 다르다 — *"지금은 Windows만 지원합니다"*는 참이고,
> *"macOS는 안 냅니다"*는 우리가 모르는 것이다. **둘 다 쓰지 말고, 지원 OS 표시가 말하게 둔다.**

### ★★ v2.2 추가 (2026-09-05 R7) — **이 페이지에 「Playtest 가입」 칸이 하나 더 생길 수 있다**

`TRUST_AND_SIGNING.md` 9절에서 1차 확인된 것: **본편이 F2P라 본 게임 스팀 키는 기본 발급 대상이 아니다**
(*"Games that are Free-to-Play … cannot request Steam Keys by default"*). 대체 경로는 **Steam Playtest**이고,
그건 **이 페이지의 표면을 바꾼다**:

> 1차 원문: *"Instead of having its own separate store page, your Steam Playtest signup will live
> **right on your main game**, so that customers can sign up and **still Wishlist or Follow the main game**."*

| 무엇 | 페이지에 미치는 것 |
|---|---|
| Playtest 가입 섹션 | ★ **우리 페이지 안에 뜬다.** 별도 페이지가 아니다. 켜고 끄는 스위치는 **Edit Store Page → Special Settings**(문안 아님, 관리 화면) |
| 위시리스트 | ★ **가입해도 위시리스트가 유지된다.** ★★ 반대로 **본 게임 베타(Release State Override) 키는 활성화 시 그 유저의 위시리스트에서 우리를 지운다**(1차) — 그래서 **본 게임 베타 키를 쓰면 안 되는 이유가 페이지 축에도 하나 더 있다** |
| 심사 | Playtest 앱의 스토어 심사는 **캡슐 이미지·아이콘만** 본다(1차). **문안 작업이 늘지 않는다** |

- **근거**: Steamworks *Steam Playtest* / *Steam Keys* 1차 원문(SteamTracking 공개 미러에서 문장 그대로 인용).
- **대가**: 페이지에 **「아직 안 나온 것」 표면이 하나 더 생긴다.** 0-3절이 막으려는 *"미완성인데 나중에 돈 받겠지"*
  인상과 같은 가족이다 — **`Early Access` 태그를 피하기로 한 것과 같은 이유로, Playtest 칸도 상시로 켜 두지 않는다.**
- ★ **버리는 조건**: **U-1**(F2P 제한이 Playtest 자식 appID에도 적용되는지)이 *"적용된다"*로 판명되면
  이 절 전체가 죽는다. **판정 수단은 파트너 사이트 실물 화면뿐이고 사용자 몫이다**(`TRUST_AND_SIGNING.md` 9-7).

### ★ Linux / Steam Deck — 명시적으로 끈다
Proton으로 Windows 빌드가 돌더라도 **Win32 창 열거가 Wine 창만 보고 리눅스 네이티브 창을 못 본다**
⇒ **헤드라인 기능이 죽는다**(구조적 추론, **실기 미확인** — 이 머신에 리눅스가 없다).
⇒ **「Steam Deck 호환성 미검증」**으로 두고 **「지원」으로 표시하지 않는다.**

---

## 0. ★★ v1 → v2: **페이지가 하는 일 자체가 바뀌었다**

### 0-1. 유료 페이지와 무료 페이지는 다른 질문에 답한다

| | v1 (유료 $7.99) | **v2 (무료)** |
|---|---|---|
| 방문자가 묻는 것 | **"이게 8달러만큼 값이 있나?"** | **"이걸 내 컴퓨터에 상주시켜도 되나?"** |
| 페이지의 일 | **가치 증명** | **마찰 제거** |
| 스크린샷의 역할 | 재미를 판다 | **안전과 정직을 판다** |
| 결정 시점 | **페이지에서** | ★ **페이지가 아니라 설치 며칠 뒤** |
| 「하지 않는 것」 목록 | 부록(4절) | ★ **본문으로 승격** |

★★ **핵심 전환**: 무료 소프트웨어에 대한 기본 가정은 **광고**이거나 **데이터**다.
우리는 둘 다 아니고, **그 사실이 무료 페이지의 가장 값진 자산**이다.
유료였을 땐 이게 부록이었지만, **무료가 되는 순간 이게 본문이다.**

### 0-2. ★★ 그래서 DLC 설명은 페이지에서 **줄인다**

**F2P에서 구매 결정은 스토어 페이지에서 일어나지 않는다.** 0원 → 첫 결제가 가장 비싼 순간이고,
그 순간은 **설치하고 며칠 지난 뒤** 앱 안에서 온다(`CHANNEL_PRICING_DECISIONS.md` 3-3).

⇒ **스토어 페이지에서 팩을 길게 설명하면, 설치도 안 한 사람에게 결제를 권하는 셈**이고
그건 무료 제품의 첫인상을 가장 나쁘게 만드는 방식이다(*"무료라더니 결국 파는 거네"*).

**규칙 S-1**: 스토어 페이지에서 DLC에 쓰는 분량은 **「하지 않는 것」 목록보다 짧게** 유지한다.
**규칙 S-2**: 팩을 설명하는 자리는 **한 곳뿐**이다(5절, 짧은 한 문단). 스크린샷 슬롯을 쓰지 않는다.

> ★ **대가를 숨기지 않는다**: 스팀은 DLC 목록을 페이지에 **자동으로 붙이고 우리가 끌 수 없다.**
> 그래서 이 규칙이 지키는 것은 **본문 분량**이지 노출 자체가 아니다.
> 그리고 이 규칙은 **위시리스트 → 팩 구매 전환**을 페이지에서 관측하기 어렵게 만든다.
> **버리는 조건**: 출시 후 팩 구매의 다수가 **설치 없이 페이지에서** 발생하는 것으로 관측되면
> 이 규칙은 틀린 것이고 즉시 폐기한다.

### 0-3. ★ F2P가 새로 만든 위험 셋 — 문안이 이걸 먼저 막아야 한다

**무료는 진입 장벽만 없애는 것이 아니라 「필터」도 없앤다.**

| # | 위험 | 왜 F2P라서 커지는가 | 문안이 하는 일 |
|---|---|---|---|
| **F-1** | ★ **불만이 전부 리뷰로 간다** | 무료 제품은 **환불할 것이 없다.** 유료라면 2시간 안에 조용히 환불하고 끝날 사람이, 무료에서는 리뷰만 남기고 떠난다 (★ **직관 + 스팀 환불 정책 구조**. 무료 제품의 리뷰 성향 통계는 **내게 1차 출처가 없다 — 미확인**) | **1절 첫 줄에서 기대를 맞춘다.** 이 카테고리 최대 불만은 *"게임인 줄 알았다"*다 |
| **F-2** | ★ **말이 안 통하는 사람이 설치한다** | 앱은 오늘 **한국어 전용**(로컬라이제이션 코드 0건). 유료였으면 페이지에서 걸러졌을 영어권 사용자가 **무료라서 그냥 설치**하고 *"Korean only"* 리뷰를 남긴다. 그 리뷰는 **영어화 뒤에도 남는다** | ★ **영어화 전에는 영어 페이지를 올리지 않는다**(기존 게이트). F2P가 이 게이트의 **비용을 키웠다** — 이제 게이트를 어기면 잃는 것이 매출이 아니라 **영구 리뷰 점수**다 |
| **F-3** | ★ **무료 + 무서명 = "이거 뭐지"** | 무료 상주 앱은 신뢰 질문을 **더** 받는다. 그리고 서명이 없다 | **`TRUST_AND_SIGNING.md`**에 별도 문서로 뺐다. 4-3절에서 요약만 |

---

## 1. 짧은 설명 (Short Description) — 스팀 상단, 한 문단

| 한국어 | English |
|---|---|
| 스틱맨 하나가 당신이 지금 열어 둔 창들 위에서 삽니다. 창 모서리를 딛고 걷고, 매달려 내려가고, 넘어질 때마다 다르게 넘어집니다. 클릭은 그대로 통과하고, 창은 1픽셀도 움직이지 않습니다. **무료입니다. 광고도 계정도 없습니다.** | A stick figure lives on top of the windows you already have open. It walks along window edges, hangs off ledges, and falls differently every time. Your clicks pass straight through and your windows never move. **It's free. No ads, no account.** |

★ **v1과 달라진 곳**: 마지막 문장. v1은 *"인터넷에 나가지 않습니다"*였는데
**그 문장은 아직 못 쓴다**(`CLAIM_AUDIT_R5.md` 6절 — 아웃바운드 미실측 + `unity_connect.*` 키 판정 대기).
대신 **오늘 확정적으로 참인 두 가지**(무료 · 광고/계정 없음)로 바꿨다.
「광고 없음」은 **우리가 안 넣기로 한 것**이라 실측이 필요 없고, 「계정 없음」은 로그인 코드가 0건이다.

★ **이 문단이 답하는 것**(M3 4절 순서): ① 뭔지 ② 방해하는지 ③ **무료라면 대가가 뭔지**.
세 번째가 v2에서 새로 들어온 질문이고, **무료 제품에서는 이게 ②보다 먼저 온다.**

---

## 2. 상세 설명 (About)

### 2-1. 첫 줄에서 기대를 맞춘다 (F-1 방어)

| 한국어 | English |
|---|---|
| **조작하지 않습니다. 지켜봅니다.** 버튼을 눌러 시킬 수는 있지만, 기본은 당신이 일하는 동안 옆에서 저 혼자 지내는 것입니다. | **You don't play it. You watch it.** You can tell it to do things, but by default it just gets on with its own day beside you while you work. |

★ **v1보다 더 중요해졌다.** 유료라면 잘못 산 사람이 **환불로** 빠지지만,
무료는 그 필터가 없어서 **전부 리뷰에 남는다**(F-1).

### 2-2. ★★ 무료의 대가를 먼저 말한다 (v2 신설 — 이 자리가 v2의 핵심)

| 한국어 | English |
|---|---|
| **무료입니다. 그리고 대가가 없습니다.** 광고를 띄우지 않고, 계정을 만들라고 하지 않고, 나중에 잠기는 기능도 없습니다. 유료 꾸미기 팩이 따로 있지만 **사지 않아도 전부 그대로 씁니다** — 팩으로만 열리는 능력이나 성능은 없습니다. | **It's free, and there's no catch.** No ads, no account, nothing that locks later. There are paid cosmetic packs, but **you lose nothing by never buying one** — no pack unlocks a stat, a speed, or a feature. |

★ **왜 이 문단이 2절 두 번째 자리에 있나**: 무료 소프트웨어를 볼 때 사람이 두 번째로 하는 생각이
*"그럼 뭘로 벌지?"*다. 그 질문을 **우리가 먼저 꺼내면 답이 되고, 사용자가 먼저 꺼내면 의심이 된다.**

★★ **이 문단은 조건부다 — 근거와 게이트**

| 부분 | 근거 | 상태 |
|---|---|---|
| "광고를 띄우지 않고" | 우리가 안 넣기로 한 것 | ✅ 확정 |
| "계정을 만들라고 하지 않고" | 로그인·계정 코드 0건 | ✅ 확정 |
| "나중에 잠기는 기능도 없습니다" | 기본 42종이 전부 **레벨 해금**, 결제 해금 경로 0(`CLAIM_AUDIT_R4.md` 3절) | ✅ 확정 |
| ★ **"팩으로만 열리는 능력이나 성능은 없습니다"** | `design/systems/ECONOMY_SPEC.md` 2-4의 **3중 방어**(기본 42종만으로 4스탯 캡 도달 / DLC 전설도 슬롯 상한 +10 고정 / 하드 클램프 20) | ★ **설계 확정 · 코드 0줄.** DLC 인프라 **소비자 0곳**(재확인: `DlcPack`/`PackManifest` 프로덕션 참조 0건) |

> ### ★ 게이트 G-F2P (리더 판정 요청)
> **팩이 실제로 존재하는 시점에 이 문장이 참인지 다시 재고 나서 페이지에 올린다.**
> 지금은 **설계 약속**이지 **코드 사실**이 아니다.
> - **1.0에 팩이 없다면**: 이 문단에서 팩 문장을 빼고 앞 세 줄만 쓴다. (그러면 전부 참이다.)
> - **1.0에 팩이 있다면**: 그 팩의 매니페스트에 스탯이 0인지 **감사 1건으로 잠근 뒤** 쓴다.
>   ★ 스탯이 붙는 순간 이 문장은 거짓이고, **그건 우리가 가장 크게 광고한 자리에서의 거짓말**이다.

### 2-3. 남의 창이 지형이다

| 한국어 | English |
|---|---|
| 열려 있는 창의 윗면이 그대로 발판이 됩니다. 창을 옮기면 따라가고, 닫으면 떨어집니다. 다른 창에 가려진 창은 발판이 되지 않습니다. | The top edge of any open window becomes solid ground. Move the window and it moves with you. Close it and it falls. A window buried behind another one isn't a shelf. |

### 2-4. 애니메이션이 아니라 물리다 ★ 이게 헤드라인이다

| 한국어 | English |
|---|---|
| 넘어지는 장면이 미리 만들어져 있지 않습니다. 관절과 무게로 계산되기 때문에, 같은 높이에서 떨어져도 매번 다르게 구르고 다르게 착지합니다. | Nothing about the falling is pre-animated. It's joints and weight, so the same drop from the same height tumbles and lands differently every time. |

★ **왜 이게 헤드라인인가**: "창 위를 걷는다"는 **이 카테고리의 입장료**다
(eSheep 1990년대 · Shimeji 2009 · Desktop Mate · PixelPet 전부 한다).
차별점은 「창을 딛는다」가 아니라 **「창을 딛는데 물리로 딛는다」의 교집합**이고,
조사 범위에서 그 교집합에 있는 제품을 **찾지 못했다**(→ 7절 금지 문구도 함께 읽을 것).

★★ **F2P에서 이 축이 더 중요해졌다.** 무료 경쟁은 **아이템 수**로 이긴다(45종·100종+).
숫자로는 진다. **물리는 개수로 셀 수 없는 유일한 축**이고, 그래서 무료 시장에서 우리가
비교당하지 않을 수 있는 유일한 자리다.

### 2-5. 먼저 나서지 않는다

| 한국어 | English |
|---|---|
| 활을 쏘거나 창에 금을 내는 장난은 **당신이 시킬 때만** 합니다. 가만히 두면 걷고, 앉고, 기지개를 켜고, 가끔 혼잣말을 합니다. **집어 들면 항의합니다 — 어디를 잡았는지에 따라 다른 말을 합니다.** | The showy stuff — archery, cracking a window — only happens **when you ask for it**. Left alone, it walks, sits, stretches, and mutters to itself. **Pick it up and it complains — and what it says depends on where you grabbed it.** |

★ 실측: 장난 계열 확률이 배포 에셋에서 **10/10 전부 0**이다. **버그가 아니라 제품 결정**이다.
★ **R5 신규 문장**(뒷문장): `Dialogue/GrabReactionLines` — 머리 6 / 다리 6 / 몸통 4 / 부위불명 1.
**「9종」이라고 세지 마라**(`CLAIM_AUDIT_R5.md` 1-2).
★★ **개별 대사를 예시로 쓰지 마라.** 풀 **구조**는 커밋됐지만 **문자열은 지금도 움직이고 있다** —
커밋 시점의 한 줄은 `"놔, 놔"`였고 작업 트리는 `"놔줘 놔줘"`다(`CLAIM_AUDIT_R5.md` 0-0-A).
★ **현행 두 빌드 어디에도 이 대사가 없다** — 재빌드 전에는 **캡처도 문안 확정도 하지 않는다.**

### 2-6. 통제권 — ★ 여섯 페르소나가 동시에 걸리는 한 줄

| 한국어 | English |
|---|---|
| 보일지 말지는 당신이 정합니다. **`⌃⌥⌘K` (Windows: `Ctrl+Alt+Win+K`) 한 번이면 캐릭터도 열린 창도 함께 사라지고, 같은 키를 다시 누르면 돌아옵니다.** | Whether it's on screen is your call. **One press of `⌃⌥⌘K` (Windows: `Ctrl+Alt+Win+K`) and the character and every open panel vanish together; press it again and they're back.** |

★★ 이 한 줄이 R5(`화면공유`: 제발 사라져라)와 M2(`보여주는사람`: 절대 사라지지 마라)를
동시에 만족시키는 **유일한 문장**이다(`personas/README.md`).

★ **R5 정정 2건**:
1. **단축키를 양 표기로 쓴다.** `Core/ShortcutLabel` — mac `⌃⌥⌘` / Windows `Ctrl+Alt+Win+`.
   `⌃⌥⌘K`만 쓰면 Windows 사용자 키보드에 없는 키다.
2. **토글이라고 명시한다.** `AppControlDirector` 캡션 원문 *"같은 키를 다시 누르면 돌아옵니다"*.
   「숨긴다」로만 쓰면 못 돌아온다고 오해한다.

★ **여전히 실기 검증 대기**이고, **현행 Windows 배포본(12:42)에 이 코드가 없다.**

### 2-7. ★ 전체화면 위에서 물러나되, 갇히지 않는다 (R5 신규)

| 한국어 | English |
|---|---|
| 전체화면 앱을 켜면 창과 패널이 스스로 물러납니다(캐릭터는 남습니다). 전체화면 **게임**에서는 캐릭터까지 사라집니다. 물러난 동안에도 톱니는 남아 있어서, 눌러서 설정까지 갈 수 있습니다 — **당신이 부른 창만 살아나고, 닫으면 곧바로 다시 물러납니다.** | Go fullscreen and the panels retreat on their own (the character stays). In a fullscreen **game** the character goes too. The gear stays reachable the whole time, so you can still get to Settings — **only what you open comes back, and it retreats again the moment you close it.** |

★ **조건 5개를 반드시 함께 지킨다**(`CLAIM_AUDIT_R5.md` 1-1 C1~C5). 특히:
- **「전부 돌아온다」가 아니다** — 등급 1 진입 전부터 떠 있던 창은 안 돌아온다.
- **`⌃⌥⌘I`(정보창 단축키)는 아직 안 된다** — 저장소가 스스로 `Assert.Ignore`로 선언한 갭.
- **현행 두 빌드에 이 코드가 없다.**

★ **왜 이 문단을 넣는가**: v1에는 *"물러난다"*만 있었고 **"그럼 어떻게 돌아오나"**에 답이 없었다.
그리고 실제로 **그 답이 없는 상태였다**(등급 1을 끄는 방법이 앱 안에 하나도 없었다).
**답이 생긴 뒤에만** 이 문단을 쓴다 — 그게 이 문서의 규율이다.

---

## 3. 기능 목록 (Feature List) — 실재하는 것만

| # | 한국어 | English | 근거 |
|---|---|---|---|
| 1 | 열린 창의 윗면을 딛고 걷는다 | Walks on the top edge of your open windows | 실기 확인, 착지 오차 0.0pt |
| 2 | 창을 옮기면 따라오고, 닫으면 떨어진다 | Follows a window you drag; falls when you close it | 실기 확인 |
| 3 | 가려진 창은 발판이 되지 않는다 | Buried windows don't count as ground | `VisibleTopEdgeSolver` |
| 4 | 턱에서 뛰어내리고 다시 기어오른다 / 모서리에 매달려 내려간다 | Hops down ledges, climbs back up, hangs off edges | 확률·지속 실측 |
| 5 | 넘어짐이 매번 다르다 (물리 시뮬레이션) | Every fall is different — simulated, not animated | Active Ragdoll + IK |
| 6 | 커서로 집어서 던질 수 있다 | Pick it up with your cursor and throw it | `DragThrowController` |
| 7 | **집으면 항의한다 — 잡은 자리에 따라 다르게** | **Complains when grabbed — differently depending on where** | ★ `GrabReactionLines`. **재빌드 필요** |
| 8 | 클릭이 그대로 통과한다 (기본 켜짐) | Clicks pass through by default | `clickThroughDefaultEnabled: 1` |
| 9 | 시켜서 하는 행동 5가지 | Five things you can ask it to do | 명령창 타일 5개(enum 파생) |
| 10 | 소리를 내지 않는다 | It never makes a sound | `m_DisableAudio=1` 빌드 강제 |
| 11 | 아무도 안 볼 때 스스로 프레임을 낮춘다 | Slows itself down when nobody's looking | `FramePacing` ★ **수치 인용 금지** |
| 12 | 언제든 한 키로 숨기고 한 키로 되돌린다 | Hide it and bring it back with one key | ★ 2-6절. **실기 검증 대기** |
| 13 | 걸치는 것 42가지 — 함께 지낸 시간으로 열린다 | 42 things to wear, unlocked by time spent together | ★ 3-1절 조건 필독 |
| 14 | 한국어 / English | Korean / English | ★ **앱 내부 영어화 착지 전에는 이 줄도 페이지도 올리지 않는다** |

★ **9번을 "행동 5가지"로 적었다.** 보관함의 「할 줄 아는 것」은 12종이지만 **명령창 타일은 5개**다 —
큰 수를 쓰고 싶어서 12를 적으면 설치한 사람이 5개를 보고 속았다고 느낀다.
★ **11번에 숫자를 쓰지 않았다.** 성능 수치는 재측정 전까지 인용 금지.
★ **목록에 없는 것**: 메뉴 막대(규명 중) · Alt+Tab 제외(실기 0회) · 네트워크 0(실측 대기).

### 3-1. ★ 13번을 이렇게 쓴 이유 (F2P에서 더 중요해졌다)

- **헤드라인에 안 쓴다.** 무료 경쟁이 45종·100종+를 말한다 — **숫자 싸움은 무료 시장에서 확실히 진다.**
- **숨기지도 않는다.** 목록 13번에 사실로 적는다.
- ★ **"함께 지낸 시간으로 열린다"를 반드시 같이 쓴다.** 신규 설치 시 열려 있는 것은
  **7개(그중 1개는 「없음」) = 실질 6종**이고 나머지는 `???`로 보인다.
  **이 문장 없이 42만 적으면 설치 직후 "속았다"가 되고, F2P에서 그건 곧장 리뷰다**(F-1).

### 3-2. ★★ v2.2 신규 (R7) — **P22(집중 유지 장치로 사는 사람)에게 이 페이지가 정직하게 약속할 수 있는 것**

`product-strategy`가 P22를 세우며 이렇게 적었다: ***"이 사람에게 팔 수 있는 것이 지금 SKU에 하나도 없다.
지갑이 열리는데 계산대가 없다."*** **그 진단은 맞다. 그런데 마케팅 축의 결론은 다르다.**

> ### ★ 본편이 무료다. **P22에게 우리가 파는 것은 팩이 아니라 「설치」다.**
> F2P 페이지에서 **전환은 결제가 아니라 설치**다. P22는 **지금 당장 결제 대상이 아니지만
> 지금 당장 설치 대상**이고, 팩 계산대는 그 뒤에 세우면 된다(아이디어 W는 후보 등록 단계).
> ⇒ **"팔 게 없으니 언급하지 말자"는 틀린 결론이다. "팔 것처럼 말하지 말자"가 맞는 결론이다.**

**오늘 실측으로 참인 것 — 이만 쓸 수 있다** (`FocusSessionPopover` · `FocusWatchDirector` · `GearRadialMenuWidget`):

| 사실 | 근거(실측) |
|---|---|
| 세션 길이를 **15 / 25 / 50분** 중에 고른다 | `FocusSessionPopover.DurationMinutes = {15,25,50}` · 기본 25분 |
| 톱니 → 부채꼴 **첫 번째 버튼**이 집중 모드다 | `GearMenuButton.FocusMode = 0` · `ButtonNames[0] = "집중 모드"` |
| 세션 중에는 **톱니 링이 잔여 시간 호**가 된다 | `RingTrack` — *"집중 모드 전용 — 세션 중에는 잔여 시간 호가 된다"* |
| **창을 자주 바꾸면 반응한다** | `FocusWatchDirector` — 전경 창 전환 빈도. ★ **신규 폴링을 만들지 않고 이미 도는 발판 캐시를 읽는다** |
| 반응 민감도를 **관대/보통/예민** 3단계로 고른다 | 팝오버 칩 3개 |
| ★ **소리를 내지 않는다** | `m_DisableAudio=1`(빌드 강제) — **집중 도구에서 이건 기능 목록 10번보다 이 자리에서 더 값이 크다** |

**쓰면 안 되는 것** (7절에 등재했다): *뽀모도로 · 타이머 앱 · 생산성 도구 · 집중력 향상*.
- **시계 위젯은 아직 없다.** 사용자가 확정한 것은 **방향**이고 코드에는 없다 — **미구현을 확정처럼 파는 것**이다.
- 이 단어를 쓰는 순간 **생산성 앱의 자로 재진다** — 통계·리포트·기기 동기화가 전부 없다.
- ★ **감시 민감도는 설정창에 없다**(팝오버에만 있다 — `FocusWatchDirector` 클래스 문서가 그렇게 자백한다).
  *"설정에서 조절합니다"*라고 쓰면 그 자리에서 거짓이다.

> ### ★ 배치 판정 — **3-1절(42종)과 같은 처방**
> **기능 목록에 사실로 한 줄 적고, 헤드라인·짧은 설명·태그에는 넣지 않는다.**
> - **근거**: 이 페이지가 답하는 질문은 *"상주시켜도 되나"*이지 *"집중이 되나"*가 아니다(0-1절).
>   짧은 설명에 집중을 올리면 **잘못 온 사람이 늘고**, F2P는 그들을 걸러 낼 가격 장벽이 없다(0-3절 F-1).
> - **대가**: P22가 검색으로 우리를 찾는 경로를 포기한다. **태그·짧은 설명에 없으면 검색에 안 걸린다.**
> - ★ **버리는 조건**: 집중 시계가 **위젯으로 실제 착지하면** 이 판정을 다시 연다.
>   그때는 헤드라인 후보이고, 아이디어 W(7번째 팩)도 그때 판정한다.

**제안하는 기능 목록 15번** (게이트: **Windows 실기 확인 후**):

| 한국어 | English |
|---|---|
| 15분·25분·50분 집중 세션을 켜 두면 끝날 때까지 곁에 있는다. 창을 자주 바꾸면 조용히 쳐다본다. | Start a 15/25/50-minute focus session and it stays beside you until the timer runs out. Switch windows too often and it just… looks at you. |

★ **"조용히 쳐다본다"에 「조용히」를 넣은 것은 수사가 아니라 사실이다** — 이 앱은 소리를 내지 않는다.

### 3-3. ★ v2.2 신규 (R7) — **P21(자국 통화로 값을 보는 사람)이 이 페이지에 만드는 규칙은 하나뿐이다**

- ★ **본편이 $0이라 P21은 이 페이지의 대부분에 걸리지 않는다.** 가격 숫자가 등장하는 자리는 **5절 팩 문단 하나**다.
- **그리고 5-1절 초안에는 이미 가격 숫자가 0개다** — 우연이 아니라 규칙 S-2("한 문단")의 부수효과다.
- ⇒ **P21에 대한 조치는 「그 상태를 깨지 않는다」가 전부다.** 7절에 금지 항목으로 등재했다.
- ★ **직관이 아니라 구조적 사실**: 스팀은 지역별로 다른 통화·다른 숫자를 **가격 칸에** 표시한다.
  본문에 USD를 박으면 **두 숫자가 한 화면에 동시에 뜬다.**
- ★ **버리는 조건**: 지역 가격 권장표를 실제로 확인해(현재 **미확인** — `strategy/ROADMAP.md` 39-1)
  일부 지역에서 팩이 최소 단위로 눌리는 것이 드러나면, **그건 문안이 아니라 가격 결정 문제**다
  → `product-strategy` 소관. **나는 가격을 정하지 않는다.**

---

## 4. ★★ "하지 않는 것" 목록 — **v2에서 본문으로 승격**

v1에서 이건 4절(부록)이었다. **무료가 되면서 이 목록이 페이지의 두 번째 기둥이 된다**(0-1절).

| 한국어 | English | 상태 |
|---|---|---|
| 광고가 없습니다. | No ads. | ✅ 확정 |
| 계정도, 로그인도 없습니다. | No account, no login. | ✅ 로그인 코드 0건 |
| OS 권한을 요구하지 않습니다 — 접근성·화면 기록·관리자 권한 어느 것도. | No OS permissions — no accessibility, no screen recording, no admin. | ✅ 소스 실측(권한 API 0건 + 양성 대조 2종) |
| 당신의 창을 옮기거나 크기를 바꾸거나 닫지 않습니다. | It never moves, resizes, or closes your windows. | ✅ 인터페이스에 그 메서드가 **없다** + 자동 감사 |
| 당신의 파일과 바탕화면 아이콘을 건드리지 않습니다. | It never touches your files or desktop icons. | ✅ 원칙 3 |
| 창 제목을 읽지 않습니다. macOS는 앱 이름과 위치만 보고, Windows는 **제목이 비어 있는지만** 확인합니다(그 확인에 쓰는 버퍼가 1글자라 제목을 읽을 수가 없습니다). | It doesn't read your window titles. On macOS it only sees app names and positions; on Windows it only checks whether a title is empty — the buffer is one character long, so it *cannot* read the title. | ✅ 양 플랫폼 실측(4-2절) |
| 소리를 내지 않습니다. | It never makes a sound. | ✅ 빌드 강제 |
| 마우스나 키보드를 대신 눌러 주지 않습니다. | It never synthesizes a click or a keystroke. | ✅ 합성 입력 API 7종 **0건** + 양성 대조 |
| ~~인터넷에 접속하지 않습니다.~~ | ~~No internet.~~ | ★ **보류.** `CLAIM_AUDIT_R5.md` 6절 — 아웃바운드 미실측 + `unity_connect.*` 키 판정 대기 |

★★ **마지막 줄을 지우지 않고 취소선으로 남긴다.** 이게 이 문서의 방식이다 —
**"왜 안 썼는가"가 다음 라운드의 자산**이고, 지우면 누군가 좋은 뜻으로 다시 넣는다.

### 4-1. ★ 예외 1건 — 반드시 같은 자리에 적는다

| 한국어 | English |
|---|---|
| **예외가 하나 있습니다.** Windows에서 작업표시줄을 자동 숨김으로 쓰신다면, 실행하는 동안에만 자동 숨김을 해제하고 종료할 때 원래대로 되돌립니다. 강제 종료로 되돌리지 못했다면 다음 실행이 먼저 복구합니다. 원래 자동 숨김을 안 쓰시는 분에게는 아무 일도 일어나지 않고, 컴퓨터에 흔적도 남지 않습니다. | **One exception.** If you use auto-hide on the Windows taskbar, we turn it off while the app runs and put it back when you quit. If a crash prevents that, the next launch restores it first. If you don't use auto-hide, nothing happens at all and nothing is written to your machine. |

★ **숨기면 위 표 전체가 거짓말이 된다.** 그리고 이 예외는 **Windows 스크린샷에 그대로 찍힌다**
(작업표시줄이 나타난 상태). **그림과 고지가 어긋나지 않게** 한다.

### 4-2. 창 제목 — 양 플랫폼 실측(v1에서 내가 정정한 항목, 유지)

| 플랫폼 | 실측 | 근거(심볼) |
|---|---|---|
| macOS | 앱 이름(`kCGWindowOwnerName`)과 bounds만. 제목 API를 **부르지 않는다** | `MacWindowService` — `kCGWindowName` **0건** |
| **Windows** | 제목 API를 **부른다**. ★ 단 버퍼가 `TitleProbeBufferChars` — *"1글자 + 널 종단이면 충분하다"* | `Win32WindowService` |

> **"안 부른다"는 Windows에서 거짓이고, "제목을 읽지 않는다"는 양쪽에서 참이다.**
> **버퍼가 1글자라 읽을 수가 없다** — 정책이 아니라 **구조적 불가능**이라 훨씬 강한 문장이다.

### 4-3. ★ 무서명 — 이 페이지에 무엇을 적을 것인가 (F-3)

**상세 판정과 대응 문장은 `TRUST_AND_SIGNING.md`가 정본이다.** 여기엔 결론만 둔다.

> ### ★★ v2.1 반전 (2026-09-05) — **이 절의 무게중심이 통째로 바뀌었다**
>
> | | v2 (Win+Mac 동시) | **v2.1 (Windows 단독)** |
> |---|---|---|
> | macOS | 서명+공증이 **1.0 차단** → 페이지 시점에 이미 서명됨 → **문장 불필요** | ★ **macOS depot이 1.0에 없다.** 이 줄은 **죽었다**(항목 자체가 페이지에 없다) |
> | Windows | 무서명 → **한 줄 필요**(각주 성격) | ★★ **1.0 페이지 = 무서명 Windows 페이지.** 이 한 줄이 **각주가 아니라 이 페이지의 신뢰 문장**이다 |
>
> ★ **왜 이게 커졌나 — 분모가 바뀌었다.** 종전에는 출시 시점 Windows 사용자 비율이 **0%**였다
> (macOS 먼저 나가므로). **지금은 100%다.** 즉 **모든 첫 사용자가 이 경고를 볼 수 있는 경로에 있다**
> (`docs/strategy/WINDOWS_STEAM_LAUNCH_CRITICAL_PATH.md` M-13이 짚은 그대로).
>
> ⇒ **판단 J-3(무서명 한 줄을 넣을 것인가)에 대한 내 권고는 「넣는다」에서 「반드시 넣는다」로 강해진다.**
> 근거: 이 페이지에는 이미 **「하지 않는 것」 목록**이 본문으로 서 있고(4절),
> **그 목록의 신뢰가 이 한 줄로 완성된다** — *"불리한 것도 먼저 적는 곳"*이라는 증거이기 때문이다.
> **판정은 리더.**

- **Windows(1.0 = 전량)**: 서명은 **첫 업데이트 전까지 연기**(리더 확정, 전환 후에도 **유지**).
  그러므로 **1.0 페이지 시점에 무서명이다.** → ★ **한 줄이 필요하다.**
  문안·근거·게이트는 `TRUST_AND_SIGNING.md` 5-1 · 8절.
  ★ **다만 「연기의 안전망」이 무효화됐다** — 조달 리드타임이 **2~3주**로 재산정되면서
  *"신고 3건 뜨면 그때 산다(T5)"*가 **실행 불가능**해졌다(`TRUST_AND_SIGNING.md` 8절).
- ~~**macOS**: 서명+공증이 1.0 차단이므로 페이지 시점에 이미 서명돼 있다~~
  → ★ **v2.1에서 죽었다.** macOS를 나중에 depot으로 추가할 때 **되살아난다**(지우지 않는다).

---

## 5. ★ 유료 팩 — **한 문단. 이게 전부다** (규칙 S-2)

### 5-0. ★★ 먼저 답한다: **그러면 팩은 무엇을 파는가**

`product-strategy`가 자기 상품을 이렇게 깎았다:
> *"이 제품의 DLC에는 코스메틱의 핵심 구매 동기가 구조적으로 없다 — **과시도 없고 능력도 없다.**"*

**그 진단은 맞다. 그리고 나는 그것이 약점이 아니라 이 팩의 정의라고 본다.**

**마케팅의 답 — 세 줄:**

1. ★★ **상주 앱은 「질려서」 죽지 않는다. 「투명해져서」 죽는다.**
   게임은 콘텐츠가 소진되어 끝나지만, 하루 8시간 화면 구석에 있는 것은 **어느 날부터 눈에 안 들어온다.**
   `personas/R4_이미써봄.md`가 기록한 이탈 형태가 정확히 그것이다 — *"일주일 만에 지웠다"*.
   ⇒ **팩이 파는 것은 「새 콘텐츠」가 아니라 「다시 눈에 들어오는 것」이다.**
   과시도 능력도 아닌 **제3의 동기**이고, **상주 앱에만 있는 동기**다.

2. ★ **아무도 안 본다는 것은 결함이 아니라 다른 시장이다.**
   보통의 코스메틱은 **관객이 있어야 값이 있고**, 그래서 관객이 없을 때 산 것은 후회가 된다.
   우리는 반대다 — **관객이 처음부터 없으므로 후회할 관객도 없다.**
   ⇒ **"아무도 안 봅니다. 그래서 취향으로만 고르면 됩니다."**
   이건 **과시 부재를 방어하는 문장이 아니라 그것을 상품으로 만드는 문장**이다.

3. ★ **능력이 없다는 것을 우리가 먼저 말한다.**
   F2P의 최대 이탈 사유는 *"안 사면 뒤처진다"*이고, 우리는 그게 **구조적으로 불가능**하다
   (`ECONOMY_SPEC` 2-4 3중 방어). **말하지 않으면 사용자는 F2P의 기본값(결국 페이월)을 가정한다.**
   ⇒ 2-2절이 이 일을 한다.

> ### ★ 팩 한 줄 (`product-strategy` P-0과 맞물리는 지점)
> 상품전략의 P-0은 **"그 애가 다르게 말하고 다르게 움직이는 하루를 판다"**다.
> **나는 그 문장을 구매 동기로 번역한다**:
>
> > **"몇 달 뒤에도 아직 눈에 들어오게 하는 것 — 그게 팩입니다."**
>
> ★ **그리고 이 문장은 제작 요건이기도 하다**(상품전략이 이미 같은 말을 했다):
> **팩에 대사와 모션이 빠지면 그 팩은 「다시 눈에 들어오게」 하지 못한다.** 옷만 든 팩은
> 첫날 한 번 갈아입히고 끝이고, 그러면 위 세 줄이 전부 거짓이 된다.
> → `design-narrative` / `design-motion`에 **리더 경유**로 전달 요청.

### 5-1. 페이지에 실을 문단 (초안)

| 한국어 | English |
|---|---|
| 꾸미기 팩이 따로 있습니다. 옷이 여섯 벌 들어 있는 게 아니라, **그 팩을 입은 동안 이 애가 다르게 말하고 다르게 움직입니다.** 사지 않아도 게임은 전부 그대로입니다. | There are cosmetic packs. A pack isn't six outfits — **while it's on, this thing talks differently and moves differently.** Buying none of them changes nothing about what you can do. |

> ### ★ 게이트 (2-2절 G-F2P와 같은 게이트)
> **팩이 실재하고, 그 팩이 실제로 대사·모션을 바꾸는 것을 확인한 뒤에만 이 문단을 올린다.**
> 오늘 실측: `DlcPack` / `PackManifest` **프로덕션 참조 0건**, 세트 어조 전환 **코드 0줄**.
> **1.0에 팩이 없다면 이 절 전체를 페이지에서 뺀다** — 그게 정직한 상태다.

---

## 6. 태그 (스팀)

**강조**: `Free to Play` / `Desktop Companion`(2026-05 신설 1급 태그) / `Casual` / `Physics` /
`Relaxing` / `Singleplayer` / `Cute` / `2D` / `Minimalist`

★ **`Free to Play`가 v2에서 새로 들어왔다.** 무료라는 사실이 태그로도 드러나야 한다 —
가격 표기만으로는 필터에 안 걸린다.
★ **`Physics`를 반드시 넣는다** — 우리 유일한 교집합 축이고, 무료 시장에서 **개수로 비교당하지 않을
유일한 자리**다(2-4절). 조사 범위에서 이 카테고리에 이 태그를 다는 제품이 드물었다.

**피할 것**: `Idle` / `Clicker`(조작을 기대하게 만든다) / `Simulation` 단독 / `Anime`(우리 조형이 아니다).
★ **v2 추가로 피할 것**: **`Early Access`** — 무료 + 얼리액세스는 *"미완성인데 나중에 돈 받겠지"*로
읽힌다(★ 직관, 출처 없음).

★ **미확인**: 위 태그가 스팀 태그 목록에 **정확히 그 이름으로 존재하는지** 나는 확인하지 못했다.
Steamworks 태그 목록은 사용자가 브라우저로 확인해야 한다.

---

## 7. ★★ 쓰면 안 되는 문장 (확정 금지 목록)

| 금지 문구 | 왜 |
|---|---|
| **"화면 공유를 자동으로 감지합니다"** | Apple·Microsoft 둘 다 공개 API 없음. **즉시 거짓 소재** |
| **"세계 최초" / "유일한" / "혁명적인"** | 조사가 보장하는 것은 **"찾지 못했다"**까지. 30년 된 카테고리다 |
| **"창을 발판으로 딛습니다"를 차별점으로** | **입장료**다. 기능 목록에는 적되 차별점으로 내세우지 않는다 |
| **성능 수치 일체**(메모리·CPU·전력·절감률) | 재측정+승인 전 금지. ★ 경쟁 비교는 **귀속 기준이 다르다**(dwm.exe vs 앱 자체) |
| **"모드를 막지 않습니다" / 커뮤니티 모드 소구** | 우리도 **플러그인 봉인**이다. 이 축으로 치면 출시 직후 같은 비판을 정면으로 받는다 |
| **"iPad/iPhone에서도"** | 배선 0 |
| **「이벤트」 관련 / 격파 놀이 / DLC 로드맵 약속** | 폐지·삭제·미확정 |
| **아이템 개수를 헤드라인 숫자로** | 무료 경쟁이 45종·100종+. ★ **F2P라서 더 확실히 진다** |
| ★ **"메뉴 막대를 덮지 않습니다"** | **규명 중**(`CLAIM_AUDIT_R5.md` 5절). 리더 실기와 코드 주석이 서로 다른 말을 한다 |
| ★ **"Alt+Tab에 안 뜹니다" / "작업표시줄에 안 뜹니다"** | ★★ **2026-09-05 사유 축소 — 셋 중 둘이 죽었다.** ~~① 미커밋~~(커밋 `1f7e139`) · ~~③ 배포본 부재~~(Windows 09-03 DLL 실측 2건) · ★ **② Windows 실기 0회만 남았다.** **여전히 금지**이되 **낮 세션 A-6 30초면 열린다** → `TRUTH_INVENTORY.md` R6-1 |
| ★ **"곧 macOS도 지원합니다" / "macOS 버전 준비 중"류** | ★ **v2.1 신규.** 지원 OS를 Windows만 선언한다(I-2). **체크박스를 꺼도 설명문에 약속이 남으면 되돌리기 비용을 그대로 치른다** — 0-4절 |
| ★ **"Steam Deck에서 됩니다" / Linux 지원** | ★ **v2.1 신규.** Proton에서 Win32 창 열거가 **Wine 창만 본다** ⇒ 헤드라인 기능이 죽는다(구조적 추론·실기 미확인). **「미검증」으로 두고 지원으로 표시하지 않는다** |
| ★ **"상점" / "다음 업데이트에 들어옵니다"를 페이지에 옮겨 적는 것** | ★ **v2.1 신규.** 앱 화면(`ShopNotice`, 양 빌드 실측 2건)에는 그 문구가 있으나 **페이지에 옮기면 로드맵 약속이 된다.** 스크린샷에 그 탭이 들어가는 것도 같은 문제 → `CAPTURE_REQUESTS_R6.md` Q-1 |
| ★ **집중 모드 `F` / 할일 알림 `J` / 하드웨어 반응 `H`를 단축키로** | ★ **v2.1 신규.** 그 셋은 **개발 게이트 뒤**였고 오늘 **표기가 제거됐다**. 집중 모드는 **톱니 → 부채꼴 ①**로 안내한다. 나머지 둘은 **사용자 진입점이 없다** — *"시키면 합니다"*의 예시로 쓰지 마라 |
| ★ **"인터넷에 접속하지 않습니다"** | 아웃바운드 미실측 + `unity_connect.*` 키 판정 대기 |
| ★ **"서명되어 있어 경고가 안 뜹니다"**(Windows) | **거짓이다.** OV 서명도 첫 경고를 없애지 못한다(Microsoft Learn 표). `TRUST_AND_SIGNING.md` |
| ★ **"팩을 사면 더 강해집니다" / 스탯·능력 암시** | 우리 설계가 정반대다(3중 방어). 이 방향으로 한 줄이라도 쓰면 2-2절 전체가 무너진다 |
| ★ **"9종의 잡힘 반응"류 합계 표기** | 한 번에 만나는 후보는 최대 6, 최소 1 |
| ★★ **문장 본문에 가격 숫자를 적는 것**(`$4.99` / `19,900원` 등) | ★ **v2.2 신규 (P21).** 스팀은 **자국 통화·지역 가격표**로 표시한다. 본문에 USD를 박으면 **그 지역 유저가 보는 숫자와 어긋난다** — 우리 문안이 그 자리에서 거짓이 된다. **가격은 스팀의 가격 칸이 말하게 둔다.** 5-1절 초안은 이미 이 규칙을 지키고 있다(숫자 0개) — **깨지 마라** |
| ★★ **"뽀모도로" / "타이머 앱" / "생산성 도구" / "집중력 향상"** | ★ **v2.2 신규 (P22).** ① **시계 위젯은 아직 없다** — 사용자가 확정한 *방향*일 뿐 코드에 없다(미구현을 확정처럼 파는 것). ② 이 단어들을 쓰는 순간 **생산성 앱과 같은 자로 재진다** — 통계·리포트·기기 동기화가 전부 없는데 그 기대가 붙는다. ③ **효과 주장**(집중력이 는다)은 우리가 잴 수 없다 |
| ★★ **"할일을 때가 되면 들고 옵니다" / "배터리가 없으면 알아서 반응합니다"류** | ★ **v2.2 신규 (R7 실측).** 이 둘은 **사용자 진입점이 없고**, ★ **출하 기본값에서 자율 발동도 꺼져 있다**(`todoReminderChance: 0` · `enableAutonomousHardwareReactions: 0` — 코드·에셋 양쪽 실측). ⇒ **기본 설치 상태에서 영영 일어나지 않는다.** 보관함 카드 문구를 페이지로 옮겨 적으면 **그대로 거짓 소재**다 → `TRUTH_INVENTORY.md` R7-2 |

### 7-1. 폐기된 판정 보존 (지우지 않는다)

v1의 6-1절에는 *"전체화면에서 물러난다는 문장은 거짓"* · *"단축키 숨김은 거짓"*이라는 판정이
있었고, **둘 다 뒤집혔다.** v1의 4절 「하지 않는 것」에는 *"인터넷에 접속하지 않습니다"*가
**조건부 허용**으로 있었고, **이번에 다시 보류로 내렸다.**

★ **판정은 양방향으로 움직인다.** 금지에서 허용으로 가는 것만큼 **허용에서 금지로 돌아오는 일**도
실제로 일어난다. 그 이력이 이 문서에 남아 있어야 다음 라운드가 같은 자리를 두 번 밟지 않는다.

---

## 8. 스크린샷 · 동영상 슬롯 배치 (v2 재배치)

순서와 구도는 `SHOT_LIST.md`, 촬영 단위는 ★ **`CAPTURE_REQUESTS_R6.md`**가 정본(R5는 대체됨).
★ **v2에서 슬롯 배정이 바뀌었다** — 「안전 증명」 슬롯이 앞으로 왔다(0-1절).

> ### ★★ v2.1 (2026-09-05) — **슬롯 순서는 그대로. 촬영 플랫폼이 전부 바뀐다**
> 아래 표의 **「빌드」 칸은 전부 폐기다.** *"지금 빌드 가능"*은 **macOS 빌드**를 뜻했고,
> **지원 OS를 Windows만 선언한 페이지에 macOS 화면을 걸 수 없다.**
> - 창 크롬이 다르다 — 헤드라인이 「남의 창 **윗면**」인데 그 윗면 생김새가 두 OS에서 다르다.
> - 단축키 표기가 다르다 — macOS 컷은 `⌃⌥⌘K`로 찍힌다. **Windows 페이지에 그게 박히면
>   지원하지 않는 키 조합을 가르치는 것**이다.
> - 4-1절의 **예외 고지(작업표시줄 자동 숨김)는 Windows 화면에만 찍힌다.** 고지와 그림이 같은 플랫폼에 있어야 한다.
>
> ⇒ **새 촬영 계획은 `CAPTURE_REQUESTS_R6.md` 1~2절(W1~W10).**
> **macOS 컷은 버리지 않는다 — 개발일지·커뮤니티·내부 판정용으로 계속 쓴다.**

| 슬롯 | 소재 | v1 대비 | 빌드 |
|---|---|---|---|
| **동영상 1** | `A-1` 같은 높이 3테이크 3분할 — **물리 증명** | 유지(무료 시장에서 유일한 비교 불가 축) | 지금 빌드 가능 |
| 스크린샷 1 | `S-1` 창 위에 서 있는 헤드라인 정지컷 | 유지 | 지금 빌드 가능 |
| **스크린샷 2** | ★ **`G-1` 균열 + 그 위로 클릭 통과** | **1단계 올림** — 「방해 안 한다」가 무료 페이지의 두 번째 질문 | 지금 빌드 가능 |
| **스크린샷 3** | ★ **설정창 — 끌 수 있다는 증명** | **3단계 올림**(v1 6번 → 3번). 무료 페이지에서 통제권은 부록이 아니다 | 지금 빌드 가능 |
| 스크린샷 4 | `A-2` 발이 창 상단선에 정확히 닿은 클로즈업 | 한 단계 내림 | 지금 빌드 가능 |
| 스크린샷 5 | 행동 명령창 — **"시키면 합니다"의 증거** | 유지 | 지금 빌드 가능 |
| 스크린샷 6 | `V-1` 활쏘기 마지막 정중앙 | 한 단계 내림 | 지금 빌드 가능 |
| ~~장비/커스터마이즈~~ | ★ **보류** | 장비 색 대비 수정 라운드 뒤(D등급) | — |
| ★ **후보(신규)** | **잡힘 대사 컷** — 머리를 잡았을 때와 다리를 잡았을 때 2분할 | 2-5절 신규 문장의 증거 | ★ **새 빌드 필요** |

★ **「무료」를 그림으로 찍을 수 없다.** 그래서 무료 페이지의 신뢰는 **스크린샷이 아니라 4절 목록**이
진다 — 슬롯 재배치가 그 사실을 반영한 것이다.

---

## 9. 플랫폼 영향

### ★ v2.1 (2026-09-05) — 반전 후
- **Windows 영향: 이 페이지 전체가 Windows 대상이 됐다.** ① 지원 OS 체크박스 Windows만(0-4절)
  ② 무서명 한 줄이 **각주 → 신뢰 문장**으로 승격(4-3절) ③ 스크린샷 전량 Windows(8절)
  ④ 금지 문구 4건 신규 등재(7절) ⑤ 단축키 표기는 `Ctrl+Alt+Win+*`가 **1차 표기**다.
- **macOS 영향: 함께 검토함 — 페이지에서 내려가되 지우지 않는다.** 4-3절의 macOS 줄은
  **취소선으로 보존**했다. macOS를 depot으로 추가하는 날 **그대로 되살아난다**(서명·공증 요구는
  사라진 것이 아니라 **일정 공식에서만 빠졌다**).

### v2 (2026-09-03) — 기록 보존
- **Windows 영향: 함께 검토함(문서만).** ① 4-1 예외 고지는 **Windows에만** 해당하고 스크린샷에 찍힌다.
  ② **단축키를 `Ctrl+Alt+Win+K`로 병기**했다(2-6절) — v1은 macOS 표기만 있었다.
  ③ **무서명 대응 한 줄이 Windows에만 필요**하다(4-3절, `TRUST_AND_SIGNING.md`).
  ④ **현행 Windows 배포본(12:42)에 2-5·2-6·2-7절 코드가 전부 없다** — Windows 소재는 재빌드 뒤.
- **macOS 영향: 없음(문서만).** 프로덕션 `.cs` 수정 0건. macOS는 서명·공증이 **1.0 차단**이므로
  페이지 시점에는 이미 서명돼 있고, 그래서 **무서명 문장이 필요 없다**(4-3절).
  ★ macOS 전용 보류 1건: **예약 띠(메뉴 막대) 규명 중** — 7절 금지 목록에 있다.

---
---

# ★★★ v2.3 (2026-09-05 저녁, R8) — **장비 이식 · 조작 변경 반영**

작성: `marketing` R8. **여전히 게시 금지 · 리더 승인 대기.**
프로덕션 `.cs`/`.asset` 수정 0건. 앱 실행 0회.

> ## 이 개정이 하는 일 — 셋
> 1. **장비 문장을 한 단계 올린다.** 지금 페이지에서 장비는 기능 목록 13번 한 줄뿐이고,
>    그건 *"27색 중 21색이 대비 미달"*이던 시절의 배치다. **그 사유가 실측으로 죽었다**(v2.3-1).
> 2. **조작 변경에 걸린 문장 2개를 「동결」한다.** 지우지도 고치지도 않고 **얼린다** —
>    무엇이 참이 될지 아직 모르는 상태에서 고쳐 쓰면 **두 번 고치게 되고, 그 사이 버전이 밖으로 나갈 위험**이 있다(v2.3-2).
> 3. **내 표기 오류 1건을 정정한다**(v2.3-3).

---

## v2.3-1. ★★ 장비 — **보류 사유가 죽었다. 그런데 승격은 아직이다**

### 무엇이 바뀌었나 (실측)

| v2까지의 보류 사유 | R8 실측 | 상태 |
|---|---|---|
| *"장비 27색 중 21색이 몸에서 대비 미달(최악 1.60:1)"* | `EQUIPMENT_PALETTE.md` §6-(1): 배경 6종 전부에서 재질 24색 **최악 3.25~3.84**, **미달 0건** | ★ **죽었다** |
| *"카드가 회색 덩어리로 보인다"*(사용자 신고) | 종전 워시 채움↔바탕 **1.20~1.32** → R19 **3.62~4.88** | ★ **죽었다(설계상)** |
| *"등급색이 조각을 먹는다"* | 등급색을 조각에서 뺐다(R-7). 등급 = 리본 칸 수·낱말·프레임 | ★ **죽었다** |
| ★ **"어느 빌드에도 없다"** | `AccessoryHandoffPalette` 바이트 프로브 **win 0 / mac 0**(양성 대조 `걸치는 것` 1/1 · 음성 `ZZZ없는문자열ZZZ` 0/0) | ★★ **살아 있다 — 유일하게 남은 것** |
| ★ **"커밋되지 않았다"** | `git ls-files` — `AccessoryHandoffPalette.cs` · `AccessoryShapeBuilder.Handoff.cs` **둘 다 미추적** | ★★ **살아 있다** |

⇒ **문안은 지금 쓸 수 있고, 그림은 아직 못 찍는다.** 아래 문단은 **문안만** 확정하고
**게시는 규율 M-1 통과 후**로 건다.

### ★ 2-8절 신설 (초안) — 상세 설명 마지막 자리

> **왜 2절 마지막인가**: 꾸미기는 **사는 이유가 아니라 남는 이유**다.
> 앞으로 당기면 「꾸미기 앱」으로 분류되고, 그 시장에서 우리는 **개수로 확실히 진다**(3-1절).
> 뒤에 두면 *"오래 둘 만한가"*라는 질문에 답하는 자리가 된다.

| 한국어 | English |
|---|---|
| 걸칠 것이 42가지 있습니다. 모자와 안경과 망토는 **머리 뒤와 앞으로 나뉘어 얹히고**, 재질마다 색이 다릅니다. **함께 지낸 시간으로 열립니다 — 사서 여는 것이 아닙니다.** 그리고 무엇을 걸치든 **더 빨라지거나 더 세지지 않습니다.** | There are 42 things to wear. Hats, glasses and capes sit in front of and behind the head, and each material has its own colour. **They open with time spent together — not with money.** And nothing you put on makes it faster or stronger. |

★ **문장 하나하나의 근거**

| 부분 | 근거 | 상태 |
|---|---|---|
| "42가지" | 기존 13번과 같은 근거. ★ **헤드라인·짧은 설명에는 여전히 안 쓴다** | ✅ |
| ★ "머리 뒤와 앞으로 나뉘어 얹히고" | R19 모자 2층(챙 장축 분할 · 왕관 뒷벽). **이게 이번 이식의 실제 알맹이**이고, **경쟁 제품 설명문에서 이런 문장을 본 적이 없다** | ★ **미빌드** |
| "재질마다 색이 다릅니다" | 재질 24색, 6종 배경 교집합 대역 안 24/24 | ★ **미빌드** |
| "함께 지낸 시간으로 열립니다 — 사서 여는 것이 아닙니다" | 기본 42종 전량 레벨 해금, 결제 해금 경로 0 | ✅ 확정 |
| ★ "더 빨라지거나 더 세지지 않습니다" | 2-2절과 **같은 약속을 장비 문단에서 다시 한다** | ★ 게이트 G-F2P와 **같은 조건** |

★★ **마지막 문장을 빼지 마라.** 꾸미기 문단은 **가만두면 「사면 세진다」로 읽히는 유일한 문단**이다.
2-2절에서 한 번 말했다고 여기서 안 해도 되는 것이 아니다 — **읽는 사람은 2-2절을 안 읽고 여기로 온다.**

### 기능 목록 13번 개정

| 지금 | v2.3 |
|---|---|
| 걸치는 것 42가지 — 함께 지낸 시간으로 열린다 | 걸치는 것 42가지 — **재질마다 다른 색, 머리 앞뒤로 나뉜 겹**. 함께 지낸 시간으로 열린다 |

★ **"16종"이라고 쓰지 마라.** 이번에 인계본 조형으로 다시 지은 것은 16종이고
**나머지 26종은 v1 경로 그대로다**(`EQUIPMENT_PALETTE.md` §4-2). **한 문장에 두 세대를 섞지 않는다.**

---

## v2.3-2. ★★★ 조작 변경 — **문장 2개를 동결한다**

리더가 알린 변경: **우상단 톱니 제거 → 캐릭터 우클릭으로 부채꼴 펼침.**
★ **저장소에는 이 변경의 사양도 코드도 없다**(오늘 `grep` 실측 — `docs/UX_FLOW.md` 36절이 최신이고
그 절은 **정확히 반대 방향**이다). ⇒ **미확정 기능을 확정처럼 홍보하지 않는다**는 규율이 그대로 걸린다.

### 동결 대상 2건 — **지우지도, 고치지도 않는다**

| # | 문장 | 왜 얼리나 |
|---|---|---|
| **F-1** | **2-7절** — *"물러난 동안에도 **톱니는 남아 있어서**, 눌러서 설정까지 갈 수 있습니다"* | 톱니가 사라지면 **그대로 거짓**이다. ★ 근거: `InfoGearIconWidget.cs:1601`이 `TryGrantUserSummon("톱니 클릭")`을 부르는 **호출부 2곳 중 하나**다(다른 하나는 `SettingsWindow.cs:586`) |
| **F-2** | **1절 짧은 설명** — *"**클릭은 그대로 통과하고**, 창은 1픽셀도 움직이지 않습니다"* | 우클릭을 다시 잡으면 **캐릭터 몸 위에서 부분적으로 안 통과한다.** ★ 근거: `UX_FLOW` 36-9 — *"우클릭을 잡으려면 그 순간 **클릭관통을 부분 해제**해야 하고 … 사용자가 그 앱에 내리려던 우클릭(문맥 메뉴)을 **가로채고 있었다**"* |

### ★ 「동결」의 정확한 뜻 (규율 신설 — **S-3**)

> **사실이 바뀔 것이 확실한데 어떻게 바뀔지 모르는 문장은, 고치지 말고 표시만 해 둔다.**
> - **지금 고치면 두 번 고친다.** 그 사이에 나간 판본이 어느 쪽으로든 거짓이 된다.
> - **지금 지우면 되살릴 근거가 사라진다.** 7-1절이 이미 못박았다 — *"판정은 양방향으로 움직인다."*
> - ⇒ **문장은 그대로 두고, 그 옆에 「동결 · 사유 · 해제 조건」 한 줄을 붙인다.**
> - ★ **동결된 문장이 하나라도 있으면 그 판본은 게시 불가다.** 이게 동결의 실효다.

| 문장 | 해제 조건 |
|---|---|
| **F-1** | (가) 새 진입점 사양이 문서로 설 것 **(나)** 그 진입점이 **등급 1에서 `TryGrantUserSummon`을 실제로 부르는지** Windows 실기 관측(**0회**) |
| **F-2** | **"캐릭터 히트 영역 위의 우클릭만 소비하는가, 아니면 그 순간 화면 전체의 관통이 잠깐 풀리는가"**가 확정될 것 — ★ **전자면 괄호 하나로 끝나고, 후자면 짧은 설명을 다시 써야 한다** |

### ★ 그리고 **지금 쓰면 안 되는 문장**이 하나 늘었다

| 신규 금지 | 왜 |
|---|---|
| ★★ **"캐릭터를 우클릭하면 메뉴가 펼쳐집니다"류** | ① **사양 0건 · 코드 0건**(`grep` 실측) ② 2026-08-31 사용자 지시(`UX_FLOW` 36절)와 **정반대 방향**이라 **뒤집힌 원문을 아직 못 봤다** ③ 그리고 **비침해 문장(F-2)과 충돌한다.** 셋 중 하나만 걸려도 금지인데 **셋 다 걸려 있다** |

★ **반대로, 톱니 제거로 죽지 **않는** 문장도 확인해 뒀다** → v2.3-3.

---

## v2.3-3. ★ 자진 정정 — **3-2절의 「톱니 링」은 내 표기 오류였다**

3-2절에 이렇게 적었다: *"세션 중에는 **톱니 링**이 잔여 시간 호가 된다."*

**실측**(`Interaction/GearRadialMenuWidget.cs:2144`):
> *"① 집중 모드 — **스톱워치**(용두 + 링 + 바늘 2). 세션이 돌면 이 링이 그대로 잔여 시간 호가 된다."*

⇒ **그건 톱니의 링이 아니라 「부채꼴 ①번 버튼의 스톱워치 링」이다.**
**부채꼴이 어디에 매달리든 살아남는다.**

| 고칠 곳 | 고친 표기 |
|---|---|
| 3-2절 | *"집중 세션이 도는 동안 **그 버튼이 스톱워치가 되어 남은 시간을 호로 보여준다**"* |
| 7절 금지 목록 중 *"집중 모드는 **톱니 → 부채꼴 ①**로 안내한다"* | ★ **동결**(F-1과 같은 사유). 안내 경로는 조작 변경 결론을 기다린다 |

★ **이 정정이 없었다면 「톱니 제거로 죽는 문장 3개」로 올릴 뻔했다. 실제로는 2개다.**
**리더에게 올릴 숫자를 하나 줄인 것이 이 라운드에서 가장 값진 한 줄이다** —
과장된 위험 보고는 과소 보고만큼 나쁘다(그 방향으로 결정이 왜곡된다).

---

## v2.3-4. 슬롯 배치 개정 (8절을 대체)

★ **슬롯 정본은 `SHOT_LIST.md` R8-1이다.** 여기서는 **페이지 문안과 그림의 대응**만 적는다.

| 슬롯 | 소재 | 이 컷이 **어느 문단을 그림으로 증명하는가** |
|---|---|---|
| 동영상 1 | `A-1` 물리 3분할 | **2-4절**(애니메이션이 아니라 물리다) — 헤드라인 |
| 스샷 1 | `S-1` 헤드라인 | **2-3절**(남의 창이 지형이다) |
| 스샷 2 | `G-1` 클릭 통과 | **1절 뒷문장 + 4절** |
| ★ 스샷 3 | **`E-1` 착용 전/후 2분할** | ★ **2-8절 신설**(v2.3-1) |
| ★ 스샷 4 | **`E-2` 카드 격자** | **3절 13번** |
| ★ 스샷 5 | **`R-1` 부채꼴 펼침** | ★★ **대응 문단 없음 — 문장이 동결됐기 때문이다.** 문장 없이 그림만 거는 것은 금지 ⇒ **F-1/F-2가 풀릴 때까지 이 슬롯은 예비로 메운다** |
| 스샷 6 | `A-2` 발↔선 | **2-3절** 보강 |
| 스샷 7 | `S-3` 설정창 | **2-6절**(통제권) |

★★ **스샷 5 자리가 이 개정의 핵심 판단이다.**
**그림이 문장보다 먼저 나가면 안 된다** — 페이지에서 설명하지 않은 조작을 스크린샷으로 가르치면
사용자는 그 조작을 시도하고, 안 되면 **버그로 신고한다.** 지금 우리는 그 조작이 어떻게 될지 모른다.

---

## v2.3-5. 신규 금지 문구 (7절에 추가)

| 금지 문구 | 왜 |
|---|---|
| ★ **"캐릭터를 우클릭하면 메뉴가 펼쳐집니다"류** | v2.3-2. 사양 0 · 코드 0 · 사용자 지시와 반대 방향 · 비침해 문장과 충돌 |
| ★ **"장비 16종을 새로 그렸습니다"를 42종과 같은 문단에** | 한 문장에 **두 세대**가 섞인다. 나머지 26종은 v1 경로 그대로다 |
| ★ **"인계본 디자인 그대로 옮겼습니다"** | ★ **두 종에서 거짓이다.** 털모자는 세로 0.80배 압축, 중절모 챙은 인계본 비 그대로면 팔 폭(4.06 R)이 된다. **"보이는 그대로 맞췄다"까지만** |
| ★ **장비 대비·색 수치를 페이지에 적는 것**(3.25:1 등) | 성능 수치 금지와 같은 가족. **설계 근거는 개발일지 소관**이지 스토어 소관이 아니다 |

---

## v2.3-7. ★★ 신설 문서 2건 — **이 페이지가 혼자 서지 못하는 부분**

이 페이지는 **문안**이다. 스팀 페이지를 실제로 세우려면 **문안 말고 두 가지가 더** 필요한데,
둘 다 저장소에 주인이 없어서 R8에 신설했다.

| 문서 | 무엇 | ★ 왜 중요한가 |
|---|---|---|
| **`STORE_ART_SPEC.md`** | 캡슐·아이콘·배너 규격과 요구서 (Steamworks 1차 출처) | ★★★ **규율 M-1이 막지 못하는 유일한 스토어 소재다** — 빌드 산출물이 아니라서. 그리고 `STORE_PAGE.md:60`대로 **Playtest 심사가 이것만 본다** ⇒ **달력에서 가장 앞일 수 있다** |
| **`TRAILER.md`** | 동영상 슬롯 1의 40초 시나리오 + 15초/6초 컷다운 | ★ **8절 슬롯 표의 「동영상 1」에 지금까지 대본이 없었다.** 소재 ID(`A-1`)만 있었다 |

★ **`STORE_ART_SPEC.md`가 이 페이지에 되돌려 준 사실 1건 (규격 정정)**:
나는 헤더 캡슐을 **460×215**로 알고 있었는데 그건 **자동 생성 절반 크기**이고
**업로드 규격은 920×430**이다. 1차 문서를 안 열었으면 **전 자산을 절반 해상도로 발주**했을 것이다.

★★ **그리고 규칙 하나가 이 페이지의 8절 계획을 건드린다**:
**캡슐에는 스크린샷을 쓸 수 없다**(Steamworks 1차: *"Screenshots … cannot appear on capsules"*).
⇒ *"바탕화면 위에 서 있는 실제 화면을 캡슐로"*라는 가장 자연스러운 안이 **규칙상 불가능**하고,
**캡슐은 `design-art`가 그려야 하는 신규 조형 과제**가 된다(리드타임이 있다).

---

## v2.3-6. 플랫폼 영향

- **Windows 영향: 함께 검토함(문서만).** ① v2.3-1의 장비 문단은 **Windows 재빌드 전 게시 불가**
  (`AccessoryHandoffPalette` win=0). ② **1pt 재질선이 Windows 1×에서 어떻게 보이는지 실기 0회** —
  `EQUIPMENT_PALETTE.md` §7이 스스로 미확인으로 적어 둔 항목이고, **그게 이 문단의 마지막 잠금**이다.
  ③ 동결 2건의 해제 관측(F-1의 등급 1 진입, F-2의 관통 범위)은 **둘 다 Windows 실기 항목**이다.
- **macOS 영향: 함께 검토함 — 페이지 축에서는 없음.** macOS 빌드에는 일부 코드가 있으나
  **어느 커밋에도 없는 상태**(R7-1 규율 M-1)라 **여기서도 근거로 쓰지 않았다.**
  프로덕션 `.cs`/`.asset` 0줄, 앱 실행 0회, Unity 락 0회.
