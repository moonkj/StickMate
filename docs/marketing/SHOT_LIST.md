# 촬영 목록 — "무엇을 찍을 것인가"와 구도

작성: `marketing` · R3 2026-09-02 저녁
★ **촬영은 아직 막혀 있다**(깨끗한 캡처 환경 부재 — `CAPTURE_PROTOCOL.md` 1절).
**이 문서는 환경이 열리는 날 그대로 실행할 수 있는 「촬영 지시서」다.**
소재 ID `S-1` / `G-1` / `V-1`은 `ROADMAP.md` 2절에서 이미 확정된 것이고, 여기서는
**리더가 이번 라운드에 지목한 「남는 자리 두 개」를 어떻게 그림으로 만드는가**를 새로 채운다.

> ★ **앱 실행·캡처 전에 리더에게 알린다.** `driver.sh stop`·전역 `Q` **절대 금지.**
> 나는 이 라운드에 **앱을 실행하지 않았다.** 아래는 전부 코드·에셋 실측에 기반한 계획이다.

---

## 0. 남는 자리 두 개 — 무엇을 증명해야 하는가

`MARKET_LANDSCAPE.md` 1-3절 결론:

| 자리 | 증명해야 하는 명제 | 왜 어려운가 |
|---|---|---|
| **A. 물리 × 남의 창** | *"이건 애니메이션이 아니라 계산이다"* | ★ **정지컷으로 증명 불가능하다.** 스프라이트도 정지컷에서는 똑같이 생겼다 |
| **B. 권한 0 · 네트워크 0** | *"이건 아무것도 요구하지 않는다"* | ★ **부재는 찍을 수 없다.** 없는 것을 어떻게 프레임에 넣는가 |

**두 자리 모두 「보통 방식으로는 찍을 수 없는 것」이다.** 그래서 구도가 곧 논증이어야 한다.

---

# A. 물리 × 남의 창

## A-1 ★★★ 「같은 높이, 세 번」 — 3분할 (동영상 1번 슬롯 / 최우선)

> **이 한 컷이 이번 라운드 최대 산출물이다.** M4·M6이 지갑을 여는 유일한 축이고,
> **캡션 한 줄 없이 물리를 증명하는 유일한 구도**다.

**논증 구조**: 스프라이트 애니메이션은 **같은 입력에 같은 그림**을 낸다.
물리는 **같은 입력에 다른 그림**을 낸다. → **같은 입력을 세 번 보여주고 결과가 다르면 끝난다.**

| 항목 | 지시 |
|---|---|
| **화면 분할** | 가로 3분할. 세 칸이 **완전히 동일한 화면**(같은 창 배치, 같은 좌표) |
| **동작** | 커서로 집어 **같은 높이·같은 지점**에서 놓는다. 3테이크 |
| **동시 재생** | 세 칸이 **같은 프레임에서 동시에 시작**. 이게 핵심 — 시차가 있으면 논증이 흐려진다 |
| **길이** | 놓는 순간 ~ 무릎앉아 착지까지. **3~4초**. 그 뒤 0.5초 정지 후 루프 |
| **프레임 안** | 창 2개(상단이 발판), Dock/작업표시줄 |
| ★ **기대 결과** | 세 칸의 **회전수·낙하 자세·착지 방향이 서로 다르다** |
| ★ **실측 근거** | `ThrowTumbleState` 회전 **220~720°/초** = **편차 3.3배**. 최대 6초, 착지 시 무릎앉기 |
| **테이크 수** | ★ **최소 8테이크 찍어 3개를 고른다.** 물리라서 원하는 그림이 언제 나올지 모른다 |
| **고르는 기준** | 셋의 **차이가 가장 크되 셋 다 「자연스러운」 것**. 하나라도 우스꽝스러우면 "버그"로 읽힌다 |

**실패 조건(찍고 나서 폐기해야 하는 경우)**
- 셋이 비슷하게 나오면 **폐기하고 다시 찍는다.** 비슷한 3분할은 논증이 아니라 **반증**이다.
- 셋 중 하나라도 미끄러짐(수평 표류)이 보이면 폐기. ★ macOS 배치모드 실측은 끝속도 **0.000000**이지만
  **Windows 실기 확인은 아직 없다**(`TRUTH_INVENTORY.md` 6절 ⑥).

---

## A-2 ★★ 「발이 선 위에 있다」 — 클로즈업 정지컷 (스크린샷 3번)

**논증**: 발판이 **비유가 아니라 좌표**라는 것.

| 항목 | 지시 |
|---|---|
| **구도** | 창 제목표시줄의 **상단 경계선이 프레임 가로를 가른다.** 그 선 위에 두 발 |
| **배율** | 200% 확대. 발바닥과 선의 접점이 **화소 단위로 보이게** |
| **캐릭터 상태** | **IDLE 직립** 또는 **한 발 내딛는 중**. 점프 중 금지(공중이면 논증이 없다) |
| **실측 근거** | Finder 창 상단 OS y=160.0 vs 착지 y=160.0 — **오차 0.0pt** |
| **금지** | 창 제목 글자가 읽히면 안 된다. ★ 창 제목은 **잘라내거나 빈 제목 창**을 쓴다 |

★ **이 컷이 `S-1`(헤드라인 정지컷)과 다른 점**: S-1은 "전체 화면에서 무슨 일이 벌어지나",
A-2는 **"정말로 닿아 있나"**다. 둘 다 필요하다. 하나로 합치면 둘 다 약해진다.

---

## A-3 ★★ 「창을 끌면 따라온다」 — GIF 3초

| 항목 | 지시 |
|---|---|
| **동작** | 캐릭터가 창 위에 선 상태에서 **그 창을 가로로 천천히 드래그** |
| **길이** | 3초 (드래그 1.5초 + 정지 1초 + 루프 이음매 0.5초) |
| **논증** | 발판이 **화면 좌표가 아니라 그 창에 묶여 있다** |
| **주의** | 드래그가 빠르면 따라붙는 과정이 안 보인다. **느리게** |
| ★ **추가 논증** | 같은 테이크 끝에 **창을 닫아 떨어뜨린다**. "창이 사라지면 발판도 사라진다"가 한 컷에 들어간다 |

---

## A-4 ★ 「모서리에 매달려 내려간다」 — GIF 4초

| 항목 | 지시 |
|---|---|
| **실측 조건** | `ledgeHangChance 0.35`. ★ **Dock 위에서는 낙차 부족으로 발동하지 않는다 — 창 위에서만 나온다** |
| **구도** | 위 창 / 아래 창 **2단**. 낙차가 프레임에 다 들어와야 한다 |
| **기다림** | 확률 0.35라 **여러 번 접근해야** 나온다. 촬영 시간을 길게 잡는다 |
| **논증** | 스프라이트 펫은 대개 이걸 못 한다. **물리 + 창 지형이 둘 다 있어야 나오는 동작** |

---

## A-5 「가려진 창은 발판이 아니다」 — 개발일지 전용 GIF

| 항목 | 지시 |
|---|---|
| **동작** | 창 A 위를 걷는 중 **창 B로 A를 덮는다** → 캐릭터가 떨어진다 |
| **근거** | `VisibleTopEdgeSolver.cs`(오클루전 컬링) |
| **왜 스토어가 아닌가** | ★ 이건 **"안 되는 것을 보여주는 컷"**이다. 스토어에서는 결함으로 읽힐 위험이 있다. **개발일지에서는 최고의 소재**다(이 정도까지 하는 곳이 드물다) |

---

# B. 권한 0 · 네트워크 0 — **부재를 어떻게 찍는가**

## B-0 원리 — 세 가지 방법뿐이다

| 방법 | 무엇을 찍나 | 어디에 쓰나 |
|---|---|---|
| **① 안 뜨는 것을 무편집으로** | 권한 대화상자가 **뜨지 않는 첫 실행 전체** | 스토어 · 유튜브 |
| **② 통과를 실증** | 클릭이 그대로 통과하는 장면 | 스토어 |
| **③ 제3자 계측기 화면** | 송수신 바이트 | ★ **개발일지 전용** |

---

## B-1 ★★★ 「컷이 없다」 — 첫 실행 무편집 한 테이크 (60~90초)

> **편집이 없다는 사실 자체가 주장이다.** 컷을 넣는 순간 "중간에 뭔가 눌렀겠지"가 된다.

| 항목 | 지시 |
|---|---|
| **시작** | 아무것도 안 띄운 깨끗한 바탕화면. **화면 녹화를 먼저 시작**하고 앱을 실행 |
| **끝** | 캐릭터가 창 위에 올라가 걷기 시작할 때까지 |
| ★ **핵심** | 그 사이에 **macOS 접근성/화면 기록 권한 대화상자가 한 번도 뜨지 않는다** |
| **컷** | **0개.** 배속도 걸지 않는다(배속은 편집으로 읽힌다) |
| **자막** | 화면 밖 여백에만. ★ **제품 UI 폰트를 쓰지 않는다** — UI처럼 보이면 목업이 된다 |
| **길이가 길어도 되나** | ★ 된다. **길이가 증거다.** 스토어용은 여기서 잘라 쓰되, **원본 무편집 파일을 함께 보관**한다 |

★ **Windows판도 같은 테이크를 따로 찍는다.** 다만 Windows에서는
**작업표시줄 자동 숨김 해제가 화면에 그대로 보인다** → **그 장면을 자르지 말고 그대로 두고,
고지 문구를 같은 영상 안에 넣는다.** 자르면 그게 은폐다.

---

## B-2 ★★★ 「클릭이 통과한다」 — `G-1`과 통합 (스크린샷 2 / GIF)

`ROADMAP.md`의 `G-1`이 이미 이 자리다. 여기서는 **B축 관점의 요구만** 덧붙인다.

- 균열(가짜)이 떠 있는 **3초 안에** 그 아래 계산기 `7` 버튼을 실제로 눌러 **숫자가 입력되는 것**까지.
- ★ **입력된 숫자가 프레임에 보여야 한다.** 클릭하는 손동작만 찍으면 논증이 아니다.
- 실측: 스윙 0.4초 + 균열 3초 후 원복. **누를 시간이 3초뿐**이므로 리허설 필요.

---

## B-3 「송수신 0」 — ★ 개발일지 전용, 스토어 금지

| 항목 | 지시 |
|---|---|
| **찍는 것** | macOS 활성 상태 보기 / Windows 리소스 모니터에서 **우리 프로세스의 송수신 바이트** |
| ★ **정직한 캡션** | *"N분 관측 중 송수신 0바이트"* — **관측 시간 창을 반드시 함께 적는다.** "0"은 잰 구간 안에서만 참이다 |
| ★ **스토어 금지 이유** | 제3자 UI 캡처는 **구매자가 검증할 수 없고**, 스토어 문맥에서는 오히려 방어적으로 보인다. 개발일지에서는 반대로 최고의 신뢰 소재다 |
| **선행 조건** | `security`의 **출하 빌드 아웃바운드 실측 절차**가 먼저 있어야 한다(`STORE_PAGE.md` 5절). 그 전엔 찍지 않는다 |

---

## B-4 「일부러 깨 보았다」 — ★ 글. 그림이 아니다

우리 감사 테스트에는 **네거티브 컨트롤**이 붙어 있다 — *"0건이라 초록"*이 아니라
**"위반 코드를 일부러 넣으면 빨개진다"**까지 검증돼 있다.

> **이건 스크린샷으로 만들 수 없고, 만들려고 하면 안 된다.** 개발일지 1편의 본문이다.
> 굳이 그림을 넣는다면 **테스트 러너가 빨간 화면**(일부러 위반을 넣은 상태) 1장 —
> ★ 단 그 화면은 **우리 제품 화면이 아니므로 스토어에 올리지 않는다.**

---

## B-5 「예외를 먼저 말한다」 — 작업표시줄 고지 컷

- Windows 스크린샷에서 **작업표시줄이 나타난 상태가 이미 찍힌다.**
- → 그 스크린샷의 캡션에 **예외 고지를 그대로 붙인다.**
- ★ **정직성 자체가 소재**다. 이 고지를 본 M5(`신뢰구매`)는 나머지 주장을 더 믿는다.

---

# C. 촬영 회차 계획 (환경이 열리는 날)

| 회차 | 찍는 것 | 필요 상태 | 선행 조건 |
|---|---|---|---|
| **1회차** | B-1(양 플랫폼) · S-1 · A-2 | 신규 저장(레벨 1) | 깨끗한 계정 |
| **2회차** | A-1(8테이크) · A-3 · A-4 | 신규 저장 | 시간 여유 — 확률 대기 |
| **3회차** | G-1/B-2 · V-1 · 행동 명령창 | 행동 확률 수동 | 리허설 |
| **4회차** | 설정창 · A-5(개발일지) | — | — |
| **보류** | 장비/커스터마이즈 | ★ 레벨 30 저장 필요 | **장비 색 대비 수정 라운드 뒤**(D등급) |

★ **「보류」 줄에 주의**: 장비 격자를 찍으려면 **레벨 30 저장 파일**이 필요하다
(신규 설치는 `???` 35칸). 그 파일로 찍은 화면을 캡션 없이 쓰면
**"산 날 이렇게 보인다"는 거짓 인상**을 준다. → 캡션에 **"함께 지낸 시간으로 열립니다"**를 병기한다.

---

# D. 전 컷 공통 금지 (`CAPTURE_PROTOCOL.md` 0절 재확인)

1. **실제 사용자 화면 금지** — 남의 창 제목·파일명이 그대로 찍힌다. **깨끗한 테스트 환경만.**
2. **목업 금지** — 그리지 않는다. 실제 빌드에서만.
3. **과거 빌드 캡처 재사용 금지** — 행동 명령창은 **560 → 508**로 줄었고(격파 타일 삭제),
   카드 [착용] 칩 대비도 바뀌었다. **옛 캡처는 전부 폐기.**
4. **격파 놀이 관련 화면 금지** — 삭제됐다.
5. **모바일 소재 금지** — 배선 0.
6. **「이벤트」 탭 금지** — 폐지됐다.

---

# E. 플랫폼 영향

- **Windows 영향: 별도 배정 필요(사유: 실기 부재).** 이 문서의 B-1·B-5는 **Windows 실기에서만**
  찍을 수 있고, 이 개발 머신에는 Windows 실행 환경이 없다. **촬영 주체가 사용자여야 한다.**
  또한 A-1의 미끄러짐 실패 조건은 Windows 실기 확인 항목 ⑥에 걸려 있다.
- **macOS 영향: 없음(문서만).** A 계열·B-1의 macOS 테이크는 이 머신에서 가능하지만
  **깨끗한 캡처 환경(별도 계정/빈 바탕화면)이 아직 없다.** 앱 실행은 리더 승인 사항이라
  이 라운드에 **실행하지 않았다.**

---
---

# ★★★ R8 (2026-09-05 저녁) — **스팀 8슬롯 확정안** · 장비 이식 + 조작 변경 반영

작성: `marketing` R8 · ★ **이 라운드에도 앱을 실행하지 않았다.** Unity·빌드·Chrome 전부 미사용.
프로덕션 `.cs`/`.asset` 수정 0건. 외부 게시 0건.

> ## ★★★ 먼저 — **이번 라운드에 「찍을 수 있는 것」은 하나도 늘지 않았다**
>
> 리더가 알려 준 두 소식(장비 16종 이식 · 우클릭 부채꼴)은 **소재로는 크게 늘었는데
> 촬영 가능 시점으로는 하나도 안 늘었다.** 오늘 직접 잰 결과다.
>
> ### 빌드 바이트 프로브 (2026-09-05 16:0x, 양성/음성 대조 동반)
> ```
> 양성 대조  걸치는 것              win=1  mac=1     ← 프로브 생존
> 음성 대조  ZZZ없는문자열ZZZ       win=0  mac=0     ← 프로브 생존
> AccessoryHandoffPalette         win=0  mac=0     ★ 어느 빌드에도 없다
> HandoffFillBase                 win=0  mac=0     ★ 어느 빌드에도 없다
> GearMenuButton                  win=1  mac=3       (부채꼴은 실재 — 단 톱니에 달려 있다)
> ShopNotice 문구                 win=2  mac=2       (프레임 금지 대상, 여전히 살아 있다)
> ```
> 대상: `Builds/Windows/StickMate_Data/Managed/StickMate.Runtime.dll`(**09-03 08:32**) /
> `Builds/macOS/StickMate.app/…/StickMate.Runtime.dll`(**09-05 08:54** — R7이 잰 08:14에서 또 바뀌었다).
>
> ### 그리고 **커밋되지 않았다**
> ```
> git ls-files --error-unmatch …/Interaction/AccessoryHandoffPalette.cs      → did not match (untracked)
> git ls-files --error-unmatch …/Interaction/AccessoryShapeBuilder.Handoff.cs → did not match (untracked)
> git ls-files --error-unmatch …/Interaction/AccessoryShapeBuilder.cs         → OK   ← 양성 대조
> ```
> ⇒ ★ **R7이 세운 규율 M-1**(*캡처 대상 빌드는 그 빌드에 들어간 코드가 커밋돼 있어야 한다*)이
> **장비 이식 전체를 막는다.** 빌드 바이트 프로브 0 **그리고** `git ls-files` 미추적 — **두 조건 다 걸린다.**
>
> ### ⇒ 이 문서의 성격
> **「지금 찍자」가 아니라 「빌드가 서는 그 시각에 그대로 실행되는 촬영 대본」이다.**
> 각 컷에 **해제 조건(무엇이 참이 되면 찍을 수 있는가)**을 붙였다. 조건이 안 걸린 컷은 **찍지 마라** —
> 그건 소재가 아니라 부채다(R7-1).

---

## R8-0. ★★ 공통 촬영 환경 — **이 표를 어기면 그 컷은 전부 폐기다**

| 항목 | 값 | 근거 |
|---|---|---|
| **플랫폼** | ★ **Windows 11** 단독 | 지원 OS를 Windows만 선언한다(`STORE_PAGE.md` 0-4). macOS 컷은 개발일지 전용 |
| **해상도** | **1920×1080 네이티브**, 디스플레이 배율 **100%** | 스팀 스크린샷 표시 규격. ★ 125/150%는 **판정용 별도 컷**(R8-9), 스토어 슬롯에 넣지 않는다 |
| **바탕화면** | 단색 밝은 회색 계열(`#F2F2F2` 대역) | `EQUIPMENT_PALETTE.md` §6-(1)이 대비를 잰 **기준 배경 4종 중 하나**. 사진 배경화면은 재질색 24색과 충돌해 대비 보장이 깨진다 |
| **잉크 프리셋** | ★ **검은 잉크(출하 기본)** | ★★ **흰 잉크 + 밝은 바탕 조합 금지.** `r19_worn_sheet.png`의 「밝은/흰」 열에서 **본체가 배경에 묻히고 장비만 떠 있다** — 눈 판정. 대비표상으로도 밝은 바탕 최악 3.25 vs 흰 잉크 머리 3.64로 둘을 겹치면 남는 게 없다 |
| **캐릭터 배율** | 출하 기본값 그대로 | ★ 0.60에서 획 1pt 하한이 걸린다(`EQUIPMENT_HANDOFF_PORT_SPEC` §14). **배율을 올려 예쁘게 찍으면 구매자 화면과 다른 그림**이 된다 |
| **계정** | ★ **깨끗한 테스트 계정** — 계정명·문서명이 창 제목·경로에 안 나오는 이름 | 정의서 규율. 남의 창 제목·파일명이 그대로 찍힌다 |
| **세이브** | 컷마다 지정(신규 설치 / 레벨 30 중 택1) — **섞지 마라** | 신규 설치는 `???` 35칸, 레벨 30은 다 열린 화면. **한 페이지에 둘이 섞이면 어느 쪽이 거짓말**이 된다 |

### ★ 프레임 금지 목록 — **한 픽셀이라도 들어오면 그 컷 폐기** (6건)

| # | 프레임에 들어오면 안 되는 것 | 왜 |
|---|---|---|
| **X-1** | **[상점] 탭 / `상점은 다음 업데이트에 들어옵니다.`** | 양 빌드 실측 2건씩. **페이지에서 안 하기로 한 로드맵 약속을 그림으로 하는 것**(`STORE_PAGE.md` 7절) |
| **X-2** | **헤더 재화 칩(보유칩·동전)** | 쓸 데가 상점뿐이고 그 상점이 「다음 업데이트」다. F2P 페이지에서 *"나중에 이걸로 돈 받겠구나"*로 읽힌다 |
| **X-3** | **보관함의 「할일 알림」·「하드웨어 반응」 카드** | ★ 출하 기본에서 **영영 안 일어난다**(`todoReminderChance: 0` · `enableAutonomousHardwareReactions: 0`, 코드·에셋 양쪽). 바이트 실측 `적어둔 할일을 때가 되면` = win 1 / mac 1 — **화면에 실재한다** |
| **X-4** | **`F` / `J` / `H` 단축키 표기** | 09-03 Windows 빌드에 **아직 그 표기가 남아 있다**(`MenuOnlyStatus` win=0). **재빌드 전 보관함 컷 전면 금지** |
| **X-5** | **`???` 잠긴 칸이 화면의 절반을 넘는 구도** | 3열 격자라 한 화면에 훨씬 많이 들어온다. 캡션 없이 찍으면 「빈 제품」으로 읽힌다 |
| **X-6** | **남의 창 제목·파일명·계정명·시계의 실제 날짜** | 정의서 규율 |

---

## R8-1. ★★★ 슬롯 배치 — **동영상 1 + 스크린샷 7**

★ **순서가 곧 논증이다.** ①무엇인가 → ②방해하는가 → ③계속 볼 것이 있는가 → ④끌 수 있는가.
장비는 ③이지 ①이 아니다 — **아이템 컷을 1번에 놓으면 「꾸미기 앱」으로 분류되고, 그 시장에서는 개수로 진다**(`STORE_PAGE.md` 3-1).

| 슬롯 | ID | **이 컷이 증명하는 명제 한 줄** | 상태 |
|---|---|---|---|
| **동영상 1** | `A-1` | *"같은 입력에 다른 결과가 나온다 = 애니메이션이 아니다"* | 기존 유지 |
| **스샷 1** | `S-1` | *"내가 지금 열어 둔 창 위에서 산다"* | 기존 유지 |
| **스샷 2** | `G-1` | *"저게 떠 있어도 내 클릭은 그대로 간다"* | 기존 유지 |
| **스샷 3** | ★ **`E-1` 신규** | *"입히면 진짜로 달라진다"* — **착용 전/후 2분할** | ★ 조건부 |
| **스샷 4** | ★ **`E-2` 신규** | *"모을 것이 있다"* — 정보창 [장비] 카드 격자 | ★ 조건부 |
| **스샷 5** | ★ **`R-1` 신규** | *"메뉴가 캐릭터에서 나온다"* — 부채꼴 펼침 | ★★ 조건부(가장 멀다) |
| **스샷 6** | `A-2` | *"발이 진짜로 그 선 위에 있다"* | 기존 유지 |
| **스샷 7** | `S-3` | *"언제든 끌 수 있다"* — 설정창 | 기존 유지 |
| 예비 A | `V-1` | 활쏘기 마지막 정중앙 | 기존 |
| 예비 B | `W7` | 잡힘 대사 2분할 | 기존 |

★ **E-1·E-2·R-1이 못 걸리면 슬롯 3·4·5는 예비 A·B와 기존 「행동 명령창」으로 메운다.**
**빈 슬롯을 목업으로 채우지 않는다.**

---

## R8-2. ★★★ `E-1` — 「입히면 달라진다」 **착용 전/후 2분할** (스샷 3)

> **왜 이 컷이 장비 슬롯의 첫 자리인가**: 카드 격자(`E-2`)는 **아이콘이 예쁘다**는 것만 증명한다.
> 구매자가 궁금한 것은 **「내 화면에 서 있는 저 애가 달라지는가」**다.
> **몸에 붙은 그림 없이 카드만 보여주면 「스티커 모음」으로 읽힌다.**

| 항목 | 지시 |
|---|---|
| **구도** | 가로 2분할. **완전히 동일한 화면**(같은 창 배치·같은 좌표·같은 자세). 왼쪽 = 맨몸, 오른쪽 = 착용 |
| **캐릭터 상태** | ★ **IDLE 직립 정면**. 걷는 중·점프 중 금지 — **실루엣이 흔들리면 「달라진 것」이 장비인지 자세인지 구분이 안 된다** |
| **입힐 조합** | ★ **왕관(HEAD) + 고글(EYES) + 긴 망토(SHOULDERS)** 3점 |
| **왜 이 셋인가 (실측)** | ① **세 슬롯이 서로 안 겹친다**(머리 / 눈 / 어깨) — 겹치면 하나가 하나를 가려 「3개 입혔다」가 안 읽힌다. ② **색이 서로 멀다**: 왕관 `#9B7922`(Gold) + 보석 `#C6443C`(Toy) **ΔE 49.8**, 고글 `#587398`(Silver) + 렌즈 `#CC5512`(TintHead) **ΔE 91.2**, 긴 망토 `#CC3C3C`(CapeRed). ③ **밝은 바탕 대비**: 왕관 3.64 · 고글 3.43 · 망토 3.62 — 전부 §6-(1) 하한 위. ④ **긴 망토는 실루엣 자체를 바꾼다**(밑단 −9.055 R = 발목) — 정지컷에서 유일하게 **형태로** 차이가 나는 조각 |
| **해상도·배율** | R8-0 그대로. **200% 확대컷은 만들지 마라** — 이 컷의 논점은 「내 화면에서 이만큼 보인다」다 |
| ★ **성공 판정** | **두 칸을 나란히 놓고 3초 안에 「셋이 늘었다」가 세어지는가.** 안 세어지면 조합을 바꾼다 |
| ★ **폐기 조건** | ① 오른쪽 칸에서 **장비가 몸보다 눈에 먼저 들어오면 폐기** — 우리는 스틱메이트를 파는 것이지 액세서리를 파는 것이 아니다. ② **왼쪽/오른쪽 자세가 1픽셀이라도 다르면 폐기**(물리라서 정확히 같은 프레임을 잡기 어렵다 — **일시정지 없이 여러 테이크 찍어 고른다**) |
| ★ **해제 조건** | **(가)** `AccessoryHandoffPalette` 바이트 프로브가 **Windows 빌드에서 1 이상** **그리고** **(나)** 그 파일이 `git ls-files`에 **잡힐 것**. 둘 다 걸려야 한다(규율 M-1) |

★ **캡션(스팀 스크린샷 캡션 칸)**: *"42가지를 걸칩니다. 능력은 하나도 안 바뀝니다."*
— 이 뒷문장이 `STORE_PAGE.md` 2-2절(팩으로 열리는 능력 없음)을 **그림 옆에서 다시 못박는다.**
꾸미기 컷은 **가만두면 「사면 세진다」로 읽히는 유일한 컷**이다.

---

## R8-3. ★★ `E-2` — 정보창 [장비] 탭 카드 격자 (스샷 4)

| 항목 | 지시 |
|---|---|
| **구도** | 정보창 전경. 3컬럼 이식본([장비] 탭 활성) |
| **세이브** | ★ **중간 진행 세이브**(대략 절반 열린 상태). ★ **레벨 30 「다 열린 화면」도, 신규 설치 「`???` 35칸」도 쓰지 마라** — 전자는 설치 직후와 어긋나고 후자는 빈 제품으로 읽힌다 |
| **카드 선정** | 프레임 안 카드에 **등급 4단이 전부 한 장 이상** 들어오게. 등급은 **리본 칸 수 · 낱말 · 썸네일 프레임**으로만 표시된다(`EQUIPMENT_PALETTE.md` R-7) — 조각 색은 등급을 말하지 않는다 |
| **프레임에서 빼는 것** | ★ **X-1(상점 탭) · X-2(재화 칩)** 둘 다. **탭 줄과 헤더가 프레임에 들어오면 이 둘이 자동으로 따라 들어온다** — 크롭으로 해결되면 크롭, 안 되면 리더 판정(R6 Q-1/Q-2) |
| ★ **필수 캡션** | *"함께 지낸 시간으로 열립니다."* — 캡션 없이 잠긴 칸이 보이면 F2P에서 **곧장 리뷰로 간다**(F-1) |
| ★ **폐기 조건** | ① 44pt 실물에서 **카드 아이콘이 회색 덩어리로 뭉치면 폐기.** 종전 워시(α0.21 등급색)에서 채움↔바탕이 **1.20~1.32**였고 그게 사용자가 신고한 「회색 카드」다. R19 값은 **3.62~4.88**이라 해결됐을 것으로 보지만 ★ **44pt 실물 눈 판정은 아직 0회다**(오프라인 4× 확대본으로만 봤다 — `EQUIPMENT_PALETTE.md` §7 미확인) ② 리본 칸 사이 틈이 안 보이면 폐기(사용자 신고 원인이 **2.68′**였다 — 5° 이심 MAR과 같다) |
| ★ **해제 조건** | E-1의 (가)(나) **+ (다)** 정보창 재작업(`CharacterInfoWindow.*` 8파일)이 **커밋될 것** |

---

## R8-4. ★★ `R-1` — 「캐릭터에서 메뉴가 나온다」 부채꼴 펼침 (스샷 5)

> ### ★★★ 이 컷은 **가장 멀다.** 그리고 **찍기 전에 리더 판정이 4건 필요하다** → R8-7

| 항목 | 지시 |
|---|---|
| **형태** | ★ **정지컷 1장 + 루프 GIF 1개**(둘 다 만든다). 정지컷은 스토어 슬롯, GIF는 개발일지·커뮤니티 |
| **정지컷 순간** | ★ **완전히 펼쳐진 뒤**가 아니라 **펼침 80% 지점**. 완전히 펼쳐진 정지컷은 「그냥 버튼 4개」로 보이고, 80% 지점은 **호가 살아 있어 「방금 나왔다」가 읽힌다** |
| **GIF 길이** | 1.6초 (우클릭 0.2 + 펼침 0.5 + 정지 0.6 + 이음매 0.3). ★ **접히는 것까지 넣지 마라** — 펼침이 논점이다 |
| **캐릭터 위치** | 화면 **가운데 아래**. ★ 모서리에 두면 **부채꼴 전체가 회전해 들어와**(UX_FLOW 32-1) 「기본 형태」가 안 찍힌다 |
| **배경** | 창 2개 위. ★ **단색 바탕 금지** — 이 컷은 「일하는 화면에서 메뉴를 연다」를 보여야 한다 |
| ★ **성공 판정** | 커서(우클릭 표시)와 캐릭터와 부채꼴 **셋이 한 덩어리로 읽히는가.** 커서가 프레임 밖이면 **무엇이 이걸 열었는지 그림이 말하지 않는다** |
| ★ **해제 조건** | ★★ **아래 4건 전부**(R8-7) |

---

## R8-5. 기존 컷 4개 — R8에서 **바뀐 것만** 적는다

| ID | R8 변경 |
|---|---|
| `A-1` 물리 3분할(동영상) | 변경 없음. ★ Windows 낙차는 **역산**이고 실측이 아니다(H-15) — **작업표시줄 위 낙차로 찍지 말고 창 위 낙차로 찍는다** |
| `S-1` 헤드라인 | ★ **장비를 입히지 마라.** 1번 컷의 명제는 「남의 창 위에서 산다」이고, 여기에 왕관을 씌우면 **명제가 둘이 되어 둘 다 약해진다** |
| `G-1` 클릭 통과 | ★ **Windows 계산기**로. macOS 계산기와 생김새가 다르다 |
| `A-2` 발↔선 클로즈업 200% | ★ **이 컷에서만 잉크 코어 0px 결함이 드러날 수 있다**(macOS Retina에서는 구조적으로 안 보인다 — H-8). **먼저 찍어 보고 판정**한다 |
| `S-3` 설정창 | ★ 단축키가 **`Ctrl+Alt+Win+K`**로 찍혀야 한다. `⌃⌥⌘K`가 찍히면 폐기 |

---

## R8-6. ★★ 「지금 당장 찍을 수 있는 것」 — **정직하게 세면 4컷이다**

| 찍을 수 있다 | 왜 |
|---|---|
| `A-1` · `S-1` · `G-1` · `A-2` | ★ **단, 09-03 Windows 빌드에 보관함·정보창이 프레임에 안 들어오는 구도로만.** 이 넷은 전부 「캐릭터 + 남의 창」이라 UI 표면이 안 걸린다 |

| 못 찍는다 | 막는 것 |
|---|---|
| `E-1` · `E-2` | 규율 M-1 (미빌드 **그리고** 미커밋) |
| `R-1` | 사양 부재 + 미구현 + 리더 판정 4건 |
| `S-3` 설정창 · `W5` 행동 명령창 · `W7` 잡힘 대사 | Windows 빌드가 09-03이라 **오늘 커밋(`1f7e139`)이 안 들어갔다** |

★ **이 표가 이번 라운드의 실질 결론이다.** 소재는 늘었고 **촬영 가능 시점은 그대로다.**
**늘어난 소재를 「곧 찍을 수 있다」로 반올림하지 않는다.**

---

## R8-7. ★★★ `R-1` 이전에 리더가 갈라 줘야 하는 것 — **4건**

> ★ **나는 이것을 「반대」로 올리는 것이 아니다.** 조작 변경은 리더·사용자 소관이고 내 소관이 아니다.
> 내가 올리는 것은 **그 변경이 이미 나가 있는 마케팅 문장 몇 개를 어떻게 건드리는가**다.

### (가) ★★ 이 방향은 **2026-08-31 사용자 지시의 정반대다** — 알고 뒤집는 것인지 확인 필요

`docs/UX_FLOW.md` **36절 제목**이 그대로다: *"부채꼴 아이콘화 + **캐릭터 우클릭 메뉴 폐지** + 행동 명령창 신설 (2026-08-31 사용자 지시)"*.
**사용자 원문**(`:3962`):

> *"그리고 캐릭터 마우스 우클릭으로 행동이나 설정 변경하는 메뉴 없애고 **기어아이콘에 메뉴하나 추가해서**
> 행동들은 거기서 클릭하면 창 하나가 떠서 행동 명령 내릴수 있게"*

⇒ **「우클릭 → 기어」로 옮기라는 지시였고, 지금 방향은 「기어 → 우클릭」으로 되돌리는 것이다.**
내 정의서가 못박은 것이 정확히 이 상황이다 — *"이미 사용자가 확정해 닫힌 것을 다시 제안하면 그 라운드는 통째로 낭비다."*
★ **사용자가 새로 뒤집은 것이라면 아무 문제 없다.** 다만 **그 원문이 어디 있는지**를 알려 주면
`TRUTH_INVENTORY`에 근거로 걸어 두겠다. 지금 저장소에는 **뒤집는 원문이 없다**(`grep` 결과 36절이 최신).

### (나) ★★★ **비침해 문장이 흔들린다** — 이게 마케팅 쪽 최대 쟁점이다

`UX_FLOW` **36-9** 근거 2 원문:

> *"**비침해가 실제로 개선된다.** 우클릭을 잡으려면 그 순간 **클릭관통을 부분 해제**해야 하고,
> 캐릭터는 남의 창 위에 서 있다 — 지금까지 우리는 **사용자가 그 앱에 내리려던 우클릭(문맥 메뉴)을
> 가로채고 있었다.**"*

**우리 짧은 설명 첫 문단이 이렇게 되어 있다**(`STORE_PAGE.md` 1절):
> *"**클릭은 그대로 통과하고**, 창은 1픽셀도 움직이지 않습니다."*

⇒ 우클릭을 다시 잡으면 **「클릭은 그대로 통과한다」가 캐릭터 몸 위에서 부분적으로 거짓**이 된다.
**거짓까지는 아니어도 「전부 통과」로 읽히는 문장이 「좌클릭만 통과」가 된다.**

★ **내 권고(문안 축)**: 그림·문장 어느 쪽도 손대지 말고, **먼저 사실을 확정해 달라** —
*"캐릭터 히트 영역 위의 우클릭만 소비하는가, 아니면 그 순간 화면 전체의 관통이 잠깐 풀리는가."*
전자면 문장을 *"클릭은 그대로 통과합니다(캐릭터를 직접 누를 때만 예외)"*로 **한 괄호 추가**로 끝난다.
후자면 **1절 문장을 다시 써야 한다.** ★ **나는 이걸 잴 수 없다 — 구현이 없다.**

### (다) ★★ 톱니가 사라지면 **팔던 문장 두 개가 같이 사라진다**

| 문장 | 지금 근거 | 톱니 제거 시 |
|---|---|---|
| `STORE_PAGE.md` **2-7절** — *"물러난 동안에도 **톱니는 남아 있어서**, 눌러서 설정까지 갈 수 있습니다"* | `InfoGearIconWidget.cs:1601` `TryGrantUserSummon("톱니 클릭")` | ★ **그대로 거짓이 된다** |
| `TRUTH_INVENTORY.md` **R5-1** — *"전체화면 앱 위에서도 톱니 한 번이면 설정까지 간다"* | 같음 | ★ **허용 → 보류로 되돌아간다** |

★ **프로덕션 코드가 스스로 이 위험을 적어 뒀다** — `InfoGearIconWidget.cs:74`:
> *"이 앱에는 **등급 1을 끄는 탈출구가 톱니 1클릭뿐**이라(`Platform/UserSurfaceSummonPolicy`)"*

★ **다만 나는 이걸 「탈출구가 0이 된다」로 과장하지 않는다 — 실측했다.**
`TryGrantUserSummon` 호출부는 **정확히 2곳**이다:
```
Interaction/InfoGearIconWidget.cs:1601   TryGrantUserSummon("톱니 클릭")
Interaction/SettingsWindow.cs:586        TryGrantUserSummon($"설정창 열기({source})")
```
그리고 **시스템 트레이가 설정창을 연다**(`SystemTrayCommandBridge` / `SystemTrayCommandRouter` —
후자에 *"3중 종료 경로는 그대로 살아 있습니다"* 문장이 있다).
⇒ **트레이가 살아 있으면 마우스 탈출구는 남는다.** 다만 **그건 우리 창이 아니라 OS 표면**이고,
**2-7절 문장은 「우리 화면에 남아 있는 것」을 팔고 있다.** 문장을 트레이로 갈아 끼우려면
★ **Windows 실기에서 트레이 → 설정창 경로가 등급 1에서 실제로 열리는지 관측이 먼저**다(0회).

### (라) ★ 내 문안 오류 1건 — **자진 신고**

`STORE_PAGE.md` 3-2절에 *"세션 중에는 **톱니 링**이 잔여 시간 호가 된다"*라고 적었는데 **부정확하다.**
실측: `GearRadialMenuWidget.cs:2144` — *"① 집중 모드 — **스톱워치**(용두 + 링 + 바늘 2). 세션이 돌면 이 링이 그대로 잔여 시간 호가 된다"*.
⇒ **그건 톱니의 링이 아니라 「부채꼴 ①번 버튼의 스톱워치 링」이다.**
★ **그래서 이 문장은 톱니가 사라져도 산다**(부채꼴이 살아 있으면). 표기만 고치면 된다 → `STORE_PAGE.md` v2.3.
★ **이걸 안 잡았으면 「톱니 제거로 죽는 문장 3개」로 리더에게 올릴 뻔했다.** 실제로는 **2개다.**

---

## R8-8. ★ 장비 시트에서 내가 **소재로 쓰지 않기로 한 것** — 3건

**보여줄 것이 늘었다고 다 쓰는 것이 아니다.** 오프라인 시트를 직접 열어 보고 내린 판정이다.

| 안 쓴다 | 왜 |
|---|---|
| **비교 시트 자체**(`r19_worn_sheet.png` 등)를 개발일지 이미지로 그대로 | ★ **열이 5개고 그중 3개가 「우리가 안 쓰는 조합」**이다(인계본 재현기 열 · 흰 잉크 열 · 어두운 바탕 열). 설명 없이 올리면 **어느 게 제품인지 아무도 모른다.** 개발일지에는 **2열만 잘라** 쓴다(R8 개발일지 초안 3막) |
| **「밝은 바탕 + 흰 잉크」 열의 어떤 컷도** | 본체가 배경에 묻힌다. 우리 제품의 **선택 가능한 조합이긴 하지만 홍보 소재로는 최악의 조합**이다 |
| **털모자 · 중절모의 "인계본과 같다" 주장** | ★ 둘 다 **일부러 인계본과 다르다.** 털모자는 균일 배율로 액자를 못 넘어 **ky 0.80 세로 압축**, 중절모 챙은 균일 배율에서 **4.06 R(팔 폭)**이 된다. *"인계본 그대로"*라고 쓰면 그 두 종에서 거짓이다 → **"보이는 그대로 맞췄다"**까지만 쓴다 |

---

## R8-9. 판정용 캡처(홍보용 아님) — 3건

| # | 찍는 것 | 용도 |
|---|---|---|
| **J-1** | 카드 **44pt 실물 1×** 격자 | `EQUIPMENT_PALETTE.md` §7 미확인 해소 — *"44pt 실물에서 잉크 윤곽 2.82~3.81이 충분한가"* |
| **J-2** | 착용 상태 **Windows 1× / 125% / 150%** 각 1장 | 1pt 하한 재질선의 실제 모습(**실기 0회**) |
| **J-3** | 정보창 3컬럼 **125% / 150%** | 레이아웃 붕괴(H-9) |

★ **J-1~J-3은 파일명 접두어 `judge-`.** 스토어 창고와 물리적으로 갈라 둔다(R6 C-4).
