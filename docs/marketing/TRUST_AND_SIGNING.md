# 무서명 출시의 홍보 비용 — 그리고 그때 쓸 문장

작성: `marketing` · R5 2026-09-03 · **프로덕션 `.cs` 0줄. 외부 게시 0건.**
전제(리더 확정): **Windows 서명은 첫 업데이트 전까지 연기**(한국 개인에게 $9.99/월 경로가 **없다** —
1차 출처 2곳 일치) / **macOS 서명·공증은 1.0 하드 블로커**(스팀 채널 요구).
근거 창고: `docs/strategy/CHANNEL_PRICING_DECISIONS.md` 10절 · `CLAIM_AUDIT_R5.md` 8-1

> **이 문서가 답하는 것 세 가지** (리더 질문 그대로):
> ① 스팀 단독이면 무서명이 홍보에 **어떤 비용**인가
> ② *"바이러스 아니냐"*가 뜰 **확률**과 그때 **쓸 문장**이 준비돼 있는가
> ③ **불신은 평판과 달리 이월된다** — 그 리스크를 어떻게 다루는가

---

## 0. 결론 네 줄

1. ★★ **가장 큰 비용은 「경고」가 아니라 「실행 차단」이다.** Smart App Control은 **MOTW를 보지
   않으므로 스팀 경유로도 비켜 갈 수 없다.** 경고는 읽고 넘기지만 **차단은 리뷰가 된다.**
2. ★★ **서명해도 첫 경고는 뜬다.** 그러므로 *"서명하면 해결"*은 계획이 아니다. **무서명이든
   서명이든 문장이 필요하다** — 서명은 그 문장을 **덜 자주 쓰게** 해 줄 뿐이다.
3. ★★ **지금 만들어지고 있는 기능이 오탐 표면을 넓힌다.** 우리는 곧 **작업표시줄과 Alt+Tab에서
   스스로를 지운다**. 무서명 + 자기 은폐 + 비문서 API는 **은폐형 악성코드와 행동 서명이 겹친다**(2-2절).
   ★ **정정**: 이 두 파일(`AppSwitcherPresencePolicy` · `WindowsTaskbarButtonRemover`)은 **아직
   커밋되지 않은 미추적 파일**이다(`CLAIM_AUDIT_R5.md` 0-0-C). **「착지」가 아니라 「진행 중」**이고,
   ⇒ **그래서 지금이 이 비용을 저울에 올리기에 가장 좋은 시점이다**(6절 J-1).
4. ★★ **불신 이월의 진짜 경로는 리뷰 점수가 아니라 「검색 결과」다.** 그리고 **F2P가 그 유입을
   증폭한다** — 무료라 설치가 많고, 무료라 환불이 없어 불만이 전부 리뷰로 간다(4-1절).

---

## 1. ① 무서명의 홍보 비용 — **네 갈래로 갈라서 센다**

리더 질문의 전제(*"스팀 단독이면"*)를 그대로 받아 **스팀 경유 설치**만 놓고 잰다.

| # | 경로 | 스팀 단독에서 실제로 걸리는가 | 홍보 비용 |
|---|---|---|---|
| **A** | **SmartScreen 다운로드 경고** | **대체로 안 걸린다.** 스팀 클라이언트가 자체 다운로더로 받으므로 MOTW가 붙지 않는다 ★ **단 "스팀이 MOTW를 안 붙인다"는 1차 출처가 없다 — 미확인** | **낮음** |
| **B** | ★★ **Smart App Control (SAC)** | ★ **걸린다.** *"Smart App Control signature checks apply to **all executable files, not just those downloaded from the Internet**"* — **MOTW를 안 본다.** 무서명 파일은 **긍정 평판이 없으면 실행이 차단**된다 | ★★★ **가장 크다 — 경고가 아니라 실행 실패다** |
| **C** | **백신 오탐** | ★ **구조적으로 노출돼 있다.** 2-2절에서 우리 자신의 API 표면을 셌다 | ★★ **큼. 특히 국내** |
| **D** | **손 배포**(프레스 키 zip · 개발일지 데모 · 베타 테스터) | ★ **전부 걸린다.** MOTW가 붙고 무서명은 *"Strong SmartScreen block"* | ★★★ **하는 순간 즉시.** → 3절 |

### 1-1. ★ 「경고」와 「차단」을 절대 같은 칸에 적지 마라

이 구분이 이 문서의 핵심이다.

| | 경고(A) | **차단(B·D)** |
|---|---|---|
| 사용자가 보는 것 | *"알 수 없는 게시자"* → **[추가 정보] → [실행]** | **실행이 안 된다** |
| 사용자가 하는 것 | 대부분 넘긴다 | **포기하거나 리뷰를 쓴다** |
| 우리 문장이 할 수 있는 일 | 미리 알려 두면 마찰이 거의 0이 된다 | ★ **문장으로 못 고친다. 서명만 고친다** |

⇒ **B가 실제로 얼마나 흔한지가 Windows 서명 연기 판정의 유일한 변수다.**
★ **SAC가 켜져 있는 기기 비율은 Microsoft가 공개하지 않는다 — 미확인.** 숫자를 지어내지 않는다.
다만 MS 지원 FAQ가 *"Recent Windows updates allow Smart App Control to be enabled without
requiring a clean installation"*이라고 적어 **모수가 넓어지는 방향**임은 시사한다.

### 1-2. ★ 서명해도 사라지지 않는 것 (거짓 소재 방지)

> **`Valid Certificate (OV/EV)` → `⚠️ Warning — app flagged as unrecognized until reputation
> accumulates; verified publisher name is displayed`** — Microsoft Learn

**서명해도 첫 경고는 뜬다.** 그러므로 아래 문구는 **금지**다(스토어 페이지 7절에 등재했다):

- ✗ *"서명되어 있어 경고가 안 뜹니다"*
- ✗ *"인증된 앱입니다"*

**서명이 실제로 사는 것은 둘뿐이다:**
1. 경고 화면에 **퍼블리셔 이름**이 뜬다(무서명은 *"알 수 없는 게시자"*).
2. ★★ **버전을 넘어 평판이 이어진다** — *"Unsigned files must build reputation anew with
   every update."*

> ### ★★ 2번이 이 제품에 특히 아프다
> **상주 앱은 업데이트가 잦다.** 무서명이면 **릴리스마다 평판이 0으로 리셋**된다.
> 즉 무서명의 비용은 **출시 1회의 비용이 아니라 릴리스마다 반복되는 비용**이다.
> ⇒ **리더 판정(「첫 업데이트 전까지 연기」)과 정확히 맞물린다** — 그 판정의 진짜 뜻은
> *"평판을 쌓기 시작하기 전까지만 미룬다"*이고, **두 번째 릴리스부터는 미루는 것의 비용이 커진다.**

---

## 2. ② *"바이러스 아니냐"*가 뜰 확률 — **숫자를 지어내지 않고 노출 요인을 센다**

★ **확률을 모른다.** 이 카테고리의 리뷰에 그 말이 뜬 비율에 대해 나는 1차 출처가 없다.
**그래서 확률 대신 「구조적 노출 요인」을 세고, 그중 우리에게 해당하는 것을 실측했다.**

### 2-1. 이 카테고리 자체의 노출 요인

| 요인 | 우리 |
|---|---|
| 상주(24시간 백그라운드) | ★ 해당 |
| 항상 위 + 반투명 오버레이 | ★ 해당 |
| **다른 앱의 창을 열거한다** | ★ 해당 |
| 무료 | ★ 해당 (F2P 확정) |
| 무서명(Windows) | ★ 해당 (1.0) |
| 광고 SDK 포함 | ✗ 해당 없음 — **우리가 이길 수 있는 유일한 칸** |

### 2-2. ★★ 우리 Windows API 표면 — **어젯밤에 넓어졌다** (실측)

`Assets/_Project/Scripts/Platform/Windows/**` P/Invoke 전수(테스트 제외). **양성·음성 대조 포함.**

| 우리가 실제로 부르는 것 | 무엇으로 읽히는가 |
|---|---|
| `EnumWindows` · `GetWindowRect` · `GetTopWindow` | 창 열거 |
| `OpenProcess` · `QueryFullProcessImageNameW` · `ProcessIdToSessionId` | **프로세스 열거** |
| `RegOpenKeyExW` · `RegEnumKeyExW` · `RegQueryValueExW` | **레지스트리 읽기** |
| `GetAsyncKeyState` | **전역 키 상태 폴링** |
| `SetWindowLong` · `WS_EX_LAYERED` · `SetLayeredWindowAttributes` | 레이어드 창 조작 |
| `SHAppBarMessage` | **셸 앱바 메시지**(승인된 예외 1건) |
| ★ `InternalGetWindowText` | ★ **비문서 API** |
| ★ `WS_EX_TOOLWINDOW` (`WindowsToolWindowStyleControl`) | ★ **Alt+Tab에서 자기를 지운다** |
| ★ `ITaskbarList::DeleteTab` (`WindowsTaskbarButtonRemover`) | ★ **작업표시줄에서 자기를 지운다** |

**★ 안 부르는 것(음성 대조 — 이게 우리를 구한다)**

| 니들 | 결과 | 확인 방법 |
|---|---|---|
| `CreateRemoteThread` | **0건** | 전수 grep |
| `VirtualAllocEx` / `WriteProcessMemory` | ★ **실호출 0건.** 매치 2건은 **「우리는 이걸 안 한다」고 적은 주석**이다 | ★ 매치를 눈으로 열어 확인했다 — **개수만 세면 반대로 읽힌다** |
| `SetWindowsHookEx` | ★ **실호출 0건.** 매치 1건은 *"후킹이나 관리자 권한도 필요 없다"*는 주석 | 같은 방법 |
| 레지스트리 **쓰기**(`RegSetValueEx`/`RegCreateKeyEx`/`RegDeleteKey`/`RegDeleteValue`) | ★ **실호출 0건.** 매치 2건은 *"선언조차 하지 않는다"*는 주석 | 같은 방법 |
| 합성 입력 7종(`SendInput`/`keybd_event`/… ) | **0건** | R4 실측, 이번 유지 |
| ★ **양성 대조** | `RegQueryValueExW`(읽기)는 **3건 실호출** | 프로브가 살아 있음 |

> ### ★★ 그래서 이번 라운드의 새 사실
> **우리는 지금 「작업표시줄과 Alt+Tab에서 자기 자신을 지우는 기능」을 넣고 있다**
> (★ **미커밋 · 진행 중** — 그래서 되돌리기가 아직 싸다).
> 그 자체는 정당하다 — macOS가 이미 하는 것(`NSApplicationActivationPolicyAccessory`)과의
> 대칭이고, `⌃⌥⌘K`의 존재 이유(화면공유 중 나오지 마)와도 맞물린다.
>
> **그러나 신뢰의 관점에서는 다르게 읽힌다**:
> `무서명` + `상주` + `프로세스/창/레지스트리 열거` + `전역 키 폴링` + `비문서 API` +
> **`작업 전환기에서 자기 은폐`** — 이 조합은 **은폐형 악성코드의 행동 서명과 겹친다.**
>
> ⇒ **이건 기능을 되돌리자는 말이 아니다.** 두 가지를 하자는 말이다:
> **(가)** Windows 서명 연기 판정의 **비용 항목에 이 조합을 추가**해 리더가 다시 저울에 올린다.
> **(나)** **매뉴얼과 스토어 페이지가 이 은폐를 먼저 말한다.** 우리가 안 말하면
> 발견한 사람이 말하고, 그때는 *"숨기려던 걸 들켰다"*가 된다.
>
> ★ 그리고 저장소가 이미 그 필요를 적어 두었다 — `AppSwitcherPresencePolicy` 클래스 문서:
> *"갚아야 할 대가 (매뉴얼에 반드시 들어간다) … 그 문장이 매뉴얼에 없으면 이 변경은 사용자를 가둔다."*

### 2-3. ★ 국내 가중치 — 사용자 환경이 이미 알려져 있다

커밋 `aaac7b2` 기록: **사용자 환경 = V3 백신.**
국내 배포에서 **V3 · 알약 계열의 휴리스틱**은 서구권 엔진과 다른 판단을 낸다(★ 이건 **직관**이고
비교 데이터가 내게 없다 — **미확인**). 다만 **위 2-2절 조합이 휴리스틱 표적이라는 것은 구조적 사실**이고,
**무서명이 그 판정을 나쁜 쪽으로 민다**는 것도 구조적이다.

⇒ **국내 커뮤니티 반응(F-3)은 스팀 리뷰보다 먼저 올 가능성이 높다.** 그쪽 문장이 3절에 있다.

---

## 3. ★★ 자동 앞당김 조건 — **마케팅이 바로 그 방아쇠를 당길 수 있는 자리에 있다**

리더 확정: **스팀 밖 직배포를 하는 순간 서명이 앞당겨진다**(itch.io · 홈페이지 zip · 디스코드).
MOTW가 붙고 무서명은 *"Strong SmartScreen block"*.

> ### ★★ 이 조건에 가장 먼저 걸리는 사람은 **나(마케팅)**다
> 마케팅이 평범하게 하는 일 중 **아래 넷이 전부 「스팀 밖 직배포」다**:
>
> | 마케팅 활동 | 서명 방아쇠인가 |
> |---|---|
> | 프레스/스트리머에게 **빌드 zip**을 보낸다 | ★ **그렇다** |
> | 개발일지에 **체험판 링크**를 건다 | ★ **그렇다** |
> | 디스코드/커뮤니티에 **베타 빌드**를 올린다 | ★ **그렇다** |
> | itch.io에 **미러**를 둔다 | ★ **그렇다** |
> | 스팀 **키**를 나눠 준다 | **아니다** — 스팀 경유다. ★ **R7 1차 확인으로 이 칸은 그대로 참이나, 「키를 쓸 수 있는가」 자체가 F2P에서 조건부가 됐다 → 9절** |
>
> ⇒ **규율 T-1(이 문서의 구속력 있는 규칙)**:
> **`marketing`은 스팀 밖으로 실행 파일을 내보내는 어떤 계획도 세우지 않는다.**
> ~~필요하면 **스팀 키**를 쓴다.~~ ★ **2026-09-05 R7 교체 — 이 탈출구의 전제가 틀렸다.**
> 1차 원문: *"Games that are **Free-to-Play** … **cannot request Steam Keys by default**."*
> **본편 F2P라 본 게임 키는 기본 발급 대상이 아니다.** ⇒ **대체 경로는 「Steam Playtest 키」**이고,
> 그건 **스토어 페이지가 서기 전에도 발급된다**(9-2). **T-1의 금지는 그대로 유지된다.**
> zip을 보내야 하는 상황이 생기면 **그 자리에서 리더에게 올린다** —
> 그건 홍보 결정이 아니라 **서명 예산 결정**이기 때문이다.
>
> ★ **macOS는 더 단호하다**: 애드혹 서명(`codesign` 실측: `Signature=adhoc`, `TeamIdentifier=not set`)은
> `com.apple.quarantine`이 붙는 순간 **그 자리에서 막힌다.** macOS 빌드를 손으로 한 번이라도
> 배포하면 **그날 Developer ID가 필요해진다.**

---

## 4. ③ 불신 이월 — **가장 위험한 경로는 리뷰 점수가 아니다**

### 4-1. ★ F2P가 이월을 증폭한다 (v2 신규 분석)

| 축 | 유료였다면 | **무료(확정)** |
|---|---|---|
| 설치 수 | 적다 | **많다** — 신뢰 질문의 절대 수가 커진다 |
| 불만의 배출구 | **환불**(2시간 내, 조용함) | ★ **환불할 것이 없다 → 전부 리뷰로 간다** |
| 잘못 온 사람 | 페이지에서 걸러진다 | ★ **안 걸러진다** (한국어 전용인데 영어권이 설치) |
| 첫 리뷰가 쌓이는 속도 | 느리다 | ★ **빠르다 — 초기에 굳는다** |

★ *"무료 제품은 환불이 없어 불만이 리뷰로 간다"*는 **스팀 환불 정책의 구조에서 따라오는 추론**이다.
**비율 통계에 대한 1차 출처는 내게 없다 — 미확인.**

### 4-2. ★★ 진짜 이월 경로는 **검색 결과**다

스팀 리뷰는 개인이 수정·삭제할 수 있고, 「최근」과 「전체」가 분리돼 있다.
**그러나 다음 세 가지는 우리가 손댈 수 없고 검색엔진에 오래 남는다:**

1. **커뮤니티 게시글 제목** — *"StickMate 바이러스인가요?"* 형태의 질문 글.
   ★ **답이 달려 해결돼도 제목은 그대로 남는다.** 그리고 사람들은 제목만 본다.
2. **백신 벤더 오탐 DB 캐시** — 오탐이 해제돼도 스크린샷은 남는다.
3. **첫 리뷰에 박힌 문장** — 나중에 서명해도 그 리뷰는 안 지워진다(리더 지적 그대로).

> ★ **그래서 이 문제의 유일한 실효 대책은 「빨리 대응하는 것」이 아니라 「먼저 말해 두는 것」이다.**
> 사후 해명은 **그 질문 글을 하나 더 만들 뿐**이다.

### 4-3. ★ 처방 — **우리가 먼저 낸다** (선제 문서)

**출시와 동시에**(사후가 아니라) 아래 셋을 준비한다.

| # | 무엇 | 근거 | 대가 | ★ 버리는 조건 |
|---|---|---|---|---|
| **T-A** | **스토어 페이지에 무서명 한 줄**(4절 문안) | 미리 알려 두면 A(경고)의 마찰은 거의 0이 된다. B(차단)는 못 고치지만 **"몰랐다"는 없앤다** | 페이지에 부정적 문장을 자진해 넣는 비용 | 서명이 1.0에 들어가면 문장을 교체한다(삭제가 아니다 — 1-2절대로 경고는 여전히 뜬다) |
| **T-B** | ★ **다중 엔진 스캔 결과를 우리가 먼저 게시** | 근거를 **1차 자료로** 만든다. 질문이 오기 전에 링크가 존재하면 커뮤니티 글이 **덜 생긴다** | ★★ **파일이 공개된다.** 출시 **전** 빌드를 올리면 유출이다 → **공개 릴리스 빌드에만.** 그리고 **오탐이 실제로 나오면 그 결과가 우리 손에서 공개된다** | ★ **스캔에서 탐지가 0이 아니면 이 전략은 역효과다.** 그때는 폐기하고 **서명을 앞당긴다**(리더 판정 필요) |
| **T-C** | **매뉴얼에 「보이지 않을 때 찾는 법」** | 우리는 이제 작업표시줄·Alt+Tab에서 자기를 지운다(2-2절). **찾는 단서가 사라진다** | 매뉴얼 분량 | `AppSwitcherPresencePolicy`의 은폐를 되돌리면 불필요 |

★ **T-C는 이미 저장소가 답을 갖고 있다**: `_userHidden`은 **세이브에 없다** — **재실행하면 숨김이
반드시 풀린다**(`AppSwitcherPresencePolicy` 클래스 문서). 이 한 문장이 매뉴얼에 있어야 한다.
→ `perf-doc`(문서화)에 **리더 경유** 전달 요청.

---

## 5. ★★ 준비된 문장 — **지금 쓴다. 나중에 쓰지 않는다**

**전부 초안이고, 게시는 리더 승인 후 사용자가 한다.**
★ 각 문장에 **「어디에 쓰는가」**와 **「무엇이 참이어야 쓸 수 있는가」**를 붙였다.

### 5-1. 스토어 페이지 한 줄 (Windows 1.0, 무서명 시점)

| 한국어 | English |
|---|---|
| **Windows에서 처음 실행할 때 「알 수 없는 게시자」 경고가 뜰 수 있습니다.** 아직 코드 서명 인증서를 사지 않았기 때문이고, 앱이 하는 일과는 관계가 없습니다. 이 앱이 무엇을 읽고 무엇을 쓰지 않는지는 위 목록에 전부 적어 두었습니다. | **Windows may show an "unknown publisher" warning the first time you run it.** That's because we haven't purchased a code-signing certificate yet — it says nothing about what the app does. Everything it reads, and everything it refuses to touch, is listed above. |

★ **쓸 수 있는 조건**: **Windows가 실제로 무서명으로 나갈 때만.** 서명이 1.0에 들어가면 이 문장은
거짓이 되므로 **반드시 교체**한다(1-2절 — 서명해도 경고는 뜨므로 문장은 남되 이유가 바뀐다).
★ **쓰면 안 되는 것**: *"안전합니다"* — 우리가 우리를 안전하다고 말하는 것은 근거가 아니다.
**대신 목록을 가리킨다.**

### 5-2. *"바이러스 아니냐"* 질문에 대한 답 (커뮤니티·리뷰 답글)

> **"서명 인증서를 아직 안 샀습니다. 그래서 Windows가 「알 수 없는 게시자」라고 말합니다.
> 그 경고는 「이 앱이 나쁘다」가 아니라 「누가 만들었는지 확인할 수 없다」는 뜻입니다.**
>
> **앱이 실제로 무엇을 하는지는 확인하실 수 있게 적어 두었습니다 —
> 창 목록과 위치는 읽고, 창 제목은 읽지 않으며(Windows에서 그 확인에 쓰는 버퍼가 1글자입니다),
> 레지스트리에는 쓰지 않고, 마우스·키보드를 대신 눌러 주지 않습니다.
> 접근성·화면 기록·관리자 권한을 하나도 요구하지 않는 것도 그래서입니다.**
>
> **인증서는 [계획 시점]에 넣을 예정입니다."**

★ **이 문장이 강한 이유는 「안전하다」고 말하지 않기 때문이다.**
**검증 가능한 사실 네 개**를 말하고, 그 넷은 전부 **자동 감사가 지키고 있다**
(`OfflineFirstNetworkAuditTests` / `UserAssetImmutabilityAuditTests` — **네거티브 컨트롤 포함**).

★ **쓸 수 있는 조건 — 문장별로 다르다**:
- 「창 제목 안 읽음」 ✅ 양 플랫폼 실측 완료
- 「레지스트리에 안 씀」 ✅ 실호출 0건 + 양성 대조
- 「합성 입력 없음」 ✅ 7종 0건
- 「권한 0」 ✅ 권한 API 0건 + 양성 대조 2종
- ✗ **「인터넷에 안 나갑니다」는 이 답글에 넣지 마라** — 아직 실측 전이다(`CLAIM_AUDIT_R5.md` 6절).
  ★ **그리고 이 답글은 의심하는 사람이 읽는 글이다.** 여기서 한 줄이 틀리면 **나머지 네 줄까지
  같이 죽는다.** 확실한 것만 쓴다.

### 5-3. 「작업표시줄에 안 보인다」에 대한 답 (선제 — 매뉴얼 + 페이지)

> **"작업표시줄과 Alt+Tab에는 나타나지 않습니다. 발표나 화면 공유 중에 우리 이름이
> 남의 화면에 찍히지 않게 하려는 것입니다(macOS에서는 처음부터 그랬습니다).
> 그래서 창을 찾는 평소 방법이 통하지 않습니다 —
> 숨겼다면 `Ctrl+Alt+Win+K`로 되돌리고, 그것도 안 되면 **앱을 다시 실행하면 숨김이 반드시 풀립니다.**"**

★ **마지막 문장의 근거**: `_userHidden`은 세이브에 없다(`CharacterSaveStore`가 이 필드를 한 번도
읽거나 쓰지 않는다). ★ **이 문장은 코드가 바뀌면 즉시 거짓이 된다** — `_userHidden`이 세이브에
들어가는 순간 폐기하고 문장을 다시 쓴다.
★ **쓸 수 있는 조건**: **Windows 실기 확인 후.** 지금은 실기 0회다(`CLAIM_AUDIT_R5.md` 8-1).

---

## 6. 리더에게 올리는 판단 3건 (나는 결정하지 않는다)

| # | 판단 | 내가 붙이는 근거 |
|---|---|---|
| **J-1** | **Windows 서명 연기 판정의 저울에 「자기 은폐 + 비문서 API」를 추가할 것인가** | 어젯밤 착지로 API 표면이 넓어졌다(2-2절). 연기 판정은 그 이전 상태에서 내려졌다 |
| **J-2** | **T-B(선제 스캔 게시)를 할 것인가** | 근거를 1차 자료로 만들 수 있는 유일한 수단. **대가는 파일 공개**이고 **버리는 조건은 탐지 ≠ 0** |
| **J-3** | **T-A(스토어 무서명 한 줄)를 1.0 페이지에 넣을 것인가** | 넣으면 페이지에 부정적 문장이 자진 등장한다. 안 넣으면 **경고를 처음 본 사람이 커뮤니티에 묻는다** — 그 글 제목이 4-2절의 영구 자산이 된다 |

★ **내가 권고하는 것은 J-3 「넣는다」 하나뿐**이다. 근거: 이 페이지에는 이미
**「하지 않는 것」 목록**이 본문으로 서 있고(스토어 v2 4절), **그 목록의 신뢰가 이 한 줄로 완성된다** —
*"불리한 것도 먼저 적는 곳"*이라는 증거가 되기 때문이다. J-1·J-2는 **비용과 유출이 걸려 리더 판정**이다.

---

## 8. ★★★ R6 (2026-09-05) — **리드타임 2~3주가 마케팅 달력을 다시 짠다**

`product-strategy`가 오늘 조달 리드타임을 재산정했다(`docs/strategy/ROADMAP.md` §25).
**이 문서의 전제 두 줄이 갱신된다.**

| 이 문서가 전제한 것 | R6 재산정 |
|---|---|
| macOS 서명·공증이 **1.0 하드 블로커** | ★ **죽었다** — Windows 단독 출시에 **macOS depot이 없다**. 출시일 공식에서 빠졌다 |
| Windows 서명 **연기**, 리드타임 미확인 | ★ **연기는 유지. 리드타임이 확정됐다 — 계획값 2~3주**(공개 하한 3~5영업일이지만 그 시계는 **완전 제출 이후에만** 돈다. 한국 개인은 주소·업무전화 검증에서 왕복이 붙고, 재제출 1회면 시계가 처음부터 다시 돈다) |
| 비용 OV **$150~300/년** | ★ **정정** — 클라우드 HSM 규칙을 지키면 **€209 gross ~ $309+/년**, 상한 미확인. 유효기간도 **458~459일**로 줄어 갱신이 1년보다 자주 온다 |

### 8-1. ★★ 이 숫자가 마케팅에 만드는 **경성 제약 3개**

| # | 제약 | 계산 |
|---|---|---|
| **K-1** | ★★ **인증서를 1.0에 넣으려면, 스팀에 출시일을 입력하기 「전에」 이미 주문돼 있어야 한다** | 출시일 입력은 출시일보다 **최소 14일** 앞(I-2). 조달은 **2~3주(14~21일+)**. ⇒ **주문 시점 ≤ 출시일 − 21일 = 출시일 입력 시점보다도 앞이다.** *"출시일 정하고 그때 사자"*는 **산술적으로 늦는다** |
| **K-2** | ★★ **프레스·스트리머 접촉의 최단 시점 = 스팀 페이지가 서는 날** | 규율 **T-1**(3절)이 스팀 밖 실행 파일 배포를 금지한다. 대체 수단은 **스팀 키**뿐이고, 키는 **앱이 스팀에 올라간 뒤에만** 존재한다. ⇒ **「출시 전 프레스 빌드」라는 흔한 마케팅 수단이 우리에겐 없다.** 있다고 가정하고 달력을 짜면 그 칸이 통째로 비어 버린다 |
| **K-3** | ★★ **위시리스트 캠페인이 성공할수록 무서명 위험이 시간축으로 압축된다** | 위시리스트는 **출시일에 한꺼번에** 알림이 나간다. 그 코호트가 **같은 48시간 안에** 「알 수 없는 게시자」를 처음 본다. 4-2절이 규정한 **영구 자산(커뮤니티 글 제목 · 검색 결과)**은 **동시에 여러 명이 물을 때** 생긴다. ⇒ **캠페인의 성공이 그대로 위험의 증폭기다** |

> ### ★ K-3의 처방 — 캠페인을 줄이는 게 아니라 **답을 먼저 세워 둔다**
> **근거**: 4-2절 결론 그대로 — *"이 문제의 유일한 실효 대책은 「빨리 대응하는 것」이 아니라
> 「먼저 말해 두는 것」이다."* 그리고 리드타임 2~3주는 **「빨리 대응」을 산술적으로 불가능**하게 만든다(8-2).
> **대가**: 출시 **전에** 부정적 문장을 페이지에 자진 게시하는 비용(J-3).
> **버리는 조건**: **1.0에 서명이 들어가면** 5-1 문안을 교체한다(삭제가 아니다 — 1-2절대로 경고는 여전히 뜬다).

### 8-2. ★★ T5(신고 3건 트리거)는 **마케팅 대응 계획으로도 성립하지 않는다**

```
출시일 D    위시리스트 알림 일제 발송 → 첫 코호트 유입
D+1~D+2     "바이러스 아니냐" 글이 생긴다면 여기서 생긴다 (4-1: F2P라 환불이 없어 전부 글로 간다)
D+2         T5 발동(신고 3건) → 주문
D+16~D+23   첫 서명 릴리스        ← ★ 그때는 그 글 제목이 이미 검색 결과에 박혀 있다
```
★ **마케팅 축의 결론은 상품전략과 같다** — *"T5를 「조달 시작 트리거」가 아니라
「이미 손에 든 인증서를 붙이는 트리거」로 바꾼다."* **사는 것은 사용자 결정이고 나는 사지 않는다.**

### 8-3. ★ 달력 초안 (D = 출시일. 전부 **초안**이고 리더 승인 대기)

| 시점 | 마케팅이 하는 일 | 걸려 있는 것 |
|---|---|---|
| **D − 21일 이전** | (조달을 한다면) **주문**. 마케팅은 결정하지 않는다 — **K-1을 달력에 못박는 것까지가 내 일** | 사용자 결재 |
| **D − 14일** | ★ **스팀에 출시일 입력 = I-2 판정 시점.** 지원 OS **Windows만**, macOS 칸 비우지 않고 **끈다** | W-gate 전량 통과가 **출시 자격** |
| **D − 14일** | ★ **무서명 한 줄(5-1)을 이때 이미 페이지에 있어야 한다.** 출시 후에 넣으면 *"몰랐다"*를 못 없앤다 | J-3 판정 |
| **D − 14 ~ D** | 개발일지·커뮤니티. ★ **T-1: 실행 파일 zip 0건.** 스팀 키만 | K-2 |
| **D** | 위시리스트 알림. ★ **답글 문안(5-2)·매뉴얼 문안(5-3)이 이미 준비돼 있어야 한다** | 5-3은 **A-6 실기 관측**에 걸려 있다 |
| **D + 1~7** | 신고 관측. ★ **여기서 조달을 시작하면 늦는다**(8-2) | — |
| **첫 업데이트** | ★ **무서명이면 평판이 0으로 리셋된다**(1-2절). **패치 노트 홍보와 경고 재발이 같은 날 겹친다** | 서명 도입 시점 |

★ **마지막 줄이 이 절에서 가장 덜 알려진 사실이다.** 상주 앱은 업데이트가 잦고,
**업데이트는 우리가 홍보를 하는 날**이다. 무서명이면 **홍보하는 날마다 경고가 새로 뜬다** —
비용이 출시 1회가 아니라 **홍보 이벤트마다 반복**된다.

### 8-4. ★ 이번 전환으로 이 문서에서 **죽은 문장 1건** (지우지 않고 표시)

7절 macOS 줄의 *"서명·공증이 1.0 하드 블로커라 페이지 시점에는 이미 서명돼 있고 무서명 문장이
필요 없다"*는 **Windows 단독 전환으로 죽었다.** macOS를 depot으로 추가하는 날 **되살아난다.**
★ **그리고 3절의 macOS 경고는 여전히 유효하다** — 애드혹 빌드를 손으로 배포하면 **그날 막힌다.**

---

## 9. ★★★★ R7 (2026-09-05) — **스팀 키 1차 확인. 리더 요약 2건이 정정됐고, 8-3 달력이 다시 짜인다**

리더가 *"스팀 키는 앱 승인 + 스토어 페이지 「출시 예정」 활성화 이후부터 발급 가능하다.
Release Override 최대 2,500개, 일반 키 최대 50,000개"*라고 확인해 주며
**「프레스 키 배포를 페이지 활성화 직후로 앞당길 수 있다」**는 일정 이득을 계산하라고 지시했다.

**1차 원문을 직접 읽었다. 이득은 있다 — 그런데 리더가 말한 그 이득이 아니고, 셋이 틀렸다.**

> **출처**: Steamworks *Steam Keys* / *Steam Playtest* 원문.
> ★ **읽는 방법**: 파트너 사이트는 로그인 뒤에서 잘리는 부분이 있어, **SteamTracking 공개 미러**의
> 원본 HTML(`SteamworksDocumentation/docs/features/keys.html` · `playtest.html`)을 받아
> 태그를 벗기고 **문장 그대로** 뽑았다. 아래 인용은 전부 그 원문이다.
> **2차 대조**: 5,000 제한은 업계 보도 3곳으로 교차 확인했다(WN Hub · Game World Observer · GameDiscover).

### 9-1. ★★ 정정 3건 — **전부 우리에게 불리한 쪽이다**

| # | 리더 요약 | **1차 원문** | 방향 |
|---|---|---|---|
| **C-1** | 일반 키 **최대 50,000개** | *"Games and applications launching on Steam may receive up to **5,000** Default Release Steam Keys"* | ★ **10분의 1.** 자릿수 오류 |
| **C-2** | 발급 시점 = **앱 승인 + 페이지 「출시 예정」 활성화 이후** | *"Developers launching their first game on Steam must wait **three weeks after the creation of an AppID** before they can request Steam keys."* | ★ **게이트가 페이지가 아니라 「AppID 생성 + 3주」다.** 우리는 **첫 게임**이라 정확히 이 문장의 대상이다 |
| **C-3** | (언급 없음) | ★★★ *"**Free Products** — Games that are **Free-to-Play**, or that are free to download on Steam, **cannot request Steam Keys by default.** If you need Steam Keys for your free product, please **submit a ticket**."* | ★★★ **본편이 F2P 확정이다.** ⇒ **본편 키는 기본적으로 발급되지 않는다** |

> ### ★★★ C-3이 이 문서의 규율 T-1을 정면으로 친다
> **T-1(3절)의 문장은 이렇다**: *"스팀 밖으로 실행 파일을 내보내지 않는다. **필요하면 스팀 키를 쓴다.**"*
> **그 탈출구가 F2P에서는 기본적으로 닫혀 있다.** T-1은 「대체 수단이 있다」를 전제로 쓴 규칙인데,
> 그 전제가 **오늘 처음 확인됐고 틀렸다.**
> ⇒ **T-1은 폐기하지 않는다**(스팀 밖 zip이 서명 데드라인을 앞당긴다는 것은 그대로 참이다).
> **대신 T-1의 「필요하면 키를 쓴다」를 9-2의 Playtest 경로로 교체한다.**

### 9-2. ★★★ 그래서 실제 이득은 여기 있다 — **Playtest 키는 스토어 페이지를 기다리지 않는다**

Valve가 지목하는 대체 경로는 **Steam Playtest**(본 게임에 딸린 **자식 appID**)다. 원문 그대로:

> *"Steam Playtest gives developers a **free, low-risk way** to get playtesting data for their game
> **without stressing out about Steam keys, user reviews, or wishlists.**"*
>
> ★★ *"Your Playtest does not require a public signup, and **you can generate and distribute keys for a
> Playtest even if your store page is not live yet.**"*
>
> ★★ *"**It's not a requirement that your Steam store page is publicly visible on Steam** to run your
> playtest using Steam keys."*
>
> *"you will typically be limited to 2,500 Release State Override keys for your base game but you can
> generally request **much larger quantities of Playtest keys**."*
>
> ★ *"You can **only** request **Standard Release** Steam keys for your playtest … **It's not possible to
> request Release State Override (beta) Steam keys for a Steam Playtest.**"* ⇒ Playtest를 **"Playable"**로 켜야 한다.
>
> ★ 노출면: *"Instead of having its own separate store page, your Steam Playtest signup will live **right on
> your main game**, so that customers can sign up and **still Wishlist or Follow the main game**."*

### 9-3. ★★ K-2를 재산정한다 — **8-1의 경성 제약 하나가 바뀐다**

| | 종전 K-2 (8-1절) | ★ **R7 재산정** |
|---|---|---|
| 프레스·스트리머 접촉 최단 시점 | **스팀 페이지가 서는 날**(= Coming Soon 게시일 T1) | ★ **`max(AppID 생성 + 21일, Playtest 앱 빌드심사 완료)`** — **스토어 페이지 게시와 무관** |
| 그래서 | *"「출시 전 프레스 빌드」라는 흔한 마케팅 수단이 우리에겐 없다"* | ★ **있다. 단 이름이 「프레스 빌드」가 아니라 「Playtest 키」다** |

**일정 이득의 크기 — 정직하게 계산한다.**

```
strategy/ROADMAP.md 3-1 플랫폼 시계:
  T0 = Direct $100 결제 + 신원/은행/세무 제출
  T1 = Coming Soon 게시 = T0 + (신원확인 2~7영업일) + (자산 준비 CS-1~6) + (심사 7영업일 여유)
  D  ≥ max( T0+30일 , T1+14일 )

R7 키 시계:
  키 최단 = AppID 생성 + 21일.  AppID 생성은 신원확인 뒤이므로 ≈ T0 + 21~28일
```

- ★ **21일 < 30일이므로 키 시계는 출시일을 늦추지 않는다.** 출시 공식은 **변동 없다.**
- ★ **이득은 「T1 − 키 최단」이고 그 부호는 자산 준비 기간(CS-1~6)이 정한다.**
  CS-3(스크린샷)이 **아직 안 찍힌 Windows 빌드**에 걸려 있으므로(R6-6·C-1)
  **T1이 길어질수록 이득이 커진다** — 즉 **우리 쪽 병목이 클수록 Playtest 경로가 값을 한다.**
- ★★ **그리고 이 계산이 상품전략의 「$100을 지금 내라」 권고를 한 겹 더 강하게 만든다**:
  **21일 키 시계와 30일 출시 시계가 둘 다 T0에서 출발한다.** 늦게 낼수록 **프레스 접촉 가능일도 같이 밀린다.**
  ★ **결재는 사용자 결정이고 나는 하지 않는다.** 내 일은 **달력에 그 줄을 못박는 것까지**다.

### 9-4. ★★ 리더 질문에 대한 답 — **키 배포는 「스팀 밖 직배포」가 아니다. 그러나 안전하지도 않다**

**질문**: *"키를 프레스/스트리머에게 주는 것이 「스팀 밖 직배포」로 간주돼 코드서명 데드라인이 자동으로 앞당겨지는가?"*

**답: 아니다 — T1~T4 어느 것도 발동하지 않는다.** 근거 셋:

1. `CHANNEL_PRICING_DECISIONS.md`의 **T4 정의가 이미 키를 명시적으로 제외**한다 —
   *"프레스·스트리머에게 **키가 아니라 빌드**를 보낸다"*. **「키가 아니라」가 조건문 안에 있다.**
2. 이 문서 3절 표도 같다 — *"스팀 키를 나눠 준다 → **아니다, 스팀 경유다**"*.
3. 기전: 수령자는 **스팀 클라이언트로 받는다.** MOTW는 다운로드한 앱이 붙이고
   패키지 관리자·런처 내부 다운로드에는 안 붙는다(**2차**).
   ★ **T1~T3도 전부 미발생**이다 — Playtest는 **스팀 안의 자식 appID**이지 itch.io도 자체 사이트도 아니다.

> ### ★★ 그런데 **「데드라인이 안 당겨진다」와 「안전하다」는 다른 말이다.** 여기서 갈린다.
>
> | | MOTW / SmartScreen 평판 | **SAC (SmartScreen App Control)** |
> |---|---|---|
> | 스팀 경유로 비껴가는가 | 비껴간다(2차) | ★ **아니다** — SAC는 **MOTW를 보지 않는다**(1차, `CHANNEL_PRICING_DECISIONS.md:627) |
>
> ⇒ **프레스·스트리머도 「알 수 없는 게시자」를 볼 수 있는 경로에 그대로 있다.**
> 그리고 4-2절이 규정한 **영구 자산**(검색에 남는 제목)은 정확히 **글을 쓰는 사람들**이 만든다.
> **일반 유저의 커뮤니티 질문 글보다 프레스·스트리머 영상 쪽이 검색 수명이 길다.**

### 9-5. ★ 그래서 새로 생긴 판정 요청 — **J-4**(리더·상품전략 소관, 나는 결정하지 않는다)

| # | 판단 | 내가 붙이는 근거 / 대가 / 버리는 조건 |
|---|---|---|
| **J-4** | ★★ **Playtest·프레스 키를 「무서명 상태로」 내보낼 것인가.** 내보낸다면 **서명 조달 목표일이 「첫 업데이트 전」에서 「첫 키가 나가기 전」으로 앞당겨진다** | **근거**: SAC는 MOTW 무관(1차) ⇒ 프레스도 경고 경로에 있다. 그리고 **리드타임 2~3주**(8절)라 *"프레스가 걸리면 그때 산다"*는 8-2와 같은 이유로 **산술적으로 늦는다**<br>**대가**: 조달을 **T0 근처까지** 더 당겨야 한다 — K-1(출시일 − 21일)보다도 앞이다<br>★ **버리는 조건**: **프레스 키를 아예 안 보내기로 하면** 이 판정은 소멸한다(그때 K-2는 종전 값으로 되돌아간다) |

★ **이건 T1~T4에 추가되는 「자동 앞당김 조건」이 아니다.** T1~T4는 **유통 규칙**이고
J-4는 **홍보 일정 선택**이다. 둘을 같은 표에 적으면 *"키를 주면 규칙 위반"*이라는 **거짓 규칙**이 생긴다.
⇒ **`CHANNEL_PRICING_DECISIONS.md` (5)절 표는 건드리지 않았다.** 그건 `product-strategy` 소관이다.

### 9-6. ★★ 마케팅 축의 신규 위험 2건 (전에 없던 것)

| # | 위험 | 근거 | ★ 버리는 조건 |
|---|---|---|---|
| **W-1** | ★★ **본 게임 베타 키는 위시리스트를 먹는다** — *"if a player has your game on their wishlist when they activate one of these keys, **it will remove your game from their wishlist**."*(1차) ⇒ 위시리스트 수가 **Popular Upcoming 노출을 정하는데**(`strategy/ROADMAP.md:528) 우리가 직접 깎게 된다 | 1차 원문 | ★ **Playtest 경로를 쓰면 소멸한다** — Playtest 가입자는 *"still Wishlist or Follow the main game"*(1차). **이것이 Playtest를 고를 가장 강한 단일 사유다** |
| **W-2** | ★ **첫 「알 수 없는 게시자」 코호트가 출시일에서 출시 「전」으로 앞당겨진다.** K-3은 위시리스트 코호트가 **48시간에 압축**된다고 봤는데, 프레스 코호트는 그보다 **먼저** 오고 **글을 남긴다** | 4-2절(영구 자산) + SAC 1차 | 서명이 첫 키 이전에 들어가면 소멸(J-4) |

### 9-7. ★ 남은 미확인 2건 — **추측으로 메우지 않는다**

| # | 미확인 | 왜 중요한가 |
|---|---|---|
| **U-1** | ★★★ **C-3(F2P는 키 발급 불가)이 Playtest 자식 appID에도 적용되는가.** *Keys* 문서는 *"free to download … cannot request Steam Keys by default"*라 하고, *Playtest* 문서는 Playtest 키 요청 절차를 **상세히 안내**한다. **두 문서가 우리 조합(F2P 본편 + Playtest)에 대해 서로 다른 인상을 준다** | ★ **이 하나가 「우리에게 프레스 채널이 있는가」를 정한다.** 없으면 K-2는 *"프레스 사전 접촉 불가"*로 되돌아간다. **판정 수단은 파트너 사이트 실물 화면뿐이고, 그건 계정이 있어야 한다 — 사용자 몫이다** |
| **U-2** | **「3주 대기」가 Playtest 자식 appID에도 적용되는가** | 적용되면 키 최단 = AppID + 21일 그대로. **보수적으로 적용된다고 가정하고 달력을 짰다**(9-3) |

### 9-8. ★ 8-3 달력 수정 — **두 줄이 앞으로 온다**

| 시점 | 마케팅이 하는 일 | 걸려 있는 것 |
|---|---|---|
| ★ **T0 (Direct 결제일)** | **여기서 21일 키 시계가 시작된다.** 달력에 못박는다 | 사용자 결재 |
| ★ **T0 + 21일 이후** | **Playtest 앱 생성 → 간이 심사 → 키 요청.** ★ **스토어 페이지를 기다리지 않는다**(9-2) | **U-1** · Playtest 빌드심사 |
| **D − 21일 이전** | (조달을 한다면) 주문 — ★ **J-4가 채택되면 이 줄이 T0 근처까지 당겨진다** | 사용자 결재 |
| **D − 14일** | 스팀 출시일 입력(I-2) · **무서명 한 줄(5-1)이 이미 페이지에 있어야 한다** | J-3 |
| **D − 14 ~ D** | 개발일지·커뮤니티. ★ **T-1: 실행 파일 zip 여전히 0건.** 키는 **Playtest 키**로 | K-2(재산정) |
| **D** 이후 | 8-3 원표 그대로 | — |

---

## 7. 플랫폼 영향 (R5 시점 기록 — **8-4의 정정을 함께 읽어라**)

> ★ **R7(2026-09-05) 플랫폼 영향**
> - **Windows 영향: 함께 검토함(문서만).** 9절 전체가 Windows 건이다 — **SAC는 MOTW를 안 보므로
>   스팀 키·Playtest 경유로도 「알 수 없는 게시자」를 비껴가지 못한다**(9-4). 프로덕션 `.cs` 수정 0건 ·
>   앱 실행 0회 · 외부 게시 0건 · 스팀 파트너 계정 접속 0회(공개 미러 HTML만 읽었다).
> - **macOS 영향: 함께 검토함 — 방향성 있다.** macOS를 depot으로 추가하는 날 **9-4의 결론이 뒤집힌다**:
>   애드혹 서명은 `com.apple.quarantine`이 붙는 순간 **그 자리에서 막히므로**, macOS는
>   *"키는 안전하다"*가 아니라 **"키로 받아도 스팀 클라이언트 밖으로 한 번이라도 나가면 그날 막힌다"**이다.
>   3절 경고가 그대로 유효하고 **더 세다.**
>
> ★ **R6(2026-09-05) 플랫폼 영향**
> - **Windows 영향: 함께 검토함(문서만).** 이 문서 전체가 이제 **1.0의 유일한 플랫폼**을 다룬다.
>   리드타임 2~3주가 **마케팅 달력의 경성 제약 3개**(K-1·K-2·K-3)를 만든다 → 8절.
>   프로덕션 `.cs` 수정 0건 · 앱 실행 0회 · 외부 게시 0건.
> - **macOS 영향: 함께 검토함 — 강등하되 지우지 않는다.** 아래 macOS 줄은 **8-4에서 죽었다고
>   표시**했고, macOS depot을 추가하는 날 되살아난다. **3절의 애드혹 배포 경고는 지금도 유효하다.**

- **Windows 영향: 함께 검토함(문서만).** 이 문서의 대부분이 Windows 건이다 —
  ① SAC는 MOTW를 안 봐서 **스팀 경유로도 안 비껴간다**(1절 B).
  ② 무서명은 **릴리스마다 평판 리셋** → 상주 앱에 특히 비싸다(1-2절).
  ③ **API 표면이 어젯밤 넓어졌다**(2-2절, 실측 + 음성 대조).
  ④ 스토어 문장·커뮤니티 답글·매뉴얼 문장 **3벌을 미리 준비했다**(5절).
  ★ 전부 **Windows 실기 0회** 상태의 판단이며, 현행 배포본(12:42)에는 관련 코드조차 없다.
- **macOS 영향: 함께 검토함(문서만).** 서명·공증이 **1.0 하드 블로커**라 페이지 시점에는 이미
  서명돼 있고 **무서명 문장이 필요 없다.** 단 **애드혹 상태에서 손 배포하면 그날 막힌다** —
  `codesign` 실측(`flags=0x2(adhoc)` / `TeamIdentifier=not set`)이 그 근거이며,
  그래서 **규율 T-1(스팀 밖 배포 금지)은 macOS에 더 강하게 적용된다**(3절).
