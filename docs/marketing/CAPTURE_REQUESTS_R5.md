# 캡처 요청 목록 — R5 (2026-09-03)

작성: `marketing` · ★ **이 라운드에도 앱을 실행하지 않았다.**
실기는 순번제다(`docs/TEAM.md` 「실기 테스트는 한 번에 하나」). 실행 전 리더 배정을 받는다.

> ★★ **R4 대비 전제가 두 개 바뀌었다.**
> **(가) 화면은 잠겨 있지 않다** — 리더가 밤새 잘못 전제했고, **실기 캡처가 가능하다.**
> **(나) 그런데 빌드가 낡았다** — **어젯밤 커밋(261파일)이 어느 빌드에도 안 들어갔다.**
>
> ⇒ 그래서 이 문서는 **R4에 없던 한 축**으로 갈라져 있다: **「지금 찍어도 되는 것」 / 「새 빌드 필요」.**
> **★ 이건 내 R4가 이미 잡은 함정이다** — *"두 빌드가 서로 다른 제품이다."* 이번엔 **세 제품**이다.

---

## 0. ★★ 먼저 — 빌드 대조표 (촬영 순서를 이게 결정한다)

**바이트 실측**(UTF-16LE, 양성·음성 대조 포함 — `CLAIM_AUDIT_R5.md` 2절):

| 기능 | 트리 `1eb0e2b`<br>(09-03 02:23) | macOS 빌드<br>**09-02 19:07** | Windows 빌드<br>**09-02 12:42** |
|---|:---:|:---:|:---:|
| 창 위 보행 · 클릭 관통 · 던지기 · 행동 명령 | ✅ | ✅ | ✅ |
| 등급 1(패널 회수) · 숨김 단축키 `⌃⌥⌘K` | ✅ | ✅ | ❌ |
| ★ **톱니 탈출구(등급 1 중 설정 도달)** | ✅ | ❌ | ❌ |
| ★ **잡힘 대사 9줄** | ✅ | ❌ | ❌ |
| ★ **온보딩 안내가 안 접힘** | ✅ | ❌ | ❌ |
| ★ **Alt+Tab / 작업표시줄 제외**(Win) | ✅ | — | ❌ |
| 번들 ID | `com.Vibelab.…` | ❌ `com.DefaultCompany.…` | 미확인 |

**검증 방식**: 양성 대조 `걸치는 것` = 양쪽 1건 / 음성 대조 `ZZZ없는문자열` = 양쪽 0건.
**보조**: `codesign -dv` → macOS 빌드의 `Identifier=com.DefaultCompany.StickMate`.

> ### ★ 결론 두 줄
> 1. **P-A군(아래)은 지금 찍어도 된다** — 그 기능들은 세 빌드에 전부 있고 이번 커밋이 안 건드렸다.
> 2. **P-B군은 새 빌드 없이는 「없는 것을 찍는 것」이다.** 대기.

---

## 1. ★★ 「지금 찍어도 되는 것」 — **P-A군 (macOS 19:07 빌드)**

**공통 선행 조건 2개** (하나라도 빠지면 전량 폐기):
- **깨끗한 캡처 환경** — `CAPTURE_PROTOCOL.md` 1절. 남의 창 제목·파일명이 찍히면 소재로 못 쓴다.
- **인스턴스 1개** — 실행 전 `pgrep -f "StickMate.app/Contents/MacOS"` **양성 대조와 함께** 확인.
  ★ **전역 `⌃⌥⌘Q` 절대 금지**(사용자 인스턴스까지 죽는다) · **`driver.sh stop` 금지**(남의 PID).
  자기 PID만 `kill -TERM`.

| # | 찍는 것 | 뒷받침하는 주장 | 형태 | 비고 |
|---|---|---|---|---|
| **A1** | ★★★ **`A-1` 「같은 높이, 세 번」 3분할** (최소 8테이크) | **"애니메이션이 아니라 물리다"** — 무료 시장에서 **개수로 비교당하지 않을 유일한 축**(STORE v2 2-4) | 동영상 1번 슬롯 | 회전 편차 3.3배라 테이크 수가 필요 |
| **A2** | ★★★ **`S-1` 헤드라인 정지컷** — 창 위에 서 있는 전경 | 기능 목록 1번 | 정지컷 | 착지 오차 0.0pt |
| **A3** | ★★★ **`G-1` 균열 + 그 위로 클릭 통과** — 계산기 `7`이 실제 입력되는 것까지 | 기능 목록 8번. ★ **v2에서 스크린샷 2번으로 승격** | GIF 5초 | `clickThroughDefaultEnabled: 1` |
| **A4** | ★★★ **설정창 — 끌 수 있다는 증명** | ★ **v2에서 3단계 승격**(6번→3번). 무료 페이지에서 통제권은 부록이 아니다 | 정지컷 | — |
| **A5** | ★★ **`A-2` 발이 창 상단선에 닿은 클로즈업(200%)** | "발판이 비유가 아니라 좌표다" | 정지컷 | — |
| **A6** | ★★ **행동 명령창**(480×508, 타일 5개, 회색 타일의 **이유 문장** 포함) | **"먼저 나서지 않습니다. 시키면 합니다."** | 정지컷 | 판정이 실행과 **같은 함수**(`CommandAvailability`) |
| **A7** | ★★ **`D-1` 「전부, 최대화돼 있어도」** — 최대화 창 1 + 반쯤 겹친 창 2~3개 사이를 옮겨 다님 | `DIFFERENTIATION.md` 3절. ★ **글로 설득이 안 되는 축** | GIF 8초 | `VisibleTopEdgeSolver` |
| **A8** | ★★ **`V-1` 활쏘기 마지막 정중앙** | 기능 목록 9번 | 정지컷 | 6.14~8.03초 |
| **A9** | ★ **`A-3` 창을 끌면 따라오고, 닫으면 떨어진다** | 기능 목록 2번 | GIF 3초 | — |
| **A10** | ★ **`A-5` 가려진 창은 발판이 아니다** | 정확도 | GIF | **개발일지 전용** |

★ **A1~A10에는 이번 커밋이 만든 것이 하나도 없다.** 그래서 지금 빌드로 찍어도 **옛 제품을 찍는
것이 아니다** — 이 열 항목에 한해 19:07 빌드와 현재 트리는 **같은 제품**이다.

★ **A4(설정창)에 주의**: 설정창의 **탭 구성과 문구는 이번 커밋에서 움직였을 수 있다.**
정지컷을 쓸 때 **탭 이름과 항목 문구가 최신인지** 재확인하고, 아니면 A4만 P-B군으로 내린다.
(**나는 이 항목을 확인하지 못했다 — 미확인.**)

---

## 2. ★★ 「새 빌드 필요」 — **P-B군 (지금 찍으면 거짓 소재)**

| # | 찍는 것 | 왜 지금 못 찍나 | 필요한 빌드 |
|---|---|---|---|
| **B1** | ★★★ **등급 1 중 톱니 탈출구** — 전체화면 앱 위 → 톱니 클릭 → 부채꼴 → 정보창 → 설정창까지 도달 → 닫으면 **0.5초 안에 다시 물러남** | ★ **19:07 빌드에는 등급 1은 있는데 탈출구가 없다.** 지금 찍으면 **톱니를 눌러도 아무 일이 안 일어나는 화면**이 찍힌다 — 그게 정확히 우리가 고친 결함이다 | macOS 재빌드 |
| **B2** | ★★★ **잡힘 대사 2분할** — 머리를 잡았을 때 / 다리를 잡았을 때 | 대사 문자열이 **어느 빌드에도 없다**(바이트 0건). ★ **게다가 문자열이 아직 커밋도 안 됐다** — 커밋 시점은 `"놔, 놔"`, 작업 트리는 `"놔줘 놔줘"`(`CLAIM_AUDIT_R5.md` 0-0-A) | macOS 재빌드 **(커밋 후)** |
| **B3** | ★★ **첫 실행 무편집 한 테이크**(60~90초, 권한 대화상자 0회) + 온보딩 안내 알약 | ① 온보딩 유지 수정이 빌드에 없다 ② ★ **이 머신은 이미 「봤음」으로 기록돼 있다** — 3절 | macOS 재빌드 **+ 3절 절차** |
| **B4** | ★★★ **숨김 단축키 왕복**(`⌃⌥⌘K` → 전체화면 왕복 → `⌃⌥⌘K`) | 19:07 빌드에 **코드는 있다.** ★ 다만 **표기를 양 플랫폼으로 바꿨고**(`Ctrl+Alt+Win+K`) 캡션이 움직였을 수 있다 | ★ **지금 찍어도 되나 캡션 문구를 대조할 것.** 다르면 재빌드 |
| **B5** | ★★ **등급 1 시연**(전체화면 앱 위에서 창·패널만 걷히고 캐릭터는 남음) | 19:07에 있다. ★ **단 B1과 이어서 찍어야 문장이 완성된다**(*"물러나되 갇히지 않는다"*) | ★ B1과 **같은 회차** |
| **B6** | ★ **Windows 전량** | 12:42 빌드에 **등급 1도 숨김 단축키도 잡힘 대사도 Alt+Tab 제외도 없다** | ★ **Windows 재빌드 + 사용자 실기** |
| **B7** | **Alt+Tab / 작업표시줄에서 사라짐**(Windows) | ★ **아직 커밋도 안 됐다**(미추적 파일) + **실기 0회** + 배포본에 없다. 저장소가 스스로 *"실기 미확인"*이라 자백 | ★ **커밋 → Windows 재빌드 → 사용자 실기** 셋 다 |
| **보류** | 장비 / 커스터마이즈 격자 | **27색 중 21색 대비 미달**(D등급) + 레벨 30 저장 파일 필요 | 색 수정 라운드 뒤 |

★ **B4·B5를 「지금 찍어도 됨」에 넣지 않은 이유**: 코드는 있지만 **화면에 뜨는 글자**가 바뀌었을 수
있다. 스크린샷은 **글자까지 제품**이다. 문구 대조 없이 찍으면 **낡은 캡션이 영구히 스토어에 남는다.**

---

## 3. ★★ 신규 선행 절차 — **「첫 실행」은 세이브만 지워서는 안 된다**

`CAPTURE_PROTOCOL.md`의 기존 절차는 **세이브 JSON만** 지운다. **그걸로는 첫 실행이 안 된다.**

온보딩 「봤음」 기록은 **`PlayerPrefs`**에 있다
(`GearRadialMenuWidget.OnboardingSeenKey` = `StickMate.GearMenu.OnboardingSeen.v1`).

**이 머신 실측(읽기 전용):**
```
~/Library/Preferences/unity.Vibelab.StickMate.plist
  "StickMate.GearMenu.OnboardingSeen.v1" = 1;     ← ★ 이미 「봤음」
  "unity.player_session_count" = 330;
```

> ### ★ B3(첫 실행 캡처) 선행 절차
> 1. 세이브: `~/Library/Application Support/Vibelab/StickMate/stickmate_character.json`
>    → **옮긴다**(`.bak`). ★ **지우지 마라 — 되돌릴 수 있어야 한다.**
> 2. ★ **PlayerPrefs**: `~/Library/Preferences/unity.Vibelab.StickMate.plist` → **옮긴다**(`.bak`).
>    ★ **구경로도 확인**: `unity.DefaultCompany.StickMate.plist`가 **따로 실재한다.**
>    빌드의 `companyName`에 따라 읽는 파일이 갈린다 — **어느 회사명으로 빌드했는지 먼저 확인.**
> 3. 촬영 후 **둘 다 되돌린다.**
>
> ★ **이 절차 없이 찍은 「첫 실행」 영상은 거짓 소재다.** 신규 사용자는 **온보딩 알약을 본다.**
> 우리 화면에는 안 뜬다 — **다른 제품을 찍는 것이다.**

★ **Windows에도 같은 함정이 있을 것으로 판단한다**(Unity PlayerPrefs = 레지스트리
`HKCU\Software\<회사>\<제품>`). **나는 확인하지 못했다 — 미확인.** Windows 회차 담당이 확인한다.

---

## 4. 찍지 말 것 (이번 회차 명시적 제외)

| 제외 | 왜 |
|---|---|
| **B1·B2·B3을 지금 빌드로** | 그 기능이 빌드에 **없다**. 찍히는 것은 **고치기 전의 화면**이다 |
| **Windows 전량** | 12:42 배포본은 **세 세대 뒤쳐졌다** |
| ★ **메뉴 막대 근처에 패널이 걸친 프레임** | **규명 중**(`CLAIM_AUDIT_R5.md` 5절). **참인지 거짓인지 판정할 수단이 지금 우리에게 없다** |
| ★ **작업표시줄·Alt+Tab 관련 일체** | 실기 0회 |
| **성능 계측 화면** | 재측정+승인 전 금지. 경쟁 비교는 **귀속 기준이 다르다** |
| **"네트워크 0" 계측기 화면** | 아웃바운드 실측 선행. ★ **그리고 `unity_connect.*` 키 판정 대기**(6절) |
| **장비·색 관련 일체** | D등급 |
| **격파 놀이 / 이벤트 탭 / 모바일** | 존재하지 않는다 |
| **레벨 30 저장 파일로 찍은 보관함을 캡션 없이** | 신규 설치 화면은 `???` **35칸**. 캡션 병기 필수 |

---

## 5. 촬영 회차 제안 (리더가 순번에 넣을 단위)

| 회차 | 항목 | 빌드 | 필요 상태 | 소요 |
|---|---|---|---|---|
| **1회차 (지금 가능)** | **A2 · A5 · A7** | 19:07 | 창 4개 배치 고정 | 중간 |
| **2회차 (지금 가능)** | **A3 · A4 · A6 · A8** | 19:07 | 행동 확률 수동 · ★ A4는 문구 대조 먼저 | 중간 |
| **3회차 (지금 가능)** | **A1 (8테이크)** | 19:07 | 시간 여유 — 물리라 대기 | 길다 |
| **4회차 (지금 가능)** | **A9 · A10** | 19:07 | — | 짧다 |
| ★ **재빌드 게이트** | — | **macOS 재빌드** | ★ 리더 재량(사용자 확정 «덮어써도 됨»). 단 **격리 미러 게이트 3종**을 먼저 통과 | — |
| **5회차 (재빌드 후)** | ★★ **B1 · B5 · B4** | 새 macOS | 전체화면 앱 1개(**게임 아님**) + 신규 저장 | 짧다. ★ **최우선** |
| **6회차 (재빌드 후)** | ★★ **B2** | 새 macOS | 머리/다리 각각 5테이크 이상(구역 판정 확인) | 중간 |
| **7회차 (재빌드 후)** | ★★ **B3** | 새 macOS | ★ **3절 절차 필수** + 깨끗한 계정 | 길다 |
| **Windows 회차** | B6 · B7 | **Windows 재빌드** | ★ 촬영 주체는 **사용자** | 별도 배정 |

★ **1~4회차를 재빌드보다 먼저 두었다.** 이유: 그 열 항목은 **재빌드해도 안 바뀐다**(0절 대조표).
**지금 찍어 두면 재빌드 대기 시간이 낭비되지 않는다.**
★ **역순으로 하지 마라** — 재빌드를 먼저 하면 A군을 **새 빌드로 다시 찍어야 하는 것처럼** 보여
회차가 늘어난다(실제로는 안 늘어나지만, 그 판단을 하려면 다시 대조표를 봐야 한다).

★ **한 회차에 인스턴스 하나.** 끝나면 **자기 PID만** 정리한다.

---

## 6. 리더에게 올리는 요청 3건

| # | 요청 | 왜 |
|---|---|---|
| **C-1** | **1~4회차를 지금 순번에 넣어 달라** | 재빌드와 무관하다. 대기가 곧 손실이다 |
| **C-2** | **macOS 재빌드 시점을 알려 달라** | 5~7회차가 전부 거기 걸려 있다. ★ 재빌드 없이 5회차를 열면 **고치기 전 화면을 찍는다** |
| **C-3** | ★ **A4(설정창) 문구가 이번 커밋에서 바뀌었는지 `ux-designer`에게 확인해 달라** | 바뀌었으면 A4는 P-B군이다. **스크린샷은 글자까지 제품이다** |

---

## 7. 플랫폼 영향
- **Windows 영향: 별도 배정 필요(사유: 실기 환경 부재 + 배포본 3세대 낡음).**
  이 머신에 Windows 실행 환경이 없다. 12:42 배포본에는 등급 1 · 숨김 단축키 · 잡힘 대사 ·
  Alt+Tab 제외가 **전부 없다**. Windows 회차는 **재빌드 뒤 사용자가** 촬영한다.
  ★ **PlayerPrefs 함정의 Windows판(레지스트리)은 미확인** — 회차 담당이 확인한다(3절).
- **macOS 영향: 없음(문서만).** 이 라운드에 앱 실행 0회
  (`pgrep` = 0, 양성 대조 `loginwindow` = 1, 음성 대조 = 0으로 프로브 생존 확인).
  프로덕션 `.cs` 수정 0건. 1~4회차는 이 머신에서 가능하나 **리더 배정 없이는 실행하지 않는다.**
