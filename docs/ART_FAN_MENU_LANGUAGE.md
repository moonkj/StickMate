# 우클릭 부채꼴 메뉴 — 시각 언어 (design-art R14, 2026-09-05)

> 사용자 신규 지시: **우상단 상시 톱니(`InfoGearIconWidget`)를 없애고, 캐릭터를 우클릭하면 부채꼴이 펼쳐진다.**
> 부채꼴은 지금까지 톱니(브라스 금속 조형) 옆에서 그 시각 언어를 물려받았다. 이제 **임의의 바탕화면(밝음·어두움·사진·동영상) + 유저 잉크 선화(흰 잉크 포함)** 위에서 펼쳐진다.
> 이 문서는 **색과 조형**만 정한다. 앵커·기준각·궤도·박자는 `ux-widgets` 소관(`docs/UX_RIGHTCLICK_FAN_MENU.md`, 설계 중 — 이 라운드 시점에 미착지).
> 숫자 전문: `design/art/fan_menu.out.txt`(계산기 `colorlab.py` 교정 통과 + 톱니 헤일로 문서값 4.37 재현) · 시트 `design/art/verify/fan_menu_sheet.png` · `fan_menu_truesize.png`.
> **오프라인 계산·래스터다. 최종 판정은 실기 캡처.**

---

## 0. 한 줄씩

| # | 결론 | 숫자 |
|---|---|---|
| 1 | **현행 5칸은 임의 바탕에서 사라진다** — 원판 `CardSurface` 회색 스윕 최악 **1.00**(3.0 미달 구간 41 %), 테(흰 α0.10 = `#32353C`)도 1.00. 원판+테를 2톤으로 봐도 1.17. 검은 잉크 팔다리와 원판 **1.14** — 캐릭터가 메뉴에 먹힌다 | §1 |
| 2 | **처방 = 2톤 가장자리**: 면 `CardSurface` Ø40 + **밝은 링 `TextPrimary` 2pt**(Ø44 안쪽). 톱니 헤일로와 같은 관용구(`ResolveHaloColor`)를 면에 적용. 회색 0~255 전 구간 보장 **3.90**(최악 `#7A7A7A`). 기하 불변, 새 토큰 0 | §2 |
| 3 | **캐릭터 분리 = 색의 출처 분리 + 2톤.** 메뉴는 잉크색을 쓰지 않는다(크롬 토큰만). 잉크가 어떤 휘도든 가장자리↔잉크 ≥ **3.90**, 장비 재질색↔면 ≥ 3.37 · ↔링 ≥ 3.31 | §3 |
| 4 | 호버·무장 면은 `Flatten`으로 불투명(`#33312D`) — 현행 α0.14는 86 % 투명(UI_SURFACE_SPEC §6.4). 브라스 기호↔면 5.38. 링은 상태와 무관하게 `TextPrimary` 유지(브라스 링은 보장 2.63 미달) | §4 |
| 5 | 글리프 5종 중 **4종이 1.5W 미달**(분침 0.75W · 다리 1.03W · 박스 구멍 1.25W · 확성기 소리선 **0.40W**). 처방 4건(§5) — 확성기는 지휘봉(UI_SURFACE_SPEC §6.3 P1-8)으로 교체 | §5 |
| 5-정정 | ★ **2026-09-06**: 위 숫자 넷 중 **셋이 캡 규약 오류였다**(다리 1.03 → **0.78**W · 확성기 소리선 0.40 → **0.75**W · ⑤ 전원 1.61「통과」 → **1.19W 미달**) ⇒ 미달은 4종이 아니라 **5종 전부**다. **최악은 여전히 확성기**지만 그 자리는 소리선이 아니라 옛 표가 재지 않은 **나팔↔목 0.43pt = 0.22W**다. 지휘봉 교체는 이 크기에서 **기하학적으로 반증**돼 **조형 재작성**으로 이행했다 | §5 |
| 6 | **종료는 자리(위성 168pt)와 기호(전원)로 갈리고, 색은 무장 순간에만** — 평상 색을 다르게 하지 않는다 | §5-2 |
| 7 | 정합성: 부채꼴이 쓰는 토큰 6개가 전부 창의 것(`CardSurface`·`TextPrimary`·`Accent`·`OnAccentSolid`·`PanelSurface`·`Flatten`). 브라스 상시 노출 1곳(톱니)이 0곳이 되고 **상호작용 순간(호버·무장·배지·체크·잔여 호)에만** 남는다 — 바탕화면에 상시로 남는 우리 픽셀은 캐릭터뿐(원칙 2에 유리) | §6 |
| 8 | DLC 6팩: R13 팔레트 이후 대역·ΔE 하한 변화 0. **드리프트 1건** — `PACK_THEME_SPEC` 「그늘 ×0.62」 vs 코드 `AccessoryTone.ShadeFactor` **0.28** | §7 |

---

## 1. 현행 스택 실측 — 톱니 옆에서는 몰랐던 것

`GearRadialMenuWidget.cs:2084-2086 · 1516-1526`: 원판 `CardSurface #1B1F26`(불투명) · 테 `CardBorder`(흰 α0.10, 1.2pt) · 기호 `TextPrimary #F2F4F7` · 호버 `Lerp(CardSurface, AccentSurface α0.14)` · 무장 `AccentSurface`.

| 요소 | 실효색 | L | 회색 스윕 최악(배경) | 3.0 미달 구간 | 검은 잉크 `#111` | 흰 잉크 |
|---|---|---:|---|---:|---:|---:|
| 원판 | `#1B1F26` | 0.0135 | **1.00** (`#1F1F1F`) | 41 % | **1.14** | 16.53 |
| 테 | `#32353C` | 0.0355 | **1.00** (`#353535`) | 49 % | 1.54 | — |
| 원판+테 2톤 | | | **1.17** (`#2B2B2B`) | | | |
| 기호 | `#F2F4F7` | 0.90 | 원판 위 15.00(배경 무관) · 원판이 사라지면 배경과 최악 1.00 | | | |

톱니 옆(우상단, 대개 메뉴 막대·창 제목 위)에서는 배경이 거의 밝았다. 캐릭터 위 = 바탕화면 아무 곳 = 어두운 배경이 절반이다.
`fan_menu_sheet.png` 2·3행 「현행」 열이 그 상태다 — 원판이 사라지고 기호만 떠 있다. 호버는 강조인데 86 %가 투명해진다(§6.4 지적 그대로).

---

## 2. ① 5칸 시각 언어 — 규칙

### 2-1. 원리 — 배경을 모르면 2톤뿐이다

바탕화면 픽셀은 못 읽는다(PALETTE_SPEC §22-2). 단색은 자기 휘도 근처 배경에서 1.00으로 간다. 답은 톱니가 이미 증명한 **역상 2톤**이다:
두 톤의 휘도 `Ld`, `Ll`에 대해 어떤 배경 `Lb`에서도 `max(CR(Ld,Lb), CR(Ll,Lb)) ≥ sqrt((Ll+0.05)/(Ld+0.05))`.

| 후보 (면 + 링) | 보장(회색 256 스윕) | α0.95 | 판정 |
|---|---:|---:|---|
| **`CardSurface` + `TextPrimary`** | **3.90** | 3.68 | **채택** — 둘 다 창의 토큰 |
| `CardSurface` + 흰 `#FFFFFF` | 4.07 | 3.86 | 흰은 이 팔레트에 토큰이 없다(글자 최고단이 `#F2F4F7`) |
| `OnAccentSolid` + `TextPrimary` (톱니 쌍) | 4.18 | 3.98 | `OnAccentSolid`는 「밝은 면 위 잉크」 토큰이지 면이 아니다 |
| `PanelSurface` + `TextPrimary` | 4.06 | 3.85 | 창 바탕 토큰 — 버튼은 카드다 |
| `CardSurface` + `IconInk` | 3.46 | 3.29 | 통과하지만 여유가 적다 |
| `CardSurface` + `Accent` 브라스 | **2.63** | 2.54 | **기각** — 밝은 바탕에서 죽는다 |
| `CardSurface` + `TextSecondary` | 2.84 | 2.71 | 기각 |

### 2-2. 규칙 F-1 ~ F-6

| # | 규칙 | 값 | 근거 |
|---|---|---|---|
| **F-1** | **원판 = 면 + 링 2톤.** 면 `UiChrome.CardSurface` Ø40 · 링 `UiChrome.TextPrimary` **2.0pt**(Ø44 안쪽) | 보장 **3.90** | 링은 「펜 하나」(`UiChrome.PenPoints` 2.0)와 같은 폭 — 부채꼴의 획·캐릭터의 획·링이 한 펜 |
| **F-2** | **알파 0.** 면·링·기호 전부 α1.0. 펼침 페이드(`AlphaFadeInSeconds`)는 전역 하나뿐(장비 O-4와 같은 규칙) | α0.95에서도 3.68 | 창 바탕 α0.96 사고(UiChrome `PanelSurface` 문서)와 같은 병을 안 만든다 |
| **F-3** | **기호 = `TextPrimary`**(면 위 15.00). 색이 바뀌는 기호는 브라스 `Accent`뿐(호버·무장·완료 체크·잔여 호) | 브라스↔면 6.85 · ↔호버면 5.38 | 강조색은 하나(UiChrome `Accent` 문서) |
| **F-4** | **상태 면 = `Flatten`**: 호버·무장 면 `Flatten(AccentSurface, CardSurface) = #33312D`. 링은 상태와 무관하게 `TextPrimary` | 호버면↔평면 1.27 — 상태는 **기호 브라스 + 배율 48/44**가 나른다 | 브라스 링(`#7A6643`)은 보장 2.63 미달 |
| **F-5** | **배지·작은 채움은 자기 2톤을 갖는다**: 할일 배지 `Accent` 면 + **`OnAccentSolid` 1pt 외곽선** | 배지↔링 **2.19**(외곽선 없으면) → 외곽선↔링 17.33 · ↔브라스 7.91 | 배지가 링을 덮는 자리(15,15)라 링과의 경계가 무르다(시트 4열) |
| **F-6** | **이름표·온보딩 알약도 2톤**: `PanelSurface` + 테두리 **`TextPrimary` 1pt**(현행 `PanelBorder` α0.16 실효 `#3A3C40`은 스윕 최악 1.00) | 보장 4.06 · 글자 16.30 | 온보딩 알약 `AccentBorder`(실효 `#77633E`)도 같은 처방 — 앱의 유일한 학습 창구가 어두운 바탕에서 사라지면 안 된다 |

### 2-3. 왜 그림자·배경판이 아닌가

- **그림자**: 2026-09-02에 패널 그림자가 삭제됐다(UiChrome `PanelSurface` 문서 (d)). 반투명 그림자는 바탕화면을 흐리게 덮는 픽셀이고, 어두운 바탕에서는 아무 대비도 못 만든다(검정 그림자↔`#1E1E1E`).
- **배경판(부채꼴 전체를 받치는 판)**: 5칸 지름 44 · 궤도 111 · 위성 168 → 판 면적이 ≈ 200×200pt. 우클릭 한 번에 바탕화면 4만 pt²를 덮는다 — 원칙 2 정면 위반. 링 2pt는 5칸 합쳐 **1,320 pt²**다.
- **잉크색 사용**: 메뉴가 잉크색이면 캐릭터와 같은 물체로 읽힌다(§3). 톱니가 잉크로 그려졌던 것은 「캐릭터의 소지품」이라는 서사였고, 부채꼴은 **창의 일부**(누르면 창이 열린다)다.

---

## 3. ② 캐릭터와의 분리 — 유저 잉크가 무엇이든

두 층이다.

1. **색의 출처가 다르다.** 캐릭터 = 잉크(`StickConfig.ResolveInkColor`, 유저가 바꾼다). 메뉴 = 크롬 토큰(`UiChrome`). 유저가 잉크를 흰색·회색·어떤 색으로 바꿔도 메뉴 픽셀은 한 값도 안 바뀐다. 그래서 「메뉴가 캐릭터가 된다」는 일이 구조적으로 없다.
2. **2톤 가장자리는 잉크 휘도 전 구간에서 선다.** 잉크가 회색 축 어디에 있든 `max(면↔잉크, 링↔잉크) ≥ 3.90`(§2-1과 같은 스윕). 실측:

| 잉크 | 링↔잉크 | 면↔잉크 | 가장자리 보장 |
|---|---:|---:|---:|
| 검정 `#111111` | **17.14** | 1.14 | 17.14 |
| 흰 `#FFFFFF` | 1.10 | **16.53** | 16.53 |
| 임의 회색(최악 L 0.1946 ≈ `#7A7A7A`) | | | **3.90** |
| 장비 재질색 18색(R13) | ≥ 3.31 | ≥ 3.37 | — |
| DLC 팩 12색 | ≥ 3.31 | ≥ 3.36 | — |

즉 검은 잉크 팔이 원판 밑을 지나면 **링이** 가르고, 흰 잉크면 **면이** 가른다. 두께·그림자 없이 색 두 개로 끝난다.
★ 기하 참고(ux-widgets): 출하 0.75에서 캐릭터 높이 ≈ 57pt, 부채꼴 폭 ≈ 222pt·위성 168pt — 머리 중심 앵커·θ₀ 90°(시트 가정)면 원판이 캐릭터와 겹치지 않는다. 겹침은 앵커가 커서이고 캐릭터가 걸어 들어올 때 생긴다 — 그때도 위 표가 성립한다.

---

## 4. 상태 표 (코더 인계)

| 상태 | 면 | 링 | 기호 | 그 밖 |
|---|---|---|---|---|
| 평상 | `CardSurface` | `TextPrimary` 2pt | `TextPrimary` | — |
| 호버 | `Flatten(AccentSurface, CardSurface)` `#33312D` | 그대로 | `Accent` | 배율 48/44 · 이름표(F-6) |
| 눌림 플래시 | 그대로 | 그대로 | 그대로 | `Accent` α0.35 플래시 유지(0.09초 순간, 대비 대상 아님) |
| 집중 세션 중 | `CardSurface` | 그대로 | 링 트랙 `TextPrimary` + 잔여 호 `Accent` | 6.85 |
| 할일 미완료 | `CardSurface` | 그대로 | `TextPrimary` + 체크 `Accent` | 배지 `Accent` + `OnAccentSolid` 글자(7.91) + **`OnAccentSolid` 1pt 외곽선**(F-5) |
| [앱 종료] 평상 | **다른 4칸과 같다** | 같다 | `TextPrimary` 전원 기호 | 자리(위성)와 기호가 갈라 준다 |
| [앱 종료] 호버 | 호버면 | 같다 | `Accent` | 이름표 「앱 종료」 |
| [앱 종료] 무장 | 호버면 | 같다 | `Accent` + 카운트다운 링 **Ø22** `Accent` 1→0 | 이름표 「정말 종료?」(UX_FLOW §53-4 그대로) |

UX_FLOW §53-4 표의 평상 `SubtleSurface`/`TextSecondary`(위성만 한 단 어둡게)는 **철회**를 권고한다 — 임의 바탕에서 `SubtleSurface #191D24`↔`CardSurface` **1.02**라 「한 단 어둡다」가 안 읽히고, 기호 `TextSecondary`는 면 위 8.11로 읽히지만 「흐린 버튼 = 비활성」으로 오독된다(설정창의 비활성 잉크 규칙과 같은 색). 위성은 **자리**가 이미 1차 신호다(UX_WIDGETS R4 「위치가 1차 신호」).

---

## 5. ③ 아이콘 5종 조형 — 44px에서 읽히는가

24pt 상자 · 획 2.0pt(W). 규칙: 인접 획 사이 여유 ≥ 1.5W = 3.0pt(EQUIPMENT_SHAPE_SPEC 규칙 1의 카드 적용값). 코드 기하(`BuildStopwatchSymbol` 등)에서 계산.

> ### ★ 2026-09-06 정정 — 캡 규약 (리더 승인, `coder-ui` 실측 재검증)
> 아래 표의 「여유」 세 칸이 **틀려 있었다.** `UiChrome.AddStroke(길이 L, 두께 t)`는 **보이는 총 길이가 L**인
> 캡슐이다(`UiChrome.cs` — `sizeDelta = length + feather×2`, 스프라이트가 캡슐). 즉 둥근 캡의 **중심**이
> 축 위 `±(L/2 − t/2)`이고 잉크의 끝점이 `±L/2`다. 원래 계산은 캡 중심을 `±L/2`로 잡은 뒤 반지름 `t/2`를
> **한 번 더** 뺐다 — 마주 보는 캡슐 쌍마다 여유를 `(t_a + t_b)/2`만큼 **작게** 본다.
> (근거: `DESIGN_FAN_MENU_ICONS.md` §1-2 · 같은 값을 `coder-ui`가 프로덕션 소스를 파싱한 별개 래스터로
> 재측정해 1/32pt 격자 안에서 재현했다 — 괄호 안이 그 실측이다.)
>
> **판정은 살아남는다**: 최악은 여전히 ④ 확성기이고, ①②③④는 그대로 미달이다.
> **바뀌는 것은 숫자와 처방 지점** 둘이다 — ④의 병은 소리선이 아니라 **나팔↔목**(원래 표가 재지 않은
> 자리, **0.43pt = 0.22W**가 카탈로그 전체 최악)이고, **⑤도 미달**이라 「통과 1건」이 **0건**이 된다.
> ⑤가 미달이라는 사실이 곧 2026-09-06 라운드에서 `PowerRingDiameterPoints` 20→22 · `PowerGapDegrees`
> 50→62를 넣은 이유다(그 두 상수는 리더 승인 후 실장됐다).

| 기호 | 가장 좁은 곳 | 옛 표 | **정정 여유** | W | 판정 | 처방 |
|---|---|---:|---:|---:|---|---|
| ① 스톱워치 | 분침 끝 ↔ 링 안쪽 | 1.50pt | **1.50pt** (실측 1.53) | 0.75 | ★ 미달 | **분침 6.5 → 5.0pt**(끝 y 6.0, 여유 3.0 = 1.5W). 시침은 3.00 통과 |
| ② 스틱맨 | 다리 끝 사이 | 2.06pt | **1.56pt** ↓ | 0.78 | ★ 미달 | 다리 벌림을 수직에서 **±25°**로(끝 사이 3.07 = 1.54W) · **획 1.8 → 2.0**(펜 하나; 머리 Ø7 구멍 3.0 = 1.5W) |
| ③ 체크리스트 | 빈 박스 구멍 | 2.50pt | **2.50pt** | 1.25 | ★ 미달 | 3행이 **산술적으로 불가능**하다(3×6.0 + 2×3.0 = 24.0 = 필드 세로 유효폭, 여백 0) → **2행**으로 내리고 박스 구멍 3.5 |
| ③ 체크리스트 | ★ 체크 ↔ 아래 박스(옛 표 미측정) | — | **1.34pt** (실측 1.34) | 0.67 | ★ 미달 | 위와 같은 처방(2행 재배치로 3.55pt) |
| ④ 확성기 | 입 ↔ 소리선 | **0.81pt** | **1.50pt** ↑ | 0.75 | ★ 미달 | 소리선 **2개 → 1개**(반경 6.6 호). 두 호가 서로 3.0pt를 지키려면 바깥 호가 필드를 2.6pt 넘는다 |
| ④ 확성기 | ★ **나팔 ↔ 목**(옛 표 미측정 · **전체 최악**) | — | **0.43pt** (실측 0.44) | 0.22 | ★ 미달 | 나팔 **낱획 4개 → 닫힌 꺾은선 1조각**(원래 한 물건이므로 간극 규칙 자체가 사라진다) |
| ⑤ 전원 | 세로획 ↔ 틈 가장자리 | 3.23pt | **2.38pt** ↓ | 1.19 | ★ **미달**(옛 표는 통과로 적혀 있었다) | **Ø20 → 22 · 틈 50° → 62°** ⇒ `(11−2)·sin31° − 1 = 3.64pt = 1.82W` |

★ **F-L2「확성기 → 지휘봉 교체」는 이 크기에서 반증됐다** — 후보 8안을 좌표로 만들어 실크기까지 렌더한
결과(`design/fanmenu/verify/r26_slot4.png`), 「봉 19pt + 방사 3획」은 방사 3획이 서로 3.0pt를 지키려면
바깥 끝이 r ≈ 21pt로 필드(15pt)를 6pt 넘어 **기하학적으로 불가능**하고, 남은 5안은 실크기에서 핀 / 펜 /
마이크 / 스와시 / 열쇠고리로 읽혔다. **형태 교체가 아니라 조형 재작성**으로 이행한다(리더 판정 2026-09-06,
`DESIGN_FAN_MENU_ICONS.md` §6-④). 의미 축(「알린다」 vs 「시킨다」)은 **손잡이를 되살리는 것**으로 완화된다.

1× Windows(44px, 획 2px)에서 미달 칸은 획 사이 **0.4~2.5px**다. `UiChrome.EdgeFeather`(0.5pt/변)까지
세면 **알파 0인 골 = 여유 − 1.0pt**이므로 ④의 0.43pt는 골이 **−0.57px** — 1×에서 두 획이 **한 줄로 합쳐진다**.
시트 `fan_menu_truesize.png`에서 분침이 링에 붙고 확성기가 뭉개지는 것이 보인다. 실기 미확인.

### 5-2. 종료만 다르게 보이는 규칙

- **1차 = 자리**(위성 168pt, 축 위) · **2차 = 기호**(전원 — 다른 넷과 실루엣 가족이 다르다, §53-5) · **3차 = 색, 무장 순간에만**(브라스 기호 + 카운트다운 링).
- **평상 색을 다르게 하지 않는다.** 빨강은 이 팔레트에 없고(강조색 하나), 브라스로 상시 표시하면 메뉴를 열 때마다 되돌릴 수 없는 버튼에 눈이 먼저 간다. 색이 뜻을 갖는 순간은 「장전됐다」 하나여야 한다(§53-4의 결론과 같다).

---

## 6. ④ 톱니가 사라진 뒤 — 앱 전체 시각 언어 정합성

| 자리 | 면 | 가장자리 | 잉크/기호 | 강조 |
|---|---|---|---|---|
| 정보창·설정창 | `PanelSurface` / `CardSurface` | `PanelBorder` α0.16 + **`PanelHighlight` 위 1px 밝은 선** | `TextPrimary` | `Accent` 브라스(칩·리본·[착용]) |
| 부채꼴(제안) | `CardSurface` | **`TextPrimary` 2pt 링** | `TextPrimary` | `Accent`(호버·무장·배지·체크·잔여 호) |
| 캐릭터·장비 | 재질색(대역 L 0.163~0.240) | 잉크 윤곽 | 잉크 | 없음(등급색 0) |

- 창의 「위 1px 밝은 선(굴절 테두리)」이 부채꼴에서는 **사방 2pt 링**이 된다 — 창 안에서는 창 바탕이 어두움을 보장하니 한 변으로 족했고, 바탕화면 위에서는 사방이 필요하다. 같은 문법, 다른 두께.
- 브라스는 사라지지 않는다. 톱니(상시 1곳)가 없어지고 **호버·무장·배지·체크·잔여 호·창 안 크롬**에 남는다. 「브라스 아카이브」의 정의는 *브라스가 상시로 보이는 것*이 아니라 *강조가 브라스 하나라는 것*이다 — 그건 그대로다.
- 새 토큰 0 · 새 hex 0. 부채꼴이 쓰는 토큰 전부가 창의 것이다: `CardSurface` `TextPrimary` `Accent` `OnAccentSolid` `PanelSurface` + `Flatten`.
- 「펜 하나」: 캐릭터 획(0.75에서 ≈2.03pt) = 부채꼴 기호 획 2.0 = 링 2.0. 캐릭터와 메뉴가 **같은 펜, 다른 잉크통**(잉크 vs 크롬)으로 그려진다 — 이것이 ②와 ④를 동시에 만족시키는 문장이다.
- `InfoGearIconWidget.ResolveHaloColor`(잉크에서 더 먼 쪽 고르기)는 위젯이 사라져도 **관용구는 살려야 한다** — `UiChrome`으로 옮길 것을 권고(코더). 부채꼴 링은 고정 `TextPrimary`라 이 함수를 쓰지 않지만, 말풍선 외곽선·앞으로의 상시 표면이 다시 쓴다.

---

## 7. ⑤ DLC 6팩 재점검 (R13 재질 팔레트 이후 처음)

| 팩 | 주색 | L | 보조색 | L | 재질 24색 최근접 ΔE | 부채꼴 면↔주색 | 링↔주색 |
|---|---|---:|---|---:|---:|---:|---:|
| 오피스 워커 | `#456ECC` | 0.1678 | `#6080CC` | 0.2228 | 8.37 | 3.43 | 4.38 |
| 사이버 아포칼립스 | `#009682` | 0.2342 | `#518C84` | 0.2217 | 10.73 | 4.47 | 3.35 |
| 네온 낙서 | `#CC1BA9` | 0.1649 | `#9C5A8E` | 0.1633 | 33.90 | 3.38 | 4.44 |
| 스포츠 | `#CC3F29` | 0.1655 | `#9E655C` | 0.1735 | 10.88 | 3.39 | 4.42 |
| 컬러 잉크 | `#9768CC` | 0.2084 | `#8563AB` | 0.1685 | **8.06** | 4.07 | 3.69 |
| 밀리터리 | `#639400` | 0.2383 | `#798C51` | 0.2342 | 15.13 | 4.54 | 3.31 |

- 대역(0.1633~0.2383)·항등·ΔE 하한 8.0: **변화 없음**(R13이 재질 hex를 하나도 안 만들었다).
- R13 규칙이 팩에 주는 것: 채움 = 팩 주/보조색 불투명(R-1) · 윤곽 = 잉크(R-2) · 독립선 = 팩색(R-3) · 팩당 보조색 조각 1(PACK_THEME_SPEC §4) 그대로. 그늘(R-5)은 팩에서도 「자기 채움 안쪽·머리 원반」에만.
- ★ **드리프트 1건**: `PACK_THEME_SPEC` §5·§10-8·§11-1의 그늘색(`#2B447E` 등)은 「`FillOutlineColor` ×0.62」로 적혀 있는데 코드는 `AccessoryTone.ShadeFactor = 0.28`이다(0.62는 옛 상수). 0.28로 다시 유도하면 오피스 `#131F39` · 사이버 `#002A24` · 네온 `#39082F` · 스포츠 `#39120B` · 잉크 `#2A1D39` · 밀리터리 `#1C2900`. **문서 값 정정 필요 — `PACK_THEME_SPEC.md` 수정은 리더 배정 후**(이 라운드는 손대지 않았다).
- 부채꼴 크롬은 팩 12색과 독립(면↔팩색 ≥ 3.36 · 링↔팩색 ≥ 3.31) — 팩을 입은 캐릭터 위에서도 가장자리가 선다.

---

## 8. 이음매 (코더용 — `.cs`는 `coder` 소관)

| 어디 | 무엇 |
|---|---|
| `GearRadialMenuWidget.BuildButton` :2084-2086 | `Surface` 지름 `d` → `d − 2·RingPoints`(Ø40) · `Border`(`CardBorder` 1.2pt) → **링 `TextPrimary` 2.0pt** `AddCircle(…, d, TextPrimary, 2f)` |
| `ApplyVisual` :1516-1526 | `surface = Color.Lerp(CardSurface, AccentSurface, hover)` → `Color.Lerp(CardSurface, UiChrome.Flatten(AccentSurface, CardSurface), hover)`; 무장도 같은 면 · `border` 상태 보간 삭제(링 고정) · 위성 평상 `SubtleSurface/TextSecondary` 분기 삭제(§4) |
| 배지 :2116 | `BadgeSurface` 아래에 `AddCircle(…, 16f + 2f, OnAccentSolid)` 한 장(F-5) |
| 이름표·온보딩 :2012 · 2046 | `AddOutline(…, PanelBorder/AccentBorder, 9)` → `TextPrimary`, 두께 1 |
| 기호 | ~~§5 처방 4건(분침 5.0 · 다리 ±22°/획 2.0 · 박스 5.0 · 확성기 → 지휘봉)~~ → ★ **2026-09-06 실장 완료**, 다만 처방이 캡 규약 정정으로 바뀌었다: 분침 **4.0**(끝 잉크 5.0) · 다리 **±25°** · 획 2.0 · 체크리스트 **2행**(박스 경로 5.0, 구멍 3.5) · 확성기 **재작성**(닫힌 나팔 + 손잡이 + 소리선 1) · ⑤ 전원 **Ø22 / 틈 62°**. 좌표 정본은 `DESIGN_FAN_MENU_ICONS.md` §6 |
| `InfoGearIconWidget` 삭제 시 | `ResolveHaloColor` → `UiChrome.HaloFor(Color ink)`로 이관(관용구 보존) · `InfoGearHaloContrastTests`는 그 함수를 겨누도록 |
| 신규 테스트(`test-engineer`) | 회색 256 스윕으로 `max(CR(면,bg), CR(링,bg)) ≥ 3.0` — 프로덕션 상수를 읽어서. 양성 대조: 링을 `CardBorder`로 바꾸면 1.17로 빨개지는가 |

세이브·스키마 0 · 기하 상수 0 변경(`ButtonDiameterPoints` 44 그대로, 링은 안쪽).

---

## 9. 리더 판정 필요 · 미확인

| # | 항목 | 내 권고 |
|---|---|---|
| F-L1 | UX_FLOW §53-4 위성 평상 `SubtleSurface/TextSecondary` 철회 | 철회(§4) — `ux-designer`/`ux-widgets` 교차 |
| F-L2 | 확성기 → 지휘봉 교체(P1-8 재상정) | ~~교체 — 수치 근거 0.40W 추가~~ → ★ **2026-09-06 종결: 교체하지 않는다.** 「봉 19pt + 방사 3획」은 이 필드에서 기하학적으로 불가능하고(방사 끝 r ≈ 21pt > 15pt) 남은 후보 5안은 실크기에서 전부 다른 물건으로 읽혔다. **조형 재작성**으로 이행 — 리더 판정 완료, 실장됨. 근거 수치도 0.40W가 아니라 **0.22W(나팔↔목)** 였다(§5 정정) |
| F-L3 | `PACK_THEME_SPEC` 그늘 ×0.62 → ×0.28 정정 | 정정(값 6개 §7) |
| F-L4 | `ResolveHaloColor` 이관 자리 | `UiChrome` |

미확인: 실기 캡처 0회 · Windows 1×에서 2pt 링의 실제 픽셀(2px, 안티앨리어싱 없이 정수) · 사진·동영상 바탕(고주파)에서 2pt 링이 충분한지 — 회색 스윕은 휘도 최악을 덮지만 질감은 못 덮는다 · 앵커·기준각(ux-widgets) 확정 후 캐릭터 겹침 실측.

---

## 10. 인계 계약

| 상대 | 지점 | 어떻게 |
|---|---|---|
| `ux-widgets` | `UX_RIGHTCLICK_FAN_MENU.md`(설계 중) 기하·박자 | 색·조형은 이 문서, 기하는 그쪽. 겹치는 1건: 위성 평상 스타일(§4 F-L1). 링이 Ø44 안쪽이라 궤도·간격·히트 원에 영향 0 |
| `ux-designer` | UI_SURFACE_SPEC §6.1(펜 하나)·§6.3(지휘봉)·§6.4(호버 Flatten) | 셋 다 이 문서가 채택·수치 보강. §6.4의 옛 파랑 값(`#243143` 등)은 브라스로 재계산(`#33312D`) |
| `coder` | §8 | |
| `test-engineer` | §8 마지막 행 | |
| 리더 | §9 | |

## 11. 플랫폼 영향

- **Windows 영향: 없음**(설계 문서·검산·시트, `.cs` 0줄). 2pt 링의 Windows 1× 실제 모습·DPI 125/150/175 %에서의 링 픽셀(2.5/3/3.5px)은 **실기 미확인** — 링 폭은 `UiChrome.PenPoints`를 참조하면 DPI 정책(`UiGlyphScalePolicy`)을 그대로 탄다.
- **macOS 영향: 함께 검토함 — 없음.** 빌드·앱 실행·Unity 락·Chrome 없음.

## 12. 재현

```bash
cd design/art/verify
python3 fan_menu.py > ../fan_menu.out.txt     # 교정(4.37 재현) → 현행 → 2톤 후보 → 분리 → 상태 → 글리프 → 알약 → 팩
python3 fan_sheet.py                          # fan_menu_sheet.png (2290×3056) · fan_menu_truesize.png (950×224)
```
