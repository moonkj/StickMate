# 집중 세션 모션 사양 — 25분을 어떻게 채우는가 + 팔짱 기하 재설계

작성: `design-motion` / 2026-09-06
범위: 자세 · 박자 · 전이 · 무게감 · 행동-텍스트 싱크.
범위 밖: 링/팝오버 UI(`ux-designer`) · 대사 문안(`design-narrative`) · 비율·획(`design-character`).

> **이 문서는 설계만 한다. 프로덕션 `.cs`는 한 줄도 고치지 않았다.** 구현 배정은 리더가 한다.
> 모든 수치의 유도·검산: `design/motion/2026-09-06_집중세션_팔짱기하_검산.py`
> 실행 결과 전문: `design/motion/2026-09-06_집중세션_팔짱기하_검산.out.txt`
> **문서 위치 판단**: 리더가 「`UX_MOTION_DANCE.md` 또는 기존 집중모드 설계 문서」를 지정했으나,
> 전자는 춤 전용이고 후자(`docs/UX_FLOW.md` 18절)는 **`ux-designer` 소유 파일**이라 내가 고칠 수 없다
> (`MOTION_SPEC.md` 2-6절이 같은 이유로 UX_FLOW 개정안을 문안으로만 넘긴 선례). 그래서
> `UX_MOTION_*` 계열의 **형제 문서**로 신설했다. 반려되면 이 파일 내용을 그대로 옮기면 된다.

> ### ⚠ 이번 라운드에서 **건드리지 않은 것** (리더 지시)
> - `FocusWatchDirector.IsTimerRingWarranted` / `IsStartPoseConfirmed` / 시작 포즈 재시도 창 — **정상 동작. 무수정.**
> - `StickConfig.pomodoroStartPoseHoldSeconds = 2` — **무수정.** 이 문서는 그 2초 **이후**를 다룬다.

---

## 0. 리더가 먼저 읽을 6줄

1. **페르소나가 맞다.** 오늘 고친 것은 세션의 **0.1333%**(2.0초 / 1500초)다. 나머지
   **99.8667%** 는 링만 떠 있고 캐릭터는 평소와 완전히 동일하다. 이 문서는 그 87%(Idle 구간)를
   집중 세션 전용 그림으로 바꾼다.
2. **팔짱은 이 리그에서 「완벽하게」는 불가능하다 — 벽이 두 개고, 둘째 벽은 아무도 몰랐다.**
   (가) 팔 0.3297 H (인체 0.44 H), 전완 하나가 몸통 길이의 **45%**.
   (나) ★ **규칙 B(크리즈) 상한 = 팔꿈치 116.55°.** 팔꿈치를 더 깊게 접어야 손이 몸으로
   들어오는데, `LimbCurveRenderer`의 필렛 원호가 획 반두께보다 작아져 **관절 안쪽에 각진
   크리즈(잉크 뭉침)** 가 남는다. 튜닝이 아니라 렌더러 기하의 벽이다. (2절)
3. **그래서 「X자 교차」는 포기한다 — 수치로 불가능을 증명했다.** 두 팔꿈치를 최대로 벌려도
   높이 차가 **0.1067**(몸통의 12.8%, 팔 획 두께의 1.02배)뿐이라 교차점이 **언제나 몸통 획
   안쪽**에서 생긴다. 화면에서는 X가 몸통선에 먹혀 안 보인다. (3-1절)
4. **대신 진짜 판별 신호를 바꾼다: 「어느 손이 가장 앞인가」.** 현행은 **가장 앞에 나온 손이
   가장 낮은 손**(몸통비 0.504, 허리)이라 정의상 "손 모으기"다. 신규는 부호를 뒤집어
   **최전방 = 가슴 높이(0.685)** 로 만든다. **이 한 줄이 이번 재설계의 전부다.** (3-3절)
5. **감사 구멍 2건 발견(별건, 지금 안 고치면 계속 안 보인다).** `LimbCurveGeometryTests`는
   `StickConfig`의 `*Elbow*Degrees` 필드만 훑는데, 집중 모드의 실제 팔꿈치는
   **팔짱 115°**(animator의 `private const`)와 **안경 121.4°**(IK 유도)다. **둘 다 검사 대상 밖**이고
   후자는 전체 최악 여유를 1.1682 → **1.0588** 로 이미 깎고 있다. (6절)
6. **「안경 밀어올리기」는 안경 유무가 문제가 아니었다 — 손이 화면에 안 나온다.** 손끝이 머리
   실루엣 밖으로 나온 양이 배율 0.75에서 **0.13pt**. 머리는 불투명이고 팔보다 위에 그려진다.
   `MOTION_SPEC.md` 13절이 손차양 122°에서 잡은 것과 **같은 병의 재발**이다. (5절)

---

## 1. 리그 실측 (배율 1.0, 전부 재유도한 값)

`SceneBootstrapper`의 상수에서 접지 보정까지 그대로 재현했다. 전신 높이가
`StickConfig.BaselineCharacterTotalHeight = 2.2746944`와 소수 7자리까지 일치하므로 재현이 옳다.

| 항목 | 값 | 비고 |
|---|---|---|
| 전신 H | 2.2746944 | 재현 검증 통과 |
| 엉덩이 y | 0.9347 | 몸통비 0.00 |
| 어깨 y | 1.7647 | 몸통비 1.00 |
| **몸통 길이 T** | **0.8300** | 이 문서의 "몸통비" 분모 |
| 몸통 상단 / 머리 중심 | 1.8347 / 2.0547 | 머리 시각 반경 0.22 |
| 상완 / 전완 | 0.380 / 0.370 | 합 0.750 = **0.3297 H** |
| **전완 ÷ 몸통** | **0.446** | 전완 하나가 몸통의 45% |
| 팔 획 / 몸통 획 | 0.1045 / 0.11495 | 합반폭 **0.1097** |

**이 문서 전용 유도 상수 3개** (추정이 아니라 위 표에서 나온다):

| 이름 | 값 | 유도 |
|---|---|---|
| 상완 **노출** 하한 | **16.78°** | `asin(합반폭 ÷ 상완) `— 팔꿈치가 몸통 획 **밖**으로 나오는 최소 어깨각 |
| 상완 **반노출** 하한 | **8.30°** | `asin(합반폭 ÷ 2상완)` — 팔꿈치가 획의 절반은 뚫고 나오는 각 |
| **몸통 기울임 상한** | **7.60°** | `asin(합반폭 ÷ 몸통)` — 이보다 기울면 **팔이 몸에서 떨어져 보인다** |

> ★ 세 번째가 중요하다. `RequestBodyLean`은 **Torso와 Head만** 회전시키고
> (`StickmanPoseAnimator.ApplyBodyPlacement`) **팔 부착점은 루트 고정이라 안 움직인다.**
> 즉 기울일수록 그려진 어깨선이 옆으로 밀려 팔 부착점과 어긋난다. 현행
> `FocusWatchLeanDegrees = −5°`는 이 상한의 **66%** 라 안전하다.
> `bodyLeanHitDegrees = 14°`가 이 상한의 1.8배인데, 그건 `bodyLeanHitRecoverRate = 7/초`로
> **0.43초 만에 95% 사라지는 순간 리액션**이라 별개다(그 순간에는 오히려 어긋남이 타격감이 된다).
> **지속 자세에는 7.60°를 넘기지 마라.**

---

## 2. 진짜 벽 — 규칙 B(크리즈) 상한 = 팔꿈치 **116.55°**

`Tests/EditMode/LimbCurveGeometryTests.곡률_반경이_획_반두께보다_커서_안쪽에_각진_크리즈가_남지_않는다`
가 쓰는 식을 **글자 그대로** 재현했다. 재현 검증: 팔꿈치 122° / 배율 0.35 → 여유 **1.0461**,
`MOTION_SPEC.md` 13-6이 기록한 값과 일치.

| 팔꿈치 | 전 배율 최악 여유 | 판정 |
|---|---|---|
| 98 (손차양) | 1.6497 | OK |
| **115 (현행 팔짱 최대)** | **1.2044** | OK — 다리 병목 위 |
| **116.55** | **1.1682** | ★ **무손상 상한** — 여기까지는 전체 최악이 다리(126°) 그대로 |
| **121.4 (현행 안경, IK)** | **1.0588** | ★ **전체 최악을 이미 깎고 있다** |
| 122 | 1.0461 | 팀이 손차양에서 **일부러 물러난** 값 |
| **124.14** | **1.0000** | ★ **절대 상한** — 넘으면 관절 안쪽에 각진 크리즈 |
| 130 | 0.8781 | 위반 |

**채택: 무손상 상한 116.55°를 예산으로 쓴다.** 절대 상한(124.14°)까지 가면 팔짱이 눈에 띄게
좋아지지만(손 몸통비 0.685 → 0.739), `MOTION_SPEC.md` 13-7이 못박은 **취약성 표**가 되살아난다 —
여유 1.00 구간에서는 `FilletLengthRatio`·`MinStrokeScreenPoints`·`MinCharacterScale` 중 **하나만
움직여도** 즉시 크리즈가 생긴다. 팀이 손차양에서 그 값을 버린 이유와 같은 이유로 여기서도 안 쓴다.

---

## 3. 【과제 2】 팔짱 포즈 기하 — 확정 사양

### 3-1. 왜 「X자 교차」를 포기했는가 (불가능 증명)

팔꿈치는 어깨 중심 반지름 0.380 원 위에만 있다. 어깨각 −44°~0° 전 구간에서

```
팔꿈치 y ∈ [1.3847, 1.4913]   폭 = 0.1067
= 몸통 길이의 12.8%,  팔 획 두께의 1.02배,  획 합반폭(0.1097)보다 작다
```

두 전완이 X로 교차하려면 **시작점(팔꿈치)의 높이가 확실히 갈라져** 있어야 하는데,
이 리그는 두 팔꿈치를 최대로 벌려도 **획 하나만큼도 못 벌린다**. 그래서 교차점은 언제나
몸통선 ±0.05 안에서 생기고 — 실제로 아래 확정 해의 교차점도 **x = −0.005** 다 —
**화면에서는 몸통 획에 먹혀 X가 보이지 않는다.**

> 이건 각도 튜닝으로 못 넘는다. 넘으려면 **어깨를 좌우로 분리**(`attachLocal.x = ±d`)해야 하는데
> 그건 2026-08-28에 "팔이 몸통에 겹쳐 안 보인다"는 사용자 스크린샷으로 **일부러 되돌린 결정**이다
> (`SceneBootstrapper.cs:802`). 되살릴 근거가 없다.

### 3-2. 그래서 무엇을 포기했고 무엇을 얻는가 (정직하게)

| 실제 팔짱의 단서 | 이 리그에서 | 판정 |
|---|---|---|
| 두 전완이 가슴 앞에서 X로 교차 | 교차점이 몸통 획 안쪽 → 안 보임 | **포기** (3-1 증명) |
| 손이 반대편 상완을 **잡는다** | 그 접촉을 만들려면 팔꿈치 **119.1°** 필요 (>116.55) | **포기** |
| 전완이 **가슴 높이**(몸통비 0.75+) | 116.55° 예산에서 최대 **0.685** | **부분 달성** |
| **최전방 점이 위쪽 전완**이고 아래 팔이 그 밑으로 들어간다 | 달성 가능 | ★ **여기에 전부 건다** |
| 상완이 갈비뼈에 붙고 팔꿈치가 뒤/아래 | 달성 가능 | 달성 |

### 3-3. 확정 각도 (배율 불변 — 각도는 배율에 안 걸린다)

두 팔에 **다른 값**을 준다. 실제 팔짱의 옆모습은 대칭이 아니다.

| | 어깨각 θu | 팔꿈치 θl | 전완 절대각 | 팔꿈치 좌표 (몸통비) | 손 좌표 (몸통비) | 앞으로 |
|---|---|---|---|---|---|---|
| **A — 위 팔 (최전방)** | **−8.5°** | **116.5°** | +108.0° (수평 +18.0°) | (−0.056, 1.389) 0.547 | (+0.296, 1.503) **0.685** | 1.34 R |
| **B — 아래 팔 (안쪽)** | **−37.0°** | **113.0°** | +76.0° (수평 −14.0°) | (−0.229, 1.461) 0.634 | (+0.130, 1.372) 0.527 | 0.59 R |

> R = 머리 시각 반경(0.22). 좌표는 배율 1.0 루트 로컬.

**팔 배정** (중요 — 반대로 넣으면 전이가 두 배로 요란해진다):

```
limb.NeutralSign > 0  (Idle 중립 +40°, 앞쪽 팔)  →  A   (이동량 48.5°)
limb.NeutralSign < 0  (Idle 중립 −40°, 뒤쪽 팔)  →  B   (이동량  3.0°)
   총 51.5°.  반대로 배정하면 108.5°  ← 2.1배
```
즉 **뒤쪽 팔은 거의 그 자리에서 팔꿈치만 접고, 앞쪽 팔이 가슴 앞으로 접혀 들어온다.**
이게 실제로 사람이 팔짱을 끼는 순서이기도 하다.

**현행 코드 형태(`중앙 ± 스태거`)로 표현하면:**

```
FocusCrossArmUpperDegrees  −14.0  →  −22.75   (A/B 중앙)
FocusCrossElbowDegrees     104.0  →  114.75   (A/B 중앙)
FocusCrossArmStaggerDegrees 11.0  →  어깨 ±14.25 / 팔꿈치 ∓1.75   ★ 두 값으로 분리 필요
```
★ 현행은 스태거 하나로 어깨(×0.5)와 팔꿈치(×1.0)를 **묶어서** 만드는데, 신규는 어깨 스태거가
크고(28.5° 전폭) 팔꿈치 스태거가 작아서(3.5° 전폭) **하나의 상수로는 표현할 수 없다.**
`FocusCrossShoulderStaggerDegrees` / `FocusCrossElbowStaggerDegrees` 로 **쪼개라.**
★ **부호도 뒤집힌다**: 현행은 `NeutralSign>0`이 더 뒤로(−19.5°) 가는데, 신규는 앞으로(−8.5°) 온다.

### 3-4. 판정 지표 — 현행 vs 신규

| 지표 | 현행 | 신규 | 의미 |
|---|---|---|---|
| **최전방 손의 몸통비** | **0.504** | **0.685** | 0.50=허리 / 0.75=가슴 |
| 가장 높은 손의 몸통비 | 0.611 | 0.685 | |
| 두 전완 절대각 차 | 11.0° | **32.0°** | 11°면 두 선이 나란해 "손 두 개"로 읽힌다 |
| **손 물림 = 위손x − 아래손x** | **−0.071** | **+0.165** | ★ **부호가 뒤집힌다** (= 팔 획의 1.58배) |
| 규칙 B 여유 | 1.2044 | 1.1694 / 1.2519 | 둘 다 다리 병목(1.1682) 이상 → **전체 최악 무변화** |

> ★ **네 번째 줄이 이번 재설계의 전부다.** 현행은 「가장 앞에 나온 손 = 가장 낮은 손」이라
> 기하학적으로 *반드시* '허리 앞에 두 손을 모은' 그림이 된다. 페르소나가 본 것은 튜닝 오차가
> 아니라 **부호 오류**였다.

### 3-5. 배율 스윕 — 세 배율 전부에서 실루엣이 남는가

| 배율 | 팔 획(pt) | 손 돌출(pt) | 전완 길이(pt) | 판정 |
|---|---|---|---|---|
| 0.35 | 2.00 | 4.09 | 4.56 | OK (돌출 > 획 × 2.0) |
| 0.60 | 2.21 | 7.00 | 7.83 | OK |
| 0.75 (출하) | 2.76 | 8.75 | 9.78 | OK |
| 1.00 | 3.68 | 11.67 | 13.04 | OK |

### 3-6. 관절 한계 / 랙돌 진입 검사

- 어깨 −37.0° — `ShoulderSwingBackLimitDegrees = 60°` 안. 통과.
- 팔꿈치 116.5° — 랙돌 힌지 한계 `MaxJointBendDegrees = 100°`를 16.5° 초과.
  **현행 115°도 이미 15° 초과**하므로 새로 생기는 문제가 아니다(능동 상태에서는 관절이 비활성).
  세션 중 랙돌 진입 경로는 배포 기본값에서 **커서로 거칠게 털어내기** 하나뿐이고, 그때는
  전신이 구르는 중이라 팔꿈치 16.5°의 되돌림은 눈에 띄지 않는다. **미확인으로 남기지 않고
  「알고 넘긴다」로 기록한다.**

---

## 4. 【과제 1】 세션 중 앰비언트 — 3층 구조

> 리더 지시 그대로: **포즈 유지시간을 늘리지 않는다.** 축을 바꾼다.
> 그리고 **파쿠르·낙하·걷기는 계속 허용한다** — 확률 분포만 바뀐다(원칙 1).

```
L0  기본 자세 (지속)   세션 중 Idle의 «중립»이 차렷이 아니라 **관망 자세**다.       ← 87% 를 덮는다
L1  미세 생명감 (항상)  호흡 + 아주 느린 무게중심 이동. 정지 조각상 방지.            ← 100% 를 덮는다
L2  제스처 (간헐)      4종, 실효 34초 간격. «화면이 상태를 계속 말하게» 하는 층.     ← 3.4%
```

### 4-1. L0 — 관망 자세 2종 (지속)

세션 중 `ApplyIdlePose` 자리에 **관망 자세**가 들어간다. 25분 내내 한 자세면 부자연스럽다는
지시에 대한 답은 **"덜 유지"가 아니라 "두 자세를 번갈아"** 다. 실제 감독자가 그렇게 서 있는다.

| | P1 팔짱 | P2 뒷짐 |
|---|---|---|
| 어깨각 | A −8.5° / B −37.0° | **−51.5°** (양팔, 스태거 ±4°) |
| 팔꿈치 | A 116.5° / B 113.0° | **72.5°** (양팔, 스태거 ∓6°) |
| 손 위치 | (+0.296, 1.503) / (+0.130, 1.372) | (−0.165, 1.183) 몸통비 0.299, **뒤 0.75 R** |
| 몸통 기울임 | **−4.0°** (기준) | **−2.5°** (기준) |
| 규칙 B 여유 | 1.1694 / 1.2519 | **2.6003** (여유 충분) |
| 옆모습 손실 | 있음 (3-2절) | **없음** — 뒷짐은 시상면 자세라 2D 옆모습에서 무손실 |

> ★ **P2를 넣는 진짜 이유**는 변주가 아니라 **가독성 보험**이다. 팔짱은 원리상 근사밖에 안 되지만
> (3-2절) 뒷짐은 옆모습에서 **손실이 0**이다. 팔짱이 "손 모으기"로 읽히는 사용자가 남아 있어도
> 뒷짐 구간에서는 **누구에게나** "지켜보고 있다"로 읽힌다.
> ★ 뒷짐 팔꿈치가 몸통선 뒤 **1.35 R**까지 나간다 → **망토와 겹칠 수 있다.
> `design-equipment` 교차 확인 필요(리더 라우팅 항목).** 나는 지시하지 않는다.

**전이 규약**
- 세션 시작 → `FocusStart`(2초, 무수정) 끝 → **P1로 이어받는다.** 팔이 내려갔다 다시 올라가면
  안 된다. `FocusStart` 종료 진행도의 팔 각도가 곧 P1의 각도이므로 **연속**이다.
- Walk / Jump / Fall / ParkourClimb / LandingCrouch 중에는 **관망 자세를 적용하지 않는다**
  (평소 그대로). Idle로 복귀할 때 **0.45초 이징**으로 다시 세운다.
  ★ `poseSmoothingRate = 35/s`는 95%까지 **0.086초**라 그대로 두면 팔이 **딱 하고 붙는다.**
  별도 포락선 `watchStanceSettle01 = SmoothStep(0, 1, t/0.45)` 필요.
- 세션 종료(`FocusComplete` / `FocusCancelled`) → 관망 자세 **즉시 해제**, 이후 평소 Idle.
  `FocusCancelled`가 포즈를 갖지 않는 현행 판단(“팔짱을 푸는 그림”)은 **그대로 옳다** —
  오히려 이번 변경으로 **의미가 생긴다**(풀 팔짱이 실제로 있으므로).

### 4-2. L1 — 미세 생명감 (항상)

| 항목 | 값 | 검산 |
|---|---|---|
| 상체 흔들림 | 기준 **−4.0° ± 2.0°**, 주기 **11초** | 범위 [−6.0, −2.0]° = 기울임 상한 7.60°의 **79%** |
| 머리 왕복폭 | **0.0780 유닛** | 배율 0.75에서 **2.06 pt** (반주기 5.5초에 걸쳐) |
| 어깨 미세 회전 | 양팔 θu **±1.5°**, 기울임과 **역위상** | 손끝 이동 0.020유닛 — "체중을 옮긴다"의 최소 신호 |
| 25분간 주기 수 | **136회** | 정지 구간 0 |

> **왜 다리를 안 건드리나**: 무릎/고관절을 흔들면 발끝이 지면에서 떨어진다
> (`ComputeFootGroundingOffset`은 보행 경로용이다). 접지 계약을 건드리지 않는 선에서
> 가장 큰 신호가 **엉덩이 피벗 기울임**이다.
> **왜 이렇게 작나**: 2026-08-31 사용자 신고 *"자꾸 머리를 움직이는데 목에서 벗어나서 이상함"*
> 이 있었던 자리다. 상한(7.60°)은 그 신고의 기하학적 원인을 그대로 일반화한 값이다.

### 4-3. L2 — 제스처 4종

**★ 새 타이머·새 확률·새 폴링을 하나도 만들지 않는다.** 발행자는
`AutoWanderController.TickResting`의 **기존 1회 추첨**(Idle 진입 후
`wanderLookAroundDelayMin~Max` 뒤 그 Idle 구간에 정확히 1회 + 최소 간격 쿨다운)이고,
**세션 중에는 그 쿨다운 값과 「어휘」만 바뀐다.** `IdleAmbientMotionRenderer`가 이미 세운
"조건은 전부 발행자에 있다" 규약 그대로다.

| id | 이름 | 지속 | 가중치 | 25분 노출 | 무엇이 움직이나 |
|---|---|---|---|---|---|
| **G1** | 자세 고쳐 잡기 | **1.30초** | 0.34 | 15.0회 | 팔짱/뒷짐을 **한 번 풀었다 다시** — 아래 박자표 |
| **G2** | 끄덕임 2박 | **1.20초** | 0.26 | 11.5회 | 자세 유지 + 상체 **앞으로 두 번(정점 +5.2°)** 끄덕였다 복귀 |
| **G3** | 화면 쪽 돌아보기 | **0.90초** | 0.24 | 10.6회 | 커서 쪽으로 몸을 튼다(필요시 방향 전환) |
| **G4** | 관망 자세 바꾸기 | **1.60초** | 0.16 | 7.0회 | **P1 ↔ P2 토글** — 평균 3.5분마다 |

> ★ **2026-09-07 개정 — G2가 "발밑 링 확인"에서 "끄덕임 2박"으로 바뀌었다.** 옛 링(`Interaction/FocusWatchRenderer`)이
> 2026-09-06 사용자 지시로 파일째 삭제됐는데 G2가 그 링을 향해 계속 상체를 숙이고 있었다(원칙 1 위반,
> `design-motion` 실측: 25분 세션당 11.5회 존재하지 않는 원에 절함). 새 동작은 **가리키는 좌표가 없어야**
> 한다는 기준으로 골랐다 — 어떤 렌더러가 사라져도 같은 방식으로 다시 거짓이 될 수 없다. 가중치(0.26)와
> 노출 횟수(11.5회)는 **불변**(추첨 확률 자체는 안 바뀌었다) — 지속시간만 1.00초 → 1.20초로 늘었다
> (아래 박자표 근거).

```
쿨다운 28초 + 다음 추첨까지 대기 평균 6.0초  →  실효 간격 34.0초,  총 44회 / 25분
제스처 총 54.0초 = 세션의 3.60%  (G2가 1.00→1.20초로 늘어 52초 → 54.0초)
최장 침묵 = 28(쿨다운) + 4.0(걷기 최대) + 2.5(추첨 지연 최대) ≈ 34.5초
```
> 가중치 합 = 1.00 (스크립트가 `assert`로 잠근다).
> **28초의 근거**: 평소값 `wanderLookAroundCooldownSeconds = 30`은 2026-08-31 사용자 신고
> *"너무 자주함"* 대응으로 정해진 값이다. 세션 중에는 「화면이 상태를 계속 말해야」 하므로
> 조금 당기되, **34.5초 최장 침묵**이 그 신고의 체감 하한(분당 1.8회 ≈ 33초)과 같은 자리다.
> 즉 빈도를 **평소보다 높이지 않는다** — 어휘만 바꾼다.

**박자표** (진행도 0~1. 기존 `FocusGlassesBeat01` 계열과 같은 표현):

```
G1 자세 고쳐 잡기 (1.30초)
   0.00 ~ 0.28   풀기      관망 자세 혼합비 1.00 → 0.25 (팔이 살짝 내려온다)
   0.28 ~ 0.36   정점 유지  0.25 고정                      ← 이 평평한 구간이 "고쳐 잡는다"를 만든다
   0.36 ~ 0.78   다시 접기  0.25 → 1.00
   0.78 ~ 1.00   안착      1.00 유지 + A/B **역할 교대**(어깨 스태거 부호 반전)
   ※ P2(뒷짐)일 때는 역할 교대 대신 손 스태거 부호만 반전한다.

G2 끄덕임 2박 (1.20초)  ★ 2026-09-07 개정 — 옛 "발밑 링 확인"(숙이고 0.28초 머문다)을 대체
   0.00 ~ 0.12   1박 숙이기    몸통 기울임 −4.0° → **+5.2°**
   0.12 ~ 0.27   1박 정점 유지 +5.2° 고정
   0.27 ~ 0.48   되올림        +5.2° → −4.0°(베이스로 복귀 — 「체류」가 아니라 「방향 반전」이 문법이다)
   0.48 ~ 0.62   2박 숙이기    −4.0° → +5.2°(같은 목표치)
   0.62 ~ 0.76   2박 정점 유지 +5.2° 고정
   0.76 ~ 1.00   복귀          +5.2° → −4.0°
   ★ 2026-09-07 test-engineer 실측 정정 — 이 자리에 원래 "골에서 출발해 감쇠로 덜 닿는다 — 실측 정점
     81%"라고 적혀 있었으나 **부호가 반대였다**. 출하 상수로 재현하면(C#·Python 두 독립 경로 + 이
     라운드의 세 번째 독립 재현 일치) 2박은 1박보다 작아지지 않고 오히려 **100.4%**다 — 「되올림」
     (0.27~0.48)이 baseLean까지 못 내려온 채 다음 램프가 바로 시작돼 골이 얕기 때문이다.
   ★ 정점 간격 0.587초(1.70Hz) / |최대| 4.93°는 재검증됨(그대로 유지). 다만 **1박 3.57pt / 2박
     2.88pt / "L1의 1.39배" / "1.00초에서는 1.24pt로 줄어 L1보다 작아진다"는 전부 그 잘못된 81%
     전제 위에서 나온 파생값이라 함께 신뢰 보류다** — 방향은 분명히 안전 쪽이다(2박이 L1에 묻히기는
     커녕 더 안 묻힌다), 그러나 정확한 pt 재도출과 "그래서 1.20초로 늘렸다"는 근거 서술 자체의
     재확인은 design-motion 소관(이 라운드에서 임의로 새 숫자로 덮어쓰지 않는다). `NodSeconds =
     1.20f` 값 자체를 되돌릴 근거는 아니다 — 기능은 이미 정상 동작 확인됨, 그 값을 고른 계산 과정의
     서술만 부정확했다.
   ★ +5.2°는 상한 7.60°의 68.4%(옛 +6.5°=86%보다 여유가 커졌다). 최악(L1 안 누른 채 겹침)도
     7.20° < 7.60°라 **L1 흔들림을 0으로 누르는 것이 더 이상 안전상 필수가 아니다**(그래도
     박자가 흐려지지 않도록 그대로 누른다 — 선택적 유지).
   ★ 「가리키는 좌표가 없다」가 이 동작을 고른 기준이다 — 발밑 링처럼 대상이 사라져도
     다시 거짓이 될 수 없다.

G3 화면 쪽 돌아보기 (0.90초)
   [커서가 지금 보는 쪽에 있을 때]  — 방향 전환 없음
   0.00 ~ 0.30   몸통 기울임 −4.0° → **+3.0°**(커서 쪽으로 살짝 내민다)
   0.30 ~ 0.55   유지
   0.55 ~ 1.00   복귀
   [커서가 반대쪽에 있을 때]        — 제자리 돌기
   0.00 ~ 0.22   예비동작   기울임 −4.0° → −6.0°(반대로 살짝 젖힘)
   0.22          ★ **facing 반전**(1프레임). 관망 자세는 좌우 대칭이라 각도 재계산 불필요
   0.22 ~ 0.60   따라돌기   기울임 −6.0° → +2.0°
   0.60 ~ 1.00   안착      +2.0° → −4.0°
   ※ Idle 중에는 MoveInputX = 0 이라 배회 AI가 facing을 덮지 않는다
     (`TickPoseRouting`의 `Mathf.Abs(move) > deadzone` 관문). 소유권 충돌 없음.
   ※ 커서를 못 읽으면(원격 데스크톱/권한 없음) **G3를 추첨에서 제외**하고 G1 가중치에 합친다.
     — 없는 대상을 향해 돌아보는 것은 원칙 1 위반이다.

G4 관망 자세 바꾸기 (1.60초)
   0.00 ~ 0.40   현재 자세 혼합비 1.00 → 0.00 (팔이 중립까지 내려온다)
   0.40 ~ 0.52   중립 통과 유지                      ← 여기서 P1/P2 목표를 바꾼다
   0.52 ~ 1.00   새 자세 혼합비 0.00 → 1.00
   ※ 중립을 반드시 통과시킨다. 팔짱↔뒷짐을 직접 보간하면 전완이 몸통을 관통한다.
```

### 4-4. 배회 확률 — 걷기를 **줄이되 없애지 않는다**

| 항목 | 평소 | 세션 중 | 근거 |
|---|---|---|---|
| `wanderPostIdleWalkChance` | 0.75 | **0.40** | 걷기를 남긴다 — 파쿠르/뛰어내리기/매달리기 경로 유지 |
| `wanderIdleDurationMin/Max` | 2.0 / 6.0 | **4.0 / 11.0** | 한 번 서면 더 오래 서 있는다 |
| 결과: Idle 점유율 | 66.0% | **87.2%** | |
| 결과: 걷기 총시간 | 510초 | **192초** | 25분 중 3.2분은 여전히 돌아다닌다 |

> ★ **원칙 1 관련 판단**: 이것은 "묶어두기"가 아니라 **분포 변경**이다. 파쿠르·낙하·매달리기는
> 전부 도달 가능하게 남는다(걷기 192초 = 평소 34% → 12.8%). 캐릭터가 턱에서 뛰어내리면
> 그건 여전히 진짜로 일어난 일이고, 대사도 그 상태에서 파생된다.
> ★ 마스터 스위치 `focusSessionAmbientEnabled = true` 하나를 끄면 위 4개 값이 전부 평소값으로
> 돌아가 **거동이 100% 예전과 같아진다**(이 저장소의 네거티브 컨트롤 규약).

### 4-5. 대사 — **이 층에는 붙이지 않는다** (원칙 1)

- **1순위 근거(이것만으로 충분하다)**: L2 제스처 4종은 **상태 전이가 아니다**
  (`FocusWatchTier.Glance`와 같은 순수 앰비언트 층). 원칙 1은 *"대사는 상태 전이가 확정된 뒤
  그 상태로부터만 파생한다"*이므로, 전이가 없으면 파생할 상태가 없다 → **대사 0건.**
- **2순위(수명 계약)**: 참고로 재보니 **여유가 아주 없지는 않다.**
  ★ 내 배정 브리핑의 *"최소 노출 하한 0.82초"* 는 **낡은 값이다** — 2026-09-01 개정으로
  하한이 **글자수 비례 예산** `clamp(0.28 + 글자수 × 0.075, 0.62, 2.20)초`로 바뀌었다
  (`Dialogue/DialogueKind.DialogueBudget`, `StickConfig.dialogueMinVisibleSeconds` 툴팁).
  그래서 제스처 지속 안에 들어가는 글자수 상한은 아래와 같다.

  | 제스처 | 지속 | 들어가는 최대 글자수 |
  |---|---|---|
  | G3 화면 쪽 돌아보기 | 0.90초 | **8자** |
  | G2 끄덕임 2박 | 1.20초 | 12자 (★ 2026-09-07 — 옛값 9자에서 지속시간 1.00→1.20초 개정에 따라 재계산) |
  | G1 자세 고쳐 잡기 | 1.30초 | 13자 |
  | G4 자세 바꾸기 | 1.60초 | 17자 |

  즉 **길이 때문에 막히는 것이 아니다.** 그런데도 붙이지 않는 이유는 1순위 하나뿐이다 —
  *"대사를 먼저 정하고 행동을 끼워 맞추지 않는다."* 앰비언트에 대사를 달기 시작하면
  25분에 44번 말하게 되고(≈34초마다 한 마디), 그건 상주 앱에서 **비침해 원칙 2**로도 진다.
- 세션 중 대사가 필요하면 그것은 **별도의 「세션 하트비트」 상태**여야 하고 그건
  **`design-narrative` 소관**이다. 나는 지시하지 않는다 — **리더 라우팅 항목.**

---

## 5. 「안경 밀어올리기」 박자 — 진단과 개정

### 5-1. 진단 — 안경 유무가 문제가 아니다

현행 `TrySolveFocusGlassesAngles`의 목표점 = `(팔길이 × 0.30, 머리 중심 y)` = **(0.2250, 2.0547)**.
머리 중심에서 거리 0.2250, 머리 시각 반경 0.220 → **바깥으로 0.0050유닛**.

| 배율 | 0.35 | 0.60 | **0.75 (출하)** | 1.00 |
|---|---|---|---|---|
| 손끝 돌출(pt) | +0.06 | +0.11 | **+0.13** | +0.18 |

머리는 **불투명**이고 팔(sortingOrder 2)보다 **위**에 그려진다(HeadFill 3 / HeadOutline 4).
즉 **손끝이 화면에 사실상 안 나오고 전완이 머리에 처박힌 그림**이 된다.
이건 `MOTION_SPEC.md` 13절이 손차양 122°에서 잡아낸 것과 **완전히 같은 병의 재발**이다.

부수 피해: 이 IK가 유도하는 팔꿈치는 **121.4°** 라 규칙 B 여유가 **1.0588** — 다리 병목
1.1682보다 낮다. **즉 지금 저장소의 전체 최악 크리즈 여유를 만드는 것은 이 박자다.**
그런데 `LimbCurveGeometryTests`는 `StickConfig` 필드만 훑으므로 **이 값을 본 적이 없다.**

### 5-2. 개정 — 관자놀이 접촉 (`MOTION_SPEC.md` 13-5와 같은 잣대)

손끝이 머리 실루엣 밖으로 나온 양을 배율 1.0에서 **2.2pt**로 맞춘다(13-5가 98°에서 얻은 값).
목표 방향은 머리 중심 기준 **+y에서 앞으로 75°** = 관자놀이.

```
목표점 = (0.2728, 2.1278)   머리 중심에서 0.2824 = 반경 + 0.0624
유도 각도: 어깨 91.3° / 팔꿈치 105.5°
규칙 B 여유 1.0588 → **1.4413**  (다리 병목 위로 복귀 = 전체 최악이 다시 다리가 된다)
유휴 '손차양'(107° / 98°)과의 차 = 어깨 15.7° / 팔꿈치 7.5°  → **구분됨**
```

| 배율 | 0.35 | 0.60 | **0.75** | 1.00 |
|---|---|---|---|---|
| 손끝 돌출(pt) | +0.77 | +1.32 | **+1.65** | +2.20 |

**파라미터 형태(현행 규약 유지 — 둘 다 팔 길이 비율이라 배율/기하 변화에 자동 추종):**
```
FocusGlassesHandForwardRatio  0.30 → 0.3637
FocusGlassesHandRiseRatio     (신설) = 0.0975     손끝 y = 머리 중심 + 팔 길이 × 이 값
```
★ 현행은 y를 `_headNeutral.y`로 **고정**하고 x만 비율로 준다. 그래서 "머리 중심 높이"에
못박혀 있고 손이 절대 위로 못 올라간다. **y도 비율로 만들어야** 이 개정이 성립한다.

### 5-3. 안경이 없을 때 — 답: **그대로 써도 된다**

- 안경 미해금 상태에서 이 동작은 **"관자놀이를 짚는다"** 로 읽힌다. 집중/생각의 관용 제스처이고
  집중 모드 톤과 어긋나지 않는다. **없는 물건을 만지는 그림이 아니다** — 손이 얼굴 옆에서 멈춘다.
- 반면 **현행 각도로는 안경이 있든 없든 손이 안 보인다.** 즉 이 항목의 실제 결함은
  "안경이 없다"가 아니라 "손이 없다"였다.
- 안경을 **쓴** 상태의 겹침(안경 조형의 실제 외곽 반경)은 **재지 않았다 — 미확인.**
  `design-equipment` 교차 확인 항목이다(리더 라우팅). 다만 개정안은 어느 경우에도 현행보다
  낫다(손끝 돌출 0.13pt → 1.65pt).

---

## 6. ★ 별건 — 크리즈 감사 구멍 2건 (지금 배정 안 해도 기록은 남겨라)

`LimbCurveGeometryTests.CollectBendAngles`는 **`StickConfig`의 public float 중 이름이
`Degrees`로 끝나고 `Elbow`/`Knee`를 포함한 필드**만 모은다. 그런데 집중 모드가 실제로 만드는
팔꿈치 각도는 둘 다 그 밖에 있다.

| 실제 각도 | 어디에 있나 | 규칙 B 여유 | 검사됨? |
|---|---|---|---|
| 팔짱 **115°** | `StickmanPoseAnimator.FocusCrossElbowDegrees(104) + Stagger(11)` — `private const` | 1.2044 | **아니오** |
| 안경 **121.4°** | `TrySolveFocusGlassesAngles`의 **IK 결과** — 상수가 아예 없다 | **1.0588** | **아니오** |
| (참고) 검사되는 최대 | `MaxJointBendDegrees = 100` | 1.4808+ | 예 |

**즉 이 테스트가 「팔꿈치 최대 100°」라고 믿는 동안 프로덕션은 121.4°를 그리고 있었다.**
춤 7종도 같은 형태일 가능성이 있다(**미확인 — 확인 안 했다**).

제안(내 담당 아님, `test-engineer` 인계 항목):
- 각도 수집을 **StickConfig 리플렉션**에서 **"포즈 API를 실제로 돌려 적용된 관절 각도를 읽는"**
  방식으로 바꾼다. 그러면 상수든 IK든 새 포즈든 자동으로 잡힌다.
- 최소한 이번 라운드에는 팔짱/안경의 확정 각도(116.5 / 105.5)를 **상수 참조로** 검사에 넣는다.
  **숫자를 테스트에 베끼지 마라**(CLAUDE.md 규약).

---

## 7. coder 인계 — 정확히 어디를 만지는가

> 아래는 **위치 지정**이지 지시가 아니다. 배정은 리더가 한다.
> `States/*`는 다른 팀이 만지는 중일 수 있으므로 **리더가 파일을 갈라 준 뒤에** 착수한다.

| # | 파일 | 지점 | 무엇을 |
|---|---|---|---|
| 1 | `States/StickmanPoseAnimator.cs` | `:921` `FocusCrossArmUpperDegrees` | −14 → **−22.75** |
| 2 | ″ | `:924` `FocusCrossElbowDegrees` | 104 → **114.75** |
| 3 | ″ | `:929` `FocusCrossArmStaggerDegrees` | **상수 1개 → 2개로 분리**: 어깨 ±14.25 / 팔꿈치 ∓1.75 |
| 4 | ″ | `:1037-1039` | 스태거 **부호 반전** (`NeutralSign>0`이 앞으로 온다). 어깨·팔꿈치를 **각자의** 스태거로 |
| 5 | ″ | `:945` `FocusGlassesHandForwardRatio` | 0.30 → **0.3637** |
| 6 | ″ | `:1065` `TrySolveFocusGlassesAngles`의 target | y를 `faceY` 고정 → `faceY + reach × 0.0975` (**신설 상수**) |
| 7 | ″ | 신규 `ApplyFocusWatchStancePose(...)` | L0/L1/L2 — P1/P2 + 흔들림 + 제스처 4종 |
| 8 | `States/StickmanBlackboard.cs` | `:2203-2213` | 세션 활성 + Idle이면 `ApplyIdlePose` **앞에서** 관망 자세 분기 |
| 9 | ″ | `:2245 BeginIdleAmbientMotion` | 세션 중이면 **집중 어휘 4종**을 받도록 열거 확장 |
| 10 | `States/AutoWanderController.cs` | `:250-259` | 세션 중 쿨다운 **28초** + 어휘 4종 가중 추첨 |
| 11 | ″ | `:274-308 ResolvePostIdleBranch` | 세션 중 `walkChance` **0.40**, Idle 길이 4.0~11.0 |
| 12 | `Core/StickmanEventBus.cs` | `:459 WanderAmbientMotion` | 열거값 4개 추가 (`FocusRecross` / `FocusNod`(★2026-09-07 개칭, 원래 `FocusRingCheck`) / `FocusScreenGlance` / `FocusStanceSwap`) |
| 13 | `Core/StickConfig.cs` | 포모도로 헤더 뒤 | 신규 필드 6개 (아래) |
| 14 | `Interaction/FocusWatchDirector.cs` | — | **읽기 전용 조회만 추가** (`IsSessionActive`는 이미 public). 타이머 로직 **무수정** |

**신규 `StickConfig` 필드 (기존 명명 관례 준수)**
```
focusSessionAmbientEnabled              = true    ★ 마스터 스위치(끄면 100% 예전 거동)
focusAmbientGestureCooldownSeconds      = 28f
focusSessionWalkChance                  = 0.40f
focusSessionIdleDurationMin             = 4.0f
focusSessionIdleDurationMax             = 11.0f
focusWatchStanceSettleSeconds           = 0.45f
```
> 자세 각도는 **StickConfig에 넣지 않는다** — 팔짱 4개 값은 서로 정합성을 갖는 **하나의 실루엣**이라
> 개별로 만지면 그림이 깨진다(`StickmanPoseAnimator`의 보행 키표·등반 상수와 같은 판단).
> 대신 **6절의 감사 구멍**을 함께 닫아야 그 상수가 크리즈 검사에 보인다.

**상태 소유권 (`design-motion` → `coder` 인계 계약)**
- 관망 자세는 **Idle의 포즈만** 바꾼다. **수평 이동은 전혀 소유하지 않는다** —
  `MoveInputX`는 배회 AI가 계속 소유한다. 제스처 중에도 위치는 안 움직인다(G3의 facing 반전 제외).
- `SpectacleEventLock`을 **잡지 않는다.** L0~L2는 상태 전이가 아니라 Idle 위의 포즈 층이다
  (`FocusWatchTier.Glance`가 락과 무관한 것과 같은 판단). 락을 잡으면 세션 내내 다른 모든
  연출이 조용히 막힌다.

---

## 8. 검증 기준 (test-engineer 인계 — **벽시계 초 기준**)

| # | 무엇 | 방법 | 합격선 |
|---|---|---|---|
| V1 | 팔짱 부호 | 팔짱 완성 프레임에서 두 손의 x·y를 실측 | **위손x − 아래손x > 0** 이고 **최전방 손 몸통비 ≥ 0.65** |
| V2 | 크리즈 | 신규 팔꿈치 116.5/113.0/105.5를 규칙 B 검사에 넣는다 | 전 배율 여유 **≥ 1.1682**(= 다리 병목 무변화) |
| V3 | 안경 손끝 | 손끝과 머리 중심 거리를 실측 | 배율 0.35/0.60/0.75/1.00 **전부** 머리 반경 **밖** |
| V4 | 기울임 상한 | 세션 25분 시뮬 중 `BodyLeanDegrees` 최댓값 | **≤ 7.60°** (G2 구간에서 L1이 실제로 눌리는지) |
| V5 | 침묵 구간 | **벽시계 300초** 세션에서 제스처 발동 시각 기록 | 연속 무발동 구간 **≤ 40초** |
| V6 | 걷기 생존 | 같은 300초에서 Walk 진입 횟수 | **> 0** (파쿠르 경로가 안 막혔다) |
| V7 | 네거티브 컨트롤 | `focusSessionAmbientEnabled = false` | Idle 포즈가 세션 밖과 **완전 동일** |
| V8 | 원칙 1 | 세션 중 발행된 `DialogueIntent` 전수 | L2 제스처(상태 전이 아님)에서 파생된 대사 **0건** |
| V9 | 팔 배정 | `NeutralSign>0` 팔의 최종 어깨각 | **−8.5°**(A) — 반대로 들어가면 전이 이동량이 2.1배 |

> ★ V5는 반드시 **벽시계**로 재라. 이 저장소 배치모드 PlayMode는 8,000fps 이상으로 돌아서
> 프레임 수 예산은 밀리초가 된다(CLAUDE.md).
> ★ **최종 판정은 실제 빌드 연속 프레임 캡처로만.** 단일 프레임으로는 "튄다"를 못 본다.
> 특히 G4(자세 토글)의 중립 통과 구간은 **반드시 연속 캡처로** 봐야 한다.

---

## 9. 플랫폼 영향

**Windows 영향: 없음.**
이 문서가 지목한 파일은 `States/StickmanPoseAnimator.cs` · `States/StickmanBlackboard.cs` ·
`States/AutoWanderController.cs` · `Core/StickConfig.cs` · `Core/StickmanEventBus.cs`로 **전부
플랫폼 중립**이다. `#if UNITY_STANDALONE_*` 분기가 하나도 없고 `Platform/` 아래를 건드리지 않는다.

**macOS 영향: 없음(같은 이유).**

단, **G3(화면 쪽 돌아보기)가 읽는 커서 좌표는 플랫폼 경로다** —
`StickmanAgent.TryGetCursorPosition`은 이미 `FocusWatchDirector.TickMouseSignal`이
세션 중 매 프레임 쓰고 있으므로 **신규 배관 0줄**이지만, **커서를 못 읽는 환경에서는
G3를 추첨에서 빼야 한다**(4-3절에 명시). 이 폴백이 없으면 "없는 화면 쪽으로 돌아보는" 그림이
나오고 그건 원칙 1 위반이다. **Windows에서 커서 조회가 실패하는 조건은 재지 않았다 — 미확인.**

**모바일(iPad/iPhone) 영향: 미확인.** 스크린샷 백드롭 모드에는 커서가 없으므로 G3가
구조적으로 도달 불가일 가능성이 높다. `dev-platform` 확인 항목(리더 라우팅).

---

## 10. 미확인으로 남긴 것 (정직하게)

1. **실제 빌드 캡처를 못 했다.** 앱 실행/빌드는 리더 사전 통보가 필요해 이번 라운드에서 하지
   않았다. 위 수치는 전부 **프리팹 기하 재유도 + 렌더러 식 재현**이다(전신 높이가 프로덕션
   상수와 소수 7자리까지 일치하는 것으로 재현의 정합성만 확인했다).
   **8절 V1~V4는 캡처로 다시 봐야 확정이다.**
2. **안경 조형의 실제 외곽 반경을 안 쟀다.** 5-3절.
3. **뒷짐(P2)과 망토의 겹침을 안 쟀다.** 팔꿈치가 몸통선 뒤 1.35 R까지 나간다. 4-1절.
4. **춤 7종에도 같은 감사 구멍이 있는지 확인 안 했다.** 6절.
5. **P1/P2 중 어느 쪽이 사용자에게 더 "지켜본다"로 읽히는지는 실측이 없다.** 그래서 둘을
   **번갈아** 쓰는 안을 택했다 — 한쪽에 전부 걸지 않는 설계다. 페르소나 검증으로 갈라야 한다.
6. **`pomodoroGraceSeconds = 120`(유예 2분) 동안의 화면**은 이번 설계 대상에 포함되지만
   **에스컬레이션 3단계와 관망 자세가 겹칠 때의 우선순위**는 안 정했다. `FocusNudge`는 상태
   전이라 락을 잡으므로 자동으로 이기지만, 1단계 `Glance`(순수 앰비언트)와 L2가 같은 프레임에
   나면 어떻게 되는지 **미확인**. `game-architect` 확인 항목.

---

## 11. 교차 레이어 영향 (`Tasklist.md`는 리더 소유라 여기 적는다)

| 상대 | 무엇이 맞물리나 |
|---|---|
| `design-narrative` | **L2 제스처에는 대사를 붙이지 않는다**(4-5절). 세션 중 대사가 필요하면 별도 상태여야 한다 |
| `design-equipment` | 뒷짐 팔꿈치 ↔ 망토(4-1절), 안경 외곽 ↔ 손끝 접촉점(5-3절) |
| `design-character` | 팔 길이가 **4.4%만 줄어도** 규칙 B 여유가 무너진다(`MOTION_SPEC.md` 13-7 표). 116.5°는 그 표를 다시 좁힌다 |
| `test-engineer` | 크리즈 감사 구멍 2건(6절) + 검증 8건(8절) |
| `ux-designer` | ★ 2026-09-07 갱신 — 발밑 링(`FocusWatchRenderer`)은 2026-09-06 삭제됐고, G2도 "링을 내려다본다"에서 **"끄덕임 2박"**(가리키는 좌표 없음)으로 대체돼 더 이상 링에 의존하지 않는다. 향후 링 계열 UI를 다시 넣더라도 G2와 재결합할 필요 없음 |
| `coder` | 7절 전체. 특히 **수평 이동 소유권은 배회 AI에 그대로 둔다** |
| `qa-regression` | V7(네거티브 컨트롤)이 회귀 기준선이다 |
