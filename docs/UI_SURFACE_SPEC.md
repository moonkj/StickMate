# StickMate UI 표면 조형 스펙 — 창·패널·메뉴·아이콘·레터링

작성: ux-designer / 2026-09-01
범위: **장비 30종 + 머리 6종을 제외한** 화면 위의 모든 것
 (창 2종 · 팝오버 3종 · 포스트잇 · 부채꼴 메뉴 · 톱니 · 만화 레터링 · 토큰 체계)
장비/머리 조형은 `docs/EQUIPMENT_SHAPE_SPEC.md`가 관할한다. 이 문서는 그것을 **다시 다루지 않는다.**

관측 대상: 2026-09-01 17:17 빌드(PID 11451, 20:05~20:14 실측). 저녁에 들어간 수정
(감쇠 그림자 / `FontDisplay 19→20·FontTitle 13→14·FontLabel 11→12` / 단축키 `,`→`P`)은
**이 빌드에 없다**. 그 셋에서 유래한 현상은 아래에서 "이미 고쳐졌을 수 있음"으로 표시했다.

측정 근거 캡처
`scratchpad/uxq/info_half.png`(정보창 [외형]) · `inv_half.png`(정보창 [보관함]) ·
`set2_half.png`(설정창 [일반]) · `crop_pet_z.png`(카드 4장 확대) · `crop_list_z.png`(보관함 행 확대) ·
`crop_set_z.png`(비활성 행 확대) / `scratchpad/minji/action_popover.png`(행동 창) ·
`hov_megaphone.png`(부채꼴 호버) · `gear_on_bright.png`(톱니 확대)

---

## 0. 한 줄 요약

> **토큰은 있는데 규약이 없다.**
> `UiChrome`은 색·여백·글자 크기를 한 곳에 못박아 뒀지만, 여섯 개 표면이 그 토큰을
> **각자 다르게 해석하거나 아예 안 쓴다**. 그래서 화면이 "한 앱"으로 안 읽힌다.
> 아래는 그 진술을 숫자로 증명하고, 숫자로 고친다.

### 실측 요약표 (전부 이 라운드에서 직접 잰 값)

| 무엇 | 있어야 할 것 | 실제 | 근거 |
|---|---|---|---|
| 표면 안쪽 여백 | 토큰 5개(4/8/12/16/24) 중 하나 | **7가지 값** 8·10·11·14·16·20·22 (7파일) | §1.1 |
| 실제 쓰이는 타입 스타일 | 7~9종 | **명목 22종 / 실효 17종** (38콜사이트가 이름만 다르고 출력 동일) | §2.1 |
| WCAG AA(4.5:1) 미달 글자 | 0곳 | **6곳 확정 + 2곳 경계** | §2.3 |
| 섹션 제목 스타일 | 1가지 | **3표면 3가지** (색·굵기·위치 전부 다름) | §1.2 |
| 정보창 [외형] 탭 죽은 공간 | 0 | **176pt** (창 높이 861의 20.4%) | §3.1 |
| 톱니의 보장 대비(모든 배경) | ≥ 3:1 | **1.00:1** | §5.1 |
| 톱니 작은 기어 이 높이 | ≥ 1.5 획 | **−0.01 획** (톱니가 물리적으로 없다) | §5.2 |
| 포스트잇 표면 알파 | 1.0 (2026-08-31 확정 규약) | **0.92** — 데스크톱 8% 비침 | §4 |
| 아이콘 획 두께 | 1가지 | **4가지** 1.70/1.80/1.87/2.00, 단조 법칙으로 설명 불가 | §6.1 |
| 대사 기울기의 입력 | (대사, 발화 회차) | **대사 문자열만** — "가뿐하네" 3회 전부 8.2도 | §7.1 |

---

## 1. 표면 공통 — 토큰이 지켜지지 않는다

### 1.1 여백: 토큰 5개, 실사용 7가지

`UiChrome.Space1..6 = 4 / 8 / 12 / 16 / 24`. 실제 표면 안쪽 여백:

| 표면 | 안쪽 여백 | 토큰인가 | 출처 |
|---|---|---|---|
| 포스트잇 | **8** | ✔ Space2 | `TodoPostItWidget.PanelPadding` |
| 행동 팝오버 카드 | **10** | ✘ | `ActionCommandPopover.CardPadding` |
| 정보창 카드 | **11** | ✘ | `CharacterInfoWindow.ThumbX` |
| 설정창 카드 | **14** | ✘ | `SettingsControls.CardPadX` |
| 팝오버 3종 바깥 | **16** | ✔ Space4 | `PopoverPanel` |
| 정보창 좌열 / 설정창 본문 | **20** | ✘ | `LeftPadX` / `ContentPadX` |
| 정보창 우열 | **22** | ✘ | `RightPadX` |

카드 **사이** 간격도 5가지: 3(보관함 행) / 4(투두 행) / 9(정보창 카드) / 12(행동 팝오버 그룹) / 14(설정창 카드).

`PlaceTopLeft`/`PlaceTopRight` 호출 105곳에 들어간 생 숫자를 전수로 뽑으면 6·7·10·12·14·15·16·18·20·22·24·25·30·32·34·38·48·51·52·60·66·90·120·122 — **최소 24가지**. 토큰 참조는 전 파일 합계 37줄뿐이고, `GearRadialMenuWidget`·`TodoPostItWidget`은 **0줄**이다.

**처방 (P2-13)**

```
Space1=4  Space2=8  Space3=12  Space4=16  Space5=20  Space6=24   // Space5 신설
```
- 5를 새로 넣는 이유: 20이 이미 8곳에서 쓰이는 **de-facto 토큰**이다. 없애는 것보다 승인하는 쪽이 싸다.
- 이관표: `22→20` · `14→12` · `11→12` · `10→12` · `9→8` · `3→4`.
- 정보창 카드는 11→12로 커지면 썸네일 폭이 119→117이 된다(카드 141 − 12×2). 아이콘 44pt는 그대로 들어간다(117 > 44). **검산 완료.**
- 못 박는 법: `Tests/EditMode/UiSpacingTokenAuditTests` — `UiGlyphExactnessAuditTests`와 **같은 방식**으로 소스를 훑어 `Place*(` 인자에 토큰이 아닌 간격 리터럴이 새로 들어오면 실패. 이미 있는 갭은 `Assert.Ignore(사유)`로 남겨 러너에 계속 보이게 한다(CLAUDE.md 규약).

### 1.2 "섹션 제목"이 세 표면에서 세 가지다

같은 물건(= 관련된 행 몇 개의 묶음 이름)인데:

| 표면 | 크기·굵기 | 색 | 대비 | 위치 | 장식 |
|---|---|---|---|---|---|
| 정보창 | 12 **Bold** | TextPrimary | 15.0 | 카드 **밖** | 도트7 + 코드 + 구분선 + 카운트 |
| 설정창 | 12 **Bold** | **Accent** | 6.20 | 카드 **안** | 없음 |
| 행동 팝오버 | 12 Reg | TextSecondary | 7.93 | 카드 **안** | 없음 |

색 3가지 · 굵기 2가지 · 위치 2가지. 이게 "표면마다 제각각"의 가장 짧은 증거다.

설정창 건은 더 나쁘다 — **Accent(파랑)를 정적 라벨에 쓰고 있고**, 바로 아래 토글의 "켜짐"도 같은 파랑이다. 캡처 `set2_half.png`에서 "화면 위 UI"(파란 글자) 밑에 파란 토글 두 개가 붙어 있다. **강조색 하나뿐인 팔레트에서 강조색을 비대화형 텍스트에 쓰면 그 색은 아무 뜻도 없어진다.**

**처방 (P1-6)**: 세 곳 모두 **T6 Eyebrow = 12pt Bold TextSecondary**(§2.2). Accent는 **상호작용/선택 상태 전용**으로 되돌린다. 정보창의 도트·코드·구분선·카운트 장식은 유지(맥락이 풍부한 자리다) — 바꾸는 것은 *제목의 토큰* 하나뿐, 호출부 3줄.

### 1.3 고도(그림자)가 두 개인데 근거가 없다

| 표면 | spread / offset | 앰비언트 도달 | 비고 |
|---|---|---|---|
| 정보창 · 설정창 | 22 / −7 | 41.8pt | 서로 배타 모달 |
| 팝오버 3종 | **6 / −2** | 11.4pt | 부채꼴과 **공존**한다 |
| 부채꼴 버튼 | 3 / — | — | |

6과 22는 유도된 값이 아니라 두 개의 매직 넘버다. 팝오버는 480×560pt까지 커지는데 6pt 감쇠 그림자는 밝은 데스크톱 위에서 사실상 분리를 못 만든다(감쇠 그림자는 코어를 뺀 램프 폭이 곧 spread다).

**처방 (P2-15)**: 고도 토큰 3단으로 명시.
```
ElevationChip    = (spread  3, offset −1)   // 부채꼴 버튼      (현행 유지)
ElevationPopover = (spread 12, offset −4)   // 팝오버 3종       (6/−2 → 2배)
ElevationWindow  = (spread 22, offset −7)   // 정보창·설정창    (현행 유지)
```
검산: offset/spread = 0.33 / 0.33 / 0.32 → 셋 다 `MaxShadowOffsetToSpreadRatio 0.6` 이하 ✔.
비율 1 : 4 : 7.3 — z 관계(칩 < 팝오버, 창은 단독)와 일치.
**단, 계산만으로 확정하지 말 것 — 밝은 배경 1회 육안 검증 필요.**

### 1.4 팝오버 3종의 크기가 가족이 아니다

| 팝오버 | 폭 × 높이 | 가로세로비 |
|---|---|---|
| 집중 | 244 × 252(대기) / 224(진행) | 0.97 / 1.09 |
| 오늘 할일 | 300 × 336 | 0.89 |
| 행동 | **480 × 560** | 0.86 |

같은 부채꼴에서, 같은 앵커에, 같은 크롬으로 열리는 세 창의 폭이 **2배 차이**다. 폭 244는 8의 배수도 아니다.
행동 창은 1512×982pt 화면에서 **가로 32% × 세로 57%** — 데스크톱 펫의 팝오버가 아니라 애플리케이션 모달의 덩치다.

**처방 (P2-15b)**: 폭을 3계단으로 고정 — **240 / 320 / 480**. 높이는 내용이 정하되 **8의 배수**로 스냅.
- 집중 244→240(−4): `ContentWidth` 212→208. 링 Ø84 + 좌우 62pt 여백 → 그대로 들어감 ✔
- 투두 300→320(+20): 행 폭 242→262, 라벨 폭 162→182 — **여유가 늘어난다** ✔
- 행동 480 유지, 다만 §3.3에서 높이를 560→**464**로 줄인다.

---

## 2. 타이포그래피 — 짝수 4계단 위에서 위계가 성립하는가

### 2.1 결론: **지금은 성립하지 않는다.** (핵심 리뷰 항목)

`FontLabel`을 11→12로 올리면서 `FontBody`와 **완전히 같아졌다**. 소스 전수 조사(크기 × 색 × 굵기):

```
명목 스타일 22종 → 실효 17종
```

이름만 다르고 **화면 출력이 완전히 같은** 짝 (총 38 콜사이트):

| 조합 | FontBody | FontLabel | 합계 |
|---|---|---|---|
| 12 Reg TextSecondary | 10 | 6 | **16** |
| 12 Reg TextPrimary | 7 | 1 | **8** |
| 12 Reg TextTertiary | 5 | 4 | **9** |
| 12 Bold TextPrimary | 1 | 2 | **3** |
| 12 Bold OnAccentSolid | 1 | 1 | **2** |

`UiChrome`의 주석은 *"둘의 구분은 이제 크기가 아니라 색과 굵기가 진다"* 고 약속했지만,
**그 약속을 지키는 코드가 한 줄도 없다.** 두 이름은 같은 색·같은 굵기로 나란히 쓰인다.
호출부의 "의도"를 남기려던 조치가 결과적으로 **의도 없는 이름 두 개**를 남겼다.

### 2.1-b ★ 그리고 "굵기"는 Windows에서 애초에 못 쓴다 (신규 발견 · Windows 리스크)

이 앱에는 **폰트 파이프라인이 두 벌**이다.

| | 대사(만화 레터링) | 창 UI 전부 |
|---|---|---|
| 폰트 | `ResolveKoreanFont()` — AppleSDGothicNeo-Heavy/ExtraBold/… 순회 | `Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf")` |
| 한글 글리프 검증 | `CanRenderKorean()` **실측** | **없음** |
| 굵기 | **진짜 Bold 페이스**를 잡고 합성 볼드를 끈다 | **합성 볼드**(`FontStyle.Bold`) |
| 실패 시 로그 | `[말풍선] 한글 폰트 확정: '...'` | **없음** |

`DialogueBubbleRenderer`는 *"합성 볼드가 16px 한글을 뭉갠다"*(자기 주석)를 이유로 진짜 Bold 페이스를 찾는데,
**창 UI는 그 교훈을 하나도 안 받았다.**

| 환경 | 12pt Bold의 물리 픽셀 | 14pt Bold | 판정 |
|---|---|---|---|
| macOS Retina (배율 2) | 24px | 28px | 안전 |
| Windows 150% (배율 1.5) | 18px | 21px | 경계 |
| **Windows 100% (배율 1)** | **12px** | **14px** | **16px 미만 — 대사 코드가 "뭉갠다"고 못 박은 구간** |

즉 **"색과 굵기로 위계를 진다"는 전제가 Windows 100%에서 무너진다.** 남는 것은 색뿐이고, 색만으로는
같은 12pt의 라벨/본문을 구분할 수 없다.

`LegacyRuntime.ttf`(Liberation Sans)에는 **한글 글리프가 없다.** 지금 화면에 한글이 나오는 것은
Unity 다이내믹 폰트의 OS 폴백 덕이고, **그 폴백 페이스가 무엇인지 macOS와 Windows에서 같다는 보장이 없다.**
→ **대사와 UI가 서로 다른 글꼴로 나오고 있을 가능성이 높다.** (macOS 실기에서 UI/대사 두부 없음은 확인,
**Windows는 미확인** — 이 머신에 Windows가 없다.)

**처방 (P2-12)** — 두 단계로 쪼갠다.
1. **즉시(저비용)**: `UiChrome.Font` 게터를 `DialogueBubbleRenderer.ResolveKoreanFont`와 **같은 후보 목록**으로 교체하되 *Regular 계열 우선*으로 뒤집고, 확정된 face 이름을 `[UI폰트] 확정: '...'`로 **1회 로그**한다. 로그가 없으면 Windows 회귀를 영원히 못 본다.
2. **후속**: 진짜 Bold face를 같이 잡아 두고 `AddText(bold:true)`가 **합성 볼드 대신 그 face로 교체**되게 한다. 못 잡은 환경에서만 합성 볼드로 떨어진다(대사 쪽과 완전히 같은 구조).
- 검증: Windows 실기에서 100%/150% 두 배율로 12pt Bold 한글 스크린샷 1장씩. **이건 내가 못 한다 — 리더 배정 필요.**

### 2.2 처방: 타입 스타일 9종 (이름 붙은 표. "폰트 상수"가 아니다)

| # | 이름 | pt | 굵기 | 색 | 대비 패널/카드 | 쓰는 곳 |
|---|---|---|---|---|---|---|
| T1 | `Display` | 20 | Bold | TextPrimary | 16.30 / 15.00 | 캐릭터 이름 (창당 1개) |
| T2 | `Title` | 14 | Bold | TextPrimary | 16.30 / 15.00 | 창 제목 · 활성 탭 · 상세 이름 |
| T3 | `TitleMuted` | 14 | Reg | TextTertiary | 5.79 / 5.33 | 비활성 탭 |
| T4 | `Body` | 12 | Reg | TextPrimary | 16.30 / 15.00 | 항목 이름 · 설정 행 라벨 |
| T5 | `BodyMuted` | 12 | Reg | TextSecondary | 8.62 / 7.93 | 부연 문장 · 값 · 비활성 행 제목 |
| T6 | `Eyebrow` | 12 | **Bold** | TextSecondary | 8.62 / 7.93 | **섹션/그룹 제목 — 3표면 공통** |
| T7 | `Caption` | 10 | Reg | TextTertiary | 5.79 / 5.33 | 캡션 · 설명 · 메타 · 카운트 |
| T8 | `OnSolid` | 12 | Bold | OnAccentSolid | (밝은 채움 위) | 강조 채움 위 글자 |
| T9 | `Interactive` | 12 | Bold | Accent | 6.74 / 6.20 | **누를 수 있는 글자만** |

- **`FontLabel` 상수는 지운다.** 26곳이 T4/T5/T6 중 하나로 간다. 이름이 의도를 담는 것은
  `FontLabel` 같은 *크기* 이름이 아니라 `Eyebrow` 같은 *역할* 이름일 때만 가능하다.
- 굵기가 Windows 100%에서 약해지는 만큼(§2.1-b), **T6의 두 번째 단서는 위치**다:
  Eyebrow는 항상 그룹의 **첫 줄 단독**으로 놓고, 아래에 4pt(Space1) 간격 뒤 첫 행이 온다.
- 자간(tracking)은 **쓰지 않는다** — 레거시 uGUI `Text`에 문자 간격 속성이 없다. 없는 기능을 스펙에 적지 않는다.
- 미사용 짝수 계단 **16 / 18은 일부러 비워 둔다.** 4계단이면 충분하고, 계단을 늘리는 것이 위계를 만들지는 않는다.

### 2.3 잉크 6단 → 4단 (WCAG 미달 6곳 제거)

| 토큰 | 패널 대비 | 카드 대비 | 콜사이트 | 판정 |
|---|---|---|---|---|
| TextPrimary | 16.30 | 15.00 | 20 | ✔ |
| TextSecondary | 8.62 | 7.93 | 23 | ✔ |
| TextTertiary | 5.79 | 5.33 | 24 | ✔ |
| **TabInactive** | 4.52 | 4.16 | 2 | **경계** (패널 위 4.52는 턱걸이, 카드 위는 미달) |
| **TextQuaternary** | **3.80** | **3.50** | **6** | **AA 미달 (4.5 필요)** |
| **TextDisabled** | **2.28** | **2.09** | 3 | **글자에 쓰면 안 되는 값** |

`TabInactive(4.52)`와 `TextQuaternary(3.80)`는 **서로 19%밖에 차이가 안 난다** — 위계를 만들지 못하면서
가독성만 갉아먹는 두 단계다.

**처방 (P2-11)**
```
TextQuaternary → 폐기, 전 콜사이트 TextTertiary 로            (3.50 → 5.33, AA 통과)
TabInactive    → 폐기, 비활성 탭은 T3(TextTertiary)로         (4.16 → 5.33)
TextDisabled   → 글자 금지. 잠긴 카드의 테두리/기호 전용으로 격하
  · [잠김] 버튼 라벨: TextDisabled(2.25) → TextTertiary(5.73 on CardSurfaceMuted)
```
결과 잉크 4단: **15.0 → 7.9 → 5.3 → (2.1 비텍스트)**. 배수 1.9× / 1.5× — 균일하고 전부 AA 통과.

### 2.4 비활성 행의 위계가 뒤집혀 있다 (설정창, 실측)

`crop_set_z.png` — "숨기기 / 보이기 단축키 ⌃⌥⌘V" 행.

| | 색 | 카드 위 대비 |
|---|---|---|
| 제목 (기능 이름) | TextDisabled | **2.09** |
| 설명 ("준비 중 — 다음 업데이트에서 켜집니다") | TextTertiary | **5.33** |

**설명이 제목보다 2.5배 더 잘 보인다.** 그리고 제목이 2.09라 사실상 안 읽힌다 — 유저는 *"뭔가가 준비 중이구나"*
는 읽는데 *"뭐가?"* 는 못 읽는다.

**처방 (P1-7) — 비활성 규칙 (앱 전역)**
> 비활성은 **한 단만** 내린다. **행 안의 상대 순서는 절대 뒤집지 않는다.**
> - 활성 행 : 제목 T4(15.0) / 설명 T7(5.33) → 비 2.8:1
> - 비활성 행 : 제목 **T5(7.93)** / 설명 T7(5.33) 그대로 → 비 1.5:1 ✔ 순서 보존
> - "못 쓴다"는 사실은 **컨트롤**(회색 토글)과 **행 바탕**(`CardSurfaceMuted`)이 말한다. 글자가 말하지 않는다.
> - 이유 문장(설명)은 **비활성 행에서 가장 중요한 글자다.** 절대 흐리지 않는다.

---

## 3. 창 두 개

### 3.1 ★ 정보창 [외형] 탭 — 176pt(창의 20.4%)가 비어 있다

**원인 (코드에서 확정)**
```
SectionCount = 4            // 상한을 '고정 예산'으로 써 버렸다
SectionStep  = 156
섹션 영역 = SectionsTopY(-72) ~ DetailY(-696) = 624 = 156 × 4
[장비] 섹션 4개 → 624 다 씀
[외형] 섹션 3개 → 468만 씀  →  남는 156 + 섹션 하단 슬랙 20 = 176pt 공백
```
**검산**: `info_half.png`에서 마지막 펫 카드 하단 y=620, 상세 패널 상단 y=795 → **175px**.
(캡처는 1512px = 1512pt 등가) 예측 176 vs 실측 175. **일치.**

즉 이건 취향 문제가 아니라 **레이아웃 버그**다. 없는 섹션의 자리를 예약하고 있다.

**처방 (P0-1)** — 셋 중 하나. 권고는 (a).

| 안 | 방법 | 창 높이 | 위험 |
|---|---|---|---|
| **(a) 권고** | `DetailY`/`PanelHeight`를 `SectionCountForTab(tab)`에서 유도. 탭 전환 시 창 높이가 861 ↔ **685**로 바뀐다 | 가변 | 탭 전환에 높이 애니메이션 필요(0.12s). 화면 중앙 고정이라 위아래로 균등하게 줄어 든다 |
| (b) | 남는 156pt를 **상세 패널**에 준다(103 → 259) | 고정 861 | 상세 패널이 한 줄짜리 설명에 259pt를 쓰게 됨 — 다른 종류의 공백 |
| (c) | [외형]에 4번째 섹션을 만든다 | 고정 861 | 콘텐츠 문제로 미룸. 지금 할 일이 아니다 |

(a)의 검산: `SectionsTopY(72) + 3×156 + DetailHeight(103) + 하단 여백(16) + TitleHeight(40)` = **685pt**.
화면 세로 982pt에서 상하 여백 148pt씩 — 넉넉하다 ✔ ([장비] 4섹션은 현행 861 그대로.)

### 3.1-b 게이지 0% 상태가 "고장"으로 읽힌다 (신규 · 20:31 빌드에서 확인)

`v2_info_h.png`(새 빌드, 신규 캐릭터 Lv.3): `STRESS 0% · 안정` / `EXP 3 / 317`.

```
트랙 색 = TrackBackground(흰 α0.09)를 PanelSurface에 합성 → #292c30
          패널 대비 = 1.28 : 1     ← 사실상 안 보인다
채움 0%  → 화면에 아무것도 없다
채움 1%  → EXP 3/317 이면 채움 폭 = 204 × 0.0095 = 1.9pt   (트랙 높이 4pt보다 얇다)
```
결과: 라벨 두 줄 밑에 **아무것도 없는 빈 줄**이 두 개 생긴다. "0이다"가 아니라 "안 그려졌다"로 읽힌다.
그리고 이건 **모든 신규 유저가 첫 실행에서 보는 화면**이다.

**처방 (P1)**
```
TrackBackground  흰 α0.09 → α0.22   (합성 #484a4e, 패널 대비 2.02:1 — 비텍스트라 3:1 대상 아님,
                                     "여기 홈이 있다"를 말하기에 충분하고 Accent 채움과의 대비 5.26은 유지)
최소 채움        값 > 0 이면 채움 폭을 최소 TrackHeight(4pt) 이상으로 클램프
                 → 둥근 끝이 온전히 그려져 "조금 찼다"가 보인다
값이 정확히 0    채움을 그리지 않는다. 트랙만 보인다 (위 α0.22 덕에 트랙은 보인다)
```

### 3.1-c 라틴 대문자 토큰이 두 가지 뜻으로 쓰인다 (신규)

같은 창 안에서 라틴 대문자가 두 역할을 겸한다.

| 자리 | 형태 | 뜻 |
|---|---|---|
| 우측 섹션 | `■ 모자  **HEAD**` — 한글 이름 **옆의 보조 코드** | "부차적 식별자" |
| 좌측 게이지 | **`STRESS`** / **`EXP`** — 한글 이름이 **아예 없다** | "1차 라벨" |

즉 `HEAD`는 "안 읽어도 되는 것"이고 `STRESS`는 "반드시 읽어야 하는 것"인데 **글자 모양이 같다.**
그리고 이 앱의 나머지 UI는 전부 한국어다.

**처방 (P1)**: 좌측 게이지도 우측 섹션과 **같은 형식**으로 맞춘다.
```
현행 :  STRESS            23% · 안정
제안 :  스트레스   STRESS      23% · 안정      ← 한글 T5 + 라틴 코드 T7 (섹션과 동일 문법)
현행 :  EXP               201 / 207
제안 :  경험치     EXP        201 / 207
```
좌측 열 폭 204pt 검산: "스트레스"(4자 × 12 = 48) + 간격 8 + "STRESS"(10pt, ≈36) = 92 <br>
+ 우측 값 "23% · 안정"(10pt, ≈52) = 144 < 204 ✔ 여유 60pt.

### 3.2 카드 4장이 "하나의 앱"이 아니다 — 세 가지 결함

`crop_pet_z.png` 확대에서 전부 확인.

**(1) 흰 채움 [착용] 버튼이 내용보다 9배 밝다 (P0-4)**
```
[착용] 버스 : 119 × 22 = 2,618 pt²  채움 #f2f4f7 (카드 대비 15.0:1)
아이템 아이콘 : 44 × 44 상자, 실 잉크 ≈ 290 pt² (카드 대비 11.9:1)
        → 최고 휘도 면적비 약 9 : 1
```
한 화면에 이 흰 막대가 **12개** 뜬다. 유저가 고르는 대상은 *아이템*인데 가장 밝은 것은 *동사*다.
같은 동사가 12번 반복되면 그건 CTA가 아니라 **반복 어포던스**이고, 반복 어포던스는 조용해야 한다.

처방 — `StyleActionButton`에 `emphasis` 인자 1개 추가. **상태→라벨 매핑은 지금처럼 한 곳 유지**
(기존 주석의 "두 벌로 두면 같은 상태를 다른 색으로 말한다"는 걱정은 그대로 지켜진다. 나뉘는 것은
상태가 아니라 *강조 등급*이다).

| 자리 | 상태 | 표면 | 테두리 | 라벨 |
|---|---|---|---|---|
| **카드**(Secondary) | 보유·미착용 | `Flatten(CardBorder, CardSurface)` **#32353c** | CardBorder | T8→**TextPrimary** (11.14:1) |
| | 착용 중 | `Flatten(AccentSurface, CardSurface)` **#243143** | AccentBorder **#3f6798** | TextOnAccent (7.16:1) |
| | 잠김 | CardSurfaceMuted | CardBorder | TextTertiary (5.73:1) |
| **상세 패널**(Primary) | 보유·미착용 | TextPrimary(흰 채움) **유지** | TextPrimary | OnAccentSolid |

→ **밝은 채움은 화면에 하나뿐**(상세 패널)이 되고, 그게 진짜 1차 행동이 된다.
색을 새로 만들지 않는다 — `#32353c`는 `SettingsControls.ButtonSurfaceOnCard`가 **이미 정의해 둔 값**이다.

**(2) 이름과 메타가 물리적으로 겹친다 (P0-5)**
```
Name : x 11 .. 89   (width 78, MiddleLeft, Overflow)
Meta : x 89 .. 130  (width 41, MiddleRight)
                    ↑ 두 상자 사이 간격 = 0pt
"리틀스틱메이트" = 한글 7자 ≈ 84pt > 78  →  6pt 넘침  →  "착용 중"과 맞닿음
```
캡처에서 실측 간격 ≈ **1.2pt** (다른 카드는 40pt 이상).

처방:
```
Name : x 11, width 70            (89 − 11 − 8)
Meta : x 89, width 41            (그대로)
간격 : 8pt (Space2) 확보
+ UiChrome.Ellipsize(Text, maxWidth)  신설 — preferredWidth 초과 시 뒤를 잘라 "…" 부착
  (uGUI 레거시 Text에 말줄임이 없다. 직접 만들어야 한다. 보관함 3열 목록도 같은 헬퍼를 쓴다)
```

**(3) 캐러셀 peek이 하필 버튼을 정확히 반으로 자른다 (P1-6)**
```
CarouselViewportWidth = 150×3 + 141×0.5 = 520.5
→ 4번째 카드는 왼쪽에서 70.5pt까지만 보인다
   그 카드의 [착용] 버튼은 11 .. 130 pt  →  70.5에서 절단  →  버튼의 정확히 50%
```
`CarouselPeekFraction 0.5`는 *카드*가 잘려 보이게 하려고 고른 값인데, **그 절단선에 무엇이 걸리는지는
아무도 안 봤다.** 결과는 "스크롤 가능"이 아니라 "레이아웃 깨짐"으로 읽힌다(캡처의 `착` 한 글자).

처방 — peek 값은 **건드리지 않는다**(그 근거는 타당하다). 절단선을 **칼에서 페이드로** 바꾼다.

먼저 **더 싼 방법을 검토하고 기각했다**: `RectMask2D.softness`는 마스크 경계를 셰이더에서
부드럽게 만들어 주는 내장 기능이고 이 프로젝트는 **한 번도 쓰지 않는다**(전 소스 grep 0건 — 그래서
지금 모든 클리핑이 칼이다). 그러나 `softness`는 `Vector2Int(x, y)`라 **한 축의 양쪽 변에 대칭으로**
걸린다. 캐러셀 뷰포트의 왼쪽 변은 첫 카드가 x=0에 딱 붙는 자리라, 오른쪽을 페이드하려고 x 소프트니스를
켜면 **첫 카드 왼쪽 24pt가 같이 흐려진다.** 그래서 못 쓴다.

```
뷰포트 좌·우 끝에 32pt 폭 가로 그라디언트 오버레이 2장
  색            : PanelSurface(#14171c) → 알파 0
  raycastTarget : false            (새 히트 영역 0개)
  스프라이트    : UiChrome의 VerticalGradientFill 을 90도 돌린 HorizontalGradientFill 1종 추가
  ★ 알파를 스크롤 위치로 구동한다 — 그래야 장식이 아니라 어포던스가 된다
      alphaLeft  = clamp01(scroll / 32)
      alphaRight = clamp01((maxScroll - scroll) / 32)
```
검산: 오른쪽 마스크 구간(x 488.5~520.5)에 걸리는 것은 4번째 카드의 x 38.5~70.5 — 썸네일(11~130) 한복판이다.
버튼(y −80..−102)도 그 x 범위에 있으므로 함께 덮인다 → 절단선이 사라진다.
스크롤 0에서 `alphaLeft = 0`이라 첫 카드는 온전히 보인다 ✔ 끝까지 밀면 `alphaRight = 0` ✔

### 3.3 행동 팝오버 — 왼쪽 정렬선이 4개다

```
패널 왼쪽 가장자리 = 0
 ├ 16  "행동 명령"(제목) · "지금 시킬 수 있어요"(상태) · "내가 누를 때만 실행돼요."(푸터)
 ├ 26  "혼자 노는 것"/"남의 창으로 노는 것"(그룹 제목) · 비침해 캡션 · 구분선
 ├ 34  행 아이콘 상자 왼쪽
 └ 74  행 이름 · 행 설명
```
16 → 26의 10pt 단은 §1.1의 off-token 값 그대로다. 캡처(`action_popover.png`)에서 세 줄이
계단처럼 어긋나 보이는 것이 바로 이 셋이다.

**처방 (P1)**
```
카드 안쪽 여백 10 → 12(Space3)  ⇒ 그룹 제목 = 28
아이콘 x 8 → 0                  ⇒ 아이콘 상자 왼쪽 = 28   (그룹 제목과 같은 선)
행 텍스트 x 48 → 40             ⇒ 텍스트 = 68            (아이콘 28 + 상자 28 + 간격 12)
```
정렬선 4개 → **3개**(16 / 28 / 68), 그중 28은 아이콘과 그룹 제목이 **공유**한다.
검산: 행 폭 `RowWidth = 448 − 12×2 = 424`, 설명 폭 `424 − 40 − 8 = 376` — 현행 372보다 넓다 ✔

**행 내부 세로가 1pt 아래로 치우쳐 있다**
```
이름 y −10..−28 · 설명 y −28..−44 → 글자 블록 10..44 (34pt)
행 높이 52 → 위 10 / 아래 8   (중앙이면 9 / 9)
```
`CardNameY −10 → −9`, `Description −28 → −27`로 1pt씩 올린다. 사소하지만 6행 전부에 걸린다.

**높이 560 → 464**: 행 높이 52는 2줄 텍스트(18+16=34)에 18pt 여백이라 과하다. **44**(설정창 RowHeight와
같은 값, 32-1의 최소 클릭 타깃)로 내리면 6행 × 8 = 48pt 절약. 그룹 여백 정리로 추가 48pt.
→ **480×464**. 화면 점유가 세로 57% → 47%로 내려간다.

**[✕ 앱 종료]가 "행동" 창 푸터에 있다** (페르소나 기지적, 조형 판단만 적는다)
조형적으로도 틀렸다 — 푸터 오른쪽 92×28 칩이 그 창에서 **유일한 파괴적 행동**인데,
표면색이 `SubtleSurface`(카드보다 어두움)라 *가장 조용한* 요소다. 위험한 것이 조용하면 오조준이 는다.
**옮기는 것이 정답**(설정창 [데이터] 또는 [일반] 하단)이고, 그 전까지는 최소한
테두리를 `AccentBorder`로 올려 "여긴 다르다"를 말해야 한다. **이동 여부는 리더 결정.**

### 3.4 보관함 탭 — 같은 창 안에서 정보 밀도가 16배 다르다

| 탭 | 아이템 표현 | 1아이템 면적 |
|---|---|---|
| [외형]/[장비] | 카드 141 × 108 | 15,228 pt² |
| [보관함] | 행 592 × 24 | 14,208 pt² … 처럼 보이지만 **한 화면에 19개 vs 12개** |

문제는 면적이 아니라 **양식**이다. 같은 물건(아이템)을 한 창에서 카드 그리드와 표로 두 번 그린다.
그리고 [보관함] 행은 **19개가 각자 둥근 사각형 + 테두리**를 갖고 있어서(캡처 `crop_list_z.png`)
인접 행의 테두리가 겹쳐 **이중 실선 이음매**가 생긴다.

**처방 (P1)**
- 행마다 테두리를 그리지 않는다. **목록 전체를 카드 1장**으로 감싸고 행 사이는 `Divider` 1pt.
  → 그린 Graphic 수 19×2 → 1 + 18. (드로우콜/오버드로 이득은 덤)
- 착용 중 행만 `AccentSurface` 채움 + `AccentBorder` 테두리(지금처럼).

**"착용 중" 글자색이 행마다 다르다** — 캡처에서 베레모=주황 / 동그란안경=청록 / 반다나=초록.
카테고리 틴트를 상태 텍스트에 쓰고 있다. **상태색은 상태를 말해야 한다.** 카테고리는 왼쪽 도트가 이미 말한다.
→ "착용 중"은 전 행 **TextOnAccent 고정**.

**페이지 표시가 `"1\n/\n1"`이다** (소스 그대로)
24pt 폭 세로 레일에 맞추려고 세 글자를 세로로 쌓았다. 게다가 색이 TextQuaternary(**3.50:1**)라
캡처에서 "/"가 안 보이고 `1` 위 `3`만 읽힌다.

처방 — 레일을 없애고 **헤더 줄 오른쪽 끝에 가로로** 놓는다.
```
걸치는 것  (42 / 42)                              [▲]  1 / 3  [▼]
├──────────────────────────────────────────────────────────────┤
```
검산: 헤더 폭 592, 좌측 문구 ≈130, 우측 페이저 그룹 24+4+36+4+24 = 92 → 합 222, 여유 370pt ✔
부수 이득: 목록 폭 560 → **592**(+5.7%), 색은 T7(5.33:1)로 AA 통과.

### 3.5 설정창

- **탭 줄에 [▲][▼]가 들어 있다.** 이 둘은 **본문**을 스크롤하는데 **탭**과 한 줄에 묶여 있어
  "탭 넘김"으로 오독된다(캡처 `set2_half.png`). → 본문 영역 오른쪽 위/아래 모서리로 옮기거나,
  §3.4와 같은 페이저 형태로 푸터에 둔다. **권고: 본문 우상/우하 모서리** (스크롤 대상 안에 있어야 한다).
- **카드 하단이 칼로 잘린다.** "시작 / 종료" 카드가 푸터 선에서 그냥 끊긴다. §3.2(3)과 같은
  스크롤 구동 페이드를 **세로로** 쓴다(본문 상·하단 24pt, `VerticalGradientFill` 그대로).
  여기도 `RectMask2D.softness`는 못 쓴다 — 같은 대칭 문제로 최상단에서 첫 카드 머리가 흐려진다.
- **설명 유무가 카드마다 다르다.** [표시] 카드는 3행 전부 설명이 있고, [화면 위 UI] 카드는
  2행 모두 설명이 없다. → 규칙: **설명은 있거나 없거나 카드 단위로 통일**. 없는 쪽에 붙이는 것이 맞다
  ("구석 크기 패널"·"톱니 아이콘"은 이름만으로 뭘 하는지 모른다).
- **푸터 오른쪽 문구가 패널 오른쪽 가장자리까지 흘러간다** — "…캐릭터 정보창 [설정] · ⌃⌥⌘," 가
  여백 없이 끝난다. 우측 여백 20pt(Space5) 확보 + 길면 `Ellipsize`.
  (문구 안의 `⌃⌥⌘,`는 오늘 `P`로 바뀌었으므로 **이미 고쳐졌을 수 있다**.)

---

## 4. ★ 포스트잇 — 앱에서 유일하게 디자인 시스템 밖에 있는 표면 (P0-2)

`TodoPostItWidget.cs` 실측:

| 항목 | 현행 | 앱 규약 |
|---|---|---|
| 표면색 | `new Color(1, 0.95, 0.6, **0.92**)` — 주석: *"디자이너 확정 전까지"* | 토큰 사용 |
| **알파** | **0.92** | **1.0** (2026-08-31 확정: "패널 뒤에 유저의 진짜 데스크톱이 있다. 알파 유리는 이 아키텍처에서 성립하지 않는다") |
| 모서리 | **직각** (`RoundedFill` 호출 없음) | RadiusPanel 14 |
| 그림자 | **없음** | ElevationWindow/Popover |
| 테두리 | 없음 | PanelBorder |
| 글자 크기 | `fontSize = 14` / `12` 생 리터럴 | UiChrome 토큰 |
| 글자색 | `new Color(0.2,0.2,0.2,**0.85**)` | 토큰 |
| 폰트 | 직접 `GetBuiltinResource` | `UiChrome.Font` |
| 버튼 라벨 | `"[숨기기]"` — 대괄호 포함 | 대괄호 없음 |

**알파 0.92의 실제 피해 (계산)**
```
같은 카드를 진한 파랑 데스크톱(#3b4fd8) 위에 : #efe59e
                흰 문서(#ffffff) 위에         : #fff3a1
                                    ΔL = 0.111  (휘도 11.1%p 차이)
```
= **배경에 따라 카드 색이 바뀐다.** 정보창·설정창·팝오버가 2026-08-31에 정확히 이 이유로 α1.0이 됐는데,
포스트잇만 그 라운드에서 빠졌다.

**처방 — 다크 팔레트로 흡수하되 "노란 포스트잇"의 정체성은 강조색으로 남긴다**
```
표면      PanelSurface #14171c  α = 1.0        (다른 창과 같은 바탕)
모서리    RadiusPanel 14
테두리    PanelBorder rgba(255,255,255,0.16)
그림자    ElevationPopover (spread 12, offset −4)
좌측 띠   폭 4pt, 색 #f5d76e(포스트잇 노랑), 카드 왼쪽 변에 붙는 세로 띠
          → "이건 메모다"를 색으로 남기되 표면 전체를 노랗게 칠하지 않는다
안쪽 여백 Space3 = 12  (현행 8)
행 높이   26 → 28   (12pt 글자 + 위아래 8)
글자      항목 = T4 Body(12 Reg TextPrimary, 16.3:1)
          체크 완료 = T7 Caption(10 Reg TextTertiary) + 취소선
버튼      "[숨기기]" → "숨기기", T7, 칩(RadiusChip 6, 표면 CardSurface, 테두리 CardBorder)
```
대안(리더가 "노란 포스트잇을 유지하라"고 하면): 표면을 `#f5d76e α=1.0`로 **불투명하게만** 바꾸고
본문색을 `#1b1f26`(대비 **11.6:1**)로. 알파만 없애도 §4의 치명적 부분은 사라진다.
**어느 쪽이든 α 0.92는 안 된다.**

### 4-b. 포스트잇 빈 상태 — 결함 아님 (확인함)

미완료 0건이면 카드를 통째로 숨긴다(`items.Count > 0`, 17절 "빈 상태 예외"). **옳다.**
상주 오버레이에서 "할 일 없음"이라고 적힌 빈 카드를 데스크톱에 계속 띄우는 것은 비침해 위반이다.
내 캡처 어디에도 포스트잇이 안 나온 이유가 이것이다 — 위 §4의 조형 결함은 전부 **소스 실측**이다.

---

## 5. ★ 톱니 아이콘 — 조형 재설계

### 5.1 어떤 잉크색으로도 안 보인다 (P0-3)

잉크는 `_config.ResolveInkColor()` = **캐릭터 잉크(기본 검정)** 고정, 배경 보정 없음, α 0.70/0.95.
톱니는 **임의의 데스크톱** 위에 맨몸으로 놓인다.

```
검정 잉크의 회색 전 구간 최악 대비   = 1.00 : 1   (거의 검은 배경에서 완전히 사라진다)
민지 실측(파란 배경 실화소)          = 1.55 : 1
흰 잉크로 뒤집으면?                  → 흰 배경에서 1.00 : 1   (문제가 이동할 뿐)
```
**단일 색으로는 원리상 해결 불가.** WCAG 비텍스트 최소 3:1을 *모든* 배경에서 만족하는 단색은 없다.

**처방 — 잉크 + 역상 헤일로 2겹** (이 앱이 이미 가진 관용구다. 만화 레터링의 `Outline`이 정확히 이것)
```
헤일로  : 같은 경로를 획 폭 W×2.2 로 먼저 그린다.  색 = 잉크의 역상
          잉크가 어두우면 헤일로 #f2f4f7, 잉크가 밝으면 헤일로 #0b1016
잉크    : 그 위에 W 로 그린다
```
**보장 대비(회색 0~255 전 구간 전수 계산):**
```
현행 단색 검정        최악 = 1.00 : 1   (배경 #101010)
잉크+헤일로 2겹       최악 = 4.18 : 1   (배경 #757575, 잉크 4.14 / 헤일로 4.18 중 좋은 쪽)
```
→ **WCAG 3:1을 39% 여유로 상회하며, 그것이 모든 배경에서 보장된다.**

**호버 피드백도 알파에서 형태로 바꾼다.** α 0.70→0.95(36% 변화)는 저대비 배경에서 아무 일도 안 일어난 것과 같다.
```
평상  헤일로 W × 2.2
호버  헤일로 W × 3.0   (0.09초 보간 — 부채꼴 HoverSeconds와 같은 값)
```
헤일로가 두꺼워지면 배경 휘도와 무관하게 아이콘이 **배경에서 떨어져 나온다**. 알파로는 못 하는 일이다.

### 5.2 형태 — 지금 톱니에는 톱니가 없다

획 W = 1.7pt 기준(`StrokeWidthPoints`). 여유(clear space)를 **획 단위**로 잰다
(`EQUIPMENT_SHAPE_SPEC`과 같은 통화. 1.5 W 미만이면 화면에서 한 줄로 뭉친다).

**현행 큰 기어 (R 13, 10이, 허브+림+스포크4+톱니링 = 요소 4겹)**

| 구간 | 여유 | 판정 |
|---|---|---|
| 허브(3.6) ~ 림(7.0) | 1.70pt = **1.00 W** | 위반 |
| 림(7.0) ~ 뿌리원(10.2) | 1.50pt = **0.88 W** | 위반 |
| **이 높이(노치 깊이)** | 1.10pt = **0.65 W** | **위반** |
| 이 끝 폭 | 2.60pt = 1.53 W | OK |

**현행 작은 기어 (R 7.8 = 0.60배, 획은 1.7 그대로)**

| 구간 | 여유 | 판정 |
|---|---|---|
| 허브 ~ 뿌리원 | 1.50pt = **0.88 W** | 위반 |
| **이 높이(노치 깊이)** | **−0.02pt = −0.01 W** | **톱니가 물리적으로 존재하지 않는다** |

이가 획보다 **얕다**. 즉 작은 기어는 톱니바퀴가 아니라 **울퉁불퉁한 고리** 하나다.
확대 캡처(`gear_on_bright.png`)의 왼쪽 아래 물체가 정확히 그렇게 보인다.

**"작게 만들 수 없다"는 것을 증명해 둔다.** 획을 고정하면 톱니 고리의 최소 외반경은
`0(허브 없음) + hs + 1.5W + hs + 1.5W + hs ≈ 8.5pt`다. 현행 작은 기어는 R 7.8 — **바닥 아래**다.
→ 작은 기어를 "제대로" 그리려면 큰 기어의 74%가 되어야 하고, 그건 이미 작은 기어가 아니다.
**작은 기어는 지운다.**

**신규 형태 — 기어 1개, 요소 2겹, 8이**

```
            ┌────────  Ø 26 pt  ────────┐
                  ╱▔╲   ╱▔╲   ╱▔╲            이 8개 (사다리꼴)
                 ╱   ╲_╱   ╲_╱   ╲           tipHalf 0.17 · rootHalf 0.26
                │                   │
               ╱   ┌───────────┐    ╲        뿌리원  r = 8.0  (획 W)
              │    │    ●      │     │       허브    r = 3.2  (★ 채움 점. 링 아님)
               ╲   └───────────┘    ╱
                │                   │        ※ 림 원 삭제
                 ╲   ╱▔╲   ╱▔╲   ╱           ※ 스포크 4개 삭제
                  ╲_╱   ╲_╱   ╲_╱

  R = 13.0   W = 2.0(§6 단일 펜)   N = 8   허브 r 3.2 채움   뿌리원 r 8.0   이 끝 r 12.0
```

| 구간 | 실측 | 판정 |
|---|---|---|
| 허브 ~ 뿌리원 | 3.80pt = **1.90 W** | OK |
| 이 높이(노치 깊이) | 3.00pt = **1.50 W** | OK |
| 이 끝 폭 @ r12 | 3.20pt = **1.60 W** | OK |
| 이 뿌리 폭 @ r8 | 3.27pt = **1.63 W** | OK |
| 이 사이 골 @ r8 | 3.02pt = **1.51 W** | OK |
| 이 사이 골 @ r12 | 6.22pt = **3.11 W** | OK |

**전 구간 1.5 W 이상.** 최소값 1.50, 최대 3.11 — 균일하다.

부수 이득:
- **점유 면적 −40%**: 두 기어 묶음 bbox 36.2 × 31.2 → **26 × 26**. 비침해 원칙 2에 유리.
- **광학 중심 = 히트 중심**: 지금은 작은 기어가 214° 쪽으로 매달려 있어 둘이 어긋난다. 단일 기어는 방사 대칭이라 일치한다.
- **N을 10에서 8로 내린 근거**: N=10이면 골 폭이 r8에서 1.29 W로 떨어져 위반한다(W=2.0 기준).
  N=8이 획 2.0에서 모든 조건을 만족하는 최대 잇수다.
- `rootHalf`를 0.30 → **0.26**으로 내린 근거: 0.30이면 골 1.26 W(위반). 0.26이 1.51 W로 딱 통과.
  `tipHalf 0.17 < rootHalf 0.26` 이므로 사다리꼴 조건 유지 ✔

**두 개짜리 기어를 잃는 것이 아쉬운가?** 작은 크기에서는 **단일 기어가 더 잘 읽힌다.**
두 개 묶음은 세계 어디서도 26pt에서 성립하지 않는다 — 그래서 macOS 시스템 설정도 Windows 설정도 기어 **하나**다.

---

## 6. 부채꼴 메뉴

### 6.1 아이콘 획: 값이 4개인데 어떤 법칙으로도 설명이 안 된다

| 자리 | 상자 | 획 | 획/상자 |
|---|---|---|---|
| 부채꼴 심볼 | 24 | **2.00** | 8.33% |
| 톱니 | 26 | **1.70** | 6.54% |
| 팝오버 행 아이콘 | 28 | **1.80** | 6.43% |
| 정보창 카드 아이콘 | 44 | **1.87** | 4.25% |

24 → 2.00, 40 → 1.70(EQUIPMENT_SHAPE_SPEC이 잠근 값), 44 → 1.87.
**상자가 커지면서 획이 내려갔다 다시 올라간다 — 단조가 아니다.** 어떤 비례 법칙으로도 나올 수 없는 조합이고,
따라서 **이 값들은 체계에서 나온 것이 아니다.**

**처방 (P2-14) — 펜은 하나다**
```
UiChrome.PenPoints = 2.0f
```
근거: `StickConfig.MinStrokeScreenPoints = 2.0`. 출하 기본 `characterScale 0.75`에서
**캐릭터 자신의 획이 화면상 ≈2.03pt**다. 이 앱의 그림은 전부 같은 펜으로 그린 것처럼 읽혀야 한다.

| 자리 | 현행 | 신규 | Δ |
|---|---|---|---|
| 캐릭터 (배율 0.75) | ~2.03 | ~2.03 | 0% |
| 부채꼴 심볼 | 2.00 | 2.00 | **0%** |
| 팝오버 행 아이콘 | 1.80 | 2.00 | +11% |
| 톱니 | 1.70 | 2.00 | +18% (§5.2가 형태를 함께 고치므로 안전) |
| 정보창 카드 아이콘 | 1.87 | 2.00 | **+7%** |

카드 아이콘 검산: `EQUIPMENT_SHAPE_SPEC`의 여유는 전부 **획 단위**로 적혀 있고 가장 빠듯한 값이
1.24 획(긴 망토 옷깃)이다. 획이 1.87→2.00이면 그 값은 1.24 × (1.87/2.00) = **1.16 획** — 스펙의
하한(규칙 6: ≥1 획) 위. **위반 없음.** 나머지 29종은 더 여유가 있다.

### 6.2 호버 이름표 / 온보딩 알약 — 앱의 유일한 학습 창구가 최소 등급이다 (P1-9)

```
현행 : 알약 높이 18pt · 글자 FontCaption 10pt · 좌우 여백 14(각 7) · 반지름 9
```
버튼은 Ø44인데 이름표는 18pt — **41%**. 그리고 아이콘 전용 내비게이션에서 이 알약이
**"이 버튼이 뭔지" 알 수 있는 유일한 수단**이다.

```
신규 : 알약 높이 24pt · 글자 T4(12 Reg TextPrimary) · 좌우 여백 20(각 10) · 반지름 12(캡슐 유지)
```
검산:
- 12pt × 1.5(Windows 150%) = 18px 정수 ✔ (짝수 규칙 통과)
- 12pt 한글 줄 상자 ≈ 14.4pt → 위아래 여백 4.8pt씩. 세로:가로 여백 = 4.8 : 10 = 1 : 2.08 (적정 대역 1:1.6~2.2)
- 이름표는 `ComputeLayout`/`UnionScreenRect`에 **참여하지 않으므로** 부채꼴 기하가 안 바뀐다.
  화면 클램프는 `ResolveHoverLabelCenter`가 이미 `halfH`로 처리한다 → 24pt로 커져도 화면 밖으로 안 나간다 ✔
- 온보딩 알약도 같은 크롬을 공유하므로 함께 커진다. 폭 = 16자 × 12 + 20 ≈ **212pt** (화면 1512pt에 여유) ✔
- 대비 변화 없음(불투명 PanelSurface 위 TextPrimary 16.3:1).

### 6.3 [행동] 아이콘 = 확성기 → 교체 (P1-8)

확성기는 **커뮤니케이션 은유**라 "알림 설정"으로 읽힌다. 창 이름은 "행동 **명령**"이고 내용은
말 걸기·활쏘기·그라피티·창 도둑·창 부수기 — **"재주를 시킨다"** 다. *(격파는 2026-09-02 삭제)*

선정 기준(이 넷을 전부 만족해야 한다):
1. 커뮤니케이션/알림 은유가 아닐 것 ← 확성기가 탈락한 이유
2. ①집중(원형) ②캐릭터(인물) ③할일(반복 행) 의 실루엣 가족과 겹치지 않을 것
3. 24pt 상자 · 획 2.0에서 모든 여유 ≥ 1.5 W(=3.0pt), 획 6개 이하
4. 한국어 사용자에게 "시킨다"로 읽힐 것

**1순위 — 지휘봉 (command baton)**
```
   ┌──── 24 pt ────┐
   │        ╲ ╱     │   끝의 방사 3획 : 길이 3.5pt(1.75 W), 45도 간격
   │       ─ ✦      │   봉 : 좌하 → 우상 대각선 1획, 길이 19pt(9.5 W)
   │      ╱   ╲     │   방사 끝 사이 호(반경 5) = 3.9pt = 1.95 W  ✔
   │    ╱           │
   │  ╱             │
   └────────────────┘   총 4획.  실루엣 = 긴 대각선 + 작은 방사 다발
```
실루엣이 ①(원) ②(인물) ③(가로 행 반복) 어느 것과도 안 겹친다 ✔

**위험**: "마법 지팡이"로 읽힐 여지가 있다. 이 앱 톤에서는 치명적이지 않다고 보지만 **판단은 리더 몫**.

**2순위 — 중심에서 네 방향으로 뻗는 화살촉 4개** ("움직이게 시킨다")
방사 대칭이라 ①(원형 스톱워치)과 실루엣 가족이 겹칠 위험이 있어 2순위로 둔다.

**채택하지 않은 것**: ✨스파클(지금은 "AI"로 읽힌다) / 손 모양(24pt에서 획 예산 초과) /
재생 삼각형(“동영상”) / 원형 화살표(“새로고침”).

### 6.4 부채꼴 버튼 호버 시 표면이 무너진다 — 조형 판단만

```
Color.Lerp(CardSurface(α 1.0), AccentSurface(α 0.14), hover)   →  hover=1 에서 α 0.14
```
커서가 올라간 순간 **버튼의 86%가 투명해져 데스크톱이 비친다**(캡처 `hov_megaphone.png`에서
호버된 ④를 통해 배경 그라디언트가 이어져 보인다).

조형적으로: **호버는 강조인데 결과는 소멸**이다. 방향이 반대다.
그리고 이 앱은 이미 정답 관용구를 갖고 있다 — `SettingsControls.ButtonSurfaceOnCard`가
`UiChrome.Flatten(반투명 토큰, 바탕)`으로 **불투명한 합성색**을 만든다. 여기만 그걸 안 쓴다.

권고 값(구현은 별도 배정, 나는 손대지 않는다):
```
호버 표면 = UiChrome.Flatten(AccentSurface, CardSurface) = #243143  (α 1.0)
호버 테두리 = UiChrome.Flatten(AccentBorder, CardSurface) = #3f6798  (카드 대비 2.84:1)
심볼      = Accent #5da1f5  (새 표면 위 4.94:1)
```

---

## 7. 만화 레터링(구 말풍선)

말풍선 도형은 2026-09-01에 폐지됐다(`DrawBubbleShapes = false`). 지금 화면에 나오는 것은
**외곽선 붙은 글자 블록**뿐이고 꼬리도 없다. 따라서 "꼬리/외곽선/배치"는 아래로 재정의한다.

### 7.1 기울기가 도장이 되는 이유와, 결정론을 깨지 않는 고치는 법 (P1)

실측 로그(같은 세션 8회 발화):
```
"가뿐하네"       8.2 / 8.2 / 8.2      (3회 전부 동일)
"하나 둘 하나 둘"  8.1 / 8.1           (2회 전부 동일)
"구경 중이야" 9.9 · "여기 좋네" 6.9 · "잠깐 쉬는 중" 6.9
```
`ComicTiltMagnitudeFor(text, …)`의 입력이 **대사 문자열뿐**이다. 부호는 놓인 쪽에서 오므로
왼쪽이면 항상 +8.2, 오른쪽이면 항상 −8.2 — 한 대사는 평생 두 각도만 갖는다.
손글씨 느낌을 내려던 장치가 **도장**이 됐다.

**처방 — 난수를 쓰지 않는 프로젝트 관례를 그대로 지킨다.**
```
해시 입력 : text                    →   text ⊕ 발화 회차(_utteranceOrdinal)
```
- `_utteranceOrdinal`은 새 대사를 래치할 때 +1 하는 int 하나. **발화 중에는 고정**이므로
  프레임마다 다시 계산해도 글자가 떨리지 않는다(현행 보장 그대로).
- 같은 (대사, 회차)는 항상 같은 각도 → **"같은 입력이면 같은 출력"** 관례 유지 ✔
- 저장/복원 불필요(세션 내 카운터).

**진폭도 같이 넓힌다**: `ComicTiltJitterRatio 0.25` → **0.45**. 기준 8도에서 4.4~11.6도.
근거: 0.25(6~10도)는 폭이 4도뿐이라 회차마다 달라져도 사람 눈에 "달라졌다"가 안 보인다.
사람이 각도 차이를 인지하는 하한은 대략 3도이므로, 연속 두 발화가 구분되려면 분포 폭이
그 2배 이상이어야 한다. 7.2도 폭이면 충분하다.
상한 검산: 11.6도에서도 `RotatedBounds`가 축정렬 경계를 다시 재므로 화면 클램프는 그대로 성립 ✔

### 7.2 기울기가 오늘의 글리프 정밀도 작업을 무효화한다 (미확인 · 조사 필요)

`ResolveFontSize()`는 오늘 `UiGlyphScalePolicy.SnapPoints()`로 **정수 픽셀에 스냅**하도록 고쳐졌다.
그런데 그 직후 `_panel.localRotation`으로 블록을 **6.9~9.9도 회전**시킨다.
회전하면 글리프 쿼드가 픽셀 격자와 어긋나 아틀라스가 바이리니어로 재샘플된다 —
**이건 이 파일 자신의 주석이 적어 둔 사실이다**(`ComicTiltMinGlyphPixels`).

즉 스냅으로 얻은 잔차 0이 회전으로 다시 깨진다. 가드는 `ComicTiltMinGlyphPixels = 14` 물리 픽셀뿐인데,
Windows 150%에서 21pt → 22pt(스냅) × 1.5 = **33px**로 가드를 한참 넘어 기울기가 켜진다.

- 33px에서 재샘플이 얼마나 보이는지는 **실기 확인이 필요하다. 나는 판단하지 않는다.**
- 그러나 **사용자 신고 "텍스트도 다 번져보임"이 창 UI가 아니라 대사였을 가능성**은
  이 라운드에서 아무도 검토하지 않았다. 신고 사진에 대사가 찍혀 있었는지 확인할 것.
- 만약 대사였다면 처방은 pt가 아니라 **기울기 하한을 올리는 것**(`ComicTiltMinGlyphPixels 14 → 24`)이다.

**→ 리더 배정 필요. 나는 "미확인"으로 남긴다.**

### 7.3 외곽선 / 배치

- 외곽선 두께 `= max(0.4, fontSize × 0.06)`. **두 배율에서 실측했다.**

  | 캐릭터 상태 | 글자 | 외곽선 t | uGUI Outline 실효(t·√2) | Windows 100%에서 물리 px |
  |---|---|---|---|---|
  | 17:17 세이브 | 21pt | 1.26pt | 1.78pt | 1.78px |
  | 20:31 신규 세이브 | **14pt** | **0.84pt** | **1.19pt** | **1.19px** |

  1.19px는 **1픽셀 미만 구간**이라 안티에일리어싱에 녹는다 — 외곽선의 목적이
  "임의의 데스크톱 위에서 글자를 떼어 놓는 것"인데 작은 캐릭터 배율에서 그 기능이 약해진다.
  획 대 외곽선의 비가 없다는 점도 문제다(글자 획 굵기는 폰트가 정하는데 외곽선은 em 비례).
  Heavy 페이스에서는 반대로 1.26pt 외곽선이 획 사이 카운터를 메울 수 있다.
  → **하한 `MinTextOutlineThickness 0.4 → 0.7`**을 제안한다(실효 0.99pt, 배율 1에서 ~1px 확보).
  다만 두 방향(너무 얇다 / 카운터를 메운다) 중 어느 쪽이 실제로 문제인지는
  **밝은 배경 + 작은 배율에서 육안 1회 확인이 필요하다. 미확인.**
- 배치 규칙(진행 방향 반대쪽 대각선 위)은 로그로 확인했고 **일관되게 지켜진다** —
  `진행방향=오른쪽 → 글자쪽=왼쪽위`, `왼쪽 → 오른쪽위`. 8회 전부 일치. **결함 없음.**

---

## 7-b. 20:31 신규 빌드 대조 (저녁 수정 반영본)

리뷰 중 리더가 새 빌드를 올렸다(20:31 빌드 / PID 87070). 같은 화면을 다시 찍어 대조했다
(`v2_info_h.png` [장비] · `v2_set_h.png` 설정창 [일반]).

**이미 고쳐진 것 — 이 문서의 우선순위에 넣지 않았다**

| 항목 | 확인 |
|---|---|
| 설정창 뒤의 거대한 계단형 그림자 헤일로 | **사라졌다.** 감쇠 그림자가 의도대로 동작한다 ✔ |
| 푸터의 `⌃⌥⌘,` 표기 | `⌃⌥⌘P`로 갱신됨 ✔ |
| 구석 호버 패널 행 | 삭제됨 ✔ |
| 푸터 우측 문구가 패널 가장자리까지 흐름 | 우측 여백 ≈16pt 확보됨 — **철회한다** |

**새 빌드에서도 그대로인 것 (§별 재확인)**

| § | 항목 | 새 빌드 |
|---|---|---|
| 1.2 | 설정창 섹션 제목이 Accent 파랑 | 그대로. "화면 위 UI"(파란 글자) 바로 밑에 파란 토글 |
| 2.4 | 비활성 행 위계 역전 | 그대로. **게다가 2행으로 늘었다** — "숨기기/보이기 단축키"·"로그인할 때 자동 실행" 둘 다 제목이 설명보다 흐리다 |
| 2.3 | `[잠김]` 라벨 2.25:1 | 그대로. [장비] 탭에 잠긴 카드 12장 = 12곳 |
| 3.2(1) | 흰 [착용] 채움 | **더 뚜렷해졌다.** [장비] 탭은 행마다 보유 1 + 잠김 3이라 흰 막대 4개가 x≈650에 세로로 늘어선다. 그 막대들이 표시하는 것은 "이미 가진 것" = 가장 덜 유용한 정보 |
| 3.2(3) | 캐러셀이 버튼 한복판을 자름 | 그대로 (`잠` 한 글자) |
| 3.1 | [장비] 4섹션은 공백 없음 | **진단 확증.** 공백은 [외형](3섹션)에서만 난다 |
| 3.5 | 카드 하단 칼 절단 / [▲][▼]가 탭 줄에 | 그대로 |

**새 빌드에서 처음 보인 것**: §3.1-b(게이지 0% 상태) · §3.1-c(라틴 대문자 이중 의미).
둘 다 **신규 캐릭터 상태**(Lv.3 / EXP 3 / STRESS 0%)라야 보이는 것들이라 이전 캡처에는 없었다.
= **빈 상태 검증은 진행도가 낮은 세이브로 해야 한다**는 교훈.

---

## 8. 우선순위 (리더 배정용)

원칙: **(눈에 보이는 정도) × (독립성) ÷ (위험)**. P0는 서로 파일이 겹치지 않아 병렬 배정이 가능하다.

### P0 — 지금 배정. 각각 1~3시간, 서로 독립.

| # | 항목 | 파일 | 왜 지금 | 검증 |
|---|---|---|---|---|
| **1** | 정보창 [외형] **176pt 공백** | `CharacterInfoWindow` | 창 높이의 20.4%가 빈칸. 첫 인상에서 제일 먼저 보인다. 순수 레이아웃 버그 | 탭별 창 높이 861/685 단언 |
| **2** | **포스트잇 토큰화** (α0.92 제거 · 둥근 모서리 · 그림자 · 폰트) | `TodoPostItWidget` | 앱에서 유일하게 시스템 밖. 이미 폐기된 알파 유리 규약이 여기만 살아 있다 | 표면 알파 == 1.0 단언 (기존 `PopoverAndHoverPanelOpacityTests` 패턴 재사용) |
| **3** | **톱니 잉크+역상 헤일로** | `InfoGearIconWidget` | 보장 대비 1.00 → 4.18. "안 보인다"가 최상위 결함 | 회색 전 구간 최악 대비 ≥ 3.0 단언 |
| **4** | 카드 **[착용] 흰 채움 → 조용한 칩** | `CharacterInfoWindow.StyleActionButton` | 내용보다 버튼이 9배 밝다. 정보창 전체 인상을 지배 | 카드 버튼 표면 != TextPrimary 단언 |
| **5** | 카드 **이름/메타 겹침** + `Ellipsize` 헬퍼 | `CharacterInfoWindow` + `UiChrome` | 실제 글자 충돌. 헬퍼는 6·§3.4가 재사용 | 최장 이름에서 두 Rect 간격 ≥ 8pt 단언 |

### P1 — P0 다음 라운드

| # | 항목 | 근거 |
|---|---|---|
| 6 | 캐러셀 peek **가로 페이드 마스크** + 보관함 행 테두리 19겹 → 카드 1장 | 절단선이 버튼을 정확히 반으로 자름 |
| 7 | **비활성 규칙**(제목 2.09 < 설명 5.33 역전) 전역 적용 | 위계 역전 |
| 8 | **[행동] 아이콘** 확성기 → 지휘봉 | 오독 |
| 9 | **호버 이름표/온보딩 10pt → 12pt, 18 → 24pt** | 유일한 학습 창구 |
| 10 | 톱니 **형태 단순화**(단일 기어 8이) | 작은 기어에 톱니가 없음(−0.01 W) |
| 11 | 행동 팝오버 **정렬선 4 → 3**, 높이 560 → 464 | 계단 정렬 |
| 12 | 섹션 제목 **3표면 통일**(T6) + 설정창 Accent 회수 | 강조색 과용 |
| 13 | 보관함 페이지 표시 `"1\n/\n1"` → 헤더 가로 배치 | 세로 3줄 슬래시 |
| **14** | **게이지 0% 상태** — 트랙 α0.09(대비 1.28) + 최소 채움 없음 | **모든 신규 유저의 첫 화면**이 "안 그려짐"으로 읽힌다 (§3.1-b) |
| 15 | `STRESS`/`EXP` 라틴 단독 → 한글 + 코드(섹션과 같은 문법) | 같은 형태가 "안 읽어도 됨"과 "꼭 읽어야 함"을 겸한다 (§3.1-c) |

### P2 — 시스템. 지금 정하면 앞으로 안 생긴다.

| # | 항목 | 비고 |
|---|---|---|
| 16 | **타입 스타일 9종 표** 도입, `FontLabel` 폐기, 잉크 6단 → 4단 | 명목 22 / 실효 17 → 9. AA 미달 6곳 제거 |
| 17 | **★ 창 UI 한글 폰트 파이프라인** + 확정 face 로그 | **Windows 리스크. 실기 검증 필요** |
| 18 | **여백 토큰** Space5 신설 + 이관 + 소스 감사 테스트 | 7가지 → 6가지 토큰만 |
| 19 | **단일 펜 2.0pt** | 획 4종 → 1종 |
| 20 | 고도 토큰 3단 + 팝오버 폭 240/320/480 | 매직넘버 제거 |

### 리뷰했으나 **손대지 말 것으로 판단**한 것

- 감쇠 그림자(`SoftShadowFill`, smoothstep 램프) — 오늘 고쳐진 부분은 **설계가 옳다.** 17:17 빌드에서
  보이는 층 밴딩은 그 수정 **이전** 것이다.
- 대사 pt 스냅(`UiGlyphScalePolicy.SnapPoints`) — 옳다. 21pt는 macOS 배율 2에서 잔차 0이다.
- 부채꼴 배치 알고리즘(회전 탐색 → 강체 이동 → 세로 폴백) — 기하가 견고하다.
- 대사 배치 규칙(진행 방향 반대쪽 대각선 위) — 8회 실측 전부 일치.
- 카테고리 틴트 4색 순환 — 일관되게 지켜지고 있다(유일한 예외가 §3.4의 "착용 중" 글자색).

---

## 9. 교차 레이어 영향 (리더 판단 필요)

| # | 항목 | 영향 | 리더 판단이 필요한 이유 |
|---|---|---|---|
| 1 | **P0-1 (a)안: 창 높이 가변** | 탭 전환에 높이 애니메이션(0.12s) 필요. `PanelSizePoints`를 읽는 테스트/진단이 탭에 따라 다른 값을 본다 | 고정 높이를 전제한 테스트가 몇 건인지 확인 필요. (b)안은 이 영향이 없다 |
| 2 | **P2-15 폰트 파이프라인** | `UiChrome.Font`는 전 창이 쓰는 단일 진입점. 바꾸면 **모든 표면의 글자 폭이 바뀐다** → 고정 폭 라벨 상자 전부 재검산 | 큰 변경. 단독 라운드로 배정할 것. **Windows 실기 없이는 완료 판정 불가** |
| 3 | **§7.2 기울기 vs 글리프 스냅** | 오늘의 pt 스냅 작업과 정면으로 상호작용 | 사용자 신고 사진에 **대사**가 있었는지 확인 필요. 나는 확인 못 했다 |
| 4 | **[✕ 앱 종료]를 [행동] 창에서 빼기** | 이 앱에는 Dock/메뉴바/트레이 아이콘이 없다. 이 버튼이 **마우스만으로 도달하는 유일한 종료 경로**다 | 옮길 곳(설정창)이 마우스로 2단계 더 멀어진다 vs "행동" 의미론. **원칙 4(탈출구)와 직결 — 리더 결정 사항** |
| 5 | **P2-17 단일 펜 2.0** | `EQUIPMENT_SHAPE_SPEC`의 여유 값이 전부 획 단위라 비율은 보존되지만, 절대 여유는 6.5% 줄어든다(최빠듯 1.24 → 1.16 획) | 스펙 하한(1 획) 위지만, `AccessoryCardIconTests`의 단언이 절대값이면 갱신 필요 |
| 6 | **설정창 [▲][▼] 이동** | 탭 줄에서 본문으로 옮기면 `SettingsWindowChromeTests`의 좌표 단언이 바뀐다 | 소소하지만 미리 알린다 |

---

## 10. 플랫폼 영향

- **macOS 영향**: §1~§6, §8 전부 macOS에 그대로 적용된다. 이 문서의 모든 실측은 macOS 15.6 /
  1512×982pt / Retina 배율 2에서 나왔다.
- **Windows 영향**:
  - §2.1-b(합성 볼드 · 한글 폴백 미검증) — **Windows 전용 리스크**다. 배율 1.0에서 12/14pt Bold
    한글이 뭉갤 것으로 예측하지만 **실기 검증 미완**. macOS는 배율 2라 영향 없음.
  - §2.2 타입 스케일은 짝수 pt(10/12/14/20)만 쓰므로 배율 1.5에서 잔차 0 ✔
  - §6.2 호버 이름표 12pt × 1.5 = 18px 정수 ✔
  - §5·§6.1 획 값(2.0pt)은 배율 1.0에서 2px, 1.5에서 3px, 2.0에서 4px — 전부 정수 ✔
  - 그 외 여백/색/형태는 **플랫폼 중립**이다.
  - 이 문서 자체는 설계이므로 프로덕션 코드 변경 0건 = Windows 컴파일 영향 없음.

---

# 11. ★ 심야 실측 재검 — "텍스트가 선명하지 않다"의 물리적 실체 (2026-09-01 심야, ux-designer)

> 사용자 원문: **"텍스트들도 선명하지 않고 깔끔한 게 하나도 없어"**
> 이 절은 그 문장을 **숫자로 옮긴 것**이다. §2.3/§2.4의 처방이 아직 적용되지 않았음을 확인하고,
> **P0 승격**을 요청한다. UX 설계(창 배치·문구)는 `docs/UX_FLOW.md` §41에 있다 — 여기는 **표면**만.

## 11.1 측정 방법 (재현 가능하게 적는다)

- **표본**: 2026-09-01 22:26~22:57 실기 캡처 3장 —
  `minji-settings-win.png` / `minji-03-infowin.png` / `minji-25-action.png`
  (`/tmp/stickmate-run/screenshots/`). macOS 15.6 · 1512×982pt · Retina 배율 2.
- **왜 캡처로 재는가**: CLAUDE.md — *"최종 판정은 실제 빌드 캡처로만 한다."*
  토큰 값을 계산해 얻은 대비는 **합성·알파·배경 텍스처를 통과한 뒤의 실제 대비가 아니다.**
- **정의**:
  - 배경 휘도 = 문장 상자 픽셀의 **하위 25% 휘도의 중앙값**
  - 획심 휘도 = **상위 3% 휘도의 중앙값** (안티에일리어싱 램프를 배제하고 획의 심만 본다)
  - 대비 = `(L_hi + 0.05) / (L_lo + 0.05)` (WCAG 2.x, sRGB 선형화)
- **왜 "최대 픽셀"이 아니라 "상위 3% 중앙값"인가**: 최대값은 단 한 픽셀의 튐에 좌우되고
  얇은 글리프의 대비를 **과대평가**한다. 실제로 같은 표본을 최대 픽셀로 재면 죽은 탭이
  2.84:1로 나오지만, 획심으로 재면 **2.35:1**이다. 후자가 눈이 보는 값에 가깝다.

## 11.2 결과 — 15행 (정렬: 대비 내림차순)

| 표면 · 문장 | 획심 대비 | AA 4.5:1 | 토큰(추정) |
|---|---:|:---:|---|
| 정보창 섹션 제목 `모자` | **18.42** | ✔ | TextPrimary |
| 설정창 본문 라벨 `전체화면 게임 감지 시 자동 숨김` | **17.40** | ✔ | TextPrimary |
| 정보창 카드 이름 `천모자` | **16.78** | ✔ | TextPrimary |
| 행동창 명령 이름 `말 걸기` | **14.98** | ✔ | TextPrimary |
| 설정창 카드 제목 `표시` | 8.99 | ✔ | Accent |
| 설정창 eyebrow | 8.30 | ✔ | TextTertiary |
| 설정창 푸터 `이 창을 여는 방법: …` | 5.62 | ✔ | TextTertiary |
| 행동창 한 줄 설명 · 비침해 캡션 | 5.33 | ✔ | TextTertiary |
| 설정창 캡션 | 5.30 | ✔ | TextTertiary |
| 설정창 비활성 탭 `[캐릭터]` | 4.84 | ✔(턱걸이) | TabInactive |
| 정보창 슬롯코드 `HEAD` | **4.02** | **✘** | TextQuaternary |
| **행동창 푸터 `내가 누를 때만 실행돼요.`** | **3.81** | **✘** | TextQuaternary |
| **행동창 비활성 이유 `지금 붙잡을 만한 작은 창이 없어요`** | **3.51** | **✘** | TextQuaternary (`:639`) |
| **정보창 섹션 카운터 `1 / 6`** | **3.36** | **✘** | TextQuaternary |
| **설정창 비활성 라벨 `숨기기 / 보이기 단축키`** | **2.57** | **✘** | TextDisabled |
| **설정창 죽은 탭 라벨 `이벤트`** | **2.35** | **✘** | TextDisabled |
| **정보창 잠긴 카드 상세 설명** | **2.30** | **✘** | TextDisabled |
| **행동창 비활성 이름 `창 도둑`** | **2.10** | **✘** | TextDisabled (`:627`) |

**§2.3의 예측과 실측 대조** — 예측이 맞았다(오차 ≤ 0.3). 즉 §2.3의 표는 신뢰할 수 있고,
**아직 적용되지 않았다**는 것만 새 사실이다.

| 토큰 | §2.3 예측(카드 위) | 이번 실측 |
|---|---:|---:|
| TextPrimary | 15.00 | 14.98 ~ 18.42 |
| TextTertiary | 5.33 | 5.30 ~ 5.62 |
| TabInactive | 4.16 | 4.84 (패널 위라 조금 높다) |
| TextQuaternary | 3.50 | 3.36 ~ 4.02 |
| TextDisabled | 2.09 | 2.30 ~ 2.57 |

## 11.3 ★ 이 표가 실제로 말하는 것 — "미달 6곳"이 아니다

> **이름은 15~18:1로 빛나고, 그 이름이 무슨 뜻인지 알려 주는 문장은 2.3~3.8:1로 지워져 있다.**
> **배수로 5배.** 위계가 **굵기**가 아니라 **지워짐**으로 표현되고 있다.
> 사용자가 "선명하지 않다 / 깔끔한 게 하나도 없다"고 말한 물리적 실체가 이것이다 —
> **글자 렌더링이 흐린 게 아니라 글자 색이 흐리다.**

렌더링 쪽은 이미 조사가 끝나 있다: `Platform/UiGlyphScalePolicy` 문서 —
*"배율 1.0 / 2.0에서는 모든 정수 pt가 이미 잔차 0이다. 즉 **이 규칙은 macOS에서 아무것도 바꾸지 않는다.**"*
→ **macOS(배율 2)에서 글리프 리샘플 번짐은 0이다. 남는 원인은 색뿐이다.**

### 11.3-b 세 개의 결정적 증거

1. **민지가 "화면에 한 글자도 없음"이라고 적은 죽은 탭에는 글자가 있다.**
   `SettingsWindow.BuildPlaceholderTab()`이 카드 + 라벨 + 캡션을 그린다.
   **2.35 : 1이라 안 읽힌 것이다.** 사용자에게 "안 읽히는 글자"와 "없는 글자"는 같다.
2. **민지가 이 앱에서 제일 잘 썼다고 한 문장이, 앱에서 가장 안 읽히는 글자다.**
   `지금 붙잡을 만한 작은 창이 없어요` = **3.51**, `내가 누를 때만 실행돼요.` = **3.81**.
   → **이 어법을 다른 표면으로 퍼뜨리기 전에 잉크를 고치지 않으면,
   우리는 "안 읽히는 좋은 글"을 앱 전체에 복사하는 것이 된다.**
3. **§2.4의 위계 역전이 실측으로 확인됐다.** 비활성 행에서
   제목 **2.57** < 설명 **5.33**. 사용자는 *"뭔가가 준비 중이구나"*는 읽고 ***"뭐가?"*** 는 못 읽는다.
4. **★ 같은 역전이 [행동] 팝오버에서도 나온다 — 국소 실수가 아니라 앱 전역 규칙의 부재다.**
   `ActionCommandPopover.cs:627/639` — 비활성 타일의 **이름 = TextDisabled(2.10)**,
   **이유 = TextQuaternary(3.51)**. 활성 타일은 이름 14.98 > 설명 5.33으로 **정상**인데,
   비활성이 되는 순간 순서가 뒤집힌다. 두 창이 서로를 모르고 **같은 실수를 독립적으로** 했다
   → 콜사이트를 하나씩 고치면 세 번째 표면에서 또 난다. **§2.4를 앱 전역 규칙으로 못 박아야 한다.**

## 11.4 처방 — §2.3 / §2.4를 그대로 집행한다 (새 처방 아님)

```
TextQuaternary → 폐기. 전 콜사이트 TextTertiary 로              3.36~4.02 → 5.33   ✔ AA
TabInactive    → 폐기. 비활성 탭은 TextTertiary 로               4.84 → 5.33        ✔
TextDisabled   → 글자 금지. 테두리 / 잠금 기호 전용으로 격하
   · 비활성 행 제목  : TextDisabled(2.57) → TextSecondary(7.93)
   · 비활성 행 설명  : TextTertiary(5.33) 유지 — 절대 흐리지 않는다
   · 죽은 탭 버튼    : TextDisabled(2.35) → TextTertiary(5.33)
   · 잠긴 카드 설명  : TextDisabled(2.30) → TextTertiary(5.33)
   · [잠김] 버튼 라벨: TextDisabled → TextTertiary

결과 잉크 4단 : 15.0 → 7.9 → 5.3 → (2.1 = 비텍스트 전용)
배수          : 1.9× / 1.5×   — 균일하고 전부 AA 통과
```

**비활성 규칙(앱 전역, §2.4에서 재확인)**
> 비활성은 **한 단만** 내린다. **행 안의 상대 순서는 절대 뒤집지 않는다.**
> "못 쓴다"는 사실은 **컨트롤(회색 토글)과 행 바탕(`CardSurfaceMuted`)**이 말한다. **글자가 말하지 않는다.**
> **이유 문장은 비활성 행에서 가장 중요한 글자다.**

## 11.5 이 라운드에서 좌표가 바뀌는 표면 (UX_FLOW §41과 쌍을 이룬다)

| 표면 | 지금 | 바꾼 뒤 | 근거 절 |
|---|---|---|---|
| `PopoverPanel` 상단 클램프 | 네 변 균일 12pt | **상단 = 예약인셋 + 12pt** (이 화면 45pt) | §41-1 |
| `InfoGearIconWidget.MarginTopPoints` | **58f 하드코딩** | 정책 유도값 **64.8pt** | §41-1 / §41-8 |
| `InfoGearIconWidget.ClampCenterPoints` `minY` | **19.8pt** (메뉴바 안까지 허용) | **64.8pt** | §41-8 |
| `SettingsControls.CardWidth` | 680 | **656** (오른쪽 스크롤 레일 20pt 확보, 좌우 여백 대칭 20/20) | §41-2 |
| `SettingsWindow.FooterHeight` | 34 | **46** (2단: 텍스트 줄 18 + 버튼 줄 28) | §41-2 |
| `SettingsWindow.ContentHeight` | 438 | **426** | §41-2 |
| 설정창 `[▲][▼]` | 탭바 오른쪽 끝 (탭과 같은 줄) | **본문 오른쪽 세로 레일** x 680~700, 트랙 x687 w6 y112~490, 썸 최소 32pt | §41-2 |
| 정보창 섹션 카운터 상자 | x 548 w 44 · T4 | **x 528 w 64 · T3**, 구분선 폭 402 → 382 | §41-9 |
| 정보창 패널 성장 축 | 피벗 0.5 (상하 균등, 탭 전환 시 상단 78pt 이동) | **상단 모서리 고정**(`anchoredPosition.y += δ/2`) | §41-7 |
| 팝오버/창 3종 타이틀 줄 | 제목 + `[✕]` | 사이에 닫기 힌트 1줄 (Caption 10, T3, 우측정렬) | §41-3 |

**폰트 pt는 하나도 안 바꾼다** — 새로 쓰는 글자는 전부 기존 `FontCaption 10` / `FontBody 12`.
짝수 pt라 배율 1.5(Windows 150%)에서도 잔차 0 (`UiGlyphScalePolicy`).

## 11.6 우선순위 정정 요청 — 리더 배정용

| 지금 | 바꿔 달라 | 사유 |
|---|---|---|
| §2.3 잉크 6단→4단 = **P2-11** | **P0** | 사용자 원문 불만의 **직접 원인**. 실측 15행. 이 라운드 나머지 9건이 전부 그 위에 얹힌다 |
| §2.4 비활성 위계 역전 = **P1-7** | **P0** | 죽은 탭 결함(§41-6)이 **문구 결함이 아니라 잉크 결함**임이 실측으로 확정됐다 |

**작업량**: 색 토큰 교체 = **콜사이트 치환**이다(TextQuaternary 6곳 · TabInactive 2곳 · TextDisabled 글자 3곳).
레이아웃 좌표 변경 0. 폰트 변경 0. **P0 중에서 가장 싸고 가장 넓게 듣는다.**

## 11.7 플랫폼 영향

- **macOS 영향**: §11 전부. 모든 실측이 macOS 15.6 / 배율 2 캡처다.
- **Windows 영향**: **별도 배정 필요**(사용자 지시로 이번 라운드 macOS 집중).
  - 색 토큰은 플랫폼 중립이므로 대비 비율은 **같아야 한다** — 그러나 **Windows 실기 대조는 미완, 미확인.**
  - Windows 배율 1.5에서는 §2.1-b(합성 볼드 · 한글 폴백)가 별개 리스크로 남아 있다. 이 절은 그것을 안 건드린다.
  - §11.5의 좌표 변경은 pt 단위라 배율 무관. `CardWidth 656 × 1.5 = 984px` 정수 ✔ / `FooterHeight 46 × 1.5 = 69px` 정수 ✔
- 이 절은 **설계·측정**이다. 프로덕션 `.cs` 변경 0건 → 양 플랫폼 컴파일 영향 없음.


---

# 12. ★ 설정창 탭바 — 미구현 탭 배지 조형 (2026-09-02, ux-designer)

> **UX 판단·근거는 `docs/UX_FLOW.md` §43에 있다. 이 절은 조형 수치만 담는다** — §11.5와 같은 관계다.
> **macOS 집중(사용자 지시). Windows 미확인 1건은 §12.5.**

## 12.1 무엇을 더하는가 (한 줄)

미구현 탭(`IsTabReady == false`) 3개의 **라벨 오른쪽에 10pt 보조 어절 `준비 중`을 붙인다.**
**새 색 0개. 새 폰트 pt 0개. 잉크 사다리 무변경.** 배지는 기호가 아니라 **글자**다.

**왜 기호가 아닌가** — 이 앱에서 `UiChrome.NonTextMuted`의 세 용법 중 **둘이 이미 발행됐다**:

| 기호 | 이미 뜻하는 것 | 출처 |
|---|---|---|
| **자물쇠** | **레벨 잠김 — "놀면 열린다"** | `CharacterInfoWindow.BuildLockGlyph` + `Lv.{n}에 열림` |
| **도트 ●** | 보관함 행의 **의미 없는 불릿**(+ 탭바 관례상 "새 것/안 읽음") | `CharacterInfoWindow` `"Dot"`, `UiChrome.RadiusDot` |
| 점선 원 | **FX `없음`** | `CharacterInfoWindow.BuildDashedRing` — 호 8 × 점 3, **8pt에서 붕괴**(커밋 `39ab690`) |

미구현 탭에 자물쇠를 붙이면 *"레벨을 올리면 열린다"*는 **거짓 약속**이 된다.
**기각 사유는 대비가 아니라 의미 충돌이다**(대비는 셋 다 통과한다 — §12.4).

## 12.2 치수 — 전부 상수에서 파생시킨다

```
BadgeText   = "준비 중"                                        Length = 4 (공백 포함)
BadgeWidth  = BadgeText.Length × UiChrome.FontCaption  = 40pt   ★ 숫자를 손으로 적지 않는다
BadgeGap    = UiChrome.Space2                          =  8pt
BadgeHeight = 14pt   (FontCaption 10의 행 상자 — BeginRow의 캡션 상자와 같은 값)
잉크        = UiChrome.InkMeta      폰트 = UiChrome.FontCaption      정렬 = MiddleLeft

TabWidth(i) = 20 + TabNames[i].Length × 11                       (준비된 탭 — 무변경)
            = 20 + TabNames[i].Length × 11 + BadgeGap + BadgeWidth   (미구현 탭 = +48pt)
```

**폭 근거**: `SettingsControls`의 세그먼트 주석이 *"한글은 폭이 pt에 가깝다"*고 적어 둔 그 근거를
그대로 쓴다. 실측 모델(한글 10pt / 공백 3pt)로 `준비 중` = **33pt** → 40pt 상자에 **여유 7pt(21%)**.

## 12.3 좌표 — 탭바 전체 (`PanelWidth 720`, `HeaderHeight 48`, `TabBarHeight 40`)

| 탭 | `Length` | 지금 폭 | 새 폭 | x 범위 | 라벨 상자 | 배지 상자 |
|---|---|---|---|---|---|---|
| 일반 | 2 | 42 | **42** | 20 – 62 | 10, w22 | — |
| 캐릭터 | 3 | 53 | **53** | 66 – 119 | 10, w33 | — |
| 이벤트 | 3 | 53 | **101** | 123 – 224 | 10, w33 | **51, w40** |
| 접근성 · 성능 | 8 | 108 | **156** | 228 – 384 | 10, w88 | **106, w40** |
| 데이터 | 3 | 53 | **101** | 388 – 489 | 10, w33 | **51, w40** |

```
검산  시작 x = ContentPadX 20,  탭 간격 = UiChrome.Space1 4
      20 +42 +4 +53 +4 +101 +4 +156 +4 +101 = 489      새 우측단 489pt
      (옛 우측단 = …+53 = 345 — 코드 주석 "탭 5개가 x 20~345"와 일치 ✔)
      [▲][▼] 칩 = x 646 ~ 700          489 < 646  →  여유 157pt ✔  칩 불가침(소은 #7-b)
```

**탭 안쪽 (탭 rect 좌상단 기준, `PlaceTopLeft`)**

```
 ┌──────────────────────────────────────────────┐  y = 0
 │      ┌────────────┐  ┌──────────────────┐    │
 │ 10pt │   이벤트    │8 │     준비 중       │10pt│   라벨 h16 @ y=−12 → 중심 −20
 │      └────────────┘  └──────────────────┘    │   배지 h14 @ y=−13 → 중심 −20
 │      x=10 w=33        x=51 w=40               │   ★ 두 중심이 탭바 정중앙에서 일치
 ├══════════════════════════════════════════════┤  y = −38,  h = 2  (밑줄, 전폭)
 └──────────────────────────────────────────────┘  y = −40
```

- **라벨 상자는 `TextAnchor.MiddleCenter`를 유지한다.** `x=10, w=Length×11`은 지금 Stretch 상자
  (`w = 20 + Length×11`)의 **정중앙 구간과 같다** → **라벨 렌더 결과가 1pt도 안 움직인다.**
  왼쪽 정렬로 바꾸면 `11f` 근사 오차(1~3pt)만큼 밀린다. **바꾸지 않는다.**
- 밑줄은 지금대로 **탭 전폭**이다(미구현 탭에서 101/156pt로 길어진다 — 밑줄은 탭의 범위를 그린다).
- `AddHitArea`가 rect 전체를 덮으므로 **클릭 타깃이 53 → 101pt로 커진다**(Fitts 이득).
  **배지는 탭을 잠그지 않는다.**

## 12.4 대비 — 적용 하한이 무엇인지 먼저 정한다

**배경은 `UiChrome.PanelSurface` 하나다** — 탭바는 `_panel`의 직계 자식이고 사이에 카드가 없다.
값은 `UiChrome.ContrastRatio`(WCAG 2.2 상대 휘도)로 계산했다.

| 잉크 | on `PanelSurface` | on `CardSurface`(최악) | 적용 하한 | 판정 |
|---|---|---|---|---|
| **`InkMeta`(T3) — 확정 배지** | **5.79 : 1** | 5.32 : 1 | `UiChrome.MinTextContrast` **4.5** | ✔ 여유 **+1.29** |
| `NonTextMuted` — 기각된 도트/자물쇠 | 3.81 : 1 | 3.50 : 1 | `UiChrome.MinNonTextContrast` **3.0** | ✔ **대비는 통과. 기각 사유는 §12.1의 의미 충돌** |
| `Accent` — 활성 밑줄 | 6.74 : 1 | 6.19 : 1 | 3.0 (비텍스트) | ✔ |
| `NonTextMuted` — 선택+미구현 밑줄 | 3.81 : 1 | — | 3.0 (비텍스트) | ✔ (현행 유지) |
| `RetiredInk.TabInactive` — 폐기 | 4.52 : 1 | **4.15 : 1** | 4.5 | ✘ **패널은 통과, 카드는 미달** — `TextBackdrops`가 왜 목록이어야 하는지의 증거 |
| `DisabledControlInk` — 글자 금지 | 2.28 : 1 | 2.09 : 1 | 4.5 | ✘ (`WarnIfNonTextInk`가 이미 막는다) |

- **배지는 10pt다.** WCAG 2.2의 "큰 글자 3:1" 예외는 **18pt 이상**(굵으면 14pt)이므로 적용되지
  않는다 → **4.5:1이 맞다.**
- **테스트는 위 숫자를 베끼지 않는다** — `UiInkHierarchyTests`의 방식대로
  `UiChrome.MinTextContrast` / `UiChrome.ContrastRatio(...)`를 **참조**한다.

**4가지 상태에서 위계가 안 뒤집힌다**

| 상태 | 라벨 | 배지 | 판정 |
|---|---|---|---|
| 선택 · 준비됨 | T1 16.32 (Bold) | — | — |
| 선택 · 미구현 | T2 8.63 (Bold) | T3 5.79 | 한 단 차 ✔ |
| 미선택 · 준비됨 | T3 5.79 | — | — |
| 미선택 · 미구현 | T3 5.79 | T3 5.79 | **같은 단**(역전 아님). 미선택 탭 라벨은 `InkTab`상 Meta 단이고 Meta는 사다리 바닥이라 더 내릴 곳이 없다 — §2.4 규칙 그대로. 종속은 **글자 크기 12 → 10**이 만든다(선례: 보관함 행 `Subtitle`) |

**`UiChrome.InkTab`은 한 줄도 안 바꾼다. 사다리는 3단 그대로다.**

## 12.5 플랫폼 영향

- **macOS 영향**: §12 전부. 검산 기준 macOS 15.6 / `PanelWidth 720` / 01:26 빌드.
- **Windows 영향 — 없음(플랫폼 중립).** 근거:
  - 새 수치 `BadgeWidth 40` / `BadgeGap 8` / `BadgeHeight 14` / `FontCaption 10` / `TabBarHeight 40`
    **전부 짝수** → 배율 1.5에서 60 / 12 / 21 / 15 / 60 px. `BadgeHeight 14 × 1.5 = 21` 정수 ✔,
    `FontCaption 10 × 1.5 = 15` 정수 ✔ (`UiGlyphScalePolicy` 잔차 0)
  - 새 우측단 `489 × 1.5 = 733.5` — **좌표는 pt로 계산되고 캔버스가 한 번에 스케일하므로 무해**하다
    (개별 요소가 반픽셀에 앉지 않는다).
  - **★ 미확인 1건 — 별도 배정 필요**: `BadgeWidth 40`은 **macOS 폰트 메트릭 기준 근사**다.
    Windows 한글 폴백에서 자간이 넓으면 `MiddleLeft` 배지가 40pt를 넘어 **다음 탭 시작점(간격 4pt)을
    침범**한다. 캡션은 넘쳐도 흐르기만 하지만 **배지는 이웃을 친다.** Windows 실기 캡처로 재확인해야
    한다(사용자 지시로 이번 라운드는 macOS만).
- 이 절은 **설계**다. 프로덕션 `.cs` 변경 0건 → 양 플랫폼 컴파일 영향 없음.

---

# 13. 만화 레터링 분리막 — 배율별 확정 스펙 (2026-09-02, ux-designer)

> **이 절은 §7.3을 대체한다.** §7.3은 외곽선 두께를 `max(0.4, fontSize × 0.06)`으로 적었는데
> 그 `0.06`은 **더 이상 존재하지 않는 값**이다(리터럴이었고, 2026-09-01에 실측 유도식
> `NarrowestHangulCounterEmRatio × CounterClosureBudget / 2 = 0.04068`로 대체됐다).
> §7.3의 표(21pt→1.26pt / 14pt→0.84pt)와 제안(`MinTextOutlineThickness 0.4 → 0.7`)은
> **폐기한다** — 아래 §13.1의 예산식이 그 제안이 풀려던 문제를 다른 방식으로 푼다.
>
> 판정 근거와 유도 전문은 `docs/UX_FLOW.md` §44-1 / §44-2. 여기에는 **구현이 참조할 수치**만 둔다.

## 13.1 예산식 — 이 절의 모든 수치가 나오는 한 줄

```
0.113 × em_물리px  =  막두께 × N  +  남는구멍
                       └ N = 2 (4방향 링) / 1 (1방향 그림자)

  0.113 = NarrowestHangulCounterEmRatio  (실측: Apple SD Gothic Neo Bold, 400px em, 10퍼센타일 중앙값)
  막두께 = fontSize × TextOutlineEmRatio × 배율        (링)
         = fontSize × TextOutlineEmRatio × 2 × 배율     (그림자 — 같은 예산을 한쪽에 몰아준다)
```

**합격선 두 개**(이름을 붙여 둔다 — 안 붙이면 판정이 섞인다. 실제로 섞여 있었다)

| | 정의 | 상수 |
|---|---|---|
| **C1 분리막 가시성** | 막 ≥ **1.000** 물리픽셀 | `OutlineRingMinPhysicalPixels` |
| **C2 속공간 개방** | 남는 구멍 ≥ **0.823** 물리픽셀 | `CounterClosureBudget 0.72`를 검증 운용점에서 환산 |

**검증 운용점**(실제 캡처로 판정된 유일한 조합) = 배율 2.00 · 13pt · em 26물리px:
`0.113 × 26 = 2.938 = 1.058 × 2 + 0.823` ✔

> ★ **링으로 C1·C2를 동시에 만족하려면 `0.113 × em_물리px ≥ 3` → em ≥ 26.5 물리픽셀이 필요하다.**
> 우리가 출하하는 다섯 배율 중 이 선을 넘는 칸은 **하나도 없다.** 배율 2.00이 26.0으로 0.5px
> 모자라고, `CounterClosureBudget`이 1.0이 아니라 0.72인 이유가 정확히 그 0.5px이다.

## 13.2 분기 규칙 (확정)

```
useRing  ⟺  fontSize ≥ OutlineLegibleFontFloor(canvasScale)
             (= 링의 막이 1물리픽셀을 채우는가. 이미 있는 함수 하나로 판정된다)

useRing == true   →  Outline, effectDistance = (t, t),      t = fontSize × 0.04068
useRing == false  →  Shadow,  effectDistance = (2t, −2t),   ★ 반드시 대각선. 축정렬 금지
```

**왜 반드시 대각선인가 (검산)** — 한글 획은 축정렬 직사각형이라, 대각선 오프셋만이 **획의
종횡비와 무관하게 정확히 둘레의 50%** 를 보호한다.

| 그림자 방향 | 세로 획(0.10×0.70em) | 가로 획(0.60×0.10em) |
|---|---|---|
| **대각선 (t, −t)** | **50.0%** | **50.0%** |
| 축정렬 (t, 0) | 43.8% | **7.1%** |
| 축정렬 (0, −t) | **6.3%** | 42.9% |

**회전** — `Shadow`/`Outline`은 `BaseMeshEffect`라 **Graphic 로컬 좌표계**에서 정점을 옮기고,
그 좌표계는 `_panel.localRotation`(기울기 6~10도)을 상속한다. → **그림자가 글자와 함께 돈다.**
보호 기하는 **회전 불변**이고 화면상 광원 방향만 20도 흔들린다(전부 한 사분면 안).
★ **화면 고정으로 만들지 말 것** — `effectDistance`에 `R(−θ)`를 매 프레임 곱하면 텍스트 메시가
매 프레임 재빌드되고, 화면 끝 좌우 뒤집기(`SideFlipSpeed 5f`) 중에는 약 0.4초 동안 계속 그렇게 된다.

**★ 상수 주석 정정 요청(리더 → coder)** — `OutlineRingMinPhysicalPixels`의 현재 주석
> *"1보다 작으면 GPU가 부분 커버리지로 섞어 **반투명 얼룩**이 되고, 그건 분리막이 되지 못한다"*

는 **대비 측면에서 틀렸다.** 0.529 커버리지의 흰 띠는 검은 배경 위에서 휘도 ≈0.53 →
대비 **11.6 : 1**로 또렷이 보인다. 서브픽셀 막이 실제로 실패하는 이유는 **위상 의존성**이다 —
같은 획인데 위치에 따라 53%로 뭉치거나 26%+27%로 두 픽셀에 갈라지고, 블록이 기울어 있어
**한 획의 길이를 따라 위상이 연속으로 변한다**(밝아졌다 어두워졌다 한다).
근거를 바꿔 적어야 다음 사람이 "그럼 0.9px은 되지 않나"로 잘못 완화하지 않는다.

## 13.3 배율별 확정 표 (§44-1 + §44-2 동시 적용 후)

| 캔버스 배율 | 환경 | 폰트 | 링 요구 pt | **분기** | 막(물리px) | 구멍(물리px) | C1 | C2 |
|---|---|---|---|---|---|---|---|---|
| **2.00** | macOS Retina · Win 200% | 13 | 13 | **링** | 1.058 | 0.823 | ✔ | ✔ |
| 1.75 | Win 175% | **16** | 15 | **링** | 1.139 | 0.886 | ✔ | ✔ |
| 1.50 | Win 150% | 14 | 17 | 그림자 | 1.709 | 0.664 | ✔ | ✘ |
| 1.25 | Win 125% | **16** | 20 | 그림자 | 1.627 | 0.633 | ✔ | ✘ |
| 1.00 | Win 100% · 비Retina | 13 | 25 | 그림자 | 1.058 | 0.411 | ✔ | ✘ |

- **볼드 폰트값**은 §44-2 (b′)(하한 이상 중 가장 가까운 잔차 0)이 바꾸는 칸이다.
- **macOS는 항상 2.00 한 칸**이고 링 분기다 → **§13 전체가 macOS 화면을 한 픽셀도 안 바꾼다.**
  단 경계가 등식(13 ≥ 13, 여유 0pt)이라 **회귀 테스트 1건이 필요하다**(UX_FLOW §44-1 ⑦).
- **C2 미달 3칸은 폰트를 18pt 이상으로 올려야만 풀린다.** 그러면 배율 0.60 캐릭터에서 대사
  블록이 키의 157%가 되어 캐릭터를 삼킨다 → **하지 않는다.** `Assert.Ignore`를 **C2 전용 사유로
  다시 쓴다**(C1 사유는 이 스펙으로 해소된다).

## 13.4 묻히는 조건 — 그림자 분기에서 무엇을 잃는가

링은 잉크↔막 경계에서 **21:1을 무조건** 보장했다(바탕화면을 몰라도 됐다). 그림자는 둘레의 50%만
보장하고, 나머지 50%는 `CR(잉크, 바탕화면)`이다. 잉크가 순검정/순백이므로 조건이 닫힌 식이다.

```
검은 잉크 : CR = 20·L_bg + 1        흰 잉크 : CR = 1.05 / (L_bg + 0.05)
```

| 판정선 | 검은 잉크가 위험한 바탕화면 | 흰 잉크가 위험한 바탕화면 |
|---|---|---|
| **T3 5.33:1** 미만(§11.2 기준) | `#808080`보다 **어두우면** | `#6b6b6b`보다 **밝으면** |
| **AA 4.5:1** 미만 | `#747474`보다 어두우면 | `#777777`보다 밝으면 |
| **실루엣 소실 1.5:1 미만** | **`#2c2c2c`보다 어두우면** | **`#d3d3d3`보다 밝으면** |

> **중요한 줄은 4.5:1이 아니라 1.5:1이다.** 획은 두 변이 다 보여야 읽히는 게 아니라 **한 변만
> 검출돼도 검출된다.** 4.5:1 아래에서 벌어지는 일은 "글자 소실"이 아니라 **"획이 배경 쪽으로
> 번지고 굵기가 불확정해지는 것"**(지각 중심이 t/2 이동). 한글 자모는 굵기가 아니라 획의 개수와
> 위치로 구별되므로 식별은 유지된다. **진짜 실패는 `CR ≈ 1` 구간**이며, 거기서는 글자가
> **한쪽 rim만 남은 반쪽 윤곽선**이 된다. → 미확인 V2(UX_FLOW §44-4).

## 13.5 채택하지 않은 대안 (근거와 함께 남긴다 — 다시 제안되지 않도록)

`Shadow`/`Outline`이 하는 일은 오프셋 집합 `S`만큼의 팽창이고, 한 축의 속공간 소모는
`max(S ∪ {0}) − min(S ∪ {0})`이다. 여기서 **`t`와 `2t` 사이의 값은 나오지 않는다.**

| 대안 | x 소모 | y 소모 | 판정 |
|---|---|---|---|
| 2방향 · 마주보는 대각선 | **2t** | **2t** | **기각 — 절약이 0이다.** 가장 흔한 오해 |
| 2방향 · 인접(같은 x부호) | t | **2t** | **기각.** `0.113em`은 ㅁ·ㅇ의 **가로·세로 런 전수조사** 값이라 두 축이 모두 구속 조건이다 → 실효 절약 0 |
| 3방향 | 2t | 2t | 기각 |
| **비대칭 링**(`Shadow` 2개 스택, `t_f : t_b = 2 : 1`) | 1.5t | 1.5t | **기각.** 보존식 `소모 ≥ 2 × 보증막` 때문에 배율 1에서 **19pt**를 부른다. 구현은 가능하다(복제 4개로 `Outline`과 동수) |
| 말풍선 판 복원 | 0 | 0 | **범위 밖.** 2026-09-01 사용자 확정 결정(만화 레터링)을 뒤집는 일이라 UX 단독으로 정하지 않는다 |

> **보존식**: 앞 막 `t_f`, 뒤 막 `t_b`일 때 `소모 = t_f + t_b`, `보증막 = min(t_f, t_b)` →
> **`소모 ≥ 2 × 보증막`.** 모든 변에서 1물리픽셀을 보증하려면 속공간 2물리픽셀은 **원리적으로**
> 피할 수 없다. 링이 비싼 것은 설계가 나빠서가 아니라 이 부등식 때문이고, 그림자가 싼 것은
> **보증막을 0으로 놓아서**다. 공짜가 아니라 교환이다.

## 13.6 §7.2(기울기가 글리프 정밀도를 무효화한다)에 대한 답

§7.2가 *"미확인 · 조사 필요"* 로 남긴 질문에 이 라운드의 오프라인 A/B가 이미 답했다
(`DialogueBubbleRenderer` 파일 머리 실측):

```
기울기 9.1도 → 0도  : 전이 픽셀 −9%,   속공간 열림 0.000 → 0.000  (변화 없음)
외곽선 0.84 → 0.4pt : 전이 픽셀 −42%,  속공간 열림 0.000 → 0.186
```

→ **기울기는 뭉갬의 원인이 아니다.** §7.2가 제안한 `ComicTiltMinGlyphPixels 14 → 24`는
**채택하지 않는다** — 그 처방은 배율 1.50에서도 기울기를 꺼서(21물리px < 24) 사용자 요구
*"대각선으로 작성해줘"* 를 Windows 전체에서 무효화한다. 기울기가 남기는 것은 **에지 폭 9%**이고,
그건 C1/C2 어느 합격선도 건드리지 않는다.

★ **다만 §7.2가 건드린 진짜 문제는 따로 있다**: 배율 **1.00에서는 기울기가 이미 꺼져 있다**
(13pt × 1.00 = 13물리px < 14). **Windows 100% 사용자는 이 앱의 대각선 레터링을 한 번도 본 적이
없고, 그 사실이 어느 문서에도 적혀 있지 않았다.** → 미확인 V1(UX_FLOW §44-4), **design-motion 배정 요청.**

## 13.7 플랫폼 영향

- **Windows 영향 — 함께 수정함(설계).** §13 전체가 Windows 배율 1.00~1.75를 대상으로 한다.
  **C1이 다섯 배율 전부에서 처음으로 충족된다**(§13.3).
- **macOS 영향 — 없음.** 배율 2.00은 링 분기이고, 모든 정수 pt의 잔차가 0이라 스냅이 항등이다.
  **화면이 한 픽셀도 안 바뀐다.** 단 분기 경계가 등식이라 회귀 1건이 필요하다.
- **새 플랫폼 분기 0건.** `useRing` 판정은 `canvasScale` 하나만 보는 순수 함수이며 기존
  `Dialogue` static 유틸 옆에 산다 — 정책을 `Platform/MacOS/`에 넣어 Windows가 물리적으로 못 부르던
  `FullscreenSuspendPolicy.cs` 사고를 반복하지 않는다.
- **이 절은 설계다. 프로덕션 `.cs` 변경 0건 → 이 라운드의 양 플랫폼 컴파일 영향 없음.**
  구현이 배정되면 `-define:UNITY_STANDALONE_WIN` / `-define:UNITY_STANDALONE_OSX` 양쪽 0에러 확인 필요.

---

# 14. ★ 캐릭터창 좌측 컬럼 — 액자를 "종이인형"으로 확정 + 보관함 레일 (2026-09-02, ux-designer)

> 판정 근거·실측 절차·반대 의견 반박은 **`docs/UX_FLOW.md` §45**에 있다. 여기에는 **표면 스펙**
> (좌표 · 치수 · 색 · 상태)만 적는다.
>
> **프로덕션 `.cs` 변경 0건.** 이 절은 설계다.

## 14.1 액자 — 무엇이 표면을 바꾸는가 (확정 표)

**【액자 불변식】** 액자 속 그림은 **네 가지**가 바뀔 때만 다시 그려진다.
**캐릭터의 상태(무엇을 하고 있는가)는 액자에 도달하지 않는다. 시간은 액자 안에서 흐르지 않는다.**

| # | 입력 | 지금 | 확정 | 출처 |
|---|---|:---:|:---:|---|
| 1 | 착용 장비 (`EquipmentModel.WornStateSignature`) | 다시 그림 | **다시 그림** | `ComputeSignature()` |
| 2 | 해금 마스크 | 다시 그림 | **다시 그림** | 〃 |
| 3 | 잉크색 (`ResolveInk()`) | 다시 그림 | **다시 그림** | 〃 |
| 4 | 캐릭터 키 (`TotalHeight`) | 다시 그림 | **다시 그림** | 〃 |
| 5 | **상태 버킷 (`_pose`)** | **다시 그림** | **★ 무시** | 〃 — **이 한 줄이 신고의 원인** |
| 6 | **시간 (`Time.unscaledTime`)** | **매 프레임 1.90 pt 상하** | **★ 무시** | `ApplyBreathing()` |

## 14.2 액자 좌표 — 상수에서 유도 (변경 없음, 기록용)

```
LeftWidth 244 · LeftPadX 20 · LeftContentWidth 204
액자      PortraitY −83 · 204 × PortraitHeight 196 · RadiusCard 8
패딩      PortraitPadding 8   → 내용(RawImage) 188 × 180
테두리    AddOutline(CardBorder, r8)   ※ 흰 잉크 프리셋에서는 rgba(1,1,1,0.18)
바탕      ResolveBackdropColor: 검은 잉크 → UiChrome.PortraitSurface / 흰 잉크 → rgb(0.145,0.157,0.180)
RT        표시 물리픽셀 × Supersample 2, 한 변 상한 2048
```

**카메라(액자) 유도** — 숫자를 옮겨 적지 않고 식을 남긴다:

```
HeadRadiusInHeight  = 0.22 / 2.2746944                        =  0.0967163
FrameInkTopRatio    = (1 − 0.0967163) + 1.80 × 0.0967163      =  1.0773730
FrameInkBottomRatio = −LegWidthRatio ÷ 2
                    = −(0.12 × 1.045 / 2.2746944) ÷ 2         = −0.0275642
FrameOrthoRatio     = (1.0773730 + 0.0275642) × 1.05 ÷ 2      =  0.5800921
가시 세로            = 2 × FrameOrthoRatio × H                 =  1.1601841 H
────────────────────────────────────────────────────────────────────────────
∴ 환산 계수          = 180 pt ÷ 1.1601841                      =  155.15 pt / H
```

이 계수 하나로 §45의 모든 실측이 나온다:

| 항목 | H 배수 | 계산 pt | 물리px(Retina) | **실측 pt** | 일치 |
|---|---|---|---|---|---|
| 호흡 peak-to-peak | 2 × 0.006 = 0.012 | **1.862** | 3.724 | **1.898** | 101.9 % |
| Standing→Busy 앞팔 끝 | 0.1372 | **21.3** | 42.6 | — | 미측정 |
| Standing→Busy 뒷팔 끝 | 0.4582 | **71.1** | 142.2 | **70.9 / 69.4** | 99.7 % / 97.6 % |
| Standing→Busy 실루엣 폭 | — | — | — | **+25.0** | 캡처 실측 |

> **실측 / 예측 = 101.9 %.** 잉크 마스크에서 **정지한 테두리 링(약 2,900 물리px)을 반드시 제외**할 것 —
> 포함하면 중심이 붙들려 **11.5% 과소평가**된다(첫 측정이 88.5%였던 이유).

## 14.3 액자 상태표 (예외 포함)

| 상태 | 액자가 보여주는 것 | 폴백 문구 | 비고 |
|---|---|---|---|
| 정상 | 정지한 정면 종이인형 | — | **모든 캐릭터 상태에서 동일** |
| 장비 0개 | 맨몸 종이인형 | — | 올바른 빈 상태. 빈 상자가 아니다 |
| RT 생성 실패 | 빈 액자 | `미리보기를 그릴 수 없어요` | **빈 액자의 유일한 뜻이 된다** |
| 도형 예외 | 실패한 묶음만 빠짐 | — | `[초상화]` 에러 로그. 2026-09-01 방어선 유지 |
| 헤드리스 | 카메라 미가동 | 폴백 문구 | `GraphicsDeviceType.Null` 가드 유지 |
| **가출** | **★ 종이인형 그대로** | — | **결정 변경 요청**(UX_FLOW §45-5-a). 재실은 프레즌스 줄이 말한다 |
| 전체화면 감지 | **창 자체가 닫힘** | — | 원칙 2. 변경 없음 |

**잉크는 어떤 상태에서도 흐리게 하지 않는다** — 흐리면 "이 장비 색이 이렇게 보이는구나"라는
액자의 유일한 임무가 훼손된다.

## 14.4 프레즌스 줄 — 표면 스펙

```
좌표      LeftPadX 20 · PresenceY −297 · 204 × 15
글자      FontLabel 12 · MiddleLeft · TextTertiary
형식      "지금  ·  {StateLabel(id)}"          ※ 28행, 4버킷보다 정확하다
```

| 항목 | 지금(실측) | 확정 |
|---|---|---|
| 갱신 빈도 | **분당 17.4 ~ 21.7회** (평균 2.8~3.4초 간격) | hold 적용 후 폭주 구간 **4회 → 2회** |
| 최단 노출 | **0.22초** — 앱 자신의 가독예산 **0.62초** 미달 | `T_hold` = 가독예산(재사용, **새 상수 금지**) |
| 갱신 규칙 | 상태가 바뀌면 즉시 | **즉시 쓰되 `T_hold` 동안 고정, 만료 시 현재 상태 재조회** |
| 가출 문구 | `지금  ·  가출 중` (86 pt) | `지금  ·  가출 중  ·  숨었어요` (**148 pt** ≤ 204, 여유 56) |

> ⚠ **미확인**: 가출 문구 폭은 CJK advance = 1.0 em 가정. **실제 빌드 캡처 1장 필요.**

## 14.5 보관함 레일 — 좌표 유도 + 확정 스펙

```
rowStep     = InventoryRowHeight 24 + InventoryRowGap 3            = 27
listHeight  = InventoryVisibleRows 20 × 27 − 3                     = 537
InventoryListWidth = RightContentWidth 592 − RailWidth 24 − Space2 8 = 560
railX       = RightPadX 22 + 560 + Space2 8                        = 590
줄 수       = ItemCatalog.Count 55 (장비 42 + 행동 13) + 헤더 2     = 57
페이지 수    = ceil(57 / 20)                                        = 3   ← 신고된 "1 / 3"과 일치 ✔
최대 스크롤  = 57 − 20                                              = 37
```

### 지금

```
        x=590
 y=-72  ┌────┐   ▲   24×24 · CardSurface · CardBorder · TextTertiary
        └────┘        ★ 1페이지에서도 ▼와 픽셀 단위로 동일. 눌러도 조용히 아무 일 없음
 y=-104 ┌────┐
        │    │
        │  1 │   ← 24 × 473 상자의 MiddleCenter.
        │  / │      FontCaption 10 · 3줄 ≈ 34.5 pt가 ★ 허공에 뜬다
        │  3 │      (▲에서 219 pt, ▼에서 227 pt 떨어짐)
        │    │      ★ 세로로 쌓인 1/3은 "3 중 1"이 아니라 분수 ⅓으로 읽힌다
        └────┘
 y=-577
 y=-585 ┌────┐   ▼   24×24 · 위와 완전히 동일
        └────┘
 y=-609
```

### 확정

```
        x=590
 y=-72  ┌────┐   ▲   24×24
        └────┘        면 CardSurface(고정) / 테두리·글리프는 CanScroll(−1)에서 파생
 y=-104 │1/3 │        24 × 16 · FontCaption 10 · MiddleCenter · InkMeta
        └────┘        가로 한 줄. ▲ 바로 아래에 붙는다(누르면 이 숫자가 준다는 인과)
        ┊
        ┊  (441 pt 공백 — 이번 라운드에서 건드리지 않는다. 트랙+썸 도입은 별건)
        ┊
 y=-585 ┌────┐   ▼   24×24
        └────┘        테두리·글리프는 CanScroll(+1)에서 파생
 y=-609
```

**가로 폭 검산** (`FontCaption 10`, Arial: 숫자 0.556 em = 5.56 pt / `/` 0.278 em = 2.78 pt /
공백 0.278 em = 2.78 pt):

| 문자열 | 계산 | 폭 | 레일 24 pt |
|---|---|---|:---:|
| `1/3` | 5.56 + 2.78 + 5.56 | **13.90 pt** | ✔ (여백 5.05 ×2) |
| `1 / 3` | 5.56 + 2.78 + 2.78 + 2.78 + 5.56 | **19.46 pt** | ✔ (여백 2.27 ×2) |
| `1/10` (여유 확인) | 5.56 + 2.78 + 5.56 + 5.56 | **19.46 pt** | ✔ |

> ⚠ **미확인**: advance 0.556 em은 Arial/Helvetica 표준값 가정이며, 좌표는 **전부 상수에서 유도**했다.
> 이번 세션에서는 클릭을 주입할 수 없어 **[보관함] 탭을 실제로 보지 못했다.**
> **보관함 탭 캡처 1장이 필요하다**(이 저장소 규칙: 최종 판정은 실제 빌드 캡처로만).

### 14.5-a 페이저 칩 상태색 — **설정창에서 그대로 베끼면 안 된다**

| 요소 | 활성 | 비활성 |
|---|---|---|
| 면 | `CardSurface` (**변하지 않는다**) | `CardSurface` |
| 테두리 | `Flatten(CardBorder, **CardSurface**)` | `Flatten(Divider, **CardSurface**)` |
| 글리프 | `InkIcon(true)` = `IconInk` | `InkIcon(false)` = `TextTertiary` |

> ★ **합성 바탕이 설정창과 다르다.** 설정창 칩의 면은 `CardSurfaceMuted` rgb(0.082,0.094,0.118)이고
> 보관함 칩의 면은 `CardSurface` rgb(0.106,0.122,0.149)다. `CardBorder`/`Divider`는 **알파 색**
> (rgba(1,1,1,0.10) / rgba(1,1,1,0.07))이므로 **어느 면 위에 올리느냐로 결과가 달라진다.**
> 설정창의 결과색을 그대로 옮기면 **테두리만 미묘하게 어긋난다.**

**칩을 숨기지 않는다** — 레일 양 끝 캡이라 하나가 사라지면 막대 자체가 고장 난 것처럼 보인다.
**죽이되 지우지 않는다**(설정창 `ApplyPageChipEnabled` 주석과 같은 근거).

### 14.5-b 동기화 지점 — **새 호출 지점을 만들 필요가 없다**

`RefreshInventoryList()` 하나에 붙이면 된다. 이미 필요한 모든 순간에 불린다:

| 계기 | 호출부 |
|---|---|
| 탭 전환 | `RefreshAll()` (741행) |
| 페이지 이동 | `ScrollInventory()` (1229행) |
| 행 선택 | `OnInventoryRowClicked()` (1390행) |
| 착용/해제 | 착용 토글 경로 (1383행) |
| 성장 이벤트 | `OnProgressionChanged()` (723행) |
| 장비 이벤트 | `OnEquipmentChanged()` (731행) |

## 14.6 플랫폼 영향

| | 내용 |
|---|---|
| **Windows 영향** | **함께 수정함(설계) — 별도 배정 불필요.** 걸린 파일은 `Interaction/CharacterPortraitStage.cs`와 `Interaction/CharacterInfoWindow.cs` 둘뿐이고 **`#if UNITY_STANDALONE_*` 분기가 0건**이다. 14.2의 유도식에 **픽셀 밀도가 들어가지 않는다** — 전부 캔버스 pt와 신장 H 배수라 다섯 배율에서 같은 pt 값이 나온다 |
| **macOS 영향** | 본론. §45의 실측은 전부 macOS Retina에서 냈다 |
| **새 플랫폼 분기** | **0건.** `PlatformParityAuditTests`에 추가 항목 없음 |
| **배율 검산** | 액자 204/196/패딩 8/내용 188×180 · 레일 24 · 칩 24 · 지시자 신규 높이 16 → ×1.25 / 1.50 / 1.75 / 2.00 **전부 정수** ✔ · 프레즌스 h15는 ×1.25 = 18.75, ×1.75 = 26.25로 잔차 있음(**기존 값**, 이번 건으로 새로 생긴 것 아님) → §44-2 (b′) 스냅 대상인지 **리더 확인 요청** · `FontCaption 10` ×1.25 = 12.5도 동일하게 기존 값 |
| **크로스 컴파일** | 이 절은 프로덕션 `.cs` 변경 0건이라 이번 라운드 컴파일 대상 없음. 구현 배정 시 `-define:UNITY_STANDALONE_WIN` / `-define:UNITY_STANDALONE_OSX` 양쪽 0에러 확인 필요 |

---

# 15. ★ 카드 외곽선을 등급별로 가른다 — 4상태 충돌 해소 (2026-09-03, ux-designer)

> 발단은 사용자 직접 지시다: *"리본을 저렇게 표시하면 전설인지 일반인지 잘 구분이 안갈거 같음.
> **카드 외곽선을 각 레벨별로 분류하는게 어때**"*
>
> 이 절은 **프로덕션 `.cs` 0줄**을 고친다. 설계와 숫자만 낸다.
> 색 계산기는 `design/art/verify/colorlab.py`(CIE76 ΔE · WCAG 2.x 대비, `calibrate()` 통과)를
> 그대로 썼다 — 자를 새로 만들지 않았다. 치수는 전부 `CharacterInfoWindow.cs` / `UiChrome.cs`
> 상수를 직접 파싱했다.

## 15.0 한 줄 결론

> **안 D 「기본 자리 승계」를 권고한다.**
> 카드 외곽선을 **1pt → 2pt**로 올리고, 4상태 우선순위(`Cards.cs:281~284`)는 **한 글자도 바꾸지 않은 채**
> 마지막 `기본` 자리만 **등급 램프 α0.55**가 승계한다.
>
> ```
> 선택      >  호버        >  착용 중       >  (기본 →) 등급 4색
> #F2F4F7      흰 α0.62       파랑 α0.75       램프 α0.55
> CR 15.00     CR 7.09        CR 4.09          CR 2.67 / 3.20 / 3.80 / 4.45
> ```
> **7색 모든 쌍 최소 ΔE 9.06**(그 쌍조차 등급 일반↔희귀이고, **상태↔등급 최소는 25.87**).
> 예약색 침범 0건. 신규 `Image` **0개**. 카드 치수 변화 **0pt**.
>
> **사용자 불만에 대한 답(숫자)**: 일반↔전설 판별 신호량이 리본 단독 대비 **1.79배**,
> 가장 어려운 인접 쌍인 **일반↔희귀는 2.05배**가 된다.
> **일반 카드에서 등급색이 칠해지는 면적은 133 pt² → 1,155 pt²로 8.7배**다.
>
> **★ 그리고 이건 새 요구가 아니라 확정본의 빠진 조각이다 — 다만 리더 브리프의 전제 하나는 틀렸다.**
> 아래 15.1에서 정정한다.

## 15.1 ★ 먼저 사실 정정 — 확정본의 「테두리」는 **카드 외곽선이 아니었다**

리더 브리프는 `docs/UX_HANDOFF_REVIEW_BRASS_ARCHIVE.md:511`의
*"「카드색」의 범위는 **리본·라벨·테두리**까지"*를 근거로 *"「테두리」가 C1 안에 있었는데 구현이 빠뜨렸다"*
고 판단했다. **그 「테두리」는 카드 외곽선이 아니다.** 정본인 `PALETTE_SPEC §27-6-4` 확정표를 그대로 옮긴다:

| # | 등급색이 **닿는다** | 실제 대상 |
|---|---|---|
| C-1 | 리본 칸 채움 | 카드 상단 4pt 리본 — **구현됨** |
| C-2 | 등급 낱말 글자색 | 상세 패널 메타 줄 — **구현됨** |
| C-3 | **썸네일 프레임 2pt** | ← **이것이 그 「테두리」다.** 썸네일 139×54 둘레 — **미구현** |
| C-4 | (조건부) 아이콘 영역 glow | 바닥을 `#101318` 이하로 낮추는 것이 선행 조건 — 보류 |

| # | 등급색이 **안 닿는다** | 근거 |
|---|---|---|
| ✘ | **카드 테두리** | *"선택 > 호버 > 착용 중 > 기본 **네 상태가 이미 점유**"* (§27-6-4 / §12-3 후보 2) |

그리고 `docs/TEAM.md:648`은 **같은 날(2026-09-03) 확정**으로 이렇게 못박았다:

> *"**카드 테두리는 등급을 말하지 않는다** — 선택 > 호버 > 착용중 > 기본 4상태로 이미 꽉 찼고 등급은 다섯 번째다."*

⇒ **정정 3건**
1. `coder-ui`가 「카드 테두리 무수정」으로 끝낸 것은 **누락이 아니라 확정 준수**다. 그 판단을 되짚지 마라.
2. **진짜 미구현분은 C-3 「썸네일 프레임 2pt」다.** 카드 외곽선이 아니다. (실측: `Cards.cs`의 등급 사용처는
   리본 `:384` `:388`과 상세 낱말 `:503`뿐이고 **썸네일 프레임도 0건**이다 — 리더 지적의 절반은 맞다.)
3. 그러므로 이번 지시는 **확정 문장 하나를 뒤집는 결정**이다. 사용자 지시가 팀 확정보다 위이므로 진행하되,
   **뒤집는다는 사실을 이 절이 명시적으로 기록한다.** `TEAM.md:648`의 그 줄은 이 안이 채택되면
   같이 고쳐져야 한다(리더 소관 — 나는 그 파일을 안 건드린다).

**그리고 사용자의 불만 자체는 옳다.** 15.3에서 그것을 숫자로 확인했다 — 리본의 약점은
*"작아서"*가 아니라 **"주변시에서 못 읽혀서"**다. 정확히 그 자리를 테두리가 메운다.

## 15.2 지금 테두리에 사는 것 — 실측 (`Cards.cs:281~284`)

```csharp
card.Outline.color = selected  ? UiChrome.TextPrimary       // #F2F4F7  a1.00
                   : hovered   ? UiChrome.CardBorderHover   // 흰       a0.24
                   : worn && owned ? UiChrome.CardBorderWorn// #5DA1F5  a0.55  ← 예약
                   :             UiChrome.CardBorder;       // 흰       a0.10
```

`CardSurface #1B1F26` 위에 평탄화한 **실효색**(화면에 실제로 나오는 값):

| 상태 | 실효색 | 카드면 대비 | 동시 최대 장수 |
|---|---|---:|---|
| 기본 | `#32353C` | **1.35** | 나머지 전부 |
| 호버 | `#52555A` | **2.20** | **≤ 1** (포인터) |
| 착용 중 | `#3F6798` | **2.82** | **섹션당 ≤ 1** |
| 선택 | `#F2F4F7` | **15.00** | **정확히 1** |

상태쌍 ΔE: 기본~호버 **13.75** / 기본~착용 33.6 / 기본~선택 73.9 / 호버~착용 28.4 / 호버~선택 60.2 / 착용~선택 61.1.

> ★★ **여기서 문제가 풀린다.** 리더 브리프는 *"여기에 등급을 얹으면 **다섯 번째 의미**다"*라고 했는데,
> **「기본」은 의미가 아니다. 「아무 상태도 아님」이다.** 실제 의미는 셋뿐이고,
> 그 셋은 전부 **한 번에 한두 장**에만 붙는다. 한 섹션 6장 중 **최소 3장, 통상 4~5장은 항상 「기본」**이다.
> 등급은 다섯 번째가 아니라 **비어 있던 자리를 채우는 것**이다.

## 15.3 판별력을 무엇으로 재는가 — 자 3개를 먼저 못박는다

*"더 낫다"*로 끝내지 않기 위해, **서로 독립인** 자 셋을 쓴다. 하나가 무너져도 나머지가 산다.

### 자 ① 신호량 = Σ (그 자리 ΔE) × (면적 pt²)

두 카드가 서로 다른 색으로 칠해지는 **모든 화소**에 대한 합.
> **★ 이건 검증된 심리물리 모델이 아니다.** ΔE는 「나란히 놓인 큰 균일 색면」용 자이고, 면적 선형 가중은
> 1차 근사다. **그래서 자 ②·③을 따로 둔다.** 이 수치만으로 판정하지 않는다.

### 자 ② 시각각 — 그 신호를 **눈이 물리적으로 해상할 수 있는가**

관측 조건 4종을 다 재서 **최악을 쓴다**(60cm / 50cm, 96~254 dpi, 배율 1.0~2.0).

| 특징 | 96dpi 100% 60cm | 96dpi 150% | 4K27 150% | MBP14 2x 50cm | **최악** |
|---|---:|---:|---:|---:|---:|
| 2pt 선 | 3.03′ | 4.55′ | 2.68′ | 2.75′ | **2.68′** |
| 리본 높이 4pt | 6.06′ | 9.10′ | 5.36′ | 5.50′ | **5.36′** |
| **리본 칸 틈 2pt** | 3.03′ | 4.55′ | 2.68′ | 2.75′ | **2.68′** |
| 전설 리본 길이 133pt | 201.4′ | 301.7′ | 178.0′ | 182.7′ | 178.0′ |

기준선: **중심시 MAR ≈ 1.0′ / 5° 이심 MAR ≈ 2.5~3.0′ / 색 판별이 안정되는 자극 폭 ≈ 10′ 이상.**

> ★★ **이것이 사용자 불만의 물리적 실체다.**
> **리본의 칸 틈 2.68~3.03′은 5° 이심 MAR와 정확히 같은 크기다.** 즉 **리본의 「개수 축」은
> 카드를 똑바로 볼 때만 작동하고, 24장을 훑는 순간 칸이 뭉개져 「길이 하나」로 무너진다.**
> 그리고 그 길이 신호가 얹힌 띠의 높이는 5.36′ — **색 판별 안정선 10′의 절반**이다.
> ⇒ 사용자는 옳다. **리본은 「들여다보는 눈」에는 완벽하고 「훑는 눈」에는 거의 안 보인다.**

### 자 ③ 등급색 신호 면적 — 특히 **일반 카드 쪽**

사용자가 지목한 쌍은 「일반 vs 전설」이다. **전설은 이미 리본 532 pt²를 갖고 있고 일반은 133 pt²뿐**이다.
판별은 약한 쪽이 정한다. **일반 카드의 신호를 키우는 안이 이기는 안이다.**

## 15.4 안 A ~ 안 E 전량 — 각각의 **실패 모드**

### 안 A — 이중 테두리 (바깥 2pt = 등급 / 안쪽 2pt = 상태)

| | |
|---|---|
| 신호량(일반↔전설) | 테두리 부분 1,022.2 pt² × ΔE 51.54(α1.00) = 52,682 → 합계 **2.31×** |
| **실패 모드 ①** | ★ **기하학적으로 불가능하다.** 바깥 2 + 안쪽 2 = **크롬 4pt**가 카드 상단 여백 8pt의 절반을 먹고, **리본(y −2 ~ −6)과 물리적으로 겹친다**(안쪽 링이 y −2 ~ −4). 리본을 지우거나 카드를 키워야 한다 — **카드 폭 1042 확대 사고**를 다시 부른다 |
| **실패 모드 ②** | 현행 호버 흰 α0.24(`#52555A`)를 안쪽 링에 그대로 쓰면 바깥 등급 α0.55와 **최소 ΔE 7.42 < 변별 하한 7.8**. **두 링이 한 줄로 뭉쳐 4pt 흐린 띠**가 된다 |
| **실패 모드 ③** | 곡률 두 개(R12 / R10)가 코너에서 어긋난다. **오프라인 렌더러가 코너 붕괴를 가린 사고 전례가 있다** |
| 판정 | ✘ **기각 — 실측으로 자리가 없다** |

### 안 B — 두께 = 등급 / 색 = 상태

| | |
|---|---|
| **실패 모드 ①** | ★ **DPI에서 계단이 무너진다.** 1/2/3/4pt는 ×1.25에서 1.25 / 2.5 / 3.75 / 5.0 px, ×1.75에서 1.75 / 3.5 / 5.25 / 7.0 px. **인접 단 차가 배율마다 다르게 뭉갠다** — 리본을 3pt→4pt로 올린 것과 **같은 병인데 이쪽은 처방이 없다**(네 단이 동시에 정수여야 하므로) |
| **실패 모드 ②** | 1pt와 2pt를 가르려면 **1.52′ vs 3.03′**을 읽어야 한다. **5° 이심 MAR 2.5~3.0′** — 주변시에서 **원리상 불가능**하다. 자 ②가 이 안을 통째로 부순다 |
| **실패 모드 ③** | ★ **사용자의 원 불만을 하나도 못 채운다.** 색이 상태를 지므로 「전설 티」가 안 난다 |
| 판정 | ✘ **기각** |

### 안 C — 등급 = 테두리 색 / 상태 = 글로우 또는 표면 워시

| | |
|---|---|
| **실패 모드 ①** | ★ **글로우는 되살릴 수 없다.** 2026-09-02 사용자 지시 *"캐릭터창 둘레로도 그림자들이 있는데 다 없애줘 깔끔하게"*로 `AddShadow` 계열 5함수가 **삭제**됐다(`UiChrome.cs:1007~1027`). 글로우는 그 지시를 되돌리는 설계다 |
| **실패 모드 ②** | 표면 워시로 호버를 옮기면 **호버가 안 읽힌다** — 흰 α0.05 리프트(`#262A31`)는 기본 면과 **ΔE 5.42 < 7.8**. α0.08까지 올리면 ΔE 8.56으로 겨우 넘지만 그때 `InkMeta` 대비가 4.91 → **4.22**로 내려간다 |
| **실패 모드 ③** | 안쪽 링 변형은 안 A의 기하 충돌을 그대로 승계 |
| 판정 | ✘ **기각** |

### ★ 안 D — 「기본 자리 승계」 (**권고**)

**우선순위를 바꾸지 않는다.** 마지막 `기본` 자리만 등급이 승계하고, 상태 셋은 지금처럼 덮는다.

| | |
|---|---|
| 신호량(일반↔전설) | 리본 40,152 + 테두리 31,802 = **71,955 (1.79×)** |
| 신호량(일반↔희귀, 최난 인접쌍) | 8,780 + 9,260 = **18,040 (2.05×)** |
| 자 ③ | 일반 카드 등급색 면적 133 → **1,155 pt² (8.7배)** · 전설 532 → 1,554 (2.9배) |
| 신규 `Image` | **0개** (`card.Outline`이 이미 있다) |
| 예약색 | 침범 0건 (착용 파랑 · 잠김 회색 그대로) |
| **실패 모드 ①** | 상태가 붙은 카드(섹션당 ≤3장)에서 **테두리가 등급을 말하지 않는다** → 15.5의 상보성 논증으로 답한다 |
| **실패 모드 ②** | 호버 α0.24 · 착용 α0.55를 **올려야 한다**(상수 2개). 안 올리면 **호버하면 테두리가 어두워지는** 역전이 난다: 전설 기본 CR 4.45 > 현행 호버 CR 2.20 |
| **실패 모드 ③** | 2pt 테두리(y 0~−2)와 리본(y −2~−6)이 **정확히 맞닿는다.** 일반 카드에서 리본 트랙 `#3A4049`와 테두리 `#62615E`가 ΔE **16.40**으로 부딪혀 **상단 139pt에 홈**이 생긴다 → 15.6-c에서 해소 |
| **실패 모드 ④** | 24장 격자의 테두리 밝기가 CR 1.35 → **2.67~4.45**로 **2.0~3.3배** 올라간다. **상주 앱 소음.** α로 통제했으나 **실기 캡처 없이 확정 금지** |
| **실패 모드 ⑤** | `TEAM.md:648`의 같은 날 확정을 뒤집는다 → 리더 판정 필요 |
| 판정 | **✔ 권고** |

### 안 E — 등급은 카드 외곽선을 안 쓰고 **썸네일 프레임 2pt**로 (= 확정된 C-3)

| | |
|---|---|
| 신호량(일반↔전설) | 40,152 + 38,077 = **78,229 (1.95×)** — α1.00을 쓸 수 있어서 안 D α0.55보다 크다 |
| 신호량(일반↔희귀) | 리본 8,780 + 738.8×15.16 = 19,979 (**2.28×**) |
| 충돌 | **0건.** 상태 기계 무수정, 예약색 무관, `TEAM.md:648` 무위반 |
| **실패 모드 ①** | ★ **사용자 지시를 문자 그대로 안 따른다.** 「카드 외곽선」이 아니다 |
| **실패 모드 ②** | 신규 `Image` **24개**(카드당 1). 안 D는 0개다 |
| **실패 모드 ③** | 썸네일 139×54 안에 아이콘 44×44가 중앙 → 세로 여유 (54−44)/2 = 5pt 중 2pt를 프레임이 먹어 **3pt만 남는다.** 자물쇠 배지(썸네일 우하단 18×17)와의 간섭 **미측정** |
| 판정 | △ **차선.** 안 D가 반려되면 이것이 답이다 |

### 비교표 (한 장)

| | 안 A | 안 B | 안 C | **안 D ★** | 안 E |
|---|:---:|:---:|:---:|:---:|:---:|
| 사용자 지시 문자 충족 | ✔ | ✔ | ✔ | **✔** | ✘ |
| 일반↔전설 신호량 배수 | 2.31× | — | 2.31× | **1.79×** | 1.95× |
| 일반↔희귀 배수 | — | — | — | **2.05×** | 2.28× |
| 신규 `Image` | 24 | 0 | 24 | **0** | 24 |
| 기하 충돌 | **불가** | — | 불가 | 해소 가능 | 미측정 |
| DPI 생존 | — | **붕괴** | — | **✔ 개선** | ✔ |
| 상태 기계 손상 | 큼 | **치명** | 큼 | **없음** | 없음 |

## 15.5 【권고】 안 D가 등급을 죽이지 않는 이유 — **두 운반자는 상보적이다**

반론이 나올 자리는 하나다: *"상태가 덮으면 그 카드는 등급을 잃는다."* **안 잃는다.**

| | 테두리 (색, 1,022 pt², 닫힌 윤곽 517pt) | 리본 (개수, 4pt 띠) |
|---|---|---|
| 잘 읽히는 조건 | **훑는 눈** — 큰 면적 · 닫힌 윤곽 | **들여다보는 눈** — 칸 틈 2.68′ 해상 필요 |
| 죽는 조건 | 회색조·완전색맹 (15.8 실측) | 주변시 (5° 이심 MAR 2.5~3.0′) |

**그리고 테두리가 덮이는 조건은 정확히 「들여다보는 중」이다:**

| 덮는 상태 | 그때 그 카드는 | 리본이 작동하는가 |
|---|---|---|
| **호버** | 포인터가 **그 위에 있다** | ✔ 중심시 안 |
| **선택** | 아래 상세 패널이 **그 아이템을 통째로 설명 중**이다(등급 낱말 포함) | ✔ 중심시 안 + 낱말 축 |
| **착용 중** | 유저가 **자기가 입힌** 것이다. 썸네일 wash · 메타 `착용 중` · 버튼 `해제`가 이미 말한다 | ✔ |

⇒ **화면이 등급을 잃는 순간은 0이다.** 이것이 원칙 1(행동-텍스트 싱크)의 시각판이다 —
*"화면에 뜬 것과 실제 상태가 어긋나는 순간을 만들지 않는다."*

## 15.6 확정 상수 — 전량 검산

### 15.6-a 색 (신규 hex **0개**. 전부 출하 상수의 α 변형)

| 자리 | 값 | 실효색(`#1B1F26` 위) | 카드면 대비 | 비고 |
|---|---|---|---:|---|
| 등급 일반 | `#9C978C` α**0.55** | `#62615E` | **2.67** | `UiChrome.RarityColor(0)` |
| 등급 희귀 | `#BCAC8B` α0.55 | `#746D5E` | **3.20** | |
| 등급 영웅 | `#DEC081` α0.55 | `#867858` | **3.80** | |
| 등급 전설 | `#FFD375` α0.55 | `#988251` | **4.45** | |
| 착용 중 | `#5DA1F5` α**0.75** ← 0.55 | `#4C80C1` | **4.09** | ★ **hex 불변 = 예약 유지.** α만 |
| 호버 | 흰 α**0.62** ← 0.24 | `#A8AAAD` | **7.09** | |
| 선택 | `#F2F4F7` α1.00 | `#F2F4F7` | **15.00** | 무변경 |

**검산 1 — 등급 α 하한.** 인접 등급 ΔE ≥ 7.8을 유지하는 **α 하한은 0.4640**(이분법 40회).
채택 **0.55**에서 인접 ΔE = **9.06 / 11.13 / 11.01**, 하한 대비 여유 **+16.2%**.
α0.45는 7.59로 **미달**이므로 0.5 언저리로 내리지 마라.

**검산 2 — 7색 전량 상호 변별.** 21쌍 최소 ΔE **9.06**(등급 일반↔희귀).
**상태 ↔ 등급만 보면 최소 25.87**(호버 ↔ 희귀). 상태끼리: 호버~선택 32.5 / 호버~착용 41.1 / 선택~착용 57.2.

**검산 3 — 호버가 반드시 「밝아져야」 한다.** 등급 최대 CR은 전설 4.45. 호버 후보:

| 흰 α | 실효색 | CR | 등급최대 대비 | vs 등급 최소 ΔE | 판정 |
|---:|---|---:|---:|---:|:---:|
| 0.24(현행) | `#52555A` | 2.20 | **0.49×** | **7.42** | ✘ 어두워지고 ΔE도 미달 |
| 0.45 | `#828488` | 4.40 | 0.99× | 14.46 | ✘ 역전 직전 |
| 0.50 | `#8D8F92` | 5.10 | 1.15× | 17.55 | △ 여유 부족 |
| **0.62** | `#A8AAAD` | **7.09** | **1.59×** | **25.87** | **✔ 채택** |
| 0.70 | `#BBBCBE` | 8.67 | 1.95× | 31.87 | 과함(선택 15.00에 접근) |

**검산 4 — 착용 중이 등급에 묻히지 않는다.** α0.55(CR 2.82)면 전설(4.45)보다 **어둡다**.
α0.75로 올려 **CR 4.09**, 등급과 최소 ΔE **42.57**. **`#5DA1F5` 자체는 안 건드렸으므로
`TEAM.md` 예약(«파랑이 임자 있음»)은 그대로 유효하다.**
> ★ 다만 α도 `UiChrome` 토큰의 일부다 — **`design-art` 확인 요청**(값 발명은 아니고 α 조정이다).

### 15.6-b 두께 = **2pt.** ★ 이건 취향이 아니라 유도된 값이다

`UiChrome.RoundedOutline(radius, thickness)`은 두께를 **텍스처 픽셀로 굽고**
`Image.Type.Sliced` + `FilterMode.Bilinear`로 확대한다(`UiChrome.cs:684~712`).
코드에서 직접 유도했다 — 알파 1.0인 **평탄부 폭 = (T − 1) 텍셀**, 출력 표본 간격 = **1/s 텍셀**.
최악 위상에서도 알파 1.0 표본이 보장되려면 **T ≥ 1 + 1/s**.

| 배율 s | 필요 T | **T=1 최악 피크 α** | T=2 |
|---:|---:|---:|---:|
| 1.00 (mac 비Retina / Win 100%) | 2.000 | 1.000 (위상 0 고정) | 1.000 |
| **1.25** | 1.800 | **0.600 — 40.0% 흐림** | **1.000** |
| **1.50** | 1.667 | **0.667 — 33.3%** | **1.000** |
| **1.75** | 1.571 | **0.714 — 28.6%** | **1.000** |
| **2.00 (mac Retina)** | 1.500 | **0.750 — 25.0%** | **1.000** |

> ★★ **이건 새 결함이 아니라 지금 출하 중인 결함이다.** 현행 `CardBorder`는 T=1이므로
> **Windows 125%에서 최대 40%, macOS Retina에서 25% 흐려진다.** 게다가 표본 위상은
> **카드의 화면 좌표에 따라 달라지므로 한 줄 6장의 테두리 밝기가 서로 다르게 보인다**
> (×1.25에서 위상 주기 4). **2pt는 이 결함을 원리적으로 없앤다** — 리본이 3pt→4pt로
> 올라간 것과 정확히 같은 처방이다(`UiChrome.RarityRibbonHeight` 문서의 「잔차 0」).

2pt는 ×1.25 / 1.50 / 1.75 / 2.00에서 **2.5 / 3.0 / 3.5 / 4.0 px** — 반 픽셀이 남지만
**평탄부가 있으므로 피크 알파는 1.0**이다. 알파 손실과 위치 잔차는 다른 문제다.

### 15.6-c ★ 리본을 2pt 내린다 — `CardRibbonTopMargin` **2 → 4**

2pt 테두리(y 0~−2)와 리본(y −2~−6)이 **정확히 맞닿아** 일반 카드 상단 139pt에
ΔE 16.40짜리 홈이 생긴다(15.4 실패 모드 ③). 세로 예산 8pt는 그대로 두고 리본만 내린다:

| 배치 | 예산 | 테두리와의 틈 | ×1.25 / 1.5 / 1.75 / 2.0 | 비정수 | 짝수 규칙 |
|---|---|---:|---|---:|:---:|
| 현행 위2 + 리본4 + 아래2 | 8 ✔ | **0pt (충돌)** | 2.5 / 3.0 / 3.5 / 4.0 | **2건** | OK |
| 위3 + 리본4 + 아래1 | 8 ✔ | 1pt | 3.75 / 4.5 / 5.25 / 6.0 | 3건 | ✘ **홀수** |
| **위4 + 리본4 + 아래0** | **8 ✔** | **2pt** | **5.0 / 6.0 / 7.0 / 8.0** | **0건** | **OK** |

> ★★ **덤 — 이 이동이 기존 미해결 결함 하나를 같이 닫는다.**
> `UiChrome.RarityRibbonHeight` 문서가 남긴 미확인 항목 **(가)** *"리본의 y 위치(카드 상단 여백 2pt)는
> ×1.25/×1.75에서 반 픽셀에 걸린다 — 두께는 정수인데 **시작점**이 밀리면 위아래 경계 한 줄이 흐려진다"*.
> **4pt로 올리면 네 배율 전부 정수가 되어 그 (가)가 사라진다.**
> 3pt는 **`UiChrome`의 「전부 짝수 pt다. 홀수로 되돌리지 마라」 규칙 위반**이라 애초에 후보가 아니다.

**대가**: 리본이 썸네일 상단과 **붙는다**(아래 여백 0). 리본 트랙 `#3A4049`와 썸네일
`CardSurfaceMuted #15181E`는 대비 1.35로 경계가 약하다 — **실기 캡처 항목**이다.
붙이는 것이 보기 싫으면 대안은 `PALETTE_SPEC §27-6-5`의 갈래 **(나)「트랙을 지우고 칸 사이 2pt 간격만 남긴다」**뿐이고,
그건 `design-art` 소관이다.

### 15.6-d 카드 치수 — **1pt도 안 커진다**

| 요소 | 현행 | 안 D | 확인 |
|---|---|---|---|
| 카드 | 161 × 108 | **동일** | ✔ |
| 테두리 | 안쪽 인셋 1pt | 안쪽 인셋 2pt | 레이아웃 무영향(`Stretch`) |
| 썸네일 좌우 여백 | `ThumbX` 11 | 11 − 2 = **9pt 시각 여백** | ✔ |
| 리본 x | 11 (`ThumbX` 파생) | **동일** | ✔ |
| 하단 버튼 | `CardActionY` −80, 높이 `MinTargetSizePoints` | **동일** | 하단 테두리는 인셋이라 안 겹침 |

## 15.7 와이어프레임

### 15.7-a 카드 한 장 (161 × 108, 안 D 적용 · 「기본」 상태 · 전설)

```
      x=0      2                          11                       150   159 161
 y=0  ╔════════════════════════════════════════════════════════════════════╗  ← 테두리 2pt = 등급 #988251
      ║ ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒ ║     (α0.55, CR 4.45)
 −2   ║ ▒▒                                                              ▒▒ ║
      ║ ▒▒         ← 2pt 틈 (CardRibbonTopMargin 4 − 테두리 2) →       ▒▒ ║
 −4   ║ ▒▒  ┌────────┬────────┬────────┬────────┐                      ▒▒ ║  ← 리본 4pt · 칸 33.25 · 틈 2
      ║ ▒▒  │████████│████████│████████│████████│  전설 = 4칸 전부 채움 ▒▒ ║     채움 #FFD375 / 트랙 #3A4049
 −8   ║ ▒▒  ├────────┴────────┴────────┴────────┤                      ▒▒ ║  ← 썸네일 139×54 (#15181E)
      ║ ▒▒  │              ┌──────┐             │                      ▒▒ ║
      ║ ▒▒  │              │ 아이콘│ 44×44      │                      ▒▒ ║
      ║ ▒▒  │              └──────┘             │                      ▒▒ ║
 −62  ║ ▒▒  └───────────────────────────────────┘                      ▒▒ ║
 −64  ║ ▒▒  왕관                            LV.20                       ▒▒ ║  ← 이름(11) / 메타(우측정렬)
 −80  ║ ▒▒  ┌───────────────────────────────────┐                      ▒▒ ║
      ║ ▒▒  │              착용                  │  139 × 44           ▒▒ ║
      ║ ▒▒  └───────────────────────────────────┘                      ▒▒ ║
−106  ║ ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒ ║
−108  ╚════════════════════════════════════════════════════════════════════╝
```

### 15.7-b 한 줄 6장 — 사용자가 실제로 보는 화면 (기본 상태 4장 + 상태 2장)

```
   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
   │▒일반 2.67│ │▒희귀 3.20│ │█선택 15.0│ │▒영웅 3.80│ │█착용 4.09│ │▒전설 4.45│
   │ ▪▫▫▫     │ │ ▪▪▫▫     │ │ ▪▪▪▫     │ │ ▪▪▪▫     │ │ ▪▪▪▪     │ │ ▪▪▪▪     │
   │  ▨       │ │  ▨       │ │  ▨       │ │  ▨       │ │  ▨       │ │  ▨       │
   └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘
     따뜻한 갈색 계단 ────────►  흰(무채)     따뜻한       파랑        가장 밝은 금
                                 ↑ 튄다        ↑           ↑ 튄다      ↑
     등급은 「같은 가족 안의 밝기 계단」으로 읽히고, 상태 둘은 「가족 밖」으로 읽힌다.
     상태가 덮은 2장은 리본(칸 수)이 등급을 계속 말한다 — 그 2장은 중심시 안이다.
```

### 15.7-c 상태 · 예외 표 (역할 규정 형식)

| | 내용 |
|---|---|
| **목표** | 카드 한 장만 보고 「전설인가 일반인가」를 **훑는 눈으로** 가른다 |
| **트리거** | 카드가 그려지는 모든 순간(`StyleCard`). 별도 트리거 없음 — **상시 상태** |
| **유저가 보는 것** | 카드 외곽선 2pt가 등급 4색 중 하나. 상태가 있으면 그 상태색 |
| **유저 액션** | 없음(읽기 전용 표식). 클릭·호버 동작 **무변경** |
| **빈 상태** | 등급이 **없는** 자리(보관함 헤더 줄, 「할 줄 아는 것」) → 테두리 = 현행 `CardBorder` 유지. **리본을 통째로 숨기는 것과 같은 규칙**(`HideRarityRibbon`). ★ **빈 등급을 「일반」으로 칠하지 마라** — 없는 것은 없게 |
| **로딩** | 해당 없음(등급은 `ItemCatalog` 정적값, I/O 없음) |
| **에러** | 열거형 범위 밖 등급 → `UiChrome.WarnUnknownRarity`가 이미 로그를 찍고 끝 색으로 물린다. **테두리도 같은 색을 받는다**(같은 함수를 타므로 자동) |
| **거부** | 해당 없음 |
| **탈출구** | 해당 없음(방해성 이벤트가 아님). 단 **소음 이의가 나오면 α 하나(0.55)로 전체를 되돌릴 수 있다** — 0.4640이 하한이고 그 아래로 내리면 등급 신호가 죽는다는 것이 15.6-a 검산 1이다 |

### 15.7-d 9상태 표(UX_FLOW 50-4-2)에 붙는 「테두리」 칸

| 상태 | 테두리 |
|---|---|
| S1 살 수 있다 / S1r / S2 / S3 / S7 / S8 | **등급색 α0.55** (상태가 없을 때) |
| S4 레벨 미달 / S5 · S6 DLC | **등급색 α0.55 — 흐리지 않는다** (15.9) |
| 위 전부에 대해 선택 / 호버 / 착용 중이면 | **그 상태색이 덮는다** (우선순위 무변경) |

## 15.8 【답 3】 리본을 남기는가 — **남긴다. 그리고 이건 §27-7의 병이 아니다**

### 근거 ① 회색조에서 **테두리 색은 원리적으로 못 산다**

| 등급 테두리 α | 회색값 4단 | 인접 최소 ΔE | 변별 7.8 |
|---:|---|---:|:---:|
| 0.55(채택값) | 97 / 109 / 121 / 133 | **4.71** | ✘ **미달** |
| 0.70 | 115 / 131 / 146 / 161 | **5.69** | ✘ |
| 1.00 | 151 / 174 / 195 / 217 | **7.70** | ✘ (근소 미달) |

**α를 1.00까지 올려도 7.70으로 못 넘는다.** 즉 **완전색맹 · 흑백 프린트 · 저조도 · 스크린샷 압축에서
테두리는 등급을 말하지 못한다.** 그 조건에서 **유일하게 사는 축이 리본의 칸 수**다(`PALETTE_SPEC §12-1`).
⇒ **리본을 지우면 접근성 축이 통째로 사라진다. 논의 종결.**

### 근거 ② 상태가 덮은 카드에서 등급의 **유일한** 운반자다 (15.5)

### 근거 ③ §27-7의 진단을 다시 읽으면 **우리 병이 아니다**

> §27-7 원문: *"인계본의 넷은 **전부 「색」 축 하나**다… 축이 하나면 그 축이 죽는 조건에서
> 네 개가 한꺼번에 죽는다. 셋으로 줄이되 **축을 셋으로 나누는 것**이 낫다."*

**병은 「자리가 몇 개인가」가 아니라 「축이 몇 개인가」다.** 우리 상태:

| 축 | 자리 | 죽는 조건 |
|---|---|---|
| **개수** | 리본 칸 수 | (없음 — 색각·명도·해상도 어디에도 안 걸린다) |
| **색** | 리본 채움 · **테두리(신규)** | 회색조 · 완전색맹 |
| **낱말** | 상세 패널 메타 | (없음) |

**자리는 4개지만 축은 3개다.** 인계본은 자리 4 / **축 1**이었다. 그리고 테두리를 색 축에 더하는 것은
**새 실패 모드를 하나도 만들지 않는다** — 색 축은 이미 있었고 이미 같이 죽는다.
⇒ **"우리도 세 번 말하게 되면 같은 병"이라는 걱정은 기각한다. 세는 단위가 자리가 아니라 축이다.**

### 그래도 남는 비용 — 정직하게

**「색으로 등급을 말하는 자리」가 1개 → 2개가 되고 면적은 133~532 → 1,155~1,554 pt²**로 커진다.
색 축이 죽는 조건에서 **잃는 양**이 그만큼 커진다는 뜻이다. 개수·낱말 축이 그대로이므로
**기능은 안 잃지만 「화려함」은 잃는다.** 이건 측정이 아니라 제품 판단이라 리더/사용자 몫이다.

### C-3(썸네일 프레임)은 어떻게 하는가 — **보류를 권고한다**

안 D를 채택하면 색 축의 자리가 **3개**가 되고(리본·테두리·프레임), 그때는 **정말로** §27-7의
「너무 여러 번 말한다」가 된다. 신호량 이득도 체감이다: 안 D 단독 1.79× → +C-3 하면 2.74×이지만
**신규 `Image` 24개와 아이콘 여유 5pt→3pt를 그 이득에 지불한다.**
⇒ **C-3는 「안 D가 실기에서 반려될 때의 대체안(안 E)」으로 남긴다.** 둘 다 하지 않는다.
**이건 `design-art`가 §27-6-4에서 확정한 항목이므로 폐기가 아니라 「보류」이고, 판정은 리더 몫이다.**

## 15.9 【답 4】 잠긴 카드 — **흐리지 않는다. `TextTertiary`와 안 부딪힌다**

**정책: 리본과 같게 흐리지 않는다.** 근거는 `Cards.cs:40~45`가 이미 적어 놓았다 —
인계본 `opacity .25`는 실측 대비 1.45~1.69로 *"거기 리본이 있다"조차 안 보였고, 잠김은
**이름 `???` · 바탕 · 썸네일 · 실루엣 · 자물쇠 배지 · 버튼 문구 여섯 곳**이 이미 말한다.
**한 부품이 두 가지를 말하기 시작하면 둘 다 안 읽힌다.**

### 잠긴 카드 면(`CardSurfaceMuted #15181E`) 위 실측

| 등급 | 실효색 | 대비 | (참고) 보유 카드 |
|---|---|---:|---:|
| 일반 | `#5F5E5A` | **2.74** | 2.67 |
| 희귀 | `#71695A` | **3.29** | 3.20 |
| 영웅 | `#847454` | **3.91** | 3.80 |
| 전설 | `#967F4E` | **4.60** | 4.45 |

인접 ΔE **9.11 / 11.18 / 11.04** — 보유 카드와 사실상 같다(잠긴 면이 더 어두워 대비가 **+2.6~3.4%** 높다).

### `TextTertiary #8B939F`(잠김 실루엣)와 충돌하는가 — **안 한다**

| 비교 | ΔE | 변별 7.8 | 식별 48.6 |
|---|---:|:---:|:---:|
| 잠김 테두리 α0.55 4색 ↔ `#8B939F` **최소** | **22.82** | ✔ (2.93배) | ✘ |
| (참고) 램프 원색 일반 `#9C978C` ↔ `#8B939F` | 13.88 | ✔ | ✘ |

**두 색은 같은 화소를 절대 안 쓴다** — 실루엣은 썸네일 139×54 **안쪽**, 테두리는 카드 가장자리 2pt.
동시 대비 오독의 기하학적 조건이 성립하지 않는다.

> ★ **그래도 정직하게 남기는 위험 하나**: 램프의 일반 `#9C978C`는 그 자체가 **따뜻한 회색**이고
> 잠김 회색 `#8B939F`와 원색 ΔE 13.88(변별 통과 · 식별 미달)이다. 테두리로 옮기면 **그 색의 면적이
> 133 → 1,022 pt²로 7.7배** 커진다. α0.55가 거리를 22.82로 벌려 주지만,
> **「잠긴 일반 카드가 통째로 회색 덩어리로 보이는가」는 실기 캡처로만 판정된다.**
> 계산상 통과 / **실기 미확인.**

## 15.10 코드 구조 영향 — 리더(아키텍트) 보고

**이 설계는 코드 구조에 영향을 준다. 명시적으로 보고한다.**

| # | 변경 | 위치 | 성격 |
|---|---|---|---|
| 1 | `AddOutline(..., thickness: 2)` | `Cards.cs:747` (카드) / `Inventory.cs` 행 | 인자 1개. **함수는 이미 `thickness`를 받는다** — 신규 API 0 |
| 2 | 3항식 마지막 `: UiChrome.CardBorder` → `: UiChrome.RarityBorder(rarity)` | `Cards.cs:284` | **우선순위 · 분기 개수 무변경** |
| 3 | 신규 공개 표면 **1개**: `UiChrome.RarityBorder(ItemRarity)` | `UiChrome.cs` | ★ **색 리터럴이 UI로 안 간다** — `RarityColor()` 재사용 + α만. `RarityColorSingleSourceTests`의 단일 출처 원칙 유지 |
| 4 | `CardBorderHover` α 0.24 → **0.62** | `UiChrome.cs:173` | 상수 1개. **다른 호출부 조사 필요**(카드 전용이 아닐 수 있다) |
| 5 | `CardBorderWorn` α 0.55 → **0.75** | `UiChrome.cs:170` | hex 불변. **`design-art` 확인 요청** |
| 6 | `CardRibbonTopMargin` 2 → **4** | `Cards.cs:73` | 상수 1개 |
| 7 | 신규 `GameObject` / `Image` | — | **0개** |

**★ `Inventory.cs:160~161`도 같이 가야 한다.** 보관함 줄은 **호버가 없어 3상태**(선택 > 착용 > 기본)이고,
같은 규칙을 적용하면 충돌이 한 단 **더 적다**. **한쪽만 고치면 같은 창 안에서 등급 표기가 갈라진다.**

**동반 테스트 권고 2건** (니들 하드코딩 금지 — 상수를 **참조**해서 비교할 것):
1. `ItemRarity` 전수에 대해 `RarityBorder(r)`의 RGB가 `RarityColor(r)`와 **같은지** + α가 하나의 상수인지.
   ★ **「4개 전부 검사했다」는 개수 단언을 함께 넣는다** — 안 그러면 열거형이 비어도 조용히 초록이 된다.
2. 4상태 + 4등급 **7색 21쌍의 최소 ΔE ≥ 7.8**을 프로덕션 상수에서 읽어 계산.
   ★ 이 테스트가 있으면 **누가 α를 만졌을 때 즉시 빨개진다** — 15.6-a 검산 1·2가 코드로 고정된다.

## 15.11 계산상 통과 / **실기 미확인** — 캡처 없이 「확인됨」이라 쓰지 않는다

| # | 항목 | 상태 |
|---|---|---|
| 1 | 색·대비·ΔE 전량 (15.6, 15.8, 15.9) | **계산상 통과**(`colorlab.calibrate()` 통과). 렌더 캡처 없음 |
| 2 | 두께 2pt의 DPI 유도 (15.6-b) | **소스에서 유도**(`UiChrome.cs:684~712`). 실기 픽셀 측정 없음 |
| 3 | **24장 격자가 시끄러운가** | ★ **미확인. 이 안의 최대 위험.** 실기 캡처 필수 |
| 4 | 리본을 4pt로 내렸을 때 **썸네일 상단과 붙는 모양** | ★ **미확인** |
| 5 | 2pt 라운드 테두리(R12)의 **코너 형태** | ★ **미확인.** 오프라인 렌더러가 코너 붕괴를 가린 전례가 있다 |
| 6 | **잠긴 일반 카드가 회색 덩어리로 보이는가** | ★ **미확인**(15.9) |
| 7 | `CardBorderHover` α 상향이 **카드 밖 다른 호출부**에 미치는 영향 | ★ **미조사.** 구현 라운드가 grep 먼저 |
| 8 | 최신 빌드 | `09-02 19:07`로 **낡았다**. 이 절의 어떤 항목도 「실기 확인됨」이 아니다 |

## 15.12 플랫폼 영향

| | 내용 |
|---|---|
| **Windows 영향** | ★ **함께 검토함 — 이 절의 핵심 근거 하나가 Windows 전용이다. 별도 배정 불필요.** 대상 파일 3개(`UiChrome.cs` / `CharacterInfoWindow.Cards.cs` / `.Inventory.cs`) **전부 `#if UNITY_STANDALONE_*` 0건**(grep 실측). **15.6-b의 2pt 유도는 Windows DPI 사다리(1.25 / 1.5 / 1.75)가 근거이고, 현행 1pt 테두리가 거기서 최대 40% 흐려지는 것을 이 절이 처음 계산했다.** 15.6-c의 `CardRibbonTopMargin 2→4`도 ×1.25 / ×1.75 반 픽셀 잔차를 **0건으로 만드는** Windows 처방이다 |
| **macOS 영향** | 색 판정은 플랫폼 공통. **배율 ×2에서도 1pt 테두리는 최악 피크 α 0.75로 25% 흐려진다** — mac은 «×1/×2뿐이라 구조적으로 안전»한 리본 높이와 **다르다**(리본은 크기, 이쪽은 선의 알파다). 2pt가 양쪽을 동시에 고친다 |
| **새 플랫폼 분기** | **0건.** `PlatformParityAuditTests`에 추가 항목 없음 |
| **배율 검산** | 테두리 2pt → 2.5 / 3.0 / 3.5 / 4.0 px(위치 잔차 있으나 **피크 알파는 1.0 보장**) · 리본 y 4pt → **5 / 6 / 7 / 8 전부 정수** ✔ · 리본 높이 4pt → 5 / 6 / 7 / 8 ✔ · **카드 폭 161 × 1.25 = 201.25는 비정수인데 이건 기존 값이고 이번 건으로 생긴 것이 아니다**(리더 확인 요청) |
| **크로스 컴파일** | 이 절은 프로덕션 `.cs` **0줄**이라 이번 라운드 컴파일 대상 없음. 구현 배정 시 `-define:UNITY_STANDALONE_WIN` / `-define:UNITY_STANDALONE_OSX` 양쪽 0에러 확인 필요 |

## 15.13 리더 결정 요청

| # | 결정 | 내 권고 | 근거 |
|---|---|---|---|
| **B-1** | 안 A~E 중 무엇 | **안 D 「기본 자리 승계」** | 15.4 비교표 — 유일하게 사용자 지시 충족 + 신규 `Image` 0 + 기하 충돌 해소 가능 |
| **B-2** | `TEAM.md:648` *"카드 테두리는 등급을 말하지 않는다"* | **사용자 지시로 폐기 → 그 줄을 고칠 것** | 15.1. 나는 그 파일을 안 건드린다 |
| **B-3** | `CardBorderWorn` α 0.55 → 0.75 | **채택**(hex 불변 = 예약 유지) | 15.6-a 검산 4. **`design-art` 확인 요청** |
| **B-4** | `CardBorderHover` α 0.24 → 0.62 | **채택** | 15.6-a 검산 3 — 안 올리면 **호버하면 어두워진다** |
| **B-5** | C-3 썸네일 프레임 2pt | **보류**(안 D의 대체안으로만) | 15.8 — 색 축 자리 3개는 진짜 과잉 |
| **B-6** | 등급 α 값 | **0.55**(하한 0.4640) | 15.6-a 검산 1 |
| **B-7** | 실기 캡처 | **안 D 확정 전 필수 5건**(15.11 #3~#7) | 빌드가 09-02 19:07로 낡았다 |
| **B-8** | 신규 색 발명 | **0건** — 전부 출하 hex의 α 변형 | 발명이 필요했다면 `design-art`로 넘겼을 것 |

---

## 15.14 ★★ 라운드 중 정정 — 리더가 기준선을 고쳤다. **재측정했고, 결론이 하나 늘었다**

> 리더 정정(2026-09-03, 이 라운드 진행 중):
> *"인계본은 애초에 칸을 나누지 않는다 … **인계본 = 단색 막대 + 낱말 2채널**이고 우리 구현 = 4칸 분할 리본이다.
> 사용자 불만은 **우리가 인계본에서 벗어난 지점**을 정확히 짚은 것이다. 「칸 수」를 지켜야 할 기준선으로 삼지 마라.
> 후보에 **「인계본대로 단색 막대로 되돌리기」**를 반드시 포함하고 … 되돌리는 것이 최선이면 그렇게 권고해도 좋다."*

**인계본 원본에서 직접 확인했다** (`docs/handoff/design_handoff_equipment_window/README.md`):

| 행 | 원문 | 우리 구현 |
|---:|---|---|
| 165 | `상단 등급 리본: 좌우 14 인셋, 높이 2, 등급색 (미보유 opacity .25)` | **4칸 분할 · 높이 4 · 인셋 11 · 미보유 안 흐림** |
| **168** | `이름 13px 600 + **우측 등급 라벨 10px 600 (등급색)**` | ★ **없다 — 낱말이 상세 패널에만 있다** |
| 132 | (상세) `메타 한 줄: 희귀 · 모자 · 오피스 워커 11px, 등급색` | 있다(`:503`) |

**리더 판독이 맞다.** 그리고 **더 큰 차이가 168행에 있다 — 인계본은 낱말을 카드에 적고 우리는 안 적는다.**

### 15.14-a 【안 F】 단색 막대로 되돌리기 — **기각한다. 네 쌍 전부 나빠진다**

같은 자(15.3)로 다시 쟀다. 막대 139×4 = **556 pt²**(등급 무관 일정) vs 4칸 채움 133~532 pt².

| 쌍 | 4칸(현행) | **단색 막대** | 막대/칸 |
|---|---:|---:|---:|
| 일반 ↔ 전설 (**사용자가 지목한 쌍**) | 40,152 | **28,654** | **0.71×** |
| 영웅 ↔ 전설 | 18,290 | **10,020** | **0.55×** |
| 일반 ↔ 희귀 | 8,780 | 8,431 | 0.96× |
| 희귀 ↔ 영웅 | 13,814 | 10,273 | 0.74× |

**막대가 더 크지만(556 > 133~532) 신호는 더 작다.** 이유는 기하가 아니라 대비 상대다 —
칸 방식에서는 «채워진 칸 ↔ 트랙 `#3A4049`» 전이가 **ΔE 83.45**로 «등급색 ↔ 등급색» ΔE 15~52보다
훨씬 크다. **어두운 트랙이 신호 증폭기 노릇을 하고 있었다.**

그리고 **되돌리면 축이 하나 죽는다**:

| | 정상 시각 | **회색조 · 완전색맹 · 흑백 캡처** |
|---|---|---|
| 4칸 | 색 + **길이 33.25 / 66.5 / 99.75 / 133pt** | **길이는 안 죽는다** ✔ |
| 단색 막대 | 색만 | 회색값 151 / 174 / 195 / 217, 인접 최소 **ΔE 7.70 < 7.8** ✘ **전부 미달** |

⇒ **단색 막대는 「덜 구분되고, 접근성 축을 잃는」 안이다. 기각.**

### 15.14-b 그리고 **인계본 등급색 자체를 우리 무대에 못 올린다** (별도 근거)

인계본 램프(README:272) `일반 #8A8F98 / 희귀 #6E9BE8 / 영웅 #B07BE0 / 전설 #E0B24A`를 그대로 재면:

| 검사 | 값 | 판정 |
|---|---:|---|
| 희귀 `#6E9BE8` ↔ **`CardBorderWorn #5DA1F5`** | ΔE **5.77** | ✘ **예약 침범.** 변별 하한 7.8 미달 — *"지금 뭘 쓰고 있는지 눈으로 못 찾겠다"*가 그대로 재현된다 |
| 일반 `#8A8F98` ↔ **`TextTertiary #8B939F`** | ΔE **2.40** | ✘ **예약 침범.** 사실상 같은 색 — *"가진 일반 아이템 14개가 못 얻은 것처럼 보인다"* |

**`TEAM.md`의 색 예약 두 줄이 정확히 이 램프를 겨눠 만들어진 것**이다.
⇒ **「인계본대로 되돌리기」는 색까지 되돌린다는 뜻이면 예약 2건을 동시에 깬다. 색은 브라스 램프를 유지한다.**

### 15.14-c 【브라스 ΔE 12.86 문제】 — **테두리가 그 문제를 2.26배 완화한다**

리더 조건: *"`coder-ui`가 칸 수를 택한 데는 근거가 있었다(§12-5: 브라스 강조색 ↔ 영웅이 ΔE 12.86).
그 문제를 어떻게 푸는지까지 답해야 되돌릴 수 있다."*

| 자리 | 브라스 `#C8A15A`와의 ΔE | |
|---|---:|---|
| **원색끼리** — 브라스 ↔ 영웅 `#DEC081` | **12.86** | ← §12-5가 지적한 값 |
| **테두리 α0.55** — 브라스 ↔ 영웅 `#867858` | **29.11** | ★ **2.26배로 벌어진다** |
| 테두리 α0.55 — 브라스 ↔ 전설 `#988251` | 18.63 | 최근접이 영웅에서 전설로 옮겨 간다 |
| 테두리 α0.55 — 브라스 ↔ 희귀 / 일반 | 39.99 / 48.99 | |

**α0.55로 카드 면 쪽으로 끌어내리면 램프 전체가 브라스에서 멀어진다.**
그리고 **인접 발생 자체가 구조적으로 막혀 있다**:

| 브라스가 카드에 나타나는 유일한 자리 | 그때 그 카드의 테두리 |
|---|---|
| `[해제]` 버튼면 `#9E814D` — **착용 중 카드에만 뜬다** | **파랑 `#4C80C1`** (착용 중이 등급을 덮으므로) |
| `[착용]` 버튼면 `#838589` — 브라스 아님(중성) | 등급색 (ΔE 22.32, 영웅 기준) |

⇒ **§12-5가 요구한 *"강조 칩과 영웅 리본을 한 자리에 붙여 놓지 않는다"*가 안 D에서 자동으로 지켜진다.**
브라스 문제는 **테두리를 도입하면 오히려 나아진다.** 리더 조건 충족.

### 15.14-d ★★ 【안 G】 그런데 **사용자 불만에 가장 직접적인 답은 테두리가 아니다 — 낱말이다**

사용자 문장을 그대로 다시 읽는다: *"**전설인지 일반인지** 잘 구분이 안 갈 거 같음."*
이건 **「나란히 놓고 다른가」(변별)가 아니라 「하나만 보고 무엇인지 맞히는가」(식별)** 를 묻고 있다.

`PALETTE_SPEC §12-0` 실측: **식별 하한 ΔE 48.6을 넘는 쌍은 여섯 쌍 중 「일반↔전설」 하나뿐**이다.
즉 **색으로는 정상 시각에서도 식별이 안 된다.** 개수도 못 한다(「3칸」은 이름이 아니다).
**문자는 ΔE 자를 우회하는 유일한 채널**이다(§12-4 「확정 — 낱말」).

**그리고 인계본은 그 낱말을 카드에 적었고(README:168) 우리는 안 적었다.**
우리가 안 적은 이유는 기하였다(§12-4 / `Cards.cs:499~505`): *"카드 메타 칸은 41pt이고 `LV.20` / `착용 중`이 이미 꽉 쓴다."*
**그 이유는 「이름줄에 넣을 자리가 없다」였지 「카드에 적으면 안 된다」가 아니었다.** 자리를 다시 찾았다.

**대비 검산 — 전건 통과(텍스트 하한 4.5)**

| 등급 | `CardSurface` 위 | `CardSurfaceMuted`(잠김) 위 | `ThumbSurfaceLocked` 위 |
|---|---:|---:|---:|
| 일반 `#9C978C` | **5.68** | **6.11** | **6.40** |
| 희귀 `#BCAC8B` | 7.41 | 7.97 | 8.34 |
| 영웅 `#DEC081` | 9.42 | 10.13 | 10.60 |
| 전설 `#FFD375` | 11.67 | 12.55 | 13.14 |

**자리 후보 2개 — 기하 검산**

```
후보 ① 이름줄 삼분할                          후보 ② 썸네일 좌하단 (★ 권고)
 x=11        81  103      150                 ┌──────────────────────────────┐
 ┌──────────┬──────┬──────────┐               │  썸네일 139 × 54             │
 │ 왕관     │ 전설 │  LV.20   │               │        ┌───────┐            │
 └──────────┴──────┴──────────┘               │        │ 아이콘 │ x47.5~91.5 │
   66pt       22     41                       │        └───────┘            │
 ✘ 이름이 90 → 66pt (26.7% 축소)              │ ┌────┐              ┌───┐   │
   아이템 이름이 잘린다                        │ │전설│ 22×12        │🔒 │   │
                                              │ └────┘ x4~26        └───┘   │
                                              └──────────────────────────────┘
                                               ✔ 아이콘과 수평 겹침 0 (26 < 47.5)
                                               ✔ 자물쇠 배지(우하단 18×17)와 반대편
                                               ✔ 이름·메타 한 글자도 안 건드린다
```

| | 후보 ① 이름줄 | **후보 ② 썸네일 좌하단** |
|---|---|---|
| 신규 `Text` | 1 | 1 |
| 기존 요소 손상 | **이름 24pt 축소 — 일급 정보** | **0** |
| 아이콘 44×44 침범 | 없음 | **없음**(x 4~26 vs 아이콘 x 47.5~91.5) |
| 자물쇠 배지 충돌 | 없음 | 없음(반대편 모서리) |
| 배경 | `CardSurface` CR 5.68~11.67 | `CardSurfaceMuted` / `ThumbSurfaceLocked` CR **6.11~13.14** ← 더 높다 |
| **미확인** | — | ★ **아이템 실루엣이 좌하단으로 흘러내리는 종이 있는가**(망토·긴망토). `design-equipment` 확인 필요 |

⇒ **후보 ②를 권고한다.**

**★ 이건 채널을 「더하는」 것이 아니라 「옮기는」 것이다.** §12-4가 확정한 「세 채널의 분업」
(개수 = 리본 / 색 = 램프 / 낱말 = 문자)이 그대로이고, **낱말의 거처만 상세 패널 → 카드**로 간다.
카드 한 장 안에서 **자리 3 / 축 3** — §27-7이 요구한 형태 그 자체다.
상세 패널의 낱말은 **그대로 둔다**(다른 표면이고, 거기서는 카테고리·레벨과 한 줄을 이룬다).

### 15.14-e 세 안의 우선순위 — **불만 문장에 얼마나 직접 답하는가**

| 순위 | 안 | 무엇을 고치는가 | 숫자 |
|---:|---|---|---|
| **1** | **안 G — 카드 위 등급 낱말** | ★ **식별**(하나만 보고 맞히기). **유일하게 가능한 채널** | ΔE 자 우회 · 대비 5.68~13.14 전건 통과 |
| **2** | **안 D — 카드 외곽선 2pt** | **변별**(나란히 놓고 다름) + 주변시 | 신호량 **1.79~2.05×** · 면적 8.7배 |
| 3 | 안 E — 썸네일 프레임 | 변별. 안 D의 대체안 | 1.95× / 충돌 0 |
| ✘ | 안 F — 단색 막대 복귀 | **아무것도 안 고친다** | **0.55~0.96×** + 회색조 축 소멸 |

> ★ **정직하게**: 사용자는 「테두리」를 지시했고 나는 **테두리를 2순위로 놓았다.**
> 근거는 §12-0의 실측 하나다 — **색은 정상 시각에서조차 등급을 식별시키지 못한다.**
> 테두리는 색이므로 그 한계를 물려받는다. **사용자 지시를 안 따르자는 것이 아니라
> (안 D는 권고안이다), 그것만으로는 불만이 안 풀릴 수 있다는 것**이다.
> **둘 다 넣는 것을 권고한다. 하나만 골라야 하면 안 G가 먼저다.**

### 15.14-f 리더 결정 요청 (추가분)

| # | 결정 | 내 권고 | 근거 |
|---|---|---|---|
| **B-9** | **안 G — 카드 위 등급 낱말** | ★ **채택 · 후보 ②(썸네일 좌하단 22×12)** | 15.14-d. **§12-4의 「낱말」 채널을 옮기는 것이지 더하는 것이 아니다** |
| **B-10** | **안 F — 단색 막대 복귀** | ✘ **기각** | 15.14-a 네 쌍 전부 0.55~0.96× + 회색조 축 소멸 |
| **B-11** | 인계본 등급색 램프 복귀 | ✘ **기각** | 15.14-b 예약 침범 2건(ΔE 5.77 / 2.40) |
| **B-12** | §12-5 브라스 조건 | **충족 — 테두리가 12.86 → 29.11로 벌린다** | 15.14-c + 구조적 회피 |
| **B-13** | 우선순위 | **안 G > 안 D** | 15.14-e. 하나만 간다면 낱말 |
| **B-14** | 인계본 미보유 `opacity .25` | ✘ **기각 유지**(현행 그대로) | `Cards.cs:40~45` 기존 실측(대비 1.45~1.69) |

### 15.14-g 15.14의 미확인

| # | 항목 | 상태 |
|---|---|---|
| 1 | 낱말 2글자의 **실제 렌더 폭**(`FontCaption 10` bold, 한글 폴백) | ★ **미측정.** 22pt는 「10pt × 2글자 + 여유」 추정치다. **Windows 폴백 폰트는 특히 미확인** — `UX_HANDOFF_REVIEW §4-1`이 macOS만 확인했다 |
| 2 | 썸네일 좌하단에 **아이템 실루엣이 흘러내리는가** | ★ **미확인.** `design-equipment` 확인 필요(망토·긴망토 의심) |
| 3 | 낱말 12pt 높이의 ×1.25 / ×1.75 잔차 | 12 → 15.0 / 18.0 / 21.0 / 24.0 **전부 정수** ✔ (짝수 규칙 준수) |
| 4 | 22pt 폭의 배율 | 22 → 27.5 / 33.0 / 38.5 / 44.0 — **비정수 2건.** 24pt로 올리면 30 / 36 / 42 / 48 전부 정수 → **폭 24pt 권고**(x 4~28, 아이콘 x47.5까지 여전히 19.5pt 여유) |
| 5 | 인계본 원본 DOM의 실제 리본/라벨 픽셀값 | **README 텍스트만 읽었다.** `reference/equipment-screen.dc.html` DOM 실측은 안 했다 — **다음 라운드(탭 3종) 과제와 함께** |

### 15.14-h 플랫폼 영향 (15.14 추가분)

| | 내용 |
|---|---|
| **Windows 영향** | **함께 검토함 — 별도 배정 불필요.** 프로덕션 `.cs` 0줄. 낱말 높이 12pt는 네 배율 전부 정수 ✔ / **폭은 22pt가 비정수 2건이라 24pt를 권고**(15.14-g #4) — 리본이 3→4pt로 올라간 것과 같은 처방이다. ★ **한글 폴백 폰트 폭은 Windows 미확인**이고, 이 문서는 그것을 미확인으로 남긴다 |
| **macOS 영향** | 대비·ΔE는 공통. 배율 ×1/×2뿐이라 22pt도 정수(22 / 44)로 떨어져 **이 잔차는 Windows 전용**이다 |
| **새 플랫폼 분기** | 0건 |


---

# 16. ★★ 캐릭터 창 **골격** — 확정본 (2026-09-03, ux-designer / 리더 판정 A~G 반영)

> 사용자 지시: ***"먼저 디자인 와꾸를 짠 다음에 각 위치에 들어가는 걸 상세히 짜는 게 맞지 않아?"***
> **맞다.** §15는 **카드 한 장 안**을 정했는데 **그 카드가 창 어디에 앉는지**가 정해진 적이 없다.
> 이 절이 그 골격이다. **§15는 전부 유효하고 하나도 지우지 않았다** — 다시 계산한 것은 **좌표뿐**이다.
>
> **이 절은 확정본이다.** 리더 판정 **A~G 전량 반영**. 열린 항목은 **권고 2건**(16.13)만 남았다.

## 16.0 확정 사항 한 장 — 무엇이 언제 누구에게서 확정됐는가

| 판정 | 결과 | 출처 · 날짜 |
|---|---|---|
| **A 창 폭 1042 → 1040** | **채택** | ★ **1042는 사용자 확정값이었다.** **리더가 DPI 근거로 2pt 조정**(2026-09-03). 점유율 81.4% → **81.25%**. 조용히 바뀐 것이 아니다 |
| **B 카드 161×108 → 160×116** | **채택** | 리더 2026-09-03 |
| **C 보관함 = 목록 유지** | **채택** | 리더 2026-09-03. **인계본과 갈라지는 유일한 지점** → 16.6 |
| **D 스탯 재분배 패널** | **`design-systems` 배정** | 리더 2026-09-03. **골격은 자리와 크기(248×152)만, 내용은 비운다** |
| **E 「고급까지 N」 제거 · 툴팁 안 만든다** | **채택 · 보류** | 리더 2026-09-03. 16.9에 **보류 사유 + 재검토 조건** |
| **F 가로 캐러셀 삭제 → 세로 그리드** | ★ **사용자 승인 완료 2026-09-03** | 사용자 원문: ***"가로 캐러셀 삭제 · 인계본대로 세로 그리드"*** |
| **G 창 높이 861 → 860** | **채택** | 리더가 **DPI 근거로 1pt 조정**(2026-09-03). ★ 출처 3단계는 아래 |
| 규칙 S6 | **승인** | 리더 2026-09-03 |

### 16.0-a ★★ 창 높이의 출처 — **세 단계를 전부 남긴다. 802가 있었다는 사실을 지우지 마라**

```
① 802  ← 사용자 확정값.        이 창 높이로 사용자가 승인한 적이 있다
② 861  ← 코드 파생값.          CharacterInfoWindow.cs:95 자백 —
                               "확정된 802는 이 레이아웃에서 성립하지 않는다.
                                본문이 세로로 861pt를 실제로 쓰고 있어(40+72+4×156+103+22)
                                창만 802로 줄이면 상세 패널이 모든 화면에서 37pt 잘린다"
                               ⇒ 861은 «사용자가 고른 값이 아니라 레이아웃이 요구한 값»이다
③ 860  ← 리더가 DPI 근거로 1pt 조정(2026-09-03).
                               861 mod 4 = 1 이라 ×1.25에서 1076.25 — 반 픽셀
                               860 = 헤더 64 + 「가장 높은 고정 열」 772(보관함 col2) + 최소 숨틈 24
```

> ### ⚠ **861을 사용자 확정값으로 오해하지 마라.** 사용자 확정은 **802**였고 861은 코드가 취한 파생값이다.
> 그래서 **G는 A와 성격이 다르다** — A(1042→1040)는 사용자 확정을 2pt 건드렸지만,
> **G는 코드 파생값을 4배수로 다듬은 것이라 사용자 확정을 건드리지 않는다.**
> ★ 그리고 **860은 802 쪽으로 1pt 가까워진 값**이지 멀어진 값이 아니다.

## 16.1 이 절이 쓴 자와 그 교정 — 숫자를 믿어도 되는 이유

**교정이 깨지면 이 절의 숫자를 전부 폐기해야 한다.** 그래서 먼저 교정한다.

| 자 | 교정 대상 | 결과 |
|---|---|---:|
| **폭 모형** `한글 1.00F / 숫자·라틴 0.55F / 쉼표 0.275F / 공백 0.30F` | `DESIGN_SYSTEMS_STATS §7-1` 예제 5건 + 그들 판정 2건 | **7/7** |
| **4배수 검사기** | 통과값 `4·24·160·116·860·796·748·712` / ★**탈락**값 `2·3·22·161·54·11·861·797·749·713` | **18/18** |
| **이름 폭 유도기** | 알려진 값 「메타 41 → 이름 87」 재현 | **1/1** ★ 아래 자백 참고 |

★ **탈락 대조를 넣은 이유**: 검사기가 전부 True를 뱉는 고장이면 「전부 통과」와 구분이 안 된다.
`861·797·749·713`(G 이전 값)을 **일부러** 먹여 False가 나오는 것을 봤다.

> ### ★ 자기 버그 자백 — **교정이 실제로 버그를 잡았다**
> 잔차 5건을 처리하다 이름 폭 계산기를 `160 − 12 − meta − 8`로 짰다. **`ThumbX`를 한 번만 뺐다.**
> 메타 41에서 **99**가 나왔는데 앞 라운드에서 독립 검증된 값은 **87**이었다 — 어긋나서 잡혔다.
> 프로덕션 유도는 두 단계다(`CharacterInfoWindow.cs:275~279`):
> `CardMetaX = CardWidth − ThumbX − CardMetaWidth` → `CardNameWidth = CardMetaX − ThumbX − CardNameGap`.
> **알려진 값으로 교정하지 않았으면 잘못된 88을 확정본에 실을 뻔했다.**

**출처 표기**: 【인】 인계본 DOM 실측 / 【우】 우리 출하 코드 실측 / 【유】 이 절이 유도 / 【미】 미확인.

### 16.1-a ★★ **`<script>`와 DOM이 서로 다른 답을 준다** — 이 라운드 최대의 방법론적 발견

`docs/HANDOFF_TABS_INVENTORY.md`(리더 작성)는 인계본의 **`<script>` 전량**을 읽었고,
이 절은 **DOM 마크업**을 읽었다. **두 판독이 3건에서 갈렸고, 갈린 쪽은 전부 DOM이 옳았다.**

| # | `<script>` 판독 | **DOM 실측** | 왜 갈렸나 |
|---|---|---|---|
| 1 | 프리셋 3개 = **외형** 탭 | ★ **보관함** 탭 | `presets`는 `<script>`에서 **탭 무관하게 한 번** 만들어진다(1058행). **어느 탭에 그려지는지는 DOM에만 있다**(`sc-if isBag` 안, 431행) |
| 2 | **아침 인사 / 밤 인사** 토글 | ★ **아침 커피컵 / 밤 잠옷** — 시간대 **소품** | `<script>` 키는 `morning`/`night`뿐(768행). **낱말은 DOM에만 있다**(348·360행, 부제 *"시간대 소품 · 기본 무료"*) |
| 3 | 보관함 = 구성요소 **2종** | ★ **4종** — 프리셋 + **스탯 재분배 패널** | 「스탯 재분배」는 **`<script>`에 데이터가 아예 없다.** DOM에 **하드코딩된 정적 패널**이다(443~445행) |

> ★★ **규칙 — 인계본은 `<script>`와 DOM을 둘 다 읽어야 끝난다.**
> - `<script>`만: **어느 탭에 붙는지**(#1) · **표시 낱말**(#2) · **데이터 없는 정적 UI**(#3)를 못 본다.
> - DOM만: 값의 **계산식**과 **기본값**을 못 본다(등급 스탯 `MAINV`, 가격 사다리).
>
> **이건 이 저장소가 반복해 온 형태의 또 한 판이다** — *"실패한 판독과 성공한 판독이 똑같이 생겼다."*
> **`<script>` 판독은 완결돼 보였다. 안 본 축이 있다는 것이 출력에 안 나타났다.**
>
> ★ 정정은 **리더가 자기 파일에 직접** 넣는다. 이 절은 그 파일을 건드리지 않았다.

### 16.1-b 인계본 DOM 실측 — §15.14-g #5가 남긴 숙제를 여기서 갚는다

§15.14-g #5: *"인계본 원본 DOM의 실제 픽셀값 — **README 텍스트만 읽었다.** DOM 실측은 안 했다."*
**이번에 읽었다.** `reference/equipment-screen.dc.html` 시안 **1a**(브라스, 채택안) 전량.

| 【인】 항목 | 값 | 줄 |
|---|---|---:|
| 창 | `1240 × 800` + `border 1px` ⇒ 내부 **1238 × 798** | 40 |
| 헤더 | `height 66 / flex 0 0 66` · 패딩 `0 24 0 28` | 42 |
| **장비 그리드** | `306px 292px 1fr` ⇒ **306 / 292 / 640** | 55 |
| **외형 그리드** | `400px 1fr` ⇒ **400 / 838** | 265 |
| **보관함 그리드** | `1fr 306px` ⇒ **932 / 306** ← **좌우가 뒤집혀 있다** | 387 |
| **상점 그리드** | `352px 1fr` ⇒ **352 / 886** | 452 |
| col1 패딩 / 간격 | `20` / `gap 13` | 57 |
| 미리보기 액자 | `height 238`, `radius 14` | 58 |
| 착용 슬롯 행 | `height 46`, `gap 6`, 패딩 `0 12` | 128 |
| col2 패딩 | `20 18`, `gap 14`, **`overflow-y:auto`** | 155 |
| 스탯 카드 | 패딩 `11 12`, `gap 8`, 게이지 `height 5`, 카드 간 `gap 10` | 164 |
| **카드 그리드** | `repeat(3,1fr)` `gap 12` ⇒ **한 줄 3장** | 224 |
| 카드 | 패딩 `13`, `gap 9`, 썸네일 `height 78`(패딩 11), 아이콘 `58`, 버튼 `29` | 226 |
| 리본 | `top:0; left:14; right:14; height:2` | 227 |
| 보관함 카드 | `repeat(4,1fr)` `gap 10`, 썸네일 `64`, 아이콘 `46` | 400 |
| 상점 팩 | `1fr 1fr` `gap 12`, 미리보기 `repeat(4,1fr) gap 7` 각 `height 52` | 488 |
| 토글 | `40 × 23`, 노브 `17`, `top 3` | 355 |

## 16.2 골격 규칙 S1~S6

| | 규칙 | 왜 |
|---|---|---|
| **S1** | 모든 열 폭·구역 높이·패딩·간격은 **4의 배수** | ×1.25 / 1.50 / 1.75에서 **잔차 0**. 리본을 3→4pt로 올린 것과 같은 처방 |
| **S2** | **잔차의 거처는 열마다 정확히 하나** — 스크롤 뷰포트 또는 바닥 고정 패널 위의 스페이서 | **눈이 크기를 재지 않는 자리**에 숨긴다. ★ **A·G 채택으로 이 규칙은 이제 「보험」이다** — 숨길 잔차가 없다 |
| **S3** | **세로 레일 집합 하나를 네 탭이 공유한다** | 탭을 바꿔도 세로선이 제자리에 있어야 「같은 창」으로 읽힌다 |
| **S4** | 발견 단서는 **카드 그리드의 부분 노출 하나**(25~75%). **조밀 목록은 행을 자르지 않는다** | 28pt 행이 91% 잘리면 「더 있다」가 아니라 **「깨졌다」**로 읽힌다 |
| **S5** | **스크롤 레일 12pt는 항상 자리를 차지한다**(썸만 숨긴다) | 안 그러면 그리드 폭이 504↔516으로 흔들리고 **섹션 헤더 구분선 길이가 탭마다 달라진다.** ★ 이미 당한 병이다(「헤더만 592에 남았다」) |
| **S6** | **바닥 고정은 「가변 슬롯 ≥ 0」인 동안만.** 0 아래로 내려가면 그 열은 **스크롤 열로 전환** | 작은 화면에서 **잘리는 대신 스크롤된다**(16.10). 리더 승인 |

### ★ A·G 채택으로 **반올림 잔차가 완전히 사라졌다**

```
                조정 전                          확정
창 폭   1042  mod4=2 → ×1.25 = 1302.5 ★      1040  mod4=0 → 1300  ✔
창 높이  861  mod4=1 → ×1.25 = 1076.25 ★      860  mod4=0 → 1075  ✔
본문     797  mod4=1 ★                        796  mod4=0 ✔
col3 뷰포트 749 mod4=1 ★                      748  mod4=0 ✔
보관함 뷰포트 713 mod4=1 ★                     712  mod4=0 ✔  = 20행 «정확히»
```
**비4배수 열: 1042에서 탭마다 1개(총 4개) → 1040에서 0개.**
**비4배수 세로값: 861에서 4개 → 860에서 0개.**

## 16.3 【답 1】 세로 레일 — 네 탭 공유

```
x =  0        248        488                792              1040
     ├─────────┼──────────┼──────────────────┼────────────────┤
        R0-R1     R1-R2         R2-R3            R3-R4
        248        240           304              248
```

| 탭 | 열 구성 | 폭 | 4의 배수 |
|---|---|---|:---:|
| **장비** | R0–R1 │ R1–R2 │ R2–R4 | **248 / 240 / 552** | ✔ ✔ ✔ |
| **외형** | R0–R2 │ R2–R4 | **488 / 552** | ✔ ✔ |
| **보관함** | R0–R3 │ R3–R4 | **792 / 248** | ✔ ✔ |
| **상점** | R0–R2 │ R2–R4 | **488 / 552** | ✔ ✔ |

**네 탭 전부 합 = 1040 ✔ / 전 열이 4의 배수 ✔**

★ **552가 세 탭에서 같다.** 장비·외형·상점의 오른쪽 열은 **같은 스크롤 컨테이너 하나**다.

### 유도 — 카드에서 거꾸로 나온다

```
① 카드 폭                160                     (판정 B)
② 한 줄 3장 + 틈 12       3×160 + 2×12 = 504       【인】도 repeat(3,1fr)
③ 스크롤 열 내부 규약      24(좌패딩) + 504 + 12(거터) + 12(레일) = 552   ★ 잔차 0
④ 남은 폭                1040 − 552 = 488
⑤ 【인】의 306:292 = 1.048 로 488을 가르되 둘 다 4의 배수
                         248 / 240   (비 1.033, 【인】과의 오차 1.4%)
⑥ 보관함 우측 패널 248    → 목록 열 = 1040 − 248 = 792
```

## 16.4 【답 3】 카드 1장 — **160 × 116** (판정 B 확정)

| | 현행【우】 | **확정** | 왜 |
|---|---:|---:|---|
| 폭 | 161 | **160** | 161 ×1.25 = **201.25** ★. 160 → 200/240/280/320 |
| 높이 | 108 | **116** | 108은 4배수지만 **내부가 8pt 초과**(아래) |
| `ThumbX` | 11 | **12** | 11 ×1.25 = **13.75** ★ |
| 썸네일 | 139 × 54 | **136 × 56** | 139는 파생 / 54 ×1.25 = **67.5** ★ |
| **메타 폭** | 41 | **40** | 41 ×1.25 = **51.25** ★. 40 → 50/60/70/80. **판정표 재검증 완료**(16.13) |
| **이름 폭** | 90 | **88** | 파생: 160 − 2×12 − 40 − 8. §15 요구 84 **여유 4pt**(87일 때 3pt보다 낫다) |

> ### ★ **메타 40 · 이름 88 · 자물쇠 20×20 은 「권고(†)」이고 리더 판단 대기다** — 16.13
> 리더 판정 A~G에는 **이 셋이 없었다.** 잔차 5건을 보고하라는 지시에 따라 내가 **찾아서 낸 제안**이다.
> **판정 전에는 현행 값(메타 41 · 이름 87 · 자물쇠 18×17)을 쓴다.**
> 셋 다 **카드 안**이라 열 좌표·창 치수에 영향이 **0**이고, 반려해도 골격은 그대로 성립한다.
> 아래 와이어프레임은 **권고가 채택된 경우**의 좌표다(메타 x 108, 자물쇠 x 124).

### 세로 검산 — 108에서는 왜 안 되는가

```
  상 테두리 (§15.6-b 유도)              2  ┐
  리본 위틈 (§15.6-c 확정 「위4」)        2  ┘ 합 4
  리본      (§15.6-c)                  4      누계   8
  썸네일                               56      누계  64
  틈                                   4      누계  68
  이름·메타줄                           16      누계  84
  틈                                   4      누계  88
  버튼 (UiChrome.MinTargetSizePoints)  24      누계 112   ← 108을 이미 4pt 초과
  하 여백                               4      누계 116 ✔
```

> **108을 지키려면 무엇을 깎아야 하는가**: 버튼 24는 WCAG 2.2 SC 2.5.8 하한이라 못 깎고,
> 리본존 8은 §15.6-c가 「위4+리본4+아래0」으로 **이미 최소화**한 값이다.
> 남는 건 썸네일 56→52(아이콘 44에 여백 4pt — 현행 5pt보다 좁다) **또는** 틈 4+4→0
> (**이름줄이 썸네일·버튼에 맞닿는다** — §15 P0-5가 고친 바로 그 병의 세로판).
> ⇒ **116으로 8pt 늘리는 것이 가장 싼 값이다.** col3은 스크롤하므로 세로 예산에 압박이 없다.
> ★ §15.6-d의 *"1pt도 안 커진다"*는 **안 D가 카드를 안 키운다**는 뜻이었고 이 재유도와 충돌하지 않는다.

### 카드 안 좌표 — `coder-ui` 재개 지점

```
      x=0   2      12        16    40           58        102    124   144 148   160
 y=0  ╔══════════════════════════════════════════════════════════════════════════════╗
   −2 ║ ▒▒                                                                        ▒▒ ║ 테두리 2pt = 등급색 α0.55 (§15 안 D)
   −4 ║ ▒▒        ← 틈 2pt (§15.6-c) →                                            ▒▒ ║
   −8 ║ ▒▒  ┌───────┬───────┬───────┬───────┐                                    ▒▒ ║ 리본 4pt · 4칸 (§15)
      ║ ▒▒  ├───────┴───────┴───────┴───────┴──────────────────────────────┐      ▒▒ ║
      ║ ▒▒  │  썸네일 136 × 56                                             │      ▒▒ ║
      ║ ▒▒  │                    ┌──────────┐                             │      ▒▒ ║
      ║ ▒▒  │                    │ 아이콘 44 │  x 58..102                  │      ▒▒ ║
      ║ ▒▒  │  ┌────┐            └──────────┘             ┌────┐          │      ▒▒ ║
      ║ ▒▒  │  │전설│ 24×12 · x 16..40                    │ 🔒 │ 20×20    │      ▒▒ ║
  −64 ║ ▒▒  └──└────┘───────────────────────────────────  └────┘──────────┘      ▒▒ ║
  −68 ║ ▒▒  왕관                                            LV.20                 ▒▒ ║ 이름 88pt / 메타 40pt(x 108..148)
  −84 ║ ▒▒                                                                        ▒▒ ║
  −88 ║ ▒▒  ┌───────────────────────────────────────────────────────────────┐     ▒▒ ║
      ║ ▒▒  │                        착용                136 × 24           │     ▒▒ ║
 −112 ║ ▒▒  └───────────────────────────────────────────────────────────────┘     ▒▒ ║
 −116 ╚══════════════════════════════════════════════════════════════════════════════╝
```

| 검산 | 값 | 판정 |
|---|---|---|
| 등급 낱말(§15.14-d 후보 ②, 폭 24는 §15.14-g #4) x 16..40 vs 아이콘 x 58 | 여유 **18pt** | ✔ 겹침 0 |
| 자물쇠 배지 20×20 x 124..144 vs 아이콘 오른끝 102 | 여유 **22pt** | ✔ |
| 자물쇠 vs 등급 낱말 (x 40 → 124) | 여유 **84pt** | ✔ |
| 이름 폭 88 vs §15가 요구한 한글 7자 ≈ 84pt | 여유 **4pt** | ✔ |
| 메타 40pt — `design-systems §7-2` 판정표 **전건 재검증**(16.13) | `착용 중`/`보유 중` 33.00 ✔ · `3,200` 24.75 ✔ | **41→40이 새로 깨는 것 0건** |

## 16.5 【답 2】【답 5】 세로 구역 경계 · 스크롤 경계 — 탭별 전량 검산

**세로 레일은 하나다: 헤더 64 + 본문 796 = 860 ✔**(둘 다 4의 배수)

### 16.5-a 장비 탭 (248 │ 240 │ 552)

```
┌── 헤더 64 ─────────────────────────────────────────────────────────────────────────┐
│ 스틱메이트 Lv.7  [장비][외형][보관함][상점•]        보유 13/16   ◎ 동전 1,240   [✕]│
├──────────────┬──────────────┬─────────────────────────────────────────────────────┤
│ col1 248     │ col2 240     │ col3 552   ← 이 열만 스크롤                          │
│ ┌──────────┐ │ 능력치 STATUS│ ● 모자 HEAD  주스탯 집중력 ───────────────── 6 / 6  │
│ │ PREVIEW  │ │ ┌──────────┐ │ ┌────────┐ ┌────────┐ ┌────────┐   ← 160×116, 틈 12│
│ │ 208×240  │ │ │집중력 41 │ │ │        │ │        │ │        │                    │
│ │          │ │ │▬▬▬│▬│▬  │ │ └────────┘ └────────┘ └────────┘                    │
│ │          │ │ │[중급] …  │ │ ┌────────┐ ┌────────┐ ┌────────┐                    │
│ └──────────┘ │ └── 104 ──┘ │ └────────┘ └────────┘ └────────┘                    │
│ ┌──모자 48─┐ │ ┌ 관찰력 ─┐ │ ● 안경 EYES  주스탯 관찰력 ───────────────── 6 / 6  │
│ ┌──안경 48─┐ │ ┌ 매력  ──┐ │ ┌────────┐ ┌────────┐ ┌────────┐                    │
│ ┌─넥타이48─┐ │ ┌ 민첩  ──┐ │ ┌────────┐ ┌────────┐ ┌────────┐                    │
│ ┌──망토 48─┐ │              │ ● 넥타이 NECK  주스탯 매력 ─────────────── 6 / 6   │
│    (가변 40) │   (가변 32)  │ ┌────────┐ ┌────────┐ ┌────────┐  ← 50% 만 보인다 ▒│
│ ┌──────────┐ │ ┌──────────┐ │ ╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌ ░│
│ │선택 아이템│ │ │테마 세트 │ │                                                   ░│
│ │  상세 248│ │ │  SET 232 │ │                                     스크롤 레일 12 ░│
│ └──────────┘ │ └──────────┘ │                                                     │
└──────────────┴──────────────┴─────────────────────────────────────────────────────┘
```

| col1 = 248 (내용 208) | pt | | col2 = 240 (내용 200) | pt |
|---|---:|---|---|---:|
| 상 패딩 | 20 | | 상 패딩 | 24 |
| 미리보기 액자 208×240 | 240 | | 「능력치 STATUS」 헤더 | 20 |
| 틈 | 12 | | 틈 | 12 |
| 착용 슬롯 4×48 + 3×8 | 216 | | 스탯 카드 4×104 + 3×12 | 452 |
| **가변 스페이서** | **40** | | **가변 스페이서** | **32** |
| 선택 아이템 상세(바닥 고정) | 248 | | 테마 세트 패널(바닥 고정) | 232 |
| 하 패딩 | 20 | | 하 패딩 | 24 |
| **합** | **796 ✔** | | **합** | **796 ✔** |

★ **가변 슬롯 40 · 32도 4의 배수다**(861에서는 41 · 33이었다).

**col3 = 552 · 상하 패딩 24 → 스크롤 뷰포트 748 ✔(4의 배수)**

```
섹션(카드 2행) = 헤더 24 + 틈 12 + 116 + 틈 12 + 116 = 280
4섹션 + 섹션 틈 3×24            = 4×280 + 72 = 1192   >  뷰포트 748  ⇒ 스크롤한다
   섹션1  y   0..280   온전히
   섹션2  y 304..584   온전히
   섹션3  y 608..888   ★ 140 / 280 = 50%  ← 규칙 S4의 발견 단서 (140도 4의 배수)
   섹션4  y 912..      안 보임
```

> ★★ **가로 캐러셀은 삭제된다 — 사용자 승인 2026-09-03.**
> 원문: ***"가로 캐러셀 삭제 · 인계본대로 세로 그리드"***. 리더가 **되돌릴 수 없는 결정**임을
> 명시하고 물은 뒤 받은 승인이다.
> 죽는 상수: `CarouselViewportWidth` / `CarouselFullCards` / `CarouselPeekFraction` /
> `CarouselDragThresholdPoints` / `CarouselClickSuppressSeconds`.
> ★ **발견 단서는 사라지지 않고 옮겨간다** — 「반쯤 걸친 카드」(가로 46%) → **「반쯤 보이는 섹션」(세로 50%)**.
> 2026-09-01 사용자 신고(*"어설픈 데서 절반 잘려 있어서 더 이상함"*)가 겨눈 것은 **자르는 선의 위치**였고,
> 세로 그리드에서는 자르는 선이 **섹션 경계**라 「허공에서 잘림」이 원리적으로 안 생긴다.

### 16.5-b 외형 탭 (488 │ 552) — 이 탭만 창 높이가 다르다

```
┌── 헤더 64 ───────────────────────────────────────────────────────────────────┐
├────────────────────────┬────────────────────────────────────────────────────┤
│ col1 488               │ col3 552  ← 스크롤 불필요                          │
│ ┌────────────────────┐ │ ▌표시 DISPLAY ─────────────────────────────       │
│ │  APPEARANCE        │ │ ┌────────────────────────────────────────────┐    │
│ │  액자 440 × 436    │ │ │ 캐릭터 크기   70%~150%              100%  68│    │
│ │  (종횡비 1.009)    │ │ │ ─────────────────────────────────────────  │    │
│ │       ○            │ │ │ 윤곽선 강조   밝은 배경에서도…    켜짐 [●]56│    │
│ │      ╱│╲           │ │ │ ─────────────────────────────────────────  │    │
│ │       │            │ │ │ 저전력 렌더링 구형 하드웨어용   [30][45][60]│    │
│ │      ╱ ╲           │ │ └────────────────────────────────── 214 ────┘    │
│ └────────────────────┘ │ ▌소품 · 팩 색상 PROPS ─────────────────────       │
│ ┌── 캐릭터 이름 120 ─┐ │ ┌────────────────────────────────────────────┐    │
│ │ 스틱│  최대 8자    │ │ │ 아침 커피컵  시간대 소품·기본무료  꺼짐 [○]56│    │
│ └────────────────────┘ │ │ 밤 잠옷      시간대 소품·기본무료  꺼짐 [○]56│    │
│                        │ │ 파쿠르 파티클 색상 [◉오피스][○사이버][○네온]│    │
│                        │ │                   [○스포츠][○잉크][○밀리]116│    │
│                        │ └────────────────────────────────── 262 ────┘    │
└────────────────────────┴────────────────────────────────────────────────────┘
```

**창 높이를 콘텐츠에서 역산한다 — 이 탭은 861/860 어느 쪽과도 무관하다:**

```
[표시]      헤더 24 + 틈 12 + 상자 214 = 250      상자 = 16 + 68 + 1 + 56 + 1 + 56 + 16
[소품·팩색] 헤더 24 + 틈 12 + 상자 262 = 298      상자 = 16 + 56 + 1 + 56 + 1 + 116 + 16
col3 콘텐츠 = 250 + 24 + 298 = 572
col3 열      = 24 + 572 + 24 = 620
창 높이      = 64 + 620      = 684        ★ 855 / 1026 / 1197 / 1368 전부 정수
col1         = 620 − (24 + 16 + 120 + 24) = 액자 440 × 436   ★ 436도 4의 배수
```

> ★ **현행 `PanelHeightForTab`(외형 705)의 존재 이유가 골격에서도 유효함이 확인됐다.** 값만 705 → **684**.
> **폐지하지 마라** — 폐지하면 외형 탭에 **176pt의 빈 아래**가 생긴다
> (뷰포트 748 − 콘텐츠 572 = 176. 본문의 **22.1%**).
> ★ **정정**: 이 값을 앞서 「233pt」로 적었는데 **틀렸다.** 그때는 설정 상자 높이를 214/262가 아닌
> 거친 추정(합 516)으로 잡고 있었다. 그 뒤 행 구성을 확정해 콘텐츠가 **572**로 확정됐고,
> 861 기준으로도 749 − 572 = **177**이지 233이 아니다. **창 높이가 아니라 콘텐츠 추정이 바뀐 것이다.**
> ★ 【인】의 「아침 인사/밤 인사」가 아니라 **「아침 커피컵 / 밤 잠옷」 시간대 소품**이다(16.1-a #2).

### 16.5-c 보관함 탭 (792 │ 248) — ★ **G 채택으로 20행이 정확히 떨어진다**

```
┌── 헤더 64 ───────────────────────────────────────────────────────────────────┐
├──────────────────────────────────────────────────────┬──────────────────────┤
│ col1 792  ← 스크롤 (행 클램프)                       │ col2 248             │
│ ▌보관함 INVENTORY ──────────────────── 13종 보유     │ 세트 프리셋 PRESET   │
│ ┌──┬──────────┬────┬──────┬──────────┬─────────────┐ │ ┌──────────────────┐ │
│ │◆ │ 중절모   │모자│ 희귀 │오피스워커│  환급 ◎ 560│ │ │오피스워커 세트완성│ │
│ ├──┼──────────┼────┼──────┼──────────┼─────────────┤ │ │ [🎩][👓][👔][🎒] │ │
│ │◆ │동그란안경│안경│ 일반 │오피스워커│  환급 ◎ 240│ │ │ 중절모·동그란안경…│ │
│ │  … 행 28 + 틈 8 = 스텝 36 × 20행 = 712pt          │ │ │ [   적용   ]  172│ │
│ │                                                   │ │ └──────────────────┘ │
│ │  ★ 뷰포트 712 = 20행 «정확히». 잘린 행이 0이다    │ │ ┌ 야간 잠행    172 ┐ │
│ │     (861에서는 713이라 1pt가 남았다)              │ │ ┌ 연회장 2종미보유 ┐ │
│ └───────────────────────────────────── 레일 12 ────┘ │      (가변 24)       │
│                                          하 패딩 24  │ ┌ 스탯 재분배 152 ─┐ │
└──────────────────────────────────────────────────────┴──────────────────────┘
```

| col1 = 792 | pt | | col2 = 248 | pt |
|---|---:|---|---|---:|
| 상 패딩 | 24 | | 상 패딩 | 24 |
| 「보관함」 헤더 + `N종 보유` | 24 | | 「세트 프리셋」 헤더 | 20 |
| 틈 | 12 | | 틈 | 12 |
| **목록 뷰포트** | **712 = 20행 정확** | | 프리셋 카드 3×172 + 2×12 | 540 |
| 하 패딩 | **24** | | **가변 스페이서** | **24** |
| | | | 「스탯 재분배」 패널 **(자리만)** | 152 |
| | | | 하 패딩 | 24 |
| **합** | **796 ✔** | | **합** | **796 ✔** |

> ### ⚠ **G의 대가 — 보관함 col2 숨틈이 25 → 24로 「하한과 정확히 같아졌다」**
> 24는 **최소 숨틈이자 `UiChrome.MinTargetSizePoints`와 같은 수**다. 즉 **여유가 0이다.**
> **하한에 붙은 값은 다음 변경에서 가장 먼저 깨진다**(리더 경고).
> 구체적으로 이 열에서 **무엇이든 4pt라도 커지면 규칙 S6이 즉시 발동해 col2가 스크롤 열로 바뀐다**:
> - 프리셋 카드가 172 → 176이 되면
> - 프리셋이 3장 → 4장이 되면(+184)
> - 「스탯 재분배」 패널이 152를 넘으면 (★ **`design-systems` 배정 중** — 가장 현실적인 위험)
>
> ⇒ **`design-systems`에게 못박아 보낼 제약**: **스탯 재분배 패널은 152pt를 넘을 수 없다.**
> 넘으면 창이 커지는 것이 아니라 **그 열 전체가 스크롤로 바뀐다.**

**행 28 + 틈 8 = 스텝 36 → 20행 = 712pt = 뷰포트 712 ✔ 잔여 0**

★ **프리셋 카드 172pt는 【인】보다 늘렸다** — 【인】은 본문이 **글**뿐인데, 「이 프리셋이 무엇을
입히는가」는 **그림이 말해야 한다**. **구성 4종 아이콘 44×44(틈 8, 합 200 = 내용 폭)** 를 넣었다.
```
프리셋 172 = 패딩16 + 이름·태그줄20 + 틈8 + 아이콘 44 + 틈8 + 본문 1줄16 + 틈12 + 버튼32 + 패딩16
재분배 152 = 패딩16 + 제목줄20 + 틈8 + 본문 3줄 48 + 틈12 + 버튼32 + 패딩16   ★ 자리만(판정 D)
```

## 16.6 ★★ 【판정 C】 보관함은 **목록이다** — 인계본과 갈라지는 유일한 지점

> ### ⚠ **이것을 「인계본 미준수」로 읽고 되돌리지 마라. 리더가 근거를 보고 채택한 판정이다(2026-09-03).**

【인】은 보관함을 **4열 카드 그리드**로 만들었다(`repeat(4,1fr)`, 400행). **우리는 목록을 유지한다.**

| # | 근거 | 숫자 |
|---|---|---|
| **1** | 【인】의 보관함 카드는 **장비 탭 카드와 거의 같은 그림**이다. 다른 것은 하단 칩이 `판매 환급`이라는 것뿐 — **같은 탭이 두 번 있는 것처럼 보인다** | 필드 6개 중 5개 동일 |
| **2** | 보관함의 일은 **훑고 파는 것**이다. **한 화면에 담기는 종 수가 같은데 세로를 3.6배 쓴다** | 목록: 스텝 36 × **20종** / 그리드: 스텝 128 × 5행 × 4열 = **20종**. 세로 소비 36 vs 128 |
| **3** | **【우】현행이 이미 목록이다** — 되돌리는 것이 아니라 **유지**다 | `InventoryRowHeight 24` / `InventoryVisibleRows 20`(실측) |

**바꾸는 것은 행 높이 하나뿐이다: 24 → 28.**
`24`는 WCAG 2.2 SC 2.5.8 하한과 **정확히 같아** 여유가 0이고, `28`은 4의 배수이면서 하한을 4pt 넘는다.
★ 그리고 **`InventoryVisibleRows` 20은 바뀌지 않는다** — 행이 커졌는데도 20행이 유지되는 이유는
창 세로 예산이 그만큼 넉넉해졌기 때문이다(2열 → 목록 전용 열 792).

> ★ **【인】에서 가져오는 것**: 행에 실을 필드는 【인】의 카드 필드를 그대로 쓴다 —
> `name` / `slotKo` / `rarLabel` + 등급색 / `themeKo` / **`refund`(환불 40%)**.
> **갈라지는 것은 「배치」뿐이고 「내용」은 인계본을 따른다.**

## 16.7 상점 탭 (488 │ 552)

```
┌── 헤더 64 ───────────────────────────────────────────────────────────────────┐
├───────────────────────────────────┬─────────────────────────────────────────┤
│ col1 488  ← 스크롤(행 클램프)     │ col3 552  ← 스크롤 불필요               │
│ ▌단품 구매 COINS                  │ ▌DLC 팩 PACKS ──── 4종 + 파티클 색상   │
│ 전 등급을 동전으로 구매할 수      │ ┌──────────────┐ ┌──────────────┐      │
│ 있습니다. DLC 팩으로도 …   (2줄)  │ │오피스워커 보유│ │사이버…    DLC│      │
│ ┌───┬───────────────────┬───────┐ │ │[🎩][👓][👔][🎒]│ │[👑][🥽][🧐][🧥]│    │
│ │◆56│ 고글        희귀   │[구매] │ │ │책상업무 성과물│ │글리치 파티클 │      │
│ │   │ 관찰력 +6 · 민첩 +2│       │ │ │기본 제공[적용]│ │₩4,900 [구매] │      │
│ │   │ 사이버 · 동전 1,400│       │ │ └── 240×204 ──┘ └──────────────┘      │
│ └───┴──── 행 72, 틈 12 ─┴───────┘ │      틈 24                              │
│  … 8행 = 660pt, 하 패딩 28        │ ┌ 네온 ┐ ┌ 스포츠 ┐                     │
│                        레일 12 ░  │ ┌ 잉크 ┐ ┌ 밀리터리┐   아래 52pt 빔     │
└───────────────────────────────────┴─────────────────────────────────────────┘
```

| col1 = 488 | pt | | col3 = 552 | pt |
|---|---:|---|---|---:|
| 상 패딩 | 24 | | 상 패딩 | 24 |
| 「단품 구매 COINS」 헤더 | 24 | | 「DLC 팩 PACKS」 헤더 | 24 |
| 틈 | 12 | | 틈 | 12 |
| 안내 문구 2줄 | 32 | | **팩 그리드 뷰포트** | **712** |
| 틈 | 16 | | 하 패딩 | 24 |
| **목록 뷰포트 664 → 660 클램프(8행)** | 660 | | | |
| 하 패딩 (24 + 잔여 4) | **28** | | | |
| **합** | **796 ✔** | | **합** | **796 ✔** |

```
팩 카드 240 × 204, 2열 · 틈 24  →  2×240 + 24 = 504 = 그리드 504   ★ 잔차 0
팩 204 = 패딩20 + 이름·배지줄20 + 틈12 + 미리보기 44 + 틈12 + 설명2줄32 + 틈12 + 가격·버튼줄32 + 패딩20
미리보기 4칸 = 4×44 + 3×8 = 200 = 내용 폭(240 − 2×20) ✔   【인】 repeat(4,1fr)과 같은 구성
6팩 = 3행 × 204 + 2 × 24 = 660  ≤  뷰포트 712   ⇒ ★ 스크롤 불필요 (아래 52pt 빔, 52도 4의 배수)
```
> 52pt(본문의 6.5%)는 **놔둔다.** 팩은 6개 고정이고 창 높이는 **왼쪽 목록**(가변 길이)이 정한다.
> 여기를 채우려고 팩 카드를 키우면 팩이 늘어나는 날 다시 뜯어야 한다.

## 16.8 【답 4】 골격은 **하나인가 넷인가** — **셸 1 + 레일 1 + 그리드 4**

| 층 | 공유 여부 | 근거 |
|---|---|---|
| **셸**(헤더 64 · 창 폭 1040 · 화면 중앙 · [✕] · 클램프) | **완전 공유** | 탭은 창 안의 상태이지 다른 창이 아니다 |
| **세로 레일**(x = 0 / 248 / 488 / 792 / 1040) | **완전 공유** | 탭을 바꿔도 세로선이 제자리 — 규칙 S3 |
| **열 구성** | **탭마다 다름**(3 / 2 / 2 / 2) | 【인】도 그렇고, 내용이 실제로 다르다 |
| **스크롤 열 내부 규약**(24 + 그리드 + 12 + 12) | **완전 공유** | 552가 세 탭에서 같은 객체 |
| **창 높이** | **콘텐츠 역산, 860 상한** | 장비/보관함/상점 = 860(스크롤이라 상한) · **외형 = 684** |

> **답은 「둘 다 아니다 — 셸 1 + 레일 1 + 그리드 4」다.**
> 레일을 공유하지 않으면 탭 전환이 **다른 창으로 갈아탄 것처럼** 보이고,
> 그리드까지 억지로 공유하면 외형 탭에 **176pt의 빈 아래**가 생긴다.

## 16.9 【판정 E】 「고급까지 N」 — 제거한다 · **툴팁은 보류(재검토 조건 명시)**

### 왜 제거인가

```
스탯 카드 내부 폭 = 240(col2) − 2×20(열 패딩) − 2×16(카드 패딩) = 168pt

① 「고급까지 27」 유지 → 효과문구 예산  62.00pt  → ★ 12문구 중 11개 말줄임
② 제거                → 효과문구 예산 124.00pt  →   12문구 중  2개 말줄임
```
```
  78.00 ①✘ ②✔ 성과물 배율 +10%       109.50 ①✘ ②✔ 쿨다운 -25% · 잔상 강화
  89.00 ①✘ ②✔ 명상 유휴 연출 해금      76.00 ①✘ ②✔ 오라 이펙트 발현
 132.50 ①✘ ②✘ 성과물 배율 +25% · 졸음  66.00 ①✘ ②✔ 오라 범위 확대
  88.00 ①✘ ②✔ 활쏘기 쿨다운 -10%     123.50 ①✘ ②✔ 오라 강도 최대 · 팩 색 연동
  76.00 ①✘ ②✔ 궤적 잔상 이펙트         59.50 ①✔ ②✔ 이동속도 +8%
                                       99.00 ①✘ ②✔ 던지기 최소 회전 보장
                                      139.50 ①✘ ②✘ 이동속도 +18% · 트레일 이펙트
```
남는 2건은 **앞 절만** 쓴다: `성과물 배율 +25%`(78.00 ✔) / `이동속도 +18%`(65.00 ✔).
둘째 절(`졸음 연출` / `트레일 이펙트`)은 **상세 패널**로 내린다.

### 어디로 옮길 수 있는가 — 자리 후보 4개 전량 검산

| 후보 | 검산 | 판정 |
|---|---|---|
| ① 임계줄에 그대로 | 예산 62pt에 11/12 말줄임 | ✘ **표시가 아니다** |
| ② **이름줄 우측**(`41 (+33)` 옆) | 이름줄 여유 **27.80pt** < 「고급까지 27」 **54.00pt** | ✘ **26pt 부족** |
| ③ **게이지 아래 눈금 라벨 줄**(`10 · 20 · 32`) | 스탯 카드 104 → **120**(+16). col2 세로 = **852** vs 본문 796 | ✘ ★ **56pt 넘침** |
| ④ **스탯 카드 호버 툴팁** | 호버 폴링은 **이미 있다**(`Input.cs:363 UpdateHover`). 툴팁 **표면**은 이 창에 없다(`GearRadialMenuWidget`에만) | △ 가능 · **신규 기구 1개** |

> **한 줄 답: 「자리」로는 못 옮긴다(②③ 전부 예산 초과). 유일한 길은 ④ 호버 툴팁이고 신규 기구 1개가 든다.**

### ★ 리더 판정 — **툴팁을 지금 만들지 않는다** (2026-09-03)

근거: 게이지의 임계 눈금 3개가 **위치로** 이미 답하고, 「고급까지 27」은 사용자가 **그 순간
행동을 바꿀 수 있는 수가 아니다**(장비 자리는 4칸뿐이고 이미 다 보인다).
**신규 기구 1개를 그 값 하나를 위해 만들 자리가 아니다.**

> ### 보류 · 재검토 조건 — **아래 중 하나라도 참이 되면 그때 무료로 얹는다**
> 1. **툴팁 기구가 다른 이유로 이 창에 생긴다.** (가장 유력: 잠긴 카드의 해금 조건 · 세트 효과 상세 ·
>    DLC 팩 미리보기). 그때는 **`Text` 하나를 더 채우는 비용**밖에 안 든다.
> 2. **임계가 3단(10/20/32)보다 촘촘해진다.** 눈금이 많아지면 「위치로 읽기」가 무너져
>    숫자가 다시 필요해진다 — `design-systems` 소관.
> 3. **사용자가 「다음 단계까지 얼마나 남았는지 모르겠다」고 신고한다.** 그 신고가 오면
>    이 판정은 반증된 것이고, 근거였던 *"행동을 바꿀 수 있는 수가 아니다"*가 틀린 것이다.
>
> ★ **정보 자체를 없애자는 것이 아니다.** 【인】 렌더에 실재하는 값(`중급까지 4`)이고,
> 우리는 그것을 **게이지 눈금이라는 다른 채널로 이미 운반하고 있다**는 것이 이 판정의 내용이다.

## 16.10 【답 6】 빈 상태 · 로딩 · 예외 — 역할 규정 형식

### 16.10-a 보유 0개 (신규 유저 / 필터 결과 0)

| | 내용 |
|---|---|
| **목표** | 「비어 있다」와 「고장났다」를 가른다 |
| **트리거** | 보관함 목록 0행 · 상점 단품 0행(전부 보유) · 카드 섹션 0장 |
| **유저가 보는 것** | 뷰포트 **중앙**에 1줄 + 1줄. 보관함 *"아직 가진 장비가 없습니다 / [상점] 탭에서 첫 장비를 얻어 보세요"* · 상점 *"살 수 있는 단품이 없습니다 / 이미 전부 가지고 있어요"*. **헤더의 개수 표시는 `0종 보유`로 남는다** — 헤더까지 사라지면 「로딩 중」과 구분이 안 된다 |
| **액션** | 보관함 빈 상태의 **[상점] 낱말은 탭으로 가는 링크**(신규 유저의 유일한 출구) |
| **레일** | ★ **썸을 숨기되 12pt 자리는 유지**(규칙 S5) — 안 그러면 빈 상태에서만 그리드가 12pt 넓어진다 |
| **탈출구** | 해당 없음(방해성 이벤트 아님). 창 닫기는 [✕]·`I` 토글 그대로 |

### 16.10-b 스크롤 불필요

| | 내용 |
|---|---|
| **어디서** | **외형 col3**(콘텐츠 572, 뷰포트 572 — 창 높이가 역산이라 **항상** 딱 맞다) · **상점 col3**(660 ≤ 712) |
| **보이는 것** | 레일 12pt는 그대로 있고 **썸만 없다.** 트랙도 안 그린다(빈 홈이 「스크롤이 고장났다」로 읽힌다) |
| **왜 자리를 비우지 않는가** | 규칙 S5. 상점에서 왼쪽은 스크롤하고 오른쪽은 안 하는데, 레일이 사라지면 **두 열의 오른쪽 여백이 12pt 어긋나 보인다** |

### 16.10-c ★★ 창이 화면보다 큰 경우 — **9개 환경 중 5개에서 잘린다. 그중 4개가 Windows**

`CharacterInfoWindow.Layout.cs:41` — `height = min(탭 높이, max(320, 화면세로 / 배율 − 32))`

| 환경 | 논리 세로 | 가용 | 장비 860 | 외형 684 |
|---|---:|---:|---|---|
| Win 1920×1080 @100% | 1080 | 1048 | OK | OK |
| **Win 1920×1080 @125%** | 864 | 832 | ★ **28pt 잘림** | OK |
| **Win 1920×1080 @150%** | 720 | 688 | ★ **172pt 잘림** | OK |
| **Win 1920×1080 @175%** | 617 | 585 | ★ **275pt 잘림** | ★ **99pt 잘림** |
| Win 2560×1440 @150% | 960 | 928 | OK | OK |
| **Win 2560×1600 @200%** | 800 | 768 | ★ **92pt 잘림** | OK |
| mac MBA 13 (1440×900) | 900 | 868 | OK (여유 8pt) | OK |
| mac MBP 14 (1512×982) | 982 | 950 | OK | OK |
| **mac 1280×800** | 800 | 768 | ★ **92pt 잘림** | OK |

> ★★ **이건 골격이 만든 문제가 아니라 지금 출하 중인 결함의 규명이다**(리더 확인 2026-09-03).
> 현행 861도 같은 5개 환경에서 잘린다. **골격은 그것을 「잘림」이 아니라 「스크롤」로 바꾼다.**

**규칙 S6 (리더 승인)** — 가변 슬롯이 0 아래로 내려가면 그 열은 **위→아래로 흐르는 스크롤 열로 전환**된다.

| 열 | 고정 합 | 이 창 높이 **미만**에서 전환 | 860에서 여유 |
|---|---:|---:|---:|
| **보관함 col2** | 772 | **836** ← 가장 먼저 | **24** ★ 하한 |
| 장비 col2 | 764 | 828 | 32 |
| 장비 col1 | 756 | 820 | 40 |
| 외형 col3 | 298 | 362 | — |
| 상점 col3 | 288 | 352 | 424 |
| 상점 col1 | 204 | 268 | 508 |
| 장비 col3 | 200 | 264 | 512 |
| 외형 col1 | 184 | 248 | — |
| 보관함 col1 | 112 | 176 | 600 |

> **첫 전환은 화면 논리 세로 868pt 미만에서 일어난다.** 그 아래로는 열이 하나씩 스크롤로 바뀌고
> **`MinPanelHeight` 320까지 아무것도 사라지지 않는다.**
> ★ 320은 장비 col3(264)·보관함 col1(176)보다 크므로 **최소 창에서도 카드 1장·목록 1행은 보인다** ✔

### 16.10-d 로딩 · 에러 · 거부

| | 내용 |
|---|---|
| **로딩** | 데이터는 `ItemCatalog`(정적) + 세이브(이미 메모리). **I/O 대기가 없다** ⇒ 스피너를 만들지 마라. 유일한 비동기는 **미리보기 액자의 촬영장 카메라 첫 프레임**이고, 액자 배경(`PortraitSurface`)이 이미 채워져 있어 **빈 사각형이 안 보인다** |
| **에러** | 등급/슬롯 열거형 범위 밖 → §15.7-c 그대로(`UiChrome.WarnUnknownRarity`). **골격은 이 경로를 안 바꾼다** |
| **거부** | 착용 불가(레벨 미달·DLC 미보유) → 카드 하단 버튼이 비활성 라벨로. §15.9 확정대로 **카드를 흐리지 않는다** |
| **탈출구** | 이 창은 방해성 이벤트가 아니다(유저가 연다). 닫기 = [✕] · `I` 재토글 · 트레이 메뉴. **바깥 클릭으로는 안 닫힌다**(사용자 확정) ⇒ **[✕]가 화면 밖으로 나가면 안 된다** — 오른쪽 앵커 유지 필수 |

## 16.11 ★★ `coder-ui` 인계표 — **확정 수치 전량. 여기만 보면 된다**

> **이 표 밖의 숫자는 근거·유도이지 구현 대상이 아니다.** 아래 값만 코드로 간다.
> ★ **†** 표시 2건은 **권고이고 리더 판단 대기**다(16.13). 판정 전에는 **현행 값을 쓴다.**

### ① 셸

| 이름 | 값 | 비고 |
|---|---:|---|
| 창 폭 | **1040** | 1042 → 1040. **리더가 DPI 근거로 2pt 조정**(2026-09-03). 1042는 사용자 확정값이었다 |
| 창 높이(장비·보관함·상점) | **860** | 802(사용자 확정) → 861(코드 파생) → **860**(리더가 DPI 근거로 1pt 조정, 2026-09-03) |
| 창 높이(외형) | **684** | `PanelHeightForTab` **유지**, 705 → 684 |
| 헤더 높이 / 본문 높이 | **64 / 796** | 제목·탭·재화·[✕]가 한 줄 |
| 화면 여백 / 최소 창 | **16 / 320×320** | 현행 유지 |

### ② 세로 레일 (네 탭 공유)

| 레일 | x |
|---|---|
| R0 / R1 / R2 / R3 / R4 | **0 / 248 / 488 / 792 / 1040** |
| 탭별 열 | 장비 `248│240│552` · 외형 `488│552` · 보관함 `792│248` · 상점 `488│552` |

### ③ 스크롤 열 내부 규약 (552 · 792 공통)

| 이름 | 값 |
|---|---:|
| 좌 패딩 / 거터 / 스크롤 레일 | **24 / 12 / 12** |
| 상 패딩 / 하 패딩 | **24 / 24** (상점 col1만 하 패딩 **28**) |
| 그리드 폭 | 552 → **504** · 792 → **744** |
| 레일 가시성 | **자리는 항상 차지, 썸만 숨김**(규칙 S5) |

### ④ 카드 (판정 B)

| 이름 | 값 | 이름 | 값 |
|---|---:|---|---:|
| 카드 | **160 × 116** | 메타 폭 / 메타 x | **40† / 108†** (현행 41 / 107) |
| 카드 틈 | **12** | 이름 폭 / 이름-메타 틈 | **88† / 8** (현행 87) |
| `ThumbX` | **12** | 이름·메타줄 y / 높이 | **−68 / 16** |
| 썸네일 | **136 × 56** (y −8) | 버튼 y / 높이 / 폭 | **−88 / 24 / 136** |
| 아이콘 | **44** (x 58..102) | 테두리 두께 / 인셋 | **2 / 2** (§15 안 D) |
| 등급 낱말 | **24 × 12**, 인셋 **4** (x 16..40) | 리본 위틈 / 높이 | **4 / 4** (§15.6-c) |
| 자물쇠 배지 | **20 × 20†** (x 124..144, 현행 18×17) | 하 여백 | **4** |

### ⑤ 장비 탭

| col1 = 248 | 값 | col2 = 240 | 값 | col3 = 552 | 값 |
|---|---:|---|---:|---|---:|
| 좌우 패딩 | 20 | 좌우 패딩 | 20 | 뷰포트 | **748** |
| 미리보기 | **208 × 240** | 헤더 | 20 | 섹션 헤더 | 24 |
| 슬롯 행 / 틈 | **48 / 8** ×4 | 스탯 카드 / 틈 | **104 / 12** ×4 | 섹션 내 틈 | 12 |
| 상세 패널 | **248**(바닥 고정) | 세트 패널 | **232**(바닥 고정) | 섹션 간 틈 | 24 |
| 가변 | **40** | 가변 | **32** | 한 줄 카드 수 | **3** |

### ⑥ 외형 · 보관함 · 상점

| 탭 | 값 |
|---|---|
| **외형** | col1 488(패딩 24) 액자 **440 × 436** · 이름 카드 **120** · 틈 16 / col3 552 설정 행 **56**, 슬라이더 행 **68**, 구분선 1, 상자 패딩 16, 그룹 틈 24 |
| **보관함** | col1 792 목록 행 **28** + 틈 **8**(스텝 36) **20행 = 뷰포트 712 정확** / col2 248 프리셋 **172** ×3 틈 12(구성 아이콘 **44** ×4 틈 8), 재분배 **152**(★ 자리만 · **152 상한**), 가변 **24**(★ 하한) |
| **상점** | col1 488 단품 행 **72** + 틈 **12** **8행 = 660**, 안내 2줄 32, 하 패딩 **28** / col3 552 팩 카드 **240 × 204** 2열 틈 **24**, 미리보기 칸 **44** ×4 틈 8, 팩 패딩 20, 뷰포트 712 |

### ⑦ 삭제 대상 (사용자 승인 2026-09-03 — 판정 F)

```
CarouselViewportWidth · CarouselFullCards · CarouselPeekFraction
CarouselDragThresholdPoints · CarouselClickSuppressSeconds
```

### ⑧ `coder-ui`가 받을 때 **첫 확인 항목** (리더 지정)

> ★ **`CharacterPortraitStage.DesignAspect`가 탭마다 달라진다.**
> 장비 액자 (208−16)/(240−16) = **0.857** / 외형 액자 **440 × 436** = **1.009** / 현행 = **1.044**.
> **지금 구조가 탭별 종횡비를 받는가?** 못 받으면 그것이 착수 전 선결 과제다.

### ⑨ 다른 팀에 못박아 보낼 제약 2건

| 대상 | 제약 |
|---|---|
| **`design-systems`** | ★ **「스탯 재분배」 패널은 152pt를 넘을 수 없다.** 넘으면 창이 커지는 것이 아니라 **보관함 col2 전체가 스크롤 열로 바뀐다**(규칙 S6). 그 열의 숨틈이 **하한 24에 정확히 붙어 있다** |
| **`design-systems`** | ★ **임계 효과 문구는 124pt(≈ 한글 12자)를 넘을 수 없다.** 넘는 2건은 앞 절만 쓴다(16.9) |

## 16.12 ★ 잔차 최종 집계 — **리더 요구: "남는 게 있으면 그것도 적어라"**

**앞 라운드의 44개 목록으로는 「전부 통과」였다. 목록을 68개로 넓히니 5건이 나왔다.**
★ 이것 자체가 이 저장소의 형태다 — **목록이 좁으면 「전부 통과」와 「안 본 것이 있다」가 똑같이 생긴다.**

| # | 값 | ×1.25 | 처리 |
|---|---|---|---|
| ① | **테두리 2pt** | 2.5 | ★ **면제** — §15.6-b가 **유도하고 의도적으로 받아들인** 잔차다. 그 절 원문: *"반 픽셀이 남지만 **평탄부가 있으므로 피크 알파는 1.0**이다. 알파 손실과 위치 잔차는 다른 문제다."* 4pt로 올리면 썸네일이 136 → 128(4배수는 유지)이지만 **§15.6-b 확정을 뒤집는 것**이라 손대지 않는다 |
| ② | 메타 폭 41 | 51.25 | **권고 H** — 40으로. 16.13 |
| ③ | 이름 폭 87 | 108.75 | ②의 **파생**. 메타 40이면 자동으로 88 |
| ④ | 자물쇠 폭 18 | 22.5 | **권고 I** — 20으로. 16.13 |
| ⑤ | 자물쇠 높이 17 | 21.25 | ④와 같은 건 |

### 최종 집계

```
골격 수치 68개 중  4의 배수 통과 67  ·  탈락 1
  탈락: 테두리 2pt  ← §15.6-b가 근거를 대고 면제한 유일한 값
  (권고 H·I가 반려되면 탈락 5건으로 되돌아간다 — 그 경우도 전부 「카드 안」이고 열 좌표는 무영향)
```

> ★ **열 폭 · 세로 구역 · 뷰포트 · 가변 슬롯은 전부 4의 배수다.** 남은 것은 **카드 안의 작은 도형 넷**뿐이고,
> 그중 하나(테두리)는 §15가 이미 판정했다.

## 16.13 리더 판단 요청 — 권고 2건 (†)

**둘 다 「카드 안」이라 열 좌표·창 치수에 영향이 0이다. 반려해도 골격은 그대로 성립한다.**

### 권고 H — 카드 메타 폭 **41 → 40** (이름 폭 87 → 88)

| 메타 | 4배수 | 이름 폭 | 4배수 | §15 요구 84 대비 |
|---:|:---:|---:|:---:|---:|
| 44 | ✔ | 84 | ✔ | **여유 0** ← 하한에 붙는다. 기각 |
| 41(현행) | ✘ 51.25 | 87 | ✘ 108.75 | 여유 3 |
| **40** | **✔ 50/60/70/80** | **88** | **✔ 110/132/154/176** | **여유 4** |

**`design-systems §7-2` 판정표를 메타 40에서 전건 재검증했다**(모형 재현 5/5 OK):

| 문자열 | 폭 | 메타 40 | 메타 41 |
|---|---:|---|---|
| `600` | 16.50 | OK | OK |
| `3,200` / `6,600` | 24.75 | OK | OK |
| `착용 중` / `보유 중` | 33.00 | OK | OK |
| `80시간`(유예 모델) | 31.00 | OK | OK |
| `동전 3,200` | 47.75 | ★초과 | ★초과 |
| `3,200동전` | 44.75 | ★초과 | ★초과 |

> ★ **초과 2건은 메타 41에서도 초과다 — 41→40이 새로 깨는 것은 0건.**
> ★ **미확인**: `LV.20`은 라틴이라 이 모형을 쓰면 안 된다(`design-systems` 자백: *"라틴 문자는
> 과대추정한다 — `LV.20`이 41.00으로 나오는데 원 문서는 들어간다고 적었다"*).
> **40에서 1pt 더 좁아지므로 실기 캡처 항목이다**(16.14 C-2).

### 권고 I — 자물쇠 배지 **18×17 → 20×20**

| | 18 × 17 | **20 × 20** |
|---|---|---|
| 4의 배수 | ✘ 22.5 / 21.25 | **✔ 25 / 30 / 35 / 40** |
| x 범위 | 126..144 | **124..144** |
| 아이콘 오른끝(102)까지 여유 | 24pt | **22pt** ✔ |
| 등급 낱말(x 40)까지 여유 | 86pt | **84pt** ✔ |
| 썸네일 높이 56에 인셋 4로 들어가는가 | ✔ | **✔**(4..24) |

> ★ 18×17은 **스펙 SVG viewBox 20×21**에서 온 값이다(`CharacterInfoWindow.cs` `LockShackle` 주석).
> **20×20은 그 원본 viewBox에 오히려 더 가깝다.**
> 다만 **배지 도형 자체는 `UiChrome`/§15 소관**이라 권고로 남긴다 — 골격이 단독으로 정할 것이 아니다.

## 16.14 실기 미확인 — **다음 빌드에서 리더가 찍을 캡처 목록**

| # | 캡처 대상 | 무엇을 보는가 | 통과 기준 | 왜 계산으로 못 보는가 |
|---|---|---|---|---|
| **C-1** | **장비 탭 전체 · 100% 배율** | 3열 경계가 x = 248 / 488에 정확히 있는가 · 카드 한 줄 **3장** · 섹션 3이 **반쯤** 보이는가 | 세로선 2개가 카드 모서리와 어긋나지 않음 · 섹션 3 노출 140/280 | 레이아웃 그룹의 실제 반올림은 런타임에만 나온다 |
| **C-2** | **카드 한 장 확대 (전설 · 잠김 각 1장)** | 등급 낱말 24×12가 아이콘·자물쇠와 **안 겹치는가** · 낱말 2글자가 24pt에 **들어가는가** · ★ **메타 칸에서 `LV.20`이 안 잘리는가**(권고 H 채택 시 40pt) | 겹침 0 · 말줄임 0 | ★ **한글 폴백 폰트 실제 렌더 폭 미측정.** 폭 모형은 **한글+숫자로만** 교정됐고 **라틴은 과대추정**한다 |
| **C-3** | **미리보기 액자 — 장비(0.857)·외형(1.009) 각 1장** | 캐릭터가 프레임 안에 들어오는가 · 장비 4종이 다 보이는가 | 머리 위·발 아래 잘림 0 | 촬영장 카메라 구도는 오프라인 계산으로 못 본다 |
| **C-4** | **보관함 탭 — 42종 보유 상태** | 20행이 **온전히** 보이고 21행이 **안 보이는가** | 반쯤 잘린 행 0개 · **하단에 남는 빈 띠 0pt**(뷰포트 712 = 20행 정확) | 클램프는 구현 후에만 잰다 |
| **C-5** | **Windows 1920×1080 @125% / @150%** | 규칙 S6이 도는가 — 잘리는가 **스크롤되는가** | 잘림 0 · 스크롤바 등장 | ★ **Windows 배율 환경이 이 머신에 없다.** 16.10-c 표는 `.Layout.cs:41` 식을 옮긴 **모형 계산** |
| **C-6** | **보관함 col2 (숨틈 24 = 하한)** | 프리셋 3장과 「스탯 재분배」 사이가 **붙어 보이지 않는가** | 시각적으로 분리됨 | ★ 24pt는 계산상 하한이지 **「충분한가」는 눈으로만** 갈린다 |

★ **캡처 없이 「확인됨」이라 쓰지 않았다.** 위 6건은 실기에서만 갈린다.
★ §15의 ΔE·대비·MAR 실측은 **그대로 유효하다** — 골격은 색을 하나도 안 건드린다.

### 16.14-a ★ 라운드 중 실측 정정 — `CardRarityWord*` 상수는 **지금 코드에 없다**

이 라운드 도중(**10:26**, 내가 소스를 읽은 뒤) 다른 라운드가 `CharacterInfoWindow.cs`를 고쳤고,
**§15.14-d의 등급 낱말 상수 3종(`CardRarityWordWidth/Height/Inset`)이 사라졌다**(중단된 `coder-ui` 되돌림).

```
grep -rn "CardRarityWord" Assets/_Project/Scripts/     → 0건
grep -c  "CardMetaWidth"  …/CharacterInfoWindow.cs     → 2건   ← 양성 대조: 프로브는 살아 있다
```

⇒ **16.4·16.11④의 「등급 낱말 24×12, 인셋 4」는 코드 서술이 아니라 설계 지시다.**
출처는 `§15.14-d`(자리) + `§15.14-g #4`(폭 24 권고)다.

★ 나머지 인용 상수는 **정정 시점에 다시 읽어 전건 일치**를 확인했다:
`PanelWidth 1042`(→1040 대상) · `LeftWidth 244` · `CardWidth 161` · `CardHeight 108` ·
`ThumbX 11` · `ThumbHeight 54` · `CardMetaWidth 41` · `InventoryRowHeight 24` ·
`InventoryVisibleRows 20` · `MinPanelWidth/Height 320`.

## 16.15 코드 구조 영향 — 리더(아키텍트) 보고

| # | 무엇이 | 지금 | 골격 | 성격 |
|---|---|---|---|---|
| **①** | **가로 캐러셀 전량** | 상수 5개 + 드래그/클릭억제 경로 | **삭제** | ★ **사용자 승인 완료**(2026-09-03) |
| **②** | **`PortraitContentSize` / `CharacterPortraitStage.DesignAspect`** | 1.044 고정 | 장비 **0.857** / 외형 **1.009** — **탭마다 다름** | ★ **`coder-ui` 첫 확인 항목**(리더 지정) |
| **③** | **`PanelHeightForTab`** | 장비/보관함/상점 861 · 외형 705 | **860 / 684**. **기구 유지** | 유지 — 폐지하면 외형에 176pt 빈 아래 |
| **④** | **열 = 2 → 3, 탭마다 열 수가 다름** | `LeftWidth 244` + `RightWidth 798` 고정 2열 | 레일 5개에서 탭별 조립 | `BuildLeftColumn`/`BuildRightColumn` **이름이 골격과 안 맞는다** — 열이 아니라 **레일 구간**을 짓는 구조로 |
| **⑤** | **규칙 S6** | 없음. `ClampPanelToScreen`이 창만 줄이고 **본문 아래를 마스크로 자른다** | 스크롤로 전환 | ★ **골격이 만든 요구가 아니라 이미 출하 중인 결함의 처방.** 골격을 안 채택해도 유효 |
| **⑥** | **`InventoryRowHeight`** | 24 (= WCAG 하한과 정확히 같아 여유 0) | **28** | 판정 C의 유일한 변경점 |
| **⑦** | **`PanelWidth` / 창 높이** | 1042 / 861 | **1040 / 860** | 리더가 DPI 근거로 조정(A·G). ★ **802가 원래 사용자 확정이었다는 사실을 주석에 남길 것** |

## 16.16 플랫폼 영향

| | 내용 |
|---|---|
| **Windows 영향** | **함께 검토함 — 별도 배정 불필요.** 프로덕션 `.cs` **0줄**. ★ **A(1040) + G(860) 채택으로 골격 68개 수치 중 67개가 ×1.25/1.50/1.75에서 잔차 0**이고, 탈락 1건(테두리 2pt)은 **§15.6-b가 근거를 대고 면제한 값**이다. **열 폭·세로 구역·뷰포트·가변 슬롯은 전부 4의 배수다.** ★ 그리고 16.10-c의 잘림 5건 중 **4건이 Windows**다 — *"사용자 신고에 플랫폼 단서가 있으면 그쪽을 먼저 고친다"*는 규칙에 따라 **규칙 S6은 Windows 우선 항목**이다 |
| **macOS 영향** | 배율 ×1 / ×2뿐이라 **4의 배수 잔차는 macOS에 없다**(짝수면 충분). 잘림은 **1280×800에서 92pt** 1건. Retina ×2에서의 2pt 테두리 알파 손실은 §15.6-b가 이미 닫았다 |
| **새 플랫폼 분기** | **0건.** 골격은 전부 캔버스 포인트이고 플랫폼 조회가 없다 |
| **모바일(iPad/iPhone)** | ★ **별도 배정 필요.** **1040 × 860은 iPhone 논리 폭(390~440)에 절대 안 들어간다** — 레일 R1(248) 하나만 해도 폭의 **57~64%**다. 레일 5개 중 무엇을 접을지는 골격이 정할 수 있지만(예: 3열 → 탭 안 2단계 세그먼트), **`dev-platform`의 스크린샷 백드롭 모드 제약을 읽어야** 결론이 난다. **이 절은 데스크톱 골격만 정했다** |

## 16.17 검산 재현

```
python3 <scratchpad>/skel_final.py     골격 전량 (W=1040, H=860)
python3 <scratchpad>/g_check.py        G 반영 후 세로 7개 열 + DPI 68개
python3 <scratchpad>/residual5.py      잔차 5건 처리 가능성
python3 <scratchpad>/namefix.py        ★ 이름 폭 유도기 — 자기 버그를 잡은 교정

  교정: 폭 모형 7/7 · 4배수 검사기 18/18(★탈락 대조 10건 포함) · 이름 폭 유도기 1/1
  열 합 4탭 전부 = 1040 ✔  ·  전 열 4의 배수 ✔
  세로 합 7개 열 전부 = 796 ✔  ·  전 열 가변 ≥ 24 ✔  ·  ★ 전 열 가변이 4의 배수 ✔
  보관함 뷰포트 712 = 20행 «잔여 0» ✔   ·   카드 세로 합 116 ✔   ·   겹침 0 ✔
  DPI 68개 중 67 통과 · 탈락 1(테두리 2pt, §15.6-b 면제)
  FAIL: 없음
```

★ 스크립트 머리에 **「이 저장소에서 반복되는 프로브 함정」 5건**을 주석으로 박아 뒀다(리더 지시).
이 라운드에 내가 그중 **2번(`find -newermt '-25 minutes'`가 bfs에서 에러 후 빈 출력)과
3번(`/tmp`가 심링크라 하강 안 함)에 실제로 빠졌고**, 양성 대조(카나리 파일)로 잡았다.
그리고 **5번(교정 없는 계산기)이 이름 폭 유도기에서 실제로 발동할 뻔했다** — `ThumbX`를 한 번만 빼서
메타 41이 **99**를 내놨는데, **알려진 값 87과 어긋나서** 잡혔다(16.1의 자백).
**셋 다 「죽은 프로브의 출력이 성공한 프로브와 똑같이 생겼다」의 또 한 판이다.**
