# Windows 스팀 출시 — 크리티컬 패스

작성 `game-architect` · 2026-09-05 **1판 06:xx / 2판 07:4x(이 문서)** · **프로덕션 `.cs` 수정 0건**
근거는 전부 이 날짜에 저장소에서 직접 잰 것이다. 문서에서 베낀 문장은 출처와 함께 표시하고,
**실측과 문서가 갈린 곳은 실측을 적었다**(§0-3에 3건).

> ## ★ 2판 갱신 요약 — 1판 이후 무엇이 바뀌었나
>
> 커밋 `1f7e139`·`c234f1e` + 미커밋 라운드 이후 **다시 재고 쓴다.** 1판의 문장 중 **내가 직접
> 뒤집는 것 4건**을 먼저 적는다(문서를 믿지 말고 실측을 믿는다는 이 문서의 규칙을 나 자신에게 적용).
>
> | # | 1판이 적은 것 | 2판 실측 (2026-09-05) | 어디 |
> |---|---|---|---|
> | ① | *"활성 빌드 타깃이 **이미 Windows**다"* | ★ **뒤집혔다. 지금은 macOS다.** 오늘 06:20~07:26 사이에 전환됐다 | §0-3 (2) |
> | ② | M-10 *"발판 알파 필터가 **3케이스가 샌다**"* | ★ **오진이었다. 코드는 이미 옳았다** — 샌 것은 「글루를 잠그는 감사」쪽이고 그건 이번에 채워졌다 | §3-A |
> | ③ | I-3 *"바뀌는 것은 **로더 한 줄**"* | **과장이다.** 계약은 안 바뀌지만 교체 비용은 「로더 + 빌드 파이프라인 + 정적 초기화 계약」이다 | §3-B |
> | ④ | §5-1 *"exe SHA가 `bf64a21a…`가 **아닌지** 확인"* | ★ **곧 무력해진다.** 아이콘·버전이 들어가는 순간 이 검사는 **항상 통과**한다 = 거짓 통과 생성기 | §5-1 |
>
> **그리고 새로 생긴 것 2건**: 오늘의 **캐릭터 정보창 디자인 불일치(2회차)**에 대한 구조 판정과
> 규칙 개정 초안(**§9**), 다음 라운드 배정 후보와 **파일 소유 분할**(**§10**).

> **입력**: 사용자 지시 — 팀 전체 목표를 **「Windows 스팀 출시」**로 좁힌다.
> **제약**: 개발 머신은 macOS. Windows 실기는 사용자의 **낮 시간**에만 열린다.
> **이 문서의 쓸모**: 리더가 다음 라운드에 22명을 재배치할 때 **무엇을 지금 주고 무엇을 낮까지
> 붙들 것인가**를 가르는 표. 전체 목록이 아니라 **순서와 그 근거**가 본체다.

---

## 0. 먼저 — 이 전환이 무엇을 뒤집는가

### 0-1. 전제가 통째로 뒤집힌 문서 3곳 (리더 정정 대상)

**이 셋은 지금 팀을 반대 방향으로 안내한다.** 코드가 아니라 문장이 위험한 자리다.

| 위치 | 지금 적힌 것 | 전환 후 |
|---|---|---|
| `Tasklist.md:22940` | *"★★★ 사용자 확정: **1차 목표는 macOS 출시** / Windows 빌드는 「낮 검증창」용"* | **폐기.** 그 밑의 파생 판정 2개(출시 순서 · Windows 빌드의 목적)도 함께 |
| `docs/verify/WINDOWS_CHECK_SESSION.md:3-4` (머리말) | *"1차 목표는 **macOS 출시**다. 이 Windows 빌드는 **출시 산출물이 아니라** … 검증 창을 쓰기 위한 도구다"* | ★ **가장 위험하다.** 사용자가 이 줄을 읽고 판정 기준을 낮춘다. 표 자체(§A~§F)는 그대로 값을 하지만 **머리말은 반대로 다시 써야 한다** |
| `docs/strategy/ROADMAP.md` §17-2 | *"스토어 페이지 최초 작성 시점에는 **macOS만 선언**한다. Windows는 CW 판정 통과 후에 넣는다"* | ★ **정확히 반대.** 이제 **Windows만 선언**하고 macOS를 나중에 넣는다. 비대칭 논증(늦게 넣는 비용 ≪ 넣었다가 빼는 비용)은 **방향만 바뀌고 그대로 유효**하다 |

★ 세 문서 모두 **본문의 측정값은 살아 있다.** 죽은 것은 **결론 문장**뿐이다. 지우지 말고 뒤집어라.

### 0-2. 출시일 공식 — 항 하나가 빠지고 하나가 게이트로 올라간다

```
종전 (ROADMAP §17-3)   출시일 = max( R-2′ , R-6a , R-5-min , W-gate )
전환 후                출시일 = max( R-2′ , R-5-min )        단, W-gate 전량 통과가 「출시 자격」
```

| 항 | 무엇 | 전환 후 |
|---|---|---|
| **R-6a** | macOS Developer ID 서명 + 공증 ($99/년, `notarytool`/`stapler`, `LibUniWinC.bundle` 딥서명) | ★★ **공식에서 빠진다.** 근거였던 *"스팀이 macOS 앱에 공증을 요구한다"*는 **macOS 빌드를 낼 때만** 발동한다. **Windows 단독 출시에는 macOS depot이 없다.** 소관 공백(4회차까지 파이프라인 0건)이 **더 이상 출시를 막지 않는다** — 이번 전환의 가장 큰 일정 이득 |
| **R-5-min** | DLC 6팩 통로 + 엔타이틀먼트 배선 | **지배항 유지.** 그리고 병목이 문서가 짚은 곳이 아니다 → §3 M-1 |
| **R-2′** | 영어 (고유 356 / 총 505) | **유지되나 근거 하나가 죽었다** → §0-3 (3) |
| **W-gate** | CW-1~CW-5 | ★ **항이 아니라 게이트가 된다.** 종전에는 *"못 따라오면 Windows만 뺀다"*가 성립했다. 이제 **W-gate가 실패하면 출시 자체가 없다.** 「조건부 동승자」에서 「유일한 플랫폼」으로 지위가 바뀌었다 |

★ **부수 이득 1건**: ROADMAP §17-5가 *"스팀의 출시일 1회 노출이 macOS 출시일에 소모되고, 그러면
93.95%의 모수가 그 노출을 영원히 못 받는다"*를 **영구 손실**로 규정했다. **전환이 그 손실을 없앤다.**
이제 그 1회를 Windows(93.95%)가 받는다. 이 전환은 일정만이 아니라 **모수 축에서도 개선이다.**

### 0-3. 실측이 문서와 갈린 것 3건

**(1) 출하 exe에 dGPU 패치가 들어 있지 않다 — 그런데 감사 대장은 「구조는 닫혔다」고 적는다.**

```
Builds/Windows/StickMate.exe   SHA-256 = bf64a21a5c91…8c656559   671,744 B   mtime 09-03 08:32
   ↑ WINDOWS_DGPU_REPORT.md §1-1이 기록한 Unity 플레이어 템플릿 해시와 바이트 단위로 동일하다
Assets/Editor/WindowsHybridGpuExportPostprocessor.cs   ← git status: ?? (미커밋 · 미빌드)
```
`PlatformParityAuditTests.cs:3736`은 *"Windows: 빌드 후처리가 PE export 값 두 개를 1 → 0으로 바꾼다.
구조는 닫혔으나 실기 미확인"*이라고 적는다. **소스는 닫혔고 산출물은 안 닫혔다.**
⇒ **지금 존재하는 Windows zip 3개 전부로는 게이트 G1을 잴 수 없다.** §4 H-0.

**(2) ~~활성 빌드 타깃이 이미 Windows다.~~ → ★★ 2판 정정: 오늘 아침 사이에 macOS로 뒤집혔다.**

1판(06:xx)에 이렇게 적었다: *"1900b0aE.dag = `UNITY_STANDALONE_WIN`(최신) / 200b0aE.dag =
`UNITY_STANDALONE_OSX`(구판) ⇒ 전환이 필요 없다."* **지금 다시 재면 정반대다:**

```
07:26  Library/Bee/artifacts/200b0aE.dag/StickMate.Runtime.rsp        UNITY_STANDALONE_OSX  ← 최신
07:26  〃                    /StickMate.Tests.EditMode.rsp            UNITY_STANDALONE_OSX
07:26  〃                    /StickMate.Tests.PlayMode.rsp            UNITY_STANDALONE_OSX
07:06  Library/Bee/artifacts/200b0aP.dag/StickMate.Runtime.rsp        UNITY_STANDALONE_OSX
06:20  Library/Bee/artifacts/1900b0aE.dag/StickMate.Runtime.rsp       UNITY_STANDALONE_WIN   ← 구판이 됐다
09-03  Library/Bee/artifacts/1900b0aP.dag/StickMate.Runtime.rsp       UNITY_STANDALONE_WIN
07:29  Library/EditorUserBuildSettings.asset                          (그 뒤에 한 번 더 써짐)
```

**⇒ 타깃 전환이 오늘 06:20~07:26 사이에 실제로 일어났다.** 그리고 사각지대가 **정확히 반대편으로
옮겨갔다.**

> ### ★★ 리더 확인 요청 2건 (이건 판정이 아니라 신고다)
> 1. **이 전환이 승인된 것인가.** CLAUDE.md: *"타깃 전환은 **전역 상태 변경**이라 동시 진행 라운드를
>    방해한다 — **리더 승인 후에만.**"* 승인된 것이면 아래 2번만 남고, 아니면 **누가 언제 왜**를
>    그 라운드에 물어야 한다. 나는 그 라운드를 모른다 — **미확인.**
> 2. ★ **지금 나오는 EditMode/PlayMode 초록은 M-7·M-8의 Windows 절반을 구조적으로 못 본다.**
>    미커밋 신규 파일 `Platform/Windows/WindowsVirtualDesktopProbe.cs`는 **1행이
>    `#if UNITY_STANDALONE_WIN`**이다(실측). macOS 타깃에서는 **타입이 존재하지 않는다.**
>    - **다행인 것**: 짝 감사 `VirtualDesktopProbeAuditTests.cs`는 **타입이 아니라 소스를 읽는다**
>      (`File.ReadAllText(ProbePath)`, `:40`) ⇒ 타깃과 무관하게 계약을 잰다. **옳은 형태다.**
>    - **그래도 안 잡히는 것**: **그 파일이 컴파일되는가**(P/Invoke 시그니처·COM 인터페이스 선언·
>      `Marshal` 사용). 그건 `xcheck.sh win`만 답한다. **이번 라운드는 그 도구 사용이 금지돼 있어
>      나는 확인하지 못했다 — 미확인. 커밋 전 필수 게이트다.**

★ **1판의 결론(「Windows 타깃을 상주로 두는 것이 옳다」)은 방향으로서 유지한다** — 다만 지금 상태가
그 반대라는 것을 알고 있어야 한다. §3 M-12를 그에 맞게 다시 썼다.

**(3) 영어(R-2′)를 떠받치던 논증 하나가 죽었다.**
ROADMAP §18-3은 *"macOS 단독일 때 **영어가 유일하게 남은 모수 확대 수단**이다"*로 영어의 값을 올렸다.
**그 전제(macOS 단독)가 사라졌다.** 영어 자체는 사용자 확정(D-5 «넣는다»)이므로 **내가 뒤집지 않는다.**
다만 `product-strategy`는 **죽은 전제를 인용해 유지하지 말고 근거를 다시 유도해야 한다** — 그 항이
출시일 공식의 지배항 후보이기 때문이다.

---

## 1. 되돌릴 수 없는 결정 — 착수 전에 못박는다

| # | 결정 | 상태 | 왜 비싼가 |
|---|---|---|---|
| **I-1** | `companyName = Vibelab` · `overrideDefaultApplicationIdentifier: 1` · 번들 ID `com.Vibelab.StickMate` | ★ **이미 확정·커밋됨**(`ProjectSettings.asset:15,167`) | 세이브 경로와 `ReservedBarRestoreLedger` 경로가 여기서 파생된다. **출시 후 재변경 = 유저 진행도 전량 소실 + 원칙 3 예외의 안전장치 고아화.** ⇒ **다시 만지지 마라.** 미관상 표기 변경도 금지 |
| **I-2** | 스팀 앱 레코드의 **지원 OS 선언** | 미착수 | 판정 시점 = **출시일 확정 입력 순간**(출시일보다 최소 14일 앞). 선언해 놓고 빼면 위시리스트 유저가 출시일에 못 산다(ROADMAP §17-2). **Windows만 선언하고 macOS는 나중에 추가** |
| **I-3** | **Steamworks 식별자 금지 니들을 여는 방식** | ★★ **2026-09-05 착지·커밋됨**(`1f7e139`) | 문은 **1판이 요구한 형태 그대로** 열렸다 — 실측: 허용 파일 **1개**(`Store/SteamPackEntitlementSource.cs`, 119줄) · 허용 심볼 **5개** · `SteamEntitlementAdapterAuditTests`가 라인 단위 재검증 · 네거티브 대조 6건. ★ **그리고 지금은 휴면 상태다**: `STICKMATE_STEAMWORKS_INSTALLED`가 `ProjectSettings.asset:685 scriptingDefineSymbols: {}`에 **없고**, `Packages/manifest.json`에 Steamworks 항목이 **0건**이라 `Query`가 항상 `Unknown`을 돌려준다(실측). ⇒ **문은 열렸으나 아직 아무것도 통과하지 않았다.** 되돌리기 비용은 지금이 가장 싸다 |
| **I-20** | ★★ **「빈 depot DLC를 `BIsDlcInstalled`가 true로 보고하는가」와 그 대비책의 착수 시점** | ★ **신규 · 미확인 · 유료 팩 출시 게이트** | `STEAMWORKS_ENTITLEMENT_EXCEPTION §6-4`가 *"이 설계에서 가장 비싼 미확인"*으로 적어 둔 것이다. 우리 6팩은 **콘텐츠가 본편에 이미 들어 있는 「라이선스만」 DLC = depot이 비어 있다.** `BIsDlcInstalled`가 보는 것은 이름 그대로 **설치**다. **false로 보고되면 정상 구매자 전원이 `NotOwned`가 된다** — 이 설계 전체가 막으려던 사고가 그 형태로 발생한다. **왜 되돌릴 수 없는 자리인가**: 대비책 (ㄱ)*(아주 작은 더미 depot 파일 1개)* 는 **DLC appID를 만드는 그 시점에 같이 정해야** 한다. 나중에 붙이면 **이미 산 사람에게 depot이 뒤늦게 내려가고**, 그 구간이 정확히 §13-10-b의 「소유했는데 파일 없음」 4중 모호성이다 — 우리 손으로 만드는 셈이다. ⇒ **I-2(스토어 지원 OS 선언)와 같은 조작 회차에 묶어라.** 자세히는 §3-B |
| **I-4** | **PE 후처리 ↔ Authenticode 순서** | 후처리 미커밋 / 서명 미조달 | `WINDOWS_DGPU_REPORT §2-3`: 서명을 도입하면 **패치가 반드시 서명보다 먼저**여야 한다. 순서가 뒤집히면 서명이 깨진다. **지금 문서로 못박아야 나중에 자동화가 순서를 안 뒤집는다** |
| **I-5** | 세이브 스키마 `CurrentVersion` | **이번 국면에서 올릴 이유가 없다** | 엔타이틀먼트는 세이브에 안 들어간다는 계약이 이미 서 있다(`PackEntitlement.cs` · `EntitlementNotInSaveAuditTests`). ⇒ **팩 작업이 스키마를 건드리면 그건 설계 이탈 신호다.** 올려야 한다면 `v(N-1)` 하위 호환 테스트 동반이 의무(CLAUDE.md) |
| **I-6** | Steam Cloud | ★ **이미 결정됨 — 다시 열지 마라** | `ARCHITECTURE.md` 5-1-10 = **Auto-Cloud(코드 0줄) · 1차 출시 이후 · 조건부**. 감사가 `ISteamRemoteStorage` 경로를 막고 있다. **1.0 범위 밖** |

---

## 2. 선결 조건 사슬 — 「A를 안 하면 B가 어떻게 막히는가」

```
H-0 새 Windows 빌드 (dGPU 후처리 포함)
     │   ← 없으면: 낮 세션의 모든 관측이 09-03 08:32 산출물에 대한 것이 된다.
     │              그 사이에 미커밋 318파일 라운드가 있다. 세션 하나가 통째로 무효.
     ▼
0-1 문서 3곳 정정
     │   ← 없으면: 사용자가 "출시 산출물이 아니다"를 읽고 판정 기준을 낮춘다.
     │              「미관이니 넘어가자」가 실제로는 출시 차단 항목이다.
     ▼
H-1 세션 설계 (§5) — 교락 제거 · 음성 대조 · 재시작 예산
     │   ← 없으면: G1이 거짓 통과한다(§5-2). 성공과 실패가 똑같이 생긴 자리다.
     ▼
낮 세션 1회 ── CW-1 · CW-2 · B-2 · A-2 · A-4 · A-7 · dGPU G1/G2 동시 수확
     │
     ▼
W-gate 판정 → 스토어 지원 OS 선언(I-2) → 출시일 확정 입력

──────── 이 사슬과 완전 병렬 (자원이 다르다: 우리 시간 vs 사용자 낮 시간) ────────

M-1 팩 조형 통로 (원칙 4의 구멍)  →  M-2 엔타이틀먼트 배선 설계(I-3)  →  R-5-min 착수
```

★ **병렬성이 이 국면의 핵심 자산이다.** W-gate는 **사용자의 낮 시간**을 쓰고 R-5-min·R-2′는 **우리
시간**을 쓴다. ROADMAP §17-3의 이 관찰은 전환 후에도 그대로 참이고, 오히려 더 중요해졌다 —
**낮 세션을 기다리며 팀을 놀리면 그 시간이 전부 손실**이다.

> ### ★★ 2판 — 이 사슬의 **머리가 바뀐다**
> 위 그림은 **H-0(새 빌드)을 희소 자원으로 취급**했다. **틀렸다.**
> 빌드 산출물 덮어쓰기는 **리더 재량**이고(사용자 확정 2026-09-02) 여러 번 해도 된다.
> **희소한 것은 「사용자의 낮 시간」이고 그건 1회다.**
>
> ```
> 2판 사슬:  3COL 1단계 골격 착지  →  export 되읽기 검사기  →  아이콘/버전 배선
>                    │                                              │
>                    └──────────────  H-0 새 빌드  ←───────────────┘
>                                          │
>                                          ▼
>                             문서 3곳 정정  →  H-1 세션 설계  →  낮 세션 1회
> ```
> **근거는 §9-1에 적었다** — 낮 세션 캡처 목록 P-1~P-7이 **전부 캐릭터 정보창**이라
> 이식 중간 상태로 찍으면 **7항목이 통째로 폐기**된다. 그리고 그중 **P-3(Windows @150%)은
> macOS에서 구조적으로 못 재는 축**이라 낮 세션을 놓치면 그 값을 다음 세션까지 못 얻는다.

---

## 3. 【A표】 macOS에서 지금 착수 가능 — 이번 라운드 배정 후보

> ★★ **2판: 아래 표는 「1판 시점의 문제 서술」로 보존한다. 현재 상태는 §3-A 상태표가 정본이다.**
> 지우지 않는 이유: **M-10처럼 문제 서술 자체가 오진이었던 것**이 있고, 그 오진과 정정이 나란히
> 남아 있어야 다음 사람이 같은 오진을 반복하지 않는다.

| # | 무엇이 막혀 있는가 (한 줄) | 담당 후보 | 심각도 | 근거 · 비고 |
|---|---|---|---|---|
| **M-1** | ★★ **팩 통로가 조형 층에서 끊긴다.** ~~`AccessoryShapeBuilder.cs` 2,804줄 / `case` 43개~~ → ★ **2026-09-05 실측으로 정정됨. 설계 완료 → `docs/GAME_ARCHITECTURE_REVIEW.md` §13** | `game-architect`(설계 **완료**) → `coder` + `design-equipment` | ★ **출시 차단** (R-5-min 지배항) | ★ **아래 M-1 정정 블록**을 읽어라 — 이 칸의 옛 서술은 **구멍을 파일 하나로 좁혔고 case 수도 틀렸다** |
| **M-2** | **엔타이틀먼트 배선이 감사와 정면 충돌한다.** 유료 6팩 = `BIsDlcInstalled` 필요인데 프로덕션에서 `Steamworks`·`SteamApps`·`SteamAPI`가 **금지 니들**이다 | `game-architect`(문 여는 형태) → `product-strategy`(채널 계약) → `coder` | ★ **출시 차단** | I-3. 계약 자체(`PackEntitlement.cs`의 3상태 · 단조 래치 · 부정 캐시 없음)는 **잘 서 있다** — 부팅 자동 실행으로 스팀이 아직 안 뜬 구간까지 이미 설계돼 있다. 남은 것은 **어댑터 1개와 감사 예외 형태** |
| **M-3** | **유령 단축키 3건 — 표시와 실제가 다르다.** 보관함이 `집중 모드 F` / `할일 알림 J` / `하드웨어 반응 H`를 사용자 단축키로 표시하는데(`ItemCatalog.cs:680,682,684`) 실제 바인딩은 개발 게이트 안이다(`AppControlDirector.cs:269,271,272` — `dev && chord`) | `coder` (+`ux-designer` 문구) | **출시 전 권장** (신규 사용자 첫인상 직격) | 플랫폼 무관 · macOS에서 지금 고친다. 처방 두 갈래: **(가)** 게이트 밖으로 꺼낸다 **(나)** 카탈로그에서 조합 표기를 뺀다(부채꼴로 진입 가능하므로 기능 손실 0). **리더 판정 필요** |
| **M-4** | **「지금 종료」 버튼 영어 넘침(CW-4)이 실측으로 안 닫혔다.** 예전 132pt 상수는 어떤 자폭 가정에서도 넘쳤고, 지금 값이 **실제 Windows 폰트 스택에서 통하는지는 미확인** | `test-engineer` | **출시 전 권장** | `SettingsWindow.cs:1617-1651`. **러너 `Text.preferredWidth`로 이 머신에서 확정 가능**(ROADMAP W-5가 "실기 없이 되는 것"으로 분류). `⌃⌥⌘Q`(3자) vs `Ctrl+Alt+Win+Q`(13자) — **최악 칸이 Windows + 영어**이고 이 머신에서 아무도 못 봤다 |
| **M-5** | **스토어 제출물 결손 3건**: `m_BuildTargetIcons: []`(Windows exe 아이콘 **없음**) · `m_ShowUnitySplashScreen: 1` · `bundleVersion: 1.0` | `dev-platform` + `marketing` | **출시 전 권장** | 아이콘은 스팀 페이지가 아니라 **작업표시줄·Alt+Tab·exe 자체**에 뜬다. 스플래시 제거 가능 여부는 **Unity 6 라이선스 축 — 미확인**(§7). ★ 부수 효과: 아이콘/버전을 넣는 순간 exe가 템플릿 해시와 달라지므로 **dGPU 패치의 「알려진 해시 이점 상실」 논점이 소멸**한다(`WINDOWS_DGPU_REPORT §2-4`가 예고한 그대로) |
| **M-6** | **`ShortcutLabel.WindowsReservedActionKeys`가 빈 배열인데, 그건 조사 결과가 아니라 미조사다** | `dev-platform` | **출시 전 권장** | `Core/ShortcutLabel.cs:101`. 우리 조합은 `Ctrl+Alt+Win+<글자>` 11개. **1차 출처(Microsoft 예약 단축키 목록)로 후보를 실기 전에 좁힐 수 있다.** 좁혀 놓으면 낮 세션 A-7의 수확이 커진다 — **실기 시간을 사는 작업** |
| **M-7** | **하단 예약 띠를 Windows에서 강제할지 「판정」이 안 나 있다** (코드가 아니라 결정) | `game-architect` → 리더 | **출시 전 권장** | `PlatformParityAuditTests.cs:275`. 조회는 양쪽 다 있다(`IReservedScreenEdgeService` 네 방향이 09-03 커밋으로 착지, macOS/Windows 양쪽 구현 확인). **macOS는 일부러 강제하지 않는다 — 그대로 옮기면 Windows에서 신고된 「작업표시줄에 걸쳐서 돌아다닌다」가 남는다.** 실기 확인은 B-2와 한 묶음 |
| **M-8** | **Windows 가상 데스크톱 동행 배선이 없다** — macOS는 `.canJoinAllSpaces`로 해결됨 | `game-architect`(정책 판정) → `dev-platform` | **출시 전 권장** (전환으로 **승격**) | `:1929`. 종전에는 「macOS 패리티 부채」였다. **Windows 단독 출시면 이건 패리티가 아니라 Windows 고유 결함**이다 — Win+Ctrl+←/→에 캐릭터가 안 따라온다. 선택지 **(a) 소속 확인만**(공개 COM `IVirtualDesktopManager`, 남의 데스크톱이면 스스로 숨음) vs **(b) 전 데스크톱 고정**(비공개 API, OS 업데이트마다 깨짐, 원칙 2와 긴장). **비문서 API 누적 개수가 백신 휴리스틱의 계수**라는 §M-10 논점과 같은 저울 |
| **M-9** | **Win+D(데스크톱 표시) 후 좌표가 오염된다** — `CaptureOverlayOrigin()`에 `IsIconic(_overlayHwnd)` 검사가 없다 | `dev-platform` | **출시 전 권장** | `:2035`. **처방이 작다**: 진입부에서 최소화면 즉시 return(직전 유효 원점 유지). **P/Invoke 선언 신규 0줄**(같은 파일에 이미 있다). macOS와 달리 **폭까지 오염되어 `AutoDpiScale`이 함께 깨진다.** 코딩은 지금, 확인은 낮 세션(Win+D 두 번) |
| **M-10** | **발판 알파 필터가 `WS_EX_TRANSPARENT` 단독 창을 놓친다 — 3케이스가 샌다** | `dev-platform` + `test-engineer` | **출시 전 권장** | `WindowsFootholdFilter.cs`는 **`#if`가 0건**(양성 대조: `Win32WindowService.cs`는 1건) ⇒ **macOS 타깃 러너가 이 파일을 실제로 컴파일·검증한다.** 즉 **지금 고치고 지금 잴 수 있는 Windows 결함**이다. 새면 전체화면 클릭관통 오버레이가 「불투명 전체화면 창」으로 읽혀 **그 아래 모든 발판을 지운다** |
| **M-11** | **시스템 오디오 감지에 실행부(Director)가 없어 프로덕션 호출부가 0건이다** | `coder` | **출시 후 가능** | `:965`. 계약·정책·양 플랫폼 구현은 착지, **부르는 사람이 없다.** ★ 리더 판정 대기 1건이 붙어 있다(춤 반응 토글 기본값) — **「켬」이면 세이브 스키마가 올라가고 I-5의 의무가 붙는다.** 1.0 범위 밖으로 미루는 것을 권고 |
| **M-12** | ★ **활성 타깃이 Windows라 지금 러너 초록은 macOS 절반을 못 본다** | 리더(정책) + `qa-regression` | **절차 · 출시 전 권장** | §0-3 (2). 전환 후 **Windows 타깃을 상주로 두는 것이 옳다.** 대가는 macOS 회귀가 조용히 쌓이는 것 ⇒ **라운드마다 `Tools/CrossCompile/xcheck.sh osx` 0에러를 완료 보고에 필수로 못박아라.** 타깃 전환(전역 상태)은 여전히 리더 승인제 |
| **M-13** | **R-6b(Windows 서명) 조달 리드타임이 미확인인데, 이제 그게 위험하다** | `product-strategy` | **출시 전 권장 (조사만)** | 연기 판정 자체(SAC 노출 모수가 좁다, `ROADMAP §19-2`)는 **전환 후에도 유효**하다 — 논증이 채널(스팀)과 SAC 발동 조건에 기대지 플랫폼 순서에 안 기댄다. **바뀐 것은 분모다**: 출시 시점 Windows 사용자 비율이 0% → 100%. T5(신고 3건)가 **유일한 안전망**이 되는데, **T5 발동 후 OV를 사면 신원 검증 왕복 때문에 즉시 대응이 불가능**하다. ⇒ **지금 사지 말고 리드타임만 확인**(조달 규칙: 클라우드 HSM 없는 CA는 가격 무관 기각) |

---

### ★ M-1 정정 블록 (2026-09-05 실측 — 내가 적은 문장을 내가 정정한다)

**설계 전문은 `docs/GAME_ARCHITECTURE_REVIEW.md` §13.** 여기에는 위 표를 뒤집는 3건만 적는다.

| 옛 서술 | 실측 | 무엇이 달라지는가 |
|---|---|---|
| *"`case` 43개"* | **42개.** 그리고 **12개는 슬롯 디스패치**라 팩이 늘어도 안 변한다(`EquipmentSlot`은 값 7개 `enum`, `PACK_THEME_SPEC` §2-1이 **팩은 슬롯을 안 늘린다**고 확정) | **공사량을 과대평가하고 있었다** |
| *"구멍은 그 아래층 **하나뿐**이다"* | ❌ **구멍은 파일 4개다.** `PACK_THEME_SPEC` §2-1 확정: **팩 6종 = HEAD·EYES·NECK·BACK·FX·PET.** FX·PET 스위치는 `CharacterFxRenderer:306` · `CharacterPetRenderer:349,1181` · `CharacterPortraitStage:1240,1317`에 있다(**28 case**) | **범위 과소평가.** 팩 경로 총 **52 case · 5 사이트 · 4 파일** |
| (없던 서술) | ★ **FX·PET은 좌표가 아니라 거동이다** — `Tick*` 실측 **FX 175줄 / PET 824줄**(`TickSnail` 하나가 375줄). 7번째 펫 ≈ **새 C# 140줄** | **데이터화로 못 닫는다.** 인터페이스+레지스트리가 필요하다 → §13-6 |

**그리고 범위가 한쪽으로는 좁아진다**: **HAIR는 팩에 안 들어간다**(같은 확정). HAIR 6 case는
이 파일에서 가장 절차적인 자리(`Mathf.Asin` 제약 풀이 · 호 생성기 · 모자 밑 클리핑)인데,
**팩 통로를 여는 데 건드릴 이유가 없다.**

**판정(§13-7-c)**: **점진 이행.** 7번째 팩을 여는 데 필요한 것은 **폴백 한 곳**이지 24 case의 이사가
아니다. 전량 이행은 **테스트 29파일·99심볼**을 물고(측정값), 기본 42종 전량을 새 경로로 옮겨
**회귀면을 스스로 만든다.** 레거시 switch에는 **래칫**(남은 수가 늘면 실패)을 걸어 끝을 만든다.

**되돌릴 수 없는 결정 4건 신규 — §1 표에 이어 붙일 것**: I-16(`StickPackManifestSO.SchemaVersion`
1→2를 `hatCoverRatio`와 **같은 라운드**에) · I-17(폴백은 **데이터 우선**) ·
I-18(`sortingOrder`는 영원히 코드 소유) · I-19(거동 등록에 **문자열 리플렉션 금지** — IL2CPP에서 조용히 죽는다).
★ **I-5는 유효하다** — 이 설계는 세이브 스키마를 한 칸도 안 올린다.

### ★ I-3 갈림길 답 — **팩당 depot 분리는 지금 하지 않는다** (§13-10)

`product-strategy`의 지적 2건은 **실측으로 옳다**(`PackRegistry.cs:180`의 `Resources.LoadAll`은
`resources.assets` **한 파일**로 구워져 depot으로 못 쪼갠다 / Addressables는 `Packages/manifest.json`에
**없다**, mtime 08-28). 그럼에도 **(가)를 지지한다 — 근거는 일정이 아니라 계약이다**:

1. ★ **depot 부재는 `Unknown`을 만들 수 없다.** 「파일 없음」은 ①안 샀다 ②**샀는데 다운로드 중이다**
   ③**사용자가 속성에서 DLC 체크를 껐다**(스팀 공식 기능) ④검증 중이다 — **넷이 똑같이 생겼다.**
   부재를 `NotOwned`로 읽으면 `PackEntitlementState` 문서가 *"이 계약이 막으려는 사고 그 자체"*라고
   적은 붕괴를 **기본 구현이 저지른다.** Steamworks 어댑터는 *"스팀이 안 떴다"*를 **감지**할 수 있지만
   **부재에는 그 신호가 없다** — (나)는 스팀 의존을 없애는 게 아니라 **정직할 능력을 없앤다.**
2. **사는 것이 거의 없다(실측)**: 아이템 42개 = **67.0 KB** / 팩 하나 ≈ **23 KB** / 빌드 **85 MB**
   = **0.03%.** 게다가 콘텐츠가 없으면 **상점에서 그 팩을 보여 줄 수 없다**(아이콘·이름이 그 에셋 안이다).
3. ★ **리더가 요구한 대체 가능성 — 확보돼 있다.** `IPackEntitlementSource.Query(packId) →
   PackEntitlementState` 하나뿐이고 `PackEntitlements.StateOf`는 **출처를 모른다.** 단조 래치·부정
   캐시 금지도 전부 소비자 쪽에 있다. ⇒ **depot 방식은 구현체를 하나 더 만드는 것이고
   `PackEntitlement.cs`는 한 줄도 안 바뀐다.**
   **단 조건 하나 — 그 구현체는 부재를 `NotOwned`로 쓰면 안 된다. 존재는 `Owned`의 양성 증거로만 쓰고
   부재는 `Unknown`이다.** 이 문장이 없으면 나중에 (나)로 갈 때 **가장 자연스러운 구현이 계약을 깬다.**
4. **지금 치르는 값은 0이다.** §13-3-d의 접속 형태(`cohortId` 조인 · 매니페스트에 에셋 참조 없음)는
   Addressables로 갈아탈 때도 그대로이고, 바뀌는 것은 **로더 한 줄**이다.
   ⇒ **(가)를 택하는 것이 (나)를 막지 않는다.** (나)의 촉발 조건은 **팩 콘텐츠가 무거워질 때**
   (고해상 텍스처·오디오)이지 **엔타이틀먼트가 아니다.**

---

## 3-A. ★ 2판 상태표 — **M-1 ~ M-13, 오늘 다시 잰 결과**

**규칙 하나만 지킨다: 「판정」과 「코드」를 같은 칸에 적지 않는다**(CLAUDE.md 리더 상시 의무).
`설계`는 문서가 있다는 뜻이고, `코드`는 파일이 바뀌었다는 뜻이며, `커밋`은 HEAD에 있다는 뜻이다.
셋은 서로 다른 사건이다.

| # | 설계 | 코드 | 커밋 | 실측 근거 (오늘 직접 확인) |
|---|---|---|---|---|
| **M-1** | ✅ 완료 | ❌ **미착수** | — | 설계 = `GAME_ARCHITECTURE_REVIEW §13`. **팩 매니페스트 `.asset`은 여전히 0건** — `StickPackManifestSO.cs.meta`의 guid `3012b81a…`를 `find … -exec grep -l`로 전수: **0**. **양성 대조 통과**(`StickConfig` guid `da6d114d…` → `DefaultStickConfig.asset` 1건 적중) ⇒ 0은 프로브가 죽어서 나온 0이 아니다 |
| **M-2** | ✅ | ✅ | ✅ `1f7e139` | `Store/SteamPackEntitlementSource.cs` 119줄 실재. **단 휴면**(I-3 칸). ★ 남은 위험은 코드가 아니라 **I-20** |
| **M-3** | ✅ | ✅ | ✅ | `ItemCatalog.cs:714-726` — 세 카드가 `ForMenuAction`/`ForAction(…, null, …)`로 바뀌었고 주석이 *"자율 트리거만 있다. 사용자 진입점이 없다"*를 명시. **유령 조합 표기 소멸** |
| **M-4** | ✅ | ✅ | ✅ | `SettingsWindow.cs:1633-1635` — `QuitButtonWidth`가 **하한**으로 강등되고 실폭은 `MeasuredWidth(종료문안, 확인문안)`의 **최댓값**. 주석이 *"★★ 2026-09-05 (M-4 종결) — 「영어 × Windows」 칸까지 러너로 실측"*을 기록. **1판이 요구한 최악 칸이 닫혔다** |
| **M-5** | ◐ **아이콘만 설계 완료** | ❌ **3건 전부 미반영** | — | ★ **여기가 상태표에서 가장 오해하기 쉬운 칸이다.** 1라운드 커밋 메시지는 *"스토어 제출물 결손 3건 **조사**"*라고 정확히 적었는데, 요약이 오가면서 「완료」로 읽힐 수 있다. **오늘 실측 — 세 값 전부 그대로다**: `ProjectSettings.asset:295 m_BuildTargetIcons: []` · `:20 m_ShowUnitySplashScreen: 1` · `:145 bundleVersion: 1.0`. 새로 생긴 것은 **조형 사양**(`design/character/APP_ICON_SPEC.md` 520줄, 미커밋)이고 **배선은 아무도 안 했다** → §10 |
| **M-6** | ✅ | ✅ | ✅ | `Core/ShortcutLabel.cs` — `WindowsReservedActionKeys`는 **여전히 빈 배열(`:123`)이고 그게 맞다**(그건 「확정 금지 목록」이다). 새로 생긴 것은 `WindowsSuspectActionKeys`(`:168`)와 **글자별 1차 출처 근거 주석**. ⇒ **낮 세션 A-7의 수확을 미리 키워 두는 작업이 끝났다** |
| **M-7** | ✅ | ✅ | ❌ **미커밋** | `Platform/SurfaceSafeAreaPolicy.cs` +130줄. **판정의 실체는 순수 함수 하나**: `EnforcesBottomReservedBand(RuntimePlatform)` = `WindowsPlayer‖WindowsEditor`. ★ **`#if` 0줄 · 플랫폼을 인자로 받는다** ⇒ macOS 머신의 EditMode가 **Windows 쪽 답까지 실행해서** 검증한다. **활성 타깃이 뒤집힌 지금(§0-3 2) 이 설계 선택이 그대로 값을 한다** |
| **M-8** | ✅ | ✅ | ❌ **미커밋** | 신규 4파일: `IVirtualDesktopMembershipSource.cs`(69) · `VirtualDesktopSuspendPolicy.cs`(61) · `Windows/WindowsVirtualDesktopProbe.cs`(226) · `Tests/EditMode/VirtualDesktopProbeAuditTests.cs`(187). ★ **리더 판정 옵션 (a)「소속 확인만」이 채택됐고 (b)「전 데스크톱 고정」은 안 갔다** — `Win32WindowService.cs:2015`가 *"창을 옮기거나 모든 데스크톱에 고정하지는 않습니다"*로 코드에 못박았다. **비공개 API 0개 ⇒ M-10(백신 휴리스틱 계수) 논점을 안 건드린다.** ★ **미확인: `xcheck.sh win` 컴파일 확인 못 함**(§0-3 2) |
| **M-9** | ✅ | ✅ | ✅ | `Win32WindowService.cs:1194-1204` — `CaptureOverlayOrigin()` 진입부에 `IsIconic(_overlayHwnd)` 가드 + **상태 전이 때만** 로그. **P/Invoke 신규 0줄**(`:206`에 이미 있었다). 1판이 적은 처방 그대로 |
| **M-10** | ✅ | ✅ | ✅ | ★★ **1판 서술이 오진이었다. 내가 정정한다.** 1판: *"발판 알파 필터가 `WS_EX_TRANSPARENT` 단독 창을 놓친다 — **3케이스가 샌다**"*. **실측: 새지 않는다.** `Win32WindowService.cs:754,758` — `ReadWindowAlpha`의 **두 반환 지점이 모두** `WindowsFootholdFilter.ResolveWindowAlpha(exStyle, …)`를 거친다(비레이어드 조기 반환 포함). `WindowsFootholdFilter.cs`는 **오늘 한 줄도 안 바뀌었다**(mtime 09-02 18:40). **진짜 구멍은 다른 것이었다**: 그 글루를 잠그는 것이 아무것도 없었고, 누군가 조기 반환을 `return 1f;`로 「최적화」하면 케이스 1이 되살아나는데 **기존 W10~W14는 한 건도 안 빨개진다**(전부 필터를 직접 부른다). 그것을 `W15`가 소스 감사로 막았다 — **필터 경유 2회 정확 일치 + 상수 반환 음성 대조 + `exStyle` 마스킹 금지**, 세 축 |
| **M-11** | — | ❌ 미착수 | — | **의도된 보류.** 1판 권고(1.0 범위 밖) 유지. ★ 다만 아래 **신규 사유 1건**이 붙는다 → §10-3 |
| **M-12** | ★ **1판과 방향이 뒤집혔다** | — | — | 지금 활성 타깃은 **macOS**(§0-3 2). ⇒ 라운드마다 못박아야 할 것이 `xcheck.sh osx`가 아니라 **`xcheck.sh win`**이다. **완료 보고에 어느 쪽을 적을지가 타깃에 따라 달라진다는 것 자체가 함정**이므로, **양쪽 다 적게 하는 편이 안전하다** |
| **M-13** | ✅ **조사 완료** | — | ✅ | `ROADMAP §25` — **2~3주**로 확정. ★ 그리고 그 절이 **1판의 안전망 가정을 무효화했다**: *"T5는 안전망으로 성립하지 않는다"*(§25-4). ⇒ **출시일 공식의 항은 아니지만, 「신고가 오면 그때 사면 된다」는 탈출구가 없어졌다** → §10-4 |

### ★ 상태표에서 리더가 가져갈 결론 3줄

1. **1판의 A표 13항 중 실제로 닫힌 것은 7건**(M-2·3·4·6·9·10·13), **코드는 됐으나 커밋 전이 2건**
   (M-7·8), **설계만 2건**(M-1·M-5의 아이콘), **미착수 1건**(M-11), **성격이 바뀐 것 1건**(M-12).
2. ★ **가장 위험한 오해는 M-5다.** *"조사했다"*가 *"고쳤다"*로 읽히면 **아이콘 없는 exe가 그대로
   스토어에 간다.** 세 값은 오늘도 `ProjectSettings.asset`에 그대로 있다.
3. ★ **M-2는 「닫혔다」가 아니라 「휴면」이다.** 정의 심볼도 패키지도 없다. **켜는 순간 I-20이 발동한다.**

---

## 3-B. ★ I-3 depot 갈림길 — **재확인 요청에 답한다**

> 리더 질의: *"이전에 「대체 가능성 확보됨」이라고 판정한 바 있다 — 그 판정이 지금 진행 상황과
> 여전히 맞는지만 재확인해라."*

**세 부분으로 나눠 답한다: (1) 그 판정은 유효한가 / (2) 내 문장 중 틀린 것 / (3) 갈림길의 성격이
바뀌었다(이게 이번의 실체다).**

### (1) 「대체 가능성 확보됨」 — ★ **유효하다. 그리고 오늘 더 강해졌다.**

1판 근거는 이음매가 하나뿐이라는 것이었다:
```csharp
public interface IPackEntitlementSource { PackEntitlementState Query(string packId); }
```
**오늘 그 위에 M-2가 얹혔는데, 이음매를 넓히지 않고 오히려 규격화했다** —
`PackEntitlements.UseSource()`가 신설됐고 **`internal` · 1회 성공만 · 단조 래치 유지**다(커밋 메시지
및 어댑터 주석 실측). 즉 **출처 교체가 「구현체를 하나 더 만들고 한 번 꽂는 것」으로 표준화됐다.**
depot 방식으로 갈아탈 때 `PackEntitlement.cs`는 여전히 **한 줄도 안 바뀐다.** ⇒ 판정 유지.

### (2) ★ 내 문장 중 **틀린 것 1건** — *"바뀌는 것은 로더 한 줄"*

1판 §13-10-e에 그렇게 적었다. **과장이다.** 실측:
```
PackRegistry.cs:180   Resources.LoadAll<StickPackManifestSO>(ResourceFolder)
PackRegistry.cs:85-91  "Resources.LoadAll은 이미 지목된 위험 지점 … 정적 초기화자에 두지 않는다"
PackRegistry.cs:194   "그 자리는 Resources.LoadAll이 물고 있어 테스트가 값을 넣어 볼 수 없고"
Packages/manifest.json  Addressables 0건 (steam 문자열도 0건)
```
**계약은 안 바뀐다는 것이 참이고, 「한 줄」이 거짓이다.** 교체가 실제로 무는 것은
**로더 + 빌드 파이프라인 + 패키지 도입 + 정적 초기화 계약 + 그 자리를 물고 있는 테스트**다.
⇒ **정직한 서술**: *"접속 형태(`cohortId` 조인 · 매니페스트에 에셋 참조 없음)는 그대로 살아남고,
교체 비용은 「계약」이 아니라 「로더와 빌드 파이프라인」에 있다. 그 비용은 아직 안 셌다 — 미확인."*

### (3) ★★ 갈림길의 성격이 바뀌었다 — **M-1을 더 이상 묶지 않는다**

`product-strategy`가 `ROADMAP §24-1-b`에 **정당한 경고**를 적었다:

> *"이 결정은 **M-1(조형 통로)과 같은 라운드에 내려야 한다.** 팩 파이프라인을 한 번 짜고 나서
> 바꾸면 그건 재작업이다."*

**그 경고의 전제는 「depot을 쓰려면 팩 콘텐츠를 `Resources/`에서 꺼내야 한다」였다.**
콘텐츠 축에서는 그 전제가 옳다(`Resources/`는 `resources.assets` 한 파일로 구워져 쪼갤 수 없다 —
실측 확인). **그런데 엔타이틀먼트 축에서는 옳지 않다.** 오늘 `security` 문서를 읽고 알았다:

```
STEAMWORKS_ENTITLEMENT_EXCEPTION §6-4
  "우리 6팩은 콘텐츠가 본편에 이미 들어 있는 「라이선스만」 DLC다 … 즉 depot이 비어 있다."
  대비책 (ㄱ):  "DLC에 아주 작은 더미 depot 파일 1개를 넣어 실제로 설치되게 한다
                (코드 0줄, 파트너 사이트 작업. 이쪽을 먼저 검토한다)"
```

**안 A가 필요로 한 것은 「팩 콘텐츠」가 아니라 「DLC당 디스크 흔적 하나」다.**
그리고 그 흔적은 **더미 파일 1개로 만들어지며, 그건 코드 0줄 · 로딩 방식 0변경 · 파트너 사이트
조작이다.** ⇒ 세 가지가 따라 나온다:

| # | 결론 | 근거 |
|---|---|---|
| **가** | ★ **M-1은 depot 갈림길에 안 묶인다.** `product-strategy`의 「같은 라운드」 요구는 **이제 성립하지 않는다** | 필요한 것이 콘텐츠 이사가 아니라 더미 파일 1개이므로 |
| **나** | ★ 대신 **다른 곳에 묶인다 — I-2(스토어 지원 OS 선언)와 같은 조작 회차**다. DLC appID를 만들 때 depot 구성을 같이 정해야 한다 | 나중에 붙이면 **이미 산 사람에게 depot이 뒤늦게 내려간다.** 그 구간이 정확히 §13-10-b의 4중 모호성이고 **우리가 스스로 만드는 것**이다 → **I-20** |
| **다** | ★★ **어댑터의 `false ⇒ NotOwned` 매핑이 「더미 depot이 항상 설치된다」에 의존하게 됐다** | `BIsDlcInstalled`가 보는 것은 **설치**다. 빈 depot이 false로 나오면 **정상 구매자 전원이 `NotOwned`**다. ⇒ **§6-4 실기 확인 전에 유료 팩을 켜면 안 된다. 이건 출시 게이트다** |

★ **그러므로 1판 §13-10의 판정 (가)는 유지하되, 부가 조건 하나가 명시적으로 붙는다**:
> **(가)를 고른다. 단 「빈 depot이 `BIsDlcInstalled`에서 true인가」가 확인되기 전까지
> `STICKMATE_STEAMWORKS_INSTALLED`를 켜지 않는다.** 켜지 않은 동안 `Query`는 `Unknown`이고,
> `Unknown`은 아무도 잠그지 않는다 — **지금 상태가 안전한 기본값이다. 유지해라.**

★ **그리고 이 부가 조건은 지금 진행 중인 M-1·M-2 작업에 영향을 주지 않는다** — M-1은 조형 통로이고
엔타이틀먼트를 안 만지며(§13 설계가 세이브 스키마도 안 올린다), M-2는 이미 착지했고 **꺼져 있다.**
⇒ **리더 질의에 대한 최종 답: 영향 없음. 재확인 결과 판정 유지. 새로 붙는 것은 I-20 하나다.**

---

## 4. 【B표】 Windows 실기를 반드시 기다려야 하는 것

> **공통 전제**: 아래 전부 **H-0(새 빌드)** 이후여야 뜻이 있다.

| # | 무엇이 막혀 있는가 | 담당 후보 | 심각도 | 낮 세션 항목 |
|---|---|---|---|---|
| **H-0** | ★ **검증 대상이 낡았다.** 출하 산출물 mtime 09-03 08:32 · **exe SHA가 Unity 템플릿과 동일**(=dGPU 패치 미적용) · 그 뒤에 미커밋 318파일 라운드가 있다 | 리더(빌드 권한) + `dev-platform` | ★ **출시 차단(절차)** | 세션 전 선결. ★★ **2판 재측정: 하나도 안 바뀌었다** — `Builds/Windows/StickMate.exe` **671,744 B · 09-03 08:32**, `StickMate_Data/Managed/StickMate.Runtime.dll` **09-03 08:32**, SHA-256 **`bf64a21a5c91…8c656559`**(1판과 바이트 단위 동일). 그리고 **그 뒤로 커밋 2개 + 미커밋 라운드 2개가 더 쌓였다.** ⇒ **격차가 벌어졌다. 언제 빌드할지는 §10-1** |
| **H-1** | **기동 + 오버레이 + 클릭 관통 + 창 열거가 사용자 기계에서 관측된 적이 없다(CW-1).** 크로스컴파일 0에러는 **C# 문법**을 본 것이고 **P/Invoke 심볼 해석·플러그인 임포트·런처 조립**은 못 본다 | `dev-platform` | ★ **출시 차단** | A-8 · A-9 |
| **H-2** | **작업표시줄 원복이 정상 종료·강제 종료 양쪽에서 성립하는가(CW-2)** | `dev-platform` | ★ **출시 차단** | B-1 · B-4 · **B-5** · B-6 · §0-1 원장 |
| **H-3** | ★★ **`rcWork`가 실제로 좁아지는가.** 안 좁아지면 **하단·측면 회피 로직 전체가 헛돈다** | `dev-platform` | ★ **출시 차단** | **B-2** — §D 전체의 전제 |
| **H-4** | **dGPU 게이트 G1~G4.** 24시간 상주 앱이 노트북 dGPU를 계속 깨워 둔다 = 배터리·팬. 스토어 리뷰의 *"배터리 잡아먹는 앱"*은 회복 불가 | `dev-platform` | ★ **출시 차단 후보** | §5-2 절차 필수 |
| **H-5** | **BitBlt 잔상/렉 — 원인 사슬은 문서로 확정됐고 탈출구가 없다.** 레이어드 창 투명이 flip-model과 양립 불가라 BitBlt 강제이고, Unity에 대체 스위치가 없다. 남은 레버는 프레임 상한·MSAA뿐이고 **그것이 실제로 듣는지 한 번도 못 봤다** | `debugger` + `perf-doc` | ★ **출시 차단 후보** | 신고 원문 *"맥에서는 렉도 거의 없었는데 윈도우에서는 렉이 심함"*. **이 앱의 헤드라인 체감이 걸린 유일한 미해결 축** |
| **H-6** | **V3 오탐(CW-5).** 무서명 + 창 열거 + 전역 키 조회 + 최상위 + 종일 상주 = 휴리스틱 표적 | `dev-platform` | ★ **출시 차단** | dGPU 패치가 이 위험을 **더** 키우는지도 함께(G3) |
| **H-7** | ★ **스팀 오버레이 주입 ↔ 레이어드 창 상호작용이 저장소 어디에도 없다** | `dev-platform` + `product-strategy` | ★ **출시 차단 후보 (신규 발견)** | §7-1 |
| **H-8** | **잉크 코어 0px** — 계측기가 실패를 성공처럼 찍는 자리. **macOS Retina는 이 상태를 구조적으로 못 본다** | `design-character` + `dev-platform` | **출시 전 권장** | **A-2**(P0 1순위) |
| **H-9** | **표시 배율 125/150/175%** — 리본 y 시작점 반픽셀, 글자 번짐. **macOS는 ×1/×2뿐이라 이 축을 구조적으로 못 본다** | `design-art` + `ux-designer` | **출시 전 권장** | C-1 · C-2 · C-3 |
| **H-10** | **한글 폰트 폴백 두부(□)** — `맑은 고딕` 체인이 끊겼는지. **이 머신에선 영원히 안 보인다** | `ux-designer` | **출시 전 권장** | A-4 (+ `✕` U+2715 글리프) |
| **H-11** | **전역 단축키 11개 중 Windows 셸이 가져간 것** | `dev-platform` | **출시 전 권장** | A-7 (M-6가 선행하면 수확 증가) |
| **H-12** | **전체화면 등급 1(게임 아님) 거동 — 실기 0회.** 톱니까지 사라지면 **안전판이 자기 자신을 지운 것** | `dev-platform` | **출시 전 권장** | F-1 · **F-2**(복귀 통로) · F-3 |
| **H-13** | **다중 모니터 「유령 띠」** — macOS에서 왼쪽 배치일 때 존재하지 않는 182pt 띠가 실제로 났다. Windows는 좌표 규약이 다르다 | `dev-platform` | **출시 전 권장** | E-1 · E-2 |
| **H-14** | **작업표시줄 버튼 / Alt+Tab 제거가 실제로 먹는가** | `dev-platform` + `marketing` | **출시 후 가능** | A-6. **버튼이 남아도 앱은 정상 동작한다** — 다만 「Alt+Tab에 안 뜹니다」를 **홍보 문장으로 쓸 수 없게 된다** |
| **H-15** | **1pt→월드 환산 실측 1회가 세 항목을 함께 닫는다**: 획 하한 · 착지 티어 · 매달리기 | `design-motion` + `design-character` | **출시 후 가능** | A-2 · B-3. 지금 Windows 낙차는 **실측이 아니라 역산**(작업표시줄 48pt 가정) |
| **H-16** | **세션 잠금 게이트 · z밴드 유령 발판** — 코드는 닫혔거나 보류, 실기 재현이 불가 | `dev-platform` | **출시 후 가능** | 잠금/해제가 낮 시간을 많이 먹는다 → **후순위 유지** |

---

## 5. 낮 세션 1회 설계 — 여기서 거짓 통과가 난다

**세션의 값은 항목 수가 아니라 「가른 것의 수」다.** 아래 5개를 안 지키면 항목을 다 돌아도 아무것도
못 가른다. 전부 이 저장소가 이미 당한 형태다.

### 5-1. 빌드 도달 확인 — `StickMate.exe` 날짜를 보지 마라
그건 Unity 런처 스텁이다. **`StickMate_Data/Managed/StickMate.Runtime.dll`의 mtime**을 본다.
그리고 **exe의 SHA-256이 `bf64a21a…8c656559`가 **아닌지**** 확인한다 — 같으면 **dGPU 패치가 안 들어간 것**이다.

> ### ★★ 2판 정정 — **이 SHA 검사는 곧 거짓 통과 생성기가 된다. 지금 대체안을 정해라.**
> 위 검사가 지금 작동하는 이유는 **exe가 Unity 플레이어 템플릿과 바이트 단위로 같기 때문**이다.
> **M-5를 하는 순간 그 조건이 사라진다** — 아이콘(`m_BuildTargetIcons`)이나 `bundleVersion`이
> 들어가면 exe는 **dGPU 패치와 무관하게** 템플릿과 달라진다. 그때부터 이 검사는 **항상 통과**한다.
>
> **「SHA ≠ 템플릿」은 「무언가 바뀌었다」를 증명하지, 「그 패치가 적용됐다」를 증명하지 않는다.**
> 그리고 그 두 출력은 **똑같이 생겼다** — 이 저장소가 반복해서 당한 바로 그 형태다.
>
> **대체 판정 (둘 다 해라, 하나는 형태 검사이고 하나는 값 검사다):**
> 1. **후처리 로그의 정규형**: `WindowsHybridGpuExportPostprocessor`가 심볼별로
>    `RVA / 섹션 / 파일오프셋 / 현재값 / 판정`을 찍는다(`:178-185`). **줄 수와 값이 채워졌는지까지**
>    본다(빈 필드가 형태 검사를 통과한 사고가 이 저장소에 있다).
> 2. ★ **산출물에서 값을 되읽는다**: 같은 파서로 `Builds/Windows/StickMate.exe`의 export 두 개를
>    다시 읽어 **실제로 `0`인지** 확인한다. **이게 유일한 값 검사다.**
>    ★ 지금 저장소에 **그 되읽기 도구가 없다**(`Tools/VerifyChange/`에 dGPU/export 관련 히트 0건 —
>    실측). **만드는 것이 M-5보다 먼저다** → §10-2.
>
> ★ **다행한 것 1건 (확인함)**: 아이콘을 넣어도 **후처리 자체는 안 깨진다.** 오프셋이 상수가 아니라
> **런타임에 export 테이블을 파싱해 얻은 값**이기 때문이다(`:158-192` — `TryReadExports` →
> `TryFind(symbolName)` → `symbol.FileOffset`). 게다가 **쓰기 전 검사 4중**(파싱 실패 / 이름
> 사전순 / forwarder / 값 판정)이 있고 하나라도 걸리면 **한 바이트도 안 쓴다.**
> ⇒ **M-5(아이콘·버전)와 dGPU 패치는 순서 의존이 없다. 같은 빌드에 넣어도 된다.**
> 순서를 지켜야 하는 것은 여전히 **I-4(패치 → 서명)** 하나뿐이다.

### 5-2. ★★ G1의 교락 — 성공과 실패가 똑같이 생겼다 (신규 지적)
사용자 기계에는 A/B 실측 때 건 **수동 GPU 선호(「절전」 = `UserGpuPreferences`)가 아직 남아 있을
가능성이 높다.** 그 상태에서 패치된 빌드를 돌리면 **내장 GPU 이름이 뜬다 — 패치와 무관하게.**

> **G1은 이 순서를 지키지 않으면 무효다:**
> 1. Windows 설정 → 그래픽에서 StickMate 관련 항목을 **전부 「Windows가 결정」으로 되돌린다**
> 2. **음성 대조** — **패치 전 빌드**(기존 zip)를 먼저 띄워 로그에 **NVIDIA가 뜨는지 확인한다.**
>    여기서 Intel이 뜨면 **그 뒤 숫자는 전부 폐기**한다(수동 설정이 안 지워진 것이다)
> 3. **패치 후 빌드**를 띄워 Intel이 뜨는지 본다
> 4. **작업 관리자 → 성능 → NVIDIA 사용률·전용 메모리가 0인지 함께 본다** — 이름만 바뀌고
>    dGPU가 켜져 있는 **부분 성공**을 거른다

### 5-3. dGPU와 BitBlt를 같은 실행에서 재지 마라
패치가 GPU를 내장으로 내리면 **BitBlt 복사 비용의 주체가 바뀐다.** 두 변수를 한 번에 바꾸면
어느 것이 효과인지 원리상 못 가른다. **(1) 패치 전 = 기준선 → (2) 패치 후 = dGPU만 바뀐 것 →
(3) 프레임 상한/MSAA** 순서로 한 번에 하나만 움직인다.

### 5-4. 재시작 예산이 세션 시간을 지배한다
`[렌더품질]`은 **프로세스당 1회**, `[렌더진단] ★A/B 요약`은 **시작 후 60초에 한 번만** 찍히고
이후 측정이 멈춘다. ⇒ **장시간 A/B를 그 줄로 재면 반드시 「차이 없음」이 나온다.**
장시간의 유일한 올바른 단위는 **5분 주기 요약 줄**이다.
그리고 **`Player.log`는 껐다 켤 때마다 덮어써진다** — 재시작이 많은 §A·§B·§C는 **구간마다** 발췌를 돌린다.

### 5-5. 실기 인스턴스는 **한 번에 하나**
세이브 파일을 모든 인스턴스가 공유한다. 동시에 여럿 돌면 **진행도가 서로 덮어쓴다** — 화면 문제가
아니라 **사용자 데이터 문제**다. `driver.sh stop`과 전역 `Q`는 여전히 **절대 금지**.

---

## 6. 이 국면에서 강등하는 것 (지우지는 마라)

`PlatformParityAuditTests.cs`의 실행되는 `Assert.Ignore`는 **13건**이다(문서 주석 5건 제외).
**Windows 관련 9건 / macOS·모바일 전용 4건.** 뒤의 4건을 내린다.

★ **2판 재측정(2026-09-05)**: 이 파일이 이번 라운드에 **+310줄** 늘었는데도 실행되는
`Assert.Ignore`는 **여전히 13건**이다(주석 내 4건은 별도로 셌다 — 1판은 5건이라 적었고
**세는 방법이 달랐을 수 있다**). ⇒ **총수는 안 변했다는 것만 확인했고, Windows 9 / macOS·모바일 4
내역이 그대로인지는 재분류하지 않았다 — 미확인.** 아래 4건을 실제로 내릴 때 함께 다시 센다.

| 항목 | 위치 | 왜 내리는가 |
|---|---|---|
| macOS Dock 자동 숨김 해제 판정 | `:386` | Windows 단독 출시에 영향 0. 판정은 이미 「Windows만 한다」로 끝나 있고 **실행만 보류** |
| macOS 상시표시영역(상태 아이템) | `:592` | Windows 쪽(`WindowsSystemTrayIcon.cs`)은 **착지 확인됨** |
| 모바일 스크린샷 백드롭 | `:738` | 4플랫폼 목표는 살아 있으나 **이번 출시 범위 밖**. 설계 모순 1건(안전망 부재)은 그대로 열어 둔다 |
| macOS `NSSupportsAutomaticGraphicsSwitching` | `:3736` | 듀얼 GPU Intel Mac이 없어 **초록을 만들 수 없다** |

★ **`Assert.Ignore`를 지우지 마라.** 러너에 「건너뜀」으로 계속 보이는 것이 이 항목들의 유일한
생존 장치다(CLAUDE.md). **사유 줄에 「Windows 출시 국면에서 강등(2026-09-05)」만 덧붙인다.**

★ 반대로 **승격되는 것 1건**: M-8(가상 데스크톱)은 「macOS 패리티 부채」에서 **「Windows 고유 결함」**으로
성격이 바뀐다. 사유 문구를 그렇게 고쳐야 다음 사람이 우선순위를 제대로 읽는다.

> ★★ **2판 — M-8은 이제 `Assert.Ignore` 항목이 아니다. 실측으로 확인했다.**
> `PlatformParityAuditTests.cs:2015-2065`가 **건너뜀이 아니라 실제 단언**으로 바뀌어 있다 —
> 계약 파일·정책 파일 존재, `IVirtualDesktopMembershipSource`의 메서드 수 **정확히 1**,
> 프로브가 `IVirtualDesktopManager`를 쓰고 **`IVirtualDesktopManagerInternal`(비공개 API)은
> 안 쓰는지**, 그리고 `SuspendsCharacter`의 세 입력 판정. **옳은 승격이다.**
> 정책은 **플랫폼 중립 위치**(`Platform/VirtualDesktopSuspendPolicy.cs`)에 있고 Windows 쪽은
> **사실 조회만** 한다 — CLAUDE.md가 `FullscreenSuspendPolicy` 사고 이후 못박은 형태 그대로다.
> ★ **다만 이 단언들은 「소스 텍스트」와 「플랫폼 중립 타입」만 본다.** `#if UNITY_STANDALONE_WIN`
> 안의 프로브가 **컴파일되는지는 여전히 못 본다**(§0-3 2). **`xcheck.sh win`이 그 자리를 메운다.**

---

## 7. 미확인 — 추측으로 메우지 않는다

**7-1. ★ 스팀 오버레이가 우리 창에 무엇을 하는가 (신규 · 아무 문서에도 없다)**
저장소 전체에서 스팀 오버레이는 **macOS 하드닝 런타임 엔타이틀먼트 2종** 맥락으로만 언급된다
(`ROADMAP:274` · `ENTITLEMENT_CONTRACT:231`). **Windows 쪽 서술은 0건이다.**
그런데 우리 Windows 출하 형상은 **`WS_EX_LAYERED` + DWM 확장 프레임 + BitBlt 스왑체인 하이브리드**이고,
스팀은 자기가 띄운 프로세스에 오버레이 렌더러를 주입한다. **둘이 만난 적이 한 번도 없다.**
- 알 수 없는 것: 주입이 투명 합성을 깨는가 / 앱이 게임으로 취급되는가 / 부팅 자동 실행(스팀 밖)과
  스팀 경유 실행에서 형상이 갈리는가.
- **잴 수 있는 유일한 방법**: 사용자 기계 + 스팀 클라이언트. **낮 세션 항목으로 신설해야 한다.**
- ★ 다행히 **엔타이틀먼트 계약은 이 경우를 이미 견딘다** — 부팅 자동 실행으로 스팀이 없으면
  `Unknown`이고 그건 `NotOwned`가 아니다(`PackEntitlement.cs`). **설계는 맞다. 확인이 없을 뿐이다.**

**7-2. Unity 6에서 스플래시를 끌 수 있는가** — 라이선스 축. `m_ShowUnitySplashScreen: 1` 실측.
**끌 수 있다고 가정하고 일정을 짜지 마라.**

**7-3. OV 인증서 조달 리드타임** — CA마다 다르고 아무도 안 물어봤다(M-13).

**7-4. GitHub 공개 릴리스 16회 다운로드 중 제3자 비율** — 구분 불가. **「제3자 도달 증거」로 쓰지 마라.**
다만 **채널은 2026-08-30부터 상시 열려 있고**(T3 이미 발동), T5 관측창도 「지금부터」로 넓혀져 있다.
⇒ **출시 전에도 「실행이 안 된다」 신고가 발생할 수 있다.** 그 신고를 받을 창구가 있는가는 미확인.

**7-5. 규모 산정** — ~~2건 불가~~ → **1건은 2026-09-05에 세었다.**
- ~~**M-1의 공사량**~~ ✅ **셌다 → §13-2 · §13-7-a.** 결과: `case` 42(43 아님) 중 **팩 경로 52 case ·
  4 파일**(FX·PET 포함, HAIR 제외). 좌표 18종 중 **17종(94%)이 현행 문법으로 표현 가능**하고
  **절차 잔존은 베레모 꼭짓점 1개**. 슬롯당 한계비용 = **에셋 1,800줄 + 골든 320줄 + 코드 −400줄**
  (NECK 파일럿 커밋 `7ed996d` 실측). 전량 이행이 무는 것 = **테스트 29파일 · 99심볼.**
  ★ **여전히 못 센 것 4건은 §13-13에 「미확인」으로 남겼다** — 특히 **M-1d(FX·PET 레지스트리)의
  실제 공수는 미측정이고, 작다고 가정하고 일정을 짜면 안 된다.**
- **H-5(BitBlt)의 잔여 레버 효과**: 프레임 상한·MSAA가 잔상을 실제로 줄이는지는 **실기 전 산정 불가**.

---

## 8. ★★ 캐릭터 정보창 디자인 불일치 — **두 번째다. 절차 결함인가를 판정한다**

> 리더 질의: *"이게 단순 실수 반복인지, 아니면 「리뷰 완료」를 기록하는 절차 자체에 구조적 결함이
> 있는지 판정해라."*

### 8-0. 판정 — **구조적 결함이다. 단순 반복이 아니다.**

**그리고 결함의 자리가 리더의 가설과 다르다.** 리더가 제시한 후보는
*"디자인 인계본은 반드시 실제로 열어서 끝까지 확인했다는 증거(스크린샷 등)를 남겨야 리뷰 완료로
인정한다"*였다. **증거를 남기는 것은 처방의 일부로는 맞지만, 이번 사고의 원인이 아니다.**

**반증이 문서에 있다. 이게 이 판정의 전부다:**

`docs/UX_HANDOFF_REVIEW_BRASS_ARCHIVE.md` 제1부 §0-A와 제2부 §0-B가 **둘 다** 이렇게 적었다:

> *"프로토타입 HTML을 **브라우저로 렌더해서 재지 않았다**(이 환경에 브라우저가 없다)."*

**그 자백은 정확했고, 빠진 능력을 정확히 지목했고, 문서에 남았다. 그리고 아무 일도 일어나지 않았다.**
그 위에서 리더 판정(`eca8c58` 브라스 채택)이 내려졌고 **코드로 착지**했으며, 그 뒤 아무도
인계본으로 돌아가지 않았다.

⇒ **결함은 「자백이 없었다」가 아니다. 「자백이 아무 상태도 만들지 않는다」다.**
2026-09-03 처방 3개 중 3번이 정확히 *"리뷰 문서에 「무엇을 안 봤는가」를 반드시 적는다"*였다.
**그 규칙은 지켜졌고, 지켜진 채로 같은 사고가 났다.** 규칙을 지켜도 사고가 나면 그건 실수가 아니라
**절차의 결함**이다. 이것이 「단순 반복」을 기각하는 근거다.

### 8-1. 09-03 처방 3개를 이번 사고에 하나씩 대 본다

| 09-03 처방 | 이번에 지켜졌나 | 막았나 | 왜 |
|---|---|---|---|
| ① **저장소로 들여온다**(`docs/handoff/…`, `diff -r` 0) | ✅ 지켜졌다 | ❌ | ★ **`diff -r 0`은 「복사의 충실도」를 재지 「원본의 자족성」을 안 잰다.** 인계본은 **저장소 안에 있으면서 열리지 않았다** |
| ② **목록부터 만든다 · 표를 소진해야 끝난다** | ✅ 지켜졌다(파일 3개 · 조각 91개 · 문자열 9건 전수) | ❌ | ★★ **커버리지 단위가 「파일」이라 「축」이 샌다.** 파일 표는 **100% 소진되면서** 레이아웃 축은 **0% 측정**일 수 있다. 실제로 그랬다 |
| ③ **「무엇을 안 봤는가」를 적는다** | ✅ 지켜졌다 (위 인용) | ❌ | ★★ **자백이 문장일 뿐 상태가 아니다.** 담당도 기한도 차단력도 없다. 다음 사람은 「리뷰 완료」라는 제목만 본다 |

**세 처방이 전부 지켜졌는데 세 번 다 못 막았다. 결함은 처방의 준수율이 아니라 처방의 대상이다.**

### 8-2. 물리적 원인 — 나도 직접 확인했다(보고를 믿지 않고 다시 쟀다)

```
docs/handoff/design_handoff_equipment_window/reference/
    equipment-screen.dc.html   6번째 줄:  <script src="./support.js"></script>
    ItemIcon.dc.html           6번째 줄:  <script src="./support.js"></script>
find . -name "support.js"          →  0건
git log --all -- "*support.js"     →  0건   (한 번도 존재한 적이 없다)
```
그 스크립트가 `React` · `DCLogic` · `<x-dc>` · `<sc-for>` · `<dc-import>`를 전부 제공한다.
⇒ **원본을 브라우저로 열면 빈 화면이다. 아무도 인계본을 본 적이 없었다.**

★ **그리고 인계본 `README.md`가 이렇게 적고 있었다**:
*"`reference/equipment-screen.dc.html`을 브라우저에서 열면 됩니다(외부 의존성은 웹폰트 CDN뿐)."*
**거짓이다. 우리는 그 문장을 검증 없이 믿었다.**
⇒ **인계본의 자기 서술은 1차 출처가 아니다.** 우리 저장소 규칙(*"문서를 믿지 말고 코드와 실측을
믿어라"*)이 **외부 인계본에는 적용되지 않고 있었다.** 그게 사각지대였다.

### 8-3. 왜 「스크린샷을 남겨라」로는 부족한가 (리더 후보안에 대한 반론)

1. **스크린샷은 「내 재현기가 무언가를 그렸다」를 증명하지, 「그것이 인계본과 같다」를 증명하지 않는다.**
   이 저장소는 그 함정에 이미 두 번 빠졌다 — *"오프라인 렌더러가 모든 점에 둥근 캡을 찍어 코너
   붕괴를 가렸다"*, 그리고 *"생성기와 검사기가 같은 잘못된 `float32` 흉내를 공유했다"*.
2. **이번 원본은 애초에 렌더가 안 된다.** 스크린샷을 요구하면 **리뷰가 그 자리에서 막히는데,
   막혔다는 사실을 어디에도 등록할 자리가 없다** — 그러면 사람이 우회한다(README 선언값으로).
3. ★ **오늘 `ux-designer`가 실제로 한 것이 올바른 형태이고, 그것을 규칙으로 승격하면 된다**:
   재현기를 만들되 **기대값을 남의 값으로 교정**했다 — 아이콘 조각 **91** = `design-equipment`의
   독립 계수, 표시 문자열 **9/9** = `design-systems`의 독립 재구현. **교정 2건 통과.**
   ⇒ **증거는 「스크린샷」이 아니라 「교정된 재현」이다.** 교정이 없는 렌더는 증거가 아니다.

### 8-4. ★ 개정 초안 — **`docs/TEAM.md`가 본체, `CLAUDE.md`는 한 줄 포인터**

**왜 TEAM.md인가**: 「디자인 인계 접수 절차」의 정본이 거기고(사용자 확정 2026-09-03),
사용자가 *"출시 이후부터 계속 추가팩 만들거야"* · *"집중 시계도 디자인 추가할거고"*라고 못박아
**인계는 상시 유입**이다. CLAUDE.md에 본문을 넣으면 두 정본이 또 갈라진다(이 저장소에 이미
`Tasklist.md` 소유권과 페르소나 기록 자리에서 **두 번** 일어난 일이다).

> **리더에게: 아래는 초안이다. 나는 `docs/TEAM.md`도 `CLAUDE.md`도 수정하지 않았다.**

#### 초안 A — `docs/TEAM.md` 「디자인 인계 접수 절차」에 **0항을 앞에 붙이고, 3항을 강화**

```markdown
### 규칙 — 인계가 오면 이 넷을 즉시 한다   ← (셋 → 넷)

0. ★ **연다. 그 자리에서.** (2026-09-05 신설 — 같은 사고가 두 번 났다)
   인계본의 대표 산출물을 **실제로 열어 눈으로 확인**한다. 안 열리면 **그것이 1번 산출물이고,
   리뷰는 거기서 멈춘다.** 진행하지 않는다.
   - **안 열리는 원인이 우리가 못 채우는 의존성이면 그 파일을 사용자에게 요청한다.**
     ★ 2026-09-05 실측: `support.js` **한 파일**이 없어서 두 번의 리뷰가 전부 헛돌았다.
     그 하나를 요청했다면 이틀치 재작업이 없었다.
   - ★ **인계본의 자기 서술을 1차 출처로 쓰지 마라.** 이번 README는
     *"브라우저에서 열면 됩니다(외부 의존성은 웹폰트 CDN뿐)"*라고 적었고 **거짓이었다.**
     저장소 규칙(*"문서를 믿지 말고 실측을 믿어라"*)은 **외부 인계본에 더 세게 적용된다.**

1. **저장소로 들여온다.** (기존) `docs/handoff/<이름>/`에 복사, `diff -r` 0 확인.
   ★ **단 `diff -r 0`은 「복사가 충실한가」만 잰다 — 「원본이 자족적인가」는 0항이 잰다.**
   둘은 다른 질문이고, 2026-09-05에 **1항이 통과한 채로 0항이 실패했다.**

2. **목록부터 만든다.** (기존) — ★ **단 표의 행은 「파일」이 아니라 「축 × 담당」이다.**
   파일 표는 **보조로** 남긴다. 이유: 파일 표는 100% 소진되면서 축이 0% 측정일 수 있고,
   2026-09-03(아이템 아트)과 2026-09-05(레이아웃)이 **둘 다 그 형태**였다.
   최소 축 목록(인계 종류에 따라 가감, 뺄 때는 사유를 적는다):
   **구조·레이아웃 / 치수 / 색 / 타이포 / 조형(아트) / 모션 / 카피 / 상태·예외**
   각 행에 **담당 이름**과 **판정(이식 · 기각 · 미측정)**을 적는다.

3. **「무엇을 안 봤는가」를 적는다.** (기존) ★★ **그리고 그것은 문장이 아니라 상태다.**
   - **「미측정」으로 남은 축은 리더가 `Tasklist.md`에 미완료 항목으로 등재한다.**
     등재 전에는 그 인계 건을 **「리뷰 완료」로 부르지 않는다.**
   - ★ **리더 판정은 축 단위로만 내린다.** 판정문에 **「이 판정이 닫는 축」**을 반드시 적는다.
     2026-09-02 「브라스 채택」은 **색 축의 판정**이었는데 인계 건 전체의 종결처럼 읽혔고,
     그 뒤 하루 동안 아무도 인계본으로 돌아가지 않았다.

4. ★ **증거는 「스크린샷」이 아니라 「교정된 재현」이다.** (신설)
   원본이 안 열려 재현기를 만들었다면 **저장소에 함께 넣고**(예: `handoff/…/render/`),
   **기대값을 다른 팀의 독립 값으로 교정한 것 최소 2건**을 통과시킨다.
   교정이 없는 렌더는 증거가 아니다 — **생성기와 검사기가 같이 틀리면 둘 다 초록이다**(§10번째 형태).
```

#### 초안 B — `CLAUDE.md` 「필독 문서」 절에 **두 줄만** 추가 (본문 복제 금지)

```markdown
- ★ **디자인 인계본이 오면 `docs/TEAM.md`「디자인 인계 접수 절차」를 먼저 연다 — 0항(그 자리에서
  연다)부터다.** 안 열리는 인계본을 선언값만 읽고 리뷰한 사고가 **2026-09-03·09-05 두 번** 났다.
  **「리뷰 완료」는 축(구조/색/조형/모션/…)마다 따로 성립하며, 미측정 축이 하나라도 남으면
  그 인계 건은 완료가 아니다.**
```

### 8-5. ★ 이 사고가 **Windows 스팀 출시**에 무엇을 하는가 (이 문서에 있는 이유)

1. **낮 세션의 값이 걸린다.** §10의 P-1~P-7 캡처 7종이 전부 이 창을 찍는다.
   **이식 중간 상태를 찍으면 그 세션 항목은 전부 폐기**된다 → §10-1.
2. **`marketing`의 소재가 걸린다.** 스토어 스크린샷의 주력이 이 창이다.
   **지금 형상으로 찍으면 출시 전에 다시 찍어야 한다.**
3. ★ **그리고 이건 출시 차단은 아니다.** 사용자 신고는 *"디자인 파일대로가 아니다"*이지
   *"동작이 안 된다"*가 아니다. **1단계(골격)만으로 신고의 대부분이 닫힌다**는 것이
   `UX_EQUIPMENT_WINDOW_3COL_PORT §8`의 판정이고, 나도 동의한다 — **4스탯(4단계)은 그 위에 얹힌
   별건이고 출시를 막지 않는다.**

### 8-6. ★ 신규 기획 ↔ 기존 구조 충돌 — **3컬럼 이식에서 내가 본 것 3건** (내 담당 축)

| # | 충돌 | 판정 |
|---|---|---|
| **C-1** | ★ **파일 충돌 — `CharacterPortraitStage.cs`를 두 라운드가 동시에 노린다.** 3COL의 **L-3**(액자 204×196 → **265×238**, `PortraitContentSize`)와 M-1d의 **§13-6-c 초상화 이중 정의 정리**(`:1240,1317`)가 **같은 파일**이다 | ⇒ **M-1d는 3COL L-3 착지 이후.** 겹쳐 돌리면 한쪽이 다른 쪽 작업 중 파일을 커밋한다(이 저장소 실제 사고 2건) → §10-3 |
| **C-2** | ★ **4스탯(3COL 4단계)이 「저장 스키마 영향 — 별건」으로 적혀 있다.** 세이브 `CurrentVersion`은 지금 **10**(`CharacterSaveStore.cs:144` 실측) | ⇒ **I-5는 여전히 유효하다**(팩 작업은 스키마를 안 올린다). **다만 4스탯이 「지속되는 값」이면 11로 올라가고 그 라운드는 v10 하위 호환 테스트 1건을 반드시 동반한다**(CLAUDE.md). ★ **먼저 답할 질문은 「4스탯이 장비에서 파생되는가, 저장되는가」다** — 파생이면 스키마가 안 움직인다. **`design-systems`가 답할 것. 미확인** |
| **C-3** | ★ **삭제된 콘텐츠 참조 1건이 인계본 안에 있다.** `DESIGN_SYSTEMS_STATS §C-11` 실측: 인계본 line 928 `{ label: '연출 스킨 교체', desc: '**격파** 성공 시 테마 이펙트' }` | ⇒ **격파 놀이는 2026-09-02 사용자 확정으로 제거됐다. 그대로 옮기면 없는 기능을 UI가 약속한다.** `design-systems`가 이미 잡아 문구 교체를 권고했다 — **옳다. 그대로 채택 권고.** ★ 이 건은 **내 담당 범위의 정확한 사례**(신규 기획이 확정된 제거 결정과 부딪히는 자리)이므로 **이식 전에 닫아야 한다** |

★ **C-3의 일반형을 규칙으로 못박을 것을 권고한다**: **인계본은 우리가 무엇을 지웠는지 모른다.**
접수 절차 2항의 축 목록에 **「삭제·확정 결정과의 대조」**를 한 행 넣으면 이 검사가 매번 돈다.
지금까지 걸러야 했던 것 실측: **격파 놀이 제거 · 미니 보스 레이드 제외 · 던짐 랙돌 폐지 ·
캐릭터 조형 잠금(눈 없음 + 머리 꽉 참)** — 네 건 다 사용자 확정이고 **네 건 다 인계본이 모른다.**

---

## 9. 다음 우선순위 배정 후보 — **순서와 그 근거**

> **원칙**: *"A를 먼저"가 아니라 **"A를 안 하면 B가 어떻게 막히는가"**.*
> 그리고 이 국면의 핵심 자산은 여전히 **병렬성**이다 — W-gate는 **사용자의 낮 시간**을 쓰고
> 나머지는 **우리 시간**을 쓴다. 낮 세션을 기다리며 팀을 놀리면 그 시간이 전부 손실이다.

### 9-1. ★★ 순서가 바뀐 것 1건 — **낮 세션이 3COL 1단계 뒤로 간다**

1판은 H-0(새 빌드)을 사슬의 맨 앞에 놓았다. **그 판단을 부분적으로 뒤집는다.**

```
1판:  H-0 새 빌드  →  문서 정정  →  H-1 세션 설계  →  낮 세션 1회
2판:  H-0 새 빌드는 여러 번 해도 된다(리더 재량, 사용자 확정).
      ★ 희소 자원은 빌드가 아니라 「사용자의 낮 시간」이고 그건 1회다.
      ⇒ 3COL 1단계(골격) 착지  →  H-0 새 빌드  →  낮 세션 1회
```

**근거 — 3COL 1단계를 안 하면 낮 세션이 어떻게 막히는가:**
1. `UX_EQUIPMENT_WINDOW_3COL_PORT §10`의 실기 캡처 대기 목록 **P-1~P-7이 전부 이 창**이다.
   이식 중간 상태를 찍으면 **7항목이 통째로 폐기**된다.
2. **P-3이 이 문서의 관심사와 정확히 겹친다** — *"Windows 1920×1080@150%: 컬럼 1이 스크롤되고
   무대가 안 눌리는가"*. 그 문서 실측상 **8개 배율 환경 중 완전형이 깨지는 유일한 곳이
   Windows @150%**다. **macOS는 ×1/×2뿐이라 이 축을 구조적으로 못 본다** — H-9과 같은 성질이고,
   **낮 세션이 아니면 영원히 못 잰다.**
3. 사용자가 오늘 신고한 화면을 **고치기 전 상태로 다시 보게 된다.**

★ **단 예외 하나**: **dGPU G1/G2 · BitBlt(H-5) · V3(H-6)은 UI와 무관하다.**
낮 세션을 굳이 하나로 묶을 필요가 없다면 **「GPU/렌더 세션」과 「UI/DPI 세션」을 갈라도 된다.**
가르면 3COL을 기다리지 않아도 된다. **어느 쪽이든 리더 판단이고, 나는 「한 세션이라면 3COL 뒤」를
권고한다.**

### 9-2. M-1(팩 파이프라인) 착수 시점 — ★ **지금이다. 단 M-1a/b만.**

**M-1은 R-5-min의 지배항이고, R-5-min은 출시일 공식의 지배항이다**(§0-2). 즉 **출시일에 직접
붙어 있는 유일한 우리-시간 항목**이다. 그리고 팩 매니페스트 `.asset`은 오늘도 **0건**(§3-A).

| 착수 후보 | 지금 가능한가 | 근거 |
|---|---|---|
| **M-1a** 폴백 한 곳 + HAIR 클립을 슬롯 층으로 | ★ **가능. 지금 배정 권고** | `AccessoryShapeBuilder.cs` 단독. 3COL은 `CharacterInfoWindow.*`라 **파일이 안 겹친다**(실측) |
| **M-1b** `hatCoverRatio` + `SchemaVersion` 1→2 | ★ **가능. M-1a와 같은 라운드여야 한다**(I-16) | ★ **주의: 이건 `StickPackManifestSO.SchemaVersion`이지 세이브 `CurrentVersion`(=10)이 아니다.** 둘을 헷갈리면 없는 하위 호환 의무를 만들거나 **있는 의무를 놓친다** |
| **M-1c** 판정 1~5 + 레거시 래칫 | 가능(M-1a/b 뒤) | 「열렸다」를 증명하는 자리. 양성 대조 설계가 본체 |
| **M-1d** FX·PET 레지스트리 + 초상화 이중 정의 | ❌ **지금은 안 된다** | ★ **C-1 파일 충돌** — `CharacterPortraitStage.cs`를 3COL L-3이 잡고 있다. **3COL L-3 착지 후.** 그리고 **§13-13의 미확인 4번**(FX·PET 레지스트리 실제 공수 미측정)이 여기 붙어 있다 — **작다고 가정하고 일정을 짜지 마라** |
| **M-1e** 슬롯 단위 이행 | 후순위 | **팩을 막지 않는다. 부채 상환이다** |

★ **M-1a를 안 하면 무엇이 막히는가(한 줄)**: `wornShapes`를 채운 팩 아이템이 화면에 **「빠짐」
표식**으로 뜬다 — 데이터는 실려 있는데 아무도 안 읽는다. **팩 조형이 거기서 통째로 막힌다.**

### 9-3. 아이콘(M-5) 구현 착수 시점 — ★ **선행 1건을 먼저 만들어라**

`design/character/APP_ICON_SPEC.md`(520줄, 미커밋)가 설계를 끝냈다.
**구현은 세 조각**이고 소관이 갈린다:

| 조각 | 소관 | 지금 가능한가 |
|---|---|---|
| (가) 프리팹을 직교 카메라로 찍는 **에디터 캡처 스크립트** | `coder`(에디터 스크립트) | 가능 |
| (나) 다중 해상도 **`.ico` 생성** | `dev-platform` | 가능 |
| (다) `ProjectSettings` **배선**(`m_BuildTargetIcons`) + `bundleVersion` + 스플래시 | `dev-platform` | 가능. **단 스플래시는 라이선스 축 미확인**(§7-2) |

★ **선행 조건 — 이걸 안 하면 낮 세션의 dGPU 판정이 거짓 통과한다**:
**아이콘·버전이 들어가는 순간 §5-1의 SHA 검사가 무력해진다.** 그러므로
**「export 값 되읽기 검사기」를 아이콘보다 먼저 만들어야 한다**(§5-1 2판 정정).
지금 저장소에 그 도구가 **없다**(`Tools/VerifyChange/`에 관련 히트 0건 — 실측).
⇒ **순서: 되읽기 검사기 → 아이콘/버전 배선 → 새 빌드(H-0) → 낮 세션.**

★ **그리고 아이콘 자체는 출시 차단이 아니지만 「스토어 제출물」이다** — 작업표시줄·Alt+Tab·exe에
뜬다. **낮 세션 캡처에도 그대로 찍히므로 세션 전에 넣는 것이 이득이다.**

### 9-4. 그 밖 — 지금 배정 가능한 것 / 지금 배정하면 안 되는 것

| 항목 | 판정 | 근거 |
|---|---|---|
| **M-7·M-8 커밋 게이트** | ★ **지금. 다른 무엇보다 먼저** | 코드는 됐는데 **Windows 컴파일이 확인 안 됐다**(§0-3 2). 활성 타깃이 macOS라 러너 초록이 그 절반을 못 본다. **`xcheck.sh win` 0에러가 커밋 전 필수** |
| **I-20 실기 확인 절차 문서화** | 지금(문서만) | 스팀 파트너 사이트 조작은 **사용자만 할 수 있다.** 우리가 할 수 있는 것은 **그날 무엇을 볼지 미리 적는 것**뿐 |
| **M-11(오디오 Director)** | **여전히 1.0 밖 권고. 사유가 하나 늘었다** | 리더 판정 대기(춤 반응 기본값)가 **「켬」이면 세이브 스키마가 11로 올라간다.** ★ 그런데 **3COL 4단계(4스탯)도 스키마 후보다**(C-2). **한 국면에 스키마 상승 후보가 둘이면 순서를 정해야 하고, 정하지 않으면 두 라운드가 각자 11로 올린다** — 이 저장소가 이미 당한 형태(v9/v10 혼선). ⇒ **둘 다 미루는 것이 가장 싸다** |
| **M-13 후속(서명 구매)** | **아직 사지 마라** | `ROADMAP §25` 확정: 리드타임 2~3주, 지배항은 **배송이 아니라 신원 검증**. ★ 그리고 **T5 안전망이 무효**라는 것이 이번에 숫자로 확정됐다 ⇒ **「신고 오면 그때 산다」는 탈출구가 없다.** 그렇다고 지금 사는 근거도 없다(SAC 모수) — **리더가 출시일 확정 시점에 「출시일 − 3주」를 역산해 발주 시점을 못박아야 한다.** 그게 이 항목의 실제 산출물이다 |
| **문서 3곳 정정(§0-1)** | ★ **아직 안 됐으면 지금** | 1판이 지목한 `Tasklist.md:22940` · `WINDOWS_CHECK_SESSION.md:3-4` · `ROADMAP §17-2`. **가장 위험한 것은 두 번째** — 사용자가 *"출시 산출물이 아니다"*를 읽고 **낮 세션의 판정 기준을 낮춘다** |

### 9-5. 한 장 요약 — 배정 순서

```
【즉시 · 우리 시간 · 서로 안 겹침】
  ① M-7/M-8 win 크로스컴파일 → 커밋            (dev-platform)   ← 미검증 코드가 트리에 앉아 있다
  ② M-1a + M-1b  (AccessoryShapeBuilder ∙ 매니페스트 스키마)     (coder + design-equipment)
  ③ 3COL 1단계 골격                             (coder-ui)      ← 사용자 신고의 본체
  ④ export 되읽기 검사기                        (dev-platform)  ← ⑥의 선행
  ⑤ 문서 3곳 결론 정정                          (리더)
  ⑥ 아이콘/버전 배선                            (dev-platform)  ← ④ 뒤
        │
        ▼
  ⑦ H-0 새 Windows 빌드                         (리더 재량)
        │
        ▼
【사용자 낮 시간 · 1회】
  ⑧ 낮 세션 — §5의 5개 절차 준수

【뒤로】 M-1d(3COL L-3 뒤) · M-1e · M-11 · 3COL 4단계(4스탯) · 서명 발주(출시일 −3주 역산)
```

---

## 10. 플랫폼 영향 (CLAUDE.md 상시 지시)

**Windows 영향: 이 문서 전체가 Windows 대상이다.** 다만 프로덕션 `.cs` 수정 0건 — 설계·판단만.
새 Windows 코드를 배정할 때는 `Tools/CrossCompile/xcheck.sh win` 0에러가 여전히 의무다.

**macOS 영향: 「함께 검토함 — 강등하되 감시는 유지」.**
전환이 macOS를 **버리는 것이 아니다.** 4플랫폼 목표(CLAUDE.md)는 그대로 살아 있고, 이번 국면에서
순서가 뒤로 갈 뿐이다. 구체적으로:
1. **R-6a(서명·공증)는 죽지 않았다** — 출시일 공식에서만 빠진다. macOS를 나중에 depot으로 추가할 때
   그대로 되살아나고, **$99/년은 iPad/iPhone 타깃에 어차피 필요한 선지출**이라 순비용이 아니다.
2. ~~**활성 타깃이 Windows인 동안 macOS 회귀가 조용히 쌓인다**(M-12).~~
   ★★ **2판 정정 — 지금은 정반대다.** 활성 타깃이 **macOS**이므로(§0-3 2 실측) 조용히 쌓이는 쪽은
   **Windows**다. 그리고 하필 **이번 라운드의 신규 Windows 코드(M-7·M-8)가 미커밋 상태로 트리에
   앉아 있다.** ⇒ **지금 라운드에 못박아야 할 것은 `xcheck.sh osx`가 아니라 `xcheck.sh win`이다.**
   ★ **더 안전한 규칙**: 어느 쪽이 활성인지에 따라 필요한 명령이 바뀐다는 것 자체가 함정이므로
   **완료 보고에 항상 양쪽 결과를 적게 한다.** 활성 타깃을 매번 확인해야 옳은 명령을 고를 수 있는
   규칙은, 확인을 빠뜨리는 순간 **성공한 검증과 똑같이 생긴 실패**를 낸다.
3. **강등한 macOS 항목 4건은 `Assert.Ignore`로 러너에 계속 보인다.** 지우면 잊힌다.

**2판 라운드 자체의 플랫폼 영향** — 이 라운드는 **문서만 썼다**(`docs/strategy/…` 1파일).
프로덕션 `.cs` 0줄 · `.asset` 0바이트 · 앱 실행 0회 · Unity 배치모드 0회 · `xcheck.sh` 0회
(리더 지시로 금지). ⇒ **Windows 영향: 없음(문서). macOS 영향: 없음(문서).**
★ 다만 이 문서가 **다른 라운드의 Windows 검증 의무를 새로 지목한다**(§9-4 ①) — 그건 배정 사항이다.

---

## 11. 이번 판에서 **못 잰 것** — 추측으로 메우지 않는다

1. ★ **M-7·M-8의 Windows 컴파일.** `xcheck.sh` 사용이 이 라운드에 금지됐다. **미확인.**
   `WindowsVirtualDesktopProbe.cs`는 전체가 `#if UNITY_STANDALONE_WIN`이고 활성 타깃은 macOS다 ⇒
   **지금 저장소의 어떤 초록도 이 파일이 컴파일된다는 증거가 아니다.**
2. ★ **활성 타깃을 누가 왜 바꿨는가.** `.rsp` mtime으로 **바뀌었다는 사실**은 쟀지만
   **주체와 승인 여부는 모른다.** 리더가 그 라운드에 직접 물어야 한다.
3. **I-20(빈 depot ↔ `BIsDlcInstalled`)** — 스팀 파트너 사이트에서만 답이 나온다. **우리가 못 잰다.**
4. **3COL 4단계(4스탯)가 세이브 스키마를 올리는가** — 「장비에서 파생」이면 안 올라간다.
   **`design-systems`가 답할 것. 미확인**(§8-6 C-2).
5. **M-1d(FX·PET 레지스트리) 공수** — `GAME_ARCHITECTURE_REVIEW §13-13`의 미확인 4번 그대로다.
   **여전히 안 쟀다. 작다고 가정하고 일정을 짜지 마라.**
6. **Unity 6 스플래시 제거 가능 여부**(§7-2) · **OV 인증서 개별 CA 리드타임**(§25가 2~3주로
   좁혔으나 CA별 편차는 미확인) — 1판과 동일.
