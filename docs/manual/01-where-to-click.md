# StickMate 사용 설명서 — 1장. 어디를 누르는가

이 장을 먼저 읽으십시오. StickMate는 조작을 시작하는 곳이 화면에 **한 곳뿐**입니다.
그 한 곳을 못 찾으면 다른 기능은 하나도 쓸 수 없습니다.

---

## 1. 먼저 알아 둘 네 가지

StickMate는 다른 앱과 다르게 동작하는 부분이 있습니다. 아래 네 가지를 먼저 확인하십시오.

| 다른 앱에서 하던 것 | StickMate |
|---|---|
| Dock 아이콘을 누른다 (macOS) | **없습니다.** StickMate는 Dock에 나타나지 않습니다. |
| 메뉴 막대 아이콘을 누른다 | **없습니다.** macOS 메뉴 막대에도 Windows 알림 영역에도 아이콘이 없습니다. |
| 캐릭터를 마우스 오른쪽 버튼으로 누른다 | **메뉴가 열리지 않습니다.** 오른쪽 클릭은 StickMate를 그대로 지나가서 아래에 있는 앱으로 갑니다. |
| 캐릭터를 마우스 왼쪽 버튼으로 누른다 | **캐릭터를 집어 올립니다.** 메뉴는 열리지 않습니다. |

**조작을 시작하는 곳은 화면 오른쪽 위의 톱니바퀴 아이콘 하나입니다.**

### 1-1. 앱 전환기에 어떻게 나오는가

StickMate는 바탕 화면 위의 오버레이라서, 운영체제의 앱 전환 화면에 거의 나타나지 않습니다.
**Windows 작업 표시줄 버튼 하나만 예외입니다.**

| | macOS | Windows |
|---|---|---|
| Dock / 작업 표시줄 **버튼** | 나타나지 않습니다 | **나타납니다** |
| 앱 전환기 (`⌘Tab` / `Alt+Tab`) | 나타나지 않습니다 | 나타나지 않습니다 |
| 메뉴 막대 / 알림 영역 아이콘 | 없습니다 | 없습니다 |

- 위 표는 **숨겼는지와 관계없이 항상 같습니다.** 앱을 켠 순간 정해지고 바뀌지 않습니다.
- **Windows에서 그 버튼을 눌러도 캐릭터가 돌아오지 않습니다.** 숨긴 것을 되돌리는 것은
  `Ctrl+Alt+Win+K`이거나 앱을 다시 실행하는 것입니다. **3장 2-5**를 보십시오.

<!-- 리더에게 — 1-1은 2026-09-03 05:07에 착지한 코드에서 읽었다(줄 번호는 쓰지 않는다).
     · 규칙: Platform/AppSwitcherPresencePolicy.cs (플랫폼 중립)
         WindowsToolWindowExStyleBit = 0x80 (WS_EX_TOOLWINDOW)
         MacOsMechanismName = "NSApplicationActivationPolicyAccessory"
         ★ TakesEffectOnAlreadyShownWindow(AltTabList)   == true
           TakesEffectOnAlreadyShownWindow(TaskbarButton) == false   <- 표의 "나타납니다"가 이 한 줄이다
     · 실행: Windows  Platform/Windows/WindowsToolWindowStyleControl.ApplyOnce
                      (WindowsOverlayStateEnforcer의 창 부착 감지 시점에 1회)
             macOS    Platform/MacOS/MacSpaceBehaviorNative.ApplyAccessoryActivationPolicyOnce
                      (MacOverlayStateEnforcer의 같은 자리)
       두 자리의 대칭 자체를 Tests/EditMode/PlatformParityAuditTests가 잠근다.
     · 남은 갭은 저장소가 **스스로 선언**해 뒀다:
         PlatformParityAuditTests.미해결_Windows_작업표시줄_버튼은_스타일_변경만으로_사라지지_않는다
       (Assert.Ignore. 선택지 3개가 그 사유문에 적혀 있고 리더 판정 대기다.)
     ★★ 리더에게 보고한 대로, 이 라운드 브리프의 「⌃⌥⌘K로 숨기면 Windows 작업표시줄에서 완전히
        사라진다」는 **코드와 다르다**. 실제로 닫힌 것은 Alt+Tab이고, 그것도 숨김과 무관하게
        **기동 시 영구**다. 그래서 본문은 브리프가 아니라 코드대로 적었다.
        버튼 제거 경로가 들어오면 1-1 표와 3장 2-5를 함께 고쳐 주십시오. -->

---

## 2. 톱니바퀴 아이콘 찾기

### 2-1. 어디에 있는가

톱니바퀴는 **화면 오른쪽 위 모서리 근처**에 있습니다.
바탕 화면 위에 직접 그려져 있고, 어떤 창에도 들어 있지 않습니다.

```
 ┌─────────────────────────────────────────────┐
 │  (macOS 메뉴 막대 / Windows 작업 표시줄)     │
 │                                        ⚙︎ ←─┼── 톱니바퀴
 │                                             │    오른쪽 끝에서 30pt
 │                                             │    위쪽 끝에서: 아래 표
 │                                             │
 │                                             │
 │                                             │
 │   🧍 ←── 캐릭터는 보통 화면 맨 아래를 걷습니다 │
 └─────────────────────────────────────────────┘
```

**세로 위치는 고정값이 아닙니다.** StickMate는 운영체제에 **위쪽 띠의 두께**를 물어보고
그 아래에 자리를 잡습니다. 남의 띠를 덮지 않기 위해서입니다.

| 이 컴퓨터의 위쪽 띠 | 톱니바퀴 중심이 화면 위쪽 끝에서 |
|---|---|
| macOS 메뉴 막대 (33pt) | **64.8pt** |
| Windows 상단 도킹 작업 표시줄 (40pt) | **71.8pt** |
| 위쪽 띠 없음 (자동 숨김 / 아래·왼쪽·오른쪽 도킹) | **31.8pt** |

**가로도 같습니다.** 작업 표시줄이나 Dock을 **화면 오른쪽에 붙여 두면** 톱니바퀴가 그만큼
왼쪽으로 물러납니다. 예를 들어 오른쪽 띠가 48pt이면 톱니바퀴 중심은 오른쪽 끝에서
**67.8pt** 자리가 되어, 톱니바퀴가 그 띠에 걸치지 않습니다.

> **먼저 보게 되는 것은 캐릭터입니다.** 캐릭터와 톱니바퀴는 화면의 반대쪽 끝에 있습니다.
> 둘을 잇는 선이나 안내는 화면에 없습니다. 그래서 이 설명서가 필요합니다.

### 2-2. 어떻게 생겼는가

- 이가 6개인 톱니바퀴 한 개입니다. 가운데에 작은 고리가 있습니다.
- 지름은 약 **29.6pt**입니다. 손톱보다 조금 작은 크기입니다.
- 캐릭터와 같은 색(기본은 검정)으로 그려지고, 밝은 테두리가 함께 그려집니다.
  이 테두리 덕분에 어두운 바탕 화면에서도 밝은 바탕 화면에서도 보입니다.
- 평소에는 약간 옅게(불투명도 0.80) 그려집니다. 커서를 올리면 진해지고(0.95) 1.10배로 커집니다.

> **macOS 사용자에게**: 톱니바퀴는 메뉴 막대 **바로 아래**에 있습니다.
> 메뉴 막대 **안**에 있는 다른 앱의 아이콘처럼 보일 수 있습니다.
> 메뉴 막대 안이 아니라 그 아래 바탕 화면 위에 있다는 점을 확인하십시오.

<!-- 캡처 요청 A: 실제 빌드에서 화면 오른쪽 위 400x300pt 영역. 톱니바퀴와 메뉴 막대의 위치 관계가 보이도록. -->

### 2-3. 메모 카드는 톱니바퀴를 비켜 갑니다

할일이 하나라도 남아 있으면, 화면 오른쪽 위에 노란 띠가 붙은 **메모 카드**가 나타납니다(4-4 참고).
카드 폭은 220pt이고, 평소에는 화면 오른쪽 끝에서 16pt 자리에 붙습니다.

**그 자리는 톱니바퀴와 겹칩니다. 그래서 카드가 스스로 왼쪽으로 물러납니다.**

| 상황 | 카드의 오른쪽 끝 |
|---|---|
| 톱니바퀴와 겹치지 않을 때 | 화면 오른쪽 끝에서 16pt |
| 톱니바퀴와 겹칠 때 | 톱니바퀴 왼쪽 끝에서 다시 8pt 더 왼쪽 |

톱니바퀴가 기본 자리에 있으면 카드는 화면 오른쪽 끝에서 **57.8pt** 지점부터 시작합니다.
톱니바퀴는 오른쪽 끝에서 **49.8pt**까지 차지합니다. **둘 사이에 8pt가 남습니다.**

- 이 계산은 **매 순간 다시** 이루어집니다. 톱니바퀴를 옮기면 카드도 따라서 자리를 다시 잡습니다.
- 톱니바퀴를 화면 **왼쪽으로** 옮기면 카드는 원래 자리로 돌아갑니다.
- 톱니바퀴를 화면 **아래로** 옮겨서 카드 높이 아래로 내려가도 마찬가지입니다.
  가로와 세로가 **둘 다** 겹칠 때만 카드가 물러납니다.
- 할일이 0건이 되면 카드가 저절로 사라집니다.
- 카드 오른쪽 위의 **[숨기기]** 버튼으로 카드만 치울 수도 있습니다.

### 2-3-1. 메모 카드도 메뉴 막대와 작업 표시줄을 비켜 갑니다

카드의 **위쪽 끝**도 톱니바퀴와 같은 방식으로 정해집니다. 운영체제에 위쪽 띠의 두께를 물어보고,
그 **아래로 16pt** 지점부터 아래로 자랍니다.

| 이 컴퓨터의 위쪽 띠 | 카드 위쪽 끝이 화면 위쪽 끝에서 |
|---|---|
| macOS 메뉴 막대 (33pt) | **49pt** |
| Windows 상단 도킹 작업 표시줄 (40pt) | **56pt** |
| 위쪽 띠 없음 | **16pt** |

**가로도 같습니다.** 작업 표시줄이나 Dock을 화면 오른쪽에 붙여 두면 카드가 그 앞에서 멈춥니다.
오른쪽 띠가 48pt이면 카드 오른쪽 끝은 화면 오른쪽 끝에서 **64pt** 자리가 됩니다.

> ★ **바뀐 점**: 이전 빌드에서는 카드가 위쪽 끝에서 **항상 16pt** 자리에 붙어서,
> macOS에서 **메뉴 막대의 아래쪽 17pt(띠의 약 절반)를 덮고 그 구간의 클릭까지 가져갔습니다.**
> 지금은 **0pt입니다.** 배터리 · Wi-Fi · 제어 센터가 가려지지 않습니다.

카드가 방해가 되면 아래 중 하나를 하십시오.

| 방법 | 어떻게 |
|---|---|
| **1. 카드를 치운다** | 카드 오른쪽 위의 **[숨기기]** 버튼을 누르십시오. |
| **2. 할일을 다 끝낸다** | 남은 할일이 0건이 되면 카드가 사라집니다. |

> **[숨기기]는 앱을 끌 때까지 유지됩니다.** 다시 보이게 하는 버튼은 없습니다.
> 앱을 껐다 켜면 카드가 돌아옵니다.

<!-- 리더에게 — 2-3-1은 2026-09-03에 **통째로 뒤집혔다**. 이전 판은 "카드가 메뉴 막대 아래 절반을
     덮는다"였고 그것이 당시에는 참이었다. 지금 근거:
       Interaction/TodoPostItWidget.ResolveTopInsetPoints
         = ReservedTopBarProbe.TopInsetPoints(...) 를 사실 조회로 쓰고
           SurfaceSafeAreaPolicy.ClampTopDownCenterY 로 판정한다(플랫폼 중립).
         같은 함수의 클래스 문서가 세 환경의 값을 직접 적어 뒀다: 33->49 / 40->56 / 0->16.
       Interaction/TodoPostItWidget.ResolveRightInsetBoundsPoints
         = ReservedEdgeProbe.Insets(...) + SurfaceSafeAreaPolicy.ClampRightAnchoredInset.
         48->64 / 62->78 / 미측정·측정된 0 -> 16.
     ★ "미측정"과 "측정된 0"을 코드가 구분하지만 **사용자에게 보이는 결과는 같다**(둘 다 16).
       그래서 본문은 그 구분을 쓰지 않았다 — 사용자가 확인할 방법이 없는 구분이다. -->

### 2-4. 톱니바퀴로 할 수 있는 두 가지

| 하고 싶은 것 | 어떻게 |
|---|---|
| **버튼 열기** | 누르고 떼십시오. 톱니바퀴가 돌면서 둥근 버튼 5개가 펼쳐집니다. |
| **버튼 닫기** | 한 번 더 누르고 떼십시오. |
| **톱니바퀴 옮기기** | **0.4초 이상 누른 채로, 그 상태에서 6pt 이상 끄십시오.** |

★ **옮기려면 두 조건을 모두 넘겨야 합니다.** 하나만으로는 옮겨지지 않습니다.

| 이렇게 하면 | 결과 |
|---|---|
| 빠르게 톡 누른다 | **버튼이 열립니다.** |
| 3초를 꾹 누르고 그대로 뗀다 | **버튼이 열립니다.** 오래 눌러도 결과는 같습니다. |
| 빠르게 누르면서 확 끈다 (0.4초 미만) | **버튼이 열립니다.** 옮겨지지 않습니다. |
| 0.4초 이상 누르고 그 상태로 6pt 이상 끈다 | **톱니바퀴가 커서를 따라옵니다.** |

> **누르는 순간에는 아무 일도 일어나지 않습니다.** 손을 떼는 순간에 결정됩니다.
> 옮기려고 눌렀는데 창부터 뜨는 일을 막기 위해서입니다.

- 끌고 있는 동안 톱니바퀴가 조금 커지고 옅어집니다. 그것이 "지금 옮기는 중"이라는 표시입니다.
- **옮긴 자리는 저장됩니다.** 앱을 껐다 켜도 그 자리에 있습니다.
- 톱니바퀴는 화면 밖으로 나가지 않습니다.
- **OS가 예약한 띠 안에는 놓이지 않습니다.** macOS 메뉴 막대 · macOS Dock ·
  Windows 작업 표시줄이 차지한 띠에 놓으면, 그 띠를 **벗어나는 데 필요한 만큼만** 자동으로
  옮겨집니다. 그 띠 위의 클릭은 StickMate에 오지 않아 **눌리지 않는 톱니바퀴**가 되기 때문입니다.
  세로 위치는 그대로 두고 가로만, 또는 그 반대로만 움직입니다.
- 버튼 5개는 **6초 동안 아무 조작이 없으면** 저절로 접힙니다.
  버튼 바깥을 클릭해도 접힙니다. 그 클릭은 StickMate가 가져가지 않고 아래 앱으로 갑니다.
- 버튼 하나를 누른 뒤 **그 버튼 밖에서 손을 떼면 취소됩니다.** 잘못 눌렀을 때 이렇게 되돌립니다.

### 2-4-1. 옮긴 톱니바퀴를 원래 자리로 되돌리기

톱니바퀴를 화면 구석이나 창이 많은 곳에 놓으면 다시 찾기 어렵습니다.
**되돌리는 버튼이 있습니다.**

> 설정 창 **[일반]** 탭 → **화면 위 UI** → **톱니 위치** → **[처음 자리로]**

가장 짧은 길은 `⌃⌥⌘P`(Windows: `Ctrl+Alt+Win+P`)로 설정 창을 바로 여는 것입니다.
그러면 **2단계**로 끝납니다. 마우스만 쓰면 톱니바퀴 → **[캐릭터]** → **[설정]** → **[처음 자리로]** 입니다.

- 확인 대화상자는 나오지 않습니다. **버튼이 그 자리에서 회색이 되는 것이 「됐다」는 표시입니다.**
- 한 번도 옮긴 적이 없으면 이 버튼은 처음부터 회색이고 `아직 옮긴 적이 없어요.`가 적혀 있습니다.
- 톱니바퀴가 아예 안 보인다면 **3장**을 보십시오.

### 2-5. 처음 한 번만 나오는 안내

버튼 5개를 처음 펼치면 작은 안내가 4.5초 동안 나타납니다.

> 커서를 올리면 각 버튼 이름이 보여요

이 안내는 **이 컴퓨터에서 평생 한 번만** 나옵니다. 버튼에는 글자가 없고 그림만 있습니다.
이름을 보려면 버튼 위에 커서를 올리십시오. 자세한 내용은 2장 4절에 있습니다.

> ★ **이 안내가 떠 있는 동안에는 버튼 5개가 접히지 않습니다.**
> 버튼은 평소 6초 동안 아무 조작이 없으면 접히는데, 그 6초는 안내가 사라진 **뒤에** 시작합니다.
> 그래서 안내를 읽는 데 쓸 수 있는 시간은 **4.5초 + 6초 = 10.5초**입니다.
>
> 이전 빌드에서는 두 시계가 **같이** 흘러서, 읽는 도중에 버튼이 접히면 그와 함께 안내도
> 사라졌습니다. 그리고 이 안내는 한 번만 나오므로 **다시 볼 수 없었습니다.**

---

## 3. 버튼 5개

톱니바퀴를 짧게 클릭하면 둥근 버튼 5개가 부채꼴로 펼쳐집니다.
버튼은 지름 44pt이고, 이름 대신 그림만 그려져 있습니다.
커서를 올리면 그 버튼의 이름이 작은 알약으로 나타납니다.

**5개는 두 줄로 나뉩니다.**

- **앞줄 4개** — 톱니바퀴에서 **화면 안쪽 방향**으로 111pt 떨어진 호(弧) 위에 나란히 섭니다.
  버튼 사이 각도는 30°이고, 4개가 차지하는 각도는 90°입니다.
- **뒷줄 1개** — **[앱 종료]** 하나입니다. 앞줄보다 한 겹 바깥인 168pt 자리에,
  부채꼴의 **한가운데 축 위**에 홀로 섭니다.

톱니바퀴가 기본 자리(오른쪽 위)에 있으면, 앞줄은 톱니바퀴의 **아래에서 왼쪽까지** 이어지고
**[앱 종료]**는 그 부채꼴 한가운데의 더 바깥쪽에 놓입니다.

| 순서 | 이름 | 자리 | 누르면 |
|---|---|---|---|
| 1 | **집중 모드** | 앞줄 | 집중 모드 창 (244 × 252pt) |
| 2 | **캐릭터** | 앞줄 | 캐릭터 정보 창 (1042pt 폭, 화면 한가운데) |
| 3 | **오늘 할일** | 앞줄 | 오늘 할일 창 (300 × 336pt) |
| 4 | **행동** | 앞줄 | 행동 명령 창 (480 × 508pt) |
| 5 | **앱 종료** | 뒷줄 | **두 번 눌러야 꺼집니다.** 아래 설명을 보십시오 |

창을 여는 방식은 두 가지입니다.

- **[캐릭터]** — 버튼 5개가 모두 접히고, 창이 화면 한가운데 열립니다.
- **[집중 모드] · [오늘 할일] · [행동]** — 누른 버튼 하나만 남고 나머지가 접힙니다.
  창은 그 버튼에서 자라나듯 열립니다. 같은 버튼을 다시 누르면 전부 닫힙니다.

**[오늘 할일]** 버튼에는 아직 끝내지 않은 할일 개수가 작은 동그라미로 붙습니다.
10건이 넘으면 `9+`로 나옵니다. 할일이 없으면 이 동그라미도 없습니다.

### 3-1. [앱 종료] 버튼 — 두 번 눌러야 꺼집니다

한 번 누르면 **아직 꺼지지 않습니다.** 이름표가 `정말 종료?`로 바뀌고,
버튼 둘레에 남은 시간이 줄어드는 고리가 그려집니다.

| 그다음에 한 일 | 결과 |
|---|---|
| **3초 안에 같은 버튼을 한 번 더 누른다** | 앱이 꺼집니다. |
| 3초 동안 아무것도 안 한다 | **저절로 취소됩니다.** 이름표가 `앱 종료`로 돌아갑니다. |
| 커서를 다른 버튼 위로 옮긴다 | **그 자리에서 취소됩니다.** 3초를 기다리지 않아도 됩니다. |

- 실수로 한 번 눌렀다면 **커서를 옆 버튼으로 옮기십시오.** 그것이 가장 빠른 취소입니다.
- 저장은 끄기 전에 자동으로 끝납니다. 어느 방법으로 꺼도 내용이 사라지지 않습니다.

<!-- 캡처 요청 B: 실제 빌드에서 버튼 5개가 펼쳐진 상태. 톱니바퀴 + 앞줄 4개 + 뒷줄 [앱 종료] + 호버 이름표 1개가 함께 보이도록. -->
<!-- 캡처 요청 B2(신규): [앱 종료]를 한 번 누른 직후. 이름표가 `정말 종료?`이고 고리가 줄어드는 순간. -->

<!-- 리더에게 — 3절은 2026-09-05에 통째로 고쳤다. 이전 판의 「버튼 4개」는 2026-09-03에 거짓이 됐다
     (사용자 지시로 위성 [앱 종료]가 신설된 라운드). 근거:
       Interaction/GearRadialMenuWidget.cs
         ButtonCount = 5 / SatelliteButtonCount = 1 / ArcButtonCount = ButtonCount - SatelliteButtonCount = 4
         OrbitRadiusPoints = 111 / SatelliteOrbitRadiusPoints = 168 / ButtonAngleStepDegrees = 30
         호 스팬 = step x (ArcButtonCount - 1) = 90도  ← 버튼이 5개가 돼도 스팬은 90도 그대로다
         SatelliteAngleOffsetDegrees = 0 (기준각 축 위)
         ButtonNames = { 집중 모드, 캐릭터, 오늘 할일, 행동, 앱 종료 }
         QuitArmedLabel = "정말 종료?"  / 무장 해제 = TickQuitArm(3초 경과 또는 커서가 다른 버튼으로)
         제한 시간의 출처는 ActionCommandPopover.QuitConfirmSeconds = 3f (두 곳이 같은 상수를 본다)
     ★ 이 라운드의 자기 정정이다 — 2026-09-05 판 갱신 이력에도 「버튼 4개」가 남아 있었다. -->

---

## 4. 각 창이 하는 일

### 4-1. 캐릭터 정보 창

버튼 **[캐릭터]** 또는 단축키로 엽니다. 화면 한가운데에 열립니다.

- **왼쪽 칸** (항상 보입니다)
  - 캐릭터 이름 — 눌러서 바로 고칠 수 있습니다.
  - 이름 오른쪽의 작은 동그라미 2개 — 잉크색(검정 / 흰색)을 고릅니다.
  - 레벨과 칭호.
  - 초상화.
  - 지금 무엇을 하고 있는지 한 줄.
  - 막대 2개 — `STRESS`(스트레스), `EXP`(경험치).
  - 기록 4줄 — 근속 / 함께한 시간 / 보유 장비 / 활쏘기 명중.
- **오른쪽 칸** (탭으로 나뉩니다)

| 탭 | 내용 |
|---|---|
| **장비** | 모자 · 안경 · 넥타이 · 망토. 각 줄이 옆으로 미는 카드 목록입니다. |
| **외형** | 머리 · 이펙트 · 펫. 같은 방식입니다. |
| **보관함** | 가진 것과 할 줄 아는 것을 한 목록으로 봅니다. |
| **상점** | **지금은 비어 있습니다.** 누르면 `상점은 다음 업데이트에 들어옵니다.` 한 줄만 나옵니다. |

- 카드를 누르면 **고르기**만 됩니다. 착용과 해제는 **카드 아래쪽 버튼**으로 합니다.
  이렇게 나눈 이유는, 카드를 옆으로 밀다가 실수로 옷이 갈아입혀지는 일을 막기 위해서입니다.
- **창을 옮길 수 있습니다.** 제목 줄을 잡고 끄십시오. 화면 밖으로는 나가지 않습니다.
  옮긴 자리는 기억하지 않습니다. 다시 열면 화면 한가운데에서 시작합니다.
- 창 오른쪽 위에 **[설정]** 버튼과 **[✕]** 버튼이 있습니다.
  설정 창을 여는 가장 짧은 길은 이 **[설정]** 버튼입니다.

<!-- 캡처 요청 C: 실제 빌드에서 캐릭터 정보 창 전체. [장비] 탭이 선택된 상태. -->

<!-- 리더에게 — 장비 42종 조형·색 개편에 대한 이번 라운드 판정: **매뉴얼 갱신 불필요(지금은).**
     확인한 것:
       (1) 이름·카테고리·개수·요구 레벨이 **하나도 안 바뀌었다.** `git diff` 상 바뀐 것은
           .asset 12개의 좌표와 color 뿐이다(예: equip_head_crown 보조색 -> #C6443C).
           ⇒ 2장 2-1 표(처음부터 가진 7종) · 부록 「7 / 42」 · 요구 레벨 5~30 전부 그대로 유효하다.
       (2) 조형·색 작업은 **아직 커밋되지 않았다**(AccessoryHandoffPalette.cs 등이 untracked,
           Resources/Items/*.asset 12개가 M). 규칙 "지금 없는 것은 매뉴얼에도 없다"에 따라
           본문에 한 줄도 넣지 않았다.
     ★ 착지하면 **그때 반드시 한 줄이 필요해진다** — 사용자가 눈으로 보게 되는 사실이 하나 생기기
       때문이다: 「장비는 아이템마다 고유한 색으로 칠해지고, 잉크색(검정/흰색) 설정은 캐릭터의 선과
       장비의 **윤곽선**에만 걸린다」(docs/EQUIPMENT_PALETTE.md R-1 · R-2).
       지금 매뉴얼은 잉크색을 4-2 표에서 「검정과 흰색 중에서 고릅니다」로만 적고 있어서,
       "장비 색까지 바뀔 것"이라는 오해를 막는 문장이 없다.
       착지 라운드에 4-1과 4-2에 각각 한 줄씩 넣고 캡처 C를 재요청해 주십시오. -->

### 4-1-1. 카드 위쪽의 짧은 띠 — 등급입니다

장비 카드마다 위쪽에 **네 칸짜리 짧은 띠**가 있고, 카드 안에 낱말도 함께 적혀 있습니다.
**채워진 칸 수가 등급입니다.**

| 채워진 칸 | 낱말 | 42종 중 몇 개 |
|---|---|---|
| 1칸 | `일반` | 14 |
| 2칸 | `희귀` | 14 |
| 3칸 | `영웅` | 7 |
| 4칸 | `전설` | 7 |

**같은 칸 안에서 늦게 열리는 장비일수록 등급이 높습니다.** 카테고리마다 6종씩 있고,
요구 레벨이 낮은 것부터 `일반` 2개 · `희귀` 2개 · `영웅` 1개 · `전설` 1개입니다.

- 카드 **테두리 색**도 등급을 함께 말합니다.
- 다만 그 카드를 **고르거나 · 커서를 올리거나 · 착용 중**일 때는 테두리가 그 상태를 먼저 말합니다.
  그때도 **띠와 낱말은 그대로** 등급을 말합니다.
- **아직 못 얻은 카드에도 띠가 있습니다.** 무엇이 나중에 열리는지 미리 볼 수 있습니다.

<!-- 리더에게 — 4-1-1은 2026-09-05에 신설했다. 등급 표시는 2026-09-03에 착지했는데(사용자 지시
     *"카드 외곽선을 각 레벨별로 분류하는게 어때"*) **매뉴얼에 한 줄도 없었다.** 화면에 새로 생긴
     부품(리본 4칸 + 낱말 + 테두리)을 사용자가 뜻도 모른 채 보고 있었다. 근거(전부 커밋된 HEAD):
       Core/ItemRarity.cs        Common/Rare/Epic/Legendary = 리본 1/2/3/4칸
       Core/ItemCatalog.RarityName  일반 / 희귀 / 영웅 / 전설  (낱말의 유일한 출처)
       Core/ItemCatalog._rarityByRank  rank 0,1=일반 / 2,3=희귀 / 4=영웅 / 5=전설
         -> 슬롯당 2/2/1/1, 42종 전체 14/14/7/7. 같은 파일 문서가 이 숫자를 직접 적어 두었다.
       Core/ItemCatalog.RarityOfMember  순위는 **슬롯 안에서 requiredLevel 오름차순**
         (동점이면 itemIndex가 작은 쪽) -> 본문의 "늦게 열릴수록 등급이 높다"가 여기서 나온다.
       Interaction/CharacterInfoWindow.Cards.cs  테두리 우선순위
         선택 > 호버 > 착용 중 > 등급.  같은 파일이 "화면이 등급을 잃는 순간은 0"이라고 적어 뒀다.
       Interaction/UiChrome.RarityFilledCells / RarityRibbonHeight(4pt) / RarityCellGap(2pt)
     ★ **등급이 능력치를 바꾸는가는 적지 않았다.** 그런 배선을 찾지 못했고(스탯 모델에 Rarity 참조
        0건), "없다"고 단정하려면 양성 대조가 필요한데 이번 라운드 범위를 넘는다. 확인되면 한 줄
        추가하거나, 없다는 것이 확정되면 "보기 위한 표시입니다" 한 줄을 넣어 주십시오. -->

### 4-1-2. [보관함] 탭 — 오른쪽 끝 칸이 「부르는 방법」입니다

[보관함] 탭은 목록 하나입니다. 머리글 두 개가 목록을 두 부분으로 나눕니다.

| 머리글 | 내용 |
|---|---|
| `걸치는 것  (가진 수 / 42)` | 장비 42종. 처음에는 `7 / 42`로 시작합니다 (2장 2-1) |
| `할 줄 아는 것  (12)` | 캐릭터가 할 줄 아는 행동 12종 |

한 번에 20줄이 보이고 목록 전체는 56줄입니다. **행동 12종은 맨 아래에 있습니다.**
오른쪽 스크롤 버튼 **[▲] [▼]** 로 내려가십시오. 한 번 누르면 20줄씩 넘어갑니다.
**[▼]를 두 번 누르면 맨 아래입니다.**

각 줄의 **오른쪽 끝 칸**에 그 줄을 부르는 방법이 적혀 있습니다.
줄을 고르면 목록 아래 상세 칸의 제목에도 같은 말이 나옵니다.

| 오른쪽 끝에 적힌 말 | 뜻 |
|---|---|
| `⌃⌥⌘A` 같은 단축키 표기 (Windows: `Ctrl+Alt+Win+A`) | 그 단축키로 지금 부를 수 있습니다. 7절 표와 같은 키입니다 |
| `톱니 메뉴에서` | 단축키가 없습니다. 톱니바퀴 → 버튼으로 부릅니다. 지금 이 표시가 붙은 것은 **집중 모드** 하나입니다 (4-3) |
| `가끔 알아서` | 부르는 방법이 없습니다. 조건이 맞을 때만 캐릭터가 스스로 합니다 |
| `착용 중` · `보유` · `Lv.20에 열림` | 장비 줄에만 나옵니다 |

> **`가끔 알아서`가 붙은 네 줄은 지금 배포되는 기본값에서 저절로 나오지 않습니다.**
> 할일 알림 · 하드웨어 반응 · 바탕화면 정리 · 블랙홀 소환 네 가지입니다.
> 발동 확률이 0으로 정해져 있고, 그 값을 바꾸는 항목은 설정 창에 없습니다. 기다리지 않아도 됩니다.

★ **2026-09-05에 이 칸에서 단축키 표기 세 개가 없어졌습니다.**
집중 모드는 `톱니 메뉴에서`로, 할일 알림과 하드웨어 반응은 `가끔 알아서`로 바뀌었습니다.
없어진 세 조합은 **일반 배포판에서 동작한 적이 없습니다.**
예전 화면을 보고 그 키를 눌렀는데 아무 일도 없었다면, 그것이 정상 동작이었습니다.
지금 실제로 동작하는 단축키는 **7절 표 12개**가 전부입니다.

### 4-2. 설정 창

캐릭터 정보 창의 **[설정]** 버튼 또는 단축키로 엽니다. 크기는 720 × 560pt입니다.
설정 창은 화면 한가운데에 고정됩니다. 옮길 수 없습니다.

탭은 5개입니다 — **[일반] [캐릭터] [이벤트] [접근성 · 성능] [데이터]**.
내용이 들어 있는 탭은 **[일반]**과 **[캐릭터]** 두 개입니다.
나머지 세 탭은 비어 있고, **탭 이름 옆에 `준비 중`이 적혀 있습니다.**
누르기 전에 읽을 수 있습니다. 자세한 내용은 2장 5절에 있습니다.

**[일반] 탭**

| 항목 | 하는 일 |
|---|---|
| 전체화면 게임 감지 시 자동 숨김 | 켜면 캐릭터도 열린 창도 함께 사라집니다. 끄면 창이 막는 클릭까지 그대로 남습니다. |
| 지금 즉시 [숨기기] / [보이기] | ⚠️ **[숨기기]는 캐릭터 · 톱니바퀴 · 열린 창을 전부 치웁니다. 이 설정 창도 같은 순간에 닫힙니다.** 되돌리는 방법은 `⌃⌥⌘K` 하나이고, 앱을 다시 실행해도 풀립니다. **누르기 전에 3장을 읽으십시오.** 줄 오른쪽의 `⌃⌥⌘K` 표시는 버튼이 아니라 안내입니다. |
| 톱니 아이콘 | 끄면 톱니바퀴가 사라집니다. **끄기 전에 9-1 (4)를 읽으십시오.** |
| 톱니 위치 [처음 자리로] | 끌어서 옮긴 톱니바퀴를 화면 오른쪽 위로 되돌립니다. 옮긴 적이 없으면 회색이고 `아직 옮긴 적이 없어요.`가 적힙니다. |
| 로그인할 때 자동 실행 | **회색입니다.** `준비 중 — 이 기능은 다음 업데이트에 들어옵니다.` |

**[캐릭터] 탭**

| 항목 | 하는 일 |
|---|---|
| 캐릭터 크기 | 캐릭터를 크게 또는 작게 합니다. |
| 잉크색 | 검정과 흰색 중에서 고릅니다. |
| 포인트 컬러 (눈 · 윤곽선) | **회색입니다.** `준비 중 — 포인트 컬러 팔레트는 다음 업데이트에 들어옵니다.` |
| 말풍선 표시 | 끄면 말풍선을 그리지 않습니다. 아래 세 줄도 함께 잠깁니다. |
| 말투 (반말 / 존댓말) | **회색입니다.** `준비 중 — 말투 고르기는 다음 업데이트에 들어옵니다.` |
| 말풍선 글자 크기 | 말풍선 글자를 키우거나 줄입니다. |
| 대사 표시 시간 | `기본` / `길게` / `아주 길게` 중에서 고릅니다. |
| 잡담 빈도 | 0%로 두면 혼잣말을 하지 않습니다. |

- **바꾼 값은 즉시 저장됩니다.** 따로 저장 버튼을 누르지 않아도 됩니다.
- 창 아래쪽 오른쪽에 **[지금 종료]** 버튼이 있습니다. 6장을 보십시오.
- 내용이 길면 창 오른쪽에 세로 스크롤 버튼 **[▲] [▼]** 가 나타납니다.

<!-- 캡처 요청 D: 실제 빌드에서 설정 창 [일반] 탭 전체. 푸터의 [지금 종료]까지 함께 보이도록. -->

### 4-3. 집중 모드 창

버튼 **[집중 모드]**로 엽니다.

1. 시간을 고릅니다 — **15분 / 25분 / 50분**. 기본은 25분입니다.
2. `지켜보기`를 켜거나 끕니다. 끄면 순수한 타이머로만 시간을 잽니다.
3. 민감도를 고릅니다 — **관대 / 보통 / 예민**.
4. **[시작]**을 누릅니다.

시작하면 창이 남은 시간을 보여주는 원으로 바뀝니다. **[그만두기]**로 끝냅니다.

**전체화면 앱이나 게임을 켜면 이 창이 닫힙니다.** 세션은 없어지지 않습니다.
톱니바퀴 → **[집중 모드]**를 다시 누르면 진행 중인 세션이 그대로 보입니다.
전체화면 **앱**(게임이 아닌 경우)이라면 **전체화면인 채로도 다시 열 수 있습니다**(3장 3-1).

남은 시간은 두 경우가 다릅니다.

| 무엇을 켰는가 | 남은 시간 |
|---|---|
| 전체화면 **게임** (또는 `⌃⌥⌘K`로 직접 숨긴 동안) | **멈춥니다.** |
| 전체화면 **앱** (게임이 아닌 발표 · 회의 · 문서) | **계속 줄어듭니다.** |

전체화면 **게임**인 동안에는 창을 다시 열 수 없어 남은 시간을 확인할 수 없습니다.
전체화면 **앱**에서는 톱니바퀴로 다시 열어 확인할 수 있습니다. 3장 3절을 보십시오.

### 4-4. 오늘 할일 창

버튼 **[오늘 할일]**로 엽니다. 탭이 두 개 있습니다 — **[할일]**, **[완료함]**.

- 위쪽 칸에 적고 **[추가]**를 누르면 목록에 들어갑니다.
- 왼쪽 네모를 누르면 완료 표시가 됩니다.
- 오른쪽 **[✕]**를 누르면 `삭제할까요?`가 나옵니다. **[삭제]** 또는 **[취소]**를 고르십시오.
  3초 안에 고르지 않으면 저절로 취소됩니다.
- 목록은 **앱을 껐다 켜도 남습니다.**
- 완료한 일은 지우지 않고 [완료함] 탭에 모입니다.

<!-- 리더에게 — 바로 위 세 줄(✕ → 삭제할까요? · 네모 체크 · 목록 유지)은 **코드가 그렇게 하기로 되어
     있는 동작**을 적은 것이고, 실기 재현이 아니다. 지금 규명 중인 할 일 목록 결함 3건(✕ 무반응 /
     체크가 화면에 안 붙음 / 지운 항목 부활)이 이 세 줄과 **정확히 같은 자리**를 겨눈다.
     원인이 나오면 이 절이 첫 번째 갱신 대상이다 — 결함 자체는 지시대로 한 줄도 적지 않았다. -->

**메모 카드**

아직 끝내지 않은 할일이 **하나라도 있으면**, 화면 오른쪽 위에 작은 메모 카드가 함께 나타납니다.
이 카드에서도 네모를 눌러 완료 표시를 할 수 있습니다.

| 항목 | 값 |
|---|---|
| 폭 | 220pt |
| 가로 위치 | 화면 오른쪽 끝에서 16pt. **톱니바퀴와 겹치면 스스로 왼쪽으로 물러납니다**(2-3). 오른쪽에 도킹한 작업 표시줄·Dock도 비켜 갑니다(2-3-1) |
| 세로 위치 | 위쪽 띠 **아래로** 16pt. macOS 메뉴 막대가 있으면 49pt(2-3-1) |
| 사라지는 조건 | 남은 할일이 0건이 되면 저절로 사라집니다 |

카드 오른쪽 위에 **[숨기기]** 버튼이 있습니다.
**이 버튼을 누르면 앱을 끌 때까지 카드가 다시 나타나지 않습니다.**
다시 보이게 하는 버튼은 없습니다. 앱을 껐다 켜면 돌아옵니다.

> **이 카드는 톱니바퀴를 가리지 않고**(2-3), **메뉴 막대와 작업 표시줄도 가리지 않습니다**(2-3-1).

### 4-5. 행동 명령 창

버튼 **[행동]**으로 엽니다. 캐릭터에게 지금 무엇을 하라고 시키는 창입니다.

**혼자 노는 것**

| 이름 | 설명 |
|---|---|
| 말 걸기 | 지금 상태 그대로 한마디 합니다. |
| 활쏘기 | 과녁을 세우고 세 발 쏩니다. |
| 그라피티 | 빈 자리에 낙서했다 지웁니다. |

**남의 창으로 노는 것**

| 이름 | 설명 |
|---|---|
| 창 도둑 | 작은 창을 미는 시늉을 합니다. |
| 창 부수기 | 금 간 유리를 3초 덮어 보입니다. |

> **실제 창은 옮기지도 닫지도 않습니다.** 전부 그림 위 연출입니다.

- 지금 할 수 없는 명령은 회색으로 보입니다. 누르면 그 이유를 화면에 적어 줍니다.
- 명령을 내려도 **창은 닫히지 않습니다.** 창을 닫아도 시킨 일은 계속 진행됩니다.
- 캐릭터가 가출한 동안에는 창 위쪽에 **[돌아와!]** 버튼이 나타납니다.

> ★ **바뀐 점**: 이 창 아래쪽에 **[✕ 앱 종료]** 버튼이 있었습니다. **지금은 없습니다.**
> 앱을 끄는 버튼은 부채꼴의 **[앱 종료]**(3-1)로 옮겨졌습니다.
> 이 창은 **캐릭터에게 시키는 일**만 담습니다 — 앱을 끄는 것은 캐릭터에게 시키는 일이 아닙니다.

---

## 5. 창을 닫는 법

**모든 창은 오른쪽 위의 [✕] 버튼으로만 닫습니다.**

아래 방법은 **동작하지 않습니다.** 이것은 고장이 아니라 정해진 동작입니다.

| 해 보게 되는 것 | 실제 결과 |
|---|---|
| 창 바깥을 클릭한다 | 창이 닫히지 않습니다. 그 클릭은 아래에 있는 앱으로 갑니다. |
| `ESC`를 누른다 | 창이 닫히지 않습니다. |
| macOS에서 `⌘W`를 누른다 | **뒤에 있던 다른 앱의 창이 닫힙니다.** StickMate 창은 그대로 있습니다. |

이유는 StickMate 창의 구조에 있습니다. StickMate는 화면 전체를 덮는 투명한 판 한 장이고,
그 안에 창을 그림처럼 그립니다. 이 판은 키보드 입력을 받지 않습니다.
그래서 키보드로 누른 키는 전부 아래에 있는 앱으로 갑니다.

**저절로 닫히는 경우**

| 대상 | 조건 |
|---|---|
| 집중 모드 · 오늘 할일 · 행동 명령 창 | 창 위에서 **180초** 동안 아무 일도 없으면 닫힙니다. |
| 버튼 5개 (부채꼴) | **6초** 동안 아무 일도 없으면 접힙니다. |
| 캐릭터 정보 창 · 설정 창 | 시간으로 닫히지 않습니다. |

---

## 6. 앱을 끄는 법 — 세 가지

| 방법 | 어떻게 | 마우스 클릭 수 |
|---|---|---|
| **1. 단축키** | macOS `⌃⌥⌘Q` · Windows `Ctrl+Alt+Win+Q` | 0 |
| **2. 부채꼴 [앱 종료]** | 톱니바퀴 → 부채꼴의 **[앱 종료]** (3-1) | 3 |
| **3. 설정 창** | 톱니바퀴 → 버튼 **[캐릭터]** → 창 위 **[설정]** → 창 아래 **[지금 종료]** | 5 |

- 2번과 3번은 **두 번 눌러야** 꺼집니다. 처음 누르면 글자가 `정말 종료?`로 바뀝니다.
  3초 안에 한 번 더 누르면 꺼집니다. 3초가 지나면 원래대로 돌아갑니다.
- 어느 방법으로 꺼도 **저장된 내용은 사라지지 않습니다.**

> ★ **바뀐 점**: 예전에는 2번이 **행동 명령 창 아래의 [✕ 앱 종료]**였습니다.
> 그 버튼은 없어졌고, 부채꼴에 **[앱 종료]** 버튼이 새로 생겼습니다(3-1).
> 끄는 방법의 **개수는 그대로 세 가지**이고, 마우스로 가는 길이 두 클릭 짧아졌습니다.

---

## 7. 단축키 전체

단축키는 **어떤 앱을 쓰고 있어도** 동작합니다. StickMate 창을 먼저 클릭할 필요가 없습니다.
누름 키 세 개를 모두 누른 상태에서 마지막 글자를 누르십시오.

| macOS | Windows |
|---|---|
| `Control` + `Option` + `Command` + 글자 | `Ctrl` + `Alt` + `Windows 키` + 글자 |

| 글자 | 하는 일 |
|---|---|
| `Q` | **앱을 끕니다.** |
| `K` | **지금 숨기기 / 다시 보이기.** 사라졌을 때 되돌리는 키이기도 합니다. **3장을 읽으십시오.** |
| `I` | 캐릭터 정보 창을 열고 닫습니다. |
| `P` | 설정 창을 열고 닫습니다. |
| `B` | 말 걸기. |
| `A` | 활쏘기. |
| `G` | 그라피티. |
| `T` | 창 도둑. |
| `X` | 창 부수기. |
| `N` | 캐릭터가 가출한 동안에만 동작합니다. 돌아오라고 부릅니다. |
| `C` | 잉크색을 검정과 흰색 사이에서 바꿉니다. |
| `R` | 로데오 커서를 켜고 끕니다. 기본은 꺼짐입니다. |

> ★ **전체화면 앱(게임이 아닌 경우)을 쓰는 동안**에는 `I` **하나만** 듣지 않습니다.
> 창이 열렸다가 곧바로 닫힙니다. `P`는 **듣습니다** — 설정 창이 열리고 그대로 남습니다.
> `Q`(끄기)와 `K`(숨기기 / 보이기)도 그대로 동작합니다.
> 캐릭터 정보 창이 필요하면 **톱니바퀴 → [캐릭터]**로 가십시오. **3장 3-1**을 보십시오.
>
> 전체화면 **게임**에서는 위 목록이 전부 듣지 않습니다. `Q`와 `K`만 남습니다.

<!--
리더에게 — 2026-09-05 (2) 단축키 표 재감사. **표는 한 글자도 바뀌지 않았다.** 근거:

  (가) 출하 12개 = Interaction/AppControlDirector.TickHotkeys 에서 `dev` 곱이 **없는** 바인딩 전수
       q c r b g t x aKey iKey pKey kKey n  = 12개.  이 표와 1:1 일치.
  (나) 개발 게이트 5개(D H S J F)는 게이트가 닫히면 **조회조차 하지 않는다** — 표에 없는 것이 맞다.
  (다) 조합키 표기의 단일 정의처는 Core/ShortcutLabel (MacModifiers "⌃⌥⌘" / WindowsModifiers
       "Ctrl+Alt+Win+"). 이 장의 Windows 표기는 그 상수와 같다.
  (라) macOS OS 예약(ShortcutLabel.MacReservedActionKeys) = { 8 , . } 셋뿐이고 우리 12개와 겹치지 않는다.

★ 아직 **쓰지 않은 것 하나** — Windows 충돌 의심 목록(2026-09-05 M-6 조사, `ShortcutLabel`):
    WindowsReservedActionKeys  = { }          <- 확정 충돌 0건
    WindowsSuspectActionKeys   = { B C D F G H K Q R }
  선정 규칙은 "Microsoft 문서에 있는 조합에서 우리 수식자 3개 중 **정확히 하나만** 뺀 것".
  출하분에서 걸리는 것은 **B C G K Q R 여섯 개**다(예: Win+Alt+R = Game Bar 녹화 시작/중지 —
  오발동하면 사용자 디스크에 영상 파일이 생긴다. Win+Alt+K = 마이크 음소거. Win+Ctrl+Q = 빠른 지원).

  ⇒ **매뉴얼에는 한 줄도 쓰지 않았다.** 그 문서가 스스로 "확정이 아니라 **의심**이고 미확인"이라고
     못박고 있고(여분 수식자가 매칭을 막는지는 1차 출처에 없다), 지금 쓰면 사용자에게
     **행동으로 옮길 수 없는 불안**만 준다. 실패 모드도 "우리가 안 돈다"가 아니라 **"둘 다 돈다"**다.
  ⇒ 리더 판단 필요: Windows 실기 세션에서 위 6개를 눌러 **OS 쪽 부수 효과**를 관찰해 주십시오.
     하나라도 재현되면 7절에 「이 조합은 Windows 기능도 함께 동작합니다」 행을 넣고,
     0건이면 이 주석을 지웁니다. **그 전에는 어느 쪽도 쓰지 않습니다.**

★ 그리고 이 절의 마지막 주의 상자 문장 — *"전체화면 게임에서는 위 목록이 전부 듣지 않습니다"* —
  는 **좁혀야 할 여지가 있다.** 발동 경로가 막히는 것은 연출 계열(그라피티·창도둑·활쏘기 등,
  각 Director가 IsSuspended로 막는다)이고, `C`(잉크색)·`R`(로데오 토글)은 AppControlDirector가
  값을 그 자리에서 바꾼다 — **화면에 안 보일 뿐 상태는 바뀐다**(C는 즉시 저장까지 한다).
  실기로 확인한 뒤 고치는 것이 맞다고 판단해 이번 라운드에는 손대지 않았다.
-->

**로데오 커서란**: 켜 두면, 커서가 5초 넘게 멈춰 있고 캐릭터가 닿을 수 있는 거리에 있을 때
캐릭터가 커서 위에 올라탑니다. 커서를 빠르게 흔들면 떨어집니다.
`R`은 이 자동 동작을 켜고 끄는 스위치입니다. 지금 당장 올라타게 하는 명령이 아닙니다.

### 7-1. 앱 안에서 단축키를 읽을 수 있는 곳

단축키를 적어 둔 화면은 **두 곳**입니다 — 설정 창과 캐릭터 정보 창의 [보관함] 탭입니다.

**설정 창 안 네 군데**

| 어디 | 무엇이 적혀 있는가 |
|---|---|
| 창 맨 아래 종료 버튼 라벨 | `지금 종료 (⌃⌥⌘Q)` |
| [일반] 탭 · `지금 즉시` 행의 오른쪽 표시와 설명 | `⌃⌥⌘K` |
| [일반] 탭 · 톱니 아이콘을 껐을 때 나오는 경고 줄 | `⌃⌥⌘I` |
| 창 맨 아래 푸터 오른쪽 | `⌃⌥⌘P` |

**[보관함] 탭의 「할 줄 아는 것」 일곱 줄** (각 줄 오른쪽 끝 칸, 4-1-2)

| 줄 | 오른쪽 끝에 적힌 표기 |
|---|---|
| 활쏘기 | `⌃⌥⌘A` |
| 혼잣말 | `⌃⌥⌘B` |
| 그라피티 | `⌃⌥⌘G` |
| 창 도둑 | `⌃⌥⌘T` |
| 로데오 커서 | `⌃⌥⌘R` |
| 창 부수기 | `⌃⌥⌘X` |
| 가출 | `⌃⌥⌘N` |

**화면 어디에도 적혀 있지 않은 것은 `C`(잉크색 바꾸기) 하나뿐입니다.**
Windows에서는 위 표기가 전부 `Ctrl+Alt+Win+` 로 바뀌어 나옵니다.

★ 그리고 **두 곳 모두 톱니바퀴에서 출발합니다.** 설정 창은 톱니바퀴 또는 `⌃⌥⌘P`로,
[보관함] 탭은 톱니바퀴 → **[캐릭터]** 또는 `⌃⌥⌘I`로 엽니다.
캐릭터와 톱니바퀴가 함께 사라진 상태에서는 어느 쪽도 읽을 수 없습니다 —
그 경우는 **3장**을 보십시오.

<!--
리더 회신 ②에 대한 답 — 이 표가 12개인 근거(2026-09-02 재실측).

  Platform/IGlobalKeyStateService.cs 의 GlobalKey 열거값 = 20
    = 조합키 3 (Command / Option / Control) + 동작키 17

  동작키 17 중 5개(D H S J F)는 Interaction/AppControlDirector.TickHotkeys 에서
  `bool dev = StickMateDevTools.Enabled;` 를 곱해 읽는다. 게이트가 닫히면 조회조차 하지 않는다.
    17 − 5 = 12  ← 이 표

  N은 갈래가 둘이다. AppControlDirector.ForceRunaway 가 IsRunawayActive 면 소환(항상 동작),
  아니면 StickMateDevTools.Enabled 를 확인하고 닫혀 있으면 아무 일도 하지 않는다.
  그래서 이 표는 N을 "가출한 동안에만 동작한다"로 적었다 — 릴리스에서 참이다.

  ★ 2026-09-02 저녁 재확인 — 위에서 지적한 거짓 주석 2건이 고쳐졌다. 지금은 갈라진 숫자가 없다:
    · IGlobalKeyStateService.cs:10  "조합키 3개 + 동작키 17개"   (3 + 17 = 20)
    · IGlobalKeyStateService.cs:146 "GlobalKey에 열거된 20개 키"
    두 줄과 이 표(12)의 관계는 위 뺄셈 그대로다. 이 표는 바뀌지 않았다.
-->

<!--
리더에게 — K 항목의 근거(2026-09-02 저녁 재확인).
  이전 판에서 "GlobalKey.K 주석이 아직 「비어 있다(예약)」라고 말한다"고 적었는데,
  그 주석은 뒤집혔다. 지금은 이렇게 적혀 있다:
    "★ 이 자리를 다른 기능에 내주지 마라. 숨김 상태의 유일한 탈출구다."
  매뉴얼 3장이 그 문장과 같은 판단 위에 서 있다.
  배선 자체는 그대로다 — AppControlDirector: `else if (kRise) Invoke(ControlAction.UserHide, ...)`.

  7-1의 네 자리는 Interaction/SettingsWindow.cs 에서 직접 읽었다(모두 ShortcutLabel.Chord 경유):
    · :313  QuitLabelText          → 종료 버튼 라벨 Q
    · :1112 HideEscapeCaption      → 숨기기 행 캡션 K   (:1125 는 같은 행의 칩)
    · :1153 _gearWarnCaption       → 톱니 경고 캡션 I
    · :1486 footer "여는 방법"      → 푸터 P
  AppControlDirector 클래스 문서의 "앱 UI 어디에도 적혀 있지 않다"는 같은 네 자리로 정정돼 있다.
-->


---

## 8. 캐릭터를 직접 만지기

| 동작 | 결과 |
|---|---|
| 캐릭터를 왼쪽 버튼으로 누른다 | 집어 올립니다. |
| 누른 채로 움직인다 | 캐릭터가 따라옵니다. |
| 버튼을 놓는다 | 놓은 방향으로 던져집니다. |
| 캐릭터를 오른쪽 버튼으로 누른다 | 아무 일도 없습니다. 그 클릭은 아래 앱으로 갑니다. |

- 집을 수 있는 것은 캐릭터가 **가만히 서 있거나 걷는 중일 때**뿐입니다.
  다른 동작을 하는 중에는 집히지 않습니다.
- 캐릭터가 그려진 부분만 클릭을 받습니다. 그 밖의 화면은 전부 아래 앱으로 그대로 지나갑니다.

### 8-1. 잡으면 말합니다

캐릭터를 집어 올리면 말풍선이 한 줄 뜹니다. **어디를 잡았는지에 따라 다른 말을 합니다.**

| 잡은 자리 | 나올 수 있는 말 |
|---|---|
| **머리** | `머리 놔` · `거긴 머리야` + 아래 공통 4줄 |
| **다리** | `다리 놔` · `거긴 다리야` + 아래 공통 4줄 |
| **몸통** | 공통 4줄만 |
| 공통 4줄 | `야!` · `놔줘 놔줘` · `안 놔?` · `어딜 잡아` |

- 한 번 집을 때 **한 줄**입니다. 오래 들고 있어도 새 말이 이어지지 않습니다.
- 앞의 말이 아직 화면에 남아 있는 동안 다시 집으면 **말하지 않습니다.** 연달아 집었을 때
  말풍선이 겹치지 않게 하기 위해서입니다.
- 몸통을 잡았을 때 부위 이름을 말하지 않는 것은 의도한 것입니다.
  `몸통 놔`가 한국어로 어색하기 때문입니다.
- 어디를 잡았는지 확실하지 않으면 `잡혔다` 한 줄만 나옵니다.
- 설정 창 **[캐릭터]** 탭에서 **말풍선 표시**를 끄면 이 말도 나오지 않습니다.

<!-- 리더에게 — 8-1 근거: Dialogue/GrabReactionLines.cs.
     풀 9줄 = HeadLines 2 + LegLines 2 + AnyLines 4 + FallbackLines 1.
     구역 판정은 같은 파일 Classify(heightRatio, headRingBottomRatio, hipRatio)이고 경계는 상수가
     아니라 Core/StickmanMetrics가 프리팹에서 실측한 랜드마크(머리 링 아랫끝 / 고관절)를 인자로 받는다.
     "몸통은 부위를 말하지 않는다"는 같은 파일의 <지어내지 않은 것> 절에 사유가 적혀 있다.
     "한 번 집기 = 한 줄"은 DialogueIntent가 1회용 토큰이라 상태 도중에 새 대사를 만드는 경로가
     프로덕션에 없기 때문(같은 파일 <변주 축은 하나뿐이다> 절).
     쿨다운은 States/DragThrowState.TryStartGrabReaction — 다음 발화 허용 시각을 **직전 줄의 노출
     상한**으로 잡는다(고른 숫자가 아니다). 그래서 본문에 초를 적지 않았다.
     말풍선 끄기: Core/AppSettingsModel.ResolveDialogueBubbleEnabled 를 Dialogue/DialogueBubbleRenderer가
     그린다. -->

---

## 9. 문제 해결

### 9-1. 캐릭터가 안 보입니다

> ★ **급하면 3장으로 가십시오.** 3장은 이 상황만 다루는 장입니다.
> 지금 당장 되돌리는 키는 `⌃⌥⌘K`(Windows: `Ctrl+Alt+Win+K`)입니다.

원인은 네 가지입니다. 위에서부터 확인하십시오.

**(1) 전체화면 게임이 켜져 있습니다**

전체화면 **게임**을 감지하면 캐릭터도 톱니바퀴도 창도 전부 사라집니다.
게임을 끄면 돌아옵니다.

**(2) 전체화면 앱이 켜져 있습니다 (게임은 아닙니다)**

발표 화면, 회의, 문서, 브라우저 같은 앱이 전체화면이면 **창 · 버튼 · 메모 카드만** 사라집니다.
캐릭터와 톱니바퀴는 그대로 남습니다. 화면에는 이유가 적히지 않습니다.

★ **이때 톱니바퀴를 누르면 그대로 열립니다.** 전체화면을 풀지 않아도 됩니다.
버튼 5개 · 캐릭터 정보 창 · 설정 창 · 나머지 창 전부 열리고 그대로 남습니다.
`⌃⌥⌘P`도 듣습니다. **`⌃⌥⌘I` 하나만 아직 듣지 않습니다** — 그때는 톱니바퀴 → **[캐릭터]**로 가십시오.
자세한 내용과 이 동작을 끄는 방법은 **3장 3절**에 있습니다.

부른 창을 **전부 닫으면 0.5초 뒤에** 다시 걷힙니다. 톱니바퀴를 다시 누르면 다시 열립니다.

> ⚠️ **이 상태에서 `⌃⌥⌘K`를 누르지 마십시오.** 이 키는 이 상태를 풀지 못합니다.
> 창이 돌아오는 대신 **캐릭터까지 사라집니다.** **여기서 누를 것은 톱니바퀴입니다.**
> (톱니바퀴 자체는 남습니다. 이미 눌렀다면 `⌃⌥⌘K`를 한 번 더 누르십시오.)

**(3) 직접 숨기기를 눌렀습니다**

`⌃⌥⌘K`(Windows: `Ctrl+Alt+Win+K`)를 눌렀거나,
설정 창 [일반]의 **[숨기기]**를 눌렀습니다.

> ★ **사라지는 것은 캐릭터뿐입니다.** 톱니바퀴 · 열어 둔 창 · 부채꼴은 **그대로 남습니다.**
> 되돌리는 방법이 세 가지 있습니다.
>
> | 방법 | 어떻게 |
> |---|---|
> | 설정 창 | **[일반]** → `지금 즉시` 줄의 **[보이기]** |
> | 부채꼴 | 톱니바퀴 → **[캐릭터]** → **[설정]** → 위와 같은 줄 |
> | 단축키 | `⌃⌥⌘K`를 한 번 더 |
>
> 앱을 껐다 켜도 항상 보이는 상태로 시작합니다. 숨김은 저장되지 않습니다.
>
> ★ 설정 창 **[일반]**에서 숨기는 방법은 `지금 즉시` 줄의 **[숨기기]** 버튼 하나입니다.
> 그 줄 오른쪽의 `⌃⌥⌘K`는 버튼이 아니라 **안내**입니다. 자세한 것은 **3장 2-3**에 있습니다.
>
> ⚠️ **단, 전체화면 게임을 켜 둔 채로 이 키를 누르면 톱니바퀴도 함께 사라집니다.**
> 그때는 게임을 먼저 끄십시오. 게임을 끄면 톱니바퀴가 돌아오고, 캐릭터는 위 세 가지 중
> 하나로 되돌립니다. **3장 2-1**을 보십시오.

**(4) 톱니바퀴만 안 보입니다**

설정 창 [일반]의 **톱니 아이콘**이 꺼져 있습니다.
이 상태에서는 마우스로 창을 열 수 없습니다.
`⌃⌥⌘I`(Windows: `Ctrl+Alt+Win+I`)로 캐릭터 정보 창을 연 다음,
**[설정]** → [일반] → **톱니 아이콘**을 다시 켜십시오.

### 9-2. 단축키가 하나도 안 먹습니다

이 컴퓨터에서 키 상태를 읽을 수 없는 경우입니다.
이때는 아래 마우스 경로만 씁니다. 화면에는 이유가 적히지 않습니다.

이 경우 앱을 끄는 방법은 두 가지입니다.

- 톱니바퀴 → 부채꼴의 **[앱 종료]** → 3초 안에 한 번 더 (3-1)
- 톱니바퀴 → **[캐릭터]** → **[설정]** → **[지금 종료]**

### 9-3. 앱을 켠 직후 클릭이 이상합니다

앱을 켠 뒤 **약 5초 동안은** 화면 클릭이 StickMate로 갑니다.
5초가 지나면 캐릭터가 그려진 부분을 뺀 나머지가 모두 아래 앱으로 지나가게 바뀝니다.
5초만 기다리십시오.

### 9-4. 명령을 눌렀는데 회색이라 안 눌립니다

행동 명령 창에서 지금 할 수 없는 명령은 회색으로 보입니다.
눌러 보면 창이 그 자리에 이유를 적어 줍니다. 그 이유를 없앤 뒤 다시 누르십시오.

### 9-5. 창이 화면을 가립니다

- **캐릭터 정보 창**은 제목 줄을 잡고 옮길 수 있습니다.
- **설정 창**은 옮길 수 없습니다. **[✕]**로 닫으십시오.
- 창이 열려 있는 동안, **그 창 모양 그대로의 범위**만 클릭을 가져갑니다.
  창 밖은 한 픽셀도 가져가지 않습니다.

---

## 부록. 실측값

이 장에 나온 숫자입니다. 전부 코드에서 읽은 값입니다. 단위는 화면 포인트(pt)입니다.

| 항목 | 값 |
|---|---|
| 톱니바퀴 중심 — 화면 오른쪽 끝에서 (오른쪽 띠 없음) | 30 |
| 톱니바퀴 중심 — 화면 오른쪽 끝에서 (오른쪽 띠 48) | 67.82 |
| 톱니바퀴 중심 — 화면 위쪽 끝에서 (macOS 메뉴 막대 33) | 64.82 |
| 톱니바퀴 중심 — 화면 위쪽 끝에서 (Windows 상단 도킹 40) | 71.82 |
| 톱니바퀴 중심 — 화면 위쪽 끝에서 (위쪽 띠 없음) | 31.82 |
| 톱니바퀴 지름 (보이는 크기) | 29.64 |
| 톱니바퀴 누를 수 있는 범위 | 중심을 가운데에 둔 정사각형. 한 변의 절반이 19.82 |
| 톱니바퀴 — 화면 오른쪽 끝에서 차지하는 구간 (오른쪽 띠 없음) | 10.18 ~ 49.82 |
| 톱니바퀴 — 화면 위쪽 끝에서 차지하는 구간 (macOS 메뉴 막대 33) | 45.00 ~ 84.64 |
| 톱니바퀴가 OS 예약 띠 안에 놓였을 때 | 띠를 벗어나는 최소량만큼 자동으로 옮겨집니다 |
| 메모 카드 폭 | 220 |
| 메모 카드 — 톱니바퀴와 안 겹칠 때, 화면 오른쪽 끝에서 | 16 ~ 236 |
| 메모 카드 — 톱니바퀴가 기본 자리일 때, 화면 오른쪽 끝에서 | 57.82 ~ 277.82 |
| 메모 카드 — 오른쪽 띠가 48일 때, 화면 오른쪽 끝에서 | 64부터 |
| 메모 카드와 톱니바퀴 사이에 남는 간격 | 8 |
| 메모 카드 — 화면 위쪽 끝에서 시작하는 지점 (macOS 메뉴 막대 33) | 49 |
| 메모 카드 — 화면 위쪽 끝에서 시작하는 지점 (위쪽 띠 없음) | 16 |
| 메모 카드가 메뉴 막대를 덮는 양 | **0** |
| 메모 카드 높이 (할일 1건일 때) | 80 |
| 톱니바퀴 평소 불투명도 | 0.80 |
| 톱니바퀴 커서 올렸을 때 불투명도 | 0.95 |
| 톱니바퀴 끌기 판정 — 누른 시간 | 0.4초 이상 |
| 톱니바퀴 끌기 판정 — 끈 거리 | 6 이상 |
| 두 조건의 관계 | **그리고**(둘 다 넘겨야 합니다) |
| 끄는 동안 톱니바퀴 배율 | 1.12 |
| 끄는 동안 톱니바퀴 불투명도 | 0.55 |
| 부채꼴 버튼 개수 | **5** (앞줄 4 + 뒷줄 [앱 종료] 1) |
| 버튼 지름 | 44 |
| 톱니바퀴 중심에서 **앞줄** 버튼 중심까지 | 111 |
| 톱니바퀴 중심에서 **[앱 종료]** 중심까지 | 168 |
| 버튼 사이 각도 | 30° |
| 앞줄 4개가 차지하는 각도 | 90° |
| [앱 종료]의 각도 | 0° (부채꼴 한가운데 축 위) |
| 버튼 5개가 다 펼쳐지는 시간 | 0.30초 |
| 버튼 자동 접힘 | 6초 |
| 처음 안내가 떠 있는 동안의 버튼 자동 접힘 | **멈춥니다** (안내가 사라진 뒤 6초부터 다시 셉니다) |
| 처음 안내를 읽을 수 있는 총 시간 | 10.5초 (4.5 + 6) |
| 캐릭터 정보 창 폭 | 1042 |
| 캐릭터 정보 창 탭 개수 | 4 (그중 내용이 있는 탭 3) |
| 설정 창 탭 개수 | 5 (그중 내용이 있는 탭 2) |
| 앱 안에서 단축키를 읽을 수 있는 자리 | 11 (설정 창 4 + [보관함] 탭 7줄) |
| 화면 어디에도 적히지 않은 단축키 | 1 (`C`) |
| [보관함] 목록 줄 수 | 56 (장비 42 + 행동 12 + 머리글 2) |
| [보관함] 한 번에 보이는 줄 수 | 20 |
| 「할 줄 아는 것」 행동 수 | 12 (단축키 7 · 톱니 메뉴 1 · 가끔 알아서 4) |
| macOS Dock 아이콘 | 없습니다 |
| Windows 작업 표시줄 버튼 | **있습니다** |
| 앱 전환기(`⌘Tab` / `Alt+Tab`) 항목 | 양쪽 다 없습니다 |
| 설정 창 | 720 × 560 |
| 집중 모드 창 | 244 × 252 (진행 중에는 244 × 224) |
| 오늘 할일 창 | 300 × 336 |
| 행동 명령 창 | 480 × 508 |
| 팝오버 3종 자동 닫힘 | 180초 |
| 종료 두 번 누르기 제한 시간 | 3초 |
| 종료 확인을 바로 취소하는 법 | 커서를 다른 버튼으로 옮깁니다 |
| 앱을 끄는 방법 | 3가지 (단축키 · 부채꼴 [앱 종료] · 설정 창 [지금 종료]) |
| 직접 숨기기(`⌃⌥⌘K`)로 사라지는 것 | **캐릭터뿐**입니다. 톱니바퀴 · 창 · 메모 카드는 남습니다 |
| 앱 시작 직후 클릭이 지나가지 않는 시간 | 5초 |
| 설정 창이 전체화면 뒤에 스스로 돌아오는 제한 시간 | 20초 (전체화면 **게임** · 직접 숨김에서만) |
| 전체화면 뒤에 스스로 돌아오는 창 | 설정 창 하나뿐입니다 |
| 전체화면 **앱**(게임 아님)에서 톱니바퀴 | 남습니다. **누르면 그대로 열립니다** |
| 전체화면 **앱**(게임 아님)에서 `⌃⌥⌘P` | 듣습니다 |
| 전체화면 **앱**(게임 아님)에서 `⌃⌥⌘I` | 듣지 않습니다 |
| 부른 창을 전부 닫은 뒤 다시 걷히기까지 | 0.5초 |
| 전체화면 **앱**(게임 아님)에서 집중 모드의 남은 시간 | 계속 줄어듭니다 |
| 전체화면 **게임**에서 집중 모드의 남은 시간 | 멈춥니다 |
| 잡힘 대사 전체 줄 수 | 9 (머리 2 · 다리 2 · 공통 4 · 부위 불명 1) |
| 한 번 집을 때 나오는 대사 | 1줄 |

---

## 갱신 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-05 (2) | **정정 6건 + 신설 1건 — 이 장이 이틀 동안 틀린 개수를 말하고 있었다.** ⓪ **4-1-1 신설 (등급 표시)** — 카드의 리본 4칸 · 낱말(`일반`/`희귀`/`영웅`/`전설`) · 테두리 색이 무엇을 뜻하는지 적었다. 2026-09-03에 착지한 화면 부품인데 매뉴얼에 한 줄도 없었다. 기존 4-1-1은 **4-1-2**로 밀렸다(참조 1곳 함께 갱신). ① **3절 전면 재작성: 「버튼 4개」 → 「버튼 5개」.** 2026-09-03에 사용자 지시로 부채꼴에 **[앱 종료]**가 신설됐는데(`GearRadialMenuWidget.ButtonCount` 4 → 5) 이 장은 계속 4개라고 적고 있었다. 앞줄 4개(궤도 111pt · 스팬 90°)와 뒷줄 [앱 종료](궤도 168pt · 축 위 0°)의 두 줄 구조, **3-1 신설**(두 번 눌러야 꺼짐 · 3초 · 커서를 옆 버튼으로 옮기면 즉시 취소)을 적었다. ② **4-5 정정** — 행동 명령 창 아래의 **[✕ 앱 종료] 칩은 삭제됐다**(같은 라운드). 이 장은 「6장을 보십시오」로 그 칩을 계속 안내하고 있었다. ③ **6절 재작성** — 끄는 방법 세 가지 중 2번이 「행동 명령 창」에서 「부채꼴 [앱 종료]」로 바뀌었다. 클릭 수를 함께 적었다(3 클릭). ④ **9-1 (3) 전면 재작성 — 이전 판은 거짓이었다.** *"숨기는 동안에는 톱니바퀴도 메모 카드도 함께 사라집니다 · 마우스로 되돌릴 방법이 없습니다"*는 2026-09-03부터 참이 아니다. 지금 `⌃⌥⌘K`는 **캐릭터만** 가리고 톱니바퀴·창·메모 카드를 남긴다(`StickmanAgent.HidesScreenSurfaces`, 사용자 확정 *"캐릭만 가리고"*). 되돌리는 길이 셋으로 늘었다. **전체화면 게임 중에 누른 경우만 예외**로 남겨 따로 적었다. ⑤ 9-1 (2)의 경고 상자도 같은 이유로 정정(톱니바퀴는 남는다). ⑥ 부록 12줄 갱신 — 버튼 개수 · 두 궤도 · 스팬 · 펼침 시간 0.301 → **0.30**초(5개 기준 재계산). |
| 2026-09-05 | **정정 3건 (유령 단축키 제거 반영).** ① **4-1-1 신설** — [보관함] 탭 오른쪽 끝 칸이 무엇을 뜻하는지 적었다(단축키 표기 / `톱니 메뉴에서` / `가끔 알아서` / 장비 3종). 이 칸에서 **집중 모드 `F` · 할일 알림 `J` · 하드웨어 반응 `H` 표기가 삭제**됐고(`Core/ItemCatalog.cs`, 2026-09-05), 세 조합은 개발 게이트 뒤라 **일반 배포판에서 동작한 적이 없다.** 매뉴얼 7절 표(12개)는 처음부터 그 셋을 빼고 있었으므로 **표는 바뀌지 않았다.** 함께: `가끔 알아서` 네 줄은 배포 기본값에서 확률 0이라 저절로 나오지 않는다는 사실을 적었다(`DefaultStickConfig.asset` — `todoReminderChance: 0` · `enableAutonomousHardwareReactions: 0` · `desktopTidyChance: 0` · `blackholeChance: 0`). ② **7-1 자기 정정** — 이전 판의 *"단축키를 적어 둔 화면은 설정 창 하나뿐"* · *"나머지 8개는 화면 어디에도 적혀 있지 않다"*는 **거짓이었다.** [보관함] 탭이 `A B G T R X N` 일곱 개를 줄마다 보여 준다. 화면에 없는 것은 `C` **하나**다. ③ 부록 5줄 갱신(자리 4 → 11 등). |
| 2026-09-03 | **정정 8건 (밤사이 변경 반영).** ① **2-1 재작성 + 자기 정정** — 이전 판의 *"톱니바퀴 중심은 화면 위쪽 끝에서 58pt"*는 **거짓이었다.** 그 상수(`MarginTopPoints = 58`)는 폐기됐고, 지금은 OS에 물어본 위쪽 띠 두께에서 유도한다(macOS 64.8 / Windows 상단 도킹 71.8 / 띠 없음 31.8). **가로도 같은 규칙이 생겼다** — 오른쪽에 도킹한 작업 표시줄·Dock을 비켜 간다. ② **2-3-1 전면 재작성** — 이전 판은 *"macOS에서 카드가 메뉴 막대 아래 절반을 덮는다"*였고 당시엔 참이었다. **지금은 0pt다.** 카드도 위쪽 띠 아래로 16pt에 붙고, 오른쪽 띠도 비켜 간다. ③ **2-4에 자동 이동 추가** — 톱니바퀴를 예약 띠 안에 놓으면 최소량만큼 밀려난다. ④ **2-5 보강** — 처음 안내가 떠 있는 동안 버튼 4개가 **접히지 않는다**(읽을 시간 6.3초 → 10.5초). ⑤ **7절 주의 상자 정정** — 전체화면 앱에서 `P`는 **듣는다.** 안 듣는 것은 `I` 하나뿐이다. ⑥ **8-1 신설** — 캐릭터를 집으면 말한다(9줄, 잡은 자리에 따라 다름). ⑦ **4-2 [일반] 표에서 `숨기기 / 보이기 단축키` 행 삭제** — 그 토글이 실제로 없어졌다. ⑧ 4-3 · 4-4 · 7-1 · 9-1 (2)(3) · 부록 20여 줄 갱신. |
| 2026-09-02 | 1장 첫 작성. 코드에서 실측한 값만 기재. 화면 캡처 4곳(A~D)은 실제 빌드 대기. |
| 2026-09-02 | **정정 4건 (전체화면 등급 반영).** ① **4-3 재작성** — 예전 판의 *"전체화면 앱을 쓰는 동안에는 남은 시간이 줄지 않습니다 · 창이 그 사실을 한 줄로 알려 줍니다"*는 **거짓이었다.** 실제로는 전체화면이면 창 자체가 닫혀 그 줄을 볼 수 없고, 남은 시간은 **게임일 때만** 멈춘다(게임이 아닌 전체화면 앱에서는 계속 준다). ② **9-1 (2) 보강** — 그 상태에서 **톱니바퀴를 눌러도 아무 창도 안 열린다**는 사실과 `⌃⌥⌘K` 금지 경고를 추가하고 3장 3절로 연결. ③ **7절에 주의 상자** — 전체화면 앱 중에는 `I`·`P`가 듣지 않고 `Q`·`K`는 듣는다. ④ 부록 5줄 추가. |
| 2026-09-02 | **정정 7건 (오후 이후 변경 반영).** ① **2-3을 통째로 다시 씀** — 메모 카드가 톱니바퀴를 **비켜 간다**(카드 오른쪽 끝이 톱니 왼쪽에서 8pt 더 왼쪽으로). 이전 판의 "가린다 / 되찾는 방법 3가지"는 더 이상 참이 아니라 삭제. ② **2-3-1 신설** — macOS에서 카드가 메뉴 막대 아래 절반을 덮고 그 구간의 클릭을 가져간다(카드 세로 인셋 16pt 고정, 예약 띠 계산 없음). ③ **2-4 재작성** — 끌기 판정이 **또는 → 그리고**로 바뀌었다(0.4초 **그리고** 6pt). 4pt였던 거리도 6pt로 정정. **꾹 눌러도 버튼이 열린다**를 명시. ④ **2-4-1 신설** — 설정 [일반] → 화면 위 UI → 톱니 위치 **[처음 자리로]**. ⑤ 4-2 [일반] 표에 톱니 위치 행 추가, 숨기기 두 행에 경고 강화. ⑥ **7-1 신설** — 단축키가 화면에 적힌 **네 자리**. 이전 판의 "화면 어디에도 적혀 있지 않다"는 거짓이었다. ⑦ 9-1을 3장으로 연결. 부록 8줄 갱신. |
| 2026-09-02 | **정정 4건.** ① 2-3 신설 — 메모 카드가 톱니바퀴를 가리는 사실과 되찾는 방법 3가지(이전 2-3·2-4는 2-4·2-5로 밀림). ② 캐릭터 정보 창에 **[상점]** 탭을 추가(비어 있음). ③ 설정 창의 탭 5개를 전부 적고, `준비 중` 표시 위치를 명시. **[일반]**·**[캐릭터]** 표에 회색 항목 3줄을 추가. ④ 4-4의 메모 카드 설명 보강 — **[숨기기]**는 앱을 끌 때까지 유지된다는 사실 추가. 부록에 겹침 좌표 6줄·탭 개수 2줄 추가. |
