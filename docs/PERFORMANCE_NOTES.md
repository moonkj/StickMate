# StickMate 성능 관측 노트 — 2026-09-01 심야 (교란 제거 라운드)

담당: perf-doc (Teammate4) · 대상: **macOS만**(사용자 지시 "윈도우는 일단 미루고 맥만")
이 라운드에서 **프로덕션 코드는 한 줄도 고치지 않았다.** 측정과 판정만 했다.
고칠 것은 8절 "리더에게 올리는 조치"에 위치와 방법까지 적어 두었다.

---

## 0. 네 줄 요약

1. **GPU 3~5배는 새 빌드의 회귀가 아니다.** 같은 바이너리 한 번의 연속 실행 안에서
   1.76ms와 0.38ms가 **둘 다** 나왔다. 조용한 구간의 새 빌드(0.38~0.42ms)는 옛 빌드로
   보고된 0.36~0.39ms와 구별되지 않는다. **오늘 밤 렌더 경로 변경(필렛 10→9, 펫 무릎,
   장비 82도형, 말풍선 외곽선)은 원인이 아니다.**
2. **원인은 앱 안이 아니라 기계 전체의 동시 부하다.** 구간 16개 중
   **StickMate 인스턴스가 1대이고 동시 빌드가 없던 4구간만 0.38~0.42**, 나머지 12구간은
   전부 0.77~2.24다. 겹침이 없다. 앞 담당이 지목한 "창 여닫음"과 리더가 걱정한 "등급 분포"는
   **둘 다 반증**됐고, 내가 중간에 세웠던 "워밍업" 가설도 **반증**됐다(3-5절).
   → **측정 규약을 3-7절에 못박았다: 인스턴스 2대 이상이면 성능 수치는 무효다.**
3. **폰트 아틀라스는 누수가 아니라 "글리프 포화"다.** 크기는 **끝까지 256×512**로 안 커지고,
   재구성은 **UI 표면을 처음 열 때만** 일어나 **8회**에서 멈춘다(앞선 관측의 8회가 재현됐다). 같은 조작이 10분 뒤엔 0회다.
   재구성 1회의 자체 비용은 **1ms 미만**이고 100ms 스파이크는 **한 번도 동반하지 않았다**
   (8회 재구성 : 0회 스파이크). **사용자 신고와 연결되지 않는다.**
4. **관측이 잘리는 진짜 원인은 프로세스 교체가 아니라 로그 삭제였다** — `driver.sh start`의
   `: > "$LOG_FILE"`. 잘려도 이어 붙는 수집기를 만들어 **지금 돌리고 있고**(5-2절),
   근본 수정은 한 줄이다(5-5절).

그리고 이 라운드에서 **계측 자체의 거짓말 2건**을 찾았다(7절). 둘 다 "갈수록 느려진다"를
로그가 **없는데도 있다고** 말하게 만드는 종류다.

## 1. 측정 환경과 계보 (숫자의 출처)

| 인스턴스 | PID | 시작 | 로그 | 비고 |
|---|---|---|---|---|
| A(옛 관측 대상) | 37699 | 22:49:28 | `/tmp/stickmate-run/stickmate.log` | 23:26에 **다른 담당 리빌드로 소실**. 요약 7건만 추출해 둠 |
| B(현행) | 67542 | 23:26:23 | 같은 경로(A를 덮어씀) | |
| C(재현 페르소나) | 68627 | 23:27:28 | `/tmp/stickmate-jaehyun/stickmate.log` | 다른 담당 소유. 읽기만 함 |

기기: Apple M2 Pro / Metal / VRAM 12124MB / 백버퍼 3024×1964 / MSAA **실측 4x** /
디스플레이 120Hz, vSyncCount=2 → 기대 60fps.

★ **A의 원본 로그는 이 조사 도중에 사라졌다.** `driver.sh start`가 `: > "$LOG_FILE"`로
공용 로그를 **자르기** 때문이다. 오늘 밤에만 22:31 / 22:49 / 23:26 세 번 잘렸다.
그래서 5절(장기 관측 설계)이 이 라운드의 산출물에 포함된다.

---

## 2. 임무 1 — "같은 조건인가"를 먼저 증명한다

리더 지시: *"등급 분포(활성/정적/정지 %)가 다르면 GPU ms 평균은 자동으로 달라진다.
등급별로 나눠서 비교해야 한다."*

**결론: 이 지표에 한해 등급 분포는 평균을 교란하지 않는다. 표본 모집단이 원래 균질하기 때문이다.**

### 2-1. 근거 — GPU 표본은 `renderFrameInterval == 1`인 프레임에서만 나온다

`Platform/FramePacing.cs`의 `RenderDiagnostics.SampleOngoingGpu()`는 렌더 가능한 프레임
8장마다 1회 표본을 뜬다. 그렇다면 정적(1/2)·정지(1/4) 등급도 비례해서 표본을 내야 한다.
**실제로는 내지 않는다.** 관측된 표본 수를 "활성(+전체화면숨김) 등급 프레임 ÷ 8"으로 예측하면:

| 인스턴스/구간 | 활성% | 숨김% | 루프Hz | 길이 | 예측 표본 | 실측 표본 | 실측/예측 |
|---|---|---|---|---|---|---|---|
| A #1 | 54 | 0 | 56.8 | 60s | 230 | 217 | 0.94 |
| A #2 | 62 | 0 | 59.2 | 300s | 1376 | 1374 | **1.00** |
| A #3 | 68 | 0 | 59.3 | 300s | 1512 | 1506 | **1.00** |
| A #4 | 46 | 0 | 59.3 | 300s | 1023 | 1008 | 0.99 |
| A #5 | 51 | 0 | 59.3 | 300s | 1134 | 1107 | 0.98 |
| A #6 | 51 | 0 | 59.3 | 300s | 1134 | 1118 | 0.99 |
| A #7 | 41 | 0 | 59.3 | 300s | 912 | 897 | 0.98 |
| B #1 | 77 | 0 | 56.8 | 60s | 328 | 322 | 0.98 |
| B #2 | 83 | **9** | 54.4 | 300s | 1877 | 1858 | **0.99** |
| B #3 | 65 | 0 | 59.6 | 300s | 1453 | 1447 | **1.00** |
| B #4 | 61 | 0 | 59.4 | 300s | 1359 | 1342 | 0.99 |
| C #1 | 59 | 0 | 57.2 | 60s | 253 | 243 | 0.96 |
| C #2 | 71 | **9** | 54.5 | 300s | 1635 | 1604 | 0.98 |

**루프가 정상 속도(54Hz 이상)인 13/13 구간에서 0.94~1.00.**
(★ 제외 2건: C #3·#4는 루프가 **12.1Hz / 24.9Hz**까지 눌린 구간이라 비율이 1.20 / 0.78로 흩어진다. 그 정도로 눌리면 등급 체류 표본 자체가 거칠어져 이 검증에 쓸 수 없다 — **제외 사유를 적어 남긴다.**)

비교로, "활성 + 정적/2 + 정지/4" 모델로 예측하면 같은 자료에서
1467 대 1107(오차 +32%)처럼 크게 어긋난다. → **정적·정지 등급은 표본을 0개 낸다.**

원인(코드 근거): `SampleOngoingGpu()`는 `FrameTimingManager.GetLatestTimings(1, …)`가
돌려주는 **가장 최근 완료된** 타이밍을 읽고 `gpu <= 0.0`이면 버린다. `renderFrameInterval`이
2/4면 최근 몇 프레임 중 대부분이 건너뛴 프레임이라 그 자리에서 0이 나오고 전부 버려진다.

### 2-2. 그래서 비교가 정당한 이유

표본 하나하나가 **전부 활성 등급(또는 숨김 등급)의 프레임**이다. 등급 분포는 표본의
**개수**만 바꾸고 **성격**은 바꾸지 않는다. → 평균 ms는 등급 믹스에 대해 1차적으로 불편(unbiased)하다.

**직접 검증**: A #2와 B #2는 등급 믹스가 완전히 다른데(활성 62%/숨김 0% vs 활성 83%/숨김 9%)
GPU ms는 **1.40 vs 1.43** — 2% 차이. 등급 믹스가 21%p 움직여도 평균은 안 움직였다.

★ 단, **절전 등급의 프레임 비용은 이 계측으로 영영 볼 수 없다**(7-1절, 조치 대상).

---

## 3. 임무 1 — 재측정 결과와 판정

### 3-1. 인스턴스 A 전 구간 (한 번의 연속 실행, **같은 바이너리**)

시각은 60초 심장박동 `[발판리포트]`를 자로 삼아 역산했다(앱 로그에 타임스탬프가 없다).

| # | 가동 구간 | GPU ms | 최악 | 표본 | 활성% | 창 열려 있던 시간 | 화면의 타 앱 |
|---|---|---|---|---|---|---|---|
| 1 | 0~59s | **1.76** | 3.74 | 217 | 54 | 5.4s (설정창) | — |
| 2 | 59~359s | **1.40** | 4.89 | 1374 | 62 | ~160s | swift-frontend |
| 3 | 355~655s | **1.73** | 4.76 | 1506 | 68 | ~194s | swift-frontend |
| 4 | 643~943s | **0.39** | 3.06 | 1008 | 46 | 9s | 없음 |
| 5 | 942~1242s | **0.38** | 2.93 | 1107 | 51 | **0s** | 없음 |
| 6 | 1254~1554s | **0.42** | 3.95 | 1118 | 51 | **51s (정보창)** | 없음 |
| 7 | 1547~1847s | **0.39** | 2.92 | 897 | 41 | **0s** | 없음 |

### 3-2. 판정 1 — **빌드 회귀가 아니다** (확정)

- 같은 실행, 같은 바이너리 안에서 **1.76ms와 0.38ms가 둘 다** 나왔다.
- 조용한 구간 3개(#5·#7 창 0초, #4 창 9초)가 **0.38 / 0.39 / 0.39ms**로 일치한다.
  이는 보고된 옛 빌드 값 **0.36~0.39ms**와 구별되지 않는다.
- → **오늘 밤 렌더 경로 변경(팔다리 필렛 폴리라인 10→9점, 펫 무릎 4마디, 장비 도형 82개,
  말풍선 외곽선 비율)은 정상 상태 GPU 비용을 올리지 않았다.**

**기전 근거(왜 올라갈 수가 없는가).** 콜드스타트 진단이 이 앱의 GPU 비용 구조를 그대로 찍는다:

```
추정 컬러버퍼 90.6MB, 프레임당 MSAA resolve 트래픽 113.3MB, 프레임당 표면 복사 45.3MB
-> 60fps 기준 9.29GB/s
```

**프레임당 158.6MB의 고정 대역폭**(3024×1964 × MSAA 4x 전체화면 오버레이)이 비용의 몸통이다.
이 값은 **정점 수·도형 수와 무관**하다 — 화면 전체를 한 번 지우고 한 번 리졸브하고 한 번 넘기는
비용이라, 선을 몇 개 더 긋는지는 여기에 안 잡힌다. 스틱 도형의 정점 수백 개는 측정 한계 아래다.

자릿수 검산: 158.6MB ÷ M2 Pro 통합 메모리 대역폭(약 200GB/s) = **0.79ms**. 실측 정상 상태는
**0.38ms**로 그보다 **낮다.** ★ 이건 어긋난 게 아니라 **맞는 방향**이다 — Apple GPU는 TBDR이라
MSAA 리졸브를 타일 메모리 안에서 끝내므로 위 산술 추정(113.3MB)은 **상한**이다.
즉 0.38ms는 "이 앱이 실제로는 화면 리졸브·프레젠트 고정분만 쓰고 있다"는 뜻이고,
**도형을 늘려서 올릴 수 있는 종류의 숫자가 아니다.**

### 3-3. 판정 2 — **"창을 여닫아서"도 아니다** (반증)

앞 담당이 남긴 교란 후보였다. 자료가 반증한다.

- **#6**: 이 앱에서 가장 무거운 표면인 **정보창(880×861 + 초상화 RenderTexture 2배 슈퍼샘플
  매 프레임 렌더)**이 300초 중 **51초** 떠 있었다. 창이 뜨면 UI 홀드로 등급이 활성에 묶이므로
  그 51초는 **활성 시간 153초의 33%**를 차지한다. 그런데 GPU는 **0.42ms** — 완전 조용한
  0.385ms 대비 **+0.035ms**.
  → **창 1장의 GPU 비용 상한 = 0.035 / 0.33 ≈ 0.11ms/프레임.**
- #2·#3의 초과분은 **+1.0 / +1.35ms**다. 창 모델로는 **10배 이상 설명이 안 된다.**
- 방향도 어긋난다. #2·#3에 오래 떠 있던 것은 **행동 팝오버(480×560)**로 정보창보다 **작다**.
  창 면적이 원인이라면 #6이 가장 높아야 한다. 아니었다.

### 3-4. 판정 3 — 자료 전체를 가르는 유일한 변수는 **기계 전체의 동시 부하**다

인스턴스 3대에서 모은 300/60초 구간 **16개 전부**를 놓고 본다.
(창 비율 = 그 구간에서 정보창/설정창/팝오버 중 하나라도 떠 있던 시간의 비율)

| 구간 | 벽시계 | 활성% | 창% | **GPU ms** | 그때 기계 상태 |
|---|---|---|---|---|---|
| A1 | 22:49~22:50 | 54 | 9 | 1.76 | swift-frontend 컴파일 창 화면에 있음 |
| A2 | 22:50~22:55 | 62 | 53 | 1.40 | 〃 |
| A3 | 22:55~23:00 | 68 | 65 | 1.73 | 〃 |
| **A4** | 23:00~23:05 | 46 | 3 | **0.39** | **인스턴스 1대, 빌드 없음** |
| **A5** | 23:05~23:10 | 51 | 0 | **0.38** | 〃 |
| **A6** | 23:10~23:15 | 51 | **17** | **0.42** | 〃 |
| **A7** | 23:15~23:20 | 41 | 0 | **0.39** | 〃 |
| B1 | 23:26~23:27 | 77 | ~0 | 1.62 | **인스턴스 2대** |
| C1 | 23:27~23:28 | 59 | ~0 | 0.77 | 〃 |
| B2 | 23:27~23:32 | 83 | 75 | 1.43 | 〃 |
| C2 | 23:28~23:33 | 71 | 높음 | 1.51 | 〃 |
| B3 | 23:32~23:37 | 65 | 59 | 1.82 | 〃 |
| C3 | 23:33~23:38 | 57 | 높음 | 1.63 | 〃 |
| B4 | 23:37~23:42 | 61 | **93** | **2.24** | 〃 |
| C4 | 23:38~23:43 | 70 | 높음 | 1.50 | 〃 (이 인스턴스는 루프가 24.9Hz까지 눌려 있었다) |
| B5 | 23:42~23:47 | 58 | 높음 | 1.15 | 〃 |

**23:00~23:20의 4개 구간만 0.38~0.42이고, 나머지 12개는 전부 0.77~2.24다. 겹침이 없다.**
그 4개 구간의 공통점은 **기계가 조용했다**는 것 하나다 — StickMate 인스턴스 1대,
동시 빌드 없음(`[발판리포트]`에서 `swift-frontend` 창이 t=540s 이후 사라지고 A4는 t=643s에 시작한다).

★ **증거의 강도를 정확히 적어 둔다.** "A4~A7 구간에 인스턴스가 1대였다"의 근거는 (i) 23:22에 직접
`pgrep`으로 1개만 확인한 것, (ii) 두 번째 슬롯 `/tmp/stickmate-jaehyun/`이 **23:27에야 생성**된 것
두 가지다. **그 20분을 연속 표본으로 지켜본 것은 아니다** — 그래서 6절 회차 3(두 번째 인스턴스를
일부러 띄우는 회차)이 이 설명의 **진짜 확정 실험**이다. 지금은 "가장 잘 맞는 설명"이지 확정이 아니다.
(연속 표본은 23:31부터 `psprobe.csv`가 받고 있다.)

**기전이 맞는다.** 3-2절의 콜드스타트 실측: 이 앱은 **프레임당 158.6MB / 60fps에서 9.29GB/s**의
메모리 대역폭을 쓴다. 인스턴스가 2대면 그대로 **18.6GB/s**이고, WindowServer가 전체화면 오버레이
2장을 합성하는 몫이 그 위에 붙는다(지금 실측: StickMate 24.2% + 14.1%, **WindowServer 12.4%**).
대역폭 경쟁은 `gpuFrameTime`을 **직접** 밀어 올린다.

**★ 그래서 창 비율도, 활성 비율도 단독으로는 자료를 못 가른다:**
- **창%**: A6(17%)=0.42 vs B1(~0%)=1.62 — **창이 더 많은 쪽이 더 싸다.** 창 모델은 반증.
- **활성%**: A2(62%)=1.40 vs B4(61%)=2.24 — **거의 같은 활성 비율에서 60% 차이.** 단독 설명 불가.
- 두 변수 모두 "조용한 기계 4구간"과 "바쁜 기계 12구간"의 경계와 **일치하지 않는다.**

### 3-5. 반증된/보류된 가설 정리

| 가설 | 판정 | 근거 |
|---|---|---|
| 오늘 밤 렌더 경로 변경(필렛 10→9 · 펫 무릎 · 장비 82도형 · 말풍선 외곽선)이 GPU를 3~5배로 올렸다 | **반증** | 같은 바이너리가 0.38과 1.76을 둘 다 냈다. 조용 구간이 옛 빌드 값과 동일. 기전상으로도 도형 수는 158.6MB/프레임 고정분 아래에 묻힌다 |
| 창을 계속 여닫아서 올라갔다 (앞 담당의 교란 추정) | **반증** | 가장 무거운 창이 활성시간의 33%였던 구간이 0.42ms. 창 0%인 구간이 1.62ms |
| 등급 분포가 달라 평균이 달라 보였다 (리더 지적) | **반증** | 표본이 전부 `renderFrameInterval==1` 프레임(2-1절, 13/13 검증). 믹스 21%p 차이에 평균 2% 차이 |
| **시간이 지나면 좋아지는 워밍업이다** | **반증** | A는 가동 11~16분에 0.39로 떨어졌는데, **B는 같은 나이에 2.24로 올라갔다.** 가동 시간은 원인이 아니다 |
| **기계 전체의 동시 부하(타 인스턴스 · 동시 빌드)** | **가장 잘 맞음(16/16)** | 조용한 4구간만 낮다. 대역폭 기전이 뒷받침 |
| 그 안에서 말풍선/파티클 채움이 얼마를 차지하는가 | **미확인** | 6절 실험 필요 |

### 3-6. 사용자 신고와의 관계 — **연결되지 않는다**

**"갈수록 느려짐"을 지지하지 않는다.**
- A: 1.76 → 1.40 → 1.73 → **0.39 → 0.38 → 0.42 → 0.39** (33분 동안 **내려간다**)
- B: 1.62 → 1.43 → 1.82 → **2.24 → 1.15** (21분. **단조가 아니라 널뛴다.** 같은 시간에
  다른 담당이 인스턴스를 1대 더 띄우고 정보창을 여닫고 있었다 — 앱 내부의 누적이 아니라
  **외부 부하의 요동**이다. 누적이라면 되돌아오지 않아야 하는데 2.24에서 1.15로 되돌아왔다.)

가동 시간과 GPU ms 사이에는 **부호가 일정한 관계가 없다.** 단조 악화가 아니다.

### 3-7. ★ 이 라운드에서 확정된 **측정 규약** (팀 전체 적용 요청)

**StickMate 인스턴스가 2대 이상 떠 있는 동안 잰 GPU/프레임 수치는 무효다.**
같은 바이너리·같은 조작에서 3.5배 차이가 났다(0.38 ↔ 1.43). 오늘 밤 "새 빌드가 3~5배"라는
관측이 나온 것도 정확히 이 때문이다.

성능 수치를 재려면 다음 세 줄을 먼저 만족시켜야 한다.
1. `pgrep -f "StickMate.app/Contents/MacOS/StickMate" | wc -l` **== 1**
2. Unity 배치모드/`swiftc`/`xcheck` 등 동시 빌드 **없음** (`driver.sh doctor`의 락 항목)
3. 측정 구간 동안 **전역 단축키 주입 없음** (5-3절 — 남의 인스턴스까지 열린다)

## 4. 임무 2 — 폰트 아틀라스 8회 재구성

★ **먼저 자기 정정 1건.** 조사 초반에 나는 *"256×256에서 4096×4096까지 2배씩 8칸 = 8회"* 라는
사다리 가설을 세웠다(첫 재구성 뒤 크기가 256×512였으므로 시작이 256×256이라는 것까지는 맞다).
**실측이 이 가설을 반증했다.** 인스턴스 B에서 재구성이 7회 누적되는 동안 아틀라스 크기는
**끝까지 256×512 그대로였다.** 커지지 않는다. 아래가 실제 기전이다.

### 4-1. 실측 — 재구성은 **UI 표면을 처음 여는 순간**에 일어나고, 곧 포화한다

인스턴스 B, 60초 창 15개(`[스톨구간]`의 `가동 N분`과 `archive.log`의 벽시계를 맞춰 놓은 표):

| 가동 | 아틀라스 | 크기 | 그 창에 일어난 UI 사건 |
|---|---|---|---|
| 1분 | **+1** | 256×512 | **정보창 첫 열림**(23:27:47) |
| 2~4분 | +0 | 256×512 | (전체화면 감지로 자동 숨김만) |
| 5분 | **+2** | 256×512 | 정보창 열림 ×2, 설정창 **첫 열림** |
| 6분 | **+2** | 256×512 | 설정창 열림, 정보창 열림 |
| 7분 | **+2** | 256×512 | 설정창 열림, 정보창 열림 |
| 8분 | +0 | 256×512 | 정보창 **탭 전환 → [외형]**(새 창 열림 없음) |
| 9분 | +0 | 256×512 | 정보창 닫힘 |
| 10분 | **+0** | 256×512 | **정보창 열림** ← ★ 같은 조작인데 이제 0회 |
| 11~15분 | +0 | 256×512 | 열고 닫음 계속 |

**누적: 1 → 1 → 1 → 1 → 3 → 5 → 7 → 7 → 7 → 7 → 7 → 7 → 7 → 7 → 7 → 8 → 8 → 8 → 8 → 8 → 8 → 8 → 8.**
**가동 16분에 8회에 닿은 뒤 23분까지 8에서 정체. 크기는 처음부터 끝까지 256×512.**
★ 앞선 관측의 **"8회"가 다른 인스턴스에서 그대로 재현됐다.** 다만 도달 시각은 25~29분이 아니라
**16분**이었다 — 창을 더 자주 열었기 때문이다. **횟수는 재현되고 시각은 재현되지 않는다**는 것이
"시간이 아니라 글리프가 정한다"는 해석을 그대로 뒷받침한다.

읽는 법:
- 재구성은 **시간이 아니라 "아직 아틀라스에 없는 글리프를 처음 요구한 순간"**에 일어난다.
- 그래서 **같은 조작(정보창 열기)이 초반엔 재구성을 부르고 10분 뒤엔 0회다.**
  앱이 평생 쓸 한글 글리프가 다 들어간 뒤에는 아무 일도 없다.
- **크기가 안 커지므로 메모리 누적이 아니다.** 이 앱의 한글 UI 코퍼스는 256×512 안에 다 들어간다.
- 앞선 관측의 "**8회 후 정체 / 25~29분**"은 **같은 포화 곡선의 느린 버전**이다. 그 관측은 창을
  자주 열지 않았으므로 글리프 발견이 느렸고, 그만큼 정체 지점이 뒤로 밀렸을 뿐이다.
  (B는 창을 계속 여닫아서 **7분 만에** 같은 곳에 도달했다.)

★ 따라서 **"왜 하필 8회인가"의 답은 "8이라는 수에 의미가 없다"**이다. 그 수는
**앱의 서로 다른 한글 글리프 집합 크기**가 정하며, 관측마다 7~8쯤에서 멈추는 것이 정상이다.
**"왜 25~29분인가"의 답도 "시간에 의미가 없다"** — 사용자가 UI를 얼마나 훑었는지가 정한다.

### 4-2. 재구성이 도는 동안 프레임 스파이크가 있는가 — **100ms급은 없다. 자체 비용은 1ms 미만.**

`[스톨구간]` 23개 창을 재구성 횟수 · **창 열림 횟수** · `LateUpdate 최대` · 100ms 스파이크로 나란히 놓았다.

| 가동 | 창 열림 | 아틀라스 | **LateUpdate 최대** | 100ms 스파이크 |
|---|---|---|---|---|
| 1분 | 1(첫 정보창) | +1 | 19.6ms | 프레임#1 2371.6ms (창 부착 콜드스타트) |
| 2~4분 | 0 | +0 | 0.6~1.7ms | 411.4ms(4분, 전체화면 숨김 — 7-2절) |
| **5분** | **3** | **+2** | **9.8ms** | **없음** |
| **6분** | **2** | **+2** | **9.6ms** | **없음** |
| **7분** | **2** | **+2** | **9.0ms** | **없음** |
| 8~10분 | 0 | +0 | 0.9ms | 없음 |
| 11분 | **1** | **+0** | **5.8ms** | 없음 |
| 15분 | 2 | +0 | 4.8ms | 없음 |
| **16분** | 0(탭 전환만) | **+1** | **1.0ms** | **없음** |
| 17~23분 | 0~1 | +0 | 0.8~1.6ms | 245~389ms(20~23분 — 7-2절) |

**판정 세 가지.**

1. **100ms급 스파이크는 한 번도 동반하지 않았다.** 재구성이 있던 창 4개는 전부 `스파이크 없음`이고,
   스파이크가 난 창은 전부 `아틀라스 +0`이다. **8회 재구성 : 0회 스파이크.**
2. ★ **자기 정정 — "재구성 = LateUpdate +4ms"는 과대주장이었다.**
   처음엔 5·6·7분 창의 9.0~9.8ms를 재구성 탓으로 읽었다. 그런데 **그 세 창은 창을 2~3번씩 연 창이기도
   했다.** 두 변수가 그 구간에서 붙어 있어 가를 수 없다. **분리해 주는 자료가 두 줄 있다:**
   - **11분 창** — 창 1번 열림, 재구성 **0회** → **5.8ms**. (창 열림만으로 5.8ms가 난다)
   - **16분 창** — 창 열림 **0회**(탭 전환만), 재구성 **1회** → **1.0ms**.
     → **재구성 1회의 자체 비용은 1ms 미만이다.**
3. 따라서 정보창을 열 때 드는 **5~10ms는 대부분 uGUI 캔버스 재빌드**이고, 아틀라스는 그 위에
   얹히는 작은 몫이다. 어느 쪽이든 16.7ms 프레임 예산 안이다.

★ **이 정정을 남기는 이유**: 처음 세 창만 보고 결론을 냈으면 **엉뚱한 곳(폰트)을 고치러 갔을 것이다.**
표본을 8개 더 모으니 분리해 주는 두 줄이 나왔다.

### 4-3. 사용자 신고와의 관계 — **연결되지 않는다** (확정)

1. **포화한다.** 7회에서 멈춘다. 그 뒤 같은 조작이 0회다. "갈수록"을 만들 수 없다.
2. **비용이 작다.** 회당 **1ms 미만**(4-2절 16분 창), 세션 전체 8회 = **10ms 미만**.
   사용자가 느낄 수 있는 크기가 아니다.
3. **재시작 대칭성이 안 맞는다.** 사용자는 *"껐다 켜면 처음과 똑같다가 점점 느려짐"* 이라고 했다.
   아틀라스는 재시작하면 비고 **다시 같은 8회를 오른다** — 즉 **매 실행 동일한 곡선**이고
   실행 간 **누적이 아니다.** 신고의 "계속계속 누적되는 느낌"과 구조가 다르다.
4. **메모리도 안 는다.** 크기가 256×512로 고정이다(≈128KB급). 잔존 부담 없음.

**결론: 임무 2는 사용자 신고와 연결되지 않는다.** 이 항목은 종결해도 된다.
(★ 남는 참고: 4-2의 "창 열기 = LateUpdate +5.8ms"는 아틀라스와 무관한 **캔버스 재빌드** 비용이며,
정보창을 반복해서 여는 조작에서는 이쪽이 항상 남는다. 지금은 예산 안이라 조치 요청은 하지 않는다.)

## 5. 임무 3 — 리빌드에 잘리지 않는 장기 관측 설계

### 5-1. 진짜 원인 규명 — 잘린 것은 **프로세스가 아니라 기록**이다

오늘 밤 관측이 끊긴 지점은 22:31 / 22:49 / **23:26**(이 조사 도중) 세 번이다. 그런데
치명상은 프로세스 교체가 아니라 **로그 소실**이었다. `driver.sh`:

```bash
: > "$LOG_FILE"      # cmd_start 안. 이전 실행의 기록을 통째로 지운다
```

`PID_FILE`도 단일 슬롯이라 새 실행이 이전 PID를 덮어쓴다. 즉 **관측 대상이 바뀌는 것보다,
바뀌기 전 기록이 사라지는 것이 더 큰 손실**이었다(이 조사에서도 A의 원본 로그를 잃었다).

### 5-2. 이미 만들어서 **지금 돌고 있는 것** (실행 완료)

프로덕션 무관, 앱 무접촉(파일 읽기만).

| 파일 | 역할 |
|---|---|
| `/tmp/stickmate-longwatch/collect.sh` | 소스 로그를 2초 폴링. **inode 변경 또는 크기 감소**를 잘림으로 판정해 `##### RUN-BOUNDARY … #####` 마커를 찍고 오프셋 0으로 되감는다. 새 바이트만 `archive.log`에 append |
| `/tmp/stickmate-longwatch/psprobe.sh` | 30초마다 살아 있는 **모든** StickMate PID의 etime/%CPU/RSS/스레드/FD를 CSV로 |
| `/tmp/stickmate-longwatch/archive.log` | 모든 줄 앞에 **벽시계 타임스탬프**를 붙인다 ★ |
| `/tmp/stickmate-longwatch/psprobe.csv` | 위 CSV |

★ **타임스탬프가 특히 중요하다.** 앱 로그에는 시각이 없어서, 이 조사에서 A의 사건 시각을
`[발판리포트]` 60초 심장박동을 자로 삼아 **역산**해야 했다. 앞으로는 그럴 필요가 없다.

이 두 스크립트는 **관측 대상이 죽어도 죽지 않는다.** 오늘 23:26 재시작도 `RUN-BOUNDARY`로
정상 기록됐다.

**다음 사람이 쓰는 법** (앱을 건드리지 않는다):

```bash
# 지금 돌고 있다. 죽었으면 다시 띄운다(중복 실행해도 오프셋 파일로 이어 붙는다):
nohup /tmp/stickmate-longwatch/collect.sh /tmp/stickmate-run/stickmate.log &
nohup /tmp/stickmate-longwatch/psprobe.sh &

# 구간 요약만 시간순으로 보기:
grep '적응형] 최근' /tmp/stickmate-longwatch/archive.log
# 실행 경계 찾기:
grep 'RUN-BOUNDARY'  /tmp/stickmate-longwatch/archive.log
```

★ **리더 판단 요청**: 이 두 스크립트는 지금 `/tmp`에 있어 재부팅하면 사라진다.
쓸 만하다고 판단되면 `Tools/LongWatch/`로 옮겨 저장소에 넣자고 제안한다
(프로덕션 코드가 아니라 관측 도구이므로 `Assets/` 밖이 맞다).

### 5-3. 아직 필요한 것 — **리더 승인이 필요한 슬롯** (실행 안 함)

로그 연속성은 위로 풀리지만, **33분을 넘는 단일 프로세스 수명**은 위로 못 산다.
제안하는 슬롯:

```
mkdir -p /tmp/stickmate-perf /tmp/stickmate-perf-home
cp -R  Builds/macOS/StickMate.app  /tmp/stickmate-perf/StickMate.app     # 빌드 스냅샷 고정
HOME=/tmp/stickmate-perf-home \
  nohup /tmp/stickmate-perf/StickMate.app/Contents/MacOS/StickMate \
        -logFile /tmp/stickmate-perf/perf.log &
```

- **왜 복사본인가**: `Builds/macOS`를 다른 담당이 덮어써도 이 프로세스와 무관하다. 그리고
  A/B에 필요한 "지금 재고 있는 빌드가 무엇인지"가 고정된다.
- **왜 `HOME` 격리인가**: ★ **원칙 3**. 모든 인스턴스가
  `~/Library/Application Support/Vibelab/StickMate/stickmate_character.json`을 공유한다.
  관측용 인스턴스가 사용자의 진행도(레벨/XP/장비)를 덮어쓰면 안 된다. `HOME`을 바꾸면
  `Application.persistentDataPath`와 `Player.log`가 함께 격리**될 것으로 본다**
  (저장소에 `/tmp/stickmate-probe-home` 선례가 있다).
  ★ **이건 아직 검증 안 했다 — 반드시 먼저 확인하고 본 관측을 시작할 것.** 10초짜리 확인 절차:
  ```bash
  # 슬롯을 띄우고 20초 뒤
  ls -la /tmp/stickmate-perf-home/Library/Application\ Support/Vibelab/StickMate/
  #  -> stickmate_character.json 이 여기 생겼으면 격리 성공.
  #  실제 사용자 세이브의 mtime 이 그동안 안 변했는지도 같이 확인한다:
  stat -f '%Sm' "$HOME/Library/Application Support/Vibelab/StickMate/stickmate_character.json"
  ```
  **격리가 안 되면 이 슬롯을 띄우지 않는다.** 원칙 3(유저 자산 불변)이 성능 관측보다 위다.
- **왜 `open`이 아니라 직접 exec인가**: 이 슬롯은 **전역 단축키를 물려받지 않는 편이 낫다.**
  조용한 관측이 목적이라 키가 안 들어오는 것이 오히려 안전 장치다.

**리더에게 요청하는 제약 3가지** (이 슬롯이 성립하려면 필요):
1. **전역 `⌃⌥⌘Q` 금지** — 권한을 가진 **모든 인스턴스**를 한꺼번에 죽인다. 관측 슬롯도 죽는다.
   종료는 그 PID만 `NSRunningApplication(pid).terminate`로.
2. **전역 단축키 주입(`driver.sh key …`) 금지** — ★ 오늘 실측으로 확인했다.
   23:27 이후 B와 C **양쪽 로그에 `[설정창] 열림`이 동시에** 찍혔다. 한 담당의 키 주입이
   **다른 담당의 "조용한" 인스턴스를 오염시킨다.** 조용한 구간을 재려면 이 시간대에
   아무도 키를 쏘지 않아야 한다.
3. **관측 창(최소 3시간) 안에 `driver.sh stop --force` 금지.**

### 5-4. 분석 규약 — 잘려도 결론을 잇는 법

`[스톨구간] #N창(60초, **가동 M분**)`에 **각 실행의 나이**가 이미 들어 있다.
그래서 여러 실행을 **가동 분을 x축으로 겹쳐 그리면** 다음이 성립한다.

- **재현성**은 잘려도 산다: 33분짜리 6개는 "0~33분 구간"을 6번 확인해 준다.
  (이 라운드의 3-2 판정이 그렇게 나왔다 — 서로 다른 실행이 같은 조건에서 같은 값을 냈다.)
- ★ **다만 가동 분만으로 겹치면 오독한다.** A와 B는 같은 나이(가동 5~10분)에서 1.40/1.43으로
  비슷했지만 가동 11~16분에서는 0.39 대 2.24로 갈렸다. **가동 분은 축이지 원인이 아니다** —
  겹쳐 그릴 때 반드시 3-7절의 기계 상태(인스턴스 수·동시 빌드)를 **같이** 기록해야 한다.
- **누적**은 못 산다: 33분을 넘는 구간은 오직 5-3 슬롯만 준다.
  → **등반 1.05초의 장기 영향과 사용자 신고 재현은 여전히 미결.**

### 5-5. 근본 수정 한 줄 (coder 배정 요청)

`.claude/skills/run-stickmate/driver.sh`의 `cmd_start`:

```bash
- : > "$LOG_FILE"
+ [ -s "$LOG_FILE" ] && mv "$LOG_FILE" "$RUN_DIR/stickmate-$(date +%Y%m%d-%H%M%S).log"
+ : > "$LOG_FILE"
```

이 한 줄이 있었으면 오늘 밤 네 번의 관측이 **한 번도 안 잘렸다.**

---

## 6. 남은 미확인을 가르는 실험 설계 (슬롯 요청)

3-4절로 "기계 전체 부하"가 가장 잘 맞는 설명이 됐지만, **그 안에서 무엇이 얼마인지**는 아직 모른다.
5-3 슬롯 1개, 총 60분이면 다 갈린다. **회차 사이에 앱을 재시작할 필요가 없다** — 조건만 바꾸면 된다.

| 회차 | 조건(모두 인스턴스 **1대**, 키 주입 없음) | 예상 소요 | 이 회차가 가르는 것 |
|---|---|---|---|
| 0 | 조용 · 아무것도 안 함 | 10분(300초 창 2개) | **기준선.** 0.38~0.42가 재현되면 3-4절 확정 |
| 1 | 조용 · 창 전부 닫힘 · **활성 체류 ≥70%인 창만 골라 봄** | 10분 | 활성 체류(=말풍선/파티클 채움)가 GPU를 올리는가 |
| 2 | 조용 · **정보창 [외형] 탭을 계속 열어 둠** | 10분 | 창 렌더 + 초상화 RT의 실제 몫 (지금 상한만 0.11ms로 압축돼 있다) |
| 3 | 회차 0과 동일 + **두 번째 인스턴스 기동** | 10분 | **인스턴스 2대의 비용**(예측: 그것만으로 3배) |
| 4 | 회차 0과 동일 + **Unity 배치 빌드 1회 동시 실행** | 10분 | 동시 빌드의 비용 |
| 5 | 회차 0으로 복귀 | 10분 | 되돌아오는지(히스테리시스 없음 확인) |

**판정 규칙(미리 못박는다).**
- 회차 0과 5가 서로 ±0.05ms 안이면 계측이 안정적이라는 뜻이다. 아니면 그 회차 전체를 버린다.
- 회차 3에서 **1.2ms 이상**이 나오면 3-4절의 "동시 부하" 설명이 확정되고,
  **오늘 밤 "새 빌드 3~5배" 소동의 원인이 그것으로 종결**된다.
- 회차 1·2가 회차 0과 ±0.1ms 안이면 **앱 내부에는 손댈 것이 없다**는 뜻이다.

**표본 크기 근거**: 조용 구간 4개(A4~A7)의 표준편차가 **±0.02ms**였다.
0.38 대 1.43을 가르는 데 300초 창 2개면 충분하다. 벽시계 기준 10분 × 6회차 = **60분**.

★ 이 실험 동안 5-3의 제약 3가지(전역 Q 금지 / 키 주입 금지 / stop --force 금지)가 지켜져야 한다.
지켜지지 않으면 **회차 0의 기준선부터 무너진다.**

---

## 7. 부수 발견 — **계측이 거짓말하는 곳 2건**

둘 다 "갈수록 느려진다"를 **없는데도 있다고** 보이게 만드는 종류라, 다음 진단이 틀어진다.

### 7-1. 절전 등급의 프레임 비용은 **원리적으로 못 잰다**

2-1절에서 확인했듯 GPU 표본은 `renderFrameInterval == 1`인 프레임에서만 나온다.
즉 **절감하려는 등급(정적 1/2, 정지 1/4)의 비용을 이 계측은 한 번도 본 적이 없다.**

파급:
- 요약 로그의 `★GPU 점유 추정 = (활성 구간 평균 ms) × (전 등급 평균 제출/초) / 10`은
  **분자와 분모의 모집단이 다르다.** 프레임당 비용이 등급과 무관하다면 근사적으로 맞지만,
  그 전제 자체가 이 계측으로는 검증 불가다.
- `최악` 값도 활성 구간의 최악일 뿐이다.

### 7-2. **절전 중인 프레임이 "프레임스파이크"로 집계된다** ★ 이번 라운드 최대 발견

`Platform/FramePacing.cs:1018-1021`:

```csharp
// 기대 프레임 시간 — 절감 등급의 "긴 프레임"은 스파이크가 아니다.
int cap = Application.targetFrameRate;
float expectedMs = cap > 0 ? 1000f / cap : 1000f / 60f;
if (dtMs < expectedMs * SpikeRelativeFactor) return;
```

**주석의 의도는 맞는데 구현이 그 의도를 실행하지 않는다.** 이 앱이 macOS에서 실제로 쓰는
절감 손잡이는 `vSyncCount`(2)와 `OnDemandRendering.renderFrameInterval`(1/2/4)이고,
`targetFrameRate`는 **모든 로그 줄에서 −1**이다. 그래서 `cap > 0`이 절대 참이 되지 않고
`expectedMs`가 **어느 등급에서나 16.7ms로 고정**된다. 정지 등급의 진짜 기대치는 66.7ms다.

게다가 `_spikeCount++`가 **쿨다운 검사보다 먼저** 있어서, 로그 줄은 5초에 한 번으로 줄어도
**누적 카운터는 전부 올라간다.**

실측 피해(인스턴스 B, `archive.log` 23:29:41~23:30:07):

```
23:29:41  누적   3회   (여기까지가 진짜)
23:29:47  누적  24회   등급=Suspended  195.7ms
23:29:51  누적  41회   등급=Suspended  293.9ms
23:29:57  누적  60회   등급=Suspended  321.5ms
23:30:01  누적  78회   등급=Suspended  359.3ms
23:30:07  누적  96회   등급=Suspended  269.5ms
```

**26초 만에 3회 → 96회.** 전부 `등급=Suspended`, 즉 **타 앱 전체화면 감지로 앱이 스스로 숨은
정상 동작 구간**이다(macOS가 가려진 창의 디스플레이 콜백까지 줄인다). 하나도 "렉"이 아니다.

### 7-2-a. 그리고 이 카운터는 지금도 자라고 있다 — **가동 23분에 누적 519회**

| 시각 | 가동 | 누적 스파이크 |
|---|---|---|
| 23:29:41 | 3분 | **3회** |
| 23:30:07 | 4분 | **96회** (전체화면 숨김 26초 동안 +93) |
| 23:48:01 | 22분 | 348회 |
| 23:49:26 | 23분 | **519회** |

로그로 실제 찍힌 9건의 등급 분포: **정지(Still, interval=4) 5건 / 전체화면숨김 2건 / 활성 2건.**

- **전체화면숨김 2건**(289ms, 288ms)은 **확실히 오탐**이다 — 앱이 스스로 숨은 정상 동작.
- **정지 등급 5건**(116 / 133 / 155 / 180 / 208ms)은 기대치가 16.7ms가 아니라 **66.7ms**로
  계산돼야 하는 프레임이다. 지금 게이트는 그 사실을 모른다.
  ★ **다만 "정지 등급의 116~208ms가 전부 정상인지"는 내가 판정할 수 없다** — 루프는 원래
  건너뛰어도 16.7ms로 계속 돌아야 하기 때문이다(구간 평균 루프가 54~59Hz인 것과 안 맞는다).
  **이건 지금 `FramePacing.cs`에서 렌더 카운터 정직성을 조사 중인 담당의 영역이다.**
  리더가 라우팅해 주기 바란다 — 나는 **"게이트가 등급을 안 본다"**까지만 확정한다.

**왜 이게 사용자 신고와 얽히는가.** 이 숫자만 보면 *"23분에 519회, 계속 늘어난다"* 로 읽히고
그건 정확히 사용자가 말한 *"갈수록 느려짐 / 데이터가 계속 누적되는 느낌"* 의 모양이다.
**하지만 이 카운터가 늘어난다는 사실만으로는 아무것도 증명하지 않는다** — 오탐이 섞여 있고,
비율(회/분)이 늘어나는지 아닌지도 지금 로그로는 안 갈린다. **8-1을 고치기 전에는 이 카운터를
사용자 신고의 근거로 쓰면 안 된다.**

★ **이 카운터는 24시간 상주 앱에서 단조증가한다.** 사용자가 전체화면 앱을 쓸수록 늘어난다.
누군가 "누적 스파이크가 계속 늘어난다 = 갈수록 느려진다"로 읽으면 **정확히 틀린 결론**에 도달한다.
사용자 신고를 로그로 확인하려는 다음 담당이 가장 먼저 밟을 지뢰다.

---

## 8. 리더에게 올리는 조치 (어디를, 어떻게)

★ 이 라운드에 **하나도 고치지 않았다.** 배정은 리더가 한다.
`Platform/FramePacing.cs`와 `Platform/MacOS/MacWindowService.cs`는 지금 다른 담당이 잡고 있어
**읽기만 했다.**

| # | 심각도 | 위치 | 지금 | 이렇게 | 왜 |
|---|---|---|---|---|---|
| 8-1 | **높음** | `Platform/FramePacing.cs:1016~1021` (스파이크 게이트) | 절전 프레임이 스파이크로 집계 | ① `FramePacing.CurrentTier`가 `Suspended`/`DisplayOff`면 **집계 자체를 건너뛴다**(같은 파일 563행에 이미 `exempt` 선례가 있다). ② `expectedMs`를 `targetFrameRate`가 아니라 **실제 손잡이**로 계산: `base = cap>0 ? 1000f/cap : 1000f/displayHz*max(1,vSyncCount)` 에 `× max(1, OnDemandRendering.renderFrameInterval)`. ③ `_spikeCount++`를 쿨다운 **뒤**로 옮기거나, 억제된 건수를 따로 센다 | 7-2. 이 카운터로 "갈수록 느려짐"을 오판한다 |
| 8-2 | 중간 | `Platform/FramePacing.cs` `RenderDiagnostics` 요약 | GPU 평균이 활성 등급만 본다는 사실이 로그에 없다 | 요약 줄에 **`표본 커버리지 = 표본×8 ÷ 루프프레임 수`**를 함께 찍고, `★GPU 점유 추정` 옆에 "활성 등급 표본 기준"이라고 한정어를 붙인다 | 7-1. 지금은 읽는 사람이 모집단을 알 수 없다 |
| 8-3 | **낮음(하향)** | `Platform/StallAttribution.cs` (`RecordFontAtlasRebuild`) | 재구성 횟수와 최악 프레임이 **따로** 집계된다 | ★ **4-2절에서 다른 방법(재구성 창의 `LateUpdate 최대` 대조)으로 이미 답을 냈으므로 급하지 않다.** 그래도 붙이면 좋다: `RecordFontAtlasRebuild`에서 `Time.frameCount`를 저장하고 같은 프레임의 `[프레임스파이크]`/`[스톨귀인]`에 "폰트아틀라스 재구성 동반" 한 단어를 덧붙인다(할당 0, 상시 비용 0) | 다음에 같은 의심이 나오면 대조표를 다시 만들 필요가 없다 |
| 8-4 | 중간 | `.claude/skills/run-stickmate/driver.sh` `cmd_start` | `: > "$LOG_FILE"`로 이전 기록 삭제 | 5-5의 한 줄(회전 저장) | 오늘 밤 네 번의 관측이 이것 때문에 잘렸다 |
| 8-5 | 낮음(기록용) | 렌더 구성 전반 | 3024×1964 전체화면 오버레이 + MSAA 4x = **프레임당 158.6MB / 60fps에서 9.29GB/s** | **지금 바꾸라는 제안이 아니다.** 화질은 design 3인의 소관이고 "선 화질 조사"에서 이미 결론이 났다. 다만 **이 앱의 GPU 비용은 도형 수가 아니라 이 고정 대역폭이 전부**라는 사실을 팀 공유 사실로 못박아 둔다 — 앞으로 "도형을 늘렸는데 GPU가 올랐나?"라는 질문에는 **측정 전에 이미 '아니오'** 라고 답할 수 있다 | 3-2 |
| 8-6 | 낮음 | 로그 정확도 | 인스턴스 A에서 `[정보창] 열림`이 **찍히지 않았다**(닫힘만). 이 조사에서 창 개폐 구간 복원을 어렵게 만들었다 | 인스턴스 B에서는 이미 찍힌다 — **수정된 것으로 보인다. 확인만 요청.** | 재발 시 다음 조사가 또 막힌다 |
| **8-7** | **높음(절차)** | 팀 측정 규약 | 성능 수치를 **인스턴스 2대·동시 빌드 중에** 재고 있다. 오늘 밤 "새 빌드 3~5배" 소동의 직접 원인이다 | **3-7절 3줄 체크를 성능 관측 지시서의 고정 문구로 넣는다.** `driver.sh doctor`가 이미 "그 밖의 인스턴스"와 "Unity 배치모드 락"을 찍으므로, **관측 전후로 `doctor` 출력을 리포트에 첨부**하게 하면 강제된다 | 같은 바이너리·같은 조작이 0.38 ↔ 1.43(3.5배)로 갈렸다 |

---

## 9. 유휴 예산 (24시간 상주 기준, 참고 기록)

`[스톨구간]`·`[스톨귀인]` 60초 창 8개의 실측. **회귀 없음. 조치 요청 아님.**

| 항목 | 빈도 | 평균 | 최악 | 60초당 | 코어 1개 대비 |
|---|---|---|---|---|---|
| 창 열거(네이티브) | 193회/60s (**0.31s 주기**) | 4.0~4.7ms | **13.9~21.3ms** | 765~864ms | **1.3~1.4%** |
| 에이전트 Update | 프레임당 1회 | 0.27~0.30ms | 21.6ms(생애) | 971~1067ms | 1.6~1.8% |
| UI창 폴링 | **프레임당 10회** | 0.00ms | 12.6ms(생애) | 108~150ms | 0.2~0.25% |
| 물리 | — | — | — | 2~3ms | ~0.005% |
| 미귀인(로직−구간합) | — | — | — | ~520ms | 0.87% |

- 창 열거 주기 **0.31초**는 팀 가이드(200~500ms) 안이다. **다만 최악 21.3ms는 60fps 한 프레임(16.7ms)을
  넘는다** — 메인 스레드 단독으로 프레임을 떨어뜨릴 수 있는 유일한 로직 항목이다. 기록만 해 둔다.
- UI창 폴링이 **프레임당 10회**(= 하루 5,180만 회)인데 합계가 0.25%라 지금은 문제가 아니다.
  표면이 늘어나면 선형으로 는다는 점만 남겨 둔다.
- 힙 **14.1~14.2MB 평탄**, RSS는 오히려 **196MB → 162MB로 감소**, 스레드 46~47 고정, FD 59 고정
  (`psprobe.csv`, 30초 간격). **추세 없음 — 누수 아님.**

### 9-1. 앱 밖에 나가는 비용 (24시간 상주 앱에서 가장 중요한 항목)

앱 자신의 `%CPU`에는 **잡히지 않는** 몫이 있다. 23:43 실측(`ps -A -o %cpu`, 인스턴스 2대 상태):

| 프로세스 | %CPU(코어 1개 기준) |
|---|---|
| StickMate (run 슬롯) | 24.2 |
| StickMate (jaehyun 슬롯) | 14.1 |
| **WindowServer** | **12.4** |
| 합계 | **50.7** |

(참고: `FramePacing` 클래스 문서에 남아 있는 이전 실측 —
`StickMate 실행 중 WindowServer 20.2% + StickMate 22.4%` / `종료 후 WindowServer 2.2%`.
즉 **인스턴스 1대가 컴포지터에 약 18%p를 부과한다.**)

- **인스턴스 1대 기준 실질 비용 ≈ 코어 1개의 25~30%**, 24시간.
  이 중 로직(9절 표)은 **3~4%p뿐**이고 나머지는 전부 **렌더/프레젠트/합성**이다.
- 3-2절의 대역폭(9.29GB/s)과 같은 이야기의 다른 계기다.
- ★ **미세 최적화(Update 할당 줄이기 등)로는 이 숫자가 안 움직인다.**
  움직이려면 (a) 제출 장수를 더 줄이거나(적응형 등급 — 이미 함) (b) **오버레이 표면 면적/MSAA를
  줄여야 한다.** (b)는 화질 결정이라 **design 3인 + 리더 합의 사항**이며, 이 문서는
  숫자만 제공한다(8-5).


---

## 10. 플랫폼 영향

- **macOS 영향**: 이 문서 전체가 macOS 실측이다. **프로덕션 코드 변경 0건**이므로 런타임 영향도 0건.
  8절은 전부 제안이다.
- **Windows 영향: 없음(이번 라운드) / 단 8-1은 별도 배정 필요**.
  - 사용자 지시("윈도우는 일단 미루고 맥만")에 따라 Windows는 이번 라운드 대상이 아니다.
  - ★ 다만 **7-2의 결함 자체는 플랫폼 중립 파일(`Platform/FramePacing.cs`)에 있다.**
    Windows는 `targetFrameRate`를 실제로 쓰므로(`Suspended → 60/2 = 30`) `cap > 0` 가지는 살아 있고
    macOS만큼 심하지 않지만, **`renderFrameInterval`을 곱하지 않는 부분은 똑같이 틀렸다.**
    → Windows 라운드가 열릴 때 8-1과 **같은 수정으로 함께** 처리해야 한다. 지금 갭으로 기록해 둔다.
  - 측정 도구(`collect.sh`/`psprobe.sh`)는 macOS 전용 bash다. Windows 등가물은 미작성.
- **모바일**: 해당 없음(스크린샷 백드롭 모드에는 창 열거·오버레이 합성이 없다).

---

## 11. 이 라운드에서 **다시 재지 않은 것** (리더 지시대로)

힙 13.1~15.0MB 추세 없음 / FD 63 고정 / 스레드 49 고정 / `화면안=아니오` 0 / 말풍선 미제거 0.
→ 9절에서 우연히 재확인됐고 **모두 일치**한다(FD 59·스레드 46~47은 인스턴스가 2개로 늘어난
지금 값이며, 어느 쪽도 **증가 추세가 없다**는 결론은 동일).

---
---

# 부록 A (2026-09-02) — Windows "덜 부드럽다" 라운드: `STICKMATE_VSYNC` 신설

담당: perf-doc (Teammate4) · 대상: **Windows 실기 A/B 손잡이 신설** ·
수정 파일 **`Assets/_Project/Scripts/Platform/FramePacing.cs` 1개뿐**.

> ★ **이 부록은 아무것도 "고쳤다"고 말하지 않는다.** 이 개발 머신에는 Windows가 없다.
> 여기 있는 것은 (a) 사용자가 스스로 A/B 할 수 있는 손잡이 하나와 (b) 그 후보가 왜 후보인지의
> 1차 출처, 그리고 (c) **반대 결론도 똑같이 가능하다**는 사실이다.

---

## 12. 지금까지 무엇이 반증됐는가 (이 라운드의 출발점)

| # | 가설 | 상태 | 근거 |
|---|---|---|---|
| 1 | GPU가 병목이라 렉이 난다 | **해결됨** | 사용자가 외장 → 내장(Intel Iris Xe) 전환. 10분 실측 **GPU 최악 222.48ms → 17.46ms(13배)**, 배회 중 스파이크 0회. 사용자 확인 "컴퓨터 렉은 확연히 줄은거 같음" |
| 2 | 남은 "덜 부드럽다"는 **정지 등급 15fps**(`renderFrameInterval=4`) 때문이다 | **반증됨** | 사용자가 `STICKMATE_STILL_DIVISOR=1`로 절전을 완전히 끄고 시험 → **"1로 바꿔서 해도 썩 부드럽진 않네 원래 이런듯"** |
| 3 | MSAA 배수가 원인이다 | **반증됨**(2026-09-01) | 4x/2x/0x 세 회차 모두 GPU 약 30%로 동일 |

2가 반증된 것이 이 라운드의 핵심이다. **절전은 원인이 아니다.**
그러면 "덜 부드럽다"는 **초당 장수가 아니라 프레임 간격**의 문제일 가능성이 남는다.

---

## 13. 남은 1순위 후보 — `vSyncCount = 0`이 코드에 박혀 있다

`Platform/FramePacing.cs`의 클래스 문서는 두 플랫폼이 **정반대 처방**을 쓴다고 적어 두었다.

```
                macOS                          Windows
합성 경로       CAMetalLayer → WindowServer    레거시 BitBlt → DWM 레이어드 창
vsync           켠다 (vSyncCount=N)            끈다 (vSyncCount=0)
상한 기구       vSyncCount(위상 고정)          Application.targetFrameRate(sleep)
```

### 13-1. 그 판단이 옳았을 수 있다 (먼저 이쪽을 적는다)

원문 근거는 이렇다: 이 앱의 Windows 창은 `DwmExtendFrameIntoClientArea` 때문에
**flip-model 스왑체인을 쓸 수 없고**, 레거시 BitBlt(리디렉션 표면) 경로로만 화면에 올라간다.
레이어드 창은 스캔아웃이 아니라 **DWM 합성**을 거치므로 앱 쪽 vsync가 찢어짐을 막아주지 못하고
**지연만 한 프레임 더한다**.

이게 맞으면 vsync를 켜도 안 나아지고 **더 나빠질 수도** 있으며, 남는 결론은
**"투명 오버레이 구조 자체의 한계"**다. → 그 경우의 후속 방향은 15절.

### 13-2. 그런데 그 근거는 "찢어짐"에 대한 것이지 "위상 균일성"에 대한 것이 아니다

같은 클래스 문서의 **macOS 절**이 이미 반대 방향의 논증을 갖고 있다:

> `targetFrameRate`는 vsync를 끄고 sleep으로 속도를 맞추므로 앱의 위상이 디스플레이 주기와
> 무관하게 떠다닌다 — 120Hz에서 60fps 평균이어도 어떤 프레임은 1회, 어떤 프레임은 2회 표시되는
> 맥놀이가 생겨 **오히려 더 끊겨 보인다**.

사용자 신고는 정확히 **"덜 부드럽다"**(= 평균이 아니라 균일성)이다.
Windows에만 이 논증을 적용하지 않은 이유가 13-1인데, **13-1은 다른 질문에 대한 답이다.**
둘 중 어느 쪽이 이 하드웨어에서 이기는지는 **실측 외에 결정할 방법이 없다.**

### 13-3. ★ 1차 출처 — 이 앱의 Windows 프레임 타이밍 계기는 **문서상 못 쓰게 되어 있다**

Unity의 D3D 백엔드는 프레임 시간을 주사율에서 유도하며, 그 계기는
`IDXGISwapChain::GetFrameStatistics`다. Microsoft 공식 문서 Remarks 원문:

> **"You cannot use GetFrameStatistics for swap chains that both use the bit-block transfer
> (bitblt) presentation model and draw in windowed mode."**
> "You can only use GetFrameStatistics for swap chains that either use the flip presentation
> model or draw in full-screen mode."
> "Statistics are not reliable in many multiple monitor scenarios, as well as scenarios where
> other fullscreen apps are running."

출처: <https://learn.microsoft.com/en-us/windows/win32/api/dxgi/nf-dxgi-idxgiswapchain-getframestatistics>

**이 앱은 정확히 그 금지 조합이다** — bitblt(flip 불가) **AND** 창 모드(전체화면 오버레이지만
윈도우 모드다). 즉 Windows에서 이 앱의 프레임 타이밍 기반은 **처음부터 문서상 유효하지 않다.**

> **미확인(정직한 한계)**: "Unity가 *바로 이* API를 쓴다"는 것은 Unity 소스를 직접 확인한 것이
> 아니라 Unity 스태프 답변(14-2)에서 유도한 것이다. Unity 엔진 소스 접근이 없어 이 연결은
> **강한 정황이지 확증이 아니다.**

---

## 14. 사용자 로그의 새 단서 — `Direct3D: ... time drift`

사용자 로그에 **이번 실행에만** 새로 뜬 줄:

```
Direct3D: detected that using refresh rate causes time drift.
          Will stop trusting refresh rate until the game window is moved.
```

### 14-1. 무슨 뜻인가

Unity의 D3D 백엔드가 **주사율에서 유도한 프레임 시간이 실제 경과 시간과 어긋나는 것을 검출**하고,
**주사율 기반 타이밍을 포기**한다는 뜻이다. 같은 계열의 형제 메시지가 결과를 더 노골적으로 적는다:

```
Direct3D: detected that vsync is broken (it does not limit frame rate properly).
          Delta time will now be calculated using cpu-side time stampling until the game window is moved.
```

→ 즉 `Time.deltaTime`이 **presentation 타임스탬프 기반에서 CPU 타임스탬프 기반으로 강등**된다.

### 14-2. 출처 (1차 / 준1차 / 미확인을 구분해서 적는다)

| 급 | 내용 | 출처 |
|---|---|---|
| **1차** | bitblt + 창 모드에서 `GetFrameStatistics`를 쓸 수 없다 | [Microsoft Learn — IDXGISwapChain::GetFrameStatistics](https://learn.microsoft.com/en-us/windows/win32/api/dxgi/nf-dxgi-idxgiswapchain-getframestatistics) |
| **준1차** (Unity 스태프 발언) | 이 메시지는 "게임에서 vsync를 켰는데 드라이버가 강제로 껐다"는 뜻 — *"VSync is enabled in the game but it's force disabled in the driver and is thus not working correctly"* (Tautvydas-Zilys, Unity) | [Unity Discussions — Direct3D: detected that using refresh rate causes time drift](https://discussions.unity.com/t/direct3d-detected-that-using-refresh-rate-causes-time-drift/866737) |
| **준1차** (Unity 스태프 발언) | 주사율 기반 deltaTime 문제의 원인으로 *"a problem with the `IDXGISwapChain::GetFrameStatistics` function call"* / *"GetFrameStatistics reports broken values"* 를 지목(Dave-Hampson, Unity). 외부 모니터에서 `Time.deltaTime`이 `1/refreshRate`에 얼어붙고, **창을 옮기면 해소**된다 | [Unity Discussions — Time.deltaTime frozen to 1/refreshRate when using VSync on external monitors](https://discussions.unity.com/t/time-deltatime-frozen-to-1-refreshrate-when-using-vsync-on-external-monitors/1664527) |
| **배경** | Unity 2020.2가 deltaTime을 CPU 타임스탬프에서 **presentation 타임스탬프** 기반으로 바꾼 이유 자체가 "미세한 jitter 제거"였다 | [Unity Blog — Fixing Time.deltaTime in Unity 2020.2 for smoother gameplay](https://unity.com/blog/engine-platform/fixing-time-deltatime-in-unity-2020-2-for-smoother-gameplay) ※ 자동 수집기에서 **403**이라 본문 원문 인용은 못 했다 — 제목/요지만 확인 |
| **미확인** | 이 메시지가 실제로 **눈에 보이는 캐릭터 떨림**을 만드는가 | 어떤 1차 출처도 이걸 말하지 않는다. **가설이다.** |

### 14-3. 왜 "vsync 설정"과 맞물리는가

- 메시지는 **주사율/vsync를 신뢰할 수 있는가**에 관한 것이다. `STICKMATE_VSYNC`는 정확히 그 축을 만진다.
- 두 메시지 모두 **"until the game window is moved"**(창을 옮길 때까지)로 끝난다. 즉 판정이
  **창/스왑체인의 디스플레이 연결 상태**에 묶여 있다 — 이 앱처럼 **모든 모니터를 덮는 창**은
  MS 문서가 경고한 *"not reliable in many multiple monitor scenarios"*에 그대로 걸린다.
- **왜 하필 이번 실행에만 떴나(미확인)**: 직전 두 실행에는 없었고 달라진 것은 **GPU가 외장 →
  내장으로 바뀐 것**뿐이다. 하이브리드 그래픽에서 외장 렌더 결과는 내장의 표시 엔진으로 복사되어
  나가므로 **제시 경로가 통째로 다르다.** 상관은 강하지만 인과는 **확인되지 않았다.**

### 14-4. 사용자가 이 줄을 실기에서 다시 확인하는 법 (재빌드 불필요)

```
%USERPROFILE%\AppData\LocalLow\<회사명>\StickMate\Player.log
```
를 메모장으로 열고 `time drift` 또는 `vsync is broken`을 찾는다. **회차마다 있는지 없는지를
적어 두는 것**이 이번 A/B의 3번째 관측값이다(1=체감, 2=5분 요약 줄, 3=이 메시지 유무).

---

## 15. 이번 라운드가 만든 것 — `STICKMATE_VSYNC`

수정 파일은 **`Platform/FramePacing.cs` 하나**다.

### 15-1. 계약

| 항목 | 값 |
|---|---|
| 변수명 | `STICKMATE_VSYNC` |
| 값 | `0` = vsync 끔(**현행 Windows 기본**) / `1` = 매 vblank / `2` = 두 번에 한 번 / `3`,`4`도 받음 |
| 범위 밖 | `0`~`4`로 **clamp** + `LogWarning`으로 요청값·clamp값을 함께 남긴다 |
| 정수가 아님 | 무시하고 **기본 동작**으로 감 + `LogWarning` |
| **변수 없음** | **기존 경로가 한 줄도 바뀌지 않은 채 그대로 돈다** |
| 적용 시점 | `ApplyOnce` 1회(시작 시). 실행 중 변경 불가 → **콜드 스타트 A/B만 유효** |

기존 7개 계측 변수(`STILL_DIVISOR` / `FORCE_MSAA` / `FORCE_TIER` / `ADAPTIVE_PACING` /
`DEVTOOLS` / `KEEP_LAYERED` / `UNLOCK_ALL`)와 **같은 관례**다.

### 15-2. ★ 코드에 이미 적혀 있던 함정을 코드로 막았다

> `vSyncCount >= 1`이면 Unity가 `targetFrameRate`를 **통째로 무시한다.**

둘을 같이 걸어 두면 **조용히 한쪽이 죽는데 로그는 둘 다 걸린 것처럼 보인다.** 그래서:

- vsync를 켜면(`>=1`) `Application.targetFrameRate = -1`로 **명시 해제**한다(macOS 경로가 이미
  쓰던 방식과 동일).
- 그 상호작용을 **로그에 문장으로** 적는다 — *"이 상태에서 targetFrameRate는 Unity가 통째로
  무시하므로 -1로 명시 해제했습니다"*.

### 15-3. ★ 로그는 **설정값이 아니라 되읽은 값**을 찍는다

이 저장소는 "설정을 믿고 결과를 안 본" 사고를 반복했다(`Screen.msaaSamples`가 백버퍼와 무관하게
요청값을 되돌려준 건). 그래서 대입 **직후** `QualitySettings.vSyncCount` /
`Application.targetFrameRate`를 **다시 읽어** 찍고, 요청값과 다르면
`★경고: ... 이 회차의 vsync 측정은 무효입니다`를 붙인다.

**되읽기도 완전하지 않다(정직한 한계)**: 그건 "Unity가 값을 받아 갔다"까지만 증명하고
"드라이버가 실제로 vblank에 맞춘다"는 증명하지 못한다. 그래서 **5분 요약 줄**에
`vsync 되읽음 N/target T, 주사율 H Hz`를 함께 찍는다 — 같은 줄의 **루프 Hz**와 나란히 놓으면
사람이 눈으로 판정할 수 있다.

> **판정 규칙**: vsync `N`을 켰으면 **루프 Hz ≈ 주사율/N** 이어야 한다.
> 안 붙으면 드라이버가 vsync를 안 지킨 것이고, 그 회차의 "vsync 켬"은 **이름뿐**이다.

### 15-4. 적응형 등급과의 상호작용 (자동으로 맞는다 — 확인함)

`InitializeAdaptiveGovernor`가 `_baseVSyncCount = QualitySettings.vSyncCount`를 **ApplyWindows
이후에** 읽으므로, 강제값이 그대로 계획 수립의 기준이 된다. 그리고
`FramePacingPolicy.BuildPlan`은 이미 `baseVSyncCount > 0` 분기를 갖고 있다(macOS가 쓰던 경로).
즉 **정책 코드는 한 줄도 고칠 필요가 없었다.**

- 보는 사람이 있는 등급(활성/정적/정지): `vSyncCount`·`targetFrameRate`를 **안 건드리고**
  `renderFrameInterval`만 나눈다 → **vsync 위상이 유지된 채** 절전이 걸린다.
- 보는 사람이 없는 등급(자리비움/전체화면숨김): `vSyncCount`를 곱해 내리고 나머지를
  `renderFrameInterval`로 넘긴다.

---

## 16. 사용자 실기 확인 목록 (이 머신에서 확인 **불가능**한 것들)

아래는 전부 **미확인**이다. 개발 머신이 macOS라 Windows 실행 자체가 없다.

| # | 확인할 것 | 어떻게 |
|---|---|---|
| 1 | `STICKMATE_VSYNC=1`이 실기에서 **적용되는가** | 시작 직후 로그에 `[FramePacing/Windows] ★STICKMATE_VSYNC=1 적용(계측용)` 줄 + `되읽음: vSyncCount 0 -> 1` |
| 2 | vsync가 **실제로 잡아주는가** | 5분 요약의 **루프 Hz**가 `주사율/1`에 붙는가 |
| 3 | 체감이 나아지는가 | 사용자 육안. **이것만이 최종 판정이다** |
| 4 | `time drift` 메시지가 회차마다 뜨는가/사라지는가 | `Player.log`에서 `time drift` 검색 |
| 5 | vsync를 켰을 때 **잔상/찢어짐이 돌아오지 않는가** | 13-1이 옳다면 여기서 회귀가 보인다. 글자 획이 유령처럼 겹치는지 확인 |
| 6 | 입력 지연이 늘지 않는가 | 캐릭터 드래그·정보창 타이틀바 드래그가 커서를 늦게 따라오는지 |

**5번과 6번이 이 실험의 대가다.** 3번이 좋아져도 5·6번이 나빠지면 채택하면 안 된다.

### 16-1. 회차 설계 (콜드 스타트 3회, 각 10분 이상)

| 회차 | 명령 | 보는 것 |
|---|---|---|
| A | `STICKMATE_VSYNC=0` (= 현행) | 기준선 |
| B | `STICKMATE_VSYNC=1` | 주사율 그대로 vsync 켬 |
| C | `STICKMATE_VSYNC=2` | 절반 주사율에 위상 고정 |

각 회차마다 **완전 종료**(작업 관리자에 `StickMate.exe`가 남지 않게) 후 시작.
`setx`는 쓰지 마라 — 사용자 환경에 영구 등록되어 나중에 평범하게 실행한 앱까지 계측 모드가 된다.

### 16-2. 복붙용 `.bat` 문안 (PowerShell 몰라도 된다)

메모장에 붙여 넣고 **`ANSI` 인코딩**으로 `vsync1.bat` 같은 이름으로 저장한 뒤 **더블클릭**한다.
(탐색기에서 exe를 그냥 더블클릭하면 환경변수가 전달되지 않는다 — 반드시 이 `.bat`으로 실행.)

**회차 B — vsync 켬 (`vsync1.bat`)**
```bat
@echo off
set STICKMATE_VSYNC=1
start "" "C:\Users\kjmoon\Downloads\StickMate-Windows-20260902b\Windows\StickMate.exe"
```

**회차 C — 절반 주사율 (`vsync2.bat`)**
```bat
@echo off
set STICKMATE_VSYNC=2
start "" "C:\Users\kjmoon\Downloads\StickMate-Windows-20260902b\Windows\StickMate.exe"
```

**회차 A — 기준선 (`vsync0.bat`)**. 그냥 평소처럼 더블클릭해도 같지만, 세 회차를 같은
방식으로 띄우는 편이 비교가 깨끗하다.
```bat
@echo off
set STICKMATE_VSYNC=0
start "" "C:\Users\kjmoon\Downloads\StickMate-Windows-20260902b\Windows\StickMate.exe"
```

> ★ **경로는 다음 빌드에서 바뀐다.** 새 빌드를 받으면 폴더 이름(`...-20260902b`)이 달라지므로
> `.bat` 안의 경로도 그때 같이 고쳐야 한다. 고치기 싫으면 `.bat`을 `StickMate.exe`와 **같은
> 폴더**에 두고 경로를 `StickMate.exe` 한 단어로 줄여도 된다:
> ```bat
> @echo off
> set STICKMATE_VSYNC=1
> start "" "%~dp0StickMate.exe"
> ```
> `%~dp0`는 "이 .bat이 있는 폴더"라는 뜻이라 빌드가 바뀌어도 그대로 쓸 수 있다.

**이미 쓰던 변수와 같이 줄 수도 있다** (줄만 늘리면 된다):
```bat
@echo off
set STICKMATE_VSYNC=1
set STICKMATE_STILL_DIVISOR=1
start "" "%~dp0StickMate.exe"
```

---

## 17. 만약 셋 다 나아지지 않으면 (13-1이 옳았던 경우)

그러면 남는 결론은 **투명 오버레이 합성 구조 자체의 한계**이고, 다음 후보는 이렇게 갈린다.
**이 라운드는 여기까지만 적고 착수하지 않는다**(전부 `dev-platform` 영역이다).

1. **`Time.deltaTime` 강등이 진짜 원인인 경우** — 캐릭터 이동/애니메이션을 `Time.deltaTime`이
   아니라 **평활화된 자체 시계**로 적분한다. 14절이 옳다면 이게 유일한 근본 수정이다.
2. **DWM 합성 경합인 경우** — flip-model을 못 쓰는 제약(=`DwmExtendFrameIntoClientArea` 의존)
   자체를 재검토해야 한다. **되돌리기 어려운 결정**이라 `game-architect` 판단이 먼저다.
3. **주사율 자체가 높은 경우** — 사용자 패널이 예컨대 144Hz인데 60fps로 그리면 144/60=2.4라
   비약수 맥놀이가 난다(macOS 절이 이미 같은 계산을 해 뒀다: 비약수는 균일할 수 없다).
   그 경우 `STICKMATE_VSYNC=2`(=72fps) 쪽이 `1`보다 오히려 나을 수 있다 — 그래서 회차 C가 있다.

---

## 18. 플랫폼 영향

- **Windows 영향: 함께 수정함.** 이번 라운드의 본체가 Windows다. **기본 동작은 무변경**
  (`STICKMATE_VSYNC` 미지정 시 `ApplyWindows`의 기존 문장이 그대로 실행된다). 새 가지는
  변수가 있을 때만 진입한다.
- **macOS 영향: 함께 수정함(대칭 유지).** 같은 변수를 macOS 경로에도 넣어
  `StickConfig.macVSyncInterval`을 덮어쓸 수 있게 했다. **한쪽에만 손잡이를 두면 그 자체가 새
  플랫폼 갭**이 되기 때문이다(CLAUDE.md "플랫폼 동시 검토"). 변수 미지정 시 macOS도 무변경.
- **모바일 영향: 없음.** `ApplyMacOS`/`ApplyWindows`는 각각 `#if UNITY_STANDALONE_*` 안이고,
  모바일 타깃에서는 새 헬퍼가 호출되지 않는다(컴파일은 된다 — 플랫폼 중립 위치).

### 18-1. 크로스 컴파일 결과

`Tools/CrossCompile/xcheck.sh`는 라운드 중 `dev-platform`이 `IPlatformWindowService`에
`GetForeignFullscreenTier()`를 추가하는 **작업 중 상태**라 테스트 어셈블리가 깨져 있었다
(CS0535 다수, **전부 그 멤버 하나 때문이며 `FramePacing.cs`를 지목한 에러는 0건**).
그래서 스크래치패드에 트리 사본을 만들고 **그 사본에서만** 해당 인터페이스 멤버에 기본 구현을
넣어 `dev-platform`의 진행 중 작업이 내 검사를 가리지 않게 한 뒤 측정했다:

```
[win] 전부 통과   runtime(editor) 0 / runtime(player) 0 / Tests.EditMode 0 / Tests.PlayMode 0 / Assembly-CSharp-Editor 0
[osx] 전부 통과   runtime(editor) 0 / runtime(player) 0 / Tests.EditMode 0 / Tests.PlayMode 0 / Assembly-CSharp-Editor 0
```

**거짓 초록 방지(양성 대조)**: `--selftest-only`로 반대 타깃 카나리아를 넣었더니 두 타깃 모두
`XCHECK_CANARY ... 거짓 초록` 에러 3건으로 **실패했다**. 카나리아가 실제로 문다 = 위 0건은 진짜다.

사용자 트리에서 직접 돌린 결과(진행 중 상태 포함)도 함께 남긴다:
`osx/runtime(editor)=0`, `osx/runtime(player)=0` — **`FramePacing.cs`가 들어가는 어셈블리는
사용자 트리에서도 0건**이다.

---
---

# 부록 C (2026-09-02) — 오디오 재검토: `m_DisableAudio`를 되돌려도 되는가

담당: perf-doc · **프로덕션 `.cs`/`ProjectSettings` 수정 0건.** 측정과 판정만 했다.
계기 원본: `docs/perf/audio_device_probe.c` · `audio_open_latency_probe.c` · `README_audio_probe.md`

## C-0. 세 줄

1. **원래 결정의 근거는 남아 있다.** `BuildStandalone.cs:302~357` + `Tasklist.md:7677~7690`에
   `sample` 스택과 **스레드 53→48**, **"측정 0.2%"**가 적혀 있다. **단 이 문서(`PERFORMANCE_NOTES.md`)에는
   오디오가 한 글자도 없었다**(양성 대조 통과: 같은 grep이 `vSync` 49건을 찾는다). 커밋 메시지에도 없다.
2. **그 0.2%는 맞았고, 동시에 틀렸다.** 오늘 다시 재니 **우리 프로세스는 0.19%**로 그대로 재현되는데,
   **`coreaudiod`가 별도로 코어 1개의 4.2~5.1%를 쓴다.** 원래 계측은 우리 프로세스만 봤다 —
   `dwm.exe`/WindowServer 함정이 오디오에서 그대로 재현됐다.
3. **더 큰 것은 CPU가 아니다.** 출력 장치를 여는 순간 `coreaudiod`가
   **`PreventUserIdleSystemSleep` 어서션을 잡는다**(실측). 즉 **오디오를 켜면 이 맥은 밤새 잠들지 않는다.**
   그리고 Unity의 자체 완화책 `m_EnableOutputSuspension`은 **에디터 전용**이다(공식 문서 확인).

## C-1. 과제 1 — 그때 무엇을 근거로 껐는가

| 항목 | 기록됨? | 값 | 위치 |
|---|:--:|---|---|
| 기전(왜 도는가) | **O** | `com.apple.audio.IOThread.client` → `FMOD::OutputCoreAudio::renderProc()` → `mix()` | `BuildStandalone.cs:344-347` |
| 스레드 감소 | **O** | **53 → 48** | `Tasklist.md:7690` |
| CPU 지분 | **O(수치만)** | **"측정 0.2%"** — 원시 출력은 남아 있지 않다 | `BuildStandalone.cs:353` |
| 전력/배터리 | **X** | *"오디오 하드웨어 전력 도메인이 계속 살아 있다"* — **기전 주장이고 측정이 아니다** | `BuildStandalone.cs:352` |
| 절전 방해 | **X** | 그때는 **고려 자체를 안 했다** | — |
| `coreaudiod` 비용 | **X** | **안 쟀다** | — |

**git 추적**: `git log -S m_DisableAudio` → `c256a58`(2026-08-31, "R5 Major 2건 수정 + 오늘 8개 라운드 통합").
**그 커밋 메시지에 오디오는 한 줄도 없다.** 8개 라운드 묶음 커밋에 딸려 들어갔다.
→ **근거는 소스 주석과 `Tasklist.md`에만 있고, 성능 문서와 커밋 이력에는 없다.** 이 부록이 그 구멍을 메운다.

## C-2. 과제 2 — 지금 다시 잰 결과 (2026-09-02 18:55~19:10, M2 Pro / 내장 스피커 44.1kHz)

**계기**: AUHAL로 기본 출력 장치를 열고 무음을 렌더하는 프로브(= FMOD가 소리 0개일 때 하던 일).
`OutputIsSilence` 플래그는 일부러 세우지 않았다(세우면 OS가 최적화해 비교가 무의미해진다).
**구간은 전부 벽시계 90초**, OFF/ON을 왕복했다.

| 구간 | 프로브 | **`coreaudiod` CPU** | 프로브 자신 | `coreaudiod` 스레드 |
|---|---|---:|---:|---:|
| OFF-1 | 없음 | **0.00 s / 90s = 0.00%** | — | 7 |
| ON512-1 | 512프레임 | **4.43 s = 4.92%** | 0.19% | 8 |
| ON512-2 | 512프레임 | **4.55 s = 5.06%** | 0.19% | 8 |
| OFF-2 | 없음 | **0.00 s = 0.00%** | — | 7 |
| ON4096-1 | 4096프레임 | **3.74 s = 4.16%** | 0.12% | 8 |
| OFF-3 | 없음 | **0.00 s = 0.00%** | — | 7 |

**겹침이 없다. 그리고 되돌아온다**(OFF-2·OFF-3이 정확히 기준선) — 히스테리시스 없음.

### C-2-a. 양성 대조 (이 저장소 필수 절차)

- **계기가 살아 있는가**: 512 → **94.1 콜백/초**, 4096 → **11.8 콜백/초**. 비율 **8.0배**로
  `44100/512 : 44100/4096`과 정확히 일치. 콜백은 추정이 아니라 **프로브가 직접 센 값**이다.
- **다른 클라이언트로 재현되는가**: 프로브를 버리고 **`afplay`로 무음 WAV**를 틀었다(구현이 완전히 다르다).
  `coreaudiod` **0.00% → 5.11% → 0.01%**. **두 계기가 일치한다.**
- **★ 내 사전 예측 1건은 반증됐다.** 나는 "버퍼를 8배로 키우면 `coreaudiod` 비용도 크게 준다"고
  예측했는데 **4.99% → 4.16%(−17%)에 그쳤다.** → `coreaudiod`의 비용은 **클라이언트 깨우기가 아니라
  "장치 스트림이 열려 있다"는 사실 자체**에 붙는다. **버퍼 크기로 못 피한다.**
- **자릿수 교차 검산**: `coreaudiod`의 7일 누적은 18,167s / 633,388s = **코어의 2.87%**.
  "열려 있을 때 5.1% / 닫혀 있을 때 0%"라면 **장치가 56% 시간 열려 있었다**는 뜻이고,
  음악·회의를 쓰는 일반 사용에 대해 무리 없는 값이다. **오늘의 5%는 오늘 부하 때문에 생긴 값이 아니다.**

### C-2-b. ★ 절전 — 이게 CPU보다 크다

프로브를 켜자마자 어서션이 **생겼다**:

```
pid 414(coreaudiod): PreventUserIdleSystemSleep
  named: "com.apple.audio.BuiltInSpeakerDevice.context.preventuseridlesleep"
  Resources: audio-out BuiltInSpeakerDevice
```

프로브를 끄면 **사라진다**(사후 확인 0건). `afplay`에서도 **똑같이** 뜬다.

의미: **디스플레이는 꺼져도 맥은 안 잔다.** 24시간 상주 + 사용자가 절전으로 등록해 쓰는 앱에서
이건 %가 아니라 **"밤새 깨어 있음 vs 자고 있음"**의 차이다.
★ **와트는 측정하지 못했다**(이 기계는 AC 연결이고 `powermetrics`는 sudo가 필요하다) —
**어서션의 존재는 실측, 전력량은 미측정.** 정직하게 구분해 적는다.

그리고 이건 팀이 **방금 고친 위반의 재발**이다. `FramePacing.ApplyDisplaySleepPolicy()`가
`Screen.sleepTimeout`을 `SystemSetting`으로 돌려놓아 원칙 2를 지켰는데,
**이 어서션은 우리가 아니라 `coreaudiod`가 잡으므로 `sleepTimeout`으로는 못 막는다.**

### C-2-c. 지금 빌드가 실제로 깨끗한지 (읽기 전용 확인)

실행 중인 사용자 인스턴스(pid 30572)를 `sample`로 떴다.
- 오디오 IO 스레드 **0개**, `FMOD`/`HALC`/`renderProc` 스택 **0건** (양성 대조: `Unity` 513건 · `Metal` 89건 매치)
- 스레드 **48개** — `Tasklist.md`의 "53 → 48"과 일치
- ★ 다만 `AudioToolbox`/`CoreAudio`/`caulk` **dylib 자체는 여전히 로드돼 있다**(16건).
  `m_DisableAudio`는 **장치와 스레드**를 없애지 나머지 프레임워크 링크를 없애지 않는다. 비용은 없다(주소공간뿐).

### C-2-d. 측정의 한계 (반드시 같이 읽을 것)

- **기계가 조용하지 않았다.** 측정 내내 다른 라운드의 의도적 8코어 부하 시험이 돌아 총 CPU가
  **900~990%**였다. 3-7절 규약의 "조용한 기계" 조건을 **못 지켰다.** OFF가 세 번 다 0.00이고
  ON이 세 번 다 4.2~5.1이라 **판정 방향은 안 흔들리지만, 절대값은 조용한 기계에서 다시 재야 한다.**
- **내장 스피커 기준값이다.** Apple Silicon 내장 스피커 경로는 `coreaudiod` 안에서 스피커 보호·
  다이내믹 처리를 돌린다. 헤드폰/외장 DAC에서는 값이 다를 수 있다(**미측정**).
- **FMOD가 아니라 내 프로브였다.** 프로브는 `memset`만 하고 FMOD는 DSP 그래프를 돈다 —
  즉 **우리 프로세스 쪽 0.19%는 하한**이다. `coreaudiod`의 4~5%는 클라이언트 구현과 무관하다
  (`afplay`로 확인).

## C-3. 과제 3 — "켠다 vs 끈다"가 아니라 "필요할 때만 켠다"가 되는가

### C-3-a. Unity 데스크톱에는 그 API가 **없다** (확인)

- `m_EnableOutputSuspension`(이 프로젝트는 이미 **1**): Unity 공식 문서 —
  *"Automatically suspends audio output after it detects the output has been silent for a long duration
  **(editor only)**."* → **빌드에서 안 먹는다.** `design-sound`의 판정과 독립적으로 같은 결론.
- `AudioSettings.Mobile.StartAudioOutput()/StopAudioOutput()`: **iOS/Android 전용.** 데스크톱 등가물 없음.
- `AudioSettings.Reset()`은 **재구성**이지 **해제**가 아니다.
→ **`m_DisableAudio=false`로 가는 순간, 소리 설정을 OFF로 둔 사용자에게도 장치가 24시간 열린다.**

### C-3-b. 그래서 남는 길은 **네이티브 온디맨드**다 — 그리고 대가를 실측했다

장치를 **열고 3초 쓰고 완전히 닫기**를 5회 반복했다(같은 포화 기계, 즉 비관적 조건):

| 회차 | 인스턴스 생성+Initialize | `Start()` 호출 | **Start → 첫 샘플** | 닫은 뒤 0.3초 `IsRunning` |
|---|---:|---:|---:|---:|
| 1(콜드) | 11.5ms | 28.5ms | 8.1ms | **0** |
| 2~5 | 1.3~2.1ms | 26.1~26.7ms | 9.7~10.0ms | **0** |

- **콜드 총 48ms, 이후 ~38ms.** `design-sound`의 최우선 소리가 **집중 세션 시작·완료**라
  40ms는 지각되지 않는다. 버튼 클릭음(가장 빡빡한 경우)에도 100ms 아래다.
- ★ **닫으면 진짜 닫힌다** — `kAudioDevicePropertyDeviceIsRunning`이 0.3초 안에 0으로 돌아온다.
  **즉 어서션도 함께 풀린다**(C-2-b의 사후 확인 0건이 그것이다).
- **pop 여부는 판정하지 않는다** — 청감 판단이고 나는 못 잰다. `design-sound`에게 넘긴다.
  (다만 열기 시점에 무음을 몇십 ms 흘려 페이드인하면 회피 가능한 종류다.)

### C-3-c. 선택지 3개 — 숫자만 놓는다 (판정은 리더)

| 안 | 상주 비용 | 절전 | 필요한 일 |
|---|---|---|---|
| **A. `m_DisableAudio=false`** | `coreaudiod` **+4~5%/코어 24시간** + 우리 쪽 ≥0.2% | **밤새 안 잠 (원칙 2 재위반)** | 없음(설정 한 줄) |
| **B. 유지 + `Platform/` 네이티브 온디맨드** | **0%** (안 낼 때) / 재생 순간만 | **영향 없음** | macOS: AudioToolbox, Windows: WASAPI 또는 `PlaySound`. `Platform/MacOS`·`Platform/Windows`에 각각, 정책은 플랫폼 중립 위치 |
| **C. 유지 (소리 없음)** | 0% | 영향 없음 | — |

**B의 대가**: 소리 자산이 Unity `AudioClip`이 아니라 **디스크 WAV**(StreamingAssets)가 된다.
DLC 사운드팩도 같은 형태여야 한다 — `design-sound`의 14키 · 6팩 구조와 **충돌하지 않는다**(키→파일 매핑).
**B의 이득**: `design-sound`가 이미 정한 **"기본 OFF"**가 비용 0과 정확히 맞물린다.
A에서는 OFF로 둔 사용자도 24시간 비용을 낸다 — **정책과 비용이 어긋난다.**

## C-4. 플랫폼 영향

- **macOS 영향**: 위 전부가 macOS 실측이다. **프로덕션 변경 0건** → 런타임 영향 0건.
- **Windows 영향: 미확인(이 기계에 Windows가 없다).** 다만 **같은 함정이 있을 근거가 있다** —
  Windows에서 열린 오디오 스트림은 `powercfg /requests`에 **"An audio stream is currently in use"**로
  뜨며 시스템 절전을 막는 것이 널리 보고돼 있다. FMOD는 Windows에서 WASAPI를 쓴다.
  → **검증 명령(관리자 권한)**: 오디오를 켠 빌드 실행 중
  `powercfg /requests` 에 오디오 항목이 뜨는가 / `powercfg /sleepstudy`.
  그리고 **`audiodg.exe`(오디오 장치 그래프)**가 macOS의 `coreaudiod` 자리다 —
  **우리 프로세스 CPU만 보면 여기서도 25배를 놓친다.**
- **모바일**: 해당 없음(스크린샷 백드롭 모드). 단 iOS/Android는 `AudioSettings.Mobile`로
  **온디맨드가 공식 API로 가능**하다 — 데스크톱만 없다.

## C-5. 리더 판정 요청

1. **A(그냥 켜기)는 원칙 2를 다시 깬다.** 방금 고친 `ApplyDisplaySleepPolicy` 위반과 **같은 결과**를
   다른 기구로 만든다. 이 사실을 모른 채 `m_DisableAudio`만 되돌리면 **회귀 테스트가 못 잡는다**
   (어서션 주인이 우리가 아니라 `coreaudiod`라서).
2. **B로 간다면 `dev-platform` 배정이 필요하다** — `Platform/` 소유이고 양 플랫폼 동시 작업이다.
3. **조용한 기계에서 C-2 표를 1회 재측정**할 것을 권한다(3-7절 규약 충족). 방향은 안 바뀔 것으로 보나
   절대값은 재야 한다. 계기는 `docs/perf/`에 넣어 뒀다 — 10분이면 끝난다.
4. **부수 관측(내 라운드 밖, 별도 배정 판단 요청)**: `pmset -g assertions`에
   `UserIsActive … "com.apple.iohideventsystem.queue.tickle.nxevent … process:StickMate"`가 떠 있었다
   (타임아웃 597초, `TimeoutActionRelease`). 사용자 입력 때문일 수도 있고 앱이 HID 이벤트를
   만들어서일 수도 있다. **오디오와 무관하게 절전을 미루는 경로**라 확인할 값어치가 있다.
   나는 사용자가 그때 기계를 만졌는지 알 수 없어 **판정하지 않는다.**
