# StickMate 보안 모델 — 1차 감사·판정 (2026-09-02)

담당: `security`(신설). 방어 전용 — 공격 도구·크랙·우회 코드는 만들지 않았다.
프로덕션 `.cs` **수정 0건**. 이 문서는 감사·판정이고 구현은 리더가 배정한다.

> 사용자 지시(2026-09-02): *"유료결제나 경험치 축적 등 아이템획득에 대한것들도 보안이 중요할거같아.
> 아무나 막 획득하면안되니까"*

---

## 0. 한 줄 결론

**리더의 위협 모델은 옳다 — 단, 두 군데를 정정한다. 그리고 진짜 문제는 "지금 뚫려 있는 것"이 아니라
「유료 경계」와 「성장 경계」가 **이미 같은 함수·같은 파일로 합류하도록 예약돼 있다**는 것이다.**

지금 뚫린 구멍은 없다. 팔고 있는 것이 없기 때문이다. 그러나 `game-architect`의 착수 순서 **4번(v10 스키마)**과
**5번(재화·상점)**을 그대로 실행하면, 그 두 라운드가 끝나는 순간 **환경변수 한 줄로 유료 팩이 열리고,
메모장으로 소유 플래그를 켤 수 있는 상태**가 된다. 막을 시점은 지금이고, 비용은 거의 0이다.

---

# 1. 위협 모델 — 검증 결과

## 1-1. 확인된 것 (리더 초안대로)

| 주장 | 판정 | 근거 |
|---|---|---|
| 네트워크 API 사용 0건 (**우리 코드**) | **참** | `UnityWebRequest`/`HttpClient`/`Socket`/`System.Net`/`WebSocket` 등 23개 패턴 전수 0건 |
| 리더보드·랭킹·소셜·공유 없음 | **참** | `Leaderboard`/`Ranking`/`Social`/`Achievement`/`Steamworks`/`Multiplayer` 전부 0건. `Share` 43건은 전부 `FileShare`·`SharedApplication` 오탐 |
| 세이브가 남에게 보이는 경로 없음 | **참** | 로컬 파일 1개, 전송 경로 0건, 클립보드·스크린샷 내보내기 0건 |
| DLC 6팩은 아직 코드에 없다 | **참** | `Rarity`/`SetBonus`/`coinBalance`/`Currency`/`Purchase`/`Entitle` **전부 0건**. `DLC` 18건은 **18건 전부 주석** |
| 남의 창 제목·파일명을 **그리지** 않는다 | **참** | `.text =` 대입 118건 중 외부 이름이 들어가는 것 0건 |

### 양성 대조 (이 저장소가 반복해 당한 함정이라 전부 붙였다)

- **grep 방식 검증**: 같은 명령이 `class`(234) `void`(1390) `File.`(32)를 찾는다. 추가로 스크래치에
  `UnityWebRequest`를 쓰는 가짜 파일을 만들어 **같은 명령으로 잡히는 것을 확인**했다.
- **`strings` 방식은 한 번 실패했고, 그 실패를 그대로 기록한다.** `Builds/macOS/StickMate.app/Contents/MacOS/StickMate`
  (132KB)에 `strings | grep Unity`가 **0건**을 냈다. 이건 "깨끗하다"가 아니라 **그 파일이 Unity 런처
  스텁**이라는 뜻이다(팀 거짓통과 사례 #7과 같은 함정). 실제 엔진은
  `Contents/Frameworks/UnityPlayer.dylib`(61MB)이고 거기서 `Unity` 21,746건이 나온다.
  **첫 0건을 그대로 보고했다면 그것이 오늘의 10번째 거짓 통과였다.**
- **이미 있는 자동 잠금**: `Tests/EditMode/OfflineFirstNetworkAuditTests.cs`가 소스 파일을 직접 읽어
  금지 API를 스캔하고, **네거티브 컨트롤**(가짜 위반 소스를 같은 스캔 함수에 흘려 잡히는지 확인)까지
  갖췄으며, 전송 계열 화이트리스트가 0건임을 별도 테스트로 고정한다. **좋은 감사다.** 아래 1-2가 그
  감사의 사각지대다.

## 1-2. ★ 정정 1 — "네트워크 0"은 **우리 코드**에 대해서만 참이다. 출하되는 플레이어는 미확인이다

```
ProjectSettings/ProjectSettings.asset:93:  submitAnalytics: 1      ← 켜져 있다
UnityPlayer.dylib                      :  "enableHWStatistics"    ← 엔진에 그 기능이 실재한다
```

`submitAnalytics`는 구 **"Disable HW Statistics"** 의 반대 스위치이고, Unity 스태프 설명으로는
*"deviceInfo(하드웨어 통계)를 보내는 단일 이벤트"*다. 우리 `UnityConnectSettings.asset`은
Analytics를 전부 `m_Enabled: 0`으로 꺼 두었고, 같은 스태프가 *"이제는 Analytics를 안 켜면 같은 효과"*
라고 답했다 — **그래서 안 보낼 가능성이 높다. 그러나 그건 포럼 답변이고 버전 의존적이다.**

**★ 여기서 중요한 것은 결론이 아니라 감사의 사각지대다.**
`OfflineFirstNetworkAuditTests`는 `Assets/_Project/Scripts/` 아래 **`*.cs`만** 읽는다
(`Directory.GetFiles(scriptsRoot, "*.cs", AllDirectories)`). `ProjectSettings.asset`도
`Packages/manifest.json`도 **한 번도 보지 않는다**. 즉 **우리 코드의 0건은 잠겨 있지만, 엔진이 켜 둔
스위치는 그 잠금 바깥에 있다.**

- **판정: 미확인.** 정적 증거만으로는 출하 빌드가 부팅 시 이벤트를 보내는지 확정할 수 없다.
- **확정 방법 1건**: 출하 빌드를 깨끗한 환경에서 첫 실행하며 패킷 캡처. **나는 돌리지 않았다**(앱 실행은
  리더 승인 사항이고 `dev-platform`이 Unity 배치모드를 잡고 있다).
- **왜 중요한가**: `MARKET_LANDSCAPE.md`가 **"네트워크 0 · 권한 0"을 이 앱의 최대 차별점**으로 세워 두었다.
  마케팅이 그 문장을 스토어 페이지에 쓰는 순간, 검증되지 않은 공개 주장이 된다. 경쟁자·리뷰어가
  패킷 한 번 잡으면 끝나는 종류의 주장이다.

## 1-3. ★ 정정 2 — 그리지는 않는다. 그러나 **로그에는 남는다** (출구가 두 개다)

리더가 확인된 사실로 적은 *"남의 창 제목·파일명을 한 글자도 그리지 않는다"* 는 **렌더에 대해서는 참**이다.
그러나 **렌더가 유일한 출구가 아니다.**

**(a) 세이브 경로가 Player.log에 그대로 찍힌다** — 리더의 브리핑대로였다. 실측 확인:
```
Player.log: [성장] 준비 완료 — 스틱메이트 Lv.4 (145/429 XP). 저장 파일=불러옴
            (/Users/kjmoon/Library/Application Support/DefaultCompany/StickMate/stickmate_character.json).
출처: Interaction/CharacterProgressionDirector.cs:82
```
(양성 대조: 같은 grep이 그 로그에서 `/Users/` 4건, `StickMate` 5건을 찾는다 — 탐지력 있음.)

**(b) ★ 더 중요한 것 — 남의 앱 신원이 로그에 들어간다. 양 플랫폼 모두.**

| 플랫폼 | 파일 | 로그에 들어가는 것 |
|---|---|---|
| Windows | `Platform/Windows/WindowsGameProcessProbe.cs:159` → `Win32WindowService.cs:1870` | **`전경 실행 파일={전체 경로}`** — 남의 exe **풀 패스**. `C:\Users\<실명>\...` 가 통째로 |
| macOS | `Platform/MacOS/MacWindowService.cs:1516,1534` → `:1403` | `판정 근거 창 = '{owner}'` — 남의 앱 **이름** |

둘 다 `Debug.Log($"[전체화면판정] ... — {reason}")`로 나간다. Windows 쪽이 더 나쁘다 —
경로에 **Windows 사용자 계정명**이 포함된다.

- **이건 원칙 3 위반이 아니다.** 읽기 전용이고, 최소 권한(`PROCESS_QUERY_LIMITED_INFORMATION`)이며,
  레지스트리는 `KEY_READ`만 쓴다. 그 파일은 **원칙 3을 매우 잘 지키고 있다**(쓰기 API를 선언조차 안 하고,
  그 사실을 테스트가 잠근다). 문제는 침해가 아니라 **로그 위생**이다.
- **왜 지금 말하는가**: 이 팀의 표준 절차가 *"릴리스 빌드를 켜고 Player.log로 확인한다"* 이고,
  버그 신고 때 사용자가 그 로그를 **개발자에게 보낸다**. 그리고 `TEAM.md`가 마케팅 캡처를
  *"깨끗한 테스트 환경에서 — 남의 창 제목·파일명이 찍힌다"* 며 이미 경계하고 있다. **그 경계가
  화면에 대해서만 걸려 있고 로그에는 안 걸려 있다.**

## 1-4. 정정 후의 위협 모델 — 이걸 기준으로 이하 전부를 판정했다

| 위협 | 누가 무엇을 잃는가 | 판정 |
|---|---|---|
| 세이브를 고쳐 레벨·XP·동전을 올린다 | **자기 자신뿐.** 리더보드·거래·멀티 전부 없음(실측) | **막지 않는다** (§4) |
| **유료 팩을 안 사고 쓴다** | **개발자 — 매출** | **★ 여기가 유일한 진짜 위협** (§2) |
| `STICKMATE_UNLOCK_ALL`이 유료 경계를 넘는다 | 오늘: 아무도. **DLC 도입 후: 개발자** | **예약된 사고** (§2-1) |
| `STICKMATE_DEVTOOLS`가 유료 경계를 넘는다 | **아무도** | **반증됨** (§2-3) |
| 로그에 남의 앱 신원·세이브 경로 | 사용자 — 프라이버시(소규모, 로컬) | **싸니까 고친다** (§5-3) |
| 출하 플레이어의 Unity HW 통계 | 사용자 — 프라이버시 / 우리 — 마케팅 주장의 신뢰 | **미확인, 측정 필요** (§5-2) |

---

# 2. 유료 경계 감사 — 이번 라운드의 본체

## 2-1. ★ `STICKMATE_UNLOCK_ALL` — 오늘은 안전하다. 그리고 그게 문제의 전부가 아니다

**릴리스 빌드에서 이 환경변수가 유료 경계를 넘는가? — 오늘은 "넘을 유료 경계가 없다".**

판정 규칙은 실제로 잘 짜여 있다(`Core/EquipmentDebugUnlock.cs`):

```csharp
public static bool ResolveUnlockAll(bool developmentConfiguration, string environmentRaw)
    => developmentConfiguration || StickMateDevTools.ResolveFromEnvironmentValue(environmentRaw);
```

- 릴리스 빌드(`UNITY_EDITOR`·`DEVELOPMENT_BUILD` 둘 다 없음) + 환경변수 미설정 → **닫힘**.
- 그 사실을 `EquipmentDebugUnlockReleaseGateTests`가 순수 함수로 잠근다(에디터에서도 릴리스 값을 재현).
- 사람이 출시 전에 상수를 되돌리는 구조가 아니다 — **컴파일 심볼이 보장한다.** 이건 잘한 설계다.
- 그래도 **릴리스 빌드 + `STICKMATE_UNLOCK_ALL=1`이면 열린다**(의도된 QA 탈출구).
  오늘 그것이 여는 것은 **레벨 게이트뿐**이고, 레벨 게이트는 무료 성장 요소다. **매출 피해 0.**

### 그런데 우회 지점이 하필 여기다

```csharp
// Core/ItemCatalog.cs:233
public bool IsOwned(StickConfig config)
    => !RequiredLevel.HasValue
       || EquipmentDebugUnlock.UnlockAll          // ← 무조건 단락시키는 OR
       || CharacterProgressionModel.Level >= RequiredLevel.Value;

// Core/EquipmentModel.cs:179
public static bool IsItemOwned(EquipmentSlot slot, int itemIndex)
    => EquipmentDebugUnlock.UnlockAll
       || CharacterProgressionModel.Level >= RequiredLevel(slot, itemIndex);
```

**이 두 함수는 "보유"라는 단어 하나에 성장·구매·결제를 전부 담게 되어 있다.** 그리고 그것이
추측이 아니라는 증거가 **코드 주석과 설계 문서 양쪽에 이미 적혀 있다**:

| 출처 | 적혀 있는 것 |
|---|---|
| `ItemCatalog.cs:244` (주석) | 상태 슬롯은 *"훗날 여기에 **가격표**가 들어와도 레이아웃을 두 번 고치지 않게"* |
| `ItemCatalog.cs` 클래스 주석 | *"훗날 판매를 얹을 때 데이터 모양이 이미 맞아 있게"* |
| `docs/UX_SHOP_AND_CURRENCY.md` §3-3 | 카드 상태표에 **S5 `DLC(백엔드 없음)` / S6 `DLC(스토어 배선 후)`** 가 이미 있다 |

**→ 유료 경계는 바로 이 `IsOwned`에 합류하도록 설계돼 있다.** 합류하는 순간
`EquipmentDebugUnlock.UnlockAll ||` 가 **그 위에 그대로 얹힌다.** 그러면
**환경변수 한 줄이 출하 빌드에서 유료 팩을 여는 크랙**이 된다 — 게다가 그 변수 이름은
**저장소 문서 9곳에 적혀 있고**, 형제 변수인 `STICKMATE_DEVTOOLS`는 **부팅 배너가 사용자에게 직접
알려준다**(`AppControlDirector.cs:187`).

> **판정: 오늘 고칠 버그는 없다. 그러나 이건 "나중에 비싸지는 것"의 교과서적 사례다.
> 지금은 `||` 하나를 나누는 일이고, DLC가 올라탄 뒤에는 매출 누수 + 스키마 마이그레이션이다.**

## 2-2. ★ 더 급한 것 — 설계 문서가 **소유 플래그를 평문 세이브에 넣기로** 이미 적어 두었다

`docs/UX_SHOP_AND_CURRENCY.md` **§9-3 (P0, 리더에게 이미 올라간 항목)**:

> *"세이브 — 재화 잔액 + **구매 소유 플래그** + 스탯별 최고 도달 단계 + 유예 기산점이 새 필드다
> → `CharacterSaveStore.CurrentVersion` 올림"*

그리고 §3-7 예외 상태표에 **`DLC 미구매 | DLC 플래그`** 가 있다.

**이 "구매 소유 플래그"가 DLC 소유까지 포함하면, DLC 소유가 평문 JSON 한 줄이 된다.** 실측한 현재 세이브:

```json
{ "version": 9, "level": 6, "currentXp": 433.89, ... "wornHead": "equip.head.fur" }
```
경로: `~/Library/Application Support/Vibelab/StickMate/stickmate_character.json`
(Windows: `AppData/LocalLow/Vibelab/StickMate/`) — **평문, 메모장으로 편집 가능, 경로는 로그에 인쇄됨.**

**두 문서가 각자 옳은 판단을 하면서 같은 지점으로 수렴하고 있다.** `ux-designer`는 "구매했으니 저장해야
한다"는 당연한 결론을 냈고, `EquipmentDebugUnlock`은 "보유 판정은 한 곳이어야 한다"는 옳은 결론을 냈다.
**둘 다 맞다. 합치면 틀린다.** — 이게 `game-architect`가 말한 *"개별이 다 옳아도 합이 어긋난다"* 의 실례다.

## 2-3. `STICKMATE_DEVTOOLS` — 유료 경계를 넘지 않는다 (반증 완료)

주석이 *"일부러 다른 변수로 뒀다"* 고 주장한다. **그 주장은 참이다 — 소비자 목록으로 확인했다.**

`StickMateDevTools.Enabled`를 읽는 프로덕션 코드는 4개뿐이다:
`RunawayDirector`(가출 강제) · `ActionCommandPopover` · `ShapeCoverageGuard`(폴백 표시) ·
`AppControlDirector`(단축키 폴링 게이트). **보유·레벨·아이템 판정 경로에 하나도 없다.**
`EquipmentDebugUnlock`이 `StickMateDevTools`에서 재사용하는 것은
`ResolveFromEnvironmentValue(string)` — **문자열 파서 순수 함수 하나**이고 게이트가 아니다.

DEVTOOLS가 여는 것: 진단 로그(D) / 하드웨어 반응 미리보기(H) / 스트레스 게이지 순환(S) /
할일 알림 데모(J) / 집중 90초 데모(F) / 가출 강제(N).
**XP·레벨을 주는 경로는 없다** — `AddXp` 호출자는 `CharacterProgressionDirector` 하나뿐이고
개발 단축키에서 도달하지 않는다. 즉 "DEVTOOLS로 레벨을 올려 아이템을 연다"는 **간접 경로도 없다.**

- **판정: 현행 유지.** 원칙 1(행동-텍스트 싱크)을 지키려고 만든 게이트지 유료 게이트가 아니다.
  배너가 사용자에게 변수명을 알려주는 것도 **오늘은 무해**하다(여는 것이 전부 연출 미리보기라서).
- **단, §3의 규칙이 적용된다**: 앞으로도 이 게이트에 **보유·결제 판정을 얹지 않는다.**

## 2-4. 지금 존재하는 유료/무료 경계 — 전수

| # | 경계 | 구현 | 우회 가능성 | 오늘의 매출 피해 |
|---|---|---|---|---|
| 1 | 장비 42종 ← `requiredLevel` | `ItemCatalog`/`EquipmentModel` | 세이브 `level` 평문 편집 / `STICKMATE_UNLOCK_ALL=1` | **0 — 무료 성장 요소** |
| 2 | 개발 연출 경로 ← `STICKMATE_DEVTOOLS` | `StickMateDevTools` | 환경변수 | **0 — 팔지 않음** |
| 3 | DLC 6팩 | **존재하지 않음** (18건 전부 주석) | — | **0** |
| 4 | 재화·상점 | **존재하지 않음** | — | **0** |

**결론: 오늘 유료 경계는 코드에 0개다. 그래서 이번 라운드는 "구멍 막기"가 아니라 "형태 정하기"다** —
리더의 브리핑대로다.

---

# 3. ★ 판정 — 유료 경계를 코드로 표현하는 방식 (이번 라운드의 산출물)

## 3-1. 규칙: **소유(entitlement)와 성장(progression)은 함수도 저장소도 공유하지 않는다**

보유를 **3층으로 분리**한다. 지금은 1층만 존재하므로, 지금 나누는 비용이 가장 싸다.

| 층 | 무엇 | 어디에 저장 | 위조되면 | `UNLOCK_ALL`이 건드려도 되는가 |
|---|---|---|---|---|
| **A. 성장 해금** | 레벨로 열리는 것 | 세이브(평문) | 자기 손해뿐 | **✅ 된다** (QA에 필요) |
| **B. 재화 구매** | 동전으로 앞당긴 것 | 세이브(평문) | 자기 손해뿐 | **✅ 된다** |
| **C. 유료 소유** | 실제 돈을 낸 DLC 팩 | **세이브에 절대 넣지 않는다** | **개발자 매출 손해** | **❌ 절대 안 된다** |

**C는 매 실행마다 스토어에 묻고, 메모리에만 둔다.** 파일에 쓰지 않으므로 **위조할 대상이 존재하지 않는다.**
이건 방어를 덧붙이는 게 아니라 **표적을 없애는 것**이고, 그래서 백신·권한·네트워크 어느 선도 건드리지 않는다.

### 코드 형태 (구현은 `coder` 배정 — 여기서는 형태만)

```
IsOwned(item)  →  item.Tier switch {
                      Progression => LevelGate(item) || UnlockAll,   // A·B — UNLOCK_ALL 여기까지만
                      Purchased   => SaveFlags.Has(item.Id) || UnlockAll,
                      Entitled    => Entitlement.Owns(item.PackId),  // C — UnlockAll 도달 불가
                  }
```

핵심은 **`UnlockAll`이 `Entitled` 가지에 문법적으로 나타나지 않는 것**이다. 주석으로 "넣지 마세요"가
아니라 **다른 함수라서 못 넣는 것**이어야 한다 — 이 저장소가 이미 배운 교훈이다
(`EquipmentDebugUnlock`이 상수에서 빌드 구성으로 옮겨간 이유가 정확히 "사람의 기억에 맡기지 않는다"였다).

## 3-2. 감사 테스트 2건 (리더 승인 시 내가 쓸 수 있다 — 정의서 예외 조항)

1. **`EntitlementNotInSaveAuditTests`** — `CharacterSaveStore`의 `SaveData` 필드 이름에
   엔타이틀먼트 계열 토큰(`dlc`/`pack`/`entitle`/`owned` + 팩 ID)이 **없음**을 소스 텍스트로 잠근다.
   ★ **양성 대조 필수**: 가짜 `SaveData`에 `public bool dlcOwnedPackA;`를 넣은 문자열을 **같은 스캔
   함수**에 흘려 잡히는지 확인한다(`OfflineFirstNetworkAuditTests`의 네거티브 컨트롤 방식을 그대로 복제).
2. **`UnlockSwitchScopeAuditTests`** — `EquipmentDebugUnlock.UnlockAll` 참조가 **A·B 계열 함수
   바깥에 나타나지 않음**을 잠근다. 지금 참조는 2곳(`ItemCatalog.cs:235`, `EquipmentModel.cs:180`)뿐이라
   **오늘 쓰면 기대값이 명확하고, DLC가 들어온 뒤엔 기대값 자체가 논쟁거리가 된다.**

---

# 4. 세이브 무결성 — 판정: **그냥 둔다** (감지도 하지 않는다)

**셋 중 "그냥 둔다"를 고른다.** 단, 그 판정은 §3의 C층 분리를 **전제로만** 성립한다.

## 4-1. 근거

1. **피해자가 가해자와 같은 사람이다.** 리더보드·거래·멀티·공유가 전부 **실측 0건**이다.
   세이브를 고쳐 Lv.99가 된 사용자가 빼앗는 것은 **자기 자신의 성장 경험**뿐이다.
   *"누가 무엇을 잃는가"를 붙일 수 없으면 그건 위협이 아니라 불안이다* — 정의서 규칙.
2. **막을 수 있다는 전제가 거짓이다.** 서명·암호화·HMAC 무엇을 쓰든 **키가 클라이언트에 들어간다.**
   서버가 없으면(있어서도 안 된다 — 선 1) 클라이언트 검증은 **속도 방지턱**이지 잠금이 아니다.
   Valve조차 자기 API에 같은 말을 적어 뒀다(§5-1).
3. **★ 백신 오탐이 이 앱에서는 실존 위험이다.** 시장 조사 결론이 *Shimeji를 죽인 건 기능이 아니라
   배포와 백신 경고*다. 난독화·패킹·안티디버그·무결성 자가검사는 **전부 백신이 싫어하는 형태**이고,
   상주 오버레이 앱이 그걸 하면 리뷰 1페이지에 *"이거 바이러스 아니냐"*가 박힌다.
   **보안이 유통을 죽이면 순손실이다.**
4. **★ 오탐 비용이 비대칭이다.** *정당한 유저를 한 명 잠그는 것이 무단 사용 열 건보다 비싸다.*
   이 저장소는 **오늘** `File.Replace` 실패로 세이브가 깨질 뻔했고(`game-architect` I-1, `design-systems`
   경고, Windows 실기 IOException 로그), `.writing` 임시 파일이 **지금 이 순간 디스크에 남아 있다**:
   ```
   stickmate_character.json.67542.writing   (9/2 02:00, 고아 상태로 잔존)
   ```
   **손상된 세이브를 "변조"로 판정하는 코드는, 이 저장소에서 치터보다 우리 버그를 먼저 만난다.**
   그때 사용자에게 *"당신은 치터입니다"* 라고 말하는 것은 우리가 낼 수 있는 최악의 오답이다.

## 4-2. 그래서 하지 않을 것 (명시적으로 기각)

| 조치 | 기각 사유 |
|---|---|
| 세이브 암호화 | 키가 클라이언트에 있다. 얻는 것 0, 잃는 것: 사용자가 자기 데이터를 못 읽음 + 복구 불가 |
| 체크섬/HMAC **+ 거부** | 우리 쓰기 버그 = 사용자 진행 소실. 4-1의 4번 |
| 변조 **감지 후 경고/제재** | 오탐 시 정당한 유저를 모욕한다. 감지해도 **할 수 있는 일이 없다**(피해자가 본인) |
| 난독화·패킹·안티디버그 | 선 3(백신) 정면 위반 |
| 온라인 검증 | 선 1(네트워크 0) 정면 위반. 이 앱의 최대 자산을 태운다 |

## 4-3. 대신 하는 것 (전부 이미 다른 담당이 하고 있거나, 비용 0)

- **저장 원자성**(`game-architect` 착수 1번) — 이건 **보안이 아니라 신뢰성**이고, 이미 최우선이다.
  내 판정은 그 순위를 **지지한다**: 세이브를 못 지키면 §4의 "그냥 둔다"가 성립하지 않는다
  (사용자가 잃은 진행을 되돌릴 방법이 손편집뿐이 되고, 그때 평문 JSON은 **기능이지 결함이 아니다**).
- **평문 유지가 오히려 옳다.** 사용자가 자기 세이브를 백업·복구·이전할 수 있다. 상주 동료 앱의 성격에 맞다.
- **C층(유료 소유)을 파일에 넣지 않는다** — §3. 이것 하나로 이 파일에 지킬 가치가 있는 것이 사라진다.

## 4-4. 개인정보 — 세이브에 사용자 입력이 들어간다 (참고, 조치 불요)

`SaveData.todos` / `todoArchive`는 **사용자가 타이핑한 할일 텍스트**를 평문 저장한다
(`TodoRecord.text`). 현재 실측 세이브는 `[]`로 비어 있다. 로컬 전용이고 전송 경로가 0건이므로
**위협은 아니다.** 다만 **§1-3(a)와 겹친다** — 세이브 경로가 로그에 찍히고, 사용자가 버그 신고 때
로그를 보낸다. 로그에 들어가는 건 경로뿐이고 할일 내용은 아니다. **조치 불요, 기록만.**

---

# 5. 스토어가 이미 해 주는 것 — 1차 출처 대조

## 5-1. Steam — Valve 자신이 "아이템 지급에 쓰지 말라"고 적어 두었다

`ISteamApps` 공식 문서 **직접 인용**:

| API | Valve의 문구 (원문) |
|---|---|
| `BIsDlcInstalled` | **"Should only be used for simple client side checks - not intended for granting in-game items."** |
| `BIsSubscribedApp` | "Only use this if you need to check ownership of another game related to yours, a demo for example." |
| `BIsSubscribed` | "This will always return **true** if you're using Steam DRM or calling `SteamAPI_RestartAppIfNecessary`." |
| `GetDLCCount` | "this value may max out at 64 ... you should set your own internal list of known DLC to check against." |

DLC 페이지는 안전한 확인 방법으로 **서버 + WebAPI 키 + `CheckAppOwnership`**을 제시한다 —
**우리가 쓸 수 없는 방법이다**(선 1).

- ★ **오프라인 동작은 Valve가 문서화하지 않았다.** DLC 문서에도 `ISteamApps` 문서에도
  오프라인 모드·Steam 클라이언트 미실행 시 동작에 대한 서술이 **없다**. → **미확인.**
  (`SteamEncryptedAppTicket`은 존재하나 *"신뢰할 수 있는 서버"*를 전제하므로 우리에게 해당 없음.)
- **그런데 이 미확인은 우리에게 치명적이지 않다.** §3의 C층을 쓰면 판정 실패 시 답이 정해져 있다 —
  **§5-4의 "실패는 관대하게" 규칙.**

## 5-2. ★ MS 스토어 — `docs/strategy/`의 **미확인 1건을 해소했다**

선행 조사에 *"MS 스토어에서 DLC를 팔 수 있는지 자체가 미확인"*으로 남아 있던 항목이다.
**규범 문서(learn.microsoft.com)의 공식 비교표로 확인했다:**

| 기능 | **Packaged (MSIX)** | **Unpackaged (Win32 EXE/MSI 링크 등록)** |
|---|---|---|
| Commerce Platform (payment, **in-apps**, subscriptions, licensing) | **MS 스토어 커머스 사용 가능** | **"Use your own or 3P commerce platform"** |
| Code signing | **MS가 무상 제공** | **개발자가 CA 인증서 구매·부담** |

**→ 답: 팔 수 있다. 단 MSIX로 낼 때만.** "기존 EXE를 링크로 등록"하는 경로는 **애드온을 팔 수 없다.**

### ★ 이게 `ROADMAP.md` 3절의 비용 계산을 뒤집는다

`MARKET_LANDSCAPE.md` 220행이 *"MS 스토어 MSI/EXE 경로의 코드서명 인증서 비용"*을 새로 발견한 비용으로
적어 두었다. **그 비용을 무는 바로 그 경로가, 동시에 DLC를 팔 수 없는 경로다.**
즉 언패키지 경로는 **두 번 손해**이고, MSIX 경로는 **서명 무료 + 애드온 판매 가능**이다.
→ `product-strategy`에 인계할 항목(리더 경유).

### 형태까지 정해 둔다 — **"durable add-on"이지 "DLC"가 아니다**

MS 문서 주의사항 인용: *"Other types of add-ons, such as **durable add-ons with packages (also known as
downloadable content or DLC)** are **only available to a restricted set of developers**."*

**→ 패키지가 딸린 진짜 "DLC"는 우리가 못 쓴다.** 우리에게 맞는 것은 **durable add-on(기본 수명 Forever)**
= **라이선스만 사고 콘텐츠는 본편에 들어 있는 형태**다. 6팩 콘텐츠가 어차피 앱 안에 있으므로 **이게 정확히
우리 모양이다.** 우연이 아니라 §3의 C층 설계와 같은 구조다.

### 확인된 제약 2건

- **"In-app purchase functionality is not currently supported in elevated applications."**
  → StickMate는 승격 실행이 아니다(권한 0). **문제 없음.** 다만 **작업표시줄 원복 예외**(`ABM_SETSTATE`)가
  혹시라도 승격을 요구하게 바뀌면 **결제가 함께 죽는다** — `dev-platform`에 전달할 제약(리더 경유).
- **`Windows.Services.Store`에는 시뮬레이터가 없다.** 문서 명시: 테스트하려면 **먼저 스토어에 게시하고
  기기에 설치해 라이선스를 받아야** 한다. → 결제 배선 라운드의 일정에 이 왕복이 들어간다.

### 남은 미확인 (정직하게 남긴다)

MS 직원 Q&A 답변은 *"MSIX packaged **from an exe file**이면 스토어 기능을 못 쓸 수 있다"* 고 말한다.
규범 표는 "MSIX면 된다"고 하고, Desktop Bridge 앱이 `StoreContext`를 쓰는 방법도 문서화돼 있다.
**Unity가 만드는 exe → MSIX Packaging Tool 경로가 애드온을 지원하는지는 두 출처가 어긋난다 — 미확인.**
결제 채널을 확정하기 전에 **실물로 확인해야 한다.** 나는 Windows 실기가 없어 재지 못했다.

## 5-3. 맥 앱스토어 — 조사 안 함

1차 출시 대상 제외(우선순위 최하, 리더 지시). 참고로 `ProjectSettings.asset:
useMacAppStoreValidation: 0`. 착수 시 영수증 검증이 별도 주제가 된다.

## 5-4. ★ 핵심 질문에 대한 답

> **오프라인 상주 앱에서 "이 사람이 이 팩을 샀는가"를 어디까지 믿을 수 있는가?**

**답: 끝까지는 못 믿는다. 그리고 믿으려고 애쓰면 안 된다.**

- Valve가 자기 API에 *"아이템 지급용이 아니다"* 라고 적어 두었다. 그 이상의 보증은 **서버로만** 살 수 있고,
  서버는 이 앱이 팔지 않기로 한 것이다.
- 따라서 **정직한 목표는 "무단 사용 불가능"이 아니라 "정직한 사용자가 정직하게 살 이유가 있고,
  실수로 잠기지 않는 것"**이다.

### 실패는 **관대하게** — 이 규칙이 §4-1의 오탐 비대칭을 상속한다

| 스토어 조회 결과 | 우리 동작 |
|---|---|
| "샀다" | 연다 |
| "안 샀다" | 잠근다 + 구매 안내(S6) |
| **조회 실패 / 오프라인 / 스토어 미실행** | **★ 직전에 성공했던 판정을 그대로 유지한다. 절대 회수하지 않는다** |

**돈을 낸 사용자에게서 비행기 안에서 팩을 빼앗는 것**은, 안 낸 사용자 열 명이 쓰는 것보다 비싸다.
이건 §4-1의 4번과 **같은 원칙의 다른 적용**이다. 그리고 이 규칙 덕분에 §5-1의 "Steam 오프라인 동작 미확인"이
**설계상 무해**해진다 — 어느 쪽이든 우리 동작이 정해져 있다.

(마지막 성공 판정을 **어디에** 기억할 것인가는 §3의 C층과 충돌한다 — 파일에 쓰면 위조 대상이 생긴다.
**권고: 프로세스 메모리 + "이번 실행에서 한 번이라도 성공했으면 유지". 실행 간 캐시는 만들지 않는다.**
사용자가 오프라인으로 앱을 재시작하면 잠기지만, 스토어 클라이언트가 로컬 라이선스를 들고 있는 것이
정상 경로라 실무상 거의 발생하지 않는다. **이건 판정이 아니라 권고다 — 실기 확인 후 확정할 것.**)

---

# 6. 지금 당장 고칠 것 — **3건** (전부 "지금 안 하면 나중에 비싸진다"에 해당)

## ★ 1순위 — 소유 3층 분리를 **v10 스키마에 못 박는다. ★ v10은 지금 열려 있다**

> ### ★★ 시급도 정정 — 이건 "다음 라운드"가 아니라 **지금 이 순간**이다
>
> 보고서를 쓰는 중에 작업 트리를 다시 쟀다. **v10 스키마 라운드가 이미 돌고 있다:**
> ```
> git diff Core/CharacterSaveStore.cs
>   + internal const int CurrentVersion = 10;      ← 이미 올라가 있다 (커밋 전)
>   + public bool   preferredMonitorSaved;
>   + public string preferredMonitorKey;
> ```
> 지금까지 v10에 들어간 새 필드는 **모니터 선택 2개뿐 — 경제·소유 필드는 아직 0개다.**
> 즉 **아직 안 늦었고, 여유도 없다.** `UX_SHOP_AND_CURRENCY.md` §9-3이 요구하는
> "재화 잔액 + **구매 소유 플래그**"가 이 v10에 추가되는 순간 결정이 굳는다.
>
> **리더에게: 이 항목만은 다른 순위와 달리 "다음에"가 성립하지 않는다.**
> v10을 커밋하기 전에 판정이 필요하다.

- **무엇**: §3-1의 규칙을 확정하고, `UX_SHOP_AND_CURRENCY.md` §9-3의 **"구매 소유 플래그"에서
  DLC 소유(C층)를 제외**한다. **재화 구매 플래그(B층)는 v10에 넣어도 된다** — 위조돼도 자기 손해뿐이다.
  구분선은 *"돈이 오갔는가"* 하나다.
- **왜 지금**: v10 스키마는 `game-architect`가 *"지금 정하지 않으면 v11·v12를 연달아 올리게 된다"*고
  적은 **되돌리기 어려운 결정**이다. DLC 플래그가 v10에 들어가면 빼는 데 **v11 + 하위호환 테스트 1건**이
  든다(CLAUDE.md 명시 규칙). 그리고 그 사이에 나간 빌드의 세이브에는 **위조 가능한 소유 플래그가 남는다.**
- **비용**: 지금 = **필드 하나를 안 만드는 것(0)**. 나중 = 스키마 마이그레이션 + 매출 누수.
- **차단 관계**: 착수 **4번을 막는다.** **새 단계를 추가하지 않는다 — 이미 열려 있는 4번에 제약 한 줄을
  넣는 것이다.**
- **좋은 소식 1건**: `CharacterInfoWindow.Shop.cs`(신규, 66줄)는 **의도적 빈 껍데기**다 —
  *"지금 가격표를 그리면 화면이 아직 정해지지 않은 값을 주장하게 된다"*는 이유로 비워 두었다.
  **상점 표면은 아직 아무 실수도 저지르지 않았다.** 위험은 화면이 아니라 **스키마 쪽에 있다.**

## ★ 2순위 — `submitAnalytics: 1` → `0`, 그리고 오프라인 감사에 `ProjectSettings.asset`을 넣는다

- **무엇**: (a) 한 줄 변경. (b) `OfflineFirstNetworkAuditTests`가 `.cs` 말고
  `ProjectSettings/ProjectSettings.asset`의 `submitAnalytics`와 `UnityConnectSettings.asset`의
  `m_Enabled`도 검사하게 확장. **★ 양성 대조**: 값을 1로 바꾼 가짜 문자열을 같은 검사에 흘려 잡히는지 확인.
- **왜 지금**: (i) 우리 마케팅이 **"네트워크 0 · 권한 0"을 공개 주장**하려 한다. 검증 안 된 공개 주장은
  가장 싸게 반박당한다. (ii) 지금 감사는 이 스위치를 **구조적으로 볼 수 없다** — 누가 나중에 켜도
  아무도 모른다. (iii) 비용이 **한 줄 + 테스트 한 건**이다.
- **주의**: 이 조치로 §1-2의 미확인이 **해소되지는 않는다**(엔진이 이미 보내는지는 여전히 측정 대상).
  다만 **양쪽 스위치가 다 꺼진 상태**가 되어 이후 측정의 기대값이 명확해진다.
- **담당 경계**: `ProjectSettings.asset`은 프로덕션 `.cs`가 아니지만 내 소유도 아니다 — **리더 배정 필요.**

## ★ 3순위 — 전체화면 판정 로그에서 **남의 앱 신원**을 뺀다 (양 플랫폼)

- **무엇**: Windows는 풀 경로 → **파일명만**(또는 해시). macOS는 owner 이름 → **길이/카테고리만**.
  판정 근거로서의 유용성은 유지된다(게임인지 아닌지가 요점이지 어느 게임인지가 아니다).
- **왜 지금**: 사용자가 **버그 신고 때 이 로그를 보낸다.** Windows 경로에는 **계정 실명**이 들어간다.
  나중에 고치면 이미 나간 로그는 회수할 수 없다. 비용은 문자열 두 곳.
- **왜 3순위**: 피해 규모가 작고 로컬이다. 1·2순위가 매출·공개주장에 걸린 반면 이건 위생 문제다.

### 4순위 이하로 내린 것 (지금 하지 않는다)

- `STICKMATE_UNLOCK_ALL` 자체 제거 — **하지 않는다.** QA 절차가 실제로 쓰고 있고, §3의 분리가 끝나면
  A·B층에 남아도 안전하다. **기능을 줄이는 게 아니라 도달 범위를 줄이는 것이 답이다.**
- 부팅 배너의 `STICKMATE_DEVTOOLS` 안내 — **유지.** 여는 것이 전부 연출 미리보기라 오늘 무해하고,
  숨기는 것은 보안이 아니라 은폐다(그리고 저장소 문서 9곳에 이미 적혀 있다).
- 세이브 무결성 조치 일체 — **§4에서 기각.**

---

# 7. `game-architect` 산출물과 맞물리는 지점

`docs/GAME_ARCHITECTURE_REVIEW.md`를 읽고 대조했다. **순서를 바꿀 필요는 없다 — 제약 2개를 끼워 넣는다.**

| 그쪽 항목 | 내 결론이 무는 지점 |
|---|---|
| **§3-2 착수 4번 — v10 스키마** | ★★ **여기가 임계점이고, 이미 열려 있다.** 작업 트리에 `CurrentVersion = 10`이 **커밋 전 상태로 존재한다**(경제 필드는 아직 0개). §6-1의 규칙은 "다음 라운드 전"이 아니라 **이 v10 커밋 전**에 확정돼야 한다. 새 단계가 아니라 **4번의 입력 제약**이다 |
| **§3-2 착수 5번 — 재화·상점** | `UnlockAll` 분리(§3-1)가 **이 라운드까지** 끝나 있어야 한다. 상점이 붙는 순간 `IsOwned`가 결제 판정을 겸한다 |
| **§3-2 착수 1번 — 저장 원자성** | **내 §4 판정의 전제다.** "세이브를 그냥 둔다"는 *세이브가 안 깨진다*를 가정한다. 1번이 미뤄지면 §4를 재검토해야 한다. → **1순위 유지에 찬성** |
| **§1-D — 원칙 4 인프라가 0이다** (`MotionPluginSO`/`EffectPluginSO` 소비자 0곳) | ★ §8의 결정이 **바로 여기에 떨어진다.** 레지스트리를 **지금 새로 짓는다** = 로딩 출처를 정할 **가장 싼 순간**. 임의 경로를 받게 지으면 나중에 닫는 것이 파괴적 변경이다 |
| **§2 되돌릴 수 없는 결정 8건** | 여기에 **9번째**를 추가 제안한다: **"유료 소유를 세이브 스키마에 넣는가"**. 넣으면 되돌리는 데 스키마 버전이 든다 — 그쪽 I-2와 정확히 같은 성격이다 |
| **§4-2 의존 방향이 이미 순환한다** | 엔타이틀먼트 조회는 **플랫폼 전용**(Steam/MS)이다. CLAUDE.md의 *"정책은 플랫폼 중립 위치에"* 규칙이 그대로 적용된다 — **판정 로직은 `Platform/`에, 스토어 SDK 호출은 `Platform/Windows`·`Platform/MacOS`에.** `FullscreenSuspendPolicy.cs` 사고의 재발 자리다 |

**`ux-designer`와의 인계**: §2-2에서 지적한 `UX_SHOP_AND_CURRENCY.md` §9-3은 **그쪽 P0 항목**이라
이미 리더에게 올라가 있다. **그 항목의 "구매 소유 플래그"를 A/B층(재화 구매)과 C층(유료 소유)으로
쪼개 달라는 요구**를 리더 경유로 전달한다. 화면 쪽 영향은 없다 — S5/S6 카드 상태표는 그대로 쓸 수 있다.

---

# 8. 원칙 4 플러그인 통로 — **사용자 결정 대기. 답을 가정하지 않고 두 갈래로 쓴다**

★ 어느 갈래든 공통: **`game-architect` §1-D에 따라 레지스트리 자체가 아직 없다. 지금이 가장 싸다.**

## (가) 봉인 — 매니페스트는 우리만 만든다

- **서명 검증 불필요.** 매니페스트가 **서명된 우리 앱 번들 안에서만** 로드되면 OS 코드서명이 이미 무결성을 보장한다.
- **필요한 것 1가지**: 레지스트리를 **`Resources`/번들 내부에서만** 로드하도록 짓는다. 임의 경로 인자를
  받지 않는다. **비용 0(지금) / 파괴적 변경(나중).**
- **매출 영향 없음.** DLC 팩도 우리 것이므로 §3의 C층으로 충분하다.

## (나) 제작 개방 · 판매 봉인 — 남이 만들되 아무도 팔지 않는다

- **매출 위협 없음** → **서명 검증(코드 신뢰) 여전히 불필요.** 여기가 흔한 오해다: 서명은 **돈이 걸릴 때**
  필요하지, 제작 개방 자체가 서명을 요구하지 않는다.
- **대신 다른 3가지가 필요해진다** (위협의 성격이 매출 → 안전·평판으로 바뀐다):
  1. **매니페스트를 끝까지 선언적으로 유지한다.** 코드·스크립트 참조·파일 경로·URL을 **필드로 두지 않는다.**
     두는 순간 데이터가 아니라 **실행 가능 표면**이 되고, 그때부터 백신이 우리를 본다(선 3).
  2. **로드 시 엄격 검증 + 실패는 조용히 건너뛴다.** 남의 팩 하나가 앱 전체를 죽이면 그 신고는 우리에게 온다.
     (`game-architect`가 지적한 `Append*`에 `default:`가 없어 **조용히 안 그려지는** 문제와 같은 계열 —
     그쪽 착수 2번과 함께 보면 좋다.)
  3. **출처를 화면에 표시한다.** 제3자 콘텐츠가 1st-party로 읽히면 **남이 쓴 대사·조형이 우리 평판이 된다.**
     원칙 1(행동-텍스트 싱크)의 책임 주체가 흐려지는 문제이기도 하다 — `design-narrative` 소관과 겹친다.
- **판정 필요 항목**: 팩이 **대사**를 넣을 수 있는가. 넣을 수 있으면 검열·신고 경로가 필요해지고,
  그건 상주 동료 앱에서 무거운 주제다. **나는 이 질문을 열어 둔다 — `design-narrative`·`product-strategy` 사안.**

## (다) 완전 개방 (판매까지) — ★ 이 갈래만 선 1을 건드린다

**사용자가 이걸 고르면, 그건 보안 결정이 아니라 사업 모델 결정이고 리더가 그렇게 보고해야 한다.**
제3자 판매는 결제 중개·정산·환불·분쟁을 낳고, 그중 무엇도 **네트워크 0으로는 불가능하다.**
`MARKET_LANDSCAPE.md`가 최대 차별점으로 세운 자산을 태우는 선택이다.

- **덜 파괴적인 대안**: 제3자 팩을 **각 스토어의 애드온으로만** 팔게 하고(우리는 중개하지 않는다),
  우리 앱은 §5-4의 스토어 조회만 한다. 이러면 네트워크 0이 유지된다. 다만 스토어 정책상
  제3자가 우리 앱의 애드온을 등록할 수 있는지는 **미확인**이다.

---

# 9. 측정하지 않은 것 (= 이 문서가 보장하지 않는 것)

정직성 규칙에 따라 명시한다.

1. **앱을 실행하지 않았고 빌드하지 않았다.** 리더 승인 사항이고 `dev-platform`이 Unity 배치모드를 잡고 있다.
   → §1-2의 패킷 캡처를 **못 했다.** `submitAnalytics` 실제 송신 여부는 **미확인.**
2. **Steam 오프라인 엔타이틀먼트 실동작 미확인.** Valve가 문서화하지 않았고, 실기 Steam 빌드가 없다.
   (§5-4의 설계가 이 미확인을 무해화하지만, 무해화한 것이지 확인한 것이 아니다.)
3. **MS 스토어 MSIX(Unity exe 경유) 애드온 지원 미확인.** 규범 표와 MS 직원 Q&A 답변이 **어긋난다.**
   Windows 실기가 없어 재지 못했다. **결제 채널 확정 전 실물 확인 필요.**
4. **`design/` 미검토** — 3인이 진행 중이라 읽지 않았다.
5. **EditMode/PlayMode 테스트를 돌리지 않았다.** 배치모드가 잡혀 있다. 위 결론은 전부
   **소스·설정·빌드 산출물·실제 세이브 파일·실제 Player.log의 정적 실측**이다.
6. **모바일(iOS) 경로 미검토.** StoreKit 영수증 검증은 §3의 C층에 네 번째 스토어로 붙는 주제다.
7. ★ **작업 트리가 살아 움직이는 중에 쟀다.** 다른 라운드들이 동시에 돌고 있어 커밋되지 않은 변경이
   **90여 개 파일**에 있다(신규 `CharacterInfoWindow.*.cs` 7개, `CurrentVersion = 10` 등).
   위 실측은 **2026-09-02 이 라운드 시점의 작업 트리 상태**이고, `HEAD`가 아니다.
   특히 §6-1의 v10 관찰은 **다음 커밋에 따라 바뀔 수 있다** — 리더는 판정 직전에 다시 확인할 것.

---

# 10. 플랫폼 영향

- **Windows 영향: 함께 검토함.** §1-3(b) 로그 누출은 **Windows가 더 심각**하다(풀 경로 + 계정명).
  §5-2 MS 스토어 결론은 **Windows 전용**이고 `ROADMAP.md`의 비용 계산을 뒤집는다.
  승격 실행 시 IAP 불가 제약은 **작업표시줄 예외**(`ABM_SETSTATE`)와 인접한다.
  `WindowsGameProcessProbe.cs`의 원칙 3 준수는 **모범 사례**로 확인했다(쓰기 API 미선언 + 테스트 잠금).
- **macOS 영향: 함께 검토함.** §1-3(b)는 macOS에도 있다(앱 이름, 경로보다는 덜 민감).
  §1-2 `submitAnalytics`·§1-1 네트워크 0은 **플랫폼 무관**(프로젝트 설정·공용 코드).
  맥 앱스토어는 1차 출시 제외라 조사하지 않았다(§5-3).
- **★ 신규 코드 없음** — 이번 라운드는 감사·판정이라 프로덕션 `.cs` **수정 0건**, 신규 플랫폼 분기 0건.
  따라서 `PlatformParityAuditTests`에 추가할 항목도 없다.

---

## 부록 — 이 문서가 근거로 삼은 1차 출처

- [ISteamApps Interface (Steamworks Documentation)](https://partner.steamgames.com/doc/api/ISteamApps)
- [Downloadable Content (DLC) (Steamworks Documentation)](https://partner.steamgames.com/doc/store/application/dlc?l=english)
- [How to distribute your Win32 application through Microsoft Store](https://learn.microsoft.com/en-us/windows/apps/distribute-through-store/how-to-distribute-your-win32-app-through-microsoft-store) — 규범 비교표
- [In-app purchases and trials (Windows.Services.Store)](https://learn.microsoft.com/en-us/windows/uwp/monetize/in-app-purchases-and-trials)
- [Create an add-on submission (Partner Center)](https://learn.microsoft.com/en-us/windows/apps/publish/publish-your-app/add-on/what-are-add-ons)
- [Win32 Desktop app - how to use AddOn Subscription (Microsoft Q&A)](https://learn.microsoft.com/en-us/answers/questions/1280704/win32-desktop-app-how-to-use-addon-subscription-(n) — 규범 표와 어긋나는 출처
- [What happened to Disable HW Statistics? (Unity Discussions, Unity 스태프 답변)](https://discussions.unity.com/t/what-happened-to-disable-hw-statistics/806662)

---
---

# T절 — 시계 조작 방어 (유휴 수급 · 오프라인 보상 · 일일 무료 아이템)

**추가 2026-09-03. 기존 §1~§10과 부록은 한 줄도 고치지 않았다 — 이 절은 그 위에 얹힌다.**

> 사용자 지시(2026-09-03): *"치트나 조작방지도(보안) 고려해야해, 강제로 컴퓨터 시계바꿔서
> 획득한다거나 하는행위등"*
> 라운드 중 전제 정정: *"접속하지 않았을때도 오프라인 보상도 생각해야하고,
> 다만 오프라인 최대 한도는 둬야겠지"*

담당: `security`. 방어 전용 — 공격 도구·크랙·우회 코드 0건. **프로덕션 `.cs` 수정 0건.**
`design-systems`가 병렬로 유휴/오프라인 **수치**를 설계 중이다. 이 절은 그 수치가 아니라
**"어떻게 검증할 것인가"**만 정하고, 그쪽 설계에 **구현 가능한 제약 7개(T-D절)**를 건다.

---

## T-0. 한 줄 결론 — 세 문장

1. **앱이 도는 동안의 시계 조작은 완전히 막을 수 있다.** 이 저장소의 수급·XP 코드는 **이미**
   벽시계를 안 쓰고 프레임 델타를 쓴다(실측). 그 성질을 **계약으로 못 박기만 하면** 된다. 비용 0.
2. **재기동을 사이에 낀 오프라인 보상은 로컬로 100% 막을 수 없다.** 검증이 아니라
   **「1회 정산 상한 + 하루 예산 1회 refill」이라는 산술**로 피해를 유한하게 만든다.
   ★ **탐지·거부·경고를 하지 않는다** — 이 앱에서 오탐 비용이 무단 획득보다 비싸다(§4-1-4와 같은 원칙).
3. ★★ **그런데 진짜 판정은 방어 기술이 아니다.** 「시계를 조작해 동전을 번다」가 **누구의 손해인가**는
   전적으로 **회복제를 실화폐로 파는가** 하나에 달려 있다. 안 팔면 §4의 「그냥 둔다」가 그대로 유지되고,
   팔면 **§4와 E-8이 무너진다.** → **T-D-6이 이 절에서 가장 중요한 항목이다.**

---

## T-1. 실측 — 이 앱은 지금 벽시계를 거의 안 읽는다 (이게 출발점이다)

프로덕션 `Assets/_Project/Scripts` 전수. **벽시계를 읽는 코드 지점은 5곳뿐이다.**

| # | 위치 | 읽는 값 | 무엇에 쓰나 | 보상에 걸리나 |
|---:|---|---|---|---|
| 1 | `Platform/ReservedBarRestoreLedger.cs:197` | `DateTime.UtcNow` | 원복 원장의 기록 시각(문자열) | ❌ |
| 2 | `Interaction/CharacterAccessoryRenderer.cs:723` | `DateTime.Now.DayOfWeek` | 요일 연출 | ❌ |
| 3 | `Interaction/CharacterPortraitStage.cs:1457` | `DateTime.Now.DayOfWeek` | 월요일 넥타이 | ❌ |
| 4 | `Core/CharacterStatsModel.cs:75` | `DateTimeOffset.UtcNow` | `DaysTogether`(근속 **표시**) | ❌ (T-D-7 참조) |
| 5 | `Core/CharacterStatsModel.cs:86` | `DateTimeOffset.UtcNow` | `firstRunUnixSeconds` 최초 1회 기록 | ❌ |

> **양성 대조**(0건이 「없다」인지 「스캐너가 못 본다」인지 가른다): 같은 grep이
> `Time.unscaledTime` 계열 **40건 이상**, `Stopwatch` **20건 이상**을 찾는다. 스캐너는 살아 있다.

### 그리고 **지금 있는 시간 기반 수급은 전부 시계 면역이다** — 이건 좋은 소식이고, 정정이기도 하다

| 채널 | 시간 원천 | 실측 위치 | 시계 조작 |
|---|---|---|---|
| 수동 XP(분당) | `Time.unscaledDeltaTime` 누적 | `Interaction/CharacterProgressionDirector.cs:117` | **면역** |
| 집중모드 잔여시간 | `Time.deltaTime` 차감 | `Interaction/FocusWatchDirector.cs:197-198` | **면역**(단 T-D-2 주의) |
| 함께한 시간 | `Time.unscaledDeltaTime` 누적 | `Interaction/CharacterStatsDirector.cs:93,118` | **면역** |

> ★ **`design/systems/ECONOMY_SPEC.md` 0-2-6의 표 한 칸을 정정한다(그쪽 문서는 안 건드렸다 — 읽기만).**
> 그 표는 집중모드를 **「시간 종속(벽시계를 실제로 쓴다)」**로 분류한다. **실재는 벽시계를 안 쓴다 —
> 프레임 델타다.** 결론(*"파밍 불가"*)은 **맞고, 그쪽이 생각한 것보다 더 강한 이유로 맞다**:
> 벽시계를 썼다면 파밍 가능했을 것이다. **분류 라벨을 「단조 시간 종속」으로 바꿔야 한다** —
> 라벨이 틀린 채 남으면 다음 사람이 "이 채널은 벽시계를 쓰니까 새 채널도 벽시계로 쓰자"고 읽는다.

### `Time.timeScale` — 프로덕션 쓰기 **0건**(실측: 히트 4건 전부 테스트)

즉 `Time.deltaTime`도 오늘은 사실상 unscaled다. **그러나 이건 우연이고 계약이 아니다** → T-D-2.

---

## T-2. 공격 시나리오 전수 — 리더 제시 6종 + 내가 찾은 4종

「누가 무엇을 잃는가」를 못 붙이면 위협이 아니라 불안이다(정의서 규칙). 전부 붙였다.

| # | 시나리오 | 필요한 조작 | 1사이클 비용 | 누가 잃나 | 방어 |
|---:|---|---|---|---|---|
| **가** | 시계 **미래** → "하루" 조기 종료 → 무료 회복제 재획득 · 유휴 캡 리셋 | 설정 클릭 몇 번 | ~20초 | 회복제 유료면 **개발자**, 아니면 본인 | **T-4 래칫**(부분) + **T-5 예산 1회 refill**(핵심) |
| **나** | 시계 **과거** → "오늘"을 무한 연장 → 8시간 캡 반복 | 〃 | ~20초 | 본인 | **T-4 래칫 — 완전 차단** |
| **다** | 앱은 안 쓰고 시스템 시계만 진행(부팅 후 방치) | 없음 | — | — | **T-3 — 원리적으로 무효**(우리는 벽시계로 수급하지 않는다) |
| **라** | 세이브 직접 편집(잔액·누적·정산시각) | 메모장 | ~60초 | 본인 | **막지 않는다** — §4·E-8 유지. T-8에 재검토 |
| **마** | ★ **오프라인 보상 소급 파밍** — 시계를 크게 앞으로 → 실행 → 상한 전액 → 반복 | 시계+재실행 | ~30초 | 〃 | **T-6 상한 C + T-5 예산 공유.** 완전 차단 불가 |
| **바** | ★ "마지막 접속 시각"보다 **과거**로 돌려 접속 | 〃 | ~30초 | 〃 | **T-4 — 경과 0으로 clamp, 거부 없음** |
| **사** | ★ **신규** — 부팅 사이 시계 조작 반복(**재부팅으로 단조 앵커를 리셋**) | 시계+**재부팅** | ~65초 | 〃 | ★ **로컬로 막을 수 없다. T-7에 정직하게 적는다** |
| **아** | ★ **신규** — 시간대/서머타임을 이용한 "로컬 자정" 리셋 (조작이 아니라 **정상 여행자도 일으킨다**) | 여행 | 0 | 본인 | **T-D-4 — UTC 고정 오프셋으로 원천 차단** |
| **자** | ★ **신규** — 프레임 델타 가속(`Time.timeScale` 상승, 리플렉션·디버거) | 프로세스 조작 | 높음 | 본인 | **T-D-2**(unscaled 강제) + 「dnSpy가 더 넓은 옆문」(E-8-1) |
| **차** | ★ **신규** — **일일 예산 채널 분리 악용**: 유휴·오프라인·[오늘 할일]이 **각자** 예산을 가지면 (가)의 상금이 채널 수만큼 곱해진다 | 없음(설계 결함) | 0 | 개발자(설계 실패) | **T-D-3 — 단일 예산 강제** |

★ **(다)에 대한 정정**: 리더 브리핑은 이걸 위협으로 들었지만, **우리 설계에서는 위협이 아니라
"아무 일도 안 일어남"이다.** 앱이 안 돌면 프레임 델타가 안 쌓이고, 재기동 정산은 상한 C로 잘린다.
**시스템 시계만 흐르는 것으로는 1동전도 만들 수 없다.** 이건 방어를 추가해서가 아니라
**벽시계를 수급 입력으로 쓰지 않기 때문**이다 — T-3이 그것을 계약으로 만든다.

---

## T-3. 방어 1 — **앱이 도는 동안 벽시계를 수급 입력으로 쓰지 않는다** (완전 방어, 비용 0)

> **T-3-a (필수·금지):** 수급·정산 판정 경로에서 `DateTime.Now` / `DateTime.UtcNow` /
> `DateTimeOffset.Now` / `DateTimeOffset.UtcNow` / `System.Environment.TickCount`를 **읽지 않는다.**
> 예외는 **T-6의 재기동 정산 1지점**뿐이고, 그 지점은 **단일 함수**여야 한다(감사가 셀 수 있게).
>
> **T-3-b (필수):** 세션 내 시간 경과는 **`Time.realtimeSinceStartupAsDouble` 델타**로 잰다.
> 이 저장소는 이미 이 값을 **「단조 증가 시각」**이라 부르고 `Platform/`에서 10곳 넘게 쓴다
> (`FullscreenSuspendPolicy.cs:438` · `RepeatedLogFolder.cs:68` 주석이 그 관례를 명시). **새 관례가 아니다.**
>
> **T-3-c (필수·회피):** `Time.deltaTime`을 쓰지 않는다. 두 가지 이유가 겹친다 —
> (i) `Time.timeScale`에 곱해진다(오늘 프로덕션 쓰기 0건이지만 **계약이 아니라 우연**이다),
> (ii) ★ **`Time.maximumDeltaTime`이 `deltaTime`을 자른다**(MS가 아니라 Unity 공식 문서:
> *"Sets a limit on the value of Time.deltaTime"*). 절전 티어·스톨·시스템 절전에서
> **실제 시간보다 적게 쌓인다.** `unscaledDeltaTime`이 같이 잘리는지는 **Unity 문서에 서술이 없다 — 미확인**
> (양쪽 문서 페이지를 직접 열어 확인했다). 그래서 **논쟁 자체를 없애는** `realtimeSinceStartupAsDouble`을 쓴다.
>
> ★ 오차 방향 주의: 잘림은 **적게 주는** 방향이라 보안 사고는 아니지만 **공정성 사고**다.
> "밤새 켜뒀는데 8시간이 안 찼다"는 신고가 이 경로로 온다.

### 이 하나로 무엇이 끝나는가

- **(다) 무효화** — 시계만 흘러도 아무것도 안 쌓인다.
- **(가)(나)를 「세션 안에서」 완전 차단** — 앱을 켜 둔 채 시계를 아무리 만져도 수급량이 안 바뀐다.
- **(자) 차단** — timeScale 무관.
- **오탐 0** — 검증도 거부도 없다. 다른 숫자를 쓸 뿐이다.

### 미확인 1건 (정직하게 남긴다)

`Time.realtimeSinceStartup(AsDouble)`이 **시스템 시계 변경에 면역인지 Unity 문서가 명시하지 않는다.**
근거는 정황이다 — 양 플랫폼 구현이 단조 카운터(Windows QPC / macOS `mach_absolute_time`)이고,
이 저장소가 이미 그렇게 부르고 있다. **확정 절차**(실기 필요, 우리 머신에서 하면 안 된다 —
사용자 시계를 건드리는 일이다): 별도 머신에서 앱을 켠 채 시계를 +1일 이동 → `[수급]` 로그의
누적 초가 **점프하지 않는지** 확인. 점프하면 T-9-2의 P/Invoke 앵커가 **필수로 승격**된다.

---

## T-4. 방어 2 — **래칫(단조 전진)**: 되돌리기는 이익이 되지 않는다. **처벌도 하지 않는다**

세이브에 들어가는 상태는 **4개뿐**이다(전부 B층 = 평문 세이브, 위조 가능 — 그래도 되는 이유는 T-8).

```
long lastSettledWallUnix;   // 마지막 정산 시각(UTC 유닉스 초). 래칫 대상
int  maxDayIndexSeen;       // 지금까지 본 최대 "우리 날짜" 번호. 래칫 대상
int  dailyBudgetSpentCoins; // 오늘 예산에서 이미 쓴 동전
int  fixedUtcOffsetMinutes; // "하루"의 기준 오프셋(첫 실행 시 1회 고정) — T-D-4
```

### T-4-a. 경과는 **음수가 될 수 없다** (시나리오 바)

```
elapsedSec = max(0, nowWallUnix - lastSettledWallUnix)
```
- **시계를 되돌리면 지급이 0**이고, 그 뒤 `lastSettledWallUnix = min(lastSettledWallUnix, nowWallUnix)`로
  **아래로 재기준한다.** ★ 얼리지 않는다 — 이게 오탐 방어의 핵심이다.
  CMOS 방전·NTP 오작동으로 시계가 과거로 튄 **정상 사용자**가 영구 동결되면 안 된다.
- 선례가 이미 있다: `CharacterStatsModel.cs:79` — `if (elapsed < 0L) return 1;` (근속이 음수가 안 되게).
  **같은 관례를 수급에 확장하는 것이지 새 규칙이 아니다.**

### T-4-b. "우리 날짜"는 **전진만 한다** (시나리오 나 — 완전 차단)

```
dayIndex   = floor((nowWallUnix + fixedUtcOffsetMinutes*60) / 86400)
budgetRefill = (dayIndex > maxDayIndexSeen)
```
- 시계를 과거로 돌려도 `dayIndex ≤ maxDayIndexSeen`이라 **예산이 다시 차지 않는다.**
- **(나)는 여기서 100% 죽는다** — 되돌리기로는 어떤 이익도 만들 수 없다.

### T-4-c. ★ **한 번의 정산이 만들 수 있는 미래는 하루를 넘지 않는다** (오탐 상한을 1일로 고정)

```
if (budgetRefill) { maxDayIndexSeen = min(dayIndex, maxDayIndexSeen + 1); ... }
```

**이 한 줄이 T절 전체에서 가장 값싸고 가장 중요한 오탐 방어다.**

| 상황 | 래칫 없이 | 이 규칙으로 |
|---|---|---|
| CMOS 방전 → 시계가 2099년 → 한 번 실행 → 사용자가 시계 수정 | `maxDayIndexSeen`이 **26,000일** 앞서 박힘 → **평생 일일 보상 0** (조용한 실패, 사용자가 영원히 원인을 모른다) | **+1일**만 앞선다 → 최대 **하루** 멈추고 정상 복귀 |
| 공격자가 2099로 두고 100번 재실행 | — | 100번 실행 = 100사이클 공격. **T-6/T-7과 같은 얘기이지 새 구멍이 아니다** |

**검산**: 어떤 벽시계 입력이 들어와도 1회 정산에서 `maxDayIndexSeen` 전진 ≤ 1,
지급 ≤ `min(C, 남은 하루 예산)`. **입력이 무한대여도 출력이 유한하다** — 이게 검증 없이 얻는 안전이다.

---

## T-5. 방어 3 — **하나의 일일 예산**을 유휴·오프라인·정액 채널이 **공유**한다 (시나리오 차)

> **T-5-a (필수):** 시간 기반 채널(유휴 · 오프라인 · 무료 회복제 · [오늘 할일])은
> **단일 `dailyBudgetSpentCoins`**에서 인출한다. 채널별 별도 예산·별도 카운터를 만들지 않는다.
>
> **T-5-b (필수):** 예산 refill은 **T-4-b의 래칫 전진 시에만**. 「마지막 refill 이후 24시간」 같은
> 상대 시간 방식은 금지 — 그건 벽시계 델타를 다시 신뢰 입력으로 끌어들인다.

**왜 이게 보안 항목인가**: 채널이 3개고 예산이 3개면 (가) 1사이클의 상금이 **3배**가 된다.
그리고 채널은 앞으로 늘어난다(ECONOMY_SPEC 0-2-6이 *"앞으로 이런 채널을 하나 더 붙일 수 있다"*고 적어 뒀다).
**예산을 나누는 결정은 조용히 공격 상금을 곱한다.**

---

## T-6. 방어 4 — 오프라인 정산: **검증하지 않는다. 자른다.** (시나리오 마)

```
// 재기동 직후 1회. 이 함수가 앱 전체에서 벽시계를 읽는 유일한 수급 지점이다(T-3-a).
int SettleOffline(long nowWallUnix, Ledger led, EconomyRates r)
{
    long elapsed = Max(0, nowWallUnix - led.lastSettledWallUnix);   // T-4-a

    int dayIndex = DayIndex(nowWallUnix, led.fixedUtcOffsetMinutes);
    if (dayIndex > led.maxDayIndexSeen) {                            // T-4-b
        led.dailyBudgetSpentCoins = 0;
        led.maxDayIndexSeen = Min(dayIndex, led.maxDayIndexSeen + 1);// T-4-c ★
    }

    long raw      = (long)(elapsed * r.OfflineCoinsPerSecond);
    int  budgetLeft = Max(0, r.DailyBudgetCoins - led.dailyBudgetSpentCoins);
    int  grant    = (int)Min(Min(raw, r.OfflineCapCoins), budgetLeft);   // ★ 3중 clamp

    led.dailyBudgetSpentCoins += grant;
    led.lastSettledWallUnix    = Min(nowWallUnix,                        // 되돌림이면 아래로 재기준
                                     Max(nowWallUnix, led.lastSettledWallUnix));
    return grant;   // 0이어도 정상이다. 경고하지 않는다.
}
```

### 이 함수의 성질 (전부 검산 가능)

| 성질 | 근거 |
|---|---|
| **출력이 항상 유한** | `grant ≤ min(OfflineCapCoins, DailyBudgetCoins)` — 입력 `nowWallUnix`가 무한대여도 |
| **거부·경고·잠금이 없다** | 반환값만 있고 판정 플래그가 없다. **오탐이라는 개념이 존재하지 않는다** |
| **시간대·서머타임 무영향** | UTC 유닉스 초 + 고정 오프셋(T-D-4) |
| **NTP 보정 무영향** | 몇 초의 보정은 `grant`를 몇 동전 움직일 뿐, 어떤 분기도 안 바꾼다 |
| **세이브 필드 부재 시** | `lastSettledWallUnix == 0` → `elapsed`가 거대 → **상한 지급**. 구버전 파일·손상 파일이 **잠기지 않고 관대하게** 끝난다(§5-4 "실패는 관대하게"의 재사용) |

> **T-6-a (필수):** `SettleOffline`은 **순수 함수**여야 한다(시각·요율·원장을 전부 인자로 받는다).
> 이 저장소의 `EquipmentDebugUnlock.ResolveUnlockAll`·`FullscreenSuspendPolicy`가 쓰는 관례이고,
> **시계를 조작하지 않고도 테스트할 수 있는 유일한 형태**다. 정적 시각 읽기가 함수 안에 있으면
> 이 절의 어떤 규칙도 회귀 테스트로 잠글 수 없다.
>
> **T-6-b (필수):** 판정은 **`Platform/` 중립 위치**(또는 `Core/`)에. 플랫폼 전용 코드는
> 사실 조회만(T-9-2를 채택할 경우). — CLAUDE.md의 `FullscreenSuspendPolicy.cs` 사고 재발 자리.

---

## T-7. ★ **막을 수 없는 것** — 정직하게 (시나리오 사)

> **재부팅을 사이에 끼우고 시계를 앞으로 옮기는 반복 공격은 서버 없이 막을 수 없다. 끝.**

로컬에서 **위조 불가능한 양**은 세 개뿐이다:
① 프로세스 단조 시계(`realtimeSinceStartupAsDouble`) — 재기동에서 리셋
② 부팅 단조 시계(`mach_continuous_time` / `QueryInterruptTime`) — **재부팅에서 리셋**
③ 검증 가동시간(프레임 델타 누적 = `TotalCompanionSeconds`의 형제) — 위조 불가하지만 **평문 세이브에 있다**

즉 **재부팅이 우리의 마지막 앵커를 지운다.** 그리고 재부팅은 40초짜리 조작이다.

### 비용·상금 검산 (숫자는 리더 브리핑 유력값 + ECONOMY_SPEC 실측)

- 1사이클 = 재부팅 ~40초 + 시계 변경 ~15초 + 실행·정산 ~10초 ≈ **65초** → **55.4사이클/시**
- 유휴 12동전/분, 오프라인이 그 절반(6/분), 상한 8시간 → **C = 2,880동전/사이클**
- → **159,552동전/시** (55.4 × 2,880)
- **동전 소모처 총액 = 4,550동전**(`ECONOMY_SPEC.md` 0-2-1, 7슬롯 42종 전부 = 650×7)
- → **159,552 / 4,550 = 35.1배.** ★ **상한 C를 아무리 낮춰도 1시간이면 경제가 끝난다.**

> ### 그래서 결론이 뒤집힌다 — **「상한을 낮춰서」 막는 게 아니다. 「살 것이 유한해서」 막힌다.**
> `design-systems`가 C를 2,880으로 하든 288로 하든 공격자는 10배 더 오래 돌릴 뿐이다.
> **진짜 방어선은 「동전으로 살 수 있는 것의 총량이 4,550으로 유한하다」와
> 「그것을 다 사도 다른 사람에게 아무 영향이 없다」이다.** 그건 §4-1-1과 **정확히 같은 논거**다 —
> 피해자와 가해자가 같은 사람이다.
>
> 그리고 그 논거를 **한 가지가 무너뜨린다** → T-D-6.

### 참고 — 레벨 천장은 동전으로 뚫리지 않는다 (그래서 파밍의 상금이 더 작다)

`ECONOMY_SPEC.md` 0-2-7 검산표: **동전이 무한이어도 완주가 최대 2일 앞당겨질 뿐**이고
Lv.30 도달일(천장)은 안 움직인다. **단 이건 「유휴·오프라인이 XP를 주지 않을 때만」 참이다** → T-D-1.

---

## T-8. 세이브 직접 편집(시나리오 라) — **§4의 「그냥 둔다」를 유지한다. 단 근거가 한 겹 얇아졌다**

T-4~T-6의 원장 4필드는 **평문 세이브**에 들어간다. `lastSettledWallUnix`를 0으로 고치면
매 실행 상한 지급이다. **T절의 방어는 시계 조작을 막지, 메모장을 막지 않는다.**

**그래도 §4·E-8을 유지한다** — 이유는 E-8의 3개 그대로다(위조자는 구매자가 아니다 / 검증이
우리 쓰기 버그를 치터보다 먼저 만난다 / 위조가 갉는 것은 자기 경제다).

> ★ **다만 정직한 정정 1건**: E-8-1은 *"평문 JSON을 고칠 줄 아는 사람에게 대안은 결제가 아니라 dnSpy다"*
> 라고 적었다. **시계 조작에는 그 논거가 약해진다** — 시계 바꾸기는 **기술 장벽이 0이고
> 설정 앱에 버튼으로 있다.** 즉 **세이브 편집자 집단보다 시계 조작자 집단이 훨씬 넓고,
> 구매 고객과 겹친다.** 그래서 T절은 §4보다 **한 단계 강하게** 대응한다(T-3~T-6).
> 그것이 이 절이 존재하는 이유이고, **그럼에도 「탐지·처벌」로는 가지 않는 이유**는 §4-1-4와 같다.

---

## T-9. 구현 단계 — 2단계로 나눈다

### T-9-1. **1단계(필수, 1.0)** — P/Invoke 0개, 새 OS 권한 0개, 백신 표면 증가 0

T-3(단조 세션 시계) + T-4(래칫) + T-5(단일 예산) + T-6(3중 clamp 순수 함수).
**새 플랫폼 호출이 하나도 없다.** 선 1(네트워크 0)·선 2(권한 0)·선 3(백신)·원칙 3 전부 무접촉.
추정 규모: 순수 함수 1개 + 세이브 필드 4개 + 호출부 1곳.

### T-9-2. **2단계(권고, 1.0 이후 또는 T-3 미확인이 깨지면 즉시)** — 부팅 단조 앵커

**용도는 「지급을 깎는 것」이 아니라 「관측」과 「T-3의 보험」이다.** 1차 출처로 양 플랫폼 확인 완료:

| 플랫폼 | API | 1차 출처 원문 | 절전 포함 | 시계 변경 면역 |
|---|---|---|---|---|
| **macOS/iOS** | `mach_continuous_time()` | SDK 헤더 `mach/mach_time.h:59` — *"like mach_absolute_time, **but advances during sleep**"* (10.12+/iOS 10+) | **포함** | 예 |
| **Windows** | `QueryInterruptTime()` (kernel32) | MS Learn — *"the interrupt-time count is **not subject to adjustments by users or the Windows time service**"*. 형제 API `QueryUnbiasedInterruptTime`이 *"**does not include** time the system spends in sleep or hibernation"*이므로 **바이어스 포함형인 이쪽은 절전을 센다** | **포함** | 예 |

★ **절전 포함이 결정적이다.** 절전을 안 세는 시계(`mach_absolute_time` / `QueryUnbiasedInterruptTime` /
`Environment.TickCount`)를 쓰면 **노트북을 덮어 둔 밤 8시간이 「0초」로 읽혀서, 가장 흔한 정상 사용자에게
오프라인 보상을 거부하게 된다.** 이건 방어가 아니라 사고다 — **잘못된 API를 고르면 오탐이 기본값이 된다.**

> **★ 실측으로 배제된 후보 1건**: `Environment.TickCount64`는 **이 프로젝트에서 쓸 수 없다.**
> `apiCompatibilityLevel: 6`(= .NET Standard 2.1)이고, Unity 동봉 참조 어셈블리
> `.../NetStandard/ref/2.1.0/netstandard.dll`에 심볼이 **없다**.
> **양성 대조**: 같은 스캔이 `get_TickCount` 1건 · `GetTimestamp` 1건을 찾는다(스캐너 정상).
> `get_TickCount64` · `TickCount64` **0건**. → 남은 선택지는 `Environment.TickCount`(int32,
> **~24.9일에 감김** — 상주 데스크톱에서 흔한 가동시간이라 **부적격**) 또는 위 P/Invoke 2종.

**백신 관점(선 3) 판정: 안전.** 두 API는 커널 시각 조회이고, `S-3`의 문제 조합
(창 열거 + 키 상태 + 레지스트리 + 상주)에 **아무것도 더하지 않는다.** 권한 증가 0.

> **T-9-2-a (필수·설계):** 앵커는 **지급을 줄이는 데 쓰지 않는다.** 앵커가 없거나 이상하면
> **1단계 경로로 그대로 떨어진다**(= 상한 지급). **실패 방향이 항상 관대한 쪽**이어야 한다 — E-1/§5-4와 같은 원칙.
> **T-9-2-b (금지):** *"같은 부팅인데 벽시계가 뛰었다"*를 **거부·경고·되돌림의 근거로 쓰지 않는다.**
> 로그(개인정보 없는 숫자 1개)까지다. ★ **근거**: 「같은 부팅 + 시계 전진」과
> 「짧은 가동 뒤 재부팅 + 진짜 장기 부재」는 우리가 가진 숫자만으로 **구별할 수 없다.**
> 구별할 수 없는 것으로 처벌하면 **정상 사용자를 잠근다.**

---

## T-10. 탐지 시 사용자 경험 — **"탐지"라는 상태를 만들지 않는다**

> ### T-10-a (필수·금지): 이 앱에는 **조작 의심 상태가 존재하지 않는다.**
> 조작 여부를 저장하는 필드, 그것을 화면·로그에 나타내는 문구, 그것에 따라 달라지는 분기를
> **만들지 않는다.** T-4~T-6은 **같은 산술을 모두에게 적용**하고, 조작자는 그 산술에서
> **자연히 적게 받는다.** 이건 관대함이 아니라 **오탐을 만들 수 있는 코드 경로를 아예 안 만드는 것**이다.

| 상황 | 우리 동작 | 사용자에게 보이는 것 |
|---|---|---|
| 시계가 뒤로 갔다 | 지급 0, 아래로 재기준 | **아무 말도 안 한다.** 평소 인사 |
| 경과가 상한을 넘었다(며칠 만·또는 조작) | 상한만큼 지급 | *"그동안 {N}동전 모아 뒀어"* — **넘친 분량을 언급하지 않는다** |
| 예산이 이미 찼다 | 지급 0 | *"오늘 몫은 다 챙겼어"* — 정상 문구. 「캡」·「제한」 단어 회피 |
| 원장 필드가 없다/손상 | 상한 지급 | 평소와 동일 |

### 금지 목록 (하나라도 하면 이 앱의 성격을 배신한다)

**계정 정지**(계정이 없다) · **세이브 무효화·초기화**(원칙 3) · **잔액 회수** ·
**"시간을 조작하셨습니다" 류 문구** · **기능 잠금** · **재실행 강요** · **숨은 페널티 누적**.

★ 근거: §4-1-4에서 실제로 있었던 일 — `.writing` 고아 파일이 디스크에 남아 있었다.
**변조 판정 코드는 이 저장소에서 치터보다 우리 버그를 먼저 만난다.** 그때 정상 사용자에게
*"당신은 치터입니다"*라고 말하는 것이 우리가 낼 수 있는 최악의 오답이다.

### 오탐 방지 체크리스트 (설계로 이미 0이 된 것들)

| 정상 사용자 상황 | 영향 | 왜 0인가 |
|---|---|---|
| 해외여행(시간대 이동) | **0** | UTC + 첫 실행에 고정한 오프셋(T-D-4). 로컬 자정을 안 쓴다 |
| 서머타임 전환 | **0** | 〃 |
| NTP 자동 보정(초 단위) | **0** | 분기를 안 바꾼다 |
| 3주 동안 컴퓨터를 안 켬 | **정상 지급**(상한까지) | 검증도 거부도 없다 |
| 노트북 덮어 두고 밤새 절전 | **정상 지급** | 앵커를 쓴다면 **절전 포함형**만(T-9-2) |
| CMOS 방전으로 시계가 과거 | 그 1회 지급 0, **즉시 정상 복귀** | 아래로 재기준(T-4-a). 동결 없음 |
| 시계가 미래로 크게 튐 → 수정 | **최대 1일** 일일 보상 지연 | T-4-c 래칫 상한 |
| 듀얼부팅·VM·시계 동기 안 되는 환경 | 0~1일 | 〃 |

---

## T-D절 — ★ `design-systems`에 거는 제약 7개 (리더가 전달할 항목)

**전부 "구현 가능한가"로 판정할 수 있게 썼다. 수치는 그쪽 소관이고 형태는 이쪽 소관이다.**

| # | 제약 | 왜 (보안 근거) | 어기면 |
|---:|---|---|---|
| **T-D-1** | ★ **유휴·오프라인 채널은 XP를 주지 않는다.** 동전만. | 오늘 XP는 **전부 프레임 델타**라 시계 면역이다(T-1 실측). 오프라인 XP를 만드는 순간 **레벨 천장이 시계로 뚫리고**, 레벨은 42종 잠금·세트·구간의 **모든** 게이트다. 「동전 무한이어도 완주 2일 단축」(0-2-7)이 **거짓이 된다** | 파밍의 상금이 「동전 4,550」에서 **「게임 전체」**로 커진다 |
| **T-D-2** | **모든 시간 누적은 `Time.realtimeSinceStartupAsDouble` 델타.** `deltaTime`·벽시계 금지 | T-3-c. `maximumDeltaTime` 잘림(공정성) + `timeScale`(보안) | 절전 티어에서 **적게 쌓여** "8시간을 켜뒀는데 안 찼다" 신고 |
| **T-D-3** | **유휴 · 오프라인 · 무료 회복제 · [오늘 할일]이 하나의 일일 예산을 공유** | 시나리오 (차). 채널 수만큼 상금이 곱해진다 | 예산 분리 개수 = 공격 상금 배수 |
| **T-D-4** | **"하루"는 로컬 자정이 아니라 「첫 실행 때 고정한 UTC 오프셋」 기준.** 오프셋은 세이브에 1회 저장하고 이후 자동으로 바꾸지 않는다 | 시나리오 (아). 로컬 자정을 쓰면 **시간대만 바꿔도 무료 리셋**이고, 그건 **정상 여행자도 일으킨다** | 조작 없이도 하루가 두 번 온다 |
| **T-D-5** | **오프라인 상한은 「시간」과 「동전」 둘 다 사양화하되, 코드가 잠그는 하드 상한은 「동전」**:<br>`grant = min(floor(elapsed × rate), C_coins, budgetLeft)` | 리더 질문에 대한 답. **시간은 위조 가능한 입력이고 동전은 우리 출력**이다. 시간에만 상한을 걸면 요율을 바꾸는 순간 상한이 조용히 이동한다 | 요율 조정이 상한을 몰래 바꾼다 |
| **T-D-6** | ★★ **회복제를 실화폐로 팔지 마라 — 또는 판다면 「소모품」이 아니라 「영구 해금(durable)」으로 팔아라** | **아래 별도 절** | **T-7의 결론과 §4·E-8이 동시에 무너진다** |
| **T-D-7** | **`DaysTogether`(근속)에 보상을 걸지 마라** | 근속은 지금 **순수 표시**라 안전하다. 래칫이 없어 시계로 부풀고 줄어든다. 보상을 걸면 **래칫 대상이 하나 더 늘고**, 그 필드는 T-4의 원장 바깥에 있다 | 무방비 채널이 하나 생긴다 |

### ★ T-D-6 상세 — 이 절 전체에서 가장 중요한 항목

**세 가지가 동시에 걸린다.**

**(1) 경제 — 회복제 유료 판매 = 「현금 → 동전」이고, 그건 `ECONOMY_SPEC`이 이미 금지를 권고한 것이다.**
`ECONOMY_SPEC.md` 0-2-7 원문: *"이 설계에는 **현금 → 동전 교환이 없다**. 현금은 DLC 팩만 산다.
… 그럼에도 **도입 금지를 권고한다**."* 회복제(동전 수급 캡 해제)는 **포장만 다른 동전 IAP**다.

**(2) 보안 — 그 순간 「피해자 = 본인」이 깨진다.**
§4·E-8·T-7의 **모든** 판정이 *"세이브를 고쳐도 시계를 돌려도 잃는 것은 자기 경험뿐"*에 기대고 있다.
회복제가 현금 SKU가 되면 **시계 조작은 그 SKU를 무료로 얻는 방법**이 되고, 손해는 개발자에게 간다.
**그리고 우리는 그걸 막을 수 없다**(T-7 — 서버가 없다, 그리고 서버는 선 1이다).
**막을 수 없는 것을 팔지 않는 것이 유일한 방어다.**

**(3) 구조 — 소모품은 C층 계약(§3-1 · E-4)과 원리적으로 충돌한다.**

| | 영구 해금(durable add-on) | 소모품(consumable) |
|---|---|---|
| 잔량 저장 | **불필요** — 매 실행 스토어에 묻는다 | **필요** — 어딘가에 남은 개수를 써야 한다 |
| 우리 세이브 | 안 넣는다(§3-1 C층 = 표적 제거) | **평문 세이브에 잔량이 앉는다 → 표적 부활** |
| 오프라인 | E-1~E-3으로 무해화됨(검증 완료) | 스토어 재고 조회 필요. **Steam Inventory Service의 오프라인 동작 미확인** |
| MS 스토어 | §5-2에서 **durable add-on이 정확히 우리 모양**이라고 확인함 | 재검토 필요 |

> **권고(판정은 리더·`product-strategy`)**: 회복제를 개당 파는 대신
> **「매일 회복제 +1개」 같은 영구 해금**으로 판다. 그러면 **팔리는 것은 소모품이 아니라 요율**이고,
> §3-1 C층 · E-1~E-5 · §5-2가 **한 줄도 안 바뀌고 그대로 적용된다.**
> ★ **다만 이렇게 해도 T-D-6(2)는 완전히 사라지지 않는다** — 유료로 산 요율 상승분을
> 시계 조작으로 흉내 낼 수 있기 때문이다. **다만 그 흉내의 상한이 T-4~T-6으로 유한해진다**는 점이
> 소모품과의 결정적 차이다(소모품은 잔량이 평문이라 **무한하다**).

---

## T-11. 감사 테스트 2건 (리더 승인 시 `security`가 작성 — 정의서 예외 조항)

전부 **소스 텍스트 스캔**이다(활성 빌드 타깃 사각지대 회피 — CLAUDE.md 규칙).
**두 건 모두 네거티브 컨트롤 필수** — `EntitlementAuditSource`의 기존 스캐너를 재사용한다(정규식 0줄).

| # | 테스트 | 잠그는 것 | **오늘의 기대값(실측)** |
|---:|---|---|---|
| 1 | `WallClockReadScopeAuditTests` | 프로덕션에서 `DateTime.Now`/`DateTime.UtcNow`/`DateTimeOffset.Now`/`DateTimeOffset.UtcNow`/`Environment.TickCount` **호출 지점의 파일 집합 등호**. 수급·정산 파일이 그 집합에 **들어오면 실패** | **정확히 4파일 5지점**: `ReservedBarRestoreLedger.cs:197` · `CharacterAccessoryRenderer.cs:723` · `CharacterPortraitStage.cs:1457` · `CharacterStatsModel.cs:75,86`. ★ **부재 단언이 아니라 집합 등호로 쓴다** — CLAUDE.md의 "부재 단언은 썩으면 조용히 초록이 된다" |
| 2 | `IncomeTimeSourceAuditTests` | 수급·정산 타입에서 `Time.deltaTime` 부재 + `realtimeSinceStartupAsDouble` 존재를 **같은 테스트에서 대조** | 오늘 수급 코드 0줄 → **`Assert.Ignore`(사유 포함) + 항상 도는 동반 경보 1건.** `EntitlementFailOpenAuditTests`가 쓴 형태를 그대로 복제하고 `TestClaimExpiryAuditTests` 명부에 등록 |

★ 2번을 **지금 Ignore로 넣어 두는 이유**: 수급 배선 라운드가 이 테스트를 **켜는 것을 잊을 수 없게** 하기 위해서다.

---

## T-12. 플랫폼 영향

- **Windows 영향: 함께 검토함.**
  - **1단계(T-9-1)는 Windows 신규 코드 0줄.** `DateTimeOffset.UtcNow`·`Time.realtimeSinceStartupAsDouble`은
    양 플랫폼 동일 동작이고 플랫폼 분기가 없다 → `PlatformParityAuditTests`에 추가할 항목 없음.
  - **2단계(T-9-2)를 채택하면 Windows 전용 파일 1개가 생긴다** — `QueryInterruptTime`(kernel32).
    그때는 `Platform/Windows/`에 **사실 조회만**, 판정은 `Platform/` 중립에(T-6-b).
    ★ 그 라운드는 **Unity 동봉 Roslyn 크로스 컴파일(`-define:UNITY_STANDALONE_WIN`) 0에러 확인이 필수**다 —
    이 개발 머신에서 Windows 파일은 한 번도 컴파일되지 않는다(CLAUDE.md).
  - `Environment.TickCount`(int32)는 **~24.9일에 감긴다.** Windows 상주 데스크톱은 그 가동시간을 흔히 넘는다
    → **쓰지 마라.** `TickCount64`는 이 프로젝트에서 **애초에 존재하지 않는다**(T-9-2 실측).
- **macOS 영향: 함께 검토함.**
  - 2단계는 `mach_continuous_time()`(libSystem, 10.12+). **하드닝 런타임·공증에 아무 영향 없음**
    (S-1의 엔타이틀먼트 목록에 추가할 항목 0). 권한 증가 0.
  - macOS 절전(뚜껑 닫기)이 **가장 흔한 오프라인 발생 경로**다. 절전을 안 세는 API를 고르면
    **정상 사용자가 오프라인 보상을 못 받는다** — T-9-2의 표가 그 함정을 지목한다.
- **모바일(iPad/iPhone)**: `mach_continuous_time`은 **iOS 10+에도 있다**(같은 헤더). T-3~T-6은 전부
  플랫폼 중립이라 그대로 이식된다. 단 iOS는 앱이 백그라운드에서 정지되므로 **"유휴"의 정의 자체가
  달라진다** — `dev-platform`·`design-systems` 사안이고 이 절의 범위 밖이다.
- **신규 코드 없음(이번 라운드)**: 감사·판정만. 프로덕션 `.cs` 수정 **0건**, 신규 플랫폼 분기 **0건**.

---

## T-13. 이 절이 보장하지 않는 것 (정직성 규칙)

1. **앱을 실행하지 않았고 빌드하지 않았다.** 위 전부 소스·`ProjectSettings`·Unity 참조 어셈블리·
   1차 출처 문서의 **정적 실측**이다. 리더 승인 없이 앱을 띄우지 않는다는 정의서 규칙을 지켰다.
2. **`Time.realtimeSinceStartup`의 시계 변경 면역은 Unity 문서에 명시가 없다 — 미확인**(T-3).
   확정 절차를 T-3에 적었다. **사용자 머신의 시계는 절대 건드리지 않았다.**
3. **`Time.unscaledDeltaTime`이 `maximumDeltaTime`에 잘리는지 미확인** — Unity 문서 두 페이지를
   직접 열었고 **양쪽 다 서술이 없다.** 결론(`realtimeSinceStartupAsDouble`을 쓴다)은 이 미확인에
   **의존하지 않는다** — 논쟁을 없애는 선택이다.
4. **Steam Inventory Service(소모품)의 오프라인 동작 미확인.** T-D-6(3)의 근거는 "미확인이라 위험하다"이지
   "안 된다"가 아니다. 소모품을 팔기로 하면 **실기 확인이 선행돼야 한다.**
5. **T-7의 65초/사이클은 추정이다**(실측 아님). 방향은 바뀌지 않는다 — 10배 느려도 3.5배 초과다.
6. **`design/systems/ECONOMY_SPEC.md`와 `docs/DESIGN_SYSTEMS_STATS.md`는 읽기만 했다**(동시 작업 중).
   T-1의 0-2-6 라벨 정정은 **요청**이지 수정이 아니다.
7. **테스트를 돌리지 않았다.** T-11의 기대값은 grep 실측이고 러너 결과가 아니다
   (E-12-6이 기록한 함정: 재구현과 본 구현이 같은 머릿속에서 나온다). **러너를 반드시 돌려라.**

### 부록 — T절이 근거로 삼은 1차 출처

- [Time.maximumDeltaTime (Unity)](https://docs.unity3d.com/ScriptReference/Time-maximumDeltaTime.html) — *"Sets a limit on the value of Time.deltaTime"*
- [Time.unscaledDeltaTime (Unity)](https://docs.unity3d.com/ScriptReference/Time-unscaledDeltaTime.html) — 잘림 서술 **없음**(미확인의 근거)
- [QueryInterruptTime (Microsoft Learn)](https://learn.microsoft.com/en-us/windows/win32/api/realtimeapiset/nf-realtimeapiset-queryinterrupttime) — *"not subject to adjustments by users or the Windows time service"*
- [QueryUnbiasedInterruptTime (Microsoft Learn)](https://learn.microsoft.com/en-us/windows/win32/api/realtimeapiset/nf-realtimeapiset-queryunbiasedinterrupttime) — *"does not include time the system spends in sleep or hibernation"*
- macOS SDK 헤더 `/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk/usr/include/mach/mach_time.h:59` — *"like mach_absolute_time, but advances during sleep"*
- Unity 동봉 참조 어셈블리 `.../Unity/Hub/Editor/6000.0.82f1/Unity.app/Contents/NetStandard/ref/2.1.0/netstandard.dll` — `TickCount64` 부재(양성 대조 포함)

---
---

# T-14절 — 전제 변경 2건에 따른 재판정 (2026-09-03, 같은 날 2차)

**T절(위)은 한 줄도 지우지 않았다. 무효화된 항목은 아래 표에 「해당 없음」으로 표시하고 이력으로 남긴다.**

> 사용자 확정 2건(리더 경유):
> 1. *"그리고 오프라인시 획득은 그냥 빼줘"* — **앱이 꺼져 있는 동안 골드 획득 0.**
> 2. *"회복제를 하루에 한개 무료제공 하루 넘어가면 없어짐. 유료로 회복제 구매할수 있고 하루에 재화
>    1500개 획득 회복제 사용시 한도 500추가 하루에 총 1000개 추가가능 즉 하루에 회복제사용은 2개까지만"*

```
기본 일일 상한  1,500동전
회복제 1개      상한 +500,  하루 최대 2개  →  절대 천장 2,500동전/일
무료 제공       하루 1개, 이월 없음("하루 넘어가면 없어짐")
나머지          유료 구매
```

**T-D-6(회복제 유료 판매 반대)은 사용자에게 고지됐고 사용자가 감수하고 재고형을 선택했다.
이 절은 그 결정을 되돌리지 않는다 — 그 안에서 최선의 방어를 설계한다.**

---

## T-14-1. 살아 있는 것 / 죽은 것 — 전수 표

| 항목 | 판정 | 근거 |
|---|---|---|
| **(마)** 오프라인 소급 파밍 | ☠ **해당 없음 — 오프라인 획득 삭제로 무효화** | 지급 경로가 사라졌다 |
| **(바)** 마지막 접속 시각 역행 | ☠ **해당 없음 — 동상** | 정산 자체가 없다 |
| **T-6** `SettleOffline` 3중 clamp 순수 함수 | ☠ **폐기** | 함수의 존재 이유가 사라졌다 |
| **T-D-5** 오프라인 상한을 동전으로 잠금 | ☠ **폐기** — 단 **원리는 T-14-5-b로 승계**(상한은 저장값이 아니라 지급 시점 재계산) | |
| **T-9-2** 부팅 단조 앵커 | ⚠ **격하 → 선택.** 오프라인 정산용으로는 불필요. **T-14-4의 크로스 세션 간격 검증에는 여전히 유일한 수단** | 절전 포함 API 판정(macOS `mach_continuous_time` / Win `QueryInterruptTime`)은 채택 시 그대로 유효 |
| **(가)** 시계 미래 → 하루 조기 종료 | ✅ **살아 있다. 그리고 최우선 위협이 됐다** | "하루"가 일일 상한·무료 회복제 **양쪽**의 경계다 |
| **(나)** 시계 과거 → 오늘 연장 | ✅ **살아 있다** | T-4-b 래칫이 계속 막는다 |
| **(다)** 시계만 진행 | ✅ **여전히 무효**(우리는 벽시계로 수급하지 않는다) | T-3 |
| **(라)** 세이브 편집 | ✅ **살아 있다. 회복제 필드가 생기며 표면이 넓어졌다** | T-14-5 |
| **(사)** 재부팅 + 시계 전진 반복 | ✅ **살아 있다 — 그런데 이제 재부팅이 필요 없어져서 더 싸졌다** | T-14-3 |
| **(아)** 시간대/서머타임 로컬 자정 리셋 | ✅ **살아 있다. 여전히 정상 여행자도 일으킨다** | T-D-4 유지 |
| **(자)** `timeScale` 가속 | ✅ 살아 있다 | T-D-2 유지 |
| **(차)** 채널별 예산 분리 | ✅ 살아 있다 | T-D-3 유지 |
| **T-3** 단조 세션 시계 | ✅ **전부 유효.** 오프라인이 없어져 **오히려 커버리지가 100%가 됐다** | 이제 수급이 전부 세션 내에서만 일어난다 |
| **T-4** 래칫(경과 음수 clamp · `maxDayIndexSeen` 전진만 · **1회 정산 1일 상한**) | ✅ **전부 유효. 이제 유일한 날짜 방어다** | T-14-3 |
| **T-5** 단일 일일 예산 | ✅ 유효 — **회복제가 이 예산의 상한을 올리는 형태로 합류** | |
| **T-10** 탐지 UX(플래그·페널티 안 만들기) | ✅ 전부 유효 | |
| **T-D-1·2·3·4·7** | ✅ 전부 유효 | |
| **T-D-6** 회복제 유료 반대 | ⚠ **사용자 결정으로 종결.** 근거는 T-14-7이 **정량으로 완화**한다 | |

> ★ **리더 브리핑에 대한 정정 1건**: *"온라인 세션 내 시나리오만 남는다"*는 **절반만 맞다.**
> **(사)는 죽지 않았고, 오히려 재부팅이 필요 없어져 20초짜리 공격으로 싸졌다** — T-14-3.
> 그리고 **T-4 래칫은 (마)(바)용이 아니라 (가)(나)용**이라 오프라인 삭제와 무관하게 그대로 살아 있다.

---

## T-14-2. ★ 새 최악 시나리오 **(카)** — 세션 내 날짜 롤오버 반복

오프라인 획득이 사라져도 **"하루"라는 개념은 안 사라진다.** 오히려 이제 **하루가 유일한 경계**다:
① 일일 상한 1,500 리셋 ② **무료 회복제 1개 재지급**.

```
09:00  앱 실행. 오늘 상한 1,500 소진, 무료 회복제 1개 사용(→ 2,000)
09:01  ★ 앱을 켜 둔 채 OS 설정에서 날짜를 +1일  → dayIndex 전진 → 상한 리셋 + 무료 회복제 1개
09:02  또 +1일 …  (재실행 불필요 · 재부팅 불필요 · 관리자 권한 불필요)
```

- **1사이클 ≈ 20초.** T-7의 재부팅 공격(65초)보다 **3.3배 싸다.**
- **T-4-c(1회 정산 최대 1일 전진)는 반복을 막지 못한다** — 매 사이클이 각각 "1일 전진"이다.
- ★ **`design-systems`가 지금 만드는 수치 설계에 이 시나리오가 반드시 들어가야 한다.**

### T-14-3. 방어 — **refill 사이에 「단조 시계로 잰 최소 간격」을 요구한다**

> **T-14-3-a (필수):** `maxDayIndexSeen` 전진(= 일일 예산 refill + 무료 회복제 재지급)은
> **직전 refill 이후 단조 시계로 `MinRefillGapSeconds`가 지났을 때만** 허용한다.
> 권고값 **20시간(72,000초)**.
>
> ```
> // 세이브가 아니라 프로세스 메모리에 둔다(위조 대상 만들지 않기 — §3-1의 표적 제거와 같은 논리)
> static double _lastRefillMonotonic = double.NegativeInfinity;
>
> bool MayRefill(int dayIndexNow, int maxDaySeen, double nowMonotonic)
>     => dayIndexNow > maxDaySeen
>        && (nowMonotonic - _lastRefillMonotonic) >= MinRefillGapSeconds;
> ```

| 사용자 | 이 규칙에 걸리는가 | 왜 |
|---|---|---|
| 종일 상주(설계 목표 사용형) | **안 걸린다** | 자정 refill 간 단조 간격 = 24h ≥ 20h |
| 하루 몇 시간만 켜는 사용자 | **안 걸린다** | 앱을 껐다 켜면 `_lastRefillMonotonic`이 `-∞`로 초기화 → 첫 refill은 **항상 통과** |
| **(카) 공격자** | ★ **완전히 걸린다** | 같은 프로세스 안에서 20초 간격 재시도 → 20h 미달 → **차단** |

★ **오탐 0인 이유가 구조적이다**: 이 규칙은 **"한 프로세스 수명 안에서 두 번째 이후의 refill"에만** 적용된다.
프로세스를 새로 켜면 무조건 1회는 통과한다. **정상 사용자를 잠글 수 있는 경로가 문법적으로 없다.**

### T-14-3-b. 우회와 그 비용 — 정직하게

| 우회 | 공격자 비용 | 우리 대응 |
|---|---|---|
| 매 사이클 **앱 재시작** | ~30초 | **부팅 단조 앵커(T-9-2)를 채택하면 막힌다** — 앵커는 프로세스를 건너 살아남는다 |
| 매 사이클 **재부팅** | ~65초 | ★ **막을 수 없다.** T-7 그대로 |

> **T-14-3-c (권고, 필수 아님):** 앱 재시작 우회까지 막으려면 T-9-2의 부팅 앵커가 필요하다.
> **비용은 P/Invoke 2개**(`mach_continuous_time` / `QueryInterruptTime`, 둘 다 절전 포함형·권한 0·백신 무관).
> **다만 T-14-7의 피해 상한 검산을 보면 이 투자는 정당화되지 않는다** — 채택 판정은 리더에게 넘긴다.
> **1.0에는 T-14-3-a(무료·순수 로직)만 넣는 것을 권고한다.**

---

## T-14-5. 재고 위조 방어 — **설계의 핵심은 「재고를 저장하지 않는 것」이다**

### T-14-5-a. ★★ 사용자 결정 *"하루 넘어가면 없어짐"*이 **그 자체로 최고의 재고 방어다**

이 결정이 없었다면 세이브에 `potionStock: N`이 생기고 **`9999`로 고치면 무한**이었다.
이월이 없으므로 **누적 재고라는 개념이 존재하지 않고**, 저장할 것은 **오늘의 소비 상태뿐**이다.

> **T-14-5-a (필수):** **유료로 산 회복제도 당일 소멸이어야 한다.**
> 유료분만 이월되면 `paidPotionStock` 필드가 부활하고 **T-14절 전체가 무의미해진다.**
> ★ 이건 수치가 아니라 **구조 제약**이다 — `design-systems`·`product-strategy` 양쪽에 걸린다.

### T-14-5-b. 세이브 필드 — **4개. 그리고 전부 「지급 시점 재계산 + clamp」로만 쓴다**

```
int  dayIndex;                 // 래칫(T-4-b·T-4-c)
int  todayGrantedCoins;        // 오늘 지급된 동전
byte potionsUsedToday;         // 0~2
bool freePotionClaimedToday;   // 무료분 수령 여부
// ★ 재고(개수) 필드는 없다. 유료 회복제 재고는 스토어가 보관한다(아래 T-14-5-d).
```

> **T-14-5-b (필수·구현 규범):** **저장된 값을 그대로 믿지 않는다. 읽는 즉시 clamp하고,
> 상한은 저장하지 않고 매번 재계산한다.**
>
> ```
> // 로드 직후 무조건. "복구"가 아니라 "정규화"다 — 실패도 경고도 없다.
> potionsUsedToday  = Clamp(potionsUsedToday, 0, MaxPotionsPerDay);      // 2
> todayGrantedCoins = Clamp(todayGrantedCoins, 0, HardCeilingCoins);     // 2,500
>
> // 상한은 필드가 아니라 함수다. 파일에 "오늘 상한" 같은 필드를 만들지 않는다.
> int DailyCap() => BaseDailyCap + PotionBonus * potionsUsedToday;        // 1500 + 500*n ≤ 2500
> int Grant(int want) { int room = Max(0, DailyCap() - todayGrantedCoins); ... }
> ```
>
> **효과(검산)**: `potionsUsedToday = 9999`를 써 넣어도 `DailyCap()`은 **2,500에서 멈춘다.**
> 위조로 넘길 수 있는 최대는 **하루 1,000동전**(회복제 2개분)이고, 그중 500은 무료분이라
> **순이득은 하루 500동전 = 유료 회복제 정확히 1개**다.
>
> ★ 그리고 **이 clamp는 방어가 아니라 위생이다** — 손상된 세이브·구버전 파일·`.writing` 잔해에도
> 같은 코드가 답을 낸다. §4-1-4가 경고한 「우리 버그를 치터보다 먼저 만나는」 코드가 **아니다**
> (거부하지 않고 값만 다듬기 때문).

### T-14-5-c. ★ 그런데 정직하게 — **회복제 필드는 이 파일에서 가장 약한 공격 벡터다**

같은 평문 파일 안에 **더 넓은 옆문이 세 개** 있고, 전부 회복제 위조보다 **쉽고 강력하다.**

| 벡터 | 편집 횟수 | 얻는 것 | 우리 clamp로 막히나 |
|---|---:|---|---|
| `potionsUsedToday`를 고친다 | 1 | **하루 +500동전**(순이득) | **막힌다**(상한 2,500) |
| `todayGrantedCoins = 0` | 1 | 하루 상한 **전액 재수령**(2,500) | ❌ 못 막는다 |
| **`coinBalance = 999999`** | **1** | ★ **경제 전체(4,550) 즉시** | ❌ 못 막는다 |

> **판정: 회복제 필드에 「추가 방어」를 붙이는 것은 순손실이다.**
> 잔액 직접 편집이 100% 우월한 상황에서 재고 필드만 서명·암호화하는 것은
> **자물쇠 옆의 열린 창문**이다. §4·E-8의 판정을 **회복제 필드에도 그대로 적용한다.**
> 우리가 하는 것은 **T-14-5-b의 clamp 한 줄**뿐이고, 그건 방어비용이 사실상 0이다.

★ **리더 질문에 대한 답 —** *"재고 위조는 시계와 무관하게 세이브 편집만으로 되므로 오히려 더 쉬운 공격일 수 있다"*:
**기술적으로는 맞고, 위협 크기로는 틀리다.**

| 축 | 시계 조작 (카) | 세이브 편집(재고 위조) |
|---|---|---|
| 기술 장벽 | **0** — OS 설정의 버튼 | `%APPDATA%`/`Application Support`를 찾아 JSON 편집 |
| 인구 | **전 사용자** | **좁다.** 구매 고객과의 겹침이 작다(E-8-1) |
| 반복 비용 | 20초 | 편집 1회 후 무한 |
| 최대 이득 | 하루 경계 무한 반복 | **잔액 직접 편집이 이미 상한**(회복제 위조는 그 부분집합) |
| 우리 대응 | **T-14-3-a로 세션 내 완전 차단** | **T-14-5-b clamp까지. 그 이상은 안 한다** |

→ **회복제 재고 위조는 새 취약점이 아니라 「이미 열려 있는 문」의 좁은 부분집합이다.**
**T-8의 정정(*시계는 장벽 0이라 §4보다 강하게 대응한다*)은 시계에만 적용되고 세이브 편집에는 적용되지 않는다.**

### T-14-5-d. 유료 회복제의 소유 판정 — E-13을 재고형에 맞게 재적용

사용자가 재고형을 골랐으므로 E-13-a(요율 판매)는 폐기된다. **그러나 E-13-1의 표는 유효하고,
거기서 나오는 결론이 하나 있다:**

> **T-14-5-d (권고):** 유료 회복제의 **미소비 재고를 우리 세이브에 쓰지 않는다.**
> 스토어 소모품(Steam Inventory Service / MS consumable add-on)이 재고를 보관하고,
> 우리는 **소비 시점에 스토어에 소비를 알린다.** → **우리 파일에 위조 대상이 존재하지 않는다**
> (§3-1 C층의 「표적 제거」를 소모품에 그대로 적용). **★ 오프라인 동작은 여전히 미확인**(E-13-3).
>
> **T-14-5-e (필수·순서):** 소비 순서는 **① 우리 상한 +500 → ② 스토어 소비 통지**다. 반대로 하지 않는다.
> 근거는 §4-1-4의 **오탐 비대칭**이다 —
> - 순서 ①②: 중간에 죽으면 **개발자가 500동전 손해**. 그리고 `potionsUsedToday ≤ 2` clamp 때문에 **하루 2,500에서 멈춘다(유한).**
> - 순서 ②①: 중간에 죽으면 **유저가 돈 낸 회복제를 잃는다.** ← 이 앱에서 낼 수 있는 최악의 오답.
>
> **T-14-5-f (필수):** 오프라인/스토어 미실행이면 **유료 구매만 막히고 무료 1개는 정상 동작한다.**
> 문구는 E-2-a를 따른다 — *"안 샀습니다"*가 아니라 *"지금 확인할 수 없습니다"*.

---

## T-14-6. 「하루 2개」 제약의 방어 가치 — **정상 경로엔 절대 천장, 위조 경로엔 clamp 자리**

리더 질문 3에 대한 답. **두 가지를 나눠야 한다.**

1. **정상 경로**: `1,500 + 500×2 = 2,500`이 **절대 천장**이다. 무제한 회복제였다면 상한이라는 개념이
   없어지고 (카) 공격의 1사이클 상금이 무한이 된다. **하루 2개 고정은 큰 방어 이득이다.**
2. **위조 경로**: 직접적으로는 도움이 **안 된다**(필드를 고치면 그만). **그러나 「코드에 하드 상한을
   박을 자리」를 준다** — 그게 T-14-5-b의 clamp이고, 그 clamp가 **위조 순이득을 하루 500동전으로
   묶는다.** ★ **상한이 명시되지 않았다면 clamp를 쓸 근거 자체가 없었다.**

> **T-14-6-a (필수):** `MaxPotionsPerDay = 2` · `HardCeilingCoins = 2500`을 **상수 1곳**에 두고,
> 감사·테스트가 **그 상수를 참조해서** 검증한다(숫자를 베끼지 않는다 — CLAUDE.md 확정 규칙,
> 2026-09-01에 `MaxCharacterScale`·스키마 버전으로 4건 깨진 사고).

---

## T-14-7. ★★ 피해 상한 검산 — **유한하다. 그리고 그 이유는 방어가 아니라 소모처다**

전제 수치: 소모처 총액 **4,550동전**(7슬롯 42종 = 650×7, `ECONOMY_SPEC` 0-2-1) ·
일일 상한 1,500 / 회복제 최대 2,500 · **팩은 동전 경제에 접촉하지 않는다**(`ECONOMY_SPEC` 확정 DS-3:
*"팩 구매 시 6종 전부 0동전 즉시 해금"*) → **소모처는 팩이 몇 개 붙어도 4,550으로 유한하다.**

| # | 검산 | 값 |
|---:|---|---|
| 1 | 완주 소요(회복제 없이) | 4,550 / 1,500 = **3.03일** |
| 2 | 완주 소요(회복제 2개 매일) | 4,550 / 2,500 = **1.82일** |
| 3 | 회복제 1개의 상대 가치 | 500 / 4,550 = **카탈로그의 11.0%** |
| 4 | **유료 회복제의 총 시장(사용자 1인당)** | 완주까지 최대 3일 × 유료 1개/일 = **최대 3개** |
| 5 | 필드 위조의 순이득(clamp 적용 시) | **하루 500동전 = 유료 회복제 1개** |
| 6 | **(카) 시계 조작의 포화점** | 4,550 / 500(=하루 무료 회복제 1개분) = **9.1회** |

> ### → **시계를 9번 돌리면 살 것이 없어진다.** 그 이상 돌릴 이유가 존재하지 않는다.
> **(카)로 잃는 최대 매출 = 사용자 1인당 유료 회복제 약 3개 값**(검산 4·6이 독립적으로 같은 자리에 떨어진다).
> 그건 **정상 사용자 1명이 평생 낼 수 있는 회복제 대금의 전부**이기도 하다.

**판정: 이 구조에서 「탐지·처벌·서버 검증」은 정당화되지 않는다.**
`T-14-3-a`(무료·순수 로직·오탐 0) + `T-14-5-b`(clamp 한 줄)까지가 비용 대비 정당한 전부다.
T-9-2 부팅 앵커는 **P/Invoke 2개를 들여 3개어치 매출을 지키는 일**이라 **1.0에서는 권고하지 않는다.**

### ★ 그러나 이 결론을 무너뜨리는 조건이 **정확히 하나** 있다 — 리더가 지켜야 할 선

> **T-14-7-a (★ 되돌릴 수 없는 결정 후보):** **동전 소모처를 무한하게 만들지 마라.**
> 소모성 반복 구매(예: 동전으로 사는 소모품·뽑기·재도전권)를 넣는 순간 **4,550이라는 천장이 사라지고,
> 위 검산 6개가 전부 무효가 된다.** 그때는 (카)의 상금이 무한이 되고,
> **우리는 그것을 로컬로 막을 수 없다**(T-7).
>
> **오늘 이 천장은 우연이 아니라 확정 판정이다** — `ECONOMY_SPEC` DS-3이 팩을 동전 경제에서
> 명시적으로 떼어 놨다. **그 판정이 곧 이 절의 보안 근거다.** 되돌리려면 T-14절 전체를 다시 써야 한다.

---

## T-14-8. `design-systems`에 거는 추가 제약 (T-D-8 ~ T-D-12) — T-D-1~7은 그대로 유효

| # | 제약 | 왜 | 어기면 |
|---:|---|---|---|
| **T-D-8** | **유료 회복제도 당일 소멸.** 미사용분 이월·비축 금지 | T-14-5-a. 이월이 있으면 `paidPotionStock` 필드가 생기고 **평문 무한 재고**가 된다 | T-14절 전체가 무의미 |
| **T-D-9** | **일일 상한은 필드가 아니라 함수**: `1500 + 500×clamp(used,0,2)`. "오늘의 상한"을 세이브에 저장하지 않는다 | T-14-5-b. 저장된 상한은 위조 대상, 재계산된 상한은 아니다 | 위조 순이득이 무한 |
| **T-D-10** | **refill(일일 상한 리셋 + 무료 회복제 재지급)은 「하나의 사건」**이다. 두 개를 다른 조건으로 나누지 마라 | 나누면 (카) 공격이 두 경계를 각각 넘어 **상금이 2배**가 되고, T-14-3-a를 두 곳에 걸어야 한다 | 방어가 한쪽에만 걸린다 |
| **T-D-11** | **`MinRefillGapSeconds`(권고 20h)를 수치 설계에 포함**하라. 이건 밸런스 수치가 아니라 **방어 파라미터**지만 정상 사용자 곡선에 닿는다 | T-14-3-a. 20h는 "자정 기준 하루 1회"와 충돌하지 않는다(간격 24h) | 값이 너무 크면 정상 사용자를 잠근다 |
| **T-D-12** | ★ **소모처 총액 4,550의 유한성을 깨지 마라** — 동전으로 사는 반복 구매품을 만들지 않는다 | **T-14-7-a. 이 절 결론 전체의 전제다** | 피해 상한이 유한 → 무한 |

### ★ 별건 — `design-systems`에 참고로 넘기는 산술 (내 소관 아님, 방어의 전제라서 적는다)

`ECONOMY_SPEC`의 기존 원형 B 일일 수입은 **64동전**이다. 새 상한 **1,500**은 그 **23.4배**이고,
**소모처 전체(4,550)를 3.03일에 소진한다**(검산 1). 즉 **경제가 3일에 끝난다.**
- 보안 관점으로는 **좋다** — 소모처가 빨리 마르면 (카)의 상금이 그만큼 빨리 0이 된다(검산 6).
- 그러나 그 말은 **유료 회복제의 총 시장이 사용자당 3개**라는 뜻이기도 하다(검산 4).
  **판매 가치와 파밍 위험이 같은 숫자에서 나온다** — 둘 중 하나만 조정할 수 없다.
- ★ **판정은 `design-systems`·`product-strategy`·리더 몫이다.** 나는 *"어느 값을 고르든
  T-14-7의 검산을 다시 돌려야 한다"*까지만 말한다.

---

## T-14-9. 감사 테스트 — T-11의 2건에 1건 추가 (전부 리더 승인 후 작성)

| # | 테스트 | 잠그는 것 | 기대값 |
|---:|---|---|---|
| 1 | `WallClockReadScopeAuditTests` | (T-11 그대로) 벽시계 호출 **파일 집합 등호** | **4파일 5지점** |
| 2 | `IncomeTimeSourceAuditTests` | (T-11 그대로) 수급 타입의 `deltaTime` 부재 ↔ `realtimeSinceStartupAsDouble` 존재를 **같은 테스트에서 대조** | 오늘 0줄 → `Assert.Ignore` + 동반 경보 |
| 3 | ★ **신규** `DailyLimitClampAuditTests` | (ㄱ) 세이브 로드 경로에 `potionsUsedToday`·`todayGrantedCoins`의 **clamp가 존재**한다 (ㄲ) 상한이 **저장 필드가 아니라 함수**다 = `SaveData`에 `dailyCap`/`todayLimit` 계열 필드 **부재** — `EntitlementNotInSaveAuditTests`의 필드명 스캐너를 **토큰만 바꿔 재사용** | 오늘 필드 0개 → `Assert.Ignore` + 동반 경보. 배선 라운드에 활성화 |

★ 3건 모두 **네거티브 컨트롤 필수**(가짜 위반 문자열을 같은 스캔 함수에 흘려 잡히는지).
★ 부재 단언은 **집합 등호**로 쓴다 — CLAUDE.md: *"부재 단언은 썩으면 조용히 초록이 된다."*
★ 상수는 **베끼지 않고 참조**한다(T-14-6-a).

---

## T-14-10. 플랫폼 영향 · 보장하지 않는 것

- **Windows 영향: 함께 검토함.** T-14-3-a(단조 최소 간격)·T-14-5-b(clamp)는 **플랫폼 분기 0** —
  `Time.realtimeSinceStartupAsDouble`과 정수 산술뿐이다. `PlatformParityAuditTests` 추가 항목 **0**.
  T-9-2는 **격하(선택)**되었으므로 Windows 전용 파일(`QueryInterruptTime`)은 **이번 라운드에 생기지 않는다.**
  유료 회복제를 MS 스토어에서 팔면 §5-2의 제약이 그대로 적용된다(**MSIX 필수 · 승격 실행에서 IAP 불가**).
- **macOS 영향: 함께 검토함.** 동상. `mach_continuous_time` 도입 보류 → S-1의 하드닝 런타임
  엔타이틀먼트 목록 **무변**. 유료 회복제는 스팀 경유이므로 맥 앱스토어 영수증 문제는 **여전히 범위 밖**.
- **모바일**: T-14 전부 플랫폼 중립. 단 iOS는 백그라운드 정지라 **"세션"의 정의가 다르고**,
  `MinRefillGapSeconds`가 정상 사용자에게 다르게 작용할 수 있다 — 모바일 라운드에서 재검토.
- **프로덕션 `.cs` 수정 0건 · 신규 플랫폼 분기 0건 · 앱 실행/빌드 0회.**

### 보장하지 않는 것 (T-13에 이어서)

8. **재부팅을 끼운 (사)/(카) 반복은 여전히 막을 수 없다.** T-7의 결론은 유효하고,
   **T-14-7의 검산이 그 미방어를 「수용 가능」으로 바꿀 뿐 「해결」로 바꾸지 않는다.**
9. **T-14-7의 검산은 `design-systems`가 재계산 중인 수치에 의존한다.** 상한 1,500·회복제 500·
   소모처 4,550 중 **어느 하나가 바뀌면 6개 검산을 전부 다시 돌려야 한다.**
10. **Steam/MS 소모품 API의 오프라인 동작 미확인**(E-13-3 그대로). T-14-5-d의 「재고를 스토어에 둔다」는
    **그 미확인 위에 서 있다.** 실기 확인 전에는 **권고이지 계약이 아니다.**
11. **`MinRefillGapSeconds = 20h`는 실측이 아니라 설계값이다.** 정상 사용자 곡선(하루 몇 시간 켜는
    사용자의 실제 세션 길이 분포)을 재지 않았다. 다만 **첫 refill은 항상 통과**하므로
    **값이 틀려도 정상 사용자를 잠글 수 없다** — 그게 이 설계의 안전 여유다.

---
---

# T-15절 — 「8시간 창」 래칫 사양 (2026-09-03, `design-systems` R10-c U-37 인계)

**T-14절은 한 줄도 고치지 않았다. 이 절은 그 위에 3번째 축을 얹는다.**

> `design-systems` R10-c 확정 반영: 무료 1개는 **"하루 2개" 한도에 포함**(유료 채널 일일 가치 상한
> 500동전) · 세이브 필드 `tonicOwned` · `tonicUsedToday` · `tonicFreeGrantedToday`(재고 없음, clamp).
> `design-systems` 표현: *"8시간 창 = 시계 조작 방어선."*

---

## T-15-0. 결론 3줄 — **채택한다. 단 두 군데를 정정한다**

1. **창을 채택한다.** 비용이 사실상 0(필드 1개 + clamp)이고 **오탐이 구조적으로 0**이다
   (정상 최대 사용이 창의 **43.4%**밖에 안 쓴다 — T-15-5 검산).
2. ★ **리더 지시 1건을 근거를 대고 반박한다 — 창 소비량은 「세이브에 남겨야」 한다.**
   프로세스 메모리에만 두면 **앱 재시작 한 번으로 창이 사라지고, 창이 없는 것과 수학적으로 같아진다**(T-15-2).
3. ★ **창의 방어 가치를 과대평가하지 마라.** 6.912배 → 2.304배는 **맞는 계산**이지만,
   **두 값 모두 「소모처 4,550을 하루 안에 소진」한다**(0.263일 vs 0.790일). 창이 사는 것은
   **약 12.6시간**이다. 진짜 방어선은 여전히 **일일 상한 2,500**과 **소모처의 유한성**이다(T-15-5).

---

## T-15-1. 창의 구현 사양

### T-15-1-a. 창이 세는 것 — **「지급된 초」이지 「앱이 켜져 있던 초」가 아니다**

> **T-15-1-a (필수):** 창은 **실제로 동전이 지급된 초**만 갉는다.
> 비유휴 상태 · 상한 도달로 지급이 0인 초 · 일시정지 · 전체화면 자동숨김 중은 **창을 갉지 않는다.**
>
> **왜**: "앱이 켜져 있던 초"로 세면 **상한에 걸려 한 푼도 못 버는 동안 창만 닳는다.**
> 그러면 정상 사용자가 **아무 이득 없이 창을 잃고**, 그건 방어가 아니라 버그다.
> (같은 계열의 실제 사고: `Time.deltaTime` 잘림이 "적게 쌓이는" 방향으로 공정성 문제를 만든다 — T-3-c.)

### T-15-1-b. 시간 원천 — **T-3을 그대로 상속한다. 새 관례 없음**

```
// 수급 틱. 벽시계를 읽지 않는다(T-3-a).
double now   = Time.realtimeSinceStartupAsDouble;      // T-3-b 단조 시계
double dt    = now - _lastTickMonotonic; _lastTickMonotonic = now;

if (!IsIdleEarning) return;                             // 창을 갉지 않는다(T-15-1-a)

int    room     = Max(0, DailyCap() - todayGrantedCoins);            // T-14-5-b, 상한은 함수
double winRoom  = Max(0.0, WindowCapSeconds - windowSecondsUsedToday); // ★ 3번째 축
if (room <= 0 || winRoom <= 0.0) return;                // 지급 0 → 창도 안 갉는다

double paySec   = Min(dt, winRoom);
int    pay      = (int)Min(room, paySec * CoinsPerSecond);

todayGrantedCoins      += pay;
windowSecondsUsedToday += paySec;      // ★ 지급된 초만
```

### T-15-1-c. 세이브 필드 — **1개 추가. clamp 규범은 T-14-5-b를 그대로 상속**

```
float windowSecondsUsedToday;   // 0 ~ WindowCapSeconds(28,800)

// 로드 직후 무조건 정규화(거부·경고 없음 — T-14-5-b와 같은 코드 형태)
windowSecondsUsedToday = Clamp(windowSecondsUsedToday, 0f, WindowCapSeconds);
if (float.IsNaN(windowSecondsUsedToday)) windowSecondsUsedToday = WindowCapSeconds;  // ★ 실패는 보수적으로
```

★ **NaN을 0이 아니라 상한으로 보내는 이유**: 0으로 보내면 **손상된 파일이 창을 리셋하는 무료 우회**가 된다.
다른 clamp들과 방향이 **반대**이므로 코드에 그 이유를 반드시 주석으로 남긴다.
(피해: 손상 파일을 만난 정상 사용자가 그날 유휴 수급을 못 한다 — **상한 2,500은 그대로 살아 있으므로
활쏘기·집중모드로 벌 수 있다.** 잠기지 않는다.)

### T-15-1-d. ★ 창의 리셋은 **refill 사건에 합류한다** — 별도 조건을 만들지 않는다

> **T-15-1-d (필수):** `windowSecondsUsedToday = 0`은 **T-14-3-a의 `MayRefill`이 참일 때만** 일어난다.
> 일일 상한 리셋 · 무료 회복제 재지급 · **창 리셋**이 **하나의 사건**이다(T-D-10을 3항목으로 확장).

**이 한 줄이 창에 별도 방어 장치를 필요 없게 만든다** — T-14-3-a(단조 20시간 간격)를 **그대로 상속**한다.
창 리셋 조건을 따로 만들면 **방어를 두 곳에 걸어야 하고, 한쪽을 빠뜨리면 조용히 새는 문**이 된다.

---

## T-15-2. ★ 리더 지시 반박 — **"세이브에 안 남김(위조 대상 없음)"은 여기서 성립하지 않는다**

리더 지시: *"`realtimeSinceStartupAsDouble` 누적, 세이브에 안 남김(위조 대상 없음)."*
**앞부분은 채택하고 뒷부분은 반박한다.** 근거는 산술이다.

| 창의 저장 위치 | 앱 재시작 시 | 하루 실효 상한 | 결과 |
|---|---|---:|---|
| **프로세스 메모리만**(리더 지시) | **0으로 리셋** | 재시작을 반복하면 **1,440분 × 12 = 17,280** | ★ **창이 없는 것과 수학적으로 동일** |
| **세이브 + T-15-1-d** | 유지 | 480분 × 12 = **5,760** | 의도대로 작동 |

**즉 "위조 대상을 안 만든다"를 지키면 「방어 자체가 없어진다」.** 둘 다 가질 수 없다.

### 그리고 세이브에 남겨도 **새 취약점이 아니다** — T-14-5-c의 표에 한 줄이 붙을 뿐이다

| 벡터 | 편집 | 얻는 것 |
|---|---:|---|
| **`windowSecondsUsedToday = 0`** | 1 | **하루 창 1회분**(= 상한 2,500에 걸려 실제로는 더 적다) |
| `todayGrantedCoins = 0` | 1 | 하루 상한 전액 2,500 |
| **`coinBalance = 999999`** | **1** | ★ **경제 전체 4,550 즉시** |

★ **창 필드 위조는 세 벡터 중 가장 약하다.** `todayGrantedCoins`를 고치는 쪽이 항상 우월하고,
`coinBalance`는 압도적이다. **자물쇠 옆에 열린 창문이 이미 두 개 있는데 세 번째 자물쇠를 안 다는 이유가 없다.**
§4 · E-8 · T-14-5-c의 판정을 **창 필드에도 그대로 적용한다: clamp까지만, 그 이상은 안 한다.**

> **T-15-2-a (필수):** 창 소비량은 **세이브에 평문으로 둔다.** 서명·암호화·해시 **전부 기각**(§4-2 표 그대로).
> **T-15-2-b (금지):** `PlayerPrefs`에 두지 않는다 — Windows에서 레지스트리에 쓰고
> **신원 문자열이 바뀌면 함께 이사한다**(S-7-4 실측). 별도 파일도 만들지 않는다(원장이 하나 더 늘고,
> S-7-2가 기록한 「고아 원장」 위험이 재발한다).

---

## T-15-3. 3축 직교 검증 (리더 질문 2) — **겹치지 않는다. 세 개 다 필요하다**

| 축 | 무엇을 제한하나 | 단위 | 누가 정하나 | 위조 가능? | 어느 공격을 막나 |
|---|---|---|---|---|---|
| **① 일일 상한** `1500 + 500×n` | **하루에 얼마를 버나** | 동전 | `design-systems`(경제 노브) | 예(`todayGrantedCoins`) | 정상 사용의 천장 |
| **② 리필 간격** `MinRefillGapSeconds=20h` | **하루가 몇 번 오나** | 단조 초 | `security`(방어) | **아니오**(프로세스 메모리) | **(카) 세션 내 롤오버 반복** |
| **③ 창** `480분` | **하루 안에 몇 분을 버나** | 지급된 초 | 양쪽(경제 겸 방어) | 예(`windowSecondsUsedToday`) | **① 이 롤오버로 무력화된 뒤의 2차선** |

### 상호작용 검증 — 4가지 상태를 전부 계산했다

| 상황 | ① 유효 | ② 유효 | ③ 유효 | 하루 실효 최대 |
|---|---|---|---|---:|
| **정상 사용자** | ✔ 먼저 걸린다(208.3분) | 무관(하루 1회) | 여유 56.6% | **2,500** |
| (카) 세션 내 롤오버 반복 | 무력 | ✔ **완전 차단** | 무관 | **2,500** |
| (카') **앱 재시작**으로 ②를 우회 | 무력 | 무력(메모리 리셋) | ✔ **여기서 걸린다 → 5,760** | **5,760** |
| (사) **재부팅**으로 전부 우회 | 무력 | 무력 | ⚠ 리셋 가능하나 **실시간 480분을 새로 기다려야 한다** | **≤ 17,280**(하루 전체) |

★ **③이 없으면 (카')에서 곧바로 17,280이 된다** — 리더/`design-systems`의 판단이 맞다.
★ **②와 ③은 서로를 대체하지 못한다**: ②는 무료(오탐 0, 위조 불가)지만 재시작에 약하고,
③은 재시작을 건너지만 위조 가능하다. **두 개가 서로의 약점을 정확히 덮는다.**

> **T-15-3-a (필수):** ②를 ③으로 대체하지 마라. ②는 **위조 불가능한 유일한 축**이다
> (세이브에 없다 = 편집할 것이 없다). ③만 남기면 세션 내 롤오버가 **`windowSecondsUsedToday` 편집 1회로** 뚫린다.

---

## T-15-4. 검산 (전부 재현 가능 — 요율 12동전/분 · 창 480분 · 상한 1,500/2,500 · 소모처 4,550)

| # | 항목 | 값 |
|---:|---|---:|
| 1 | 창 소진 시 하루 최대 | 480 × 12 = **5,760** |
| 2 | 무창(하루 전체) | 1,440 × 12 = **17,280** |
| 3 | 무창 / 정상 최대 | 17,280 / 2,500 = **6.912배** ✔ `design-systems` 값과 일치 |
| 4 | 창 / 정상 최대 | 5,760 / 2,500 = **2.304배** ✔ 일치 |
| 5 | **상한 2,500 도달 소요** | 208.3분 = **3.47시간** → **창의 43.4%** |
| 6 | 상한 1,500(회복제 0개) 도달 | 125.0분 = **2.08시간** → **창의 26.0%** |
| 7 | 완주(정상 2,500/일) | 4,550 / 2,500 = **1.82일** |
| 8 | 완주(창 5,760/일) | 4,550 / 5,760 = **0.790일** |
| 9 | **완주(무창 17,280/일)** | 4,550 / 17,280 = **0.263일 = 6.32시간** |
| 10 | **창이 사는 시간** | 0.790 − 0.263 = **0.527일 ≈ 12.6시간** |

---

## T-15-5. ★ 정직한 평가 — **창을 방어로서 과대평가하지 마라**

`design-systems`의 *"8시간 창 = 시계 조작 방어선"*은 **비율로는 맞고 절대량으로는 약하다.**

> **검산 9가 말하는 것**: 창이 없으면 **조작을 하나도 안 하고 앱을 6.32시간 켜 두는 것만으로
> 소모처 전체(4,550)가 소진된다.** 그리고 **창이 있어도 0.79일 = 약 19시간이면 소진된다.**
> **6.912배와 2.304배는 둘 다 「하루 안에 게임이 끝난다」의 다른 표현이다.**

- 창이 실제로 사는 것은 **약 12.6시간**(검산 10)이다. 그게 이 방어의 정직한 값어치다.
- **진짜 방어선은 세 개 중 ①(일일 상한 2,500 → 1.82일)이고, 그보다 더 근본적인 것은
  T-14-7-a의 「소모처 4,550의 유한성」이다.** 창은 그 둘의 **보조선**이다.
- 그럼에도 **채택한다**: 비용이 필드 1개 + clamp이고, 오탐이 0이며(검산 5·6),
  ②가 못 덮는 (카') 구멍을 정확히 덮는다. **싸고 오탐 없고 구멍 하나를 정확히 메우면 그건 좋은 방어다.**

> ★ **`design-systems`에 돌려보내는 관측 1건(수치 판정은 그쪽 몫)**: 검산 9가 보여주는
> *"조작 없이 6.32시간이면 완주"*는 **보안 결함이 아니라 경제 스케일 문제**다.
> 요율 12동전/분은 기존 원형 B(64동전/일)의 **23.4배**이고, 창·상한 중 **어느 것도 그 스케일을
> 되돌리지 못한다.** 나는 요율을 판정할 수 없다 — 다만 **T-14-7의 검산 6개가 이 요율에 매달려 있으므로
> 요율이 바뀌면 그 6개를 다시 돌려야 한다**는 것만 못 박는다.

---

## T-15-6. 오탐 방지 — 창이 정상 사용자를 잠글 수 있는 경로가 있는가

| 정상 사용자 | 창에 걸리나 | 근거 |
|---|---|---|
| 회복제 2개 다 쓰는 헤비 유저 | **안 걸린다** | 상한 2,500이 **208.3분**에 먼저 걸린다 = 창의 43.4%(검산 5) |
| 회복제 0개 유저 | **안 걸린다** | 125분 = 창의 26.0%(검산 6) |
| 24시간 상주(설계 목표 사용형) | **안 걸린다** | 지급이 멈추면 창도 안 갉는다(T-15-1-a) |
| 앱을 여러 번 껐다 켜는 사용자 | **안 걸린다** | 창은 누적이고 하루 총량이 같다. 재시작이 창을 **늘리지도 줄이지도** 않는다 |
| 세이브 손상 · 구버전 파일 | **그날 유휴 수급만 멈춘다.** 잠기지 않는다 | NaN → 상한(T-15-1-c). 활쏘기·집중모드·[오늘 할일]은 그대로 |
| 시간대 이동 · 서머타임 | **무관** | 창은 단조 초 누적이라 벽시계를 안 본다 |

> **T-15-6-a (필수·T-10 상속):** 창 소진 시 **"제한"·"차단"·"의심" 문구를 쓰지 않는다.**
> 화면에는 아무것도 뜨지 않는다(정상 사용자는 도달하지 않으므로 문구를 만들 필요 자체가 없다).
> **창 소진을 기록하는 플래그를 세이브에 만들지 않는다** — 그건 T-10-a가 금지한 「조작 의심 상태」다.

---

## T-15-7. 테스트 — ★ **감사(소스 스캔)와 동작(순수 함수)을 나눈다. 섞으면 둘 다 약해진다**

리더 요구(*"감사 테스트 3번에 창 래칫 검증도 포함"*)를 **분리해서 이행한다.** 이유:
소스 스캔은 "코드가 그렇게 생겼나"를 보고, 17,280/5,760은 "코드가 그렇게 계산하나"다. **다른 질문이다.**

### (A) `DailyLimitClampAuditTests` — 소스 스캔(T-14-9 #3 확장)

| 검사 | 오늘의 기대값 |
|---|---|
| 로드 경로에 `potionsUsedToday`·`todayGrantedCoins`·**`windowSecondsUsedToday`** clamp 존재 | 0줄 → `Assert.Ignore` + 동반 경보 |
| `SaveData`에 `dailyCap`/`todayLimit`/**`windowCap`** 계열 필드 **부재**(상한은 함수여야 한다) — `EntitlementNotInSaveAuditTests`의 필드명 스캐너를 **토큰만 바꿔 재사용** | 위반 0 |
| **네거티브 컨트롤 필수** — 가짜 `public int dailyCapCoins;`를 같은 스캔 함수에 흘려 잡히는지 | 잡혀야 함 |

### (B) ★ 신규 `IdleWindowRatchetTests` — **순수 함수 동작 + 네거티브 컨트롤**(리더 요구 3의 본체)

```
// 하루(1,440분)를 시뮬레이션. 롤오버는 무한 허용(=②가 뚫린 최악 가정).
int SimulateDay(double windowCapSeconds)   // T-15-1-b의 순수 함수를 그대로 호출

[Test] 창_480분이면_하루_5760에서_잘린다()
    => Assert.AreEqual(5760, SimulateDay(WindowCapSeconds));          // ★ 상수 참조, 숫자 안 베낌

[Test] ★네거티브컨트롤_창을_없애면_실제로_17280까지_샌다()
    => Assert.AreEqual(17280, SimulateDay(double.PositiveInfinity));
```

> ★ **(B)의 두 번째 테스트가 이 라운드에서 가장 중요한 한 줄이다.**
> 첫 번째만 있으면 **"창이 걸려서 5,760"인지 "다른 이유로 우연히 5,760"인지 구별할 수 없다.**
> 이 저장소가 반복해 당한 형태가 정확히 그것이다(실패한 측정과 성공한 측정이 똑같이 생겼다).
> **17,280이 나오는 것을 실제로 보여야 5,760이 창의 성과임이 증명된다.**
>
> **T-15-7-a (필수):** 5,760 · 17,280 · 480 · 12를 **테스트에 숫자로 베끼지 않는다.**
> `WindowCapSeconds` · `CoinsPerSecond` 상수를 참조해 **테스트 안에서 계산한다**
> (CLAUDE.md 확정 규칙, 2026-09-01 `MaxCharacterScale`·스키마 버전 4건 파손 사고).
>
> **T-15-7-b (필수):** 시뮬레이션은 **벽시계 기준(초)**으로 예산을 잡는다. 프레임 수 기반 금지
> (CLAUDE.md 2026-09-01: 배치모드 PlayMode가 2,000fps 이상으로 돈다). ★ 단 (B)는 순수 함수라
> **PlayMode가 아니라 EditMode**여야 한다 — 실시간을 기다릴 이유가 없다.

---

## T-15-8. `design-systems`에 거는 추가 제약 (T-D-13~15. T-D-1~12는 그대로 유효)

| # | 제약 | 왜 | 어기면 |
|---:|---|---|---|
| **T-D-13** | **창은 「지급된 초」만 갉는다.** 앱 가동 초·비유휴 초·상한 도달 후의 초는 안 갉는다 | T-15-1-a | 상한에 걸린 정상 사용자가 **아무 이득 없이 창을 잃는다** |
| **T-D-14** | **창 리셋은 일일 상한 리셋·무료 회복제 재지급과 「하나의 사건」**(T-D-10을 3항목으로 확장) | T-15-1-d. 나누면 T-14-3-a 방어를 **세 곳에 걸어야 하고 한 곳을 빠뜨리면 조용히 샌다** | 방어가 한쪽에만 걸린다 |
| **T-D-15** | ★ **창 480분과 상한 2,500 사이에 최소 1.5배 이상의 여유를 유지하라.** 현재 2.304배 | 여유가 1.0배 아래로 내려가면 **창이 정상 사용자에게 먼저 걸린다** = 오탐이 기본값이 된다. 요율·상한·창 중 **하나만 바꿔도 이 비율이 움직인다** | 정상 사용자가 창에 잠긴다 |

**T-D-15 검산 조건**: `WindowCapMinutes × CoinsPerMinute ≥ 1.5 × MaxDailyCap`.
현재 `480 × 12 = 5,760 ≥ 1.5 × 2,500 = 3,750` ✔ (여유 2.304배).
★ **요율을 12 → 8로 낮추면** `480 × 8 = 3,840 ≥ 3,750` **간신히 통과(1.536배)**,
**요율 7이면 3,360 < 3,750 → 위반**(창이 먼저 걸린다). **요율 하향은 창 길이 재검토를 동반해야 한다.**

---

## T-15-9. 플랫폼 영향 · 보장하지 않는 것

- **Windows 영향: 함께 검토함.** 창은 `Time.realtimeSinceStartupAsDouble` + float 누적 + clamp뿐 —
  **플랫폼 분기 0**, P/Invoke 0, 새 OS 권한 0, 백신 표면 증가 0(선 3 무접촉).
  `PlatformParityAuditTests` 추가 항목 **0**. T-9-2(부팅 앵커)는 **여전히 격하 상태**이므로
  Windows 전용 파일은 이번에도 **생기지 않는다.**
  ★ 세이브 필드가 1개 늘어나므로 `CharacterSaveStore.CurrentVersion` 상승 라운드에 포함되고,
  그 라운드는 **`vN-1` 하위 호환 테스트 1건을 반드시 동반한다**(CLAUDE.md 명시 규칙).
  구버전 파일에는 필드가 없으므로 **기본값 0 = "오늘 창을 아직 안 썼다"**가 되고, 그건
  **사용자에게 관대한 방향**이다 ✔.
- **macOS 영향: 함께 검토함.** 동상 — 플랫폼 분기 0. 하드닝 런타임 엔타이틀먼트 목록 **무변**(S-1).
  ★ macOS 절전(뚜껑 닫기)에서 `realtimeSinceStartupAsDouble`이 멈추는지는 **미확인**이지만,
  **멈추는 쪽이 우리에게 옳다** — 절전 중에는 지급도 0이어야 하므로 창도 안 갉는 것이 맞다.
  즉 **어느 쪽이든 설계가 성립한다**(T-3의 미확인과 같은 성질: 무해화됐지 확인된 게 아니다).
- **모바일**: 창 자체는 중립. 단 iOS는 백그라운드 정지라 **"유휴"의 정의가 달라져** 창의 소비 속도가
  달라진다 — 모바일 라운드에서 T-D-15 비율을 **다시 계산해야 한다.**
- **프로덕션 `.cs` 수정 0건 · 신규 플랫폼 분기 0건 · 앱 실행/빌드 0회.**

### 보장하지 않는 것 (T-14-10의 8~11에 이어서)

12. **창은 (사) 재부팅 반복을 막지 못한다.** 재부팅은 ②(메모리)를 리셋하고, 그러면 창 리셋이 자유로워진다.
    **다만 창을 리셋해도 실시간 480분을 새로 기다려야 하므로 하루 상한은 17,280을 넘지 못한다** —
    그건 「종일 켜 둔 사람의 값」이지 조작으로 만든 값이 아니다.
13. **검산 1~10은 요율 12동전/분 · 창 480분 · 상한 1,500/2,500 · 소모처 4,550에 전부 의존한다.**
    `design-systems`가 **어느 하나라도** 바꾸면 10개를 다시 돌리고 **T-D-15 비율을 재검산해야 한다.**
14. **테스트를 돌리지 않았다.** T-15-7의 5,760·17,280은 **파이썬 독립 계산**이고 러너 결과가 아니다
    (E-12-6이 기록한 함정: 재구현과 본 구현이 같은 머릿속에서 나온다). **(B)를 반드시 러너로 돌려라.**
15. **`MinRefillGapSeconds = 20h`는 여전히 설계값**(T-14-10의 11). 창은 그 값에 의존하지 않는다 —
    ②가 완전히 없어도 창은 5,760에서 자른다.
