# 캐릭터 본체 형태 언어 스펙 — 얼굴 / 펫 통일 / 최단 선분 안전 한계

> 작성: **design-character**(캐릭터 조형 디렉터) / 2026-09-01
> 지시: 리더 배정 3건 — ① 기본 상태 얼굴 0개 ② 펫·주인 시각 언어 통일 ③ 본체 최단 선분 규칙
> 사용자 지시: **"윈도우는 일단 미루고 맥만 중점적으로"**
> 프로덕션 `.cs`는 **한 줄도 수정하지 않았다.** 구현은 리더가 `coder`에게 배정한다.
> 측정 재현 스크립트: 세션 스크래치패드(`edt.py` / `analyze2.py` / `m3d.py` / `pet2.py`) — 방법은 4절에 전부 적었다.

---

## 0. 세 줄 요약

| # | 임무 | 판정 |
|---|---|---|
| 1 | 얼굴 0개 | **지금이 옳다.** 참고 이미지 ~14장·인물 20명+ 중 **얼굴이 있는 인물은 0명**이다. 눈을 되살리면 안 된다. 첫인상이 "그림자"인 진짜 원인은 얼굴이 아니라 **잉크색이 검정**이라는 것 — 참고의 스틱맨은 **전원 채도 높은 유채색**이다. |
| 2 | 펫·주인 통일 | **획을 굵히지 마라(0.022 → 0.036은 반대 방향이다).** 참고의 "속 빈 머리" 인물은 획/머리 = **13.3%**이지 22.3%가 아니다. 우리 펫은 이미 16.5~20.9%로 참고보다 **두껍다**. 진짜 격차는 **무릎이 없다는 것** 하나다. |
| 3 | 최단 선분 | 본체 팔다리는 규칙을 **어기고 있다 — 배율 0.45 미만에서**. 무릎앉아(126°) 기준 배율 0.35에서 여유 **0.76배**(1.0 미만 = 위반). 기존 테스트가 못 잡는 이유는 **프리팹(배율 0.75)에서만 재기 때문**이다. |

---

## 1. 참고 이미지 — 리더가 볼 수 없으므로 글로 옮긴다

`/Users/kjmoon/.claude/uploads/bf3ed972-.../` 33장 중 **14장이 Alan Becker(@alanbecker) 유튜브 쇼츠 캡처**다.
나머지는 문서 사진/무관한 게임 스샷이다. 등장 인물(색 = 정체성):

| 파일 | 장면 | 인물 |
|---|---|---|
| `8875f6ce` | "Slime Trampoline" — 주황이 왼쪽에 서 있고, 파랑이 그 옆을 걸어가고, 노랑이 초록 블록 위에서 상체를 앞으로 크게 굽힘 | 주황(**속 빈 링 머리**) / 파랑·노랑(꽉 찬 머리) |
| `d0e80eae` | 같은 편 — 셋이 각각 아이콘에 매달리거나 만세하고 공중에 뜸 | 같음 |
| `be3d1e14` | 파랑이 유튜브 썸네일(오믈렛) 위에 몸을 기울여 들여다봄 | 파랑 |
| `2ecda686` | **정지·측면·걷기** — 빨강과 초록이 나란히 걷는 정면 광량 장면. **두께 측정에 가장 좋은 프레임** | 빨강 / 초록 |
| `1e3a256c`,`23213986`,`5a31dd2c` | "Workout" — 빨강·초록이 나무 옆에서 서 있기/자세 잡기. **머리띠·팔목밴드 착용** | 빨강 / 초록 |
| `492afa04` | **소파에 셋이 앉아 있음** — 초록은 폰을 보고, 파랑은 책을 읽고, 빨강은 컵을 듦. 근접 장면 | 초록/파랑/빨강 |
| `798295cc` | "Couple Discover" — **공원 벤치 앞에 회색 인물(속 빈 링 머리)과 흰 인물(꽉 찬 머리 + 긴 머리카락 + 원피스)이 손을 잡고 서 있음**. 이 쇼츠만 팔다리가 **끝으로 갈수록 뾰족하게 가늘어지는(taper)** 다른 화풍이다 — **우리 화풍이 아니다. 따라 하지 마라.** |
| `dfc5632f`,`5f1bf377` | "Yellow's Speedrun" — 노랑이 마인크래프트 물 블록 앞에서 곡괭이를 듦 | 노랑 |

### 1-1. ★ 얼굴이 있는 인물은 **한 명도 없다**
근접 장면(`492afa04` 소파, `798295cc` 벤치)에서도 눈·입·눈썹·표정판이 **전무**하다.
머리는 **완전한 무지(無地) 면**이다. 20명+ 인스턴스 중 예외 0건.

### 1-2. 머리는 **원**이다(타원이 아니다)
2차 모멘트로 등가 타원을 맞춰 재면 장/단축비 **a/b = 1.017**(빨강), **1.017**(초록).
즉 **머리 자체는 방향을 담지 않는다.** "머리를 기울여 표정을 만든다"는 이 화풍에서 성립하지 않는다.

### 1-3. 머리는 척추 접선 위에 **정확히 얹혀 있다**
머리 중심에서 몸통 중심선(관절 위 1.55R~2.65R 구간 최소자승 접선)까지의 **수직 거리**:
빨강 **+0.040R**, 초록 **+0.006R**. 즉 "목이 앞으로 캔틸레버로 나가 있다"는 가설은 **반증됐다**.
(눈으로는 그렇게 보이지만 그것은 몸통이 기운 상태에서 원이 한쪽으로 튀어나와 보이는 착시다. 나도 처음에 그렇게 봤다가 측정에서 틀렸다.)

### 1-4. 획은 마디 안에서 **일정하다**(taper 없음)
빨강: 다리 구간 y=280~720px 동안 34.2 → 34.9px. 초록: 36.1 → 36.8px. **변동 2% 이하.**
→ `798295cc`(뾰족한 팔다리)는 그 쇼츠 전용 화풍이고 AvA 본편 화풍이 아니다.
목/몸통만 팔다리보다 굵다: 빨강 목 38.5px vs 팔다리 34.9px = **1.10배**.
우리 규격(팔 0.100 / 몸통 0.110 / 다리 0.120)의 몸통/팔 = 1.10 — **일치한다. 바꾸지 마라.**

### 1-5. 색 — 전원 채도 높은 **유채색**, 검정 0명
PNG 스크린샷(무압축)에서 뽑은 원색:

| 인물 | RGB | HEX |
|---|---|---|
| 주황 | (255, 111, 0) | `#FF6F00` |
| 노랑 | (255, 197, 0) | `#FFC500` |
| 하늘 | (38, 192, 255) | `#26C0FF` |
| 빨강 | (220, 19, 0) | `#DC1300` |
| 초록 | (94, 182, 0) | `#5EB600` |

말뭉치 전체에서 **검정 스틱맨은 0명**이다. 유일한 어두운 인물(`798295cc`의 회색 `#505050`)은
**밝은 배경 위에 있고 머리가 속 빈 링**이다.

---

## 2. 임무 ① — 얼굴 없이도 캐릭터로 읽히게 하기

### 2-1. 판정: **얼굴을 넣지 않는 것이 옳다** (근거 3중)

1. **참고에 얼굴이 0개다**(1-1). 이 장르는 얼굴 없이 성립하는 것이 아니라, **얼굴이 없는 것이 이 장르다.**
2. **눈은 어느 배율에서도 물리적으로 존재할 수 없다.** 재검산:
   - `BaselineEyePupilRadius = 0.018` → `CreateFilledDot`이 경로반경 r을 폭 2.4r로 그리므로 **잉크 지름 = 4.4r**.
   - 배율 1.0: 눈 잉크 지름 **2.79pt** < 몸통 획 **4.05pt**. 즉 눈이 획 하나보다 작다(0.69획).
   - 배율 0.35: 눈 획이 화면 하한(2pt)에 걸려 **부풀어** 잉크 지름 2.44pt / 머리 7.43pt = **33%**. 이번엔 눈이 머리의 1/3이 되는 왕눈이 된다.
   - **작아지면 사라지고 커지면 괴물이 된다. 중간이 없다.** 2026-08-30 삭제 판단은 옳았다.
3. **화면상 크기가 참고의 1/4다.** 참고 인물은 iPhone 화면에서 **약 263pt** 높이(770px ÷ 2.93 배율).
   우리 출하 기본(배율 0.75)은 **60.1pt**. 4.4배 작다. 60pt짜리 인물의 머리는 13.6pt이고, 그 안에
   눈 두 개 + 간격을 넣으려면 각 눈이 3pt 이하여야 하는데 그건 획 하나다.

### 2-2. 그럼 왜 "귀엽다"가 아니라 "실루엣/그림자"로 읽히는가 — 측정으로 좁힌 답

우리 형태와 참고 형태를 **재보니 거의 전부 일치했다.** 그래서 남는 원인은 하나뿐이다.

| 지표 | 우리(배율 0.75) | 참고 실측 | 판정 |
|---|---|---|---|
| 키 ÷ 머리 잉크 지름 | 4.41 (배율 1.0: 4.52) | 4.74 / 4.83 | 우리 머리가 **5~8% 더 크다**. 더 키울 여지 없음 |
| 획 ÷ 머리 잉크 지름 | 22.29% | 21.1% / 22.4% / 21.8% / 21.9% | **일치** |
| 엉덩이 높이 ÷ 키(바닥 기준) | 41.1% | 42.9% / 42.7% / 41.9% | **일치** |
| 어깨 ~ 머리 밑변 거리 | 0.318 R | 약 0.1~0.3 R (실루엣 분리점 역산) | **일치**(오히려 우리가 목이 약간 길다) |
| 몸통 획 ÷ 팔 획 | 1.10 | 1.10 | **일치** |
| 머리 채움 | 꽉 참 | 유채색 인물 전원 꽉 참 | **일치** |
| **잉크색** | **검정 `#000`** | **전원 유채색, 검정 0명** | ★ **여기만 다르다** |

> **결론:** 기하학은 이미 맞다. 첫인상이 "그림자"인 이유는 **꽉 찬 검정 원이 그림자와 물리적으로 같은 것**이기 때문이다.
> 꽉 찬 머리는 참고의 문법이 맞지만, 참고에서 그 면은 **"저 주황색 애"라는 정체성 색면**이지 검정 덩어리가 아니다.
> 얼굴을 되살리는 것보다 **색을 주는 것**이 참고에 훨씬 가깝고, 훨씬 싸다.

### 2-3. 제안 M1-A — 잉크 프리셋에 **유채색 5종**을 추가한다 (최우선, 최소 비용)

- 현재 `StickConfig.inkColor`는 `StickmanInkColor.Black` / `White` **2종뿐**이고, 색 자체는
  `primaryOutlineColor` / `whiteInkColor` 필드에 이미 분리돼 있으며 런타임 세터(`SetRuntimeInkColor` 계열)도 이미 있다.
  → **배관을 새로 만들 것이 없다.** enum에 5개를 더하고 색표를 잇는 작업이다.
- 추가할 값은 1-5절의 실측 5색을 **그대로** 쓴다. 임의로 예쁜 색을 만들지 마라 — 이 5색이
  "Alan Becker 스틱맨처럼 보인다"는 인상의 실제 원인이다.
- **기본값 판단은 리더/ux-designer 소관**이다. 내 권고는 **기본값을 `#26C0FF`(하늘)로 바꾸는 것**이다:
  검정은 밝은 바탕화면에서 그림자로, 어두운 바탕화면에서 소실된다. 하늘색은 실측 5색 중
  밝은 배경(L*≈72)과 어두운 배경 양쪽에서 대비가 가장 균형적이다. 다만 이건 **UI 표면 결정이므로 리더가 결정**한다.
- **검정 프리셋은 남긴다**(사용자가 고른 적 있는 값을 뺏지 않는다).

### 2-4. 제안 M1-B — 검정/흰색 프리셋에서만 **머리를 링으로** (조건부, 리더 판단 필요)

참고 말뭉치의 **유일한 무채색 인물**(`798295cc` 회색 `#505050`, 밝은 배경)이 **속 빈 링 머리**를 쓴다.
유채색 인물은 전원 꽉 찬 머리다. 즉 참고는 **"무채색이면 링, 유채색이면 채움"**을 실제로 지키고 있다.

- 구현은 `SceneBootstrapper`의 `CreateFilledDisc(head, "HeadFill", ...)` 한 줄을 프리셋으로 게이트하면 된다.
  `HeadOutline`은 남아 있으므로 치수 계약 C1(`StickmanMetrics.HeadRadius`)은 무영향이다.
- ★ **경고**: 이것은 2026-09-01 사용자 직접 지시 **"눈 삭제하고 머리도 그냥 다 채워주고"**와 정면으로 충돌한다.
  그러므로 **M1-A(색)를 먼저 하고, M1-B는 사용자가 검정을 계속 쓰겠다고 할 때만** 리더가 사용자에게 되물어야 한다.
  내 권고는 **M1-A만 하고 M1-B는 보류**다.

### 2-5. 하지 말아야 할 것 (반증된 안들)

| 안 | 반증 |
|---|---|
| 머리를 타원으로 만들어 기울인다 | 참고 머리는 원이다(a/b = 1.017). 타원은 이 화풍이 아니다 |
| 목을 캔틸레버로 앞에 내민다 | 머리-척추 수직 오프셋 실측 0.006R / 0.040R = **사실상 0** |
| 목을 길게 뽑는다 | 참고 어깨는 머리 밑변 바로 아래(~0.1R). **우리(0.318R)가 이미 더 길다** |
| 머리를 키운다 | 우리 4.41~4.52 머리 vs 참고 4.74~4.83. **이미 우리 머리가 더 크다** |
| 팔다리를 끝으로 갈수록 가늘게 한다 | AvA 본편 화풍은 taper 0%(변동 2% 이하). taper는 `798295cc` 한 쇼츠 전용 |
| 눈을 다시 넣는다 | 2-1의 세 근거 |

### 2-6. 리더 → design-motion 전달 사항 (내 소관 밖, 측정치만 넘긴다)

참고의 걷기 프레임에서 **몸통은 절대 수직이 아니다**: 척추 접선이 수직에서
**−8.8°**(빨강) / **+15.5°**(초록) / **−14.8°**(파랑) 기울어 있다.
얼굴이 없는 캐릭터에서 "살아 있음"을 나르는 것은 결국 이 기울기다.
우리 `SetBodyLean`은 ±30°까지 있으므로 배관은 있다 — **실제 Idle/Walk에서 몇 도가 들어가는지는
design-motion이 판단할 일**이다. 나는 형태 담당이라 여기까지만 적는다.

---

## 3. 임무 ② — 펫과 주인의 시각 언어 통일

### 3-1. ★ 먼저: 리더가 준 전제 하나가 틀렸다

> "배율 0.9 이상에서 펫이 14.9%로 얇아진다 … 0.036이면 전 배율 22.3%"

**14.9%는 화면상 최소 두께 하한(2pt)을 빼고 계산한 값이다.** 하한을 넣으면:

`Stroke = Height × 0.022`이고 `RenderStroke = max(Stroke, 2pt)`인데,
`0.022 × 2.2746944 × s ≥ 0.0567376` ⟺ `s ≥ 1.134` — **`MaxCharacterScale`이 1.0이므로 펫 획은 전 구간에서 항상 하한(정확히 2.00pt)에 눌려 있다.**

| 배율 | 펫 키 | 링 경로반경 | 비례 획 | **실제 그려지는 획** | 링 바깥지름 | 획/바깥지름 | 몸의 획/머리 | 비 |
|---|---|---|---|---|---|---|---|---|
| 0.35 | 12.63pt | 1.77pt | 0.62pt | **2.00pt** | 5.54pt | **36.13%** | 26.92% | ×1.34 |
| 0.75 | 27.06pt | 3.79pt | 1.32pt | **2.00pt** | 9.58pt | **20.88%** | 22.29% | ×0.94 |
| 1.00 | 36.08pt | 5.05pt | 1.76pt | **2.00pt** | 12.10pt | **16.52%** | 22.85% | ×0.72 |

즉 실제 값은 14.9%가 아니라 **16.5 ~ 36.1%**이고, 낮은 배율에서는 오히려 **몸보다 34% 두껍다**.

### 3-2. ★ 그리고 22.3%는 **속 빈 머리에 적용되는 법칙이 아니다** (참고 실측)

같은 프레임 안에서 **꽉 찬 머리 인물과 속 빈 링 머리 인물을 동시에 잰 결과**:

| 프레임 | 꽉 찬 머리 잉크 지름 | 링 머리 바깥지름 | **링/꽉참** | 링 구멍 지름 | 획(둘 다 동일) | 획/꽉참 | **획/링바깥** |
|---|---|---|---|---|---|---|---|
| `8875f6ce` | 145.8px | 226px | **1.550** | 162px | 30.5~32.0px | 21.9% | **13.5%** |
| `d0e80eae` | 102.6px | 158.6px | **1.545** | 117px | 20.6~22.2px | 21.6% | **13.1%** |

**법칙 정리(참고에서 유도):**
- 획은 **온 그림에 하나**다. 인물이 링 머리든 채움 머리든 **획 두께는 같다.**
- 링 머리는 대신 **바깥지름을 1.55배로 키운다.** 그래야 잉크 질량이 채움 머리와 맞는다
  (검산: 링 잉크 면적 ÷ 채움 잉크 면적 = 1.09 / 1.17 — 15% 이내).
- 그 결과 **링 머리의 획/바깥지름은 13.3%**가 되고, 이건 **정상**이다. 22.3%가 아니다.

**우리 펫에 대입:**
펫이 "주인의 0.45배 미니어처"라면 채움 머리 지름은 `0.45 × 주인 머리 잉크 지름`일 것이다.
지금 펫의 링 바깥지름을 그 값과 비교하면:

| 배율 | 0.45 × 주인 머리 | 펫 링 바깥 | 비율 | 참고 법칙 |
|---|---|---|---|---|
| 0.35 | 3.34pt | 5.54pt | ×1.657 | 1.55 |
| 0.75 | 6.13pt | 9.58pt | **×1.562** | **1.55** |
| 1.00 | 7.98pt | 12.10pt | **×1.516** | **1.55** |

> **우리 펫의 링 머리 크기는 이미 참고 법칙과 오차 1% 안에서 정확하다.**
> 그리고 획/링바깥은 참고 13.3% 대비 우리는 16.5~20.9% — **우리 펫 링이 참고보다 이미 두껍다.**

### 3-3. 판정 M2-A — **`StrokeRatio` 0.022 → 0.036 변경을 반대한다**

- 0.036으로 올리면 획/링바깥이 전 배율 22.3%가 되는데, 그건 **참고 링 머리(13.3%)의 1.68배**다.
- 배율 1.0 기준 획이 2.00pt → 2.90pt(1.45배)로 굵어진다. 펫 키는 36pt다. **36pt짜리 인물에 2.9pt 획**은
  획/키 = 8.0%로, 주인(4.05pt / 80.2pt = 5.05%)의 **1.59배**다 — 미니어처가 아니라 **뭉툭한 다른 종족**이 된다.
- 지금 값 0.022는 우연이 아니라 **주인의 평균 획 비율(0.050535/키) × 0.45 = 0.02274**의 반올림이다.
  즉 **자기닮음(self-similar) 미니어처**로 정확히 설계돼 있다. 그 의도를 유지해야 한다.
- **권고: `StrokeRatio`는 0.022 그대로 둔다.**

### 3-4. 그럼 격차의 정체는 무엇인가 — 남은 세 가지

측정으로 3-1~3-3을 걷어내면 주인/펫 차이는 **세 개만** 남는다.

| # | 차이 | 실측 | 참고는? |
|---|---|---|---|
| **A** | **펫에 무릎/팔꿈치가 없다** | 펫 마디는 sagitta = 현의 9%인 **고정 활** 하나. 걸어도 굽지 않는다 | 참고 인물 전원 무릎·팔꿈치가 접힌다. 이게 "몸이 뚝딱거림"의 정체 |
| B | 펫 획이 전 배율 2.00pt로 **고정**이다 | 주인은 2.00 → 4.42pt로 자란다. 배율 1.0에서 주인 4.42 : 펫 2.00 = 2.21배 | 참고는 한 화면 인물이 모두 같은 획. 여기선 미니어처라 다른 게 맞다 |
| C | 펫 머리가 링, 주인이 채움 | 3-2에서 확인: **참고에도 두 형식이 공존한다.** 크기 법칙도 이미 맞다 | 정체성 차이지 화풍 파탄이 아니다 |

> **A가 유일한 진짜 결함이다.** C는 결함이 아니다. B는 미니어처의 정의상 불가피하다.

### 3-5. 제안 M2-B — 펫에 무릎/팔꿈치를 준다 (**A 해결, 권고 1순위**)

**핵심: 새 기하학을 만들지 않는다. 본체가 쓰는 함수를 그대로 부른다.**
`LimbCurveRenderer.BuildLimbPolyline(upperLength, lowerLength, angleDegrees, strokeWidth, dest)`는
이미 **Transform 계층 없이 2분절 마디 하나를 납작한 폴리라인으로** 굽는다(정보창 초상화가 이미 쓴다).
펫이 이걸 부르면 **"같은 시각 언어"가 문자 그대로 같은 수식**이 된다.

**마디 분할비 — 주인의 비를 그대로 옮긴다:**
- 다리: 주인 0.50 : 0.45 → **0.5263 : 0.4737**
- 팔:   주인 0.38 : 0.37 → **0.5067 : 0.4933**

**펫 마디 현(chord) 길이**(현재 `AppearanceShapeBuilder.MiniFigure` 값에서 유도, h = 펫 키):
- 다리 = `hypot(0.10, 0.40)·h` = **0.41231 h** → 위 0.21701 h / 아래 0.19530 h
- 팔(뒤) = `hypot(0.10, 0.30)·h` = **0.31623 h** → 위 0.16023 h / 아래 0.15600 h
- 팔(앞) = `hypot(0.14, 0.30)·h` = **0.33106 h** → 위 0.16775 h / 아래 0.16331 h

**굽힘 각도 안전 상한**(4절 규칙 B, `FilletLengthRatio = 0.35` 기준):

| 배율 | 펫 획 | 다리 위/아래(pt) | **무릎 최대** | 팔 위/아래(pt) | **팔꿈치 최대** |
|---|---|---|---|---|---|
| 0.35 | 2.00pt | 2.74 / 2.47 | **81.2°** | 2.02 / 1.97 | **68.9°** |
| 0.75 | 2.00pt | 5.87 / 5.29 | **122.4°** | 4.34 / 4.22 | **111.1°** |
| 1.00 | 2.00pt | 7.83 / 7.05 | **135.1°** | 5.78 / 5.63 | **125.4°** |

→ 걷기 무릎 스윙에 필요한 각도(주인 키표 최대 50°)는 **전 배율에서 여유가 크다**.
→ 4절 제안(`FilletLengthRatio` 0.42)을 함께 넣으면 배율 0.35 상한이 **무릎 91.5° / 팔꿈치 78.8°**로 오른다.

**함께 손봐야 하는 자리(빠뜨리면 조용히 깨진다):**
1. `AppearanceShapeBuilder.MiniLimbBowRatio` / `MiniLimbPoints` / `Limb()`는 **무릎이 생기면 역할이 끝난다.**
   활(bow)과 필렛을 동시에 넣으면 마디가 두 번 휜다.
2. `CharacterPetRenderer.LimbNeutralDegrees`는 **마지막 점**을 읽는다 — `BuildLimbPolyline`도 마지막 점이 끝점이므로 **계약 유지 ✔**.
3. `MakeLine`은 **0번 점**을 회전축으로 옮긴다 — `BuildLimbPolyline`의 0번 점은 뿌리(0,0)이므로 **계약 유지 ✔**.
4. 테스트 `펫_네_마디가_모두_같은_쪽으로_굽어_O자_X자_다리가_되지_않는다`는 **중간 점의 볼록 방향**으로 판정한다.
   무릎이 생기면 중간 점이 무릎이 되므로 **이 테스트는 무릎 부호(무릎은 뒤, 팔꿈치는 앞) 검사로 재작성해야 한다.**
5. 점 수: 마디당 4점 → 9점(중복점 제거 후, 4-5절 참고). 4마디 × 5점 = **정점 20개 증가** — 무시할 비용.

### 3-6. 제안 M2-C — 펫 머리를 채우고 싶다면 (리더가 그래도 원할 경우의 정확한 기하)

리더 메모대로 `r/W = 1.894`(배율 0.75)이라 **닫힌 원 하나로는 안이 안 채워진다**(채우려면 `W ≥ 2r`).
주인이 쓰는 `CreateFilledDisc` 법칙을 그대로 옮기면 된다: **바깥반경 R → 경로반경 R/2.2, 폭 1.0909R.**

**두 가지 선택지 — 반드시 골라야 한다:**

| 안 | 바깥반경 R (배율 0.75) | 경로반경 | 채움 폭 | 머리 지름 | 펫이 몇 머리? | 평가 |
|---|---|---|---|---|---|---|
| **C-1 현재 크기 유지** | 4.789pt | 2.177pt | 5.224pt | 9.58pt | **2.83** | ★ **참고 법칙 위반.** 링을 그대로 채우면 채움 머리가 참고 기준의 **1.55배**가 된다 = 대두 인형 |
| **C-2 0.45 미니어처로 축소** | 3.067pt | 1.394pt | 3.346pt | 6.13pt | **4.41** | 참고 준수. 주인(4.41 머리)과 **정확히 같은 비율**이 된다 |

- **C-2를 쓸 경우의 새 상수**: 펫 머리 바깥반경 = **0.1133 × 펫 키**(현행 링 경로반경 0.14h 대신).
  검산: 0.1133 × 27.06pt × 2 = 6.13pt ✔ = 0.45 × 주인 머리 잉크 지름(13.63pt) ✔
  ★ 단일 상수로는 정확히 맞지 않는다: 주인 머리 링(0.063s)이 배율 0.9 아래에서 2pt 하한에 눌려
  주인 머리가 배율에 정비례하지 않기 때문이다. 배율별 정확값은 **0.1106**(1.00) / **0.1133**(0.75) /
  **0.1323**(0.35). 상수 하나로 갈 거면 **출하 기본 배율 0.75의 0.1133**을 쓰고, 배율 0.35에서
  머리가 의도보다 14% 작아지는 것을 허용하거나, 주인 머리 실측(`StickmanMetrics.HeadRadius`)에서
  파생시켜라(후자가 옳다 — 이 저장소의 다른 렌더러가 전부 그렇게 한다).
- **C-2 부작용**: 펫이 2.98머리 → 4.52머리가 된다. **"작고 귀여운 동반자"에서 "그냥 작은 사람"으로 바뀐다.**
  이건 조형 결정이지 버그 수정이 아니다 — **리더가 사용자에게 물어야 하는 사안**이다.
- **배율 0.35 주의**: C-2의 채움 폭 1.823pt < 하한 2.00pt라 `StickmanAgent._dynamicVisuals` 훑기가
  2.00pt로 올린다 → 바깥반경이 1.671 → 1.760pt로 **5% 커진다.** 그 정도는 허용 범위지만 알고 있어야 한다.
- **구현 주의**: `MakeLine`은 폭을 `RenderStroke`로 **하드코딩**한다. 채움 선은 폭이 다르므로
  `MakeLine(..., float width = 0f)` 같은 선택 인자가 필요하다(0이면 `RenderStroke`).
  그리고 **채움 선을 `_lines` 배열의 맨 뒤(인덱스 6)에 넣어야 한다** — 앞에 끼우면
  `ApplyMiniPose`/`_miniLimbNeutral`이 쓰는 **팔다리 인덱스 2~5가 통째로 밀린다.**

> **내 권고 순위: M2-B(무릎) ≫ M2-C. 획(M2-A)은 손대지 않는다.**
> 머리 채움은 "격차"의 원인이 아니고(3-2), 채우면 오히려 참고에서 멀어지거나(C-1)
> 펫의 성격을 바꾼다(C-2).

---

## 4. 임무 ③ — 최단 선분 / 최대 꺾임각 안전 한계

### 4-1. 규칙을 정확히 세 개로 분리한다 (여기서 지금까지 섞여 있었다)

두께 W(반폭 ρ = W/2)인 폴리라인이 각 θ로 꺾일 때 안쪽 오프셋 선은 꼭짓점에서 **ρ·tan(θ/2)** 만큼
뒤로 물러난 자리에서 만난다. 따라서:

| 규칙 | 조건 | 언제 쓰나 |
|---|---|---|
| **A — 고립 코너** | `선분 ≥ ρ·tan(θ/2)` | 양옆이 긴 직선인 **단발 꺾임**. `LimbCurveRenderer`의 "발" 기록이 적은 그 규칙 |
| **B — 단조 체인** | `선분 ≥ 2ρ·tan(Δφ/2) = W·tan(Δφ/2)` | **연속한 꼭짓점이 전부 같은 쪽으로** 꺾이는 경우. 원호 표본이 여기 해당. 양 끝에서 동시에 깎이므로 A의 **2배**가 필요하다 |
| **C — 곡률 반경** | `r ≥ ρ` | 이미 `LimbCurveGeometryTests (4)`가 검사 중 |

★ **B와 C는 사실상 같은 규칙이다.** 원호를 Δφ 간격으로 표본하면
`선분 = 2r·sin(Δφ/2)`이므로 B ⟺ `r ≥ ρ / cos(Δφ/2)`.
Δφ = 21°(우리 최대)에서 `1/cos(10.5°) = 1.017` — 즉 **B는 C를 1.7% 조인 것**이다.
따라서 **본체 팔다리는 새 테스트가 필요 없다. (4)번 테스트가 이미 그 규칙이다.**

★ **"선분 ≥ 획 W"(펫 테스트의 규칙 1)는 규칙이 아니라 휴리스틱이다.**
꺾임각 2·atan(2) = 126.87° 이상에서만 정확한 규칙보다 엄격해진다.
우리 필렛 원호의 꺾임은 21° 이하이므로 이 휴리스틱은 **필요량의 12~16배**를 요구한다 —
본체 팔다리에 그대로 적용하면 **안전한 형상을 오탐으로 반려한다.** 적용하지 마라.

### 4-2. ★ 발견 1 — **본체 팔다리는 배율 0.45 미만에서 이미 규칙을 어기고 있다**

전 자세 × 3배율 실측(전체 표는 4-6절). 각 마디의 최악값:

| 배율 | 다리 획 | 최악 자세 | Δφ | 최단 선분 | 필요량(B) | **여유** | r/ρ (테스트4) |
|---|---|---|---|---|---|---|---|
| **0.35** | 2.00pt | 무릎앉아 앞 126° | 21.00° | **0.283pt** | 0.371pt | **0.76×** ❌ | **0.776** ❌ |
| 0.45 | 2.00pt | 무릎앉아 앞 126° | 21.00° | 0.364pt | 0.371pt | 0.98× ❌ | 0.998 ❌ |
| 0.50 | 2.21pt | 무릎앉아 앞 126° | 21.00° | 0.447pt | 0.410pt | 1.09× | 1.109 |
| 0.75 | 3.32pt | 무릎앉아 앞 126° | 21.00° | 0.773pt | 0.614pt | 1.26× | 1.280 |
| 1.00 | 4.42pt | 무릎앉아 앞 126° | 21.00° | 1.031pt | 0.819pt | 1.26× | 1.280 |

| 배율 | 팔 획 | 최악 자세 | Δφ | 최단 선분 | 필요량(B) | **여유** | r/ρ |
|---|---|---|---|---|---|---|---|
| **0.35** | 2.00pt | 주위 살피기 122° | 20.33° | **0.310pt** | 0.359pt | **0.86×** ❌ | **0.878** ❌ |
| 0.40 | 2.00pt | 주위 살피기 122° | 20.33° | 0.399pt | 0.359pt | 1.11× | 1.129 |
| 0.75 | 2.76pt | 주위 살피기 122° | 20.33° | 0.670pt | 0.495pt | 1.35× | 1.374 |
| 1.00 | 3.68pt | 주위 살피기 122° | 20.33° | 0.893pt | 0.661pt | 1.35× | 1.374 |

**임계 배율(이분 탐색):** 다리 = **0.451**, 팔 = **0.398**.
`MinCharacterScale = 0.35`이므로 **다이얼 하단 0.35~0.45 구간이 위반 구간**이다.

**원인은 화면상 2pt 하한이다.** 배율이 내려가면 기하학은 줄지만 획은 2pt에서 멈춘다.
배율 0.35에서 실효 획 배수는 다리 **1.29배**, 팔 **1.55배**다.

**기존 테스트가 왜 못 잡았나:** `LimbCurveGeometryTests`의 (3)(4)는 `ReadLimbSpec(prefab)`로
**프리팹에 구워진 폭**을 읽는다. 프리팹은 `characterScale = 0.75`로 구워지므로
**언제나 배율 0.75 하나만 검사한다.** 다이얼을 내렸을 때의 조합은 검사 범위 밖이다.

### 4-3. ★ 발견 2 — `SolveFilletLength`의 **단위 불일치**(이게 위반을 2배로 키운다)

```csharp
// LimbCurveRenderer.Rebuild()
float width = limb.LowerLine.startWidth;   // ← 월드 단위 (LineRenderer 폭은 Transform 스케일을 안 따라간다)
// LimbCurveRenderer.SolveFilletLength()
float sagitta = t * Mathf.Tan(0.5f * halfAngle);   // ← t는 로컬 단위 (프리팹 좌표)
if (sagitta > MaxSagittaPerStrokeWidth * width) t *= maxSagitta / sagitta;   // ← 로컬 vs 월드 비교
```

`ApplyCharacterScale`은 루트 `localScale = 배율/0.75`를 주지만 **마디 길이는 프리팹 로컬 값 그대로**다.
반면 획은 월드 값이다. 배율이 0.75가 아니면 이 비교의 단위가 어긋난다.

| 배율 | 루트 스케일 | 캡이 실제로 허용하는 sagitta | 의도 | 결과 |
|---|---|---|---|---|
| 0.35 | 0.4667 | **0.47 W** | 1.00 W | 캡이 2.1배 **과하게** 조여 t가 작아지고 r이 작아진다 |
| 0.75 | 1.0000 | 1.00 W | 1.00 W | 정확 |
| 1.00 | 1.3333 | 1.33 W | 1.00 W | 관절이 의도보다 33% 더 뭉툭해진다 |

**영향 크기**: 단위만 고쳐도 배율 0.35 다리의 `r/ρ`가 **0.776 → 0.990**으로 올라간다(위반 폭 22% → 1%).

★ **그리고 이 버그가 있는 한 `FilletLengthRatio`를 올려도 낮은 배율에서는 아무 효과가 없다.**
캡이 걸리면 `t = W/tan(h/2)`가 되어 원래 t와 무관해지기 때문이다.
(검산: 배율 0.35·다리·126°에서 `FilletLengthRatio` 0.35든 0.42든 캡 후 `t = 0.092590`으로 **동일**.)

### 4-4. 제안 M3 — 두 줄 수정으로 전 배율 안전 확보

| 순서 | 수정 | 근거 |
|---|---|---|
| **M3-1** | `SolveFilletLength`에 넘기는 획 두께를 **로컬 단위로 환산**한다(획 ÷ 루트 lossyScale). 또는 `Rebuild`가 `limb.LowerLine.startWidth / transform.lossyScale.y`를 넘긴다 | 4-3. 단독으로 위반 폭 22% → 1% |
| **M3-2** | `FilletLengthRatio` **0.35 → 0.42** | M3-1 이후에야 효력이 있다. 4-5의 검산 참조 |

**M3-1 + M3-2 적용 후 검산(최악값):**

| 배율 | 다리 여유(B) | 다리 r/ρ | 팔 여유(B) | 팔 r/ρ | 다리 sagitta/W |
|---|---|---|---|---|---|
| 0.35 | **1.17×** ✔ | 1.188 | **1.05×** ✔ | 1.063 | 0.71 |
| 0.45 | 1.50× | 1.528 | 1.34× | 1.366 | 0.92 |
| 0.75 | 1.51× | 1.536 | 1.62× | 1.649 | 0.92 |
| 1.00 | 1.51× | 1.536 | 1.62× | 1.649 | 0.92 |

- 전 배율 · 전 자세에서 규칙 B **여유 ≥ 1.05배**. 위반 0건.
- sagitta는 어디서도 `MaxSagittaPerStrokeWidth = 1.0`을 넘지 않는다 → **기존 테스트 (3) 통과 유지.**
- "뼈"가 남는가: 배율 0.75에서 다리 위 마디 직선 구간이 **9.05pt → 8.22pt**(−9%),
  배율 0.35에서 **4.65pt → 3.84pt**. 마디의 **58~62%가 여전히 직선**이다(0.5를 넘으면 활이 되는데 0.42는 안전).
- **최악 여유가 1.05배(팔·배율 0.35·122°)로 빠듯하다.** 여유를 더 원하면
  `idleAmbientLookElbowDegrees = 122°`를 낮추는 쪽이 더 싸다 — 다만 그건 **design-motion 소관**이므로
  리더가 전달해야 한다. 110°로 낮추면 같은 조건 여유가 1.05 → 1.53배가 된다.

### 4-5. ★ 발견 3 — `BuildLimbPolyline`이 **길이 0인 선분**을 낸다 (현재 프로덕션)

`LimbCurveRenderer.BuildLimbPolyline`은 `PolylinePointCount = 10`점을 채우는데,
**인덱스 4와 5가 같은 좌표**다(두 반호가 관절에서 정확히 만나도록 설계돼 있으므로 필연이다).

수치 검증(Lu=0.3750, Ll=0.3375, θ=126°, W=0.09405 — 배율 0.75 다리):

```
선분 0→1  9.0548pt
선분 1→2  0.7733pt
선분 2→3  0.7733pt
선분 3→4  0.7733pt
선분 4→5  0.0000pt   ← ★ 길이 0
선분 5→6  0.7733pt
선분 6→7  0.7733pt
선분 7→8  0.7733pt
선분 8→9  7.7330pt
```

- 지금 이 폴리라인을 그리는 소비자는 **정보창 초상화의 미니 피규어**다(`CharacterPortraitStage.DrawLimb`).
- **육안 영향은 미확인**이다 — LineRenderer가 길이 0 구간을 어떻게 처리하는지 실기로 확인하지 않았다.
  다만 "두꺼운 폴리라인 안의 퇴화 선분"은 2026-09-01 발 실패와 **정확히 같은 계열**이고, 구조적으로 제거 대상이다.
- **권고**: `PolylinePointCount`를 `PointsPerSegment * 2 - 1 = 9`로 내리고 관절점을 한 번만 쓴다.
  (3-5의 펫 무릎 제안이 이 함수를 쓰게 되므로 **그 전에 고쳐야 한다.**)
- ★ **주의**: `PolylinePointCount`는 초상화가 버퍼 크기로 읽는 공개 계약이다. 함께 고쳐야 한다.

### 4-6. 전 자세 안전 한계표 (현행 / 배율 0.35 — 가장 위험한 조합)

`Δφ` = 필렛 원호 표본 하나의 꺾임각. `여유` = 최단 선분 ÷ 규칙 B 필요량.

| 상태 | 무릎 θ | Δφ | 최단선분 | 필요량 | 여유 | 팔꿈치 θ | Δφ | 최단선분 | 필요량 | 여유 |
|---|---|---|---|---|---|---|---|---|---|---|
| IDLE 서 있기 | 4° | 0.67° | 0.647pt | 0.012 | 55.6× | 10° | 1.67° | 0.531 | 0.029 | 18.3× |
| ARCHERY 활쏘기 | 12° | 2.00° | 0.645 | 0.035 | 18.5× | — | | | | |
| LEDGE HANG 매달리기 | 14° | 2.33° | 0.644 | 0.041 | 15.8× | 8° | 1.33° | 0.532 | 0.023 | 22.9× |
| GROUND-LOSS HANG | — | | | | | 22° | 3.67° | 0.526 | 0.064 | 8.2× |
| FALL 낙하 | 38° | 6.33° | 0.623 | 0.111 | 5.6× | 20° | 3.33° | 0.527 | 0.058 | 9.1× |
| PARKOUR 맨틀 | 39° | 6.50° | 0.622 | 0.114 | 5.5× | 18° | 3.00° | 0.528 | 0.052 | 10.1× |
| DRAG 발버둥 | 40° | 6.67° | 0.621 | 0.116 | 5.3× | 38° | 6.33° | 0.513 | 0.111 | 4.6× |
| WALK 키표 최대 | 50° | 8.33° | 0.606 | 0.146 | 4.2× | 25° | 4.17° | 0.524 | 0.073 | 7.2× |
| LANDING CROUCH 뒷무릎 | 55° | 9.17° | 0.597 | 0.160 | 3.7× | 24° | 4.00° | 0.525 | 0.070 | 7.5× |
| WALK 진폭 1.35배 | 67.5° | 11.25° | 0.570 | 0.197 | 2.9× | 33.8° | 5.62° | 0.517 | 0.098 | 5.3× |
| RAGDOLL 관절 한계 | 100° | 16.67° | 0.473 | 0.293 | 1.6× | 100° | 16.67° | 0.389 | 0.293 | 1.3× |
| THROW TUMBLE | 104° | 17.33° | 0.451 | 0.305 | 1.5× | 96° | 16.00° | 0.400 | 0.281 | 1.4× |
| IDLE AMBIENT 주위살피기 | — | | | | | **122°** | 20.33° | **0.310** | **0.359** | **0.86× ❌** |
| **LANDING CROUCH 앞무릎** | **126°** | 21.00° | **0.283** | **0.371** | **0.76× ❌** | 26° | 4.33° | 0.523 | 0.076 | 6.9× |

(배율 0.75 / 1.00에서는 최악값이 각각 1.26× / 1.26×(다리), 1.35× / 1.35×(팔)로 **위반 없음.**)

### 4-7. 규칙 적용 예외 — 오탐을 막기 위해 명시한다

| 대상 | 규칙 적용? | 이유 |
|---|---|---|
| 팔다리 위/아래 마디 (5점) | **적용** | 규칙 B. 단, 위/아래는 **별개 LineRenderer**라 관절에서 폴리라인 조인이 아예 없다(둥근 캡 두 개가 겹칠 뿐). 발이 실패한 이유가 정확히 이것 — 발은 **같은 폴리라인에 붙었다** |
| Torso (2점 직선) | 해당 없음 | 꺾임 0 |
| `HeadOutline` 24각 링 | 적용 · 여유 충분 | 배율 1.0 13.8×, 배율 0.35 5.4× |
| **`HeadFill`** | ★ **예외** | 경로반경 r을 폭 2.4r로 그리는 **의도적 과채움**이다. `W/2 = 1.2r > r`이라 규칙을 정의상 위반한다. 규칙은 "획으로 읽혀야 하는 선"에만 적용한다. 자동 검사를 만들 때 **반드시 제외 목록에 넣어라** |
| 펫 마디 (현행 4점 활) | 적용 · 여유 5.6~21× | 4-1의 "선분 ≥ W" 휴리스틱은 필요량의 **12배**를 요구한다. 실제 위험 없음 |

### 4-8. 리더에게: 테스트 배정 제안

1. `LimbCurveGeometryTests (3)(4)`가 **프리팹 폭 하나만** 보는 것을 고친다 —
   `MinCharacterScale ~ MaxCharacterScale` 구간을 훑고, 각 배율에서
   `W = max(bakedW × s/0.75, StickConfig.MinStrokeScreenPoints / ReferencePointsPerWorldUnitApprox)`,
   길이는 프리팹 로컬 × `s/0.75`로 계산한다. **숫자를 테스트에 베끼지 말고 전부 프로덕션 상수를 참조할 것**(CLAUDE.md).
2. 본체 팔다리에 **별도의 "최단 선분" 테스트를 새로 만들지 마라.** 4-1에서 증명했듯
   규칙 B는 (4)번 테스트를 `1/cos(Δφ/2)`만큼 조인 것이다. (4)번의 임계값을
   `1.0` → `1f / Mathf.Cos(0.5f * dphi)`로 바꾸는 **한 줄**이면 된다.
3. 펫 테스트 `선분이_획보다_짧지_않다`는 휴리스틱임을 주석에 명시하고, 같은
   `1/cos` 형태로 통일할 것을 권한다(지금은 필요량의 12배를 요구한다).
4. `BuildLimbPolyline`의 길이 0 선분(4-5)에 대한 회귀 테스트 1건.

---

## 5. 우선순위 요약 (리더 배정용)

| 순위 | 항목 | 담당 제안 | 비용 | 근거 |
|---|---|---|---|---|
| **P0** | M3-1 단위 불일치 + M3-2 `FilletLengthRatio` 0.42 | coder | 2줄 + 테스트 | 4-2/4-3. 다이얼 하단이 지금 깨져 있다 |
| **P0** | 4-8-1 테스트를 전 배율로 확장 | test-engineer | 테스트만 | 못 잡는 이유 그 자체 |
| **P1** | M1-A 잉크 유채색 5종 추가 | coder(+ux-designer가 기본값 결정) | enum + 색표 | 2-2. 얼굴 문제의 진짜 원인 |
| **P1** | M2-B 펫에 무릎/팔꿈치 | coder | 기존 함수 재사용 | 3-4-A. 펫 격차의 유일한 실질 원인 |
| **P2** | 4-5 `BuildLimbPolyline` 길이 0 선분 제거 | coder | 상수 1 + 루프 | M2-B의 선행 조건 |
| **보류** | M2-C 펫 머리 채움 | — | — | 3-6. 사용자 확인 필요(펫 성격이 바뀐다) |
| **보류** | M1-B 무채색일 때 머리 링 | — | — | 2-4. 사용자 직접 지시와 충돌 |
| **반려** | `StrokeRatio` 0.022 → 0.036 | — | — | 3-3. 참고 법칙과 반대 방향 |

---

## 6. 미확인 · 남은 한계 (성급히 단정하지 않는다)

1. **실제 빌드 캡처로 최종 판정하지 않았다.** PID 27208이 검증용으로 떠 있어 앱을 띄우지 않았다.
   이 문서의 모든 수치는 **소스 상수 + 참고 이미지 실측**에서 나온 것이다.
   4-2의 위반(배율 0.35 무릎앉아)이 **눈에 보이는 노치로 나타나는지는 미확인**이다 —
   규칙 위반은 "안쪽 윤곽이 자기교차한다"는 기하학 사실이고, LineRenderer가 그것을 어떻게
   삼각형으로 바꾸는지(단색 겹침이라 안 보일 수도, 알파 이중합성으로 어둡게 뭉칠 수도 있다)는 실기 확인이 필요하다.
   → **리더가 배율 0.35 + 무릎앉아 착지 캡처 1장**을 잡아 주면 확정된다.
2. **`LineRenderer.startWidth`가 Transform 스케일을 따라가지 않는다**는 전제는
   `StickmanAgent`의 기존 주석과 실측 기록(0.056738 = 정확히 2.00pt)을 신뢰해 그대로 썼다. 직접 재확인하지 않았다.
3. `798295cc`(벤치 커플)의 흰 인물은 배경과 색이 겹쳐 마스크 분리에 실패했다.
   그 프레임의 "링/채움 = 1.55" 검증은 못 했다. 대신 `8875f6ce`·`d0e80eae` 두 프레임에서 1.550 / 1.545로 확인했다.
4. 참고 이미지는 전부 **동영상 프레임 캡처**다. 모션 블러/압축으로 획 두께가 ±1px 흔들린다.
   그래서 내 측정치(21.1~22.4%)가 팀 확정값(22.3% ± 0.2%)보다 평균 0.5%p 낮게 나온다 — **모순이 아니라 같은 값이다.**
5. 참고 인물의 **어깨 높이**는 실루엣 분리점에서 역산했다(팔이 몸통과 겹쳐 직접 못 잰다).
   추정 오차 ±0.2R. "우리 목이 참고보다 약간 길다"는 이 오차 안에 있을 수 있다.

---

## 7. 플랫폼 영향

- **macOS 영향: 있음(설계 대상 그 자체).** 이 문서의 모든 수치는 macOS 실행 기준(Retina, `ReferencePointsPerWorldUnitApprox = 35.25pt/유닛`)이다.
- **Windows 영향: 없음.** 이 문서가 다루는 것은 전부 **플랫폼 중립 코드**다 —
  `Editor/SceneBootstrapper.cs`(프리팹 굽기), `Core/StickmanStrokeWidths.cs`, `Core/StickConfig.cs`,
  `States/LimbCurveRenderer.cs`, `Interaction/AppearanceShapeBuilder.cs`,
  `Interaction/CharacterPetRenderer.cs`. `Platform/MacOS/`·`Platform/Windows/` 아래 파일은 **한 개도 언급하지 않았다.**
  단, `MinStrokeScreenPoints`(2pt)를 월드로 바꾸는 환산은 **화면 DPI/카메라 크기에 의존**하므로,
  Windows의 100%/125%/150% 스케일링에서 4-2의 임계 배율(0.451 / 0.398)이 달라질 수 있다.
  → **구현 시 test-engineer가 `PlatformParityAuditTests`에 "획 하한 환산이 플랫폼 중립인가" 항목 1건**을
  추가할 것을 권한다(지금 당장 고칠 갭은 아니므로 `Assert.Ignore` 사유 기재로 남겨도 된다).
- 사용자 지시("윈도우는 미루고 맥만")에 따라 Windows 전용 수정은 하지 않았고, 필요도 없다.

---
---

# R2 — 검증 라운드 (2026-09-01 야간, design-character)

> 배정: "오늘 밤 팔다리 기하가 전면 교정됐다. 네 사양이 실제로 그대로 들어갔는지 검증하고,
> 남은 조형 판단 3건을 내려라." · 사용자 지시 "윈도우는 일단 미루고 맥만 중점적으로."
>
> **이 라운드에서 처음으로 실제 빌드 캡처로 잰다.** 6절 미확인 ①②를 닫는다.
> 프로덕션 `.cs`는 한 줄도 수정하지 않았다(읽기만).

## 8. 측정 기준 — 이번 라운드가 쓴 숫자의 출처

### 8-1. 실행 중인 빌드 실측 (읽기 전용 캡처 1장, 앱은 건드리지 않음)

`screencapture`(읽기 전용)로 캡처 1장만 받았다. 앱 조작·빌드·`driver.sh stop` 없음.

> ★ 배정문의 PID 37699는 이 라운드 도중 **다른 경로로 재시작됐다**(내가 죽인 것이 아니다 — `screencapture`
> 외에는 아무것도 실행하지 않았다). 측정 종료 시점의 인스턴스는 **67542**(`/tmp/stickmate-run/`)와
> **68627**(`/tmp/stickmate-jaehyun/`) 둘이다. **리더 확인 요망.**
> 아래 수치는 `/tmp/stickmate-run/stickmate.log`의 실행 환경과 같은 시점 캡처에서 나왔고,
> 캡처 실측(9-1)이 그 로그의 배율 0.60 예측과 ±2%로 맞으므로 자기정합적이다.

```
/tmp/stickmate-run/screenshots/designchar-verify-band.png   (3024×360 물리픽셀, Retina 2×)
```

로그(`/tmp/stickmate-run/stickmate.log`)에서 읽은 실행 환경:

| 값 | 실측 |
|---|---|
| `Screen` (Unity 픽셀) | 3024 × 1964 |
| `desktopDpiScale` (Retina) | 0.500 → 화면 높이 **982 OS pt** |
| `cam.orthographicSize` | 12.00 |
| **월드 1유닛당 OS 포인트** | 982 / 24 = **40.9167 pt/유닛** |
| 월드 1유닛당 물리픽셀 | 1964 / 24 = **81.8333 px/유닛** |
| 적용 배율 / 구워진 배율 | **0.60** / 0.750 (루트 스케일 0.800) |

### 8-2. ★ 하한(2pt)의 월드 값이 **두 개**다 — 이번 라운드의 핵심 사실

| 이름 | 값(월드 유닛) | 어디서 쓰나 |
|---|---:|---|
| `F_bake` = 2 / `ReferencePointsPerWorldUnitApprox`(35.25) | **0.0567376** | Editor/SceneBootstrapper가 **프리팹을 구울 때** |
| `F_run` = 2 / 40.9167 | **0.0488798** | `StickmanAgent.ResolveMinStrokeWorldWidth()`가 **런타임에** |

`F_run`이 `F_bake`보다 **13.8% 작다**(이 맥이 굽기 근사보다 넓은 창을 쓰기 때문).
두 값이 다르다는 사실 자체가 아래 9-3·10-2의 원인이다.

> **리더 표의 하한과 다르다.** 배정문 임무 2의 표(1.0→16.5% / 0.75→20.9% / 0.35→36.1%)는
> 역산하면 하한 **0.08695 유닛**(≈ 23.0 pt/유닛)에서 나온 값이고, 계산식은 `액세서리 획 ÷ 2R`이다
> (`몸 획`이 아니다 — 세 배율 모두 소수점 셋째 자리까지 재현된다).
> 이 맥의 실측값도 굽기 근사값도 아니다. **출처 미확인.** 아래 표는 전부 위 두 값으로 다시 계산했다.

### 8-3. 참고 이미지 재측정 (색 마스크 + 정확 EDT, 정지·직립 자세만)

`uploads/…/8875f6ce-image.png` (@alanbecker, 인물 3인). **숙인 노랑은 제외**(단축).

| 인물 | 자세 | 머리 | 획(EDT 능선) | 머리 잉크 지름 | 획 ÷ 머리 지름 |
|---|---|---|---:|---:|---:|
| 파랑 | 걷기·몸통 직립 | **채움** | 32.0 px(중앙값) / 32.7 px(가중평균) | **146.2 px** | **21.9 ~ 22.4 %** |
| 주황 | 정지·직립 | 링(속 빈 원) | 31.1 px | 바깥 폭 ~196 px | 15.8 % |

- **채운 머리 인물이 21.9~22.4%** — 팀 확정값 **22.3% ± 0.2%와 일치**한다. 뒤집을 근거 없음.
- 주황(링 머리)이 15.8%인 것은 3-2절이 이미 확정한 사실의 재확인이다
  (**22.3%는 속 빈 머리에 적용되는 법칙이 아니다**). 우리 머리는 채움이므로 파랑 쪽이 기준이다.
- 참고 획의 산포는 30~36 px(±6%)이고 대부분 관절 접합부의 EDT 팽창이다 —
  **참고에는 팔/몸통/다리 두께 차이가 없다**(1-4절 "taper 없음"과 같은 결론).

---

## 9. 임무 ① 판정 — **네 사양은 그대로 들어갔다** (배율 ≥ 0.65 구간에서 정확히)

### 9-1. 실물 캡처 실측 vs 예측 — `LineRenderer.startWidth`는 월드 유닛이 맞다

배율 0.60, 81.8333 px/유닛. 측정은 부분 커버리지(α)를 적분하고 마디 기울기로 수직폭 보정.

| 부위 | **캡처 실측(물리px)** | 예측 A: `max(baked·s/0.75, F_run)` | 예측 B: 예측 A × lossyScale(0.8) |
|---|---:|---:|---:|
| 몸통(수직) | **5.844** | 5.645 (+3.5%) | 4.516 (−22.7%) |
| 왼다리 | **6.169** | 6.157 (**+0.2%**) | 4.926 (−20.1%) |
| 오른다리 | **6.196** | 6.157 (+0.6%) | 4.926 (−20.5%) |
| 오른팔(전완) | **5.260** | 5.130 (+2.5%) | 4.104 (−22.0%) |
| 왼팔(전완) | **5.210** | 5.130 (+1.6%) | 4.104 (−21.3%) |

→ **예측 A가 평균 +1.7% 오차로 맞는다**(MSAA 4× 커버리지 양자화 0.25px + 라운드캡 팽창 범위).
→ **6절 미확인 ②를 닫는다**: `LineRenderer.startWidth`는 Transform 스케일을 따라가지 않는다.
   `StickmanAgent`의 기존 주석이 옳다.

> ⚠ **부수 발견(진단 전용, 조형 무관)**: `Platform/MacOS/MacOverlayStateEnforcer.LogRenderQualityDiagnostics`는
> `startWidth × lossyScale.x × pixelsPerUnit`으로 찍는다. 위 실측에 따르면 **`lossyScale`을 곱하면 안 된다**.
> 그래서 로그의 "획 두께 실측 3.20~9.43 물리픽셀"은 배율 0.6에서 실제보다 20% 작다(참값 4.00~11.79).
> 프로덕션 그림에는 영향 없다 — **로그 숫자만 틀린다.** 리더 판단 사항(내 소관 아님).

### 9-2. 획 ÷ 머리 잉크 지름 — 전 배율표

분모는 **2R + 링 두께**다(2R 아님). `링(s) = max(0.0567376·s/0.75, F_run)`, `D(s) = 0.44s + 링(s)`.

| 배율 | 팔 pt | 몸통 pt | 다리 pt | 링 pt | 머리 잉크 지름 pt | 팔 % | **몸통 %** | 다리 % | 평균 % |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0.35 | 2.00 | 2.00 | 2.00 | 2.00 | 8.30 | 24.09 | 24.09 | 24.09 | **24.09** |
| 0.40 | 2.00 | 2.00 | 2.05 | 2.00 | 9.20 | 21.74 | 21.74 | 22.31 | 21.93 |
| **0.468** | 2.00 | 2.20 | 2.40 | 2.00 | 10.42 | **19.19** | 21.11 | 23.03 | **21.11** ← 최악 |
| 0.50 | 2.14 | 2.35 | 2.57 | 2.00 | 11.00 | 19.43 | 21.38 | 23.32 | 21.38 |
| 0.60 | 2.57 | 2.82 | 3.08 | 2.00 | 12.80 | 20.04 | 22.04 | 24.05 | 22.04 |
| 0.646+ | — | — | — | — | — | 20.27 | **22.29** | 24.32 | **22.29** |
| **0.75**(출하) | 3.21 | 3.53 | 3.85 | 2.32 | 15.82 | 20.27 | **22.29** | 24.32 | **22.29** |
| **1.00** | 4.28 | 4.70 | 5.13 | 3.10 | 21.10 | 20.27 | **22.29** | 24.32 | **22.29** |

**판정: 목표 22.3% ± 0.2% — 배율 0.646 이상 전 구간에서 22.29%. 정확히 맞는다.**
사용자 신고("몸의 선 자체가 얇아서")에 대한 대응은 **성공했다.** 되돌리지 마라.

하한 탈출 배율(이 맥): **다리 0.3898 / 몸통 0.4252 / 팔 0.4677 / 링 0.6461 / 액세서리 1.0183.**
(굽기 근사 화면에서는 각각 0.4525 / 0.4936 / 0.5429 / 0.7500 / 1.1820 — **곡선의 모양은 같고 배율축만 이동한다.**
최악값 19.19% / 21.11%은 두 화면에서 **동일**하고 발생 지점만 0.468 → 0.543으로 옮겨간다.)

**형태 언어 보증 구간을 이렇게 선언한다:**

| 구간 | 상태 |
|---|---|
| **0.646 ~ 1.00** | ✅ 사양 그대로. 22.29%, 팔<몸통<다리 셋 다 살아 있음 |
| 0.468 ~ 0.646 | ⚠ 허용. 비율 19.2~22.3%(링만 하한에 눌려 머리가 상대적으로 굵어진 구간) |
| 0.390 ~ 0.468 | ⚠ **팔=몸통** 병합. 무게중심 하강 의도 소실 시작 |
| 0.35 ~ 0.390 | ⚠ **팔=몸통=다리** 완전 평탄, 비율 24.09%. **실루엣 전용 구간** |

이 표는 새 제약이 아니라 **지금 코드가 이미 하고 있는 일의 기술**이다. 고칠 것은 없다.

### 9-3. ★ 새로 발견 — 링 두께가 **명목값 0.063이 아니라 0.0756501**로 굳어 있다

굽기 시점(배율 0.75)에 `0.063 × 0.75 = 0.04725 < F_bake = 0.0567376`이라 **하한에 눌려 구워졌고**,
런타임은 그 구워진 값에 `s/0.75`를 곱한다. 즉 **실효 링 계수 = 0.0567376 / 0.75 = 0.0756501**
(명목의 **120.1%**). 배율 1.0에서 링은 0.063이 아니라 0.07565로 그려진다.

이것은 사고가 아니라 **행운**이었다: 그 20% 부풀림이 없으면 몸통 비율은 22.29%가 아니라
`0.11495 / (0.44+0.063) = 22.85%`가 된다. 즉 **지금 22.3%에 정확히 닿는 이유의 절반은
굽기 시점 화면 근사 상수(846pt)다.** 그 상수를 누가 고치면 조형이 조용히 움직인다.

**제안 M4 (무위험·시각 무변화)** — 명목값을 실효값으로 정직하게 적는다.

```
Editor/SceneBootstrapper.cs
  BaselineHeadOutlineWidth : 0.063f  →  0.0756501f
```

검산: `0.0756501 × 0.75 = 0.0567376` = `F_bake`와 **정확히 같다** → 구워지는 프리팹의 링 폭은
**소수점까지 지금과 동일**하다(= 그림이 1pt도 안 변한다). 바뀌는 것은 "이 값이 화면 근사 상수에
의존한다"는 숨은 결합이 사라진다는 것뿐이다. **coder 배정 시 회귀 위험 0.**

---

## 10. 임무 ② 판정 — 액세서리 획이 낮은 배율에서 몸보다 굵다? → **그런 일은 일어나지 않는다**

### 10-1. 전제 반증 (구조적 증명)

몸 획과 액세서리 획은 **같은 하나의 하한**을 읽는다 — `StickmanAgent.MinStrokeWorldWidth`
(소비자: `CharacterAccessoryRenderer` / `CharacterPetRenderer` / `CharacterFxRenderer`, 전부 확인함).
따라서 둘 다 `max(k·s, F)` 꼴이고, `max`는 `k`에 대해 **단조 증가**다.

```
k_액세서리 = 0.048   <   k_팔 = 0.10450   <   k_몸통 = 0.11495   <   k_다리 = 0.12540
⇒ 모든 s, 모든 F에 대해  액세서리 획 ≤ 팔 획 ≤ 몸통 획 ≤ 다리 획
```

**액세서리 획이 몸 획보다 굵어지는 것은 배율·화면·DPI와 무관하게 불가능하다.** 등호는 둘 다
하한에 눌린 구간에서만 성립한다.

| 배율 | 액세 pt | 팔 pt | 몸통 pt | 다리 pt | 액세÷팔 | 액세÷몸통 | 액세÷다리 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| **0.35** | 2.00 | 2.00 | 2.00 | 2.00 | **1.000** | **1.000** | **1.000** |
| 0.40 | 2.00 | 2.00 | 2.00 | 2.05 | 1.000 | 1.000 | 0.974 |
| 0.468 | 2.00 | 2.00 | 2.20 | 2.40 | 1.000 | 0.909 | 0.833 |
| 0.60 | 2.00 | 2.57 | 2.82 | 3.08 | 0.780 | 0.709 | 0.650 |
| **0.75** | 2.00 | 3.21 | 3.53 | 3.85 | 0.624 | 0.567 | 0.520 |
| **1.00** | 2.00 | 4.28 | 4.70 | 5.13 | 0.468 | 0.425 | 0.390 |

최악은 **1.000(같다)**이지 1.34가 아니다.

### 10-2. 그러면 실제 결함은 무엇인가 — **굵기 역전이 아니라 위계 소실**

배율 0.468 미만에서 액세서리·팔·몸통이 한 값으로 뭉친다. 그림에서 사라지는 것은
"장비가 몸보다 얇다"는 정보이지 "장비가 몸보다 굵다"는 오류가 아니다.

**판정: 지금이 옳다.** 근거는 산술이다.

평탄한 하한을 버리고 "위계를 보존하는 하한"을 쓰려면 두 방식뿐인데 **둘 다 더 나쁘다**:

| 방식 | 배율 0.35에서의 결과 | 판정 |
|---|---|---|
| 가장 얇은 것(액세서리)을 2pt로 고정하고 나머지를 비례로 올린다 | 다리 = 2.00 × (0.1254/0.048) = **5.23 pt**. 전신 32.6pt에 획 5.2pt = 신장의 16% | ✗ 캐리커처가 된다 |
| 가장 굵은 것(다리)을 2pt로 고정하고 나머지를 비례로 내린다 | 액세서리 = 2.00 × (0.048/0.1254) = **0.77 pt** | ✗ 왕관·안대끈·목줄이 사라진다(하한을 만든 이유 그 자체) |

비율 폭이 `0.048 : 0.1254 = 1 : 2.6`인데 가장 얇은 쪽 하한이 2pt이므로,
위계를 보존하면 가장 굵은 쪽이 **반드시** 5.2pt가 된다. 전신 32.6pt에서 이는 불가능하다.
**평탄한 하한이 유일한 해다.** 액세서리 획을 건드릴 이유는 이 항목에는 없다
(굵히는 안이 EYES 6종을 깨뜨리는 문제는 `docs/ACCESSORY_STROKE_WEIGHT_VERDICT.md`가 이미 보류시킨 대로 유지).

### 10-3. ★ 다만 **하한이 진짜로 잘못 걸린 곳이 딱 하나 있다 — 머리 링**

2pt 하한의 정당성은 **"이 선은 보여야 한다"**이다. 그런데 머리 링은 **보이지 않아도 되는 선**이다:

- 링은 채움(`HeadFill`, 바깥 반경 R)과 **같은 잉크색**이다. 링이 하는 유일한 일은
  실루엣을 `R`에서 `R + W_ring/2`까지 **넓히는 것**이다(SceneBootstrapper 150~163행이 명시).
- 즉 링에 걸린 하한은 **획을 보이게 하는 것이 아니라 머리를 키운다.** 범주 오류다 —
  11절에서 `HeadFill`을 규칙 B에서 면제하는 것과 **정확히 같은 종류**의 오류다.

그 결과 **배율이 내려갈수록 캐릭터가 조용히 chibi화한다**:

| 배율 | 링 pt | 머리 잉크 반경(유닛) | 명목 대비 | **머리 개수**(신장÷머리 잉크 지름) |
|---:|---:|---:|---:|---:|
| ≥ 0.646 | 비례 | 0.2578·s | 100.0% | **4.485** |
| 0.60 | 2.00(하한) | 0.1564 | +6.6% | 4.440 |
| 0.50 | 2.00(하한) | 0.1344 | +13.9% | 4.321 |
| 0.40 | 2.00(하한) | 0.1124 | +27.5% | 4.155 |
| **0.35** | 2.00(하한) | **0.1014** | **+15.2%(반경) / +31.7%(면적)** | **4.045** |
| (하한 면제 시 전 배율) | — | 0.2515·s | — | **4.585** ← 팀 확정 4.57~4.61과 일치 |

**팀 확정 "우리 4.57~4.61 머리"는 명목 기하다. 실제로 그려지는 것은 4.485이고, 배율 0.35에서
4.045까지 내려간다.** 참고는 4.80~5.00이므로 이것은 **참고에서 멀어지는 방향**이다
(배정문이 금지한 "머리를 키우는" 변경이 하한을 통해 자동으로 일어나고 있었다).

**제안 M5 — 머리 실루엣을 하한에서 떼어낸다** (리더 판단 필요, 트레이드오프 있음)

```
Editor/SceneBootstrapper.BuildStickmanPrefab
  (a) HeadFill 의 바깥 반경 :  headVisualRadius
                            →  (BaselineHeadVisualRadius + BaselineHeadOutlineWidth * 0.5f) * bodyScale
                               = 0.2515 * bodyScale
  (b) HeadOutline(링)      :  폭을 하한에서 면제(= BaselineHeadOutlineWidth * bodyScale 그대로),
                               또는 헤어라인으로 낮춘다. 경로 반경 0.22*bodyScale 은 절대 건드리지 않는다.
```

- **계약 C1 무영향**: `StickmanMetrics.HeadRadius`는 링의 **첫 점 x**(= 경로 반경 0.22·s)를 읽는다.
  폭도 채움 반경도 읽지 않는다. 액세서리 28종 리그 / 초상화 액자 / 말풍선 위치 전부 무변화.
- `HeadFill` 자체 하한 검산: 폭 = 1.0909 × 0.2515 × 0.35 = **0.09603 유닛 > F_run 0.04888** →
  최소 배율에서도 하한에 안 걸린다(현행 여유 1.96배 → 개선 후 1.96배 유지).
- **얻는 것**: 머리 개수가 전 배율 **4.585 고정**. 획÷머리 비율도 배율 0.468 이상에서 **22.85% 고정**
  (지금은 19.19~22.29%로 흔들린다). 화면 근사 상수 의존이 사라진다.
- **잃는 것(정직하게)**: 배율 0.35에서 획÷머리 비율이 **24.09% → 27.76%**로 올라간다.
  그 배율의 머리 잉크 지름은 8.30pt → 7.21pt가 되고, 획 2pt 대비 머리가 4.15배 → 3.61배로 줄어
  **머리가 팔다리와 덜 구별된다.**
- **판정 보류 사유**: 위 트레이드오프는 산술로 못 정한다. **배율 0.35 실제 빌드 캡처 1장**이 필요하다.
  → **리더에게: "머리 하한 면제 A/B" 캡처 2장을 잡아 주면 그 자리에서 확정한다.**
  급하지 않다면 **현행 유지가 안전**하다(0.35는 이미 실루엣 전용 구간으로 선언돼 있다).

---

## 11. 임무 ③ 판정 — `HeadFill`의 규칙 B 예외는 **조형적으로 성립한다. 유지하라**

### 11-1. 예외는 튜닝 실패가 아니라 **수학적으로 불가피**하다 (증명)

`N`각형 경로(경로반경 r)를 폭 `W = k·r`로 그릴 때 규칙 B의 여유는

```
여유 = 최단 선분 / (W · tan(Δφ/2)) ,  Δφ = 2π/N
     = 2r·sin(π/N) / (k·r·tan(π/N))
     = 2·cos(π/N) / k
```

- 안쪽까지 채워지려면 `r − W/2 ≤ 0` ⟹ **`k ≥ 2`**.
- 규칙 B가 성립하려면 `여유 ≥ 1` ⟹ **`k ≤ 2·cos(π/N) < 2`**.

**두 조건은 동시에 성립할 수 없다. `N`을 아무리 키워도(무한대에서도) 불가능하다.**

| N | k=2.00 | k=2.05 | k=2.20 | **k=2.40(현행)** |
|---:|---:|---:|---:|---:|
| 24(현행) | 0.9914 | 0.9673 | 0.9013 | **0.8262** ← 보고된 0.826과 일치 |
| 48 | 0.9979 | 0.9735 | 0.9071 | 0.8315 |
| ∞ | 1.0000 | 0.9756 | 0.9091 | 0.8333 |

현행 0.8262는 `k=2.4`에서 이론 최대치(0.8333)의 **99.1%**다. **더 짜낼 여지가 없다.**
그리고 여유는 배율과 무관하다(채움 폭 `0.24·s`는 배율 0.204 미만에서만 하한에 걸리는데
`MinCharacterScale = 0.35`라 절대 도달하지 않는다) — **전 배율에서 정확히 0.8262 고정**이다.

### 11-2. 규칙 B가 막으려는 사고가 채움에서는 일어나지 않는다

규칙 B의 실제 계약은 **"안쪽 오프셋 윤곽이 자기교차해 실루엣에 크리즈/노치가 남는 것"**을 막는 것이다.

- 채움 도형에서 자기교차하는 것은 **안쪽 오프셋**이고, 그 영역은 **전부 잉크 안쪽**이다.
- 눈에 보이는 경계는 **바깥 오프셋**뿐인데, **볼록 다각형의 바깥 오프셋은 절대 자기교차하지 않는다.**
  (24각형은 볼록이다.) 따라서 **실루엣은 증명 가능하게 안전하다.**
- 24각 근사의 변 중앙 함몰은 `r(1−cos 7.5°) = 0.0086r = R의 0.39%` = 배율 0.75에서 **0.023pt** —
  링 두께 2.32pt 안에 완전히 묻힌다.

**판정: 예외는 조형적으로 성립한다. 배열에서 빼지 마라.** "채움 면은 획이 아니다"라는
현재 주석의 논리가 정확하다.

### 11-3. 다만 예외에 **조건 한 줄을 붙여라** (지금은 안전, 미래에 깨질 수 있다)

`k = 2.4`면 안쪽 가장자리가 중심을 `−0.2r` **지나친다.** 즉 중심 근처에서 24개 삼각형 띠가
서로 **겹쳐 그려진다.** 지금은 잉크가 완전 불투명(α=1)이라 겹침이 보이지 않는다.

`Core/StickmanAgent.cs:1013`에 **`TODO(Phase 2 렌더링 레이어): 즉시 on/off 대신 ≤200ms 페이드
아웃/인 연출 추가`**가 있다. **그 페이드가 들어오는 순간 α<1이 되고, 겹친 삼각형이 이중 합성되어
머리 한가운데에 24갈래 별/장미 무늬가 뜰 수 있다.**

- 이것이 실제로 보이는지는 **미확인**이다(LineRenderer가 겹침을 어떻게 삼각형화하는지 실기 확인 필요).
- 지금 고칠 것은 없다. 대신 예외 문구에 **전제**를 명시할 것을 권한다:
  > "이 예외는 **캐릭터 잉크가 완전 불투명(α = 1)일 때만** 성립한다. 몸 잉크에 알파 페이드를
  > 도입하는 라운드는 배율 0.35·α=0.5에서 머리 중심 캡처를 반드시 동반한다."
- 그때의 해법도 미리 적어 둔다: **`CreateFilledDisc`를 삼각형 팬 메시로 바꾸면** 겹침이 0이 되어
  `k` 제약 자체가 사라진다. 단 이 저장소의 "LineRenderer만 사용" 컨벤션을 깨므로 그 라운드의 판단 사항이다.

---

## 12. 임무 ④ 판정 — 펫 팔의 볼록 방향

### 12-1. 판정: **펫은 주인의 축소판이다. 부호 상속은 옳다. "의도된 교정"에 동의한다.**

근거는 취향이 아니라 이 저장소가 이미 코드로 선언해 둔 정의다. `CharacterPetRenderer`는
펫의 거의 모든 형태값을 **주인에게서 실측해 온다**:

| 값 | 출처 |
|---|---|
| 마디 분할비 `UpperFraction` | 주인 프리팹의 `LeftLeg`/`LeftArm` 선분 길이 실측 |
| 기본 굽힘각 `NeutralBendDegrees` | 주인 `StickConfig.idleKneeBendDegrees` / `idleElbowBendDegrees` |
| 보행 무릎 진폭 | 주인 키표에서 **비율**로 유도(주석: "미니어처는 비율이 같아야 한다") |
| 곡선 수식 | `LimbCurveRenderer`의 **같은 static 함수** |

이 정의 아래에서 `KneeBendSign` / `ElbowBendSign` 상속은 **부수 효과가 아니라 정의의 일부**다.

그리고 잃어버린 "넷이 다 진행 방향으로 휘던" 그림은 **조형 언어가 아니었다.**
그것은 `AppearanceShapeBuilder`의 **고정 활**(sagitta = 현의 9%)이었고 — **걸어도 굽지 않았다.**
해부학적 진술이 전혀 아닌 장식이었고, 그것이 3-5절이 지목한 "몸이 뚝딱거린다"의 정체다.
장식을 잃고 관절을 얻은 것이므로 **순이득**이다.

참고 이미지(8-3의 파랑 인물)에서도 무릎과 팔꿈치는 **서로 반대쪽으로** 접힌다. 참고와 일치한다.

**"별개 캐릭터인가"에 대한 답: 아니다.** 이름이 `리틀스틱메이트`이고 실루엣이 사람형 2족이다.
다른 조형 언어를 줘야 할 근거(사족·무기물·의인화되지 않은 종)가 하나도 없다.

> ★ **육안 최종 확인은 빌드 캡처 필요.** 지금 빌드는 낡았다(오늘 밤 팔다리 교정 전).
> 필요한 캡처: **배율 0.75, 펫 소환 상태, 보행 중 1장 + 무릎앉아 착지 1장.**
> 내가 판정을 뒤집을 조건은 하나뿐이다 — 팔꿈치와 무릎이 **같은 프레임에서 서로 상쇄돼 보여
> 펫이 "곧은 막대 4개"로 되돌아 보이는 경우**. 산술로는 그럴 이유가 없다(12-2의 돌출량 참고).

### 12-2. 클램프 판정: **받아들일 만하다. 단 배율 0.35에서 주인과 펫이 갈라지는 것은 사실이다.**

`BuildLimbPolylineBetween`은 **양 끝점을 고정**한다. 그래서 굽힘각 클램프가 바꾸는 것은
**관절이 현(chord)에서 옆으로 튀어나오는 양뿐이고, 펫의 도달거리·접지·무릎앉아 내림 거리는
1픽셀도 바뀌지 않는다**(`ResolveMiniCrouchDrop`은 벌림과 몸통 내림이 담당).

관절 돌출량 `h/C = ru·rl·sin θ / d²` (현 길이 C 대비):

| 관절 | 분할비 f | 원래 각 | 돌출 | 클램프 각 | 돌출 | 감소 |
|---|---:|---:|---:|---:|---:|---:|
| 무릎(무릎앉아) | 0.5263 | 126.0° | 0.968 C | **91.5°** | 0.510 C | **−47.3%** |
| 팔꿈치(텀블) | 0.5067 | 96.0° | 0.555 C | **78.8°** | 0.411 C | **−26.0%** |

- 배율 0.35에서 펫 전신은 화면상 ~14.7pt, 다리 현은 ~5~6pt다. 무릎 돌출 감소 0.46 C ≈ **2.5pt ≈ 획 1.25개**.
  **육안으로 겨우 보이는 양이다.**
- 대안(클램프를 풀어 126°를 그대로 그린다)은 **안쪽 윤곽 자기교차** = 2026-09-01 "발" 실패의
  (C2) 그 실패 모드다. 관절에 핀치/노치가 난다.
  **얕아진 무릎은 열화(degradation)이고, 노치는 결함(defect)이다. 열화가 낫다.**
- 클램프가 프레임마다 떨리지 않는지 확인함: `MaxBendDegrees`는 `PrepareMiniLimbs`에서 **한 번만**
  계산되고 현 길이·획에만 의존한다 → 안정적. 문제 없음.

**따라서 클램프 자체는 승인한다.** 다만 남는 비대칭 하나를 리더에게 보고한다:

> ★ **배율 0.35에서 주인은 무릎 126°를 그대로 접는데(오늘 밤 `FilletLengthRatio` 0.42 교정으로
> 최악 여유 1.046 확보) 펫만 91.5°에서 잘린다.** 즉 같은 무릎앉아에서 **펫의 무릎이 주인보다
> 눈에 띄게 덜 접힌다.** "미니어처는 비율이 같아야 한다"는 이 파일 자신의 원칙과 어긋난다.
>
> **원인은 배율이 아니라 구조다.** 주인의 위/아래 마디는 **별개 LineRenderer 2개**라 관절에
> 폴리라인 조인이 아예 없다(둥근 캡 두 개가 겹칠 뿐 — 4-7절). 펫은 **한 줄 폴리라인**이라
> 조인이 있고, 그래서 규칙 B에 걸린다.
>
> **해법은 이미 이 파일에 적혀 있다** — "발" 기록 (D) 두 번째 항목:
> *"발을 같은 폴리라인에 붙이지 말고 **독립 LineRenderer 2점 선**으로 그릴 것 — 코너 조인이
> 아예 없어져 (C2)가 구조적으로 사라진다."*
> **펫 마디를 관절에서 둘로 쪼개면** 펫도 주인과 같은 한계를 갖는다. 비용: 펫 하나당 GameObject +4.
> **우선순위는 낮다**(배율 0.35 + 무릎앉아 + 펫 소환이 동시에 성립할 때만 보인다). 리더 판단.

### 12-3. 발에 대하여 — **이 라운드에서 제안하지 않는다**

8-3에서 잰 참고 인물(파랑)에는 **발이 있다.** 그러나 `States/LimbCurveRenderer.cs`의 "발" 기록
(D)의 4개 조건 중 내가 이번 라운드에 만족시킬 수 있는 것이 **하나도 없다**:
① 길이를 `|bias|`로 줄이지 않는 대안 각도 제한 미설계, ② 독립 LineRenderer 전환 미설계,
③ 본체 팔다리용 "최단 선분 ≥ 획" 테스트 미배치, ④ 실제 빌드 캡처 미확보(빌드가 낡았다).
**따라서 발은 제안 대상이 아니다.** 사용자 지시("발을 넣으면서 이상해짐") 유효.

---

## 13. R2 우선순위 (리더 배정용)

| # | 항목 | 성격 | 위험 | 권고 |
|---|---|---|---|---|
| **M4** | `BaselineHeadOutlineWidth` 0.063 → **0.0756501** (9-3) | 정직성 · **그림 무변화** | **0** | coder 배정 권고 |
| **M5** | 머리 실루엣을 2pt 하한에서 분리 (10-3) | 조형 · 트레이드오프 있음 | 중 | **배율 0.35 A/B 캡처 후 확정.** 급하지 않으면 현행 유지 |
| **M6** | `HeadFill` 규칙 B 예외 문구에 **"α = 1일 때만"** 전제 추가 (11-3) | 문서 1줄 | 0 | 다음에 그 파일 만지는 사람이 함께 |
| **M7** | 펫 마디를 관절에서 **독립 LineRenderer 2개**로 분리 (12-2) | 주인/펫 한계 통일 | 중 | 우선순위 낮음. 리더 판단 |
| **M8** | `MacOverlayStateEnforcer` 진단 로그의 `× lossyScale` 제거 (9-1) | **로그만** 틀림 | 0 | 내 소관 아님 — 리더 판단 |
| — | 획 두께 `LineWidthScale = 1.045` | **검증 완료 · 그대로 두라** | — | 되돌리지 마라 |
| — | `HeadFill` 규칙 B 예외 | **판정: 유지** | — | 배열에서 빼지 마라 |
| — | 펫 무릎/팔꿈치 부호 상속 + 클램프 | **판정: 승인** | — | 최종 육안은 빌드 캡처 대기 |
| — | 액세서리 획 두께 | **판정: 손대지 마라** | — | 10-2 산술이 근거 |

## 14. R2 미확인 (성급히 단정하지 않는다)

1. **배정문 임무 2 표의 하한 0.08695 유닛의 출처를 못 찾았다.** 이 맥 실측(0.04888)도
   굽기 근사(0.05674)도 아니다. 다른 창 크기/다른 인스턴스일 수 있다. **리더 확인 요망** —
   만약 사용자 환경에서 실제로 그 값이 나온다면 9-2 표의 배율축이 통째로 이동한다
   (곡선 모양과 최악값 19.19% / 21.11%는 불변).
2. **α < 1에서 `HeadFill` 중심에 24갈래 무늬가 뜨는지 미확인**(11-3). 지금은 α=1이라 무해.
3. **펫 팔꿈치 방향의 육안 판정은 빌드 캡처 대기**(12-1). 산술로는 승인.
4. 캡처한 캐릭터가 **밀짚모자 + 망토 + 배낭**을 착용 중이라 머리 잉크 지름을 실측으로 못 쟀다
   (모자 챙이 머리 좌우를 덮는다). 머리 수치는 전부 소스 상수 유도값이다. 팔다리 획 5종은 실측이다.
5. 캡처 배율이 **0.60**이라 0.35/0.75/1.00을 직접 재지 못했다. 다만 0.60에서 예측식이
   ±2%로 맞았고 그 식은 배율에 대해 닫혀 있으므로 표는 신뢰할 만하다.
6. **어두운 배경만 확인했다**(사용자 바탕화면이 짙은 파랑). 밝은 배경에서의 가독성은 미확인 —
   잉크가 검정이라 밝은 배경이 유리한 방향이지만, 실측하지 않았다.

## 15. R2 플랫폼 영향

- **macOS 영향: 있음(이 라운드의 측정 기준 그 자체).** 9-1의 실측은 macOS Retina 2× / 3024×1964 /
  `orthographicSize` 12 / 40.9167 pt·유닛 환경이다. M4·M5는 `Editor/SceneBootstrapper.cs`
  (플랫폼 중립, 프리팹 굽기) 한 파일만 건드리므로 macOS 전용 코드는 하나도 없다.
- **Windows 영향: 없음(별도 배정 불필요).** 이 라운드가 언급한 파일은 전부 플랫폼 중립이다 —
  `Editor/SceneBootstrapper.cs`, `Core/StickmanStrokeWidths.cs`, `Core/StickConfig.cs`,
  `Core/StickmanAgent.cs`, `States/LimbCurveRenderer.cs`, `Interaction/CharacterPetRenderer.cs`,
  `Interaction/AccessoryShapeBuilder.cs`. `Platform/Windows/` 아래 파일은 한 개도 읽지도 언급하지도 않았다.
  **조형 판정 자체는 플랫폼 중립이다** — 형태·비율·곡률은 OS와 무관하다.
  단 하나 플랫폼 의존인 것은 **2pt 하한의 월드 환산**이고(8-2), 그래서 9-2 표의 **배율축**이
  Windows 100%/125%/150% 스케일링에서 이동한다. **비율 곡선의 모양과 최악값(19.19% / 21.11% / 24.09%)은
  이동하지 않는다** — 그 값들은 `k` 비(0.048 : 0.1045 : 0.11495 : 0.1254 : 0.063)에서만 나오기 때문이다.
  → 사용자 지시("윈도우는 미루고 맥만")대로 Windows 수정은 하지 않았고, **필요도 없다.**
  기존 7절이 권고한 `PlatformParityAuditTests` 항목("획 하한 환산이 플랫폼 중립인가")은 그대로 유효하다.
- **9-1의 진단 로그 결함(M8)만 `Platform/MacOS/MacOverlayStateEnforcer.cs`에 있다.** 프로덕션 그림에는
  영향이 없고 Windows에는 대응 코드가 없어 패리티 갭도 아니다. 내가 고치지 않았다(내 소관 아님).
